# Jour 71 — Réseau et web : DNS, TCP, TLS, HTTP/2 (culture solide)

> **Mois 3** · **Semaine 11** · Compétence : **HTTP / API** · Difficulté : Intermédiaire/5 · Durée : 4.5 h

[← Dashboard](../../) · [Semaine 11](../week-11.md) · [Mois 3](../month-03.md) · [Correction](../solutions/day-071-solution.md)

## 🎯 Objectif du jour
Comprendre la pile réseau sous HTTP pour répondre aux questions système d'entretien.

## 📖 Cours approfondi
**Concepts abordés :** DNS · TCP/handshake · TLS/HTTPS · HTTP/1.1 vs 2 · Latence vs bande passante.

Sous HTTP il y a une pile : DNS traduit le nom de domaine en adresse IP (annuaire distribué, avec caches à chaque étage) ; TCP établit une connexion fiable (le « handshake » en 3 temps, la retransmission des paquets perdus) ; TLS chiffre le canal (HTTPS = HTTP dans TLS) — il garantit la CONFIDENTIALITÉ (personne ne lit) et l'INTÉGRITÉ (personne ne modifie), via les certificats qui prouvent l'identité du serveur.

La distinction clé en performance : LATENCE (le temps d'un aller-retour — incompressible, dictée par la distance) vs BANDE PASSANTE (le débit). Chaque connexion coûte des allers-retours (DNS + TCP + TLS) avant le premier octet utile — d'où les connexions réutilisées et HTTP/2 qui multiplexe plusieurs requêtes sur une connexion.

« Que se passe-t-il quand tu tapes une URL ? » est LA question d'entretien système : ta réponse doit dérouler cette pile, dans l'ordre, en 3-5 minutes.

**Leçon(s) de fond à lire/relire :**
- [HTTP, REST et JSON](/doc/lessons/http-rest-json)
- [Concevoir une API](/doc/lessons/api-design-basics)

## ✍️ Pratique autonome
> **D'abord sans IA.** Tente seul au moins 30 minutes. Ne copie-colle jamais une réponse d'IA : lis, ferme, réécris de mémoire.

### Exercice principal
Trace (avec dig, curl -v, DevTools) le trajet complet d'une requête vers une vraie API. Documente chaque étape (DNS→TCP→TLS→HTTP) avec un schéma légendé. Réponds : pourquoi HTTPS, que protège-t-il ?

## 📦 Livrable attendu
Schéma légendé du trajet réseau + réponses aux questions système.

## ✅ Critères de validation
- [ ] Le trajet complet tracé avec de vrais outils (dig, curl -v) et documenté
- [ ] Schéma légendé DNS → TCP → TLS → HTTP correct
- [ ] Réponse orale de 3-5 min à « que se passe-t-il quand on tape une URL » (enregistrée)

## ⚠️ Erreurs fréquentes
- Confondre chiffrement (TLS) et authentification.
- Croire que HTTPS cache l'existence de la requête (le domaine fuite via SNI/DNS).

## 🔗 Ressources
- high-performance-browser-networking (livre gratuit en ligne, chapitres 1-4)

## 🤖 Consigne d'utilisation de l'IA
Construis le schéma seul : c'est une question d'entretien fréquente.

## 🏢 Cas métier
Cas typique : appeler un LLM (POST + header d'auth + corps JSON + streaming), exposer ton système RAG comme une API (`POST /questions`), ou consommer une API tierce — comprendre HTTP, c'est comprendre comment les systèmes IA se parlent.

## 🎤 Question d'entretien
**« Décris le trajet d'une requête HTTP et le rôle des statuts. »** → DNS → TCP → TLS → HTTP ; requête (méthode + URL + headers + corps) / réponse (statut + corps) ; sans état ; 2xx succès, 4xx faute client, 5xx faute serveur ; idempotence de GET/PUT/DELETE pour les retries.

## 🧠 À retenir
- Concepts clés du jour : DNS, TCP/handshake, TLS/HTTPS, HTTP/1.1 vs 2, Latence vs bande passante.
- Approfondis via la leçon de fond : HTTP, REST et JSON, Concevoir une API.
- Je dois pouvoir REFAIRE l'exercice seul demain et l'EXPLIQUER à l'oral — sinon ce n'est pas encore acquis.

## 🚀 Pourquoi ça comptera plus tard
- **En architecture** : toute intégration système passe par des APIs — comprendre HTTP en profondeur, c'est comprendre comment les systèmes se parlent.
- **En IA/LLM** : appeler un LLM, C'EST une requête HTTP (headers d'auth, corps JSON, streaming, statuts d'erreur) ; ton RAG est une API qui appelle des APIs.
- **En entretien** : « que se passe-t-il quand tu tapes une URL » et « design une API pour X » sont des classiques absolus.

## 🧩 Questions de réflexion (à faire seul)
- Qu'est-ce que je ne comprends pas encore parfaitement ?
- Comment j'expliquerais ce concept à l'oral, en entretien ?
- Où précisément le réutiliserai-je dans un projet IA/data/archi ?

➡️ **[Voir la correction](../solutions/day-071-solution.md)** — uniquement après avoir vraiment essayé.
