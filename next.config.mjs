/** @type {import('next').NextConfig} */
const nextConfig = {
  // App 100% locale : pas d'optimisation d'images distantes ni de télémétrie utile.
  reactStrictMode: true,
};

export default nextConfig;
