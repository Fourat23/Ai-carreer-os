# Jour 326 — DocSense : CI complète

> **Mois 11** · **Semaine 47** · Compétence : **Agents** · Difficulté : Intermédiaire/5 · Durée : 4.5 h

[← Dashboard](../../) · [Semaine 47](../week-47.md) · [Mois 11](../month-11.md) · [Correction](../solutions/day-326-solution.md)

## 🎯 Objectif du jour
Automatiser la qualité.

## 📖 Cours approfondi
**Modèle mental : la CI complète automatise la qualité de DocSense — lint, tests unitaires et éval smoke tournent à chaque push, de sorte qu'aucun changement cassant ne passe inaperçu.** Tu as posé une CI vide au jour 307 ; ici tu la RENDS complète : elle vérifie maintenant réellement le code (lint), la logique (tests unitaires) et même la qualité IA (éval smoke en mode replay/mock). Une CI complète est le filet de sécurité qui protège les dernières semaines de build — chaque commit est validé automatiquement, la régression est impossible à merger sans la voir.

Les trois niveaux de vérification de la CI :
1. **Le lint** : la qualité de forme du code (style, erreurs évidentes, imports inutilisés). Rapide, il maintient la cohérence et attrape les erreurs triviales avant qu'elles n'atteignent la revue.
2. **Les tests unitaires** : la logique du cœur (le RAG, les workflows, le refus). Grâce à l'architecture hexagonale (jour 310), le cœur se teste avec des stubs — rapide, déterministe, gratuit. La CI lance ces tests à chaque push : une régression de logique est attrapée immédiatement.
3. **L'éval smoke (en mode replay/mock)** : le point spécifique à l'IA. Une évaluation LÉGÈRE (quelques questions du golden set) tourne en CI, mais avec les appels LLM MOCKÉS ou REJOUÉS (jour 327) — pas de vrais appels (coûteux, lents, non déterministes) dans la CI. Le smoke test vérifie que le pipeline complet ne casse pas (l'ingestion tourne, le retrieval retourne des chunks, la génération produit un format valide), sans mesurer la qualité fine (qui se fait avec le harnais complet, hors CI).

Pourquoi la CI complète est le filet des dernières semaines : le mois 12 est intense (fonctionnalités, sécurité, polish) ; sans CI, une amélioration peut casser silencieusement le RAG core ou un workflow, et on le découvre trop tard. Avec une CI complète, chaque commit est vérifié sur les trois niveaux — impossible de merger du code qui casse le lint, les tests ou le pipeline. C'est le principe « rendre la qualité automatique » (jour 307) porté à maturité. Et une CI qui inclut une éval smoke IA est un signal de sophistication : peu de projets testent leur pipeline LLM en CI. « Ma CI lint, teste, et vérifie que le pipeline IA tourne, à chaque commit » démontre une culture de qualité de production rare sur un projet de portfolio.

**L'ingénierie de livraison, au-delà de « lint + tests » (fondations à maîtriser).**

**CI vs livraison continue vs déploiement continu.** L'INTÉGRATION continue (CI) fusionne et VÉRIFIE chaque changement (lint/tests/build) en continu. La LIVRAISON continue (CD) garde l'application TOUJOURS DÉPLOYABLE — un artefact validé est prêt à partir, mais un humain décide QUAND (une approbation). Le DÉPLOIEMENT continu va plus loin : chaque changement vert part AUTOMATIQUEMENT en production, sans intervention. Confondre les deux CD est l'erreur classique : la différence est l'approbation humaine.

**Anatomie d'un pipeline.** Un PIPELINE est une suite de STAGES (verify → package → deploy), chaque stage contenant des JOBS, chaque job des STEPS. Les jobs s'exécutent sur un RUNNER (agent). Ce vocabulaire structure toute chaîne de livraison, quel que soit l'outil (GitHub Actions, GitLab CI, Jenkins…).

**DAG, dépendances et parallélisme.** Les jobs forment un GRAPHE ORIENTÉ SANS CYCLE (DAG) via leurs dépendances (`needs`). Les jobs SANS dépendance entre eux tournent en PARALLÈLE (lint et tests en même temps) ; ceux qui dépendent d'autres attendent (build après lint ET tests : fan-in). Le CHEMIN CRITIQUE est la plus longue chaîne de dépendances : c'est lui qui fixe la durée minimale du pipeline. Un cycle de dépendances est une erreur de configuration refusée.

**Statuts et politiques d'échec.** Chaque job finit success/failed/skipped/cancelled/blocked. En FAIL-FAST (défaut), un échec dur annule les jobs restants — feedback rapide. « allow-failure » tolère l'échec d'un job non critique (ex. un lint indicatif) sans faire échouer le pipeline. Un job dont un PRÉREQUIS a échoué est BLOQUÉ (pas exécuté).

**Triggers et politiques de branche.** Un pipeline se déclenche sur un ÉVÉNEMENT : push, pull request, tag, manuel, planifié. Des FILTRES de branche/tag restreignent (ex. « déployer seulement sur un tag v* »). Une POLITIQUE DE BRANCHE (protection de la branche principale) exige que la CI soit verte avant de merger : le pipeline devient une QUALITY GATE.

**Artefacts et cache — à ne pas confondre.** Un ARTEFACT est un RÉSULTAT du pipeline (image, binaire, rapport) conservé avec une RÉTENTION, et promu d'un environnement à l'autre. Un CACHE accélère les builds en réutilisant un travail antérieur (dépendances installées) tant que sa CLÉ (souvent le hash du manifeste) n'a pas changé ; une mauvaise clé = cache jamais réutilisé (builds lents) ou périmé (résultats faux). L'artefact est un livrable ; le cache, une optimisation.

**Secrets et environnements (moindre privilège).** Les secrets (clés de déploiement) sont INJECTÉS par l'environnement et MASQUÉS dans les logs — jamais en clair, jamais dans le code, jamais dans un artefact. Chaque ENVIRONNEMENT (dev/staging/production) a ses secrets et ses règles ; on applique le MOINDRE PRIVILÈGE (le job de test n'a pas les clés de prod).

**Approbations, promotion et rollback.** La PROMOTION fait avancer le MÊME artefact validé de staging vers production (jamais un rebuild : on déploie ce qu'on a testé). Une APPROBATION MANUELLE peut bloquer avant la prod. Le ROLLBACK ramène rapidement la version précédente si la nouvelle casse — d'où l'intérêt de garder les artefacts précédents et des versions immuables.

**Stratégies de déploiement progressif.** BLUE-GREEN : deux environnements identiques ; on bascule le trafic d'un coup vers le nouveau (bleu → vert), rollback = rebascule instantanée. CANARY : on envoie un PETIT pourcentage du trafic vers la nouvelle version, on OBSERVE les métriques, puis on augmente progressivement — on limite le rayon d'impact d'un bug.

**Pipeline lent ou FLAKY, et dette de pipeline.** Un test FLAKY passe/échoue de façon non déterministe (dépendance au temps, à l'ordre, au réseau) : il détruit la confiance dans la CI (« relance, ça repassera ») et doit être corrigé ou isolé, jamais ignoré. Un pipeline LENT se diagnostique par le chemin critique (paralléliser, mettre en cache). La DETTE DE PIPELINE (étapes redondantes, cache mal réglé, secrets en dur, pas de rollback) se rembourse comme toute dette : par impact × risque.

**Observabilité post-livraison.** Livrer n'est pas fini : on SURVEILLE la version déployée (taux d'erreur, latence, logs) pour détecter une régression que la CI n'a pas vue, et déclencher un rollback si besoin. La supply-chain (provenance et intégrité des artefacts : digests, SBOM) garantit qu'on déploie bien CE qu'on a construit.

**Leçon(s) de fond à lire/relire :**
- [Agents IA : fondamentaux](/doc/lessons/agents-fundamentals)
- [Agents avancés et orchestration](/doc/lessons/agent-workflows-orchestration)
- [Sorties structurées et function calling](/doc/lessons/structured-outputs-tools)
- [Intégration continue (CI/CD)](/doc/lessons/ci-cd)

## 🧭 Exemple guidé
*Étudie ce pas-à-pas, puis FERME-le et attaque la pratique autonome de mémoire.*

**Énoncé** : la CI GitHub Actions complète de DocSense — lint + tests unitaires + éval smoke (replay/mock).

**Exemple simple** : l'éval smoke en une idée — lancer 3 questions du golden set avec les appels LLM MOCKÉS (réponses enregistrées), et vérifier que le pipeline produit un résultat au bon format. On teste que ça TOURNE, pas la qualité fine.

**Raisonnement pas-à-pas** : ajouter les trois niveaux au workflow CI, mocker les appels LLM pour l'éval smoke (rapide, déterministe, gratuit).

**Le workflow CI** :
```yaml
# .github/workflows/ci.yml — à chaque push :
jobs:
  qualite:
    - lint (ruff/flake8)                          # qualité de forme
    - tests unitaires (cœur avec stubs, j.310)    # logique, rapide/gratuit
    - éval smoke : 3 questions golden, appels LLM MOCKÉS (j.327)
        → vérifie que le pipeline TOURNE (ingestion→retrieval→génération, format OK)
        → PAS de vrais appels LLM (coûteux, lents, non déterministes) en CI
# La CI passe au ROUGE si lint, tests OU smoke échouent → régression bloquée
```

**Mini-quiz** : (a) Pourquoi mocker les appels LLM dans l'éval smoke de la CI ? (b) Que vérifie une éval smoke vs le harnais complet ? (c) Que garantit une CI complète pour les dernières semaines ?
→ (a) les vrais appels sont coûteux, lents et non déterministes — inadaptés à une CI qui tourne à chaque push ; (b) le smoke vérifie que le pipeline TOURNE (format valide), le harnais complet mesure la qualité fine (hors CI) ; (c) chaque commit est vérifié sur 3 niveaux — impossible de merger une régression sans la voir.

**Variante** : casse volontairement le pipeline (change un format de sortie) et pousse — l'éval smoke doit passer au ROUGE. Une CI qui ne détecte pas un pipeline cassé n'est pas un filet.

## ✍️ Pratique autonome
> **D'abord sans IA.** Tente seul au moins 30 minutes. Ne copie-colle jamais une réponse d'IA : lis, ferme, réécris de mémoire.

### Exercice principal
CI GitHub Actions : lint + tests unitaires + éval smoke (mode replay/mock).

## 📦 Livrable attendu
CI complète verte.

## ✅ Critères de validation
- [ ] Le livrable est produit et correspond à : CI complète verte.
- [ ] J'ai d'abord tenté seul (sans IA) au moins 30 minutes.
- [ ] Je peux expliquer chaque décision à l'oral, en 2 minutes.
- [ ] J'ai testé/vérifié le résultat, pas seulement supposé que « ça marche ».

## ⚠️ Erreurs fréquentes
- Vrais appels LLM en CI : coûteux, lents, non déterministes — mocker/rejouer pour l'éval smoke.
- Confondre éval smoke et évaluation de qualité : le smoke vérifie que ça TOURNE (format), le harnais complet mesure la qualité (hors CI).
- CI qui ne teste pas le pipeline IA : les régressions du pipeline passent inaperçues — inclure une éval smoke.
- CI qui ne passe jamais au rouge : vérifier qu'elle détecte un pipeline cassé, sinon ce n'est pas un filet.

## 🏢 Cas métier
Une CI complète qui inclut une vérification du pipeline IA est un standard de production rare sur les projets IA : la plupart testent le code classique mais laissent le pipeline LLM non testé (« c'est non déterministe, on ne peut pas tester »), et découvrent les régressions en production. Les équipes matures mockent/rejouent les appels LLM pour tester que le pipeline TOURNE (format, enchaînement) en CI, tout en gardant l'évaluation de qualité fine hors CI. Pour DocSense, une CI qui lint, teste et vérifie le pipeline à chaque commit protège les semaines intenses du mois 12 et signale une culture de qualité de production. « Ma CI teste même mon pipeline LLM, en mode mock » est un signal de sophistication qui distingue un projet de portfolio.

## 🎤 Question d'entretien
**« Comment testes-tu du code qui appelle un LLM en CI ? »** → Je mocke ou rejoue les appels LLM : les vrais appels sont coûteux, lents et non déterministes — inadaptés à une CI qui tourne à chaque push. Ma CI complète a trois niveaux : lint (forme), tests unitaires (logique du cœur, testable avec stubs grâce à l'hexagonal), et éval smoke (quelques questions du golden set avec appels LLM MOCKÉS, pour vérifier que le pipeline TOURNE — ingestion → retrieval → génération, format valide — sans mesurer la qualité fine qui se fait hors CI). Chaque commit est ainsi vérifié sur trois niveaux, impossible de merger une régression sans la voir. Peu de projets testent leur pipeline LLM en CI — c'est un signal de maturité.

## 🧠 À retenir
- Approfondis via la leçon de fond : Agents IA : fondamentaux, Agents avancés et orchestration, Sorties structurées et function calling.
- Je dois pouvoir REFAIRE l'exercice seul demain et l'EXPLIQUER à l'oral — sinon ce n'est pas encore acquis.

## 🚀 Pourquoi ça comptera plus tard
- **Bankable** : « ma CI teste même mon pipeline LLM en mode mock » est un signal de sophistication rare — peu de projets IA testent leur pipeline en CI.
- La CI complète protège les semaines intenses du mois 12 : chaque commit vérifié, régression bloquée.
- Le mock/replay des appels LLM (approfondi jour 327) est une compétence de test spécifique à l'IA, directement valorisée.

## 🧩 Questions de réflexion (à faire seul)
- Ta CI lance lint + tests + une éval « smoke » en mode replay : pourquoi cette éval de CI doit-elle rejouer des réponses enregistrées plutôt qu'appeler le vrai LLM à chaque exécution ?
- Ta CI est verte mais dure cinq minutes : que déplaces-tu hors du chemin critique de la CI pour garder un retour rapide, et qu'est-ce que tu gardes absolument ?
- Pourquoi une CI qui inclut une éval smoke attrape-t-elle une classe de régressions que le lint et les tests unitaires, seuls, laissent passer ?

➡️ **[Voir la correction](../solutions/day-326-solution.md)** — uniquement après avoir vraiment essayé.
