// Enrichissement Batch 5H — jours 61 à 90 (fin du mois 3 et trimestre 1 : Projet 2
// LivreAPI de bout en bout, sécurité web/OWASP, auth par token, refactoring, réseau,
// Linux/Git avancés, documentation, lecture de code, architecture — modularité/3-tiers/
// observabilité/cache/trade-offs, Python d'introduction, React et full-stack, bilans).
// Fusionné dans generate-curriculum.mjs (import + merge DAYS_ENRICH).
// Jours de travail : theory (rendu en theoryExtra, APRÈS la théorie des extras),
//   guided, quiz [{q,a}], caseStudy, interview, future,
//   solution {logic, simple, improved, pitfalls, checks, oral}, lessons (override).
// Jours de revue (63, 70, 77, 84) : reviewSynthese, reviewGrid, remediation, interview.
// Les titres, objectifs, exercices et livrables réels des jours 61-90 ne sont PAS modifiés.

export const ENRICH_61_90 = {
  // ────────────────────────────────────────────────────────────────────────
  // MOIS 3 · Semaine 9 (jours 57-63) : SQLite, Projet 2 LivreAPI (socle)
  // ────────────────────────────────────────────────────────────────────────

  // ── Projet 2 — LivreAPI : schéma et CRUD livres ──
  61: {
    lessons: ['express-backend.md', 'sql-foundations.md', 'api-design-basics.md'],
    theory: `**Modèle mental : on construit le socle d'une API dans un ORDRE précis, de bas en haut — le SCHÉMA d'abord (un \`init.sql\` exécutable qui reconstruit la base d'une commande), la couche DATA ensuite (toutes les requêtes paramétrées, isolées dans leur module), les ROUTES enfin (traduction HTTP pure). Chaque étage se teste AVANT de monter le suivant, si bien qu'un bug est toujours localisé à l'étage qu'on vient d'ajouter.** Ce n'est pas TaskFlow (jour 45, stockage JSON en mémoire) : c'est une vraie base relationnelle branchée derrière une structure en couches — le passage du prototype au socle crédible.\n\nPourquoi construire de bas en haut. Si l'on code les routes avant d'avoir un schéma et une couche data fiables, chaque bug peut venir de trois endroits à la fois (SQL, logique, HTTP) — impossible à isoler. En posant d'abord le schéma reconstructible, puis la couche data testée en isolation, puis les routes, chaque nouvel étage repose sur un socle DÉJÀ vérifié : le débogage reste local. C'est le « walking skeleton » (jour 45) appliqué à une base SQL.\n\nLe schéma reconstructible, depuis les fondations. \`init.sql\` contient les \`CREATE TABLE\` (avec clés primaires, types, contraintes \`NOT NULL\`, clés étrangères) et se rejoue d'une commande : n'importe qui — toi demain, un collègue, la CI — reconstruit une base identique de zéro. Un schéma versionné rend le projet REPRODUCTIBLE ; un schéma qui n'existe que « dans la base de mon poste » est irreproductible (« ça marche chez moi »).\n\nLe choix des statuts, dès maintenant. Les statuts HTTP (jour 50) se décident À LA CONCEPTION, pas après : 201 + l'objet créé (avec son id) sur un POST réussi, 200 + l'objet sur un GET, 204 sur un DELETE, 404 net sur un id inconnu, 400 détaillé sur une entrée invalide. Les corriger après coup CASSE les clients qui en dépendent ; les choisir bien d'emblée est gratuit. Chaque endpoint suit le même squelette : VALIDER l'entrée → INTERROGER (en paramétré) → répondre le STATUT correct.\n\nLa différence avec TaskFlow. TaskFlow persistait en JSON via une interface \`Store\` ; ici la couche data parle à SQLite en requêtes paramétrées (jour 57), et la structure routes/services/data (jour 53) est réelle. Le contrat conçu au jour 60 se concrétise : chaque endpoint promis est implémenté, testable via Postman.\n\nLimites et compromis. Un CRUD complet est répétitif (add/list/get/update/delete se ressemblent) : la tentation de dupliquer est réelle — factoriser la couche data aide, mais sur un projet d'apprentissage la clarté prime sur l'abstraction prématurée. Et un socle « qui marche » n'est pas « fini » : la sécurité (jours 67-68), les tests (jour 65) et la doc (jour 66) viendront le durcir.\n\nLa règle du jour : **construis le socle de bas en haut (schéma reconstructible → couche data paramétrée → routes), teste chaque étage avant le suivant, choisis les statuts à la conception (201/200/204/400/404), et fais que \`init.sql\` reconstruise la base d'une seule commande. Chaque endpoint : valider → interroger (paramétré) → statut correct.**`,
    guided: `**Énoncé** : implémenter l'endpoint \`POST /livres\` de bout en bout (schéma → couche data paramétrée → route), avec le bon statut.\n\n**Exemple simple** : le squelette d'un endpoint. Trois gestes invariables — VALIDER (l'entrée est-elle correcte ? sinon 400), INTERROGER (la couche data, en paramétré), RÉPONDRE (201 + l'objet créé avec son id). Tous les endpoints suivent ce moule.\n\n**Raisonnement pas-à-pas** : (1) le schéma définit la table \`livres\` (init.sql, rejouable) ; (2) la couche data expose \`inserer\`/\`tous\` en requêtes paramétrées ; (3) la route valide, appelle la couche data, renvoie 201 + l'objet ; (4) une entrée invalide → 400 avant tout accès base.\n\n**Implémentation** :\n\`\`\`sql\n-- init.sql : reconstruit la base d'une commande (sqlite3 livres.db < init.sql)\nCREATE TABLE IF NOT EXISTS livres (\n  id        INTEGER PRIMARY KEY AUTOINCREMENT,\n  titre     TEXT NOT NULL,\n  auteur_id INTEGER REFERENCES auteurs(id),\n  genre     TEXT,\n  disponible INTEGER NOT NULL DEFAULT 1\n);\n\`\`\`\n\`\`\`ts\n// COUCHE DATA : SQL isolé, requêtes PARAMÉTRÉES (jour 57)\nconst dataLivres = {\n  inserer: (titre: string, genre: string) =>\n    db.prepare('INSERT INTO livres (titre, genre) VALUES (?, ?)').run(titre, genre),\n  parId: (id: number) =>\n    db.prepare('SELECT * FROM livres WHERE id = ?').get(id),\n};\n\n// ROUTE : valider -> interroger -> statut correct\napp.post('/livres', (req, res) => {\n  const { titre, genre } = req.body;\n  if (typeof titre !== 'string' || !titre.trim())\n    return res.status(400).json({ erreurs: ['titre requis'] });   // 400 AVANT la base\n  const info = dataLivres.inserer(titre.trim(), genre ?? null);\n  const cree = dataLivres.parId(Number(info.lastInsertRowid));\n  res.status(201).json(cree);                                     // 201 + l'objet CRÉÉ (avec id)\n});\n\`\`\`\nLe schéma se rejoue d'une commande, la couche data isole le SQL paramétré, et la route suit valider → interroger → 201. Un GET sur un id inconnu renverra 404 ; une entrée invalide, 400 — décidés dès la conception.`,
    quiz: [
      { q: "Pourquoi construire l'API de bas en haut (schéma → data → routes) ?", a: "Pour que chaque étage repose sur un socle déjà vérifié : un bug est alors localisé à l'étage qu'on vient d'ajouter, au lieu de pouvoir venir du SQL, de la logique ou du HTTP à la fois." },
      { q: "Que doit garantir le fichier `init.sql` ?", a: "Que la base se reconstruit à l'identique d'une seule commande : le projet est reproductible par n'importe qui (toi demain, un collègue, la CI), contrairement à une base qui n'existe que sur ton poste." },
      { q: "Quel statut renvoyer sur un POST réussi, et pourquoi le choisir tôt ?", a: "201 + l'objet créé (avec son id). On choisit les statuts à la conception parce que les corriger après coup casse les clients qui en dépendent ; bien les choisir d'emblée est gratuit." },
      { q: "Quel est le squelette invariable de chaque endpoint ?", a: "Valider l'entrée (sinon 400 avant tout accès base) → interroger la couche data en paramétré → répondre le statut correct (201/200/204/404)." },
    ],
    caseStudy: `Construire une API sur une vraie base relationnelle, avec un schéma versionné et une structure en couches, est le pain quotidien du développement backend en entreprise. Le schéma reconstructible (init.sql, puis les migrations) est ce qui permet à une équipe de partager une base cohérente, de monter des environnements de test jetables, et à la CI de tourner sur une base propre — sans lui, chaque poste diverge et les bugs « ça marche chez moi » prolifèrent. Le choix rigoureux des statuts dès la conception est ce qui rend l'API consommable par les autres équipes (front, mobile) sans surprises. Un développeur qui livre un CRUD sur base avec statuts corrects, requêtes paramétrées et couche data isolée démontre exactement le socle attendu d'un profil back junior. LivreAPI, construit proprement, devient une pièce de portfolio que le recruteur peut cloner, reconstruire d'une commande et inspecter.`,
    interview: `**« Comment implémentes-tu proprement le socle CRUD d'une API sur base de données ? »** → Je construis de bas en haut. D'abord le SCHÉMA dans un \`init.sql\` versionné qui reconstruit la base d'une seule commande — c'est ce qui rend le projet reproductible par un collègue ou la CI. Ensuite la couche DATA : tout le SQL y est isolé et PARAMÉTRÉ (jamais de concaténation, pour éviter l'injection). Enfin les ROUTES, qui ne font que traduire le HTTP : chaque endpoint suit le même squelette — VALIDER l'entrée (sinon 400 avant tout accès base), INTERROGER la couche data, répondre le STATUT correct. Je choisis les statuts à la conception : 201 + l'objet créé avec son id sur un POST, 200 sur un GET, 204 sur un DELETE, 404 sur un id inconnu, 400 détaillé sur une entrée invalide — parce que les corriger après coup casse les clients. Chaque étage se teste avant de monter le suivant, donc un bug reste localisé. **Relance possible** : « pourquoi renvoyer l'objet créé et pas juste un 201 vide ? » → parce que le client a besoin de l'id généré par la base pour les opérations suivantes (le lire, le modifier, le supprimer) ; renvoyer l'objet complet lui évite un aller-retour et rend l'API plus pratique à consommer.`,
    future: `- **Bankable** : livrer un CRUD sur vraie base avec schéma versionné, statuts corrects et couche data paramétrée est exactement le socle attendu d'un profil back junior — inspectable et reconstructible par un recruteur.\n- Ce socle est la fondation directe des jours suivants du Projet 2 : relations et emprunts (62), recherche/pagination (64), tests (65), sécurité (67-68), durcissement (85).\n- L'ordre schéma → data → routes et le schéma reconstructible préparent les migrations, la CI et le déploiement des mois suivants.`,
    solution: {
      logic: "Concrétiser le contrat conçu au jour 60 en construisant de bas en haut : schéma reconstructible (init.sql), couche data isolée en requêtes paramétrées, routes qui suivent valider → interroger → statut correct. Choisir les statuts à la conception (201/200/204/400/404) et tester chaque étage avant le suivant. La preuve : init.sql reconstruit la base d'une commande et chaque endpoint répond le bon statut, testé via Postman.",
      simple: "CRUD livres qui fonctionne : POST/GET/PUT/DELETE branchés sur SQLite. L'API répond.",
      improved: "Isoler tout le SQL dans la couche data en paramétré, faire que init.sql reconstruise la base de zéro d'une commande, choisir des statuts corrects partout (201 + objet créé, 204 sur DELETE, 404 sur id inconnu, 400 détaillé), valider avant tout accès base, et tester chaque endpoint via Postman. Recycler la structure de l'API citations.",
      pitfalls: ["Statuts incorrects (200 pour une création ou une erreur) : casse les clients et détruit le langage des statuts.", "Validation oubliée sur POST/PUT : une entrée invalide atteint la base au lieu d'un 400 net.", "Schéma non reconstructible (base créée à la main) : projet irreproductible.", "SQL non paramétré ou éparpillé hors de la couche data : faille d'injection et migration difficile."],
      checks: ["CRUD livres complet sur SQLite, chaque endpoint testé via Postman.", "Statuts corrects partout (201/200/204/400/404).", "init.sql reconstruit la base de zéro en une commande.", "Toutes les requêtes sont paramétrées et isolées dans la couche data."],
      oral: "Explique l'ordre de construction : « schéma reconstructible d'abord, couche data paramétrée ensuite, routes enfin — chaque étage testé avant le suivant, donc un bug reste localisé ». Insiste sur les statuts choisis à la conception (201 + objet créé, 404, 400) et sur init.sql qui reconstruit la base d'une commande. Relier à l'API citations (recyclage) et au contrat du jour 60 montre que tu construis méthodiquement, pas au hasard.",
    },
  },

  // ── Projet 2 — LivreAPI : relations et logique d'emprunt ──
  62: {
    lessons: ['database-modeling.md', 'express-backend.md'],
    theory: `**Modèle mental : la LOGIQUE MÉTIER est ce qui distingue une vraie API d'un CRUD généré. « On ne peut emprunter qu'un livre DISPONIBLE » est une RÈGLE, et les règles vivent dans la couche SERVICE — pas dans les routes (qui ne font que traduire le HTTP), pas dans la base seule. Et comme l'emprunt est une opération à ÉTAPES MULTIPLES (vérifier la disponibilité → créer l'emprunt → marquer indisponible), c'est le cas d'usage exact des TRANSACTIONS : sans elle, une panne au milieu laisse un livre fantôme, ni vraiment emprunté ni vraiment disponible.** Implémenter l'emprunt, c'est placer la règle au bon endroit et garantir l'atomicité.\n\nPourquoi la règle métier vit dans le service. Si « pas de double emprunt » est écrit dans la route, elle est intestable sans serveur et se duplique à chaque endroit qui emprunte. Dans la base seule (une contrainte), elle est difficile à exprimer et à faire remonter proprement au client. Dans le SERVICE (jour 53), elle est testable en isolation, réutilisable, et le bon statut peut être renvoyé. La couche service est le siège des DÉCISIONS métier ; les routes traduisent, la data persiste.\n\nLa transaction pour une opération multi-étapes, depuis les fondations. Emprunter, c'est : (1) vérifier que le livre est disponible ; (2) créer la ligne d'emprunt ; (3) marquer le livre indisponible. Ces trois étapes doivent réussir ENSEMBLE ou aucune (atomicité, jour 58) : si une panne survient après (2) mais avant (3), on aurait un emprunt enregistré ET un livre encore marqué disponible — un état incohérent qui autoriserait un double emprunt. La transaction enveloppe les trois étapes ; en cas d'échec, un ROLLBACK annule tout.\n\nLe statut du conflit métier, une nuance à connaître. Tenter d'emprunter un livre déjà emprunté n'est ni une erreur de saisie (400) ni un bug serveur (500) : c'est un CONFLIT avec l'état actuel de la ressource → **409 Conflict**. Choisir 409 plutôt que 400 ou 500 est un signe de maîtrise des statuts : le client comprend que sa requête était bien formée mais que l'état ne le permet pas.\n\nLes JOIN entrent dans l'API. Ramener « un livre avec son auteur » ou « les emprunts d'un membre avec les titres » se fait par des JOIN (jour 55) dans la couche data : \`GET /livres/42\` peut renvoyer le livre ET le nom de son auteur en une requête, plutôt que de forcer le client à faire deux appels. Le JOIN assemble les données liées côté base, là où c'est efficace.\n\nLimites et compromis. Placer TOUTE la logique dans le service peut créer des services volumineux — on les découpe par domaine si besoin. Les transactions ont un coût (verrous) : on les garde ciblées sur les opérations réellement liées. Et exprimer une règle à la fois dans le service ET comme contrainte base (défense en profondeur) est parfois justifié, au prix d'une duplication assumée.\n\nLa règle du jour : **place la logique métier dans le SERVICE (testable, réutilisable, pas dans les routes), enveloppe les opérations multi-étapes (emprunt : vérifier → créer → marquer) dans une TRANSACTION pour l'atomicité, renvoie 409 sur un conflit métier (double emprunt), et utilise les JOIN pour ramener les données liées.**`,
    guided: `**Énoncé** : implémenter \`POST /livres/:id/emprunter\` avec la règle « livre disponible ? » dans le service, une transaction atomique, et le statut 409 en cas de conflit.\n\n**Exemple simple** : où vit la règle. La ROUTE reçoit la requête et renvoie le statut ; le SERVICE décide (« disponible ? sinon conflit ») ; la DATA exécute la transaction. La décision métier est dans le service, isolée et testable.\n\n**Raisonnement pas-à-pas** : (1) le service vérifie la disponibilité (règle métier) ; (2) si indisponible → il signale un conflit → la route renvoie 409 ; (3) sinon, une TRANSACTION crée l'emprunt ET marque le livre indisponible atomiquement ; (4) une panne au milieu → rollback, aucun état à moitié fait.\n\n**Implémentation** :\n\`\`\`ts\nclass ConflitMetier extends Error {}\n\n// COUCHE DATA : la transaction rend les étapes atomiques (jour 58)\nconst emprunterTx = db.transaction((livreId: number, membreId: number) => {\n  const livre = db.prepare('SELECT disponible FROM livres WHERE id = ?').get(livreId);\n  if (!livre) throw new Error('livre introuvable');\n  if (livre.disponible === 0) throw new ConflitMetier('livre déjà emprunté');   // règle\n  db.prepare('INSERT INTO emprunts (livre_id, membre_id, date_emprunt) VALUES (?, ?, ?)')\n    .run(livreId, membreId, new Date().toISOString());\n  db.prepare('UPDATE livres SET disponible = 0 WHERE id = ?').run(livreId);\n  // si une étape échoue -> ROLLBACK : jamais un emprunt sans livre marqué indisponible\n});\n\n// ROUTE : traduit le HTTP, mappe le conflit métier sur 409\napp.post('/livres/:id/emprunter', (req, res) => {\n  try {\n    emprunterTx(Number(req.params.id), req.body.membreId);\n    res.status(201).json({ message: 'emprunté' });\n  } catch (e) {\n    if (e instanceof ConflitMetier) return res.status(409).json({ message: e.message }); // 409 !\n    throw e;   // vrai bug -> middleware d'erreurs (500 générique)\n  }\n});\n\n// JOIN : ramener le livre AVEC son auteur en une requête\nconst livreAvecAuteur = (id: number) => db.prepare(\n  'SELECT l.*, a.nom AS auteur FROM livres l LEFT JOIN auteurs a ON a.id = l.auteur_id WHERE l.id = ?'\n).get(id);\n\`\`\`\nLa règle « disponible ? » vit dans la transaction du service, l'opération est atomique (rollback si échec), le double emprunt renvoie 409 (pas 400 ni 500), et le JOIN ramène l'auteur en une requête.`,
    quiz: [
      { q: "Où doit vivre la règle « pas de double emprunt », et pourquoi ?", a: "Dans la couche SERVICE : elle y est testable en isolation, réutilisable, et permet de renvoyer le bon statut. Dans une route elle serait intestable et dupliquée ; dans la base seule, difficile à exprimer et à remonter au client." },
      { q: "Pourquoi l'emprunt doit-il être une transaction ?", a: "C'est une opération multi-étapes (vérifier → créer l'emprunt → marquer indisponible) qui doit réussir ENTIÈREMENT ou pas du tout. Sans transaction, une panne au milieu laisse un état incohérent (emprunt créé mais livre encore disponible)." },
      { q: "Quel statut pour une tentative d'emprunt d'un livre déjà emprunté, et pourquoi ?", a: "409 Conflict : la requête est bien formée (donc pas 400) et ce n'est pas un bug serveur (donc pas 500) — c'est un conflit avec l'état actuel de la ressource. Le client comprend que l'état ne permet pas l'action." },
      { q: "À quoi sert un JOIN dans l'API ?", a: "À ramener les données liées en une seule requête (un livre AVEC le nom de son auteur), assemblées côté base où c'est efficace, plutôt que de forcer le client à faire plusieurs appels." },
    ],
    caseStudy: `La logique métier et l'intégrité transactionnelle sont le cœur de valeur de tout système d'entreprise sérieux — e-commerce, réservation, banque, gestion de stock. La règle « on ne vend pas un produit en rupture », « on ne réserve pas un siège déjà pris », « on ne débite pas sans créditer » est exactement l'analogue du double emprunt, et son absence cause des incidents coûteux (survente, double réservation, argent perdu). Placer ces règles dans une couche service testable, et envelopper les opérations liées dans des transactions atomiques, est ce qui distingue un système fiable d'un système qui « marche jusqu'à la première panne au mauvais moment ». Le choix du statut 409 pour un conflit métier est un détail qui signale la maîtrise. En entretien back, « où mets-tu la logique métier ? » et « comment garantis-tu qu'une opération à plusieurs étapes ne laisse pas d'état incohérent ? » sont des questions directes, et savoir répondre couche service + transaction + 409 démontre un vrai niveau.`,
    interview: `**« Comment implémentes-tu une opération métier comme "emprunter un livre" de façon fiable ? »** → Je sépare les responsabilités. La ROUTE ne fait que traduire le HTTP ; la RÈGLE métier (« on ne peut emprunter qu'un livre disponible ») vit dans la couche SERVICE, où elle est testable en isolation et réutilisable. Comme l'emprunt est une opération à plusieurs étapes — vérifier la disponibilité, créer la ligne d'emprunt, marquer le livre indisponible — je l'enveloppe dans une TRANSACTION : les trois réussissent ensemble ou un rollback annule tout. Sans ça, une panne entre la création de l'emprunt et le marquage laisserait un état incohérent, autorisant un double emprunt. Pour le conflit « livre déjà emprunté », je renvoie 409 Conflict — pas 400 (la requête est bien formée) ni 500 (ce n'est pas un bug) : c'est un conflit avec l'état de la ressource. Et je ramène les données liées (le livre avec son auteur) par un JOIN, en une requête. **Relance possible** : « et si tu voulais une défense supplémentaire contre le double emprunt ? » → j'ajouterais une contrainte au niveau base (par exemple une règle d'unicité sur les emprunts actifs) en défense en profondeur : la règle dans le service pour l'expérience et le bon statut, la contrainte base comme filet ultime contre une race condition.`,
    future: `- **Bankable** : « où mets-tu la logique métier ? » et « comment évites-tu un état incohérent sur une opération multi-étapes ? » sont des questions d'entretien back directes ; couche service + transaction + 409 est la réponse attendue.\n- La logique d'emprunt/retour est le cœur de LivreAPI et le pattern des règles métier (survente, réservation, débit/crédit) qu'on retrouve dans tout système transactionnel.\n- Les transactions atomiques et le placement des règles préparent les systèmes distribués et les questions de cohérence des mois avancés.`,
    solution: {
      logic: "Placer la règle métier (« livre disponible ? ») dans la couche service, envelopper l'opération multi-étapes (vérifier → créer l'emprunt → marquer indisponible) dans une transaction atomique, renvoyer 409 sur le conflit de double emprunt, et utiliser des JOIN pour ramener les données liées. La preuve : une interruption simulée ne laisse rien à moitié fait, et le double emprunt est refusé par un 409.",
      simple: "Endpoints emprunter/rendre qui fonctionnent, avec une vérification de disponibilité. Le cœur métier tourne.",
      improved: "Mettre la règle dans le service (pas la route), rendre l'emprunt TRANSACTIONNEL (rollback prouvé par interruption simulée), mapper le conflit sur 409 (pas 400/500), et ramener livre + auteur par JOIN. Éventuellement ajouter une contrainte base en défense en profondeur contre les races.",
      pitfalls: ["Emprunter un livre déjà emprunté (règle non vérifiée ou vérifiée hors transaction → race condition).", "Pas de transaction : une panne au milieu laisse un emprunt sans livre marqué indisponible (état incohérent).", "Mauvais statut : 400 ou 500 pour un double emprunt au lieu de 409 Conflict.", "Règle métier dans la route au lieu du service : intestable et dupliquée."],
      checks: ["Le double emprunt est refusé avec un 409 (testé).", "L'opération emprunt est transactionnelle (interruption simulée → rien n'est à moitié fait).", "La règle métier vit dans le service (vérifiable en lisant le code).", "Les JOIN ramènent les données liées (livre + auteur) en une requête."],
      oral: "Structure autour du placement et de l'atomicité : « la règle métier vit dans le service, testable ; l'emprunt est une transaction, atomique ; le conflit renvoie 409 ». Explique le scénario d'incohérence sans transaction (emprunt créé mais livre encore disponible) et le rollback qui l'évite. Mentionner la contrainte base en défense en profondeur contre les races montre que tu penses au-delà du chemin heureux — le niveau attendu sur une opération critique.",
    },
  },

  // ── Revue semaine 9 : SQLite, modélisation, Projet 2 (socle) ──
  63: {
    reviewSynthese: `La semaine 9 a fait passer d'une API en mémoire à une vraie application backend sur base de données. Le branchement SQLite (jour 57) : les requêtes PARAMÉTRÉES qui séparent code et données — LA défense contre l'injection — et la couche data isolée (l'interface Store appliquée à une vraie base). La modélisation (jour 58) : normaliser pour une source de vérité, indexer les colonnes filtrées/jointes souvent (compromis lecture/écriture), et les transactions ACID pour l'atomicité. Postman avancé (jour 59) : une collection = doc vivante + suite de tests + démo, qui vérifie statut ET forme. Le cadrage du Projet 2 (jour 60) : données d'abord (table de liaison pour les emprunts), contrat d'API avant le code. Puis la construction du socle de LivreAPI : schéma reconstructible → couche data paramétrée → routes, statuts choisis à la conception (jour 61) ; et la logique métier — règle dans le service, emprunt transactionnel, 409 sur conflit, JOIN pour les données liées (jour 62). Le fil rouge : **une vraie API backend se construit de bas en haut sur un modèle solide — sécurité (paramétrage), intégrité (normalisation, transactions), et logique métier au bon endroit (service).** LivreAPI a un socle crédible ; les semaines suivantes le durciront (recherche, tests, sécurité, doc).`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Brancher une base en requêtes paramétrées et prouver que l'injection échoue | |\n| Modéliser en 3NF, justifier un index, écrire une transaction atomique | |\n| Faire d'une collection Postman une doc vivante + suite de tests (statut ET forme) | |\n| Cadrer par les données (table de liaison) et écrire le contrat avant le code | |\n| Construire un CRUD sur base de bas en haut avec statuts corrects (schéma → data → routes) | |\n| Placer la logique métier dans le service, la rendre transactionnelle, renvoyer 409 sur conflit | |\n\n**Total /18** : ≥ 15 = socle backend solide, cap sur le durcissement · 11-14 = revoir les items ≤ 1 · < 11 = rejouer avant recherche/tests/sécurité, car LivreAPI est ta 2e pièce de portfolio.`,
    remediation: `- **SQL concaténé / injection possible** : rejoue le jour 57 — zéro concaténation, requêtes paramétrées partout, couche data isolée ; prouve l'échec de l'injection.\n- **Redondance / index injustifiés / pas de transaction** : jour 58 — normalise (une source de vérité), justifie chaque index, enveloppe les opérations liées dans une transaction.\n- **Postman superficiel** : jour 59 — teste la FORME du corps, pas juste le statut ; variables d'environnement ; scénario enchaîné.\n- **Modèle bancal / contrat après le code** : jour 60 — données d'abord (table de liaison pour les emprunts), contrat d'API avant l'implémentation.\n- **Socle désordonné / statuts flous** : jour 61 — schéma reconstructible → data paramétrée → routes, statuts choisis à la conception (201/200/204/400/404).\n- **Logique dans la route / pas d'atomicité / mauvais statut** : jour 62 — règle dans le service, emprunt transactionnel (rollback), 409 sur conflit.`,
    interview: `1. **« Comment te protèges-tu de l'injection SQL ? »** Attendu : requêtes paramétrées (séparation code/données), zéro concaténation, couche data isolée, preuve par tentative qui échoue.\n2. **« Explique normalisation, index et transaction. »** Attendu : une source de vérité (3NF), index = compromis lecture/écriture, transaction = atomicité ACID.\n3. **« Qu'est-ce qu'une bonne collection Postman ? »** Attendu : doc vivante + tests (statut ET forme) + démo ; variables ; scénario enchaîné.\n4. **« Comment cadres-tu un projet d'API ? »** Attendu : données d'abord (table de liaison), contrat avant le code, jalons démontrables.\n5. **« Où mets-tu la logique métier et comment garantis-tu l'atomicité ? »** Attendu : couche service, transaction pour les opérations multi-étapes, 409 sur conflit métier.`,
  },

  // ────────────────────────────────────────────────────────────────────────
  // MOIS 3 · Semaine 10 (jours 64-70) : LivreAPI (finition), sécurité, refactor
  // ────────────────────────────────────────────────────────────────────────

  // ── Projet 2 — LivreAPI : recherche, pagination, filtres ──
  64: {
    lessons: ['sql-foundations.md', 'api-design-basics.md'],
    theory: `**Modèle mental : recherche, pagination et filtres RAFFINENT une collection sans la changer — ils passent tous en query string sur un GET. La recherche se fait avec \`LIKE\` (le \`%\` se concatène à la VALEUR paramétrée, jamais au SQL). La pagination standard \`LIMIT\`/\`OFFSET\` cache un PIÈGE de performance : \`OFFSET 100000\` force le moteur à LIRE et JETER 100 000 lignes — sur gros volume, on pagine par CURSEUR (\`WHERE id > dernierIdVu LIMIT 20\`). Les filtres combinables reprennent le pattern « critère absent = ne contraint pas ».** Ajouter ces capacités, c'est rendre l'API utilisable à l'échelle tout en connaissant les limites de chaque technique.\n\nLa recherche avec LIKE, depuis les fondations. \`WHERE titre LIKE ?\` avec la valeur \`%terme%\` cherche « contient terme ». Deux points d'attention : la SENSIBILITÉ À LA CASSE dépend du moteur (SQLite est insensible pour l'ASCII par défaut sur \`LIKE\`, à documenter) ; et le \`%\` doit être ajouté à la VALEUR passée en paramètre (\`'%' + terme + '%'\`), PAS concaténé dans la chaîne SQL — sinon on rouvre la porte à l'injection (jour 57).\n\nLa pagination et son piège, depuis les fondations. \`LIMIT n OFFSET m\` renvoie n lignes après en avoir sauté m. Le problème : pour atteindre \`OFFSET 100000\`, le moteur doit PARCOURIR et jeter les 100 000 premières lignes à chaque requête — le coût croît avec le numéro de page. C'est acceptable sur de petits volumes, catastrophique sur de gros. La parade est la pagination par CURSEUR : au lieu d'un décalage, on garde le dernier id vu et on demande \`WHERE id > dernierIdVu ORDER BY id LIMIT 20\` — coût constant, car un index sur \`id\` permet de sauter directement. Connaître cette limite d'OFFSET est une question d'entretien classique.\n\nLes filtres combinables, depuis les fondations. Chaque filtre (\`?disponible=1&genre=sf\`) est une condition ; un filtre ABSENT ne doit rien contraindre (jour 46, côté SQL cette fois). On construit dynamiquement le \`WHERE\` en n'ajoutant que les conditions fournies, chacune paramétrée. Le piège falsy resurgit : \`disponible=0\` est un filtre légitime, il faut tester la PRÉSENCE du paramètre, pas sa véracité.\n\nLa performance et les index. Une recherche/un tri sur une colonne sans index force un parcours complet. Ajouter un index sur les colonnes RÉELLEMENT recherchées/triées (titre, auteur_id) accélère les lectures — mais chaque index a un coût en écriture (jour 58), donc on le justifie par une mesure, pas par réflexe.\n\nLimites et compromis. OFFSET est simple mais ne passe pas l'échelle (curseur pour les gros volumes, mais le curseur complique le « sauter à la page N »). LIKE \`%terme%\` avec joker en tête ne peut pas utiliser un index classique (recherche plein texte dédiée pour aller plus loin). Les filtres dynamiques doivent rester paramétrés — construire du SQL par concaténation pour « aller vite » rouvre l'injection.\n\nLa règle du jour : **recherche (\`LIKE\` avec le \`%\` sur la valeur paramétrée, casse documentée), pagination (\`LIMIT/OFFSET\` simple mais coûteux en profondeur → curseur sur gros volume), filtres combinables (critère absent = ne contraint pas, tester la présence), et index justifiés par une mesure. Tout raffine la collection en query string, sans jamais concaténer de SQL.**`,
    guided: `**Énoncé** : implémenter \`GET /livres?recherche=...&genre=...&page=...\` avec recherche LIKE paramétrée, filtres combinables et pagination — puis comprendre le piège d'OFFSET.\n\n**Exemple simple** : le piège d'OFFSET. \`LIMIT 20 OFFSET 20\` (page 2) est rapide ; \`LIMIT 20 OFFSET 100000\` (page 5000) force le moteur à lire et jeter 100 000 lignes. Même requête, coût très différent selon la profondeur — d'où le curseur sur gros volume.\n\n**Raisonnement pas-à-pas** : (1) je construis le WHERE dynamiquement, en n'ajoutant que les filtres FOURNIS, chacun paramétré ; (2) la recherche ajoute une condition LIKE avec le \`%\` sur la VALEUR ; (3) la pagination ajoute LIMIT/OFFSET ; (4) je note la limite d'OFFSET et l'alternative curseur.\n\n**Implémentation** :\n\`\`\`ts\nfunction chercherLivres(opts: { recherche?: string; genre?: string; page?: number }) {\n  const conditions: string[] = [];\n  const params: unknown[] = [];\n\n  if (opts.recherche !== undefined) {            // filtre PRÉSENT (pas de piège falsy)\n    conditions.push('titre LIKE ?');\n    params.push('%' + opts.recherche + '%');     // le % sur la VALEUR, jamais dans le SQL\n  }\n  if (opts.genre !== undefined) {\n    conditions.push('genre = ?');\n    params.push(opts.genre);\n  }\n  const where = conditions.length ? 'WHERE ' + conditions.join(' AND ') : '';\n\n  const taille = 20;\n  const offset = ((opts.page ?? 1) - 1) * taille;\n  const sql = \`SELECT * FROM livres \${where} ORDER BY id LIMIT ? OFFSET ?\`;\n  return db.prepare(sql).all(...params, taille, offset);\n}\n\n// PIÈGE OFFSET : à la page 5000, OFFSET 100000 lit et jette 100 000 lignes.\n// Alternative CURSEUR (coût constant grâce à l'index sur id) :\nfunction pageParCurseur(dernierIdVu: number) {\n  return db.prepare('SELECT * FROM livres WHERE id > ? ORDER BY id LIMIT 20').all(dernierIdVu);\n}\n\`\`\`\nLes filtres ne contraignent que s'ils sont fournis (paramétrés), la recherche met le \`%\` sur la valeur, et la pagination par curseur reste rapide en profondeur là où OFFSET s'écroule. Un index sur \`titre\` justifié par une mesure accélère la recherche.`,
    quiz: [
      { q: "Où doit-on ajouter le `%` d'un LIKE, et pourquoi ?", a: "Sur la VALEUR passée en paramètre (`'%' + terme + '%'`), jamais dans la chaîne SQL. Concaténer le terme dans le SQL rouvrirait la faille d'injection ; le paramètre le traite comme une donnée." },
      { q: "Quel est le piège de performance de LIMIT/OFFSET ?", a: "Pour atteindre `OFFSET 100000`, le moteur doit lire et jeter les 100 000 premières lignes : le coût croît avec le numéro de page. Sur gros volume, on pagine par curseur (`WHERE id > dernierIdVu LIMIT n`), à coût constant." },
      { q: "Comment un filtre absent doit-il se comporter, et quel piège éviter ?", a: "Un filtre absent ne doit rien contraindre (ne pas l'ajouter au WHERE). Piège falsy : `disponible=0` est un filtre légitime — tester la PRÉSENCE du paramètre (undefined), pas sa véracité." },
      { q: "Pourquoi ajouter un index sur les colonnes recherchées, et à quel prix ?", a: "Sans index, une recherche/tri force un parcours complet (lent). L'index accélère les lectures mais coûte en écriture (il faut le maintenir), donc on le justifie par une mesure, pas par réflexe." },
    ],
    caseStudy: `Recherche, pagination et filtres sont des fonctionnalités que TOUTE application à données non triviales doit fournir — liste de produits, résultats de recherche, historique, catalogue. Le piège d'OFFSET est un problème de performance très réel : de nombreuses applications ralentissent dramatiquement sur les pages profondes ou les grands ensembles, et la migration vers une pagination par curseur (utilisée par les grandes API comme celles de réseaux sociaux, sous forme de « cursor »/« next token ») est une optimisation classique en production. La recherche LIKE avec joker en tête qui ne peut pas utiliser d'index mène, à l'échelle, à adopter une recherche plein texte dédiée. Et construire des filtres dynamiques SANS concaténer de SQL (donc en paramétré) est une exigence de sécurité constante. Savoir implémenter ces capacités ET connaître leurs limites (OFFSET coûteux, LIKE non indexable, index à mesurer) démontre qu'on pense performance et sécurité, pas seulement fonctionnalité — exactement ce qu'un entretien back explore avec « comment paginerais-tu un million de lignes ? ».`,
    interview: `**« Comment implémentes-tu la pagination d'une API, et quelle est la limite de LIMIT/OFFSET ? »** → La pagination standard est \`LIMIT n OFFSET m\` : renvoyer n lignes après en avoir sauté m. Elle est simple et correcte sur de petits volumes, mais elle cache un piège : pour atteindre \`OFFSET 100000\`, le moteur doit PARCOURIR et jeter les 100 000 premières lignes à chaque requête — le coût croît avec la profondeur de la page. Sur de gros volumes, je passe à une pagination par CURSEUR : au lieu d'un décalage, je garde le dernier id vu et je demande \`WHERE id > dernierIdVu ORDER BY id LIMIT 20\` ; grâce à l'index sur \`id\`, le moteur saute directement, à coût constant. Le compromis du curseur : on perd le « sauter à la page N » arbitraire, mais on gagne la scalabilité (c'est ce qu'utilisent les grandes API, sous forme de next token). Pour la recherche, j'utilise \`LIKE\` en mettant le \`%\` sur la VALEUR paramétrée (jamais dans le SQL, sinon injection), et je documente la sensibilité à la casse selon le moteur. Les filtres combinables n'ajoutent au WHERE que les critères fournis, chacun paramétré, en testant leur présence pour éviter le piège falsy. **Relance possible** : « et la recherche \`LIKE '%terme%'\` à grande échelle ? » → le joker en tête empêche d'utiliser un index classique, donc à l'échelle je passerais à une recherche plein texte dédiée (index full-text).`,
    future: `- **Bankable** : « comment paginerais-tu un million de lignes ? » est une question d'entretien back classique ; connaître le piège d'OFFSET et la pagination par curseur démontre une pensée performance.\n- Recherche/pagination/filtres sont des capacités que toute application à données doit fournir — réutilisées dans le front de LivreAPI (jours 88-89) et tout projet ultérieur.\n- Le réflexe « raffiner en query string sans concaténer de SQL » et « justifier un index par une mesure » consolide sécurité et performance ensemble.`,
    solution: {
      logic: "Ajouter recherche (LIKE avec % sur la valeur paramétrée), pagination (LIMIT/OFFSET, en connaissant son piège de profondeur → curseur sur gros volume) et filtres combinables (critère absent = ne contraint pas, tester la présence contre le piège falsy), le tout en query string et sans jamais concaténer de SQL. Justifier les index par une mesure. La preuve : la pagination est testée en profondeur et la limite d'OFFSET est comprise.",
      simple: "Recherche par titre, pagination LIMIT/OFFSET et un ou deux filtres qui fonctionnent. La liste est exploitable.",
      improved: "Construire le WHERE dynamiquement en paramétré (filtres présents seulement, piège falsy évité), mettre le % de LIKE sur la valeur, documenter la sensibilité à la casse, tester la pagination (page 1, milieu, au-delà de la fin), EXPLIQUER la limite d'OFFSET et présenter l'alternative curseur, et ajouter un index justifié par une mesure avant/après.",
      pitfalls: ["Concaténer le terme de recherche dans le SQL (au lieu du % sur la valeur paramétrée) : faille d'injection.", "Ignorer le coût d'OFFSET en profondeur : l'API s'écroule sur les grandes pages/volumes.", "Piège falsy sur un filtre (disponible=0 confondu avec pas de filtre) : tester la présence, pas la véracité.", "Recherche/tri sans index sur gros volume : parcours complet lent."],
      checks: ["Recherche par titre/auteur fonctionnelle et insensible à la casse (choix documenté).", "Pagination testée (page 1, page du milieu, page au-delà de la fin).", "Filtres combinables corrects + un index justifié par une mesure.", "La limite d'OFFSET est comprise et l'alternative curseur est décrite."],
      oral: "Montre que tu connais les limites : « OFFSET est simple mais lit et jette les lignes sautées — coûteux en profondeur, donc curseur sur gros volume ». Explique le % sur la valeur paramétrée (sécurité), le filtre absent qui ne contraint pas (piège falsy) et l'index justifié par une mesure. Terminer par « LIKE avec joker en tête n'est pas indexable → recherche plein texte à l'échelle » prouve que tu vois au-delà du cas simple.",
    },
  },

  // ── Projet 2 — LivreAPI : tests d'intégration ──
  65: {
    lessons: ['testing-foundations.md', 'express-backend.md'],
    theory: `**Modèle mental : un test d'INTÉGRATION vérifie le système ASSEMBLÉ — une vraie requête HTTP traverse routes → services → base, et on vérifie la RÉPONSE ET l'ÉTAT de la base. Il est complémentaire du test unitaire (une fonction isolée, jour 47) : l'unitaire LOCALISE précisément les bugs de logique, l'intégration attrape les bugs de CÂBLAGE (mauvaise route, mauvaise colonne, transaction oubliée) qu'aucun test unitaire ne voit. La règle d'or : une base de TEST isolée, réinitialisée, JAMAIS la base de dev.** Tester l'API de bout en bout, c'est prouver que les pièces fonctionnent ENSEMBLE, pas seulement séparément.\n\nPourquoi l'intégration en plus de l'unitaire. Un test unitaire vérifie une fonction pure (la logique d'emprunt décide bien « indisponible → conflit »). Mais il ne voit pas si la route est bien branchée, si la requête SQL vise la bonne colonne, si la transaction est réellement appliquée. Ces bugs de CÂBLAGE n'apparaissent qu'en assemblant le système. Les deux niveaux sont complémentaires : l'unitaire est rapide et localise (« CETTE fonction est fausse »), l'intégration est plus lent mais réaliste (« le PARCOURS complet est faux »). La pyramide des tests : beaucoup d'unitaires, moins d'intégration.\n\nComment ça fonctionne, depuis les fondations. Un test d'intégration ENVOIE une vraie requête HTTP à l'API (via \`fetch\` sur un serveur de test, ou \`supertest\`), puis vérifie deux choses : la RÉPONSE (statut, corps) ET souvent l'ÉTAT de la base (la ligne a-t-elle bien été créée/modifiée ?). Il exerce le vrai chemin routes → services → data → SQL, exactement comme un client réel.\n\nLa base de test isolée, règle non négociable. Les tests s'exécutent sur une base de TEST séparée, RÉINITIALISÉE avant chaque suite (ou chaque test). Deux raisons : ne jamais POLLUER ni détruire la base de dev ; et rendre les tests REJOUABLES à l'infini et INDÉPENDANTS de l'ordre (un test ne doit pas dépendre des données laissées par un autre). Une base réinitialisée garantit un état de départ connu.\n\nLa différence unitaire vs intégration. L'UNITAIRE isole (une fonction, pas de base, pas de réseau) : rapide, précis sur la localisation, mais aveugle au câblage. L'INTÉGRATION assemble (HTTP + logique + base réelle de test) : plus lent, mais attrape les erreurs de branchement et prouve que le système fonctionne pour de vrai. On teste en priorité les RÈGLES MÉTIER (le double emprunt → 409) et les cas d'erreur, pas seulement les chemins heureux.\n\nLimites et compromis. Les tests d'intégration sont plus lents et plus fragiles (dépendent de la base, du montage) que les unitaires — d'où la pyramide : peu d'intégration, beaucoup d'unitaire. Réinitialiser la base a un coût (temps) mais garantit l'indépendance. Et tester TOUS les chemins d'intégration est irréaliste : on cible les parcours critiques et les règles métier.\n\nLa règle du jour : **écris des tests d'INTÉGRATION qui envoient de vraies requêtes HTTP et vérifient réponse ET état de la base, sur une base de TEST isolée et réinitialisée (jamais la base de dev). Ils attrapent les bugs de câblage que l'unitaire ne voit pas — cible les règles métier et les cas d'erreur, pas seulement les chemins heureux.**`,
    guided: `**Énoncé** : écrire un test d'intégration qui vérifie la règle du double emprunt (409) de bout en bout, sur une base de test réinitialisée.\n\n**Exemple simple** : ce qu'un test d'intégration voit et pas l'unitaire. L'unitaire vérifie que la fonction \`emprunter\` renvoie « conflit » si indisponible. L'intégration vérifie que \`POST /livres/1/emprunter\` répète deux fois renvoie 201 puis 409 — donc que la route, le service, la transaction et la base sont tous bien branchés.\n\n**Raisonnement pas-à-pas** : (1) j'utilise une base de TEST réinitialisée avant la suite ; (2) j'envoie une vraie requête HTTP ; (3) je vérifie le statut ET l'état résultant ; (4) je cible la règle métier (double emprunt → 409), pas juste le chemin heureux.\n\n**Implémentation (node:test + fetch)** :\n\`\`\`ts\nimport { test, before, beforeEach } from 'node:test';\nimport assert from 'node:assert/strict';\n\nbeforeEach(() => {\n  // base de TEST isolée, réinitialisée -> tests rejouables et indépendants de l'ordre\n  db.exec('DELETE FROM emprunts; DELETE FROM livres;');\n  db.prepare('INSERT INTO livres (id, titre, disponible) VALUES (1, ?, 1)').run('Test');\n});\n\ntest('emprunter deux fois le même livre : 201 puis 409', async () => {\n  const url = 'http://localhost:3000/livres/1/emprunter';\n  const body = { method: 'POST', headers: { 'Content-Type': 'application/json' },\n                 body: JSON.stringify({ membreId: 1 }) };\n\n  const r1 = await fetch(url, body);\n  assert.equal(r1.status, 201);                       // 1er emprunt réussit\n\n  const r2 = await fetch(url, body);\n  assert.equal(r2.status, 409);                       // 2e refusé : CONFLIT métier\n\n  // vérifier aussi l'ÉTAT de la base : le livre est bien indisponible\n  const livre = db.prepare('SELECT disponible FROM livres WHERE id = 1').get();\n  assert.equal(livre.disponible, 0);\n});\n\ntest('emprunter un livre inexistant : 404', async () => {\n  const r = await fetch('http://localhost:3000/livres/999/emprunter',\n    { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: '{\"membreId\":1}' });\n  assert.equal(r.status, 404);                          // cas d'erreur, pas juste le heureux\n});\n\`\`\`\nLe test exerce le vrai parcours HTTP → service → transaction → base, vérifie réponse ET état, cible la règle métier (409) et un cas d'erreur (404), et tourne sur une base réinitialisée — rejouable à l'infini.`,
    quiz: [
      { q: "Qu'est-ce qu'un test d'intégration vérifie, par rapport à un test unitaire ?", a: "Le système ASSEMBLÉ : une vraie requête HTTP traverse routes → services → base, et on vérifie réponse ET état de la base. L'unitaire isole une fonction ; l'intégration attrape les bugs de câblage (mauvaise route/colonne, transaction oubliée)." },
      { q: "Pourquoi une base de test isolée et réinitialisée est-elle obligatoire ?", a: "Pour ne jamais polluer/détruire la base de dev, et pour rendre les tests rejouables à l'infini et indépendants de l'ordre : une base réinitialisée garantit un état de départ connu." },
      { q: "Que teste-t-on en priorité dans une API ?", a: "Les RÈGLES MÉTIER (le double emprunt → 409) et les cas d'erreur (404, 400), pas seulement les chemins heureux — c'est là que se cachent les bugs qui comptent." },
      { q: "Pourquoi la pyramide des tests recommande-t-elle plus d'unitaires que d'intégration ?", a: "Les tests d'intégration sont plus lents et plus fragiles (base, montage) ; les unitaires sont rapides et localisent précisément. Beaucoup d'unitaires pour la logique, moins d'intégration pour les parcours critiques." },
    ],
    caseStudy: `Les tests d'intégration sont ce qui donne confiance pour livrer et modifier une API en entreprise. Ils attrapent une classe de bugs invisibles aux tests unitaires : une route mal branchée, une requête qui vise la mauvaise colonne, une transaction oubliée, un statut incorrect — des erreurs de CÂBLAGE qui passent tous les tests de logique mais cassent le système réel. Sur une base modifiée par plusieurs développeurs, une suite d'intégration verte est le filet qui permet de refactorer et d'ajouter des fonctionnalités sans casser l'existant en silence. La règle de la base de test isolée est fondamentale : des tests qui polluent la base de dev, ou qui dépendent de l'ordre d'exécution, sont pires que pas de tests (faux positifs, flakiness). Les équipes matures exécutent la suite d'intégration en CI à chaque push. Savoir écrire des tests d'intégration qui vérifient réponse ET état, sur une base réinitialisée, en ciblant les règles métier, démontre une culture de qualité directement valorisée — et « comment testes-tu une API ? » est une question d'entretien récurrente.`,
    interview: `**« Quelle est la différence entre un test unitaire et un test d'intégration, et lequel écris-tu pour une API ? »** → Un test UNITAIRE isole une fonction (la logique d'emprunt décide-t-elle bien « indisponible → conflit ? ») : rapide, sans base ni réseau, il localise précisément un bug de logique. Un test d'INTÉGRATION vérifie le système ASSEMBLÉ : il envoie une vraie requête HTTP qui traverse routes → services → base, et vérifie la RÉPONSE ET l'ÉTAT de la base. Il attrape les bugs de CÂBLAGE qu'aucun unitaire ne voit : route mal branchée, requête sur la mauvaise colonne, transaction oubliée, mauvais statut. Pour une API, j'écris les deux, selon la pyramide : beaucoup d'unitaires pour la logique, moins d'intégration pour les parcours critiques et les règles métier — par exemple emprunter deux fois le même livre doit renvoyer 201 puis 409, et je vérifie aussi que le livre est bien devenu indisponible en base. Règle non négociable : les tests tournent sur une base de TEST isolée, réinitialisée avant chaque suite, jamais la base de dev — pour qu'ils soient rejouables à l'infini et indépendants de l'ordre. Je cible les règles métier et les cas d'erreur, pas seulement les chemins heureux. **Relance possible** : « pourquoi ne pas tout tester en intégration ? » → parce que c'est plus lent et plus fragile ; l'unitaire localise mieux et coûte moins — d'où la pyramide.`,
    future: `- **Bankable** : « comment testes-tu une API ? » est une question d'entretien récurrente ; distinguer unitaire (logique) et intégration (câblage) et parler de base de test isolée démontre une culture de qualité.\n- Une suite d'intégration verte est le filet qui autorise le refactoring (jour 69) et le durcissement (jour 85) sans casser l'existant.\n- Tester sur base isolée et cibler les règles métier prépare la CI (mois 11) où ces tests s'exécutent à chaque push.`,
    solution: {
      logic: "Écrire des tests d'intégration qui envoient de vraies requêtes HTTP et vérifient réponse ET état de la base, sur une base de TEST isolée et réinitialisée. Cibler les règles métier (double emprunt → 409) et les cas d'erreur (404, 400), pas seulement les chemins heureux. La preuve : la suite passe deux fois de suite sans nettoyage manuel (rejouable, indépendante de l'ordre).",
      simple: "Quelques tests qui appellent les endpoints et vérifient le statut. On couvre les cas principaux.",
      improved: "Utiliser une base de TEST isolée réinitialisée avant chaque suite, vérifier la réponse ET l'état de la base, couvrir les règles d'emprunt (dont le 409) et les cas d'erreur (404, 400), et garantir que les tests passent deux fois de suite sans nettoyage manuel. Distinguer explicitement des tests unitaires du Projet 1.",
      pitfalls: ["Tester sur la base de dev : pollution et destruction de données réelles.", "Ne tester que les chemins heureux : les bugs se cachent dans les règles métier et les cas d'erreur.", "Tests dépendants de l'ordre ou d'un état laissé par un autre : faux positifs, flakiness.", "Vérifier seulement la réponse HTTP sans l'état de la base : on rate une transaction non appliquée."],
      checks: ["Suite d'intégration verte sur base de test isolée et réinitialisée.", "Les règles d'emprunt testées (dont le 409).", "Les tests passent deux fois de suite sans nettoyage manuel (rejouables).", "Réponse ET état de la base vérifiés sur au moins un test."],
      oral: "Oppose les deux niveaux : « l'unitaire isole la logique et localise ; l'intégration assemble et attrape le câblage — route, colonne, transaction ». Insiste sur la base de test isolée et réinitialisée (rejouable, indépendante de l'ordre) et sur le ciblage des règles métier (double emprunt → 409) et cas d'erreur. Mentionner la pyramide des tests et l'exécution en CI montre que tu situes les tests dans une vraie pratique d'équipe.",
    },
  },

  // ── Projet 2 — LivreAPI : documentation, README, ADR, démo ──
  66: {
    lessons: ['readme-documentation.md', 'technical-storytelling.md'],
    theory: `**Modèle mental : la documentation d'une API a un critère UNIQUE — un inconnu peut-il la tester en 5 minutes ? Le README répond dans l'ORDRE aux questions du lecteur : c'est quoi → comment lancer → comment tester (importer la collection) → comment c'est construit (architecture) → quelles limites. La collection Postman EST la doc vivante (clone → import → lance → tout passe), et la démo enregistrée (2 min, écran + voix) est un MULTIPLICATEUR : elle prouve que ça marche sans rien installer, et elle t'entraîne à PRÉSENTER.** Finaliser LivreAPI, c'est le rendre utilisable et compréhensible par quelqu'un qui n'a pas ta tête.\n\nPourquoi la doc décide de la visibilité d'un projet. Un projet dont personne ne sait l'installer, l'utiliser ni comprendre l'architecture est INVISIBLE — le meilleur code reste inexploité. La documentation n'est pas un supplément : c'est ce qui transforme un tas de code en projet UTILISABLE par d'autres (recruteur, collègue, toi dans six mois). Le critère opérationnel « un inconnu opérationnel en 5 minutes » rend la qualité de la doc MESURABLE : il suffit de tester ses propres instructions sur un clone frais.\n\nLe README, dans l'ordre du lecteur. Le lecteur arrive avec des questions, dans un ordre précis : QU'EST-CE que c'est (une phrase claire) → COMMENT LE LANCER (commandes exactes, prérequis) → COMMENT LE TESTER (importer la collection Postman, exemples d'appels) → COMMENT C'EST CONSTRUIT (l'architecture en couches, le schéma) → QUELLES LIMITES (ce que ça ne fait pas, la dette assumée). Répondre dans CET ordre, c'est écrire pour le lecteur, pas pour soi.\n\nLa collection Postman comme doc vivante. Contrairement à une doc figée qui se périme, une collection exportée et versionnée est EXÉCUTABLE : on la clone, on l'importe, on lance — et tout passe. Elle documente l'usage réel (URLs, corps, headers) et prouve que l'API fonctionne, en un geste. C'est le prolongement direct du jour 59.\n\nLa démo enregistrée, un multiplicateur. Une démo de 2 minutes (partage d'écran + voix) prouve que ça marche SANS que le lecteur installe quoi que ce soit, et — bénéfice caché — elle t'ENTRAÎNE à présenter ton travail, la compétence du mois 12 commencée dès maintenant. La méthode : un scénario écrit, une répétition, une prise propre.\n\nL'ADR n°2, un arbitrage réel. Un Architecture Decision Record capture une décision structurante : ici « validation à la main vs librairie » (jour 54). Il documente le contexte, les options, la décision, ses conséquences, et le POINT DE BASCULE (ce qui ferait passer à une librairie de validation). L'ADR prouve, comme au jour 47, que tu arbitres consciemment.\n\nLimites et compromis. Un README trop long dilue l'essentiel, trop court n'aide pas — viser l'utilisabilité en 5 minutes calibre la longueur. Une démo se répète (mais pas dix fois : une prise correcte suffit). Documenter chaque détail est contre-productif ; on documente ce qui aide le lecteur à démarrer et à comprendre les décisions.\n\nLa règle du jour : **vise le critère « un inconnu teste l'API en 5 minutes » : README dans l'ordre du lecteur (quoi → lancer → tester → construit → limites), collection Postman comme doc vivante et exécutable, démo 2 min enregistrée (preuve + entraînement à présenter), et un ADR qui trace une décision structurante. Teste ta doc sur un clone frais.**`,
    guided: `**Énoncé** : structurer le README de LivreAPI dans l'ordre du lecteur et vérifier le critère « opérationnel en 5 minutes » sur un clone frais.\n\n**Exemple simple** : le test décisif. Clone ton propre dépôt dans un dossier vierge et suis TES instructions à la lettre. Si tu bloques (une étape manque, un prérequis non dit), un recruteur bloquera aussi — c'est le critère « 5 minutes » qui échoue.\n\n**Raisonnement pas-à-pas** : (1) j'écris le README dans l'ordre des questions du lecteur ; (2) j'exporte la collection Postman (doc vivante) ; (3) j'enregistre une démo de 2 min ; (4) je teste tout sur un clone frais.\n\n**Structure (README.md)** :\n\`\`\`md\n# LivreAPI — API REST de gestion d'une bibliothèque\nUne API de prêt de livres (CRUD, emprunts, recherche), sur Node/Express + SQLite.\n\n## Lancer (prérequis : Node 20+)\n\\\`\\\`\\\`bash\nnpm install\nsqlite3 livres.db < init.sql     # reconstruit la base d'une commande\ncp .env.example .env             # renseigner API_TOKEN\nnpm start                        # http://localhost:3000\n\\\`\\\`\\\`\n\n## Tester en 2 minutes\nImporter \\\`postman/LivreAPI.json\\\` dans Postman -> \"Run collection\" : tout passe.\n\n## Architecture\nroutes -> services (logique métier) -> data (SQL paramétré). Schéma : voir \\\`init.sql\\\`.\nDécisions : voir \\\`docs/adr/\\\`.\n\n## Limites connues\nPagination par OFFSET (curseur prévu si volume) ; auth par token simple (jours 67-68).\n\`\`\`\n\`\`\`md\n# docs/adr/002-validation-manuelle-vs-librairie.md\n## Contexte : valider les entrées de LivreAPI.\n## Options : (a) validation à la main (zéro dépendance, explicite) ;\n##           (b) librairie (Zod/Joi : concise, schémas réutilisables).\n## Décision : validation à la main pour la v1 (apprentissage, peu de champs).\n## Point de bascule : passer à une librairie si le nombre de schémas explose.\n\`\`\`\nLe README répond dans l'ordre du lecteur, la collection prouve l'API en un clic, l'ADR trace la décision, et le test sur clone frais valide le critère « 5 minutes ».`,
    quiz: [
      { q: "Quel est le critère unique d'une bonne documentation d'API ?", a: "Un inconnu peut-il la tester en 5 minutes ? Ce critère rend la qualité mesurable : il suffit de suivre ses propres instructions sur un clone frais." },
      { q: "Dans quel ordre un README doit-il répondre aux questions du lecteur ?", a: "C'est quoi → comment lancer → comment tester (importer la collection) → comment c'est construit (architecture) → quelles limites. L'ordre des questions du lecteur, pas celui de l'auteur." },
      { q: "Pourquoi une collection Postman est-elle une meilleure doc qu'un texte figé ?", a: "Elle est EXÉCUTABLE : on la clone, l'importe, la lance et tout passe. Elle documente l'usage réel et prouve que l'API marche, contrairement à un texte figé qui se périme." },
      { q: "Quel double bénéfice apporte une démo enregistrée de 2 minutes ?", a: "Elle prouve que ça marche sans rien installer pour le lecteur, ET elle t'entraîne à PRÉSENTER ton travail — la compétence de communication commencée dès maintenant." },
    ],
    caseStudy: `La documentation est ce qui détermine si un projet est ADOPTÉ ou ignoré, en entreprise comme en open-source. Un service interne sans README utilisable coûte du temps à chaque nouvel arrivant et à chaque équipe qui veut l'intégrer ; un projet open-source sans documentation claire n'a pas d'utilisateurs, quel que soit son code. Le critère « opérationnel en 5 minutes » est concret : les projets qui le respectent sont adoptés, les autres non. La collection Postman comme doc vivante et exécutable est une pratique standard pour les API (avec OpenAPI/Swagger), car elle ne peut pas se périmer sans qu'on le remarque. Pour un portfolio, la documentation est décisive : un recruteur qui clone LivreAPI juge d'abord le README (peut-il lancer et tester ?), et une démo de 2 minutes lui prouve le résultat sans installation. Savoir documenter pour un lecteur précis, et s'entraîner à présenter via une démo, sont des compétences de communication technique directement évaluées — et la présentation orale d'un projet est un exercice d'entretien fréquent.`,
    interview: `**« Comment documentes-tu un projet pour qu'il soit vraiment utilisable par quelqu'un d'autre ? »** → Je vise un critère unique et mesurable : un inconnu peut-il tester l'API en 5 minutes ? Pour ça, mon README répond dans l'ORDRE des questions du lecteur — c'est quoi, comment le lancer (commandes exactes, prérequis), comment le tester (importer la collection Postman), comment c'est construit (l'architecture en couches), et quelles sont les limites connues. J'écris pour le lecteur, pas pour moi. La collection Postman est ma doc VIVANTE : exécutable, versionnée, elle prouve que l'API marche en un clic et ne se périme pas sans qu'on le voie. J'ajoute une démo de 2 minutes (écran + voix) qui prouve le résultat sans que le lecteur installe quoi que ce soit — et qui m'entraîne à présenter. Et je trace les décisions structurantes dans des ADR, par exemple « validation à la main vs librairie », avec le point de bascule. Le test décisif : je clone mon propre dépôt dans un dossier vierge et je suis mes instructions ; si je bloque, un recruteur bloquera. **Relance possible** : « qu'est-ce qui distingue un bon README d'un mauvais ? » → un bon README répond aux questions du lecteur dans son ordre et est testé sur un clone frais ; un mauvais décrit le projet dans l'ordre de l'auteur, suppose un contexte absent, et n'a jamais été vérifié par un tiers.`,
    future: `- **Bankable** : la documentation décide de la visibilité d'un projet en portfolio ; un recruteur juge d'abord le README, et une démo de 2 min prouve le résultat sans installation.\n- Le critère « opérationnel en 5 minutes » et la collection comme doc vivante sont des pratiques standard pour les API (OpenAPI/Swagger) en entreprise.\n- La démo enregistrée entraîne la présentation orale — compétence du mois 12 et exercice d'entretien fréquent (« présente-moi un projet »).`,
    solution: {
      logic: "Finaliser LivreAPI pour l'utilisabilité : README dans l'ordre du lecteur (quoi → lancer → tester → construit → limites), collection Postman exportée comme doc vivante et exécutable, démo 2 min enregistrée, et ADR n°2 (validation manuelle vs librairie) avec point de bascule. Le critère de réussite mesurable : suivre ses propres instructions sur un clone frais et être opérationnel en 5 minutes.",
      simple: "Un README avec installation et usage, plus la collection exportée. Le projet est présentable.",
      improved: "Structurer le README dans l'ordre des questions du lecteur et le TESTER sur un clone frais (opérationnel en 5 min), exporter/versionner la collection Postman, enregistrer une démo de 2 min (scénario écrit, une répétition, une prise), et écrire l'ADR n°2 avec le point de bascule explicite. Documenter les limites connues honnêtement.",
      pitfalls: ["Collection non versionnée : la doc vivante disparaît avec le poste de l'auteur.", "README qui suppose un contexte absent (prérequis non dits, chemins codés en dur) : l'inconnu bloque.", "README dans l'ordre de l'auteur et jamais testé sur un clone frais.", "ADR réduit à une phrase, sans point de bascule : la décision paraît subie."],
      checks: ["README complet testé (suivre ses propres instructions sur un clone frais).", "Collection Postman exportée + démo 2 min enregistrée.", "ADR n°2 écrit avec le point de bascule explicite.", "Un inconnu serait opérationnel en 5 minutes (critère vérifié)."],
      oral: "Pose le critère mesurable : « un inconnu teste l'API en 5 minutes ; je le vérifie sur un clone frais ». Décris le README dans l'ordre du lecteur et la collection comme doc vivante exécutable. Souligne le double bénéfice de la démo (preuve sans installation + entraînement à présenter) et l'ADR qui trace une décision avec son point de bascule. Insister sur « je teste ma propre doc sur un clone » montre que tu penses au lecteur, pas à toi.",
    },
  },

  // ── Sécurité web de base : OWASP appliqué à tes APIs ──
  67: {
    lessons: ['authentication.md', 'error-handling.md', 'api-design-basics.md'],
    theory: `**Modèle mental : la sécurité n'est pas une FEATURE qu'on ajoute à la fin — c'est une PROPRIÉTÉ à vérifier PARTOUT, sur chaque entrée, chaque réponse, chaque configuration. Et la posture qui change tout : ATTAQUER son propre système. Tu ne SAIS pas si ton API est sûre tant que tu n'as pas essayé de la casser. L'OWASP Top 10 recense les failles web les plus exploitées ; quatre te concernent immédiatement — injection, authentification cassée, exposition de données, mauvaise configuration.** Auditer tes propres API et corriger de vraies failles, c'est adopter l'état d'esprit d'attaquant qu'on applique ensuite aux systèmes LLM (mois 8).\n\nPourquoi la sécurité est une propriété transversale. Un développeur qui pense « je sécuriserai à la fin » a déjà perdu : une faille peut vivre dans n'importe quelle entrée non validée, n'importe quelle réponse trop bavarde, n'importe quel secret mal rangé. La sécurité se vérifie à CHAQUE point du système, pas dans un module « sécurité » ajouté après coup. Et elle ne se PROUVE que par l'attaque : croire son app sûre sans l'avoir attaquée est l'erreur fondamentale.\n\nLes quatre familles OWASP qui te concernent, depuis les fondations. **L'INJECTION** (SQL surtout) : entrée interprétée comme du code — ta défense est le paramétrage (jour 57). **L'AUTHENTIFICATION CASSÉE** : tokens absents, faibles, ou mal vérifiés, routes protégées qui ne le sont pas vraiment (jour 68). **L'EXPOSITION DE DONNÉES** : renvoyer TROP — mots de passe hashés, emails d'autres utilisateurs, stack traces internes (jour 54) ; on ne renvoie que le NÉCESSAIRE. **La MAUVAISE CONFIGURATION** : secrets commités dans Git, CORS ouvert à \`*\`, messages d'erreur bavards qui renseignent l'attaquant, dépendances non à jour.\n\nLa posture de l'attaquant, depuis les fondations. La seule façon de savoir si une API est sûre est d'essayer de la CASSER : envoyer une injection et vérifier qu'elle échoue, appeler une route protégée sans token, demander les données d'un autre utilisateur, provoquer une erreur et lire la réponse (fuit-elle des détails internes ?). Cet état d'esprit — supposer que tout est attaquable jusqu'à preuve du contraire — est ce qui distingue un système sécurisé d'un système « pas encore attaqué ».\n\nLe rate limiting, une défense simple. Limiter le nombre de requêtes par client et par fenêtre de temps protège contre le brute-force (deviner un token/mot de passe) et l'abus (déni de service applicatif). Un rate limiting même simple (compteur par IP) élève significativement le coût d'une attaque.\n\nLimites et compromis. La sécurité est un CONTINUUM, jamais « terminée » : on réduit la surface d'attaque, on ne l'annule pas. Trop de restrictions nuisent à l'usage (rate limit trop bas, validation trop stricte) ; le bon niveau protège sans bloquer les usages légitimes. Et l'audit maison ne remplace pas un audit professionnel — mais l'état d'esprit d'attaquant est la base indispensable.\n\nLa règle du jour : **traite la sécurité comme une propriété à vérifier PARTOUT (chaque entrée, réponse, config), et PROUVE-la en attaquant ton propre système. Audite les quatre familles OWASP qui te concernent — injection (paramétrage), auth cassée (tokens vérifiés), exposition (ne renvoyer que le nécessaire), mauvaise config (secrets hors du code) — corrige de vraies failles, et ajoute un rate limiting.**`,
    guided: `**Énoncé** : auditer une route de ton API selon OWASP en l'ATTAQUANT, puis corriger une faille d'exposition de données.\n\n**Exemple simple** : la faille d'exposition. \`GET /membres/1\` qui renvoie \`{ id, nom, email, motDePasseHash }\` EXPOSE le hash du mot de passe — inutile au client, précieux pour un attaquant. La correction : ne renvoyer que les champs NÉCESSAIRES.\n\n**Raisonnement pas-à-pas** : (1) j'ATTAQUE chaque famille : injection (déjà paramétré ?), auth (route protégée sans token ?), exposition (la réponse fuit-elle ?), config (secrets, erreurs bavardes ?) ; (2) je repère une faille réelle ; (3) je la corrige et je PROUVE la correction ; (4) j'ajoute un rate limiting simple.\n\n**Implémentation (audit + corrections)** :\n\`\`\`ts\n// 1. EXPOSITION : ne renvoyer que le nécessaire (pas le hash, pas d'autres emails)\napp.get('/membres/:id', (req, res) => {\n  const m = dataMembres.parId(Number(req.params.id));\n  if (!m) return res.status(404).json({ message: 'introuvable' });\n  const { motDePasseHash, ...public } = m;      // on RETIRE le champ sensible\n  res.json(public);                             // on ne renvoie que le nécessaire\n});\n\n// 2. MAUVAISE CONFIG : erreurs non bavardes (pas de stack au client, jour 54)\n// 3. INJECTION : requêtes paramétrées partout (jour 57) -> vérifié par relecture\n\n// 4. RATE LIMITING simple : limiter le brute-force et l'abus\nconst hits = new Map<string, { n: number; t: number }>();\nfunction rateLimit(req, res, next) {\n  const ip = req.ip, now = Date.now();\n  const e = hits.get(ip) ?? { n: 0, t: now };\n  if (now - e.t > 60000) { e.n = 0; e.t = now; }   // fenêtre d'1 min\n  e.n++; hits.set(ip, e);\n  if (e.n > 100) return res.status(429).json({ message: 'trop de requêtes' }); // 429\n  next();\n}\napp.use(rateLimit);\n\`\`\`\nL'audit par l'attaque révèle la fuite du hash (corrigée en ne renvoyant que le nécessaire), confirme le paramétrage anti-injection, et le rate limiting (429 au-delà de la limite) élève le coût d'un brute-force.`,
    quiz: [
      { q: "Pourquoi la sécurité n'est-elle pas une feature qu'on ajoute à la fin ?", a: "C'est une PROPRIÉTÉ à vérifier partout : une faille peut vivre dans n'importe quelle entrée non validée, réponse trop bavarde ou config. On la vérifie à chaque point, pas dans un module ajouté après coup." },
      { q: "Quelle posture prouve qu'une API est sûre ?", a: "ATTAQUER son propre système : envoyer une injection, appeler une route protégée sans token, demander les données d'un autre, provoquer une erreur pour voir ce qui fuit. Croire son app sûre sans l'avoir attaquée est l'erreur de base." },
      { q: "Cite les quatre familles OWASP qui te concernent immédiatement et leur défense.", a: "Injection (paramétrage), authentification cassée (tokens vérifiés), exposition de données (ne renvoyer que le nécessaire), mauvaise configuration (secrets hors du code, erreurs non bavardes, CORS restreint)." },
      { q: "À quoi sert le rate limiting ?", a: "À limiter le nombre de requêtes par client et par fenêtre de temps : il protège contre le brute-force (deviner un token/mot de passe) et l'abus, en élevant le coût d'une attaque (429 au-delà de la limite)." },
    ],
    caseStudy: `La sécurité web est un enjeu critique et permanent : les failles OWASP (injection, auth cassée, exposition de données, mauvaise configuration) sont responsables de la majorité des brèches réelles — fuites de données personnelles, vols de comptes, compromissions de bases. Le coût d'une brèche (financier, réputationnel, légal avec le RGPD) est énorme, et la plupart des failles exploitées sont connues et évitables. La posture « attaquer son propre système » est exactement celle des équipes de sécurité (pentest, red team) et des programmes de bug bounty. En entreprise, la sécurité est une responsabilité partagée par tous les développeurs, pas seulement une équipe dédiée : chacun doit valider les entrées, ne pas exposer de données superflues, garder les secrets hors du code. Savoir auditer une API selon OWASP, attaquer son propre code et corriger de vraies failles est une compétence directement valorisée — et l'état d'esprit d'attaquant se transfère aux systèmes LLM (injection de prompt, mois 8). « Comment sécurises-tu une API ? » est une question d'entretien back de plus en plus fréquente.`,
    interview: `**« Comment abordes-tu la sécurité d'une API ? »** → Je traite la sécurité comme une PROPRIÉTÉ à vérifier partout, pas comme une feature ajoutée à la fin : une faille peut vivre dans n'importe quelle entrée, réponse ou config. Et je pars du principe que je ne SAIS pas si mon API est sûre tant que je ne l'ai pas ATTAQUÉE. Concrètement, j'audite les quatre familles OWASP qui me concernent. L'INJECTION : je vérifie que tout le SQL est paramétré (zéro concaténation). L'AUTHENTIFICATION CASSÉE : j'appelle mes routes protégées sans token pour confirmer qu'elles refusent bien. L'EXPOSITION DE DONNÉES : je vérifie que mes réponses ne renvoient que le nécessaire — jamais un hash de mot de passe, l'email d'un autre utilisateur, ou une stack trace. La MAUVAISE CONFIGURATION : secrets hors du code (.env gitignored), CORS restreint, messages d'erreur non bavards. Puis je corrige les vraies failles trouvées et je le prouve. J'ajoute aussi un rate limiting pour freiner le brute-force et l'abus (429 au-delà de la limite). **Relance possible** : « quelle faille as-tu réellement trouvée chez toi ? » → typiquement une exposition — un endpoint qui renvoyait l'objet complet, hash inclus ; je l'ai corrigé en ne renvoyant que les champs publics. Ça m'a appris qu'on fuit des données par défaut si on ne choisit pas explicitement ce qu'on renvoie.`,
    future: `- **Bankable** : « comment sécurises-tu une API ? » est une question d'entretien back de plus en plus fréquente ; auditer selon OWASP et attaquer son propre code démontre une culture de sécurité recherchée.\n- Les quatre familles OWASP (injection, auth, exposition, config) et la posture d'attaquant se transfèrent directement à la sécurité LLM du mois 8 (injection de prompt, fuite de données).\n- La sécurité comme responsabilité de chaque développeur (valider, ne pas exposer, cacher les secrets) est un attendu de tout poste, renforcé par le RGPD.`,
    solution: {
      logic: "Traiter la sécurité comme une propriété à vérifier partout et la prouver en ATTAQUANT ses propres API. Auditer les quatre familles OWASP : injection (paramétrage), auth cassée (tokens vérifiés), exposition (ne renvoyer que le nécessaire), mauvaise config (secrets hors du code, erreurs non bavardes). Corriger 3 failles réelles avec preuves avant/après, et ajouter un rate limiting. La preuve : les failles trouvées sont concrètes et les corrections vérifiées par nouvelle attaque.",
      simple: "Passer en revue les failles OWASP et corriger ce qu'on trouve. La sécurité s'améliore.",
      improved: "Auditer les 4 familles en ATTAQUANT réellement chaque API (injection tentée, route protégée appelée sans token, réponse inspectée pour les fuites, config vérifiée), documenter 3 failles trouvées avec avant/après et preuve de correction, ajouter un rate limiting testé (dépasser la limite → 429), et relier la posture d'attaquant à la sécurité LLM à venir.",
      pitfalls: ["Croire son app sûre sans l'avoir attaquée : la sécurité ne se suppose pas, elle se prouve.", "Messages d'erreur qui fuient des infos internes (stack, requête, chemins) : renseignent l'attaquant.", "Exposer trop de données par défaut (hash, emails d'autres, champs internes) faute de choisir ce qu'on renvoie.", "Secrets commités ou CORS ouvert à * : mauvaise configuration exploitable."],
      checks: ["Audit des 4 familles de failles fait sur TES deux APIs, avec preuves.", "3 failles réelles trouvées et corrigées (avant/après documenté).", "Rate limiting fonctionnel (testé en dépassant la limite → 429).", "Aucune réponse ne fuit de données superflues ni de détails internes (vérifié par attaque)."],
      oral: "Pose la posture : « la sécurité est une propriété à vérifier partout, et je ne la suppose jamais — je l'attaque ». Déroule les 4 familles OWASP et leur défense (paramétrage, tokens, ne renvoyer que le nécessaire, secrets hors du code), donne une faille réelle trouvée chez toi (typiquement une exposition) et sa correction. Relier l'état d'esprit d'attaquant à l'injection de prompt LLM (mois 8) montre que tu vois la sécurité comme une compétence transversale.",
    },
  },

  // ── Auth par token et gestion des secrets ──
  68: {
    lessons: ['authentication.md', 'deployment-secrets.md'],
    theory: `**Modèle mental : l'authentification par token, c'est le client qui envoie un secret dans le header \`Authorization\` À CHAQUE requête (HTTP est sans état, jour 50), et un MIDDLEWARE qui le vérifie AVANT les routes protégées. Pourquoi le header et pas l'URL ? Parce que les URLs sont LOGGÉES partout — serveurs, proxies, historiques de navigateur — donc un secret dans l'URL est un secret PUBLIÉ. Et les secrets vivent dans l'ENVIRONNEMENT (\`.env\`, jamais commité), jamais en dur dans le code.** Protéger les routes d'écriture de LivreAPI, c'est appliquer l'hygiène de secrets qui resservira pour tes clés d'API LLM (mois 8).\n\nPourquoi une authentification, et pourquoi par token. HTTP est sans état : le serveur ne « se souvient » de personne d'une requête à l'autre. Pour savoir QUI fait une requête, le client doit donc PROUVER son identité à CHAQUE appel — en joignant un token (un secret prouvant qu'il est autorisé). Le token est vérifié par le serveur avant de laisser passer vers une route sensible. C'est la conséquence directe du caractère sans état : l'auth se rejoue à chaque requête.\n\nLe header Authorization, depuis les fondations. Le token voyage dans le header \`Authorization: Bearer <token>\`, PAS dans l'URL. Raison cruciale : les URLs sont enregistrées partout (logs de serveurs, de proxies, historique du navigateur, referers) — un secret dans une URL fuit dans tous ces journaux. Un header n'est pas loggé par défaut. C'est une règle de sécurité absolue : un secret ne va JAMAIS dans une URL.\n\nLe middleware d'authentification, depuis les fondations. Un middleware (jour 53) placé AVANT les routes protégées lit le header \`Authorization\`, vérifie le token, et : soit il attache l'identité à \`req\` et appelle \`next()\` ; soit il répond 401 (non authentifié) et court-circuite. L'ORDRE compte : le middleware d'auth doit s'exécuter avant les routes qu'il protège. On protège typiquement les routes d'ÉCRITURE (POST/PUT/DELETE), en laissant éventuellement les lectures publiques.\n\nLes secrets dans l'environnement, depuis les fondations. Un secret (token, clé d'API, mot de passe de base) ne vit JAMAIS dans le code (il serait publié dans Git, visible par tous). Il vit dans une variable d'ENVIRONNEMENT, lue via \`process.env\`, chargée depuis un fichier \`.env\` qui est GITIGNORÉ (jamais commité). Un fichier \`.env.example\` (commité, SANS les valeurs) documente les variables attendues. Cette hygiène prépare exactement la gestion des clés d'API LLM du mois 8.\n\nLa différence 401 vs autres. Sans token ou token invalide → 401 (non authentifié, jour 50). À distinguer de 403 (authentifié mais pas le droit). Un token présent mais faux reste un 401 : le serveur ne reconnaît pas l'identité.\n\nLimites et compromis. Un token statique partagé est simple mais limité (pas d'expiration, pas d'identité par utilisateur) — les vrais systèmes utilisent des tokens signés (JWT) avec expiration, ou des sessions. Le token dans le header suppose HTTPS (sinon il transite en clair, jour 71). Et gérer les secrets en \`.env\` est le minimum ; en production, on utilise un gestionnaire de secrets dédié.\n\nLa règle du jour : **authentifie par token dans le header \`Authorization\` (jamais dans l'URL, qui est loggée partout), vérifié par un MIDDLEWARE placé avant les routes protégées (401 si absent/invalide). Range les secrets dans l'ENVIRONNEMENT (\`.env\` gitignored, \`.env.example\` commité), jamais en dur dans le code — l'hygiène qui resservira pour tes clés LLM.**`,
    guided: `**Énoncé** : protéger les routes d'écriture de LivreAPI par un middleware d'auth token, avec le secret en \`.env\`.\n\n**Exemple simple** : header vs URL. \`Authorization: Bearer abc123\` (header, non loggé) est sûr ; \`POST /livres?token=abc123\` (URL) publie le secret dans tous les logs et historiques. Le token va TOUJOURS dans le header.\n\n**Raisonnement pas-à-pas** : (1) le secret vit dans \`.env\` (gitignored), lu via \`process.env\` ; (2) un middleware lit le header \`Authorization\`, compare le token ; (3) absent/invalide → 401 ; (4) le middleware est placé AVANT les routes d'écriture.\n\n**Implémentation** :\n\`\`\`ts\nimport 'dotenv/config';\n// .env (gitignored) :  API_TOKEN=un-secret-long-et-aleatoire\n// .env.example (commité) :  API_TOKEN=\nconst TOKEN = process.env.API_TOKEN;                 // jamais en dur dans le code\n\n// MIDDLEWARE d'auth : vérifie AVANT les routes protégées\nfunction authRequise(req, res, next) {\n  const header = req.get('Authorization') ?? '';\n  const token = header.startsWith('Bearer ') ? header.slice(7) : null;\n  if (!token || token !== TOKEN) {\n    return res.status(401).json({ message: 'authentification requise' });   // 401\n  }\n  next();                                            // token valide -> on continue\n}\n\n// Lectures publiques, ÉCRITURES protégées (ordre : middleware AVANT la route)\napp.get('/livres', listerLivres);                    // public\napp.post('/livres', authRequise, creerLivre);        // protégé\napp.delete('/livres/:id', authRequise, supprimerLivre);\n\`\`\`\nSans token → 401, mauvais token → 401, bon token → succès. Le secret vit dans \`.env\` (gitignored, avec un \`.env.example\` commité), jamais dans le code ni dans l'URL. Un audit de l'historique Git doit confirmer qu'aucun secret n'a jamais été commité.`,
    quiz: [
      { q: "Pourquoi le token va-t-il dans le header Authorization et jamais dans l'URL ?", a: "Parce que les URLs sont loggées partout (serveurs, proxies, historique navigateur, referers) : un secret dans l'URL est un secret publié. Un header n'est pas loggé par défaut." },
      { q: "Pourquoi l'authentification se rejoue-t-elle à chaque requête ?", a: "Parce que HTTP est sans état : le serveur ne se souvient de personne d'une requête à l'autre, donc le client doit prouver son identité (joindre le token) à chaque appel." },
      { q: "Où vivent les secrets, et que ne fait-on jamais ?", a: "Dans l'environnement : un fichier `.env` gitignored, lu via `process.env`, avec un `.env.example` commité (sans valeurs) qui documente les variables. On ne met JAMAIS un secret en dur dans le code (il serait publié dans Git)." },
      { q: "Quel statut pour une requête sans token ou avec un token invalide ?", a: "401 (non authentifié) : le serveur ne reconnaît pas l'identité. À distinguer de 403 (authentifié mais pas le droit). Un token présent mais faux reste un 401." },
    ],
    caseStudy: `L'authentification et la gestion des secrets sont des fondamentaux de sécurité que TOUTE application exposée doit maîtriser, et leurs défaillances sont parmi les causes les plus fréquentes de brèches. Un secret commité dans Git (clé d'API, mot de passe de base) est une fuite classique et coûteuse — les dépôts publics sont scannés en continu par des bots qui exploitent les clés exposées en minutes ; même dans un dépôt privé, un secret dans l'historique reste une bombe. Le token dans l'URL qui fuit dans les logs est un autre cas réel. Les équipes matures imposent : secrets hors du code (variables d'environnement, gestionnaires de secrets comme Vault ou les secrets managés du cloud), tokens dans les headers, vérification par middleware, et scan de l'historique Git. Cette hygiène est directement transférable aux clés d'API LLM (mois 8), qui coûtent de l'argent réel si elles fuient. Savoir implémenter une auth par token propre, ranger les secrets dans l'environnement et vérifier qu'aucun n'a fuité dans Git est un attendu de base — et « où mets-tu tes secrets ? » est une question d'entretien fréquente.`,
    interview: `**« Comment gères-tu l'authentification et les secrets d'une API ? »** → Pour l'authentification : comme HTTP est sans état, le client doit prouver son identité à chaque requête en joignant un token dans le header \`Authorization: Bearer <token>\` — JAMAIS dans l'URL, parce que les URLs sont loggées partout (serveurs, proxies, historique navigateur), donc un secret dans l'URL est publié. Un middleware placé AVANT les routes protégées lit le header, vérifie le token, et répond 401 s'il est absent ou invalide, sinon laisse passer. Je protège typiquement les routes d'écriture (POST/PUT/DELETE) en laissant éventuellement les lectures publiques. Pour les secrets : ils ne vivent JAMAIS dans le code — ils seraient publiés dans Git. Je les range dans des variables d'environnement, chargées depuis un fichier \`.env\` qui est gitignored, avec un \`.env.example\` commité (sans les valeurs) qui documente les variables attendues. Je vérifie aussi l'historique Git pour confirmer qu'aucun secret n'a jamais été commité. Cette hygiène est exactement celle que j'appliquerai à mes clés d'API LLM. **Relance possible** : « un token statique partagé suffit-il en production ? » → non : il n'a ni expiration ni identité par utilisateur ; en vrai on utilise des tokens signés (JWT) avec expiration ou des sessions, et le token suppose HTTPS pour ne pas transiter en clair.`,
    future: `- **Bankable** : « où mets-tu tes secrets ? » et « comment authentifies-tu ? » sont des questions d'entretien fréquentes ; secrets hors du code et token dans le header sont les réponses attendues.\n- L'hygiène des secrets (.env gitignored, .env.example, scan de l'historique Git) est directement transférable aux clés d'API LLM du mois 8, qui coûtent de l'argent si elles fuient.\n- Le middleware d'auth et la protection des routes d'écriture préparent l'authentification réelle (JWT, sessions) et le déploiement sécurisé.`,
    solution: {
      logic: "Protéger les routes d'écriture par un middleware d'auth qui lit le token dans le header Authorization (jamais l'URL) et répond 401 si absent/invalide, placé AVANT les routes protégées. Ranger les secrets dans l'environnement (.env gitignored, .env.example commité), jamais dans le code. Vérifier l'historique Git. La preuve : les 3 cas (sans token, mauvais token, bon token) sont testés et aucun secret n'a jamais été commité.",
      simple: "Un middleware qui vérifie un token sur les routes d'écriture, secret lu depuis .env. L'accès est protégé.",
      improved: "Vérifier le token dans le header (jamais l'URL), tester les 3 cas (sans token → 401, mauvais token → 401, bon token → succès), mettre le secret en .env gitignored avec un .env.example commité, et AUDITER l'historique Git pour confirmer qu'aucun secret n'a jamais fuité. Discuter la limite d'un token statique (JWT/expiration en vrai).",
      pitfalls: ["Token dans l'URL : fuite dans les logs, proxies et historiques — un secret publié.", "Secret en dur dans le code : publié dans Git, visible par tous.", ".env non gitignored ou secret déjà présent dans l'historique Git : la fuite persiste même après suppression.", "Middleware d'auth placé après la route, ou protégeant les mauvaises routes : la protection ne s'applique pas."],
      checks: ["Routes d'écriture protégées : sans token → 401, mauvais token → 401, bon token → succès (les 3 testés).", "Secrets en .env, .env dans .gitignore, .env.example commité.", "Audit de l'historique Git : aucun secret jamais commité.", "Le token est lu dans le header Authorization, jamais dans l'URL."],
      oral: "Explique la conséquence du sans-état : « l'auth se rejoue à chaque requête, via un token dans le header — jamais l'URL, qui est loggée partout ». Décris le middleware qui vérifie avant les routes protégées (401 sinon), et l'hygiène des secrets (.env gitignored, .env.example, scan de l'historique Git). Relier cette hygiène aux clés d'API LLM (mois 8) et mentionner la limite d'un token statique (JWT en vrai) montre une vision au-delà de l'exercice.",
    },
  },

  // ── Consolidation mois 3 : refactor complet d'une API ──
  69: {
    lessons: ['clean-code.md', 'testing-foundations.md', 'architecture-basics.md'],
    theory: `**Modèle mental : refactorer, c'est améliorer la STRUCTURE d'un code SANS changer son COMPORTEMENT — et la garantie que le comportement n'a pas changé, ce sont les TESTS. La méthode : d'abord VERROUILLER par des tests ce qui marche, ensuite SEULEMENT restructurer, en relançant les tests à chaque étape. Refactorer sans filet, c'est casser en croyant améliorer. Et on procède par PETITES étapes committables (extraire une fonction, déplacer un module, renommer), jamais par « grande réécriture » qui laisse trois jours avec un système cassé.** Consolider le mois 3, c'est prendre ton API la plus faible et la porter à une structure propre, testée, documentée — sous filet.\n\nPourquoi refactorer, et pourquoi sous tests. Le code accumule de la DETTE : à force d'ajouts rapides, la structure se dégrade (logique dans les routes, erreurs dupliquées, config en dur). Refactorer paie cette dette pour retrouver de la vélocité. Mais toucher à la structure risque de casser le comportement — sauf si des tests le VERROUILLENT d'abord. Les tests transforment le refactoring d'un pari (« j'espère ne rien casser ») en une opération sûre (« les tests confirment que le comportement est identique »). Sans tests, on ne refactore pas : on réécrit à l'aveugle.\n\nLa méthode des petites étapes, depuis les fondations. On ne fait JAMAIS une « grande réécriture » (big-bang) : elle laisse le système cassé pendant des jours, cumule les changements, et devient impossible à déboguer. On procède par PETITES étapes, chacune laissant le système FONCTIONNEL et COMMITTABLE : extraire une fonction, déplacer un module, renommer, centraliser les erreurs. Après chaque étape, on relance les tests. Un historique de petites étapes vertes est traçable et réversible ; un big-bang est un saut dans le vide.\n\nCe qu'on refactore dans une API, depuis les fondations. Les cibles typiques : SÉPARER les couches (sortir la logique des routes vers les services, jour 53), CENTRALISER les erreurs (un middleware unique, jour 54), SORTIR la config (secrets et paramètres en variables d'environnement, jour 68), NOMMER clairement (jour 40). Chaque amélioration réduit le couplage et clarifie les responsabilités, sans changer ce que l'API FAIT.\n\nLa dette technique, à documenter. La dette est ce qui reste imparfait après le refactoring : ce qu'on n'a pas eu le temps de traiter, un compromis assumé, un TODO justifié. On la DOCUMENTE (quoi, pourquoi accepté, quel signal déclencherait son paiement) plutôt que de la laisser invisible. Documenter la dette, c'est la rendre gérable au lieu de subie.\n\nLimites et compromis. Refactorer sans tests est dangereux — mais écrire les tests d'abord a un coût (temps) qu'on est tenté de zapper : c'est justement là que le refactoring échoue. Tout refactorer d'un coup est un anti-pattern (big-bang) ; améliorer par petites étapes est plus lent mais sûr. Et il faut savoir S'ARRÊTER : refactorer à l'infini (« encore un peu plus propre ») a un rendement décroissant — on vise « assez propre pour la suite », pas la perfection.\n\nLa règle du jour : **refactore = améliorer la structure SANS changer le comportement, garanti par des tests écrits AVANT ; procède par PETITES étapes committables (jamais un big-bang), en relançant les tests à chaque fois ; cible les couches, les erreurs centralisées, la config sortie ; et DOCUMENTE la dette restante (quoi, pourquoi, signal de paiement).**`,
    guided: `**Énoncé** : refactorer une route qui contient toute la logique vers une structure en couches, sous protection d'un test écrit AVANT.\n\n**Exemple simple** : le filet d'abord. Avant de toucher à une route qui fait tout (validation + SQL + réponse), j'écris un test d'intégration qui verrouille son comportement actuel. Ensuite je restructure : si le test reste vert, le comportement n'a pas changé. Sans ce test, je refactore à l'aveugle.\n\n**Raisonnement pas-à-pas** : (1) j'écris un test qui verrouille le comportement ACTUEL ; (2) j'extrais la logique de la route vers un service (petite étape) ; (3) je relance le test (vert = comportement inchangé) ; (4) je commit ; (5) étape suivante (centraliser les erreurs, sortir la config), test à chaque fois.\n\n**Avant / Après** :\n\`\`\`ts\n// AVANT : tout dans la route (intestable sans serveur, dupliqué)\napp.post('/livres', (req, res) => {\n  if (!req.body.titre) return res.status(400).json({ e: 'titre' });   // validation\n  const info = db.prepare('INSERT INTO livres (titre) VALUES (?)').run(req.body.titre); // SQL\n  res.status(201).json({ id: info.lastInsertRowid });                 // réponse\n});\n\`\`\`\n\`\`\`ts\n// 1. TEST écrit AVANT (verrouille le comportement)\ntest('POST /livres crée et renvoie 201', async () => {\n  const r = await fetch(url, { method: 'POST', headers: h, body: '{\"titre\":\"X\"}' });\n  assert.equal(r.status, 201);\n});\n\n// 2. APRÈS : couches séparées (petite étape, test toujours vert)\nconst serviceLivres = {\n  creer: (titre: string) => {\n    if (!titre?.trim()) throw new ErreurValidation('titre requis');   // logique -> service\n    return dataLivres.inserer(titre.trim());                          // SQL -> data\n  },\n};\napp.post('/livres', (req, res) => {\n  const cree = serviceLivres.creer(req.body.titre);   // la route ne fait que traduire\n  res.status(201).json(cree);\n});\n// erreurs centralisées (middleware), config en .env : étapes suivantes, test à chaque fois\n\`\`\`\nLe test écrit AVANT garantit que le comportement est identique après restructuration ; les petites étapes committables rendent le refactoring sûr et réversible ; la dette restante (ex. « validation encore manuelle ») est documentée.`,
    quiz: [
      { q: "Qu'est-ce que refactorer, et qu'est-ce qui garantit qu'on ne casse rien ?", a: "Améliorer la STRUCTURE du code sans changer son COMPORTEMENT. La garantie, ce sont les TESTS écrits AVANT : s'ils restent verts après restructuration, le comportement est identique." },
      { q: "Pourquoi procéder par petites étapes plutôt qu'une grande réécriture ?", a: "Le big-bang laisse le système cassé pendant des jours, cumule les changements et devient impossible à déboguer. Les petites étapes laissent le système fonctionnel et committable à chaque fois — traçable et réversible." },
      { q: "Quelles sont les cibles typiques d'un refactoring d'API ?", a: "Séparer les couches (logique hors des routes → services), centraliser les erreurs (middleware unique), sortir la config (secrets/paramètres en environnement), nommer clairement. Chaque amélioration réduit le couplage sans changer ce que l'API fait." },
      { q: "Que fait-on de la dette technique restante après un refactoring ?", a: "On la DOCUMENTE (quoi reste imparfait, pourquoi c'est accepté, quel signal déclencherait son paiement) au lieu de la laisser invisible — la rendre gérable plutôt que subie." },
    ],
    caseStudy: `Le refactoring sous tests est une compétence quotidienne et à fort enjeu en entreprise : le code vit des années, s'alourdit de dette, et doit être régulièrement assaini pour rester modifiable. Un développeur qui refactore SANS tests introduit des régressions silencieuses — il « améliore » et casse en même temps, ce qui érode la confiance de l'équipe. Un développeur qui verrouille par des tests puis avance par petites étapes committables améliore le code en toute sécurité, et son historique raconte une progression maîtrisée. Le big-bang (« je réécris tout ») est un anti-pattern classique qui échoue souvent : des semaines sans livrer, un système cassé, un abandon fréquent. Les équipes matures refactorent en continu, par petites touches, protégées par une suite de tests, et gèrent la dette technique explicitement (backlog de dette, règle du boy-scout « laisse le code plus propre que tu ne l'as trouvé »). Savoir refactorer sous filet et documenter la dette est un signal de maturité directement évalué — et « comment refactores-tu un code legacy sans le casser ? » est une question d'entretien fréquente.`,
    interview: `**« Comment refactores-tu un code existant sans casser son comportement ? »** → La règle absolue : je ne refactore JAMAIS sans filet. Refactorer, c'est améliorer la STRUCTURE sans changer le COMPORTEMENT, et la seule garantie que le comportement n'a pas changé, ce sont les tests. Donc je commence par VERROUILLER par des tests ce qui marche aujourd'hui — même du code que je vais restructurer, j'écris d'abord un test qui capture son comportement actuel. Ensuite seulement je restructure, par PETITES étapes committables : extraire une fonction, sortir la logique d'une route vers un service, centraliser les erreurs, sortir la config en environnement — et je relance les tests à CHAQUE étape. Si un test casse, je sais exactement quelle petite étape l'a cassé. Je ne fais jamais de « grande réécriture » big-bang, qui laisse le système cassé pendant des jours et devient impossible à déboguer. Enfin, je DOCUMENTE la dette restante : ce qui reste imparfait, pourquoi c'est accepté, et quel signal déclencherait son paiement. **Relance possible** : « et si le code legacy n'a aucun test ? » → j'écris d'abord des tests de caractérisation qui capturent le comportement actuel (même bizarre), pour créer le filet ; ensuite je refactore. On ne peut pas refactorer sûrement du code non testé sans d'abord le rendre testable.`,
    future: `- **Bankable** : « comment refactores-tu du legacy sans le casser ? » est une question d'entretien fréquente ; tests d'abord + petites étapes + dette documentée est la réponse de maturité.\n- Refactorer sous filet permet de faire évoluer et durcir LivreAPI (jour 85) sans régression, et applique la règle du boy-scout au quotidien.\n- La gestion explicite de la dette technique et le refus du big-bang sont des réflexes d'ingénieur réutilisés sur tout projet à long terme.`,
    solution: {
      logic: "Refactorer sous filet : d'abord verrouiller le comportement par des tests écrits AVANT, ensuite restructurer par petites étapes committables (couches séparées, erreurs centralisées, config sortie), en relançant les tests à chaque étape. Jamais de big-bang. Documenter la dette restante (quoi, pourquoi accepté, signal de paiement). La preuve : les tests écrits avant restent verts, l'historique montre plusieurs petites étapes.",
      simple: "Améliorer la structure de l'API (séparer un peu les couches, centraliser les erreurs). Le code est plus propre.",
      improved: "Écrire les tests AVANT de toucher au code (verrouiller le comportement), refactorer par PETITES étapes committables (chacune laisse le système fonctionnel, tests verts), cibler couches/erreurs/config, et DOCUMENTER la dette restante (quoi, pourquoi accepté, signal de paiement). Éviter tout big-bang.",
      pitfalls: ["Refactorer sans tests : on casse le comportement en croyant l'améliorer, régressions silencieuses.", "Big-bang (tout réécrire d'un coup) : système cassé pendant des jours, impossible à déboguer, souvent abandonné.", "Ne pas commiter les petites étapes : perte de la traçabilité et de la réversibilité.", "Laisser la dette restante invisible au lieu de la documenter : elle est subie au lieu d'être gérée."],
      checks: ["Le refactor est couvert par des tests écrits AVANT (et toujours verts après).", "Historique : plusieurs petites étapes commitées, jamais un big-bang.", "La dette restante est documentée (quoi, pourquoi accepté, signal de paiement).", "Le comportement de l'API est inchangé (mêmes réponses, prouvé par les tests)."],
      oral: "Martèle la règle : « je ne refactore jamais sans tests — ils garantissent que le comportement ne change pas ». Décris la méthode : verrouiller par des tests, puis petites étapes committables (couches, erreurs, config), tests à chaque fois, jamais de big-bang. Mentionner les tests de caractérisation pour du legacy non testé et la documentation de la dette montre que tu sais opérer sur du code réel, pas seulement sur du neuf.",
    },
  },

  // ── Revue semaine 10 : LivreAPI (finition), sécurité, refactor ──
  70: {
    reviewSynthese: `La semaine 10 a porté LivreAPI de « socle qui marche » à « projet crédible et sécurisé ». Recherche, pagination et filtres (jour 64) : raffiner une collection en query string, avec le piège d'OFFSET (curseur sur gros volume) et le % de LIKE sur la valeur paramétrée. Tests d'intégration (jour 65) : vérifier le système assemblé (HTTP → service → base) sur une base de test isolée, pour attraper les bugs de câblage que l'unitaire ne voit pas. Documentation (jour 66) : le critère « un inconnu teste en 5 minutes », README dans l'ordre du lecteur, collection comme doc vivante, démo enregistrée, ADR. Sécurité OWASP (jour 67) : la sécurité comme propriété à vérifier partout, PROUVÉE en attaquant son propre système (injection, auth, exposition, config). Auth par token et secrets (jour 68) : token dans le header (jamais l'URL), middleware de vérification, secrets en .env gitignored. Refactoring (jour 69) : améliorer la structure sans changer le comportement, garanti par des tests écrits avant, par petites étapes. Le fil rouge : **un projet crédible n'est pas seulement fonctionnel — il est testé (câblage vérifié), documenté (utilisable par un inconnu), sécurisé (attaqué par son auteur) et maintenable (refactorable sous filet).** LivreAPI est désormais une vraie 2e pièce de portfolio.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Recherche/pagination/filtres en paramétré, en connaissant le piège d'OFFSET (curseur) | |\n| Écrire des tests d'intégration sur base isolée (réponse ET état, règles métier) | |\n| Documenter pour « un inconnu opérationnel en 5 minutes » (README, collection, démo, ADR) | |\n| Auditer la sécurité en attaquant son API (4 familles OWASP) et corriger de vraies failles | |\n| Authentifier par token (header, middleware) et ranger les secrets hors du code | |\n| Refactorer sous tests, par petites étapes, en documentant la dette | |\n\n**Total /18** : ≥ 15 = LivreAPI est une vraie pièce de portfolio · 11-14 = revoir les items ≤ 1 · < 11 = consolider avant la culture d'ingénieur (semaines 11-12).`,
    remediation: `- **OFFSET coûteux / SQL concaténé dans la recherche** : rejoue le jour 64 — % de LIKE sur la valeur paramétrée, curseur sur gros volume, filtres présents seulement.\n- **Tests superficiels / sur base de dev** : jour 65 — base de test isolée réinitialisée, vérifier réponse ET état, cibler les règles métier (409) et cas d'erreur.\n- **Doc inutilisable** : jour 66 — critère « 5 minutes » testé sur clone frais, README dans l'ordre du lecteur, collection vivante, ADR avec point de bascule.\n- **Sécurité supposée, non prouvée** : jour 67 — attaque tes propres API (4 familles OWASP), corrige de vraies failles, ajoute un rate limiting.\n- **Token dans l'URL / secret en dur** : jour 68 — token dans le header, middleware avant les routes protégées, secrets en .env gitignored, audit de l'historique Git.\n- **Refactor à l'aveugle / big-bang** : jour 69 — tests écrits AVANT, petites étapes committables, dette documentée.`,
    interview: `1. **« Comment paginerais-tu un million de lignes ? »** Attendu : piège d'OFFSET (lit et jette), pagination par curseur (WHERE id > dernierIdVu), index ; LIKE non indexable → full-text.\n2. **« Différence entre test unitaire et test d'intégration ? »** Attendu : isolé (logique) vs assemblé (câblage, HTTP → base) ; base de test isolée, cibler les règles métier.\n3. **« Qu'est-ce qui rend un projet utilisable par un inconnu ? »** Attendu : critère « 5 minutes », README dans l'ordre du lecteur, collection vivante, démo, ADR ; testé sur clone frais.\n4. **« Comment sécurises-tu une API ? »** Attendu : sécurité comme propriété vérifiée partout, attaquer son propre système, 4 familles OWASP, rate limiting.\n5. **« Comment refactores-tu sans casser, et où mets-tu tes secrets ? »** Attendu : tests d'abord + petites étapes + dette documentée ; token dans le header, secrets en .env hors du code.`,
  },
};
