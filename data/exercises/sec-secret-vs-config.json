{
  "id": "sec-secret-vs-config",
  "title": "Sécurité : secret ou configuration ?",
  "difficulty": 2,
  "summary": "Classe une clé de configuration : 'secret' si son nom contient password/token/key/secret/credential (insensible à la casse), sinon 'config'. Un secret ne va jamais dans une ConfigMap ni dans le code.",
  "runtime": "python3",
  "language": "python",
  "skills": [
    "secu"
  ],
  "sprint": "v46",
  "activeFile": "solution.py",
  "workspace": {
    "entry": "solution.py",
    "files": [
      {
        "path": "solution.py",
        "content": "def classify(key):\n    # TODO : 'secret' si nom sensible, sinon 'config'\n    return 'config'\n"
      }
    ]
  },
  "reference": {
    "solution.py": "def classify(key):\n    k = key.lower()\n    for m in ('password', 'token', 'key', 'secret', 'credential'):\n        if m in k:\n            return 'secret'\n    return 'config'\n"
  },
  "tests": [
    {
      "id": "t1",
      "name": "API_KEY → secret",
      "kind": "call-equals",
      "export": "classify",
      "args": [
        "API_KEY"
      ],
      "expected": "secret"
    },
    {
      "id": "t2",
      "name": "LOG_LEVEL → config",
      "kind": "call-equals",
      "export": "classify",
      "args": [
        "LOG_LEVEL"
      ],
      "expected": "config"
    },
    {
      "id": "t3",
      "name": "db_password → secret (privé)",
      "kind": "call-equals",
      "export": "classify",
      "args": [
        "db_password"
      ],
      "expected": "secret",
      "private": true
    }
  ]
}
