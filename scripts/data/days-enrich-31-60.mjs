// Enrichissement Batch 5G — jours 31 à 60 (mois 2 et 3 : structures de données
// appliquées, TypeScript, POO et design patterns, clean code et debugging,
// Projet 1 TaskFlow, HTTP/REST/Express, validation, SQL et modélisation,
// Projet 2 LivreAPI). Fusionné dans generate-curriculum.mjs.
// Jours de travail : theory (rendu en theoryExtra, APRÈS la théorie des extras),
//   guided, quiz [{q,a}], caseStudy, interview, future,
//   solution {logic, simple, improved, pitfalls, checks, oral}, lessons (override).
//   (guided intègre un exemple simple + un raisonnement pas-à-pas ; le mini-quiz
//    est porté par le champ quiz ; solution.logic donne une solution simple PUIS améliorée.)
// Jours de revue (35, 42, 49, 56) : reviewSynthese, reviewGrid, remediation, interview.
// Les titres, objectifs, exercices et livrables réels des jours 31-60 ne sont PAS modifiés.

export const ENRICH_31_60 = {
  // ────────────────────────────────────────────────────────────────────────
  // MOIS 2 · Semaine 5 (jours 31-34) : structures de données appliquées
  // ────────────────────────────────────────────────────────────────────────

  // ── Deux sommes et le réflexe hash map ──
  31: {
    lessons: ['data-structures-intro.md', 'algorithmic-thinking.md'],
    theory: `**Modèle mental : une Map ÉCHANGE de la mémoire contre du temps. Au lieu de re-parcourir le tableau pour chercher « la valeur qui me manque » (le complément), tu la mémorises au fur et à mesure et tu l'interroges en O(1). La double boucle O(n²) devient une seule passe O(n) parce que la recherche interne — le vrai coût — disparaît, absorbée par la table de hachage.** Ce n'est pas une astuce propre à twoSum : c'est LE réflexe qui transforme une famille entière de problèmes de « chercher une paire » de quadratique en linéaire.\n\nPourquoi ce pattern existe, depuis les fondations. Chercher un élément dans un tableau non trié coûte O(n) : tu dois potentiellement tout regarder. Une table de hachage range les éléments dans des « cases » calculées à partir de la valeur elle-même (la fonction de hachage) : retrouver une valeur ne demande plus de parcourir, mais de recalculer sa case — coût O(1) en moyenne. Le prix : de la mémoire supplémentaire (la table) et une hypothèse de bon hachage (les collisions dégradent vers O(n) dans le pire cas, rare en pratique).\n\nComment le pattern « complément » fonctionne concrètement. Pour \`twoSum(arr, cible)\`, à chaque élément \`x\` tu ne cherches pas « existe-t-il un \`y\` tel que \`x + y = cible\` » en re-balayant : tu demandes à la Map « ai-je DÉJÀ vu \`cible - x\` ? ». Si oui, la paire est trouvée ; sinon tu enregistres \`x\` et tu continues. Chaque valeur n'est vue qu'une fois pour l'écriture, une fois pour l'interrogation.\n\nLa différence avec les approches proches, pour ne pas confondre. Le **tri + deux pointeurs** résout aussi twoSum en O(n log n) et O(1) mémoire — meilleur si la mémoire est critique ou si le tableau est déjà trié, mais il DÉTRUIT l'ordre (donc les indices d'origine). La **double boucle** O(n²) reste acceptable pour de très petits tableaux (n < ~50) où la simplicité prime. Le choix hash map gagne quand n est grand ET qu'on peut payer la mémoire — le cas le plus fréquent en entretien.\n\nLes limites et compromis à assumer. La Map coûte O(n) de mémoire : sur des volumes énormes contraints en RAM, le tri + deux pointeurs redevient pertinent. Et l'O(1) est amorti « en moyenne » : un adversaire ou de mauvaises clés peuvent provoquer des collisions. Enfin, attention au sens de la question — chercher le complément AVANT ou APRÈS avoir inséré l'élément courant change si l'on autorise \`i == j\`.\n\nLa règle du jour : **dès que tu écris une double boucle dont la boucle interne CHERCHE quelque chose, demande-toi si une Map (mémoriser le vu pour interroger le passé en O(1)) ne supprime pas cette boucle interne. C'est le réflexe d'entretien le plus rentable de tout le mois.**`,
    guided: `**Énoncé** : écrire \`twoSum(arr, cible)\` en O(n) qui renvoie les deux INDICES dont les valeurs somment à la cible, puis comprendre pourquoi une variante (sous-tableau de somme nulle) réutilise le même réflexe via les sommes cumulées.\n\n**Exemple simple** : sur \`[2, 7, 11, 15]\` avec cible \`9\`. La naïve teste \`(2,7), (2,11), (2,15), (7,11)…\` → 6 paires. Le réflexe complément : à l'élément \`2\`, je cherche \`9 - 2 = 7\` dans la Map (vide → j'enregistre \`2→index 0\`) ; à l'élément \`7\`, je cherche \`9 - 7 = 2\` → trouvé à l'index 0 → réponse \`[0, 1]\`. Deux éléments visités, zéro boucle interne.\n\n**Raisonnement pas-à-pas** : (1) je parcours une seule fois ; (2) pour chaque \`x\`, je calcule le complément \`cible - x\` ; (3) je demande à la Map si ce complément a déjà été vu ; (4) si oui → paire trouvée ; (5) sinon j'enregistre \`x → son index\` et je continue. L'ordre « chercher PUIS enregistrer » garantit qu'on n'apparie pas un élément avec lui-même.\n\n**Implémentation** :\n\`\`\`ts\nfunction twoSum(arr: number[], cible: number): [number, number] | null {\n  const vus = new Map<number, number>();   // valeur -> index\n  for (let i = 0; i < arr.length; i++) {\n    const complement = cible - arr[i];\n    if (vus.has(complement)) {              // déjà vu -> paire trouvée\n      return [vus.get(complement)!, i];\n    }\n    vus.set(arr[i], i);                     // sinon, mémoriser puis continuer\n  }\n  return null;                              // aucune paire\n}\n\nconsole.log(twoSum([2, 7, 11, 15], 9));     // [0, 1]\nconsole.log(twoSum([3, 3], 6));             // [0, 1] : chercher AVANT d'insérer\nconsole.log(twoSum([1, 2, 3], 100));        // null\n\`\`\`\n\n**Le même réflexe, variante sous-tableau de somme nulle** : on parcourt en gardant la somme cumulée ; si une même somme cumulée réapparaît, le segment entre les deux positions somme à zéro. La Map mémorise \`sommeCumulée → index\`, initialisée à \`{0: -1}\` pour capter un préfixe entier.\n\`\`\`ts\nfunction sousTableauSommeNulle(arr: number[]): [number, number] | null {\n  const vues = new Map<number, number>([[0, -1]]);  // somme 0 \"avant le début\"\n  let cumul = 0;\n  for (let i = 0; i < arr.length; i++) {\n    cumul += arr[i];\n    if (vues.has(cumul)) return [vues.get(cumul)! + 1, i];\n    vues.set(cumul, i);\n  }\n  return null;\n}\nconsole.log(sousTableauSommeNulle([3, 4, -7, 5]));   // [0, 2] : 3+4-7 = 0\n\`\`\``,
    quiz: [
      { q: "Pourquoi la Map fait-elle passer twoSum de O(n²) à O(n) ?", a: "Elle supprime la boucle interne de RECHERCHE : demander « ai-je vu le complément ? » coûte O(1) au lieu de re-parcourir le tableau en O(n). On échange de la mémoire (la table) contre du temps." },
      { q: "Pourquoi faut-il chercher le complément AVANT d'insérer l'élément courant ?", a: "Sinon on risque d'apparier un élément avec lui-même (i == j). En cherchant d'abord, la Map ne contient que les éléments STRICTEMENT précédents." },
      { q: "Pourquoi la Map des sommes cumulées est-elle initialisée avec {0: -1} ?", a: "Pour capter le cas où un PRÉFIXE entier (depuis l'index 0) somme à la cible/zéro : la somme 0 est réputée « vue avant le début », à l'index -1." },
      { q: "Quand le tri + deux pointeurs bat-il la hash map pour twoSum ?", a: "Quand la mémoire est critique (O(1) contre O(n)) ou que le tableau est déjà trié. Le compromis : le tri détruit l'ordre d'origine, donc les indices." },
    ],
    caseStudy: `Le réflexe hash map est omniprésent dès qu'un système doit « rapprocher » des données à grand volume : dédoublonnage de contacts (a-t-on déjà vu cet email ?), rapprochement bancaire (associer un débit à un crédit du même montant), détection de doublons dans un flux d'événements, jointure en mémoire de deux jeux de données. Un développeur qui écrit une double boucle sur 100 000 lignes fait ramer le service pendant des secondes ; celui qui indexe un côté dans une Map répond en millisecondes. En entretien technique, twoSum et ses variantes sont un filtre de premier tour quasi systématique — savoir passer du naïf à l'optimal EN EXPLIQUANT le compromis mémoire/temps est exactement ce que le recruteur mesure.`,
    interview: `**« On te donne un tableau et une cible. Renvoie les indices de deux éléments dont la somme vaut la cible. Optimise. »** → Je pars de la solution naïve pour cadrer : deux boucles imbriquées testent toutes les paires, O(n²). Je l'optimise avec le réflexe complément : une seule passe, et pour chaque élément \`x\` je demande à une Map « ai-je déjà vu \`cible - x\` ? » — la Map répond en O(1), donc le total tombe à O(n) en temps contre O(n) en mémoire. Un détail important : je cherche le complément AVANT d'insérer l'élément courant, pour ne pas l'apparier avec lui-même. Le compromis à assumer : j'échange de la mémoire contre du temps ; si la RAM était critique, un tri + deux pointeurs ferait O(n log n) et O(1) mémoire, mais détruirait les indices d'origine. **Relance possible** : « et pour trouver un sous-tableau CONTIGU de somme nulle ? » → même réflexe avec les sommes cumulées : si une somme cumulée réapparaît, le segment entre les deux positions somme à zéro (Map initialisée à \`{0: -1}\`).`,
    future: `- **Bankable** : twoSum et le réflexe hash map sont LE kata de premier tour d'entretien technique — le maîtriser en expliquant le compromis mémoire/temps est un signal direct d'employabilité.\n- Le pattern « mémoriser le vu pour interroger le passé en O(1) » se réutilise partout : dédoublonnage, jointures en mémoire, caches, détection de doublons dans un flux.\n- Les sommes cumulées + Map ré-apparaissent dans les problèmes de sous-tableaux et de fenêtres, jusqu'aux traitements de données du mois data.`,
    solution: {
      logic: "Reconnaître la famille « chercher une paire/un complément ». La question centrale : au lieu de re-balayer le tableau pour chaque élément, mémoriser ce qu'on a vu et l'interroger en O(1). Pour twoSum : Map valeur→index, chercher le complément avant d'insérer. Pour les variantes (différence donnée, doublon à distance ≤ k, somme nulle), c'est le MÊME réflexe adapté (complément = valeur±d, fenêtre glissante, ou sommes cumulées).",
      simple: "Double boucle : pour chaque i, chercher un j > i tel que arr[i] + arr[j] == cible. Correct, O(n²), acceptable sur de très petits tableaux. À présenter d'abord en entretien pour cadrer le problème.",
      improved: "Une seule passe avec une Map valeur→index : pour chaque élément, tester si le complément a déjà été vu, sinon l'enregistrer. O(n) temps, O(n) mémoire. Chronométrer les deux versions sur 100 000 éléments matérialise l'écart. Mentionner l'alternative tri + deux pointeurs (O(n log n), O(1) mémoire) pour montrer qu'on connaît le compromis.",
      pitfalls: ["Chercher le complément APRÈS avoir inséré l'élément courant : on l'apparie avec lui-même (i == j) à tort.", "Sommes cumulées : oublier d'initialiser la Map avec {0: -1}, ce qui rate les préfixes qui somment à la cible.", "Confondre « renvoyer les valeurs » et « renvoyer les indices » : la Map doit stocker l'index, pas juste la présence.", "Croire que la Map garantit O(1) dans le PIRE cas : c'est O(1) amorti/moyen ; des collisions massives dégradent vers O(n)."],
      checks: ["twoSum trouve bien la paire quand elle existe et renvoie null sinon (cas testés à la main).", "Le cas [3,3] cible 6 renvoie [0,1] (preuve que la recherche précède l'insertion).", "Le benchmark montre un écart croissant O(n) vs O(n²) sur 10k puis 100k éléments.", "La variante somme nulle est vérifiée sur un cas construit ([3,4,-7,5] → [0,2])."],
      oral: "Déroule la démarche en entretien : « je pose la naïve O(n²), puis je remarque que la boucle interne CHERCHE — donc une Map la supprime ». Insiste sur le compromis mémoire/temps (O(n) mémoire pour O(n) temps) et sur le détail « chercher avant d'insérer ». Terminer en reliant à une variante (sommes cumulées) prouve que tu as compris le PATTERN, pas juste appris twoSum par cœur — c'est ce qui fait la différence.",
    },
  },

  // ── Récursion consolidée : backtracking d'introduction ──
  32: {
    lessons: ['recursion.md', 'algorithmic-thinking.md'],
    theory: `**Modèle mental : générer toutes les combinaisons, c'est PARCOURIR un arbre de décisions. À chaque élément, deux branches — « je le prends » ou « je ne le prends pas ». La récursion DESCEND dans une branche, atteint une feuille (une solution complète), puis REMONTE en défaisant le dernier choix pour explorer l'autre branche : c'est le backtracking. Le nombre de feuilles (2ⁿ pour les sous-ensembles, n! pour les permutations) EST la taille du résultat, donc le coût exponentiel vient du PROBLÈME, pas d'une maladresse.** Distinguer « exponentiel inévitable » (générer 2ⁿ sous-ensembles) de « exponentiel évitable » (fib naïf qui recalcule) est une compétence d'analyse cruciale.\n\nPourquoi le backtracking existe. Beaucoup de problèmes demandent d'ÉNUMÉRER ou de CHERCHER parmi toutes les configurations possibles : tous les sous-ensembles, toutes les permutations, tous les placements valides (Sudoku, N-reines). On ne peut pas les lister « à plat » : leur nombre dépend de n. Le backtracking les construit INCRÉMENTALEMENT — un choix à la fois — et abandonne (« élague ») une branche dès qu'elle ne peut plus mener à une solution, ce qui évite d'explorer des configurations mortes.\n\nLe squelette universel, depuis les fondations. Trois gestes : **choisir** (ajouter un élément à la solution partielle), **explorer** (appel récursif sur le reste), **défaire** (retirer l'élément pour tester l'autre branche). Le cas de base : « plus de décisions à prendre » → on enregistre une COPIE de la solution partielle. Le « défaire » est ce qui distingue le backtracking d'une simple récursion : on RÉUTILISE le même accumulateur en le remettant dans son état d'avant, au lieu d'en créer un à chaque nœud.\n\nLa différence avec les concepts proches. La **récursion simple** (factorielle, somme) fait UN appel par niveau et remonte une valeur ; le backtracking fait PLUSIEURS appels par niveau (une par branche) et construit un ensemble de résultats. La **programmation dynamique** s'attaque aux problèmes où les sous-problèmes se RÉPÈTENT (fib) : on mémorise pour ne pas recalculer — inutile ici, car chaque sous-ensemble est unique. Le backtracking et la DP répondent donc à des besoins opposés : énumérer/chercher vs optimiser en réutilisant.\n\nLimites et compromis. L'exponentiel plafonne l'usage : générer les sous-ensembles de 30 éléments = un milliard de feuilles, impraticable. On ne backtrack en pratique que si n est petit OU si l'élagage coupe la majorité de l'arbre (contraintes fortes). Piège mémoire classique : muter l'accumulateur PARTAGÉ sans en enregistrer une COPIE au fond — toutes les « solutions » finissent identiques (des références au même tableau vidé au retour).\n\nLa règle du jour : **quand un problème demande « toutes les » configurations ou « existe-t-il une » configuration valide, pense arbre de décisions + backtracking (choisir → explorer → défaire), enregistre une COPIE au cas de base, et sache dire si l'exponentiel est imposé par le problème ou par ta solution.**`,
    guided: `**Énoncé** : générer tous les sous-ensembles de \`[1, 2, 3]\` (il y en a 2³ = 8) par récursion à choix binaire, en observant la descente et la remontée.\n\n**Exemple simple** : l'arbre de décision pour \`[1, 2]\`. Racine : rien de choisi. Niveau 1 (élément 1) : branche « avec 1 » / branche « sans 1 ». Niveau 2 (élément 2) : chaque branche se re-divise. Feuilles : \`{1,2}, {1}, {2}, {}\` = 2² = 4. Ajouter l'élément 3 double chaque feuille → 8.\n\n**Raisonnement pas-à-pas** : (1) je traite les éléments dans l'ordre, un « index de décision » qui avance ; (2) à chaque index, deux appels : un où je PRENDS l'élément (je le \`push\`), un où je ne le prends pas ; (3) quand l'index dépasse le dernier élément → cas de base → j'enregistre une COPIE de la solution partielle ; (4) après l'appel « je prends », je \`pop\` pour DÉFAIRE et laisser la branche « je ne prends pas » repartir d'un état propre.\n\n**Implémentation** :\n\`\`\`ts\nfunction sousEnsembles<T>(arr: T[]): T[][] {\n  const resultats: T[][] = [];\n  const partiel: T[] = [];\n\n  function explorer(i: number): void {\n    if (i === arr.length) {          // cas de base : plus de décision\n      resultats.push([...partiel]);  // COPIE, sinon on pousse une référence mutée\n      return;\n    }\n    partiel.push(arr[i]);            // CHOISIR : je prends arr[i]\n    explorer(i + 1);                 // EXPLORER la branche \"avec\"\n    partiel.pop();                   // DÉFAIRE (backtrack)\n    explorer(i + 1);                 // EXPLORER la branche \"sans\"\n  }\n\n  explorer(0);\n  return resultats;\n}\n\nconsole.log(sousEnsembles([1, 2, 3]));\n// [[1,2,3],[1,2],[1,3],[1],[2,3],[2],[3],[]]  -> 8 sous-ensembles\n\`\`\`\n\n**La même mécanique, permutations** : au lieu de « prendre/ne pas prendre », on choisit à chaque niveau UN élément non encore utilisé (n choix, puis n-1…), d'où n! feuilles.\n\`\`\`ts\nfunction permutations<T>(arr: T[]): T[][] {\n  const res: T[][] = [], partiel: T[] = [], utilise = new Set<number>();\n  function explorer(): void {\n    if (partiel.length === arr.length) { res.push([...partiel]); return; }\n    for (let i = 0; i < arr.length; i++) {\n      if (utilise.has(i)) continue;      // on ne réutilise pas un élément\n      utilise.add(i); partiel.push(arr[i]);      // choisir\n      explorer();                                 // explorer\n      partiel.pop(); utilise.delete(i);           // défaire\n    }\n  }\n  explorer();\n  return res;\n}\nconsole.log(permutations([1, 2, 3]).length);   // 6 = 3!\n\`\`\``,
    quiz: [
      { q: "Pourquoi les sous-ensembles de n éléments sont-ils au nombre de 2ⁿ ?", a: "Chaque élément offre 2 choix indépendants (dedans/dehors), donc 2 × 2 × … × 2 (n fois) = 2ⁿ configurations. L'arbre de décision a 2ⁿ feuilles." },
      { q: "À quoi sert précisément le « pop » (défaire) dans le backtracking ?", a: "Il remet l'accumulateur PARTAGÉ dans son état d'avant le choix, pour que la branche suivante reparte propre. Sans lui, les choix s'accumulent et corrompent les autres branches." },
      { q: "Pourquoi enregistre-t-on `[...partiel]` et non `partiel` au cas de base ?", a: "`partiel` est un tableau muté en continu : pousser sa référence ferait pointer tous les résultats vers le MÊME tableau, vidé au retour. La copie fige l'état à cet instant." },
      { q: "En quoi cet exponentiel diffère-t-il de celui de fib(n) naïf ?", a: "Ici l'exponentiel est imposé par la TAILLE du résultat (2ⁿ sous-ensembles à produire) — inévitable. Pour fib naïf, il vient de sous-problèmes RECALCULÉS — évitable par mémoïsation." },
    ],
    caseStudy: `Le backtracking résout les problèmes de configuration et de contrainte omniprésents en ingénierie : générer les combinaisons d'options d'un produit, résoudre un solveur de contraintes (planning, placement), explorer les coups possibles d'un moteur de jeu, énumérer les chemins valides d'un routeur. Un moteur de règles métier qui doit trouver « une affectation valide de tâches à des créneaux » est un backtracking déguisé. La clé en production : l'élagage. Un développeur qui génère toutes les configurations PUIS filtre fait exploser le temps ; celui qui abandonne une branche dès qu'une contrainte est violée rend le problème traitable. En entretien, « génère toutes les combinaisons/permutations » ou « résous ce Sudoku » teste précisément si tu sais structurer choisir → explorer → défaire et raisonner sur la complexité.`,
    interview: `**« Écris une fonction qui génère tous les sous-ensembles d'un tableau, et donne sa complexité. »** → C'est un parcours d'arbre de décisions : chaque élément a deux branches (le prendre ou non), donc 2ⁿ feuilles = 2ⁿ sous-ensembles. J'utilise le squelette du backtracking : une fonction récursive avec un index de décision, qui à chaque niveau fait deux appels (« avec » et « sans » l'élément), et au cas de base (index au bout) enregistre une COPIE de la solution partielle. Le geste crucial est le « défaire » (\`pop\`) entre les deux branches, pour que l'accumulateur partagé reparte propre — et la copie au cas de base, sinon tous les résultats pointent vers le même tableau muté. La complexité est O(2ⁿ) en temps, imposée par la taille du résultat, pas par une maladresse — c'est un exponentiel INÉVITABLE, à distinguer de celui de fib naïf qui, lui, se corrige par mémoïsation. **Relance possible** : « et pour les permutations ? » → même mécanique, mais à chaque niveau on choisit un élément NON ENCORE UTILISÉ (n puis n-1…), d'où n! feuilles.`,
    future: `- **Bankable** : « génère toutes les combinaisons/permutations » et les puzzles de contraintes (Sudoku, N-reines) sont des exercices d'entretien classiques ; le squelette choisir → explorer → défaire les résout tous.\n- Le raisonnement « exponentiel imposé par le problème vs par la solution » est exactement ce qui prépare la programmation dynamique et l'optimisation.\n- La rigueur sur l'accumulateur partagé (copier au fond, défaire au retour) est une source de bugs subtils que savoir éviter distingue un candidat mûr.`,
    solution: {
      logic: "Voir le problème comme un arbre de décisions et appliquer le squelette du backtracking : à chaque niveau, énumérer les choix possibles, pour chacun CHOISIR (muter l'accumulateur), EXPLORER (récursion), puis DÉFAIRE (remettre l'accumulateur en état). Au cas de base, enregistrer une COPIE de la solution partielle. Sous-ensembles = choix binaire par élément (2ⁿ). Permutations = choix d'un élément non utilisé (n!). CombinaisonsDeSomme = sous-ensembles avec élagage quand la somme dépasse la cible.",
      simple: "Sous-ensembles par récursion à deux appels (avec/sans l'élément courant), en accumulant dans un tableau partiel copié au cas de base. Direct et suffisant pour de petits n.",
      improved: "Factoriser le squelette (choisir/explorer/défaire) réutilisable pour les trois générateurs, ajouter l'ÉLAGAGE pour combinaisonsDeSomme (abandonner une branche dès que la somme partielle dépasse la cible), et dessiner l'arbre de décision pour [1,2,3] afin de PROUVER que l'exécution correspond aux 8 feuilles. Nommer la complexité (2ⁿ, n!) et sa cause.",
      pitfalls: ["Muter l'accumulateur partagé sans en enregistrer une COPIE au cas de base : tous les résultats deviennent identiques.", "Oublier le « défaire » (pop) entre deux branches : les choix d'une branche polluent la suivante.", "Manquer le cas de base (plus d'éléments à décider) : récursion infinie ou dépassement de pile.", "Confondre l'exponentiel inévitable (2ⁿ résultats) avec un exponentiel dû à une mauvaise solution (recalcul redondant)."],
      checks: ["sousEnsembles([1,2,3]) retourne exactement 8 tableaux, tous distincts.", "permutations([1,2,3]) retourne 6 résultats, chacun une permutation valide sans doublon.", "combinaisonsDeSomme ne retient que les sous-ensembles dont la somme égale la cible.", "L'arbre de décision dessiné correspond nœud par nœud aux appels tracés (ajouter des console.log)."],
      oral: "Explique en dessinant : « chaque élément = un embranchement, la récursion descend, atteint une feuille = une solution, puis remonte en défaisant ». Insiste sur les deux pièges qui trahissent la compréhension : copier au cas de base et défaire au retour. Termine par l'analyse de complexité en distinguant l'exponentiel du problème (inévitable) de celui d'une solution maladroite (mémoïsable) — c'est ce raisonnement qui impressionne, plus que le code lui-même.",
    },
  },

  // ── Stacks et Queues : implémenter pour posséder ──
  33: {
    lessons: ['data-structures-intro.md'],
    theory: `**Modèle mental : une Stack et une Queue ne sont pas des « conteneurs » de plus — ce sont des DISCIPLINES D'ACCÈS. La Stack impose LIFO (dernier entré, premier sorti, comme une pile d'assiettes) ; la Queue impose FIFO (premier entré, premier sorti, comme une file d'attente). Choisir la bonne discipline rend un problème trivial ; choisir la mauvaise le rend inextricable. Leur valeur tient à ce qu'elles INTERDISENT (l'accès arbitraire), pas à ce qu'elles offrent.** Un tableau permet tout ; une Stack ne permet QUE d'empiler et dépiler au sommet — cette contrainte est exactement ce qui modélise l'imbrication, l'historique et l'ordre de traitement.\n\nPourquoi ces structures existent, depuis les fondations. Certains problèmes ont une structure d'IMBRICATION (parenthèses, appels de fonctions, undo/redo) où « le dernier ouvert doit se fermer en premier » : c'est LIFO, une pile. D'autres ont une structure d'ORDONNANCEMENT équitable (tâches à traiter dans l'ordre d'arrivée, parcours en largeur) : c'est FIFO, une file. Plutôt que de re-coder cette logique à la main, on encapsule la discipline dans un type dont l'interface elle-même empêche de tricher.\n\nComment elles fonctionnent et pourquoi O(1). Sur un tableau, \`push\`/\`pop\` en FIN sont O(1) (on touche le sommet). La Stack s'implémente donc directement sur un tableau. La Queue naïve utilise \`push\` (enfiler en fin) + \`shift\` (défiler en tête) — mais \`shift\` est O(n) car il DÉCALE tous les éléments ! La vraie Queue O(1) garde deux index (tête et queue) ou une liste chaînée, pour ne jamais décaler. Cette nuance est un piège d'entretien fréquent.\n\nLa différence avec les concepts proches. Un **tableau** offre l'accès indexé O(1) mais n'impose aucune discipline. Une **Deque** (double-ended queue) autorise les deux bouts — plus souple mais moins « parlante » sur l'intention. Le **Set/Map** vise l'appartenance/l'association, pas l'ordre de traitement. Choisir Stack ou Queue, c'est DÉCLARER la sémantique du problème dans le type — un lecteur comprend immédiatement « ici l'ordre d'imbrication compte » ou « ici l'ordre d'arrivée compte ».\n\nLes applications qui rendent le concept concret. Stack : historique undo/redo, vérification de parenthèses \`()[]{}\`, évaluation d'expressions, et… la CALL STACK de la récursion (chaque appel empile son contexte, le retour dépile — la récursion EST une pile déguisée). Queue : files de tâches, buffers de messages (mois 10), et le parcours en largeur BFS (jour 34).\n\nLimites et compromis. Une Stack/Queue générique ajoute une indirection (méthodes) sur un simple tableau : négligeable en lisibilité gagnée, mais inutile si le besoin est ponctuel. Piège \`shift\` O(n) déjà cité. Et sur une Stack, oublier de tester la vacuité avant \`pop\`/\`peek\` renvoie \`undefined\` silencieux — d'où l'intérêt d'un \`isEmpty()\` explicite.\n\nLa règle du jour : **quand un problème parle d'imbrication ou de « le dernier compte d'abord », pense Stack (LIFO) ; quand il parle d'ordre d'arrivée ou de traitement équitable, pense Queue (FIFO). La bonne discipline d'accès transforme la difficulté — et méfie-toi du \`shift\` O(n) pour une file.**`,
    guided: `**Énoncé** : implémenter \`parenthesesValides(str)\` avec une Stack — vérifier que \`()\`, \`[]\`, \`{}\` sont correctement imbriqués. C'est l'application canonique de LIFO.\n\n**Exemple simple** : pourquoi une pile ? Dans \`([])\`, la dernière ouvrante \`[\` doit se fermer AVANT la première \`(\`. « Le dernier ouvert se ferme en premier » = LIFO exactement. J'empile chaque ouvrante ; sur une fermante, je vérifie qu'elle correspond au sommet (le dernier ouvert).\n\n**Raisonnement pas-à-pas** : (1) je parcours les caractères ; (2) ouvrante → je l'empile ; (3) fermante → si la pile est vide, il y a une fermante orpheline → invalide ; sinon je dépile et vérifie la correspondance ; (4) à la fin, la pile doit être VIDE (sinon des ouvrantes n'ont jamais été fermées).\n\n**Implémentation** (avec une Stack générique) :\n\`\`\`ts\nclass Stack<T> {\n  private items: T[] = [];\n  push(x: T): void { this.items.push(x); }          // O(1)\n  pop(): T | undefined { return this.items.pop(); }  // O(1)\n  peek(): T | undefined { return this.items[this.items.length - 1]; }\n  isEmpty(): boolean { return this.items.length === 0; }\n  size(): number { return this.items.length; }\n}\n\nfunction parenthesesValides(str: string): boolean {\n  const pile = new Stack<string>();\n  const paires: Record<string, string> = { ')': '(', ']': '[', '}': '{' };\n  for (const c of str) {\n    if (c === '(' || c === '[' || c === '{') {\n      pile.push(c);                       // ouvrante : empiler\n    } else if (c in paires) {\n      if (pile.pop() !== paires[c]) return false;  // fermante : le sommet doit correspondre\n    }\n  }\n  return pile.isEmpty();                   // reste-t-il des ouvrantes non fermées ?\n}\n\nconsole.log(parenthesesValides('([]{})'));  // true\nconsole.log(parenthesesValides('([)]'));    // false : mauvais imbriquement\nconsole.log(parenthesesValides('(('));      // false : ouvrantes en trop\nconsole.log(parenthesesValides('))'));      // false : fermante orpheline\n\`\`\`\n\n**Le piège Queue à connaître** : une file naïve sur \`shift()\` est O(n) par défilement. Version O(1) à deux index :\n\`\`\`ts\nclass Queue<T> {\n  private items: T[] = [];\n  private tete = 0;                          // on n'utilise JAMAIS shift()\n  enqueue(x: T): void { this.items.push(x); }        // O(1)\n  dequeue(): T | undefined { return this.items[this.tete++]; }  // O(1) amorti\n  isEmpty(): boolean { return this.tete >= this.items.length; }\n}\n\`\`\``,
    quiz: [
      { q: "Quelle est la différence de discipline entre une Stack et une Queue ?", a: "La Stack est LIFO (dernier entré, premier sorti) ; la Queue est FIFO (premier entré, premier sorti). La Stack modélise l'imbrication, la Queue l'ordre d'arrivée." },
      { q: "Pourquoi une Queue implémentée avec `shift()` est-elle un problème ?", a: "`shift()` retire en TÊTE d'un tableau et DÉCALE tous les autres éléments : O(n) par opération. Sur gros volume, la file devient quadratique. Il faut deux index ou une liste chaînée pour O(1)." },
      { q: "Pourquoi `parenthesesValides` doit-elle vérifier que la pile est VIDE à la fin ?", a: "Une pile non vide signifie des ouvrantes jamais fermées (ex. `((`). Sans ce test, la chaîne serait déclarée valide à tort." },
      { q: "Quel lien y a-t-il entre une Stack et la récursion ?", a: "La récursion utilise implicitement une pile : la « call stack ». Chaque appel empile son contexte, chaque retour le dépile — LIFO. Une récursion peut se dérouler en pile explicite." },
    ],
    caseStudy: `Stacks et Queues structurent des mécanismes qu'on utilise tous les jours sans les nommer. Stack : la fonction annuler/rétablir de tout éditeur (chaque action empilée, undo = dépiler), l'historique de navigation « précédent » d'un navigateur, l'évaluation des expressions dans un interpréteur, la pile d'exécution qui trace tes erreurs. Queue : les files de messages (RabbitMQ, SQS) qui découplent producteurs et consommateurs, les files de tâches d'un worker asynchrone, l'ordonnancement équitable des requêtes d'un serveur. Choisir la bonne discipline est un acte de conception : un système de tâches qui traiterait le DERNIER arrivé d'abord (Stack au lieu de Queue) affamerait les premières tâches. En entretien, « valide ces parenthèses » ou « implémente une file avec deux piles » testent la maîtrise fine de ces disciplines et de leurs coûts.`,
    interview: `**« Implémente une fonction qui vérifie qu'une chaîne de parenthèses \`()[]{}\` est bien équilibrée. »** → C'est l'application canonique d'une pile (LIFO), parce que la règle « la dernière parenthèse ouverte doit se fermer en premier » EST du dernier-entré-premier-sorti. Je parcours la chaîne : chaque ouvrante, je l'empile ; chaque fermante, je dépile et je vérifie que l'ouvrante dépilée correspond bien — sinon la chaîne est invalide (mauvais imbriquement ou fermante orpheline si la pile est vide). À la fin, la pile doit être VIDE ; sinon des ouvrantes n'ont jamais été fermées. C'est O(n) en temps et O(n) en mémoire dans le pire cas (que des ouvrantes). **Relance possible** : « et si je te demande d'implémenter une Queue avec deux Stacks ? » → j'empile les entrées sur une pile « in » ; pour défiler, si la pile « out » est vide je transvide « in » dedans (ce qui inverse l'ordre → FIFO), puis je dépile « out ». Chaque élément est transvidé au plus une fois → O(1) amorti par opération, ce qui montre que je maîtrise le coût réel, pas juste l'API.`,
    future: `- **Bankable** : la validation de parenthèses et « implémente une file avec deux piles » sont des classiques d'entretien ; maîtriser LIFO/FIFO et leurs coûts O(1) est un acquis de base attendu.\n- La discipline d'accès est un réflexe de conception : files de messages, workers asynchrones (mois 10), historique undo/redo dans une UI.\n- La Queue est l'outil du BFS (jour 34) et la Stack éclaire la récursion (call stack) — ces structures reviennent tout au long du parcours.`,
    solution: {
      logic: "Encapsuler la DISCIPLINE d'accès dans un type générique : Stack (push/pop/peek en O(1) sur un tableau) et Queue (enqueue/dequeue en O(1) via deux index, JAMAIS shift). Puis appliquer : parenthesesValides empile les ouvrantes et vérifie la correspondance au dépilement, exigeant une pile vide à la fin ; la file FIFO ordonne un traitement. La compétence : reconnaître QUELLE discipline modélise le problème.",
      simple: "Stack et Queue sur un tableau avec les opérations natives (push/pop pour la pile ; push/shift pour la file). parenthesesValides directe. Correct fonctionnellement, mais la file en shift() est O(n).",
      improved: "Queue O(1) à deux index (ou liste chaînée) pour éviter le coût de shift, isEmpty() explicite pour éviter les undefined silencieux, et gestion soignée des cas d'échec de parenthesesValides (fermante orpheline ET ouvrante en trop). Savoir citer un cas d'usage réel de chaque structure et le lien Stack↔récursion.",
      pitfalls: ["Implémenter la Queue avec shift() : O(n) par défilement, quadratique sur gros volume — mentionner la version à deux index.", "Oublier le cas « ouvrantes en trop » : ne pas tester que la pile est vide à la fin déclare `((` valide à tort.", "Oublier le cas « fermante orpheline » : dépiler une pile vide (pop → undefined) sans le traiter.", "Ne pas tester isEmpty avant pop/peek : renvoie undefined silencieusement au lieu de signaler l'erreur."],
      checks: ["Stack et Queue génériques compilent en strict et passent des tests manuels (push/pop/peek/isEmpty/size).", "parenthesesValides gère les imbrications correctes ET les quatre cas d'échec (mauvais ordre, ouvrante en trop, fermante orpheline, vide).", "La Queue ne fait aucun shift() : dequeue reste O(1) sur 100k éléments.", "Un cas d'usage réel de la Stack et de la Queue est cité et justifié."],
      oral: "Ouvre sur la vraie valeur : « ce ne sont pas des conteneurs, ce sont des disciplines — LIFO pour l'imbrication, FIFO pour l'ordre d'arrivée ». Démontre parenthesesValides en insistant sur les deux tests souvent oubliés (pile vide à la fin, fermante orpheline). Puis marque des points en signalant spontanément le piège shift() O(n) et le lien Stack↔call stack : ça prouve que tu penses en COÛTS et en sémantique, pas juste en syntaxe.",
    },
  },

  // ── Linked lists, arbres BST : coûts et parcours ──
  34: {
    lessons: ['data-structures-intro.md', 'algorithmic-thinking.md'],
    theory: `**Modèle mental : une structure de données est un COMPROMIS matérialisé — elle rend certaines opérations rapides en en rendant d'autres lentes. La liste chaînée échange l'accès indexé (O(n)) contre l'insertion en tête (O(1)). Le BST échange l'ordre d'insertion contre une recherche O(log n) — À CONDITION d'être équilibré. Il n'existe pas de structure « meilleure » dans l'absolu : il n'y a que des compromis adaptés (ou non) à un profil d'accès.** Comprendre une structure, c'est savoir ce qu'elle rend cher pour comprendre ce qu'elle rend gratuit.\n\nLa liste chaînée, depuis les fondations. Un tableau stocke ses éléments CONTIGUS en mémoire : l'index i se calcule (adresse de base + i × taille) → accès O(1), mais insérer en tête oblige à DÉCALER tout le reste → O(n). Une liste chaînée stocke des NŒUDS dispersés, chacun pointant vers le suivant : insérer en tête = créer un nœud qui pointe l'ancienne tête → O(1), mais accéder au n-ième = suivre n pointeurs → O(n). Le tableau et la liste sont donc des MIROIRS : ce que l'un rend rapide, l'autre le rend lent.\n\nLe BST, depuis les fondations. Un arbre binaire de recherche range chaque valeur selon une règle : plus petites À GAUCHE, plus grandes À DROITE. Chercher revient à la recherche binaire du jour 16, mais EN STRUCTURE : à chaque nœud, on élimine une moitié du sous-arbre → O(hauteur). Si l'arbre est équilibré, hauteur ≈ log₂(n) → recherche O(log n). Le parcours « in-order » (gauche, nœud, droite) ressort les valeurs TRIÉES — c'est LA propriété qui justifie le BST : il maintient un ordre trié tout en permettant insertion et recherche rapides.\n\nLa différence entre les parcours (DFS vs BFS). Le **DFS** (parcours en profondeur) plonge dans une branche jusqu'au bout avant de remonter — c'est la récursion naturelle de l'arbre (in-order, pre-order, post-order selon QUAND on visite le nœud). Le **BFS** (parcours en largeur) visite niveau par niveau, à l'aide d'une QUEUE (jour 33) : on enfile la racine, puis on défile un nœud en enfilant ses enfants. DFS utilise la pile (implicite via récursion), BFS utilise la file : le choix dépend de si l'on cherche « en profondeur » (un chemin) ou « au plus proche » (le niveau le plus court).\n\nLe piège majeur et son remède. Insérer des valeurs DÉJÀ TRIÉES dans un BST le fait DÉGÉNÉRER en liste chaînée : chaque insertion va toujours à droite, la hauteur devient n, la recherche retombe à O(n). C'est pourquoi les BST « auto-équilibrés » (AVL, rouge-noir — à connaître de NOM) existent : ils réorganisent l'arbre à chaque insertion pour garantir une hauteur logarithmique.\n\nLimites et compromis en pratique. En réalité, le tableau bat SOUVENT la liste chaînée malgré une meilleure complexité théorique de la liste, à cause du CACHE CPU : les éléments contigus d'un tableau se chargent par blocs, alors que les nœuds dispersés d'une liste provoquent des défauts de cache. La complexité asymptotique n'est pas toute la vérité — le matériel compte.\n\nLa règle du jour : **choisis une structure selon son PROFIL D'ACCÈS : liste chaînée pour l'insertion/suppression fréquente sans besoin d'index ; BST (équilibré !) pour maintenir un ensemble trié avec recherche O(log n). Connais le piège du BST dégénéré (données triées) et le rôle du cache CPU qui favorise souvent le tableau en pratique.**`,
    guided: `**Énoncé** : implémenter un BST minimal (insert, contains, inorder) et PROUVER que le parcours in-order ressort les valeurs triées.\n\n**Exemple simple** : insérer \`5, 3, 8, 1, 4\`. On part de 5 (racine). 3 < 5 → à gauche. 8 > 5 → à droite. 1 < 5, < 3 → à gauche de 3. 4 < 5, > 3 → à droite de 3. In-order (gauche, nœud, droite) parcourt : 1, 3, 4, 5, 8 → TRIÉ. La structure encode l'ordre ; le parcours le révèle.\n\n**Raisonnement pas-à-pas** : (1) insert : comparer la valeur au nœud courant, descendre à gauche si plus petite, à droite si plus grande, s'arrêter à la place vide ; (2) contains : même descente, on élimine une moitié à chaque étape → O(hauteur) ; (3) inorder : récursion gauche → visiter le nœud → récursion droite ; comme tout le sous-arbre gauche est plus petit et le droit plus grand, l'ordre de visite est croissant.\n\n**Implémentation** :\n\`\`\`ts\nclass Noeud {\n  constructor(public valeur: number,\n              public gauche: Noeud | null = null,\n              public droite: Noeud | null = null) {}\n}\n\nclass BST {\n  private racine: Noeud | null = null;\n\n  insert(v: number): void {\n    this.racine = this.inserer(this.racine, v);\n  }\n  private inserer(n: Noeud | null, v: number): Noeud {\n    if (n === null) return new Noeud(v);           // place vide trouvée\n    if (v < n.valeur) n.gauche = this.inserer(n.gauche, v);\n    else if (v > n.valeur) n.droite = this.inserer(n.droite, v);\n    return n;                                      // (doublons ignorés)\n  }\n\n  contains(v: number): boolean {\n    let n = this.racine;\n    while (n) {                                    // O(hauteur) : on élimine une moitié\n      if (v === n.valeur) return true;\n      n = v < n.valeur ? n.gauche : n.droite;\n    }\n    return false;\n  }\n\n  inorder(): number[] {\n    const out: number[] = [];\n    const parcourir = (n: Noeud | null) => {\n      if (!n) return;\n      parcourir(n.gauche);   // 1. tout ce qui est plus petit\n      out.push(n.valeur);    // 2. le nœud\n      parcourir(n.droite);   // 3. tout ce qui est plus grand\n    };\n    parcourir(this.racine);\n    return out;\n  }\n}\n\nconst arbre = new BST();\n[5, 3, 8, 1, 4].forEach(v => arbre.insert(v));\nconsole.log(arbre.inorder());     // [1, 3, 4, 5, 8]  -> trié, PROUVÉ\nconsole.log(arbre.contains(4));   // true\nconsole.log(arbre.contains(7));   // false\n\`\`\`\n\n**La démonstration du piège** : insère \`1, 2, 3, 4, 5\` (déjà triés) → chaque valeur va à droite → l'arbre est une ligne, hauteur 5 = n → \`contains\` devient O(n). Tu VOIS pourquoi les arbres auto-équilibrés existent.`,
    quiz: [
      { q: "Pourquoi insérer en tête d'une liste chaînée est-il O(1) alors que dans un tableau c'est O(n) ?", a: "La liste crée un nœud qui pointe l'ancienne tête : rien à déplacer. Le tableau doit DÉCALER tous les éléments existants d'un cran pour libérer l'index 0." },
      { q: "Pourquoi le parcours in-order d'un BST ressort-il les valeurs triées ?", a: "Par construction, tout le sous-arbre gauche d'un nœud est plus petit, tout le droit plus grand. Visiter gauche → nœud → droite parcourt donc les valeurs en ordre croissant." },
      { q: "Que se passe-t-il si on insère des valeurs déjà triées dans un BST ?", a: "Chaque insertion part du même côté : l'arbre dégénère en liste chaînée (hauteur = n), et la recherche retombe de O(log n) à O(n). D'où les arbres auto-équilibrés (AVL, rouge-noir)." },
      { q: "DFS et BFS : lequel utilise une pile, lequel une file ?", a: "DFS (profondeur) utilise une pile — implicitement, la call stack de la récursion. BFS (largeur) utilise une file : on enfile la racine, on défile en enfilant les enfants, niveau par niveau." },
    ],
    caseStudy: `Les structures chaînées et arborescentes fondent des systèmes bien réels. Arbres de recherche équilibrés : les INDEX de bases de données (B-trees, cousins des BST) qui rendent une requête SQL sur des millions de lignes quasi instantanée (jour 58) reposent exactement sur « chaque comparaison élimine une fraction des données ». Le système de fichiers, le DOM d'une page web, l'arbre de syntaxe d'un compilateur sont des arbres parcourus en DFS/BFS. Listes chaînées : les files de messages, les historiques, certaines implémentations de LRU cache. Comprendre le piège du BST dégénéré, c'est comprendre pourquoi un index sur une colonne auto-incrémentée mal géré peut se déséquilibrer. En entretien, « inverse un arbre binaire », « trouve la profondeur », « parcours en largeur » sont des grands classiques ; savoir choisir DFS vs BFS et raisonner sur la hauteur est directement évalué.`,
    interview: `**« Explique la différence de coûts entre un tableau et une liste chaînée, puis pourquoi un BST permet une recherche en O(log n). »** → Le tableau et la liste sont des miroirs. Le tableau range ses éléments contigus : accès indexé O(1) (l'adresse se calcule), mais insertion en tête O(n) (il faut tout décaler). La liste chaînée range des nœuds dispersés reliés par des pointeurs : insertion en tête O(1) (on crée un nœud qui pointe l'ancienne tête), mais accès au n-ième O(n) (suivre n pointeurs). Pour le BST : il range plus petit à gauche, plus grand à droite, donc chercher élimine une moitié du sous-arbre à chaque niveau — c'est la recherche binaire en structure, O(hauteur), soit O(log n) SI l'arbre est équilibré. Le parcours in-order (gauche, nœud, droite) en ressort les valeurs triées. Le piège crucial : insérer des données déjà triées dégénère le BST en liste chaînée (hauteur n, recherche O(n)), d'où les arbres auto-équilibrés comme AVL ou rouge-noir. **Relance possible** : « et en pratique, le tableau ne bat-il pas souvent la liste ? » → si, à cause du cache CPU : les éléments contigus d'un tableau se chargent par blocs, alors que les nœuds dispersés d'une liste provoquent des défauts de cache — la complexité asymptotique ne dit pas tout, le matériel compte.`,
    future: `- **Bankable** : les questions sur les arbres (inversion, profondeur, BFS/DFS) et les coûts tableau/liste sont des classiques d'entretien ; savoir raisonner sur la hauteur et le BST dégénéré est un signal de solidité.\n- Les BST/B-trees fondent les INDEX de bases de données (jour 58) : comprendre « chaque comparaison élimine une moitié » explique pourquoi un index accélère une requête.\n- DFS/BFS servent à parcourir tout arbre ou graphe : DOM, système de fichiers, dépendances — et la file du BFS réutilise directement la Queue du jour 33.`,
    solution: {
      logic: "Traiter chaque structure comme un COMPROMIS et l'implémenter pour le vérifier. Liste chaînée : nœuds reliés par pointeurs, insertion en tête O(1), accès O(n), à prouver par un benchmark contre le tableau. BST : insert/contains par descente comparative O(hauteur), inorder récursif (gauche/nœud/droite) qui ressort le tri, hauteur pour mesurer l'équilibre. BFS avec la Queue du jour 33. Point clé : montrer le BST qui dégénère sur des données triées.",
      simple: "LinkedList (append/prepend/removeAt/toArray) et BST (insert/contains/inorder) implémentés directement, testés sur de petits cas construits à la main. Suffisant pour comprendre les pointeurs.",
      improved: "Ajouter les benchmarks qui MATÉRIALISENT les compromis : insertion en tête liste vs unshift tableau (la liste gagne en théorie), et lecture séquentielle (le tableau gagne en pratique, cache CPU). Prouver l'inorder trié sur 20 insertions aléatoires, mesurer la hauteur, et DÉMONTRER la dégénérescence sur des données triées. Implémenter le BFS avec la Queue du jour 33 pour relier les structures.",
      pitfalls: ["Perdre le reste de la liste en réaffectant les pointeurs dans le mauvais ordre lors d'une insertion/suppression.", "Croire le BST toujours O(log n) : sur des insertions triées il dégénère en O(n) — d'où AVL/rouge-noir à citer.", "Confondre les parcours DFS (pile/récursion) et BFS (file) : le BFS a besoin d'une Queue explicite.", "Conclure « la liste bat le tableau » sur la seule complexité : le cache CPU favorise souvent le tableau en pratique."],
      checks: ["LinkedList : les 4 opérations correctes, y compris sur liste vide (removeAt hors bornes géré).", "BST : inorder ressort les valeurs triées, prouvé sur 20 insertions aléatoires.", "Le benchmark insertion-en-tête liste vs unshift tableau montre l'écart attendu et est expliqué.", "La dégénérescence du BST sur données triées est reproduite (hauteur = n) et commentée."],
      oral: "Structure ta réponse autour du mot COMPROMIS : « chaque structure rend une chose gratuite en en rendant une autre chère ». Oppose tableau et liste comme des miroirs, puis explique le BST par la recherche binaire en structure et prouve l'inorder trié. Marque des points en soulevant spontanément les deux nuances de maturité : le BST dégénéré (et son remède auto-équilibré) et le rôle du cache CPU — ça montre que tu vas au-delà de la complexité de manuel.",
    },
  },

  // ── Revue semaine 5 : structures de données appliquées ──
  35: {
    reviewSynthese: `La semaine 5 a transformé les structures de données de « notions théoriques » en OUTILS de résolution, chacune porteuse d'un compromis à savoir énoncer. Le réflexe hash map (jour 31) : échanger de la mémoire contre du temps pour faire passer une recherche de paire de O(n²) à O(n) — le kata twoSum et toute sa famille. Le backtracking (jour 32) : parcourir un arbre de décisions par choisir → explorer → défaire, avec un exponentiel imposé par le problème (2ⁿ, n!) qu'on distingue d'un exponentiel évitable. Les disciplines d'accès Stack (LIFO) et Queue (FIFO) (jour 33) : la bonne discipline rend un problème trivial, et gare au \`shift()\` O(n). Les structures chaînées et arborescentes (jour 34) : le tableau et la liste sont des miroirs, le BST offre O(log n) s'il est équilibré, et le cache CPU rappelle que la complexité ne dit pas tout. Le fil rouge de la semaine : **une structure de données n'est jamais « meilleure » dans l'absolu — elle rend certaines opérations rapides au prix d'autres ; choisir, c'est faire correspondre le profil d'accès du problème au compromis de la structure.** Tu entres dans la semaine 6 (TypeScript et POO) avec le socle algorithmique qui rend le reste du mois solide.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Reconnaître le pattern complément et écrire twoSum en O(n) avec une Map | |\n| Structurer un backtracking (choisir → explorer → défaire) et enregistrer une copie au cas de base | |\n| Distinguer un exponentiel imposé par le problème d'un exponentiel évitable | |\n| Choisir Stack (LIFO) ou Queue (FIFO) selon le problème et connaître le piège shift() O(n) | |\n| Implémenter une liste chaînée et un BST, expliquer leurs coûts | |\n| Prouver l'inorder trié et expliquer la dégénérescence d'un BST sur données triées | |\n| Distinguer DFS (pile/récursion) et BFS (file) | |\n\n**Total /21** : ≥ 17 = socle solide, passe à la semaine 6 · 12-16 = revoir les items ≤ 1 · < 12 = rejouer les jours concernés avant TypeScript, car ces réflexes sont le socle des entretiens techniques.`,
    remediation: `- **Double boucle réflexe / pas de Map** : rejoue le jour 31 — dès qu'une boucle interne CHERCHE, demande-toi si une Map la supprime ; chronomètre O(n) vs O(n²) pour ancrer l'écart.\n- **Backtracking bancal (résultats identiques)** : jour 32 — copie la solution partielle au cas de base (\`[...partiel]\`), défais (\`pop\`) entre les branches ; dessine l'arbre de [1,2,3].\n- **Confusion Stack/Queue ou shift() O(n)** : jour 33 — LIFO pour l'imbrication, FIFO pour l'ordre d'arrivée ; réimplémente la Queue à deux index.\n- **BST cru toujours O(log n)** : jour 34 — reproduis la dégénérescence sur données triées (hauteur = n) et nomme AVL/rouge-noir comme remède.\n- **DFS/BFS confondus** : jour 34 — DFS = pile/récursion (un chemin en profondeur), BFS = file (niveau par niveau, plus court chemin en nombre d'arêtes).`,
    interview: `1. **« twoSum : optimise la solution naïve. »** Attendu : reconnaître le pattern complément, Map valeur→index, O(n) temps contre O(n) mémoire, chercher avant d'insérer, savoir citer l'alternative tri + deux pointeurs.\n2. **« Génère tous les sous-ensembles / permutations d'un tableau. »** Attendu : arbre de décisions, squelette choisir/explorer/défaire, copie au cas de base, complexité 2ⁿ / n! imposée par le problème.\n3. **« Valide une chaîne de parenthèses. »** Attendu : pile LIFO, correspondance au dépilement, pile vide à la fin, gestion des fermantes orphelines et ouvrantes en trop.\n4. **« Implémente une file avec deux piles. »** Attendu : transvider « in » vers « out » à vide, O(1) amorti, compréhension du coût réel derrière l'API.\n5. **« Pourquoi un BST peut-il devenir O(n) et comment un tableau peut-il battre une liste chaînée ? »** Attendu : dégénérescence sur données triées (→ AVL/rouge-noir), et cache CPU qui favorise la contiguïté du tableau.`,
  },

  // ────────────────────────────────────────────────────────────────────────
  // MOIS 2 · Semaine 6 (jours 36-41) : TypeScript, POO, design patterns
  // ────────────────────────────────────────────────────────────────────────

  // ── TypeScript : le typage qui attrape les bugs avant l'exécution ──
  36: {
    lessons: ['typescript-basics.md'],
    theory: `**Modèle mental : TypeScript ne change RIEN à ce que ton code fait — il ajoute un CONTRAT vérifié À LA COMPILATION, par-dessus JavaScript. Le compilateur \`tsc\` est un relecteur infatigable qui lit tout ton code AVANT exécution et signale les incohérences (mauvais type d'argument, propriété inexistante, cas \`null\` oublié). Le typage est une documentation qui ne peut pas mentir, parce que la machine la vérifie partout, pour toujours. Les erreurs \`tsc\` ne sont pas des obstacles : ce sont des bugs futurs attrapés gratuitement.** Apprendre à LIRE ces erreurs est la vraie compétence — plus que la syntaxe des annotations.\n\nPourquoi le typage statique existe. JavaScript est dynamiquement typé : une variable peut être un nombre puis une chaîne, et une faute (appeler \`.toUpperCase()\` sur un nombre) n'explose qu'À L'EXÉCUTION, parfois en production chez l'utilisateur. Le coût d'un bug croît avec le temps avant sa détection. Le typage statique DÉPLACE la détection au plus tôt — pendant que tu écris, dans l'éditeur — là où corriger coûte quelques secondes. C'est la même logique que les tests, mais gratuite et permanente.\n\nComment ça fonctionne, depuis les fondations. Tu ANNOTES les contrats aux frontières (signatures de fonctions, formes d'objets) : \`function payer(montant: number): Recu\`. Le compilateur PROPAGE ces types par INFÉRENCE : tu n'écris pas \`const total: number = a + b\`, il déduit \`number\` seul. Puis il VÉRIFIE la cohérence de tout le graphe d'appels. À l'exécution, tout ce typage DISPARAÎT (transpilation en JS) : c'est un filet de sécurité au développement, à coût zéro à l'exécution.\n\nLa différence entre les acteurs, pour ne pas confondre. \`tsc\` VÉRIFIE et transpile ; \`node\` (ou \`tsx\`) EXÉCUTE. Une erreur \`tsc\` n'est PAS une erreur d'exécution : c'est un avertissement anticipé. \`any\` DÉSACTIVE le vérificateur pour une valeur — c'est renoncer à l'outil, à bannir. \`unknown\` dit « type inconnu pour l'instant » et FORCE une vérification (narrowing) avant tout usage : c'est la bonne réponse aux données externes (JSON d'une API, saisie utilisateur) dont on ne connaît pas la forme à la compilation.\n\nLe mode strict et son intérêt. \`strict: true\` active notamment \`strictNullChecks\` : \`null\` et \`undefined\` ne sont plus assignables implicitement partout. Résultat : le compilateur t'OBLIGE à traiter le cas « la valeur pourrait être absente » — exactement la classe de bugs (\`Cannot read property of undefined\`) qui hante le JavaScript. La moitié des bugs du mois 1 auraient été attrapés à la compilation.\n\nLimites et compromis. Le typage a un COÛT d'écriture (annotations, batailles avec le compilateur au début) et une COURBE d'apprentissage. Il ne garantit pas la CORRECTION logique (un type juste peut cacher une mauvaise formule) : il élimine une CLASSE de bugs (les incohérences de type), pas tous. Et les types peuvent devenir complexes au point de nuire à la lisibilité — le bon dosage est de typer les frontières et laisser l'inférence faire le reste.\n\nLa règle du jour : **type les CONTRATS aux frontières, laisse l'inférence propager, active \`strict\`, bannis \`any\` au profit de \`unknown\` + validation, et surtout apprends à LIRE les erreurs du compilateur comme des bugs futurs attrapés gratuitement — c'est la compétence, pas la syntaxe.**`,
    guided: `**Énoncé** : convertir une petite fonction JavaScript en TypeScript strict et observer un bug RÉVÉLÉ par le typage.\n\n**Exemple simple** : une fonction JS qui « marche »… parfois.\n\`\`\`js\n// version JS : aucun garde-fou\nfunction prixTTC(prixHT, taux) {\n  return prixHT * (1 + taux);\n}\nprixTTC(100, 0.2);      // 120  ✅\nprixTTC(\"100\", 0.2);    // \"1001.2\"  ❌ concaténation silencieuse !\n\`\`\`\n\n**Raisonnement pas-à-pas** : (1) j'annote les paramètres et le retour avec les types attendus ; (2) je compile en strict ; (3) le compilateur REFUSE l'appel fautif AVANT toute exécution ; (4) je lis l'erreur, qui pointe la ligne et la cause exacte.\n\n**Implémentation** :\n\`\`\`ts\nfunction prixTTC(prixHT: number, taux: number): number {\n  return prixHT * (1 + taux);\n}\n\nprixTTC(100, 0.2);       // 120  ✅\nprixTTC(\"100\", 0.2);     // ❌ tsc : Argument of type 'string' is not\n                         //    assignable to parameter of type 'number'.\n\`\`\`\nLe bug de concaténation (\`\"100\" * ...\`) qui aurait produit un prix faux en production est attrapé à la compilation, avec un message qui DÉSIGNE le coupable.\n\n**any vs unknown sur une donnée externe** :\n\`\`\`ts\nfunction traiter(donnee: unknown): number {\n  // interdit d'utiliser 'donnee' sans vérifier : le compilateur FORCE le narrowing\n  if (typeof donnee === 'number') return donnee * 2;   // ici donnee est number\n  throw new Error('attendu: number');\n}\n// avec 'any' à la place de 'unknown', donnee * 2 passerait SANS vérification -> bug latent\n\`\`\``,
    quiz: [
      { q: "Quelle est la différence entre `tsc` et `node` ?", a: "`tsc` VÉRIFIE les types et transpile en JavaScript, avant exécution. `node` EXÉCUTE le JavaScript. Une erreur `tsc` est un avertissement anticipé, pas une erreur d'exécution." },
      { q: "Pourquoi préférer `unknown` à `any` pour une donnée externe ?", a: "`any` désactive toute vérification (bug latent possible). `unknown` FORCE à vérifier le type (narrowing) avant d'utiliser la valeur — sûr par défaut pour un JSON d'API ou une saisie." },
      { q: "Qu'apporte concrètement le mode `strict` (strictNullChecks) ?", a: "Il rend `null`/`undefined` non assignables implicitement : le compilateur oblige à traiter le cas « valeur absente », éliminant la classe des « Cannot read property of undefined »." },
      { q: "Le typage garantit-il que mon code est correct ?", a: "Non : il élimine une CLASSE de bugs (incohérences de type), pas les erreurs de LOGIQUE. Un type juste peut cacher une formule fausse. Le typage complète les tests, il ne les remplace pas." },
    ],
    caseStudy: `L'adoption massive de TypeScript en entreprise (front comme back Node) tient à une raison économique : sur une base de code de plusieurs dizaines de milliers de lignes maintenue par une équipe, le typage attrape à la compilation des régressions qui, sinon, ne se révéleraient qu'en production. Renommer un champ, changer une signature, supprimer une propriété : le compilateur liste INSTANTANÉMENT tous les endroits à corriger, là où en JS il faudrait une couverture de tests parfaite (illusoire) ou des bugs découverts par les utilisateurs. C'est aussi une documentation vivante : un nouveau développeur lit les types pour comprendre les contrats sans deviner. Concrètement, savoir écrire du TypeScript strict, lire ses erreurs et manier \`unknown\` aux frontières est aujourd'hui une exigence de base sur la plupart des offres front et back JS — pas un plus.`,
    interview: `**« Pourquoi typer du JavaScript avec TypeScript, et quelle est la différence entre \`any\` et \`unknown\` ? »** → Le typage ajoute un contrat vérifié À LA COMPILATION, sans rien changer à l'exécution : il déplace la détection des bugs de type au plus tôt (dans l'éditeur, où corriger coûte quelques secondes) au lieu de la laisser exploser en production. C'est une documentation qui ne peut pas mentir, parce que le compilateur la vérifie partout. En mode strict, il attrape notamment les \`null\`/\`undefined\` oubliés — la classe de bugs la plus fréquente en JS. Pour \`any\` vs \`unknown\` : \`any\` DÉSACTIVE le vérificateur pour une valeur, on retombe dans les risques du JS non typé — à bannir. \`unknown\` dit « type inconnu » et FORCE une vérification (narrowing) avant tout usage : c'est la réponse correcte aux données externes (JSON d'API, saisie) dont on ignore la forme à la compilation. **Relance possible** : « le typage garantit-il un code correct ? » → non : il élimine les incohérences de type, pas les erreurs de logique ; un type juste peut cacher une formule fausse — le typage complète les tests, il ne les remplace pas.`,
    future: `- **Bankable** : maîtriser TypeScript strict, lire ses erreurs et manier \`unknown\` aux frontières est une exigence de base sur la majorité des offres front et back JS aujourd'hui.\n- Le réflexe « typer les contrats aux frontières » prépare directement les types avancés (jour 37), la POO typée (jour 38) et tout le Projet 1 TaskFlow.\n- L'idée « déplacer la détection des bugs au plus tôt » est la même que celle des tests (jour 47) et de la validation aux frontières (jour 54) — un principe transversal de fiabilité.`,
    solution: {
      logic: "Comprendre que TypeScript vérifie des CONTRATS à la compilation sans changer l'exécution. La démarche de conversion : annoter les signatures (paramètres, retour), laisser l'inférence faire le reste à l'intérieur, activer strict, remplacer chaque any par un type précis ou unknown + validation, puis LIRE chaque erreur tsc comme un bug futur désigné. La preuve de compréhension : documenter au moins un bug réel révélé par le typage.",
      simple: "Ajouter les annotations de types aux signatures des 3 fichiers, corriger jusqu'à zéro erreur tsc. Fonctionnel, mais on peut rester superficiel (any qui traîne).",
      improved: "Activer strict et viser ZÉRO any : remplacer les any par des types précis ou unknown + narrowing aux frontières (données externes), traiter explicitement les cas null/undefined, et TENIR un journal des bugs que le typage a révélés (ex. concaténation silencieuse, propriété absente). Savoir expliquer any vs unknown avec un exemple.",
      pitfalls: ["Mettre `any` pour faire taire le compilateur : c'est désactiver l'outil et réintroduire les bugs du JS non typé.", "Croire que tsc exécute le code : il VÉRIFIE puis transpile ; c'est node (ou tsx) qui exécute.", "Sur-annoter en dupliquant ce que l'inférence déduit déjà (`const x: number = 2`) : bruit inutile.", "Croire qu'un code qui compile est forcément correct : le typage n'attrape pas les erreurs de logique."],
      checks: ["Les 3 fichiers convertis compilent en strict, zéro any (vérifiable par recherche `any` dans le code).", "Au moins un bug réel révélé par le typage est documenté dans les notes.", "Les données externes passent par `unknown` + narrowing, pas par `any`.", "Les cas null/undefined possibles sont traités explicitement (pas d'accès non gardé)."],
      oral: "Formule l'idée forte : « TypeScript ne change pas ce que le code fait, il ajoute un contrat vérifié qui déplace les bugs de type de la production vers l'éditeur ». Montre un exemple de bug attrapé (concaténation silencieuse), oppose any (désactive) et unknown (force la vérification), et conclus par l'honnêteté : « ça élimine une classe de bugs, pas la logique fausse ». Savoir LIRE une erreur tsc à voix haute, en la reliant à sa cause, est le signal recherché.",
    },
  },

  // ── Types avancés : interfaces, unions, littéraux, génériques ──
  37: {
    lessons: ['typescript-basics.md'],
    theory: `**Modèle mental : les types avancés servent à rendre les états ILLÉGAUX IMPOSSIBLES à représenter. Une union littérale \`type Statut = 'panier' | 'payee' | 'expediee'\` transforme une simple chaîne en CONTRAT vérifié : la faute de frappe \`'payé'\` devient une erreur de compilation, et un \`switch\` sur le statut peut être prouvé EXHAUSTIF. Un générique \`<T>\` écrit UNE logique valable pour tous les types sans perdre le typage. Le but n'est pas d'annoter plus : c'est de modéliser le domaine si précisément que le compilateur refuse les valeurs absurdes.** Bien typé, le domaine se documente et se protège lui-même.\n\nPourquoi ces outils existent. Avec les types de base, on peut encore écrire des non-sens acceptés : un statut \`\"livré\"\` mal orthographié, un objet auquel il manque un champ, une fonction \`premier\` recopiée pour chaque type. Les types avancés ferment ces trous : les unions littérales bornent les valeurs possibles, les interfaces imposent la forme des objets, les génériques éliminent la duplication tout en gardant la vérification. C'est le passage de « le code compile » à « le code ne PEUT pas être dans un état incohérent ».\n\nComment ils fonctionnent, depuis les fondations. Une **interface** décrit la FORME d'un objet (ses propriétés et leurs types), extensible et implémentable. Un **type alias** nomme n'importe quel type, y compris les UNIONS (\`A | B\` : l'un OU l'autre) et intersections (\`A & B\` : les deux à la fois). Les **types littéraux** font d'une valeur constante un type (\`'payee'\` est un type qui n'accepte que cette chaîne) ; leur union borne un ensemble fini. Le **narrowing** est le mécanisme par lequel le compilateur AFFINE le type dans une branche : après \`if (typeof x === 'string')\`, \`x\` est \`string\` à l'intérieur. Un **générique** \`premier<T>(arr: T[], p: (x: T) => boolean): T | undefined\` paramètre le type : appelé sur des \`number[]\` il renvoie \`number | undefined\`, sur des \`Commande[]\` il renvoie \`Commande | undefined\` — une seule implémentation, typage préservé.\n\nLa différence interface vs type, souvent demandée. \`interface\` est privilégiée pour les objets extensibles (elle supporte la fusion de déclarations et l'\`implements\`). \`type\` est indispensable pour les unions, intersections, alias de primitives et types calculés. En pratique : interface pour la forme d'un objet, type pour tout le reste (surtout les unions). Les deux se recouvrent largement pour les objets simples.\n\nLe bénéfice décisif : l'exhaustivité vérifiée. Sur une union littérale, un \`switch\` couvrant tous les cas permet au compilateur de PROUVER qu'aucun cas n'est oublié (via un cas \`default\` typé \`never\`). Ajouter un statut demain provoque une erreur de compilation partout où il faut le gérer — le compilateur devient ta checklist de refactoring.\n\nLimites et compromis. Sur/sous-contraindre un générique nuit : \`<T>\` trop libre n'apporte rien, \`<T extends { id: number }>\` peut être nécessaire pour accéder à \`.id\`. Les types trop élaborés (conditionnels, mappés imbriqués) deviennent illisibles — la sophistication doit servir la clarté, pas l'exhiber. Et le typage reste effacé à l'exécution : il ne valide pas les données réelles (d'où la validation runtime, jour 54).\n\nLa règle du jour : **modélise le domaine pour rendre les états illégaux irreprésentables — unions littérales pour borner les valeurs, interfaces pour les formes, génériques pour une logique réutilisable typée. Vise l'exhaustivité vérifiée par le compilateur, sans sur-typer au point de nuire à la lisibilité.**`,
    guided: `**Énoncé** : modéliser une commande e-commerce avec un statut en union littérale, puis écrire une fonction générique \`premier<T>\` réutilisable.\n\n**Exemple simple** : le statut. Écrit \`statut: string\`, la valeur \`'payé'\` (accent, faute) passe silencieusement. Écrit \`statut: 'panier' | 'payee' | 'expediee'\`, cette même faute devient une erreur de compilation — le type EST le garde-fou.\n\n**Raisonnement pas-à-pas** : (1) je décris les formes avec des interfaces ; (2) je borne le statut par une union littérale ; (3) j'écris un \`switch\` exhaustif dont le \`default: never\` PROUVE qu'aucun cas n'est oublié ; (4) j'extrais la logique « trouver le premier élément qui satisfait un prédicat » en une fonction générique valable pour tout type.\n\n**Implémentation** :\n\`\`\`ts\ntype Statut = 'panier' | 'payee' | 'expediee';\n\ninterface Produit { id: number; nom: string; prix: number; }\ninterface LigneCommande { produit: Produit; quantite: number; }\ninterface Commande { id: number; lignes: LigneCommande[]; statut: Statut; }\n\nfunction messageStatut(c: Commande): string {\n  switch (c.statut) {\n    case 'panier':   return 'En cours de constitution';\n    case 'payee':    return 'Paiement reçu';\n    case 'expediee': return 'Colis expédié';\n    default:\n      const _exhaustif: never = c.statut;   // erreur de compil si un statut est oublié\n      return _exhaustif;\n  }\n}\n\n// Générique : UNE implémentation, typage préservé pour tout T\nfunction premier<T>(arr: T[], p: (x: T) => boolean): T | undefined {\n  for (const x of arr) if (p(x)) return x;\n  return undefined;\n}\n\nconst n = premier([1, 2, 3, 4], x => x % 2 === 0);        // number | undefined -> 2\nconst commandes: Commande[] = [];\nconst c = premier(commandes, cmd => cmd.statut === 'payee'); // Commande | undefined\n\`\`\`\nAjoute demain \`'remboursee'\` à \`Statut\` : le \`switch\` ne compile plus tant que tu ne l'as pas géré. Le compilateur t'a listé le travail.`,
    quiz: [
      { q: "Que signifie « rendre les états illégaux irreprésentables » ?", a: "Modéliser les types si précisément qu'une valeur absurde ne peut même pas être écrite : une union littérale interdit un statut mal orthographié, une interface interdit un objet auquel il manque un champ." },
      { q: "À quoi sert un `default: never` dans un switch sur une union littérale ?", a: "À obtenir l'exhaustivité vérifiée : si un cas de l'union n'est pas traité, l'affectation à `never` échoue à la compilation. Le compilateur devient la checklist de refactoring." },
      { q: "Quand préférer `interface` à `type` (et l'inverse) ?", a: "`interface` pour les formes d'objets extensibles (implements, fusion). `type` pour les unions, intersections, alias de primitives et types calculés. Union littérale = toujours `type`." },
      { q: "Qu'apporte un générique par rapport à dupliquer la fonction pour chaque type ?", a: "Une SEULE implémentation valable pour tous les types, avec le typage préservé : `premier<T>` renvoie `number|undefined` sur des nombres, `Commande|undefined` sur des commandes — sans `any` ni copier-coller." },
    ],
    caseStudy: `La modélisation par types précis est ce qui distingue une base de code robuste d'une base fragile en entreprise. Un domaine métier (commande, paiement, abonnement) modélisé par des unions littérales et des interfaces strictes rend des CLASSES ENTIÈRES de bugs impossibles : on ne peut pas mettre un statut invalide, oublier un champ obligatoire, ni ignorer un nouveau cas sans que le compilateur ne le signale. Les états illégaux irreprésentables, c'est moins de tests défensifs à écrire et moins d'incidents en production. Les génériques, eux, fondent les bibliothèques réutilisables (une fonction de tri, un client d'API typé) sans sacrifier la sécurité. Concrètement, savoir modéliser un domaine avec unions et interfaces, et écrire une fonction générique correctement contrainte, est exactement ce qu'un lead cherche dans une revue de code — et une source fréquente de questions d'entretien front/back TypeScript.`,
    interview: `**« Comment utiliserais-tu le système de types pour empêcher un statut de commande invalide, et qu'apporte un type générique ? »** → Je remplace \`statut: string\` par une union littérale \`'panier' | 'payee' | 'expediee'\` : dès lors, une valeur mal orthographiée devient une erreur de compilation — le type est le garde-fou. Mieux, un \`switch\` sur ce statut avec un cas \`default\` typé \`never\` me donne l'EXHAUSTIVITÉ vérifiée : si j'ajoute un statut demain, le code ne compile plus tant que je ne l'ai pas traité, donc le compilateur me sert de checklist de refactoring. C'est le principe « rendre les états illégaux irreprésentables ». Pour le générique : \`premier<T>(arr: T[], p: (x: T) => boolean): T | undefined\` écrit UNE seule logique valable pour tous les types en préservant le typage — sur des nombres il renvoie \`number | undefined\`, sur des commandes \`Commande | undefined\`, sans \`any\` ni duplication. C'est le mécanisme derrière \`Array\`, \`Map\`, \`Promise\`. **Relance possible** : « et si le générique doit accéder à une propriété commune ? » → je le CONTRAINS : \`<T extends { id: number }>\` autorise \`x.id\` tout en restant générique — sur/sous-contraindre est le principal piège des génériques.`,
    future: `- **Bankable** : modéliser un domaine par unions et interfaces, et écrire un générique bien contraint, sont des attendus de revue de code et des questions d'entretien TypeScript récurrentes.\n- « Rendre les états illégaux irreprésentables » est un principe de conception qui structure tout le Projet 1 TaskFlow (types de tâches, statuts) et au-delà.\n- Les génériques préparent la compréhension des bibliothèques typées (Array, Promise, clients d'API) et de la programmation fonctionnelle typée (jour 43).`,
    solution: {
      logic: "Utiliser les types pour interdire l'incohérence : interfaces pour les formes d'objets, union littérale pour borner un ensemble fini de valeurs (statut), switch exhaustif avec default:never pour que le compilateur signale tout cas oublié, et générique correctement contraint pour une logique réutilisable sans any. La preuve : ajouter un statut casse la compilation là où il faut agir.",
      simple: "Modéliser Produit/LigneCommande/Commande en interfaces, le statut en union littérale, et écrire premier<T> sans contrainte. Compile et fonctionne.",
      improved: "Ajouter le switch exhaustif à default:never (exhaustivité prouvée), contraindre le générique quand il accède à une propriété (<T extends {id:number}>), utiliser premier<T> sur DEUX types différents pour démontrer l'inférence, et garder zéro any avec des champs optionnels explicites (?). Savoir justifier interface vs type.",
      pitfalls: ["Laisser le statut en `string` au lieu d'une union littérale : une faute de frappe passe sans être détectée.", "Oublier l'exhaustivité (pas de default:never) : un cas non géré ne déclenche aucune erreur à l'ajout d'un statut.", "Générique sur- ou sous-contraint : `<T>` trop libre n'apporte rien ; sans `extends`, impossible d'accéder à une propriété commune.", "Croire que le typage valide les données réelles : il est effacé à l'exécution ; les données externes exigent une validation runtime (jour 54)."],
      checks: ["Le modèle de commande compile et le statut est une union littérale (une valeur invalide est refusée par tsc).", "Le switch est exhaustif : ajouter un statut fictif provoque une erreur de compilation.", "La fonction générique premier<T> est utilisée sur 2 types différents avec inférence correcte.", "Aucun any ; les champs optionnels sont explicites (?)."],
      oral: "Résume par le principe : « je modélise pour que les états illégaux soient impossibles à écrire ». Montre l'union littérale qui bloque une faute de frappe, puis le switch default:never qui transforme le compilateur en checklist de refactoring. Enchaîne sur le générique (une logique, tous les types, typage préservé) et son piège (bien le contraindre). Terminer par « le typage n'est pas effacé par hasard : à l'exécution, il faut encore valider les données externes » montre une compréhension complète.",
    },
  },

  // ── POO en TypeScript : encapsulation, héritage, polymorphisme ──
  38: {
    lessons: ['typescript-basics.md', 'design-patterns-intro.md'],
    theory: `**Modèle mental : les quatre piliers de la POO (encapsulation, abstraction, héritage, polymorphisme) servent UN seul but pratique — programmer contre une INTERFACE (un contrat) plutôt que contre une implémentation. Le polymorphisme est le cœur : une fonction \`alerter(notifieurs: Notifieur[])\` ne connaît QUE le contrat \`envoyer()\`, jamais les classes concrètes ; ajouter un canal Slack demain = une classe de plus, ZÉRO modification ailleurs. C'est le principe ouvert/fermé, et le germe de l'inversion de dépendance.** La POO bien comprise n'est pas « tout mettre en classes » : c'est isoler ce qui varie derrière un contrat stable.\n\nPourquoi la POO existe. À mesure qu'un programme grandit, le vrai défi est le COUPLAGE : quand ajouter une fonctionnalité oblige à modifier dix endroits, le code devient fragile. La POO propose de regrouper données + comportements dans des objets, de CACHER l'état interne (pour que personne ne dépende des détails), et d'exposer des contrats stables. Le bénéfice : on peut faire évoluer les implémentations derrière un contrat sans casser le reste.\n\nLes quatre piliers, depuis les fondations. **Encapsulation** : cacher l'état (\`private\`, \`readonly\`) et n'exposer que des méthodes — l'objet garantit ses propres invariants, personne ne le met dans un état incohérent de l'extérieur. **Abstraction** : exposer un contrat simple (\`envoyer(msg)\`) en masquant la complexité (l'API SMTP derrière Email). **Héritage** : une classe étend une autre (\`extends\`, \`super\`) pour réutiliser — puissant mais à manier avec parcimonie. **Polymorphisme** : traiter des objets de types différents via un contrat commun — le pilier qui donne la souplesse.\n\nLe polymorphisme par interface, en détail. On définit \`interface Notifieur { envoyer(msg: string): void }\`. Email, SMS, Push l'implémentent chacun à sa façon. \`alerter(notifieurs: Notifieur[], msg)\` itère et appelle \`envoyer\` sans savoir laquelle : c'est le CONTRAT qui garantit que l'appel a un sens. Ajouter Slack = écrire \`class Slack implements Notifieur\` — \`alerter\` n'est pas touchée. C'est l'open/closed (ouvert à l'extension, fermé à la modification) et l'inversion de dépendance (dépendre de l'abstraction, pas du concret — exactement ton interface \`Store\` du jour 44).\n\nLa différence héritage vs composition, et vs fonctionnel. L'**héritage** crée un lien fort « est-un » : un héritage profond (A extends B extends C) est FRAGILE, car un changement dans B casse A. La **composition** (« a-un » : assembler des comportements) est plus souple et souvent préférable — d'où la règle « composition > héritage ». Et une **fonction pure + closure** (jour 22) fait souvent le travail d'une petite classe : la classe se justifie quand on a un ÉTAT encapsulé avec des méthodes qui l'opèrent ; sinon, une fonction suffit.\n\nLimites et compromis. La POO ajoute de la CÉRÉMONIE (classes, interfaces) : sur-appliquée, elle noie une logique simple sous des couches inutiles. Tout mettre en classe par réflexe est un anti-pattern. Le bon jugement : classe pour un état + comportements liés ou pour du polymorphisme ; fonction pour une transformation sans état.\n\nLa règle du jour : **programme contre des contrats (interfaces), pas contre des implémentations. Encapsule l'état, exploite le polymorphisme pour être ouvert à l'extension et fermé à la modification, préfère la composition à l'héritage, et n'utilise une classe que quand un état encapsulé le justifie — sinon une fonction suffit.**`,
    guided: `**Énoncé** : construire un système de notifications polymorphe — une interface \`Notifieur\`, plusieurs implémentations, une fonction \`alerter\` qui ne dépend que du contrat.\n\n**Exemple simple** : la puissance du contrat. \`alerter\` écrite une fois pour toutes ne mentionne JAMAIS Email ni SMS ; elle appelle \`envoyer\` sur des \`Notifieur\`. C'est ce découplage qui rend l'ajout d'un canal indolore.\n\n**Raisonnement pas-à-pas** : (1) je définis le CONTRAT (\`interface Notifieur\`) ; (2) chaque canal l'implémente à sa manière ; (3) \`alerter\` prend une liste de \`Notifieur\` et appelle le contrat, sans connaître les classes ; (4) je PROUVE l'open/closed en ajoutant un canal sans toucher \`alerter\`.\n\n**Implémentation** :\n\`\`\`ts\ninterface Notifieur { envoyer(msg: string): void; }\n\nclass Email implements Notifieur {\n  constructor(private readonly adresse: string) {}   // état encapsulé\n  envoyer(msg: string): void { console.log(\`[Email->\${this.adresse}] \${msg}\`); }\n}\nclass SMS implements Notifieur {\n  constructor(private readonly numero: string) {}\n  envoyer(msg: string): void { console.log(\`[SMS->\${this.numero}] \${msg}\`); }\n}\n\n// Polymorphe : ne connaît QUE le contrat 'envoyer'\nfunction alerter(notifieurs: Notifieur[], msg: string): void {\n  for (const n of notifieurs) n.envoyer(msg);\n}\n\nalerter([new Email('a@ex.fr'), new SMS('0600')], 'Serveur down !');\n\n// OUVERT à l'extension : ajouter Slack ne modifie PAS alerter\nclass Slack implements Notifieur {\n  constructor(private readonly canal: string) {}\n  envoyer(msg: string): void { console.log(\`[Slack#\${this.canal}] \${msg}\`); }\n}\nalerter([new Slack('ops')], 'Nouveau canal, zéro modif ailleurs.');\n\`\`\`\n\n**La version fonctionnelle, pour comparer** :\n\`\`\`ts\ntype Notifier = (msg: string) => void;\nconst email = (adr: string): Notifier => msg => console.log(\`[Email->\${adr}] \${msg}\`);\nconst alerterFn = (ns: Notifier[], msg: string) => ns.forEach(n => n(msg));\n// Même découplage avec une fonction + closure : la classe se justifie surtout\n// quand l'objet porte PLUSIEURS méthodes et un état riche.\n\`\`\``,
    quiz: [
      { q: "Quel est le but pratique commun des quatre piliers de la POO ?", a: "Programmer contre une INTERFACE (un contrat) plutôt que contre une implémentation, pour réduire le couplage et pouvoir faire évoluer les détails sans casser le reste." },
      { q: "Pourquoi `alerter` illustre-t-elle le principe ouvert/fermé ?", a: "Elle ne dépend que du contrat `Notifieur.envoyer`. Ajouter un canal (Slack) = une nouvelle classe, sans MODIFIER `alerter` : ouverte à l'extension, fermée à la modification." },
      { q: "Pourquoi préférer la composition à l'héritage profond ?", a: "Un héritage profond (A extends B extends C) crée un couplage fort et fragile : changer B casse A. Composer (assembler des comportements) est plus souple et localise les changements." },
      { q: "Quand une classe se justifie-t-elle face à une fonction + closure ?", a: "Quand l'objet porte un ÉTAT encapsulé opéré par plusieurs méthodes liées. Pour une simple transformation sans état, une fonction pure suffit — tout mettre en classe est un anti-pattern." },
    ],
    caseStudy: `Le polymorphisme par interface est le mécanisme qui rend les systèmes d'entreprise EXTENSIBLES. Un service de paiement définit une interface \`MoyenPaiement\` ; Carte, PayPal, Virement l'implémentent ; le moteur de commande ne connaît que le contrat. Ajouter Apple Pay = une classe, sans toucher le moteur ni risquer de régression sur les moyens existants. Même schéma pour les exporteurs (PDF/CSV/Excel), les stratégies de tarification, les fournisseurs de stockage (S3/local/GCS). Ce découplage réduit le coût et le risque de chaque évolution — la valeur business directe d'une bonne conception objet. À l'inverse, un code où \`alerter\` contiendrait un \`if canal === 'email' … else if … \` doit être modifié (et re-testé) à chaque nouveau canal. Savoir concevoir contre des interfaces, et arbitrer classe vs fonction vs héritage vs composition, est évalué en entretien back et lors des revues de code.`,
    interview: `**« Explique le polymorphisme avec un exemple, et pourquoi il rend le code extensible. »** → Le polymorphisme, c'est traiter des objets de types différents via un CONTRAT commun. Exemple : une interface \`Notifieur { envoyer(msg) }\` implémentée par Email, SMS, Push. Une fonction \`alerter(notifieurs: Notifieur[])\` itère et appelle \`envoyer\` sans jamais connaître les classes concrètes — c'est le contrat qui garantit que l'appel a un sens. Le bénéfice : ajouter un canal Slack = écrire \`class Slack implements Notifieur\`, ZÉRO modification d'\`alerter\`. C'est le principe ouvert/fermé (ouvert à l'extension, fermé à la modification) et le germe de l'inversion de dépendance : on dépend de l'abstraction, pas du concret. Sans polymorphisme, \`alerter\` serait une cascade de \`if canal === …\` à modifier et re-tester à chaque ajout. **Relance possible** : « faut-il tout mettre en classes ? » → non : la classe se justifie pour un état encapsulé avec des méthodes liées ou pour du polymorphisme ; une simple transformation sans état se fait mieux avec une fonction pure. Et je préfère la composition à l'héritage profond, plus fragile.`,
    future: `- **Bankable** : concevoir contre des interfaces (polymorphisme, open/closed) et arbitrer classe/fonction/héritage/composition sont des attendus d'entretien back et de revue de code.\n- L'interface \`Notifieur\` préfigure directement l'interface \`Store\` du Projet 1 (jour 44) : dépendre d'une abstraction pour changer d'implémentation (mémoire, JSON, plus tard SQL) sans casser le reste.\n- Le polymorphisme est le socle des design patterns (jour 39, Strategy/Factory/Adapter) et de l'architecture en couches (jour 53).`,
    solution: {
      logic: "Concevoir autour d'un CONTRAT (interface) : définir Notifieur.envoyer, faire implémenter chaque canal, et écrire alerter qui ne dépend que du contrat. Encapsuler l'état de chaque classe (private/readonly). Prouver l'open/closed en ajoutant un canal sans modifier alerter. Puis comparer honnêtement avec une version fonctionnelle pour montrer qu'on sait quand la classe se justifie (état + méthodes liées) et quand une fonction suffit.",
      simple: "Interface Notifieur, classes Email/SMS/Push, fonction alerter polymorphe. Fonctionne et illustre le polymorphisme de base.",
      improved: "Ajouter un 4e canal SANS modifier aucun code existant (preuve de l'open/closed), écrire la version fonctionnelle équivalente et rédiger la comparaison (quand classe, quand fonction), et utiliser private/readonly pour montrer l'encapsulation. Discuter composition > héritage sur un exemple.",
      pitfalls: ["Recourir à un héritage profond fragile là où la composition (ou une interface) suffirait.", "Tout mettre en classe par réflexe : une fonction pure convient souvent mieux qu'une classe sans état.", "Écrire `alerter` avec des `if canal === …` au lieu du polymorphisme : chaque ajout force une modification et une régression possible.", "Exposer l'état interne (pas de private) : d'autres finissent par en dépendre, ce qui casse l'encapsulation."],
      checks: ["Les deux versions (POO et fonctionnelle) fonctionnent et sont comparées par écrit.", "Ajouter un 4e notifieur ne modifie AUCUN code existant (prouvé).", "L'état des classes est encapsulé (private/readonly), pas exposé publiquement.", "On sait énoncer quand une classe se justifie face à une fonction + closure."],
      oral: "Va droit au cœur : « la POO sert à programmer contre un contrat, pas contre une implémentation ; le polymorphisme est le pilier central ». Démontre avec Notifieur/alerter et l'ajout indolore de Slack (open/closed). Puis nuance en praticien mûr : « je ne mets pas tout en classe ; une fonction suffit sans état, et je préfère la composition à l'héritage profond ». Faire le lien avec l'interface Store à venir (jour 44) montre que tu vois la continuité de la conception.",
    },
  },

  // ── Design patterns pratiques : ceux que tu utilises déjà ──
  39: {
    lessons: ['design-patterns-intro.md'],
    theory: `**Modèle mental : un design pattern est un NOM donné à une solution que tu réinventes déjà. Ce n'est ni une bibliothèque à importer ni une obligation : c'est un VOCABULAIRE partagé qui permet à deux ingénieurs de dire « ici, une Strategy » et de se comprendre instantanément. La compétence n'est pas de mémoriser 23 patterns, mais de RECONNAÎTRE le problème récurrent et de nommer la solution — et de résister à la tentation de les appliquer sans le problème correspondant.** Un pattern appliqué sans besoin est de la sur-ingénierie, un anti-pattern.\n\nPourquoi les patterns existent. Dans les années 90, des ingénieurs ont remarqué que les mêmes solutions revenaient d'un projet à l'autre pour les mêmes problèmes de conception. Les nommer et les cataloguer a créé un langage commun : au lieu de décrire « une classe qui décide quelle sous-classe instancier selon une config », on dit « une Factory ». Le pattern capture aussi le CONTEXTE (quand l'utiliser) et les CONSÉQUENCES (ses compromis), pas seulement le code.\n\nLes cinq patterns du jour, depuis leur problème. **Strategy** : injecter un comportement interchangeable — tes callbacks d'ordre supérieur (jour 22) EN SONT ; on passe la « stratégie » de calcul (tarif, tri) en paramètre pour la changer sans toucher l'appelant. **Factory** : centraliser la création d'objets quand le choix du type dépend d'une donnée (créer le bon \`Notifieur\` selon une config) — l'appelant demande un produit sans connaître la classe concrète. **Adapter** : uniformiser deux interfaces incompatibles (envelopper deux API tierces derrière une interface commune, ton module \`api.ts\`). **Observer** : notifier des abonnés d'un changement (les écouteurs d'événements du DOM sont des observers). **Singleton** : garantir une instance unique (une connexion DB) — mais c'est un ÉTAT GLOBAL déguisé, à limiter car il complique les tests et couple tout à cette instance.\n\nLa différence à saisir : pattern vs réflexe nommé. Tu utilises déjà Strategy quand tu passes une fonction de comparaison à un tri, Factory quand une fonction décide quel objet créer, Observer quand tu ajoutes un \`addEventListener\`. Les patterns ne t'apprennent pas à coder autrement : ils METTENT UN NOM sur ce que tu fais, ce qui rend le code plus lisible (l'intention est déclarée) et les discussions plus rapides.\n\nLe piège central : la sur-ingénierie. Le plus difficile n'est pas d'appliquer un pattern, c'est de SAVOIR NE PAS l'appliquer. Introduire une Factory abstraite pour deux cas triviaux, un Observer là où un appel direct suffit, un Singleton par confort d'accès global : chaque pattern non justifié ajoute de la complexité sans bénéfice. La règle : le pattern doit RÉSOUDRE un problème que tu as réellement, pas anticiper un problème hypothétique.\n\nLimites et compromis. Les patterns sont issus de langages orientés objet classiques ; en JS/TS, certains sont natifs (les fonctions de première classe rendent Strategy trivial) ou moins nécessaires. Sur-utiliser les patterns rend le code « astronaute » (abstrait sans raison). Le bon usage : reconnaître, nommer, appliquer avec parcimonie.\n\nLa règle du jour : **un pattern est un nom pour une solution récurrente, un outil de communication — reconnais le problème AVANT de nommer la solution, applique avec parcimonie, et rappelle-toi que le vrai savoir-faire est aussi de savoir NE PAS appliquer un pattern (la sur-ingénierie est un anti-pattern).**`,
    guided: `**Énoncé** : illustrer le pattern Strategy EN CONTEXTE de ton propre code (le calcul de tarif du jour 5), puis nommer le problème qu'il résout.\n\n**Exemple simple** : sans Strategy, une fonction \`prix\` truffée de \`if typeClient === 'vip' … else if 'pro' …\` doit être MODIFIÉE à chaque nouveau type de client. Avec Strategy, on INJECTE la règle de calcul : ajouter un type = passer une nouvelle fonction, sans toucher l'appelant.\n\n**Raisonnement pas-à-pas** : (1) j'identifie ce qui VARIE (la règle de calcul du prix) ; (2) je l'extrais derrière un contrat (une fonction \`(base) => number\`) ; (3) l'appelant reçoit la stratégie en paramètre ; (4) ajouter une stratégie ne modifie plus l'appelant — c'est Strategy, et c'est exactement l'open/closed du jour 38.\n\n**Implémentation** :\n\`\`\`ts\n// Le comportement qui VARIE, extrait derrière un contrat\ntype Tarif = (base: number) => number;\n\nconst tarifs: Record<string, Tarif> = {\n  standard: base => base,\n  vip:      base => base * 0.8,      // -20 %\n  pro:      base => base * 0.9,      // -10 %\n};\n\n// L'appelant ne connaît que le CONTRAT Tarif : il ne change jamais\nfunction prixFinal(base: number, strategie: Tarif): number {\n  return Math.round(strategie(base) * 100) / 100;\n}\n\nconsole.log(prixFinal(100, tarifs.vip));   // 80\nconsole.log(prixFinal(100, tarifs.pro));   // 90\n\n// Ajouter 'partenaire' = une entrée de plus, prixFinal N'EST PAS touchée (open/closed)\ntarifs.partenaire = base => base * 0.7;\nconsole.log(prixFinal(100, tarifs.partenaire));  // 70\n\`\`\`\nLe même code montre que Strategy n'est que le nom de « injecter un comportement » — que tu faisais déjà avec les fonctions d'ordre supérieur (jour 22).\n\n**Une Factory, en une image** :\n\`\`\`ts\ninterface Notifieur { envoyer(m: string): void; }\nfunction creerNotifieur(type: 'email' | 'sms'): Notifieur {\n  // l'appelant demande un produit sans connaître la classe concrète\n  return type === 'email' ? new Email() : new SMS();\n}\n\`\`\``,
    quiz: [
      { q: "Qu'est-ce qu'un design pattern, fondamentalement ?", a: "Un NOM donné à une solution récurrente à un problème de conception — un vocabulaire partagé entre ingénieurs, pas une bibliothèque ni une obligation d'usage." },
      { q: "Quel problème résout le pattern Strategy ?", a: "Faire varier un comportement (calcul, tri, tarif) sans modifier l'appelant : on INJECTE la stratégie en paramètre. Ajouter une variante = une fonction de plus, appelant intact (open/closed)." },
      { q: "Pourquoi le Singleton est-il à manier avec précaution ?", a: "C'est un état global déguisé : il couple tout le code à une instance unique et complique les tests (on ne peut pas isoler ni remplacer facilement l'instance)." },
      { q: "Quel est l'anti-pattern principal autour des design patterns ?", a: "La sur-ingénierie : appliquer un pattern SANS avoir le problème qu'il résout. Le vrai savoir-faire inclut de savoir NE PAS appliquer un pattern." },
    ],
    caseStudy: `Les design patterns structurent le code d'entreprise et, surtout, les CONVERSATIONS entre développeurs. En revue de code, « remplace ce switch par une Strategy » ou « ce couplage appelle un Adapter » transmet une intention complète en trois mots — c'est un gain de communication réel dans une équipe. Concrètement : les frameworks eux-mêmes sont bâtis sur des patterns (les middlewares Express sont une Chain of Responsibility, les hooks React relèvent de l'Observer, un ORM cache un Factory/Repository). Savoir les reconnaître aide à comprendre les bibliothèques qu'on utilise. Mais la maturité se voit surtout dans la RETENUE : un candidat qui plaque une AbstractFactory sur un problème trivial inquiète autant qu'un candidat qui ne connaît aucun pattern. Les recruteurs valorisent celui qui reconnaît le problème, nomme la bonne solution, et sait dire « ici, pas besoin de pattern ».`,
    interview: `**« Cite un design pattern que tu as déjà utilisé sans le savoir, et explique le problème qu'il résout. »** → La Strategy. Dès qu'on passe une fonction de comparaison à un tri, ou une règle de calcul de tarif en paramètre, on fait du Strategy : on INJECTE le comportement qui varie derrière un contrat, au lieu de le coder en dur dans une cascade de \`if\`. Le problème résolu : faire évoluer ce comportement sans modifier l'appelant — ajouter un type de tarif devient « une fonction de plus », l'appelant reste intact. C'est exactement le principe ouvert/fermé. Un pattern n'est pas une bibliothèque, c'est un NOM sur une solution récurrente : ça rend le code lisible (l'intention est déclarée) et les discussions d'équipe rapides. Je connais aussi Factory (créer le bon objet selon une config), Adapter (uniformiser deux API incompatibles), Observer (les écouteurs d'événements), Singleton (instance unique, mais état global déguisé à limiter). **Relance possible** : « quand NE faut-il PAS utiliser de pattern ? » → quand le problème correspondant n'existe pas : plaquer une Factory abstraite sur deux cas triviaux ajoute de la complexité sans bénéfice — la sur-ingénierie est elle-même un anti-pattern.`,
    future: `- **Bankable** : reconnaître et nommer les bons patterns (et savoir s'en abstenir) accélère les revues de code et est un signal de maturité recherché en entretien.\n- Les patterns éclairent les frameworks que tu utiliseras : middlewares Express (Chain of Responsibility), hooks React (Observer), ORM (Repository/Factory).\n- Strategy et Factory prolongent directement le polymorphisme (jour 38) et préparent l'architecture en couches et l'injection de dépendances (jours 53-54).`,
    solution: {
      logic: "Traiter les patterns comme un vocabulaire : pour chacun, partir du PROBLÈME récurrent, montrer la solution EN CONTEXTE de son propre code (Strategy = tarifs jour 5, Factory = créer le bon Notifieur, Adapter = uniformiser deux API, Observer = événements DOM, Singleton = connexion DB), et énoncer en une phrase ce qu'il résout. La preuve de compréhension : retrouver un pattern déjà réinventé dans le code du mois 1, et savoir dire quand NE PAS en mettre.",
      simple: "Implémenter les 5 patterns avec des exemples (même jouets) et une phrase de description chacun. Montre qu'on connaît les noms.",
      improved: "Ancrer chaque pattern dans SON PROPRE code (pas des exemples copiés) : Strategy sur les tarifs réels, Factory sur les Notifieur du jour 38, Adapter sur un module d'API existant. Nommer un pattern qu'on avait réinventé sans le savoir (creerValidateur = Strategy + Factory). Et ajouter une note sur la sur-ingénierie : un cas où l'on choisit délibérément de NE PAS appliquer de pattern.",
      pitfalls: ["Appliquer un pattern sans le problème correspondant : sur-ingénierie, l'anti-pattern principal.", "Abuser du Singleton comme accès global pratique : couplage et tests difficiles.", "Copier des exemples jouets déconnectés de son code, sans identifier le problème réel résolu.", "Croire qu'il faut mémoriser tous les patterns : l'important est de reconnaître le problème et de nommer la solution."],
      checks: ["Les 5 patterns sont implémentés EN CONTEXTE du propre code (pas des exemples génériques).", "Pour chaque pattern, le problème résolu est écrit en une phrase.", "Au moins un pattern est retrouvé dans le code du mois 1 et nommé.", "Un cas de sur-ingénierie évitée (où l'on choisit de ne pas mettre de pattern) est identifié."],
      oral: "Pose le cadre : « un pattern est un nom sur une solution récurrente, un outil de communication — pas une obligation ». Illustre avec Strategy sur ton propre calcul de tarif (« je l'utilisais déjà via les fonctions d'ordre supérieur »). Puis démarque-toi par la maturité : « le vrai savoir-faire, c'est aussi de NE PAS appliquer un pattern quand le problème n'existe pas — la sur-ingénierie est un anti-pattern ». Cette retenue rassure plus qu'un catalogue récité.",
    },
  },

  // ── Clean code : nommage, fonctions courtes, commentaires utiles ──
  40: {
    lessons: ['clean-code.md'],
    theory: `**Modèle mental : le code est LU dix fois plus qu'il n'est écrit — par tes collègues, et par TOI dans six mois. Le clean code optimise donc pour le LECTEUR, pas pour l'écrivain. Un bon nom rend un commentaire inutile ; une fonction courte tient dans la tête ; un commentaire dit POURQUOI (la décision, la contrainte invisible), jamais QUOI (la ligne se lit toute seule). L'objectif n'est pas l'esthétique : c'est de réduire le coût de COMPRÉHENSION future, qui est le vrai coût d'un logiciel.** Écrire clairement est un acte d'empathie envers le prochain lecteur — souvent soi-même.\n\nPourquoi le clean code existe. Un logiciel vit des années et change constamment. Le temps dominant n'est pas l'écriture initiale, mais la LECTURE lors de chaque correction, ajout, débogage. Du code obscur taxe chacune de ces lectures : il faut re-déchiffrer, deviner l'intention, craindre de casser. Le clean code est un investissement qui se rembourse à chaque relecture — c'est de l'économie, pas de la coquetterie.\n\nLes leviers principaux, depuis les fondations. **Le nommage révélateur** : le nom doit dire l'INTENTION (\`calculerRemiseFidelite\` > \`calc\` > \`x\`). Un bon nom élimine le besoin d'un commentaire explicatif : le code se lit comme une phrase. **Les fonctions courtes à une seule responsabilité** : une fonction qui tient à l'écran, qui fait UNE chose, se comprend et se teste d'un coup d'œil. **Le niveau d'abstraction cohérent** : ne pas mélanger, dans un même bloc, la logique métier de haut niveau et les détails techniques de bas niveau — le lecteur perd le fil. **Le commentaire = POURQUOI** : documenter la décision non évidente (« on trie ici parce que l'API l'exige »), la contrainte cachée, le piège — jamais paraphraser le code.\n\nLa différence commentaire utile vs bruit. Un commentaire qui répète le code (\`i++; // incrémente i\`) est du BRUIT : il n'ajoute rien et se PÉRIME (le code change, le commentaire ment). Un commentaire utile capture une information ABSENTE du code : le pourquoi d'un choix, un lien vers un ticket, un avertissement (« ne pas paralléliser : l'API limite à 1 req/s »). Règle : si tu ressens le besoin de commenter QUOI fait le code, essaie d'abord de mieux le NOMMER.\n\nLe DRY avec discernement. « Don't Repeat Yourself » dit de factoriser les duplications VRAIES (deux morceaux identiques qui évoluent ensemble). Mais forcer une abstraction sur deux choses qui se RESSEMBLENT sans être liées crée un couplage pire que la duplication : quand elles divergent, l'abstraction devient un nœud. La bonne question : « ces deux bouts changeront-ils toujours ENSEMBLE ? » — si non, laisse-les séparés.\n\nLimites et compromis. Le clean code est affaire de JUGEMENT, pas de règles absolues : des fonctions trop découpées fragmentent la lecture ; un nom trop long nuit autant qu'un trop court. Refactorer sans TESTS risque de casser en silence (d'où les tests, jour 47). Et « propre » dépend du contexte d'équipe : la cohérence avec le code existant prime souvent sur la préférence personnelle.\n\nLa règle du jour : **écris pour le lecteur futur (souvent toi) : des noms qui révèlent l'intention, des fonctions courtes à une responsabilité, un niveau d'abstraction cohérent, des commentaires qui disent POURQUOI. Applique DRY avec discernement, et souviens-toi qu'un bon nom rend un commentaire inutile.**`,
    guided: `**Énoncé** : refactorer un fragment obscur en appliquant nommage, fonctions courtes et « commentaire = pourquoi », et mesurer le gain de lisibilité.\n\n**Exemple simple** : le AVANT — correct mais illisible.\n\`\`\`ts\n// AVANT : que fait ce code ?\nfunction p(a: any[]): number {\n  let x = 0;\n  for (let i = 0; i < a.length; i++) {\n    if (a[i].t === 1 && a[i].q > 0) x += a[i].pr * a[i].q; // ???\n  }\n  return x * 1.2; // 1.2 ?\n}\n\`\`\`\n\n**Raisonnement pas-à-pas** : (1) je NOMME ce que le code calcule (un total TTC de lignes actives) ; (2) je remplace les champs cryptiques par des noms d'intention ; (3) j'extrais la condition en une fonction au nom parlant ; (4) je remplace le \`1.2\` magique par une constante nommée ; (5) je garde UN commentaire, sur le POURQUOI d'un choix non évident.\n\n**Implémentation (APRÈS)** :\n\`\`\`ts\nconst TAUX_TVA = 0.2;\n\ninterface Ligne { type: 'produit' | 'service'; quantite: number; prixUnitaire: number; }\n\nfunction estLigneFacturable(ligne: Ligne): boolean {\n  return ligne.type === 'produit' && ligne.quantite > 0;\n}\n\nfunction totalTTC(lignes: Ligne[]): number {\n  const totalHT = lignes\n    .filter(estLigneFacturable)                          // intention claire\n    .reduce((somme, l) => somme + l.prixUnitaire * l.quantite, 0);\n  // POURQUOI : la TVA est appliquée au total, pas ligne à ligne,\n  // pour éviter les écarts d'arrondi cumulés (exigence comptable).\n  return totalHT * (1 + TAUX_TVA);\n}\n\`\`\`\nLe AVANT et le APRÈS produisent le MÊME résultat, mais le second se lit sans effort : les noms portent l'intention, la constante remplace le nombre magique, et le seul commentaire capture une décision INVISIBLE dans le code (l'ordre d'application de la TVA).`,
    quiz: [
      { q: "Pourquoi optimise-t-on le code pour le lecteur plutôt que pour l'écrivain ?", a: "Parce que le code est LU bien plus souvent qu'écrit (chaque correction, ajout, débogage le relit). Le coût dominant d'un logiciel est la compréhension future, pas la frappe initiale." },
      { q: "Que doit dire un commentaire utile, et que ne doit-il pas dire ?", a: "Il doit dire POURQUOI (une décision, une contrainte cachée, un piège). Il ne doit pas paraphraser QUOI fait le code : ce commentaire-là est du bruit qui se périme quand le code change." },
      { q: "Comment un bon nom peut-il rendre un commentaire inutile ?", a: "Un nom qui révèle l'intention (`estLigneFacturable`) fait que le code se lit comme une phrase : plus besoin d'un commentaire pour expliquer ce que fait la ligne." },
      { q: "Pourquoi appliquer DRY avec discernement ?", a: "Factoriser deux choses qui se RESSEMBLENT sans changer ensemble crée un couplage pire que la duplication : quand elles divergent, l'abstraction devient un nœud. On factorise ce qui évolue vraiment ensemble." },
    ],
    caseStudy: `La lisibilité du code est un enjeu économique majeur en entreprise, où une base est maintenue des années par des équipes qui tournent. Du code clair réduit le temps d'onboarding d'un nouvel arrivant, accélère chaque correction de bug (on comprend avant de modifier), et diminue le risque de régression (on voit ce qu'on touche). À l'inverse, du code obscur crée une « dette » : chaque évolution coûte plus cher et fait plus peur. C'est pourquoi les revues de code portent autant sur la clarté (noms, découpage, commentaires) que sur la correction. Un développeur qui livre du code lisible, testable et bien nommé fait gagner du temps à toute l'équipe — une valeur directe et mesurée. Savoir refactorer un fragment obscur en expliquant CHAQUE choix (pourquoi ce nom, pourquoi extraire cette fonction) est exactement ce qu'on évalue en entretien et en période d'essai.`,
    interview: `**« Qu'est-ce que du code propre pour toi, et comment refactorerais-tu une fonction obscure ? »** → Du code propre optimise pour le LECTEUR, parce que le code est lu dix fois plus qu'il n'est écrit — par l'équipe et par moi-même dans six mois. Concrètement : des noms qui révèlent l'INTENTION (un bon nom rend un commentaire inutile), des fonctions courtes à UNE responsabilité qui tiennent à l'écran, un niveau d'abstraction cohérent (ne pas mêler métier de haut niveau et détails techniques), et des commentaires qui disent POURQUOI un choix a été fait, jamais QUOI fait le code. Pour refactorer une fonction obscure : je la renomme d'après ce qu'elle calcule vraiment, je remplace les variables et nombres magiques par des noms/constantes parlants, j'extrais les conditions complexes en fonctions au nom explicite, et je ne garde que les commentaires qui capturent une décision invisible dans le code. Le tout à comportement CONSTANT, idéalement sous couverture de tests pour ne rien casser. **Relance possible** : « ne risque-t-on pas de trop découper ? » → si : le clean code est du jugement, pas des règles absolues. Des fonctions atomisées fragmentent la lecture, et DRY appliqué sans discernement (factoriser deux choses juste ressemblantes) crée un couplage pire que la duplication.`,
    future: `- **Bankable** : livrer du code lisible et bien nommé fait gagner du temps à l'équipe entière ; c'est évalué en revue de code, en entretien et en période d'essai.\n- Le clean code rend le refactoring et le débogage (jour 41) plus sûrs, et prépare la revue de code du Projet 1 (jour 47) et les ADR (décisions d'architecture documentées).\n- « Optimiser pour la compréhension future » est un principe transversal qui sous-tend la documentation (README, jour 47) et l'architecture en couches (jour 53).`,
    solution: {
      logic: "Refactorer à comportement CONSTANT en appliquant des leviers précis : nommage révélateur d'intention, fonctions courtes à une responsabilité, niveau d'abstraction cohérent, constantes nommées à la place des nombres magiques, et commentaires réduits au POURQUOI. Construire d'abord une checklist de code smells SEUL (preuve de compréhension), puis l'appliquer sur un fichier réel avec un diff avant/après commenté, chaque changement justifiable en une phrase.",
      simple: "Renommer les variables floues et découper la plus grosse fonction. Améliore déjà nettement la lecture.",
      improved: "Écrire d'abord sa propre checklist de 10 code smells (avant toute ressource), puis refactorer un fichier du mois 1 en documentant le AVANT/APRÈS, en remplaçant les nombres magiques par des constantes, en extrayant les conditions en fonctions nommées, et en réservant les commentaires au pourquoi. Idéalement sous tests pour garantir le comportement inchangé, et justifier chaque choix en une phrase.",
      pitfalls: ["Sur-commenter l'évident (`i++ // incrémente`) au lieu de documenter la seule décision non triviale.", "Refactorer sans tests : on casse le comportement en silence — d'où l'intérêt des tests (jour 47).", "Sur-découper en fonctions atomiques : la lecture se fragmente autant qu'avec une fonction trop longue.", "Forcer DRY sur deux bouts juste ressemblants : couplage plus coûteux que la duplication quand ils divergent."],
      checks: ["La checklist de 10 code smells est écrite AVANT consultation de ressources.", "Un fichier du mois 1 est refactoré avec un diff avant/après commenté.", "Le comportement est inchangé (idéalement prouvé par des tests ou une comparaison de sorties).", "Chaque renommage/découpage est justifiable en une phrase (intention, responsabilité)."],
      oral: "Ancre tout sur le principe : « le code est lu dix fois plus qu'écrit, donc j'optimise pour le lecteur futur, souvent moi ». Montre un avant/après où un bon nom a supprimé un commentaire, où une constante a remplacé un nombre magique. Puis nuance en praticien : « c'est du jugement, pas des règles — je ne sur-découpe pas, et je n'applique DRY que sur ce qui change ensemble ». Insister sur « refactorer sous tests pour ne rien casser » montre que tu penses sécurité, pas seulement esthétique.",
    },
  },

  // ── Debugging méthodique : la compétence qu'on n'enseigne jamais ──
  41: {
    lessons: ['clean-code.md', 'testing-foundations.md'],
    theory: `**Modèle mental : déboguer n'est PAS modifier du code au hasard jusqu'à ce que « ça marche » — c'est une enquête scientifique. On REPRODUIT le bug de façon fiable, on ISOLE la zone coupable par bissection, on formule UNE hypothèse FALSIFIABLE, on la PROUVE par une observation, puis on corrige la CAUSE (pas le symptôme) et on re-reproduit pour valider. Le débogueur méthodique bat le devineur pressé à tous les coups, parce qu'il élimine des possibilités au lieu d'en espérer une.** Changer du code sans hypothèse, c'est jouer à la loterie ; suivre la méthode, c'est converger.\n\nPourquoi une méthode est nécessaire. Face à un bug, la tentation est de modifier « ce qui a l'air suspect » et de relancer. Sur un bug simple, on a parfois de la chance ; sur un bug subtil (référence partagée, async, off-by-one), on tourne en rond des heures, on introduit de NOUVEAUX bugs, et on ne comprend jamais la cause — donc il reviendra. La méthode remplace la chance par une convergence garantie : chaque étape RÉDUIT l'espace des causes possibles.\n\nLes six étapes, depuis les fondations. (1) **Reproduire fiablement** : sans un cas qui déclenche le bug à volonté, impossible de savoir si un fix marche. C'est le prérequis absolu. (2) **Lire le message d'erreur EN ENTIER** : la stack trace donne souvent le fichier, la ligne et la nature — l'ignorer, c'est jeter la carte au trésor. (3) **Isoler par bissection** : couper le parcours en deux, vérifier l'état au milieu (log/breakpoint), et éliminer la moitié saine — la recherche binaire (jour 16) appliquée au débogage, qui localise en O(log n) étapes. (4) **Formuler UNE hypothèse falsifiable** : « la liste est mutée à l'étape X » — précise, testable. (5) **La prouver par observation** AVANT de corriger : un log ou un breakpoint confirme ou infirme. (6) **Corriger la cause** puis re-reproduire : le symptôme peut disparaître alors que la cause demeure.\n\nLa différence corriger la cause vs le symptôme. Un \`if (valeur == null) valeur = 0\` qui masque un \`null\` inattendu FAIT DISPARAÎTRE le crash mais laisse la vraie question ouverte (pourquoi est-ce null ?) — le bug ressurgira ailleurs. Corriger la cause, c'est remonter à l'origine du \`null\`. Le symptôme est ce qu'on voit ; la cause est ce qui le produit.\n\nLes familles de bugs et leur signature. **Référence partagée** : deux variables pointent le même objet, modifier l'une change l'autre (copie superficielle vs profonde). **Off-by-one** : bornes de boucle (\`<\` vs \`<=\`, index 0 vs 1). **Type inattendu** : un \`NaN\` ou \`undefined\` qui se propage silencieusement. **Async** : l'ordre d'exécution n'est pas celui de lecture du code (une valeur lue avant d'être résolue). **État périmé** : une donnée mise en cache non rafraîchie. Reconnaître la signature accélère le diagnostic.\n\nLimites et compromis. La méthode a un coût d'installation (reproduire prend parfois du temps) qui décourage sur un bug « évident » — mais c'est justement sur les bugs non évidents qu'elle fait gagner des heures. Le \`console.log\` stratégique suffit souvent ; le débogueur pas-à-pas (breakpoints, inspection) est plus puissant pour l'état complexe et l'async. Le « rubber duck » (expliquer le bug à voix haute, même à un canard) révèle souvent la faille dans le raisonnement avant tout outil.\n\nLa règle du jour : **ne modifie JAMAIS au hasard. Reproduis d'abord, lis l'erreur en entier, isole par bissection, formule une hypothèse falsifiable, prouve-la par observation, corrige la CAUSE, re-reproduis. Reconnais les familles de bugs à leur signature — c'est ce qui transforme une chasse aléatoire en enquête qui converge.**`,
    guided: `**Énoncé** : débusquer un bug de RÉFÉRENCE PARTAGÉE avec la méthode, au lieu de patcher au hasard.\n\n**Exemple simple** : le symptôme déroutant.\n\`\`\`ts\nconst modele = { tags: [] as string[] };\nfunction creerTache(titre: string) {\n  const t = { titre, tags: modele.tags };   // BUG : même référence partagée !\n  return t;\n}\nconst a = creerTache('A');\nconst b = creerTache('B');\na.tags.push('urgent');\nconsole.log(b.tags);   // ['urgent'] ?!  b n'a jamais reçu 'urgent'\n\`\`\`\n\n**Raisonnement pas-à-pas (la méthode)** : (1) REPRODUIRE : le cas ci-dessus déclenche le bug à coup sûr. (2) SYMPTÔME : modifier \`a.tags\` change \`b.tags\`. (3) HYPOTHÈSE falsifiable : « \`a.tags\` et \`b.tags\` sont le MÊME tableau en mémoire ». (4) PROUVER par observation : \`console.log(a.tags === b.tags)\` → \`true\` → hypothèse CONFIRMÉE. (5) CAUSE : \`tags: modele.tags\` copie la RÉFÉRENCE, pas le contenu. (6) FIX de la cause + re-reproduire.\n\n**Correction (cause traitée)** :\n\`\`\`ts\nfunction creerTache(titre: string) {\n  const t = { titre, tags: [...modele.tags] };   // copie du CONTENU, plus de partage\n  return t;\n}\nconst a = creerTache('A'), b = creerTache('B');\nconsole.log(a.tags === b.tags);   // false -> références distinctes\na.tags.push('urgent');\nconsole.log(b.tags);              // [] -> b intact, bug résolu à la CAUSE\n\`\`\`\nLe réflexe naïf aurait été de « réinitialiser \`b.tags\` » (patch du symptôme) — le partage serait resté et le bug réapparu ailleurs. La méthode a mené à la cause en une hypothèse prouvée.`,
    quiz: [
      { q: "Quelle est la toute première étape du débogage méthodique, et pourquoi ?", a: "REPRODUIRE le bug de façon fiable : sans un cas qui le déclenche à volonté, on ne peut ni comprendre la cause ni vérifier qu'un fix fonctionne vraiment." },
      { q: "Qu'est-ce que la bissection appliquée au débogage ?", a: "Couper le parcours du code en deux, observer l'état au milieu (log/breakpoint), éliminer la moitié saine et recommencer : la recherche binaire, qui localise la zone coupable en O(log n) étapes." },
      { q: "Quelle est la différence entre corriger le symptôme et corriger la cause ?", a: "Le symptôme est ce qu'on voit (un crash) ; la cause est ce qui le produit. Masquer le symptôme (ex. forcer une valeur par défaut) fait disparaître le crash mais laisse le bug ressurgir ailleurs." },
      { q: "Cite deux familles de bugs et leur signature.", a: "Référence partagée : modifier une variable en change une autre (copie superficielle). Off-by-one : erreur de borne de boucle (`<` vs `<=`). Aussi : async (ordre d'exécution) et type inattendu (NaN/undefined propagé)." },
    ],
    caseStudy: `Le débogage méthodique est une compétence quotidienne et sous-estimée : un développeur passe une part énorme de son temps non à écrire, mais à COMPRENDRE pourquoi quelque chose ne marche pas — en dev, en pré-prod, et sous pression lors d'un incident en production. Face à un bug critique qui coûte de l'argent à chaque minute, celui qui modifie au hasard aggrave souvent la situation (nouveaux bugs, régressions) ; celui qui reproduit, isole et prouve une hypothèse converge et documente la cause racine (post-mortem). Les familles de bugs (référence partagée, async, état périmé) reviennent constamment dans le code réel. C'est aussi un signal d'entretien fort : « raconte comment tu as débogué un problème difficile » teste précisément si tu as une MÉTHODE ou si tu tâtonnes — et les équipes matures pratiquent le rubber duck et les post-mortems sans blâme.`,
    interview: `**« Raconte comment tu t'y prends pour déboguer un problème difficile. »** → Je ne modifie jamais au hasard : je traite le bug comme une enquête. D'abord je le REPRODUIS de façon fiable — sans ça, impossible de valider un fix. Je lis le message d'erreur EN ENTIER (la stack trace donne souvent la ligne et la cause). J'ISOLE par bissection : je coupe le parcours en deux, j'observe l'état au milieu avec un log ou un breakpoint, et j'élimine la moitié saine — c'est la recherche binaire appliquée au débogage. Puis je formule UNE hypothèse falsifiable (« la liste est mutée ici ») et je la PROUVE par observation AVANT de toucher au code. Enfin je corrige la CAUSE, pas le symptôme — masquer un null par une valeur par défaut fait disparaître le crash mais laisse le bug ressurgir ailleurs — et je re-reproduis pour valider. Je m'appuie aussi sur les signatures des familles de bugs : référence partagée, off-by-one, async, type inattendu, état périmé. **Relance possible** : « et si tu bloques complètement ? » → le rubber duck : j'explique le bug ligne à ligne à voix haute (à un collègue ou littéralement à un canard) ; verbaliser révèle presque toujours la faille dans mon raisonnement.`,
    future: `- **Bankable** : « raconte un débogage difficile » est une question d'entretien récurrente qui distingue ceux qui ont une MÉTHODE de ceux qui tâtonnent ; une réponse structurée est un signal fort.\n- La méthode reproduire → isoler → prouver → corriger la cause sert en dev quotidien et lors des incidents en production (analyse de cause racine, post-mortem).\n- Reconnaître les familles de bugs (référence partagée, async) prépare directement le débogage du Projet 1 et des API du mois 3 (erreurs, async).`,
    solution: {
      logic: "Appliquer un PROCESSUS au lieu de deviner : reproduire fiablement, lire l'erreur en entier, isoler par bissection, formuler une hypothèse falsifiable, la prouver par observation, corriger la CAUSE puis re-reproduire. Pour chacun des 5 bugs, documenter symptôme → hypothèse → preuve → fix, et nommer la famille (logique, typage, référence partagée, off-by-one, async). La preuve de maîtrise : aucun fix « au hasard », chaque correction découle d'une hypothèse prouvée.",
      simple: "Trouver les bugs en lisant le code et en testant. Fonctionne sur les bugs évidents, mais échoue ou tourne en rond sur les subtils (async, référence partagée).",
      improved: "Dérouler la méthode complète et la DOCUMENTER pour chaque bug (symptôme, hypothèse falsifiable, preuve par observation, cause, fix, re-reproduction), en identifiant la famille et en corrigeant la cause plutôt que le symptôme. Formaliser sa propre méthode de débogage écrite et affichée. Utiliser le débogueur pas-à-pas pour l'async/l'état complexe, pas seulement console.log.",
      pitfalls: ["Modifier du code au hasard sans hypothèse : on introduit de nouveaux bugs et on ne comprend jamais la cause.", "Corriger le symptôme (valeur par défaut qui masque un null) au lieu de la cause : le bug revient ailleurs.", "Ne pas lire le message d'erreur en entier : on ignore la ligne et la nature déjà fournies.", "Ne pas reproduire avant de corriger : impossible de savoir si le fix marche vraiment."],
      checks: ["Les 5 bugs sont trouvés avec, pour chacun, symptôme → hypothèse → preuve → correction documentés.", "Chaque fix découle d'une hypothèse PROUVÉE, aucun changement au hasard.", "La cause (pas le symptôme) est corrigée, vérifiable par re-reproduction.", "La méthode personnelle de débogage est écrite et la famille de chaque bug est nommée."],
      oral: "Déroule les étapes dans l'ordre en insistant sur ce qui trahit la méthode : « je REPRODUIS d'abord, je PROUVE une hypothèse AVANT de corriger, et je corrige la CAUSE, pas le symptôme ». Donne un exemple concret (référence partagée prouvée par `a === b`). Mentionner la bissection (recherche binaire appliquée au bug) et le rubber duck montre que tu as un vrai arsenal, pas juste des console.log — c'est ce que le recruteur veut entendre.",
    },
  },

  // ── Revue semaine 6 : TypeScript, POO, design patterns, clean code ──
  42: {
    reviewSynthese: `La semaine 6 a fait passer du code « qui marche » à du code SÛR et LISIBLE. TypeScript (jour 36) : un contrat vérifié à la compilation qui déplace les bugs de type de la production vers l'éditeur ; \`any\` désactive, \`unknown\` force la vérification. Les types avancés (jour 37) : rendre les états illégaux irreprésentables — unions littérales, interfaces, génériques, exhaustivité vérifiée par le compilateur. La POO (jour 38) : programmer contre un CONTRAT (interface) pas une implémentation, le polymorphisme comme pilier central, composition > héritage. Les design patterns (jour 39) : des NOMS sur des solutions récurrentes, un vocabulaire — et la sur-ingénierie comme anti-pattern. Le clean code (jour 40) : optimiser pour le lecteur futur, un bon nom rend un commentaire inutile. Le débogage méthodique (jour 41) : une enquête (reproduire → isoler → prouver → corriger la cause), pas une loterie. Le fil rouge : **on ne se contente plus de faire fonctionner le code — on le rend vérifiable par le compilateur, extensible par des contrats, lisible pour le prochain, et diagnosticable par une méthode.** Ces réflexes de qualité conditionnent tout le Projet 1 (semaine 7) et la crédibilité de ton portfolio.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Expliquer ce que TypeScript vérifie (contrat à la compilation) et any vs unknown | |\n| Modéliser avec unions littérales/interfaces/génériques (états illégaux irreprésentables) | |\n| Concevoir contre une interface (polymorphisme, open/closed) et arbitrer classe vs fonction | |\n| Nommer 3-4 design patterns par leur problème et reconnaître la sur-ingénierie | |\n| Refactorer pour la lisibilité (nommage, fonctions courtes, commentaire = pourquoi) | |\n| Débuguer avec méthode (reproduire, isoler, prouver, corriger la cause) | |\n\n**Total /18** : ≥ 15 = prêt pour le Projet 1 · 11-14 = revoir les items ≤ 1 · < 11 = rejouer avant TaskFlow, car ces réflexes de qualité sont ce que le projet doit démontrer.`,
    remediation: `- **any qui traîne / typage superficiel** : rejoue le jour 36 — remplace chaque any par un type précis ou unknown + narrowing ; documente un bug révélé par le typage.\n- **États illégaux encore possibles** : jour 37 — union littérale pour borner les valeurs, switch exhaustif à default:never, générique bien contraint.\n- **Code procédural déguisé en classes / if en cascade** : jour 38 — programme contre une interface, remplace les if par du polymorphisme, préfère la composition.\n- **Patterns plaqués ou ignorés** : jour 39 — pars du problème avant de nommer la solution ; identifie un cas de sur-ingénierie à éviter.\n- **Code illisible** : jour 40 — noms d'intention, fonctions courtes, commentaire = pourquoi ; refactore sous tests.\n- **Débogage au hasard** : jour 41 — reproduire, isoler par bissection, prouver une hypothèse, corriger la cause.`,
    interview: `1. **« Pourquoi TypeScript, et any vs unknown ? »** Attendu : contrat vérifié à la compilation qui déplace les bugs de type au plus tôt ; any désactive le contrôle, unknown force le narrowing aux frontières.\n2. **« Comment empêcher un état invalide par les types ? »** Attendu : union littérale, interface stricte, switch exhaustif (default:never) ; « rendre les états illégaux irreprésentables ».\n3. **« Explique le polymorphisme et l'open/closed. »** Attendu : programmer contre une interface, ajouter une implémentation sans modifier l'appelant ; composition > héritage.\n4. **« Cite un design pattern utilisé sans le savoir, et quand NE PAS en mettre. »** Attendu : Strategy (injection de comportement) ; la sur-ingénierie comme anti-pattern.\n5. **« Comment débogues-tu un problème difficile ? »** Attendu : reproduire, lire l'erreur, isoler par bissection, prouver une hypothèse, corriger la cause (pas le symptôme), rubber duck.`,
  },

  // ────────────────────────────────────────────────────────────────────────
  // MOIS 2 · Semaine 7 (jours 43-48) : FP typée, Projet 1 TaskFlow, consolidation
  // ────────────────────────────────────────────────────────────────────────

  // ── Programmation fonctionnelle : composition et pureté en TS ──
  43: {
    lessons: ['typescript-basics.md', 'clean-code.md'],
    theory: `**Modèle mental : typer les fonctions d'ordre supérieur transforme tes CONVENTIONS (pureté, immutabilité, composition) en CONTRATS vérifiés par le compilateur. \`compose<A, B, C>(f: (b: B) => C, g: (a: A) => B)\` DIT que la sortie de g doit nourrir l'entrée de f — si les types ne s'emboîtent pas, la composition n'a pas de sens, et tsc le refuse avant l'exécution. \`readonly\` fait de l'immutabilité une règle mécanique : muter devient une erreur de compilation, pas une discipline qu'on espère tenir.** La programmation fonctionnelle typée, c'est rendre le pipeline de données PROUVÉ correct dans ses assemblages.\n\nPourquoi la FP typée est puissante. En JavaScript, la pureté et l'immutabilité sont des CONVENTIONS : rien n'empêche de muter un tableau censé être constant, ou de composer deux fonctions dont les types ne collent pas — le bug n'apparaît qu'à l'exécution. Le typage transforme ces intentions en garde-fous : le compilateur vérifie que les compositions s'emboîtent et que rien ne mute ce qui est \`readonly\`. On passe d'une discipline personnelle à une garantie outillée.\n\nLes briques, depuis les fondations. Une **fonction pure typée** : sa signature promet une sortie déterministe pour des entrées données, sans effet de bord — testable en isolation. La **composition** (\`compose\`/\`pipe\`) enchaîne des transformations : \`pipe(nettoyer, valider, formater)\` lit le pipeline dans l'ordre. Le générique \`pipe<A, B, C>\` garantit que chaque étape reçoit le type que la précédente produit. L'**immutabilité** avec \`readonly\`/\`as const\` : \`readonly string[]\` interdit \`push\`/\`pop\` à la compilation ; \`as const\` fige un littéral en type le plus précis (et immuable).\n\nLa différence FP vs POO, pour choisir. La POO regroupe état + comportements dans des objets (jour 38) ; la FP sépare les DONNÉES (immuables) des TRANSFORMATIONS (fonctions pures). Un pipeline de données pur est plus facile à raisonner (pas d'état caché), à tester (entrée → sortie) et à paralléliser. La POO brille pour modéliser un domaine avec état encapsulé et polymorphisme. Ce ne sont pas des rivales : on choisit selon le problème, et TS supporte les deux.\n\nL'apport concret du typage sur la composition. Sans types, \`compose(f, g)\` peut assembler deux fonctions incompatibles silencieusement (le bug surgit à l'appel). Avec \`compose<A, B, C>\`, le compilateur EXIGE que \`g: A → B\` et \`f: B → C\` : impossible de mal brancher le pipeline. Le typage n'est pas décoratif ici — il attrape une classe entière d'erreurs d'assemblage.\n\nLimites et compromis. Les génériques de \`compose\`/\`pipe\` variadiques (nombre d'étapes variable) sont NOTABLEMENT difficiles à typer parfaitement — d'où les surcharges dans les bibliothèques (Ramda, fp-ts). Un générique trop lâche perd le type au milieu du pipeline (retombe à \`any\`), un trop strict devient illisible. L'immutabilité par copie a un coût mémoire (nouveaux objets) rarement problématique mais réel sur de très gros volumes. Et la FP poussée à l'extrême (point-free, monades) peut nuire à la lisibilité pour une équipe non familière.\n\nLa règle du jour : **type tes fonctions d'ordre supérieur pour transformer pureté et immutabilité en contrats vérifiés — compose/pipe génériques qui garantissent l'emboîtement des étapes, readonly qui interdit la mutation à la compilation. Choisis FP (données + transformations pures) ou POO (état encapsulé + polymorphisme) selon le problème, sans dogme.**`,
    guided: `**Énoncé** : écrire un \`pipe\` typé générique et un mini-pipeline de traitement 100 % pur, où le compilateur garantit l'emboîtement des étapes et interdit la mutation.\n\n**Exemple simple** : la valeur du typage sur pipe. \`pipe(doubler, versChaine)\` doit refuser \`pipe(versChaine, doubler)\` si \`doubler\` attend un nombre mais reçoit une chaîne — le compilateur attrape le mauvais branchement avant l'exécution.\n\n**Raisonnement pas-à-pas** : (1) je type \`pipe\` pour que chaque étape reçoive le type produit par la précédente ; (2) j'écris des transformations PURES (pas d'effet de bord) ; (3) je marque les données \`readonly\` pour que le compilateur interdise toute mutation ; (4) je vérifie que réordonner mal les étapes provoque une erreur de compilation.\n\n**Implémentation** :\n\`\`\`ts\n// pipe typé à 3 étapes : chaque sortie nourrit l'entrée suivante\nfunction pipe<A, B, C>(f: (a: A) => B, g: (b: B) => C): (a: A) => C {\n  return (a: A) => g(f(a));\n}\n\n// transformations PURES et typées\nconst doubler = (n: number): number => n * 2;\nconst versLabel = (n: number): string => \`valeur=\${n}\`;\n\nconst traiter = pipe(doubler, versLabel);\nconsole.log(traiter(21));         // \"valeur=42\"\n// pipe(versLabel, doubler)       // ❌ tsc : string non assignable à number\n\n// immutabilité vérifiée : le compilateur interdit la mutation\nfunction total(readonlyLignes: readonly number[]): number {\n  // readonlyLignes.push(0);      // ❌ tsc : push n'existe pas sur readonly[]\n  return readonlyLignes.reduce((s, x) => s + x, 0);   // lecture pure\n}\nconsole.log(total([10, 20, 12]));  // 42\n\`\`\`\nLe mauvais ordre des étapes et la tentative de mutation sont TOUS DEUX attrapés à la compilation : la convention est devenue un contrat.`,
    quiz: [
      { q: "Qu'apporte le typage d'un `compose`/`pipe` générique ?", a: "Il garantit que chaque étape reçoit le type produit par la précédente : impossible d'assembler deux fonctions incompatibles ; le mauvais branchement du pipeline est refusé à la compilation." },
      { q: "Que fait `readonly` pour l'immutabilité ?", a: "Il transforme l'immutabilité de convention en règle mécanique : muter un `readonly string[]` (push/pop) devient une erreur de compilation, pas juste une discipline qu'on espère tenir." },
      { q: "Quand choisir la FP plutôt que la POO ?", a: "La FP (données immuables + transformations pures) convient aux pipelines de données faciles à raisonner et tester. La POO convient à un domaine avec état encapsulé et polymorphisme. On choisit selon le problème, sans dogme." },
      { q: "Quel est le piège des génériques de `compose` ?", a: "Trop lâches, ils perdent le type au milieu du pipeline (retour à `any`) ; parfaitement typés pour un nombre variable d'étapes, ils deviennent très complexes (d'où les surcharges dans les bibliothèques)." },
    ],
    caseStudy: `Le style fonctionnel typé domine le traitement de données moderne, du front (transformations d'état immuables dans React/Redux) au back (pipelines ETL, transformation de flux). Sa valeur en entreprise : des transformations PURES et immuables sont prévisibles (pas d'état caché qui surprend), testables unitairement (entrée → sortie, sans montage de fixtures), et sûres à composer et paralléliser. Le typage ajoute la garantie que les pipelines s'assemblent correctement — une classe d'erreurs éliminée avant tout test. Concrètement, savoir écrire des fonctions pures typées, composer proprement et exploiter \`readonly\` est directement valorisé sur les postes front (gestion d'état immuable) et data (pipelines) ; et savoir ARBITRER FP vs POO selon le problème, plutôt que d'imposer un dogme, est un signe de maturité que les leads recherchent.`,
    interview: `**« Qu'apporte le typage à la programmation fonctionnelle, et quand choisis-tu FP plutôt que POO ? »** → Le typage transforme les CONVENTIONS de la FP en contrats vérifiés. La pureté et l'immutabilité, en JS, ne sont qu'une discipline ; en TS, \`readonly\` fait de la mutation une erreur de compilation, et un \`compose\`/\`pipe\` générique garantit que chaque étape reçoit le type produit par la précédente — impossible d'assembler un pipeline incompatible, le bug d'branchement est attrapé avant l'exécution. Pour le choix FP vs POO : la FP sépare les données immuables des transformations pures, idéale pour les pipelines de données faciles à raisonner et à tester (entrée → sortie, pas d'état caché) ; la POO regroupe état et comportements, idéale pour modéliser un domaine avec encapsulation et polymorphisme. Ce ne sont pas des rivales : je choisis selon le problème, et TypeScript supporte les deux. **Relance possible** : « pourquoi les compose variadiques sont-ils durs à typer ? » → parce qu'un nombre variable d'étapes exige des surcharges génériques ; trop lâche, le type se perd au milieu (retour à any), d'où les surcharges des bibliothèques comme fp-ts.`,
    future: `- **Bankable** : écrire des fonctions pures typées, composer proprement et manier readonly est valorisé sur les postes front (état immuable) et data (pipelines) ; arbitrer FP vs POO sans dogme est un signe de maturité.\n- Les pipelines purs et immuables préparent le traitement de données du mois data et la gestion d'état immuable côté React (mois ultérieurs).\n- Le réflexe « transformer une convention en contrat vérifié » prolonge le typage (jours 36-37) et fiabilise le code du Projet 1.`,
    solution: {
      logic: "Réinvestir la boîte à outils fonctionnelle du mois 1 en la TYPANT : compose/pipe génériques qui garantissent l'emboîtement des étapes, memoriser générique, fonctions pures typées, et données readonly pour une immutabilité vérifiée. Construire un mini-pipeline 100 % pur où le compilateur interdit la mutation et le mauvais branchement. La preuve : réordonner mal les étapes ou tenter une mutation provoque une erreur de compilation.",
      simple: "Réécrire compose/pipe/memoriser en TS avec des types simples et un pipeline. Fonctionne, mais des génériques trop lâches peuvent perdre le type au milieu.",
      improved: "Typer compose/pipe génériquement (chaque étape reçoit le type précédent), rendre memoriser générique et testé sur 2 types de fonctions, et marquer tout le pipeline readonly pour prouver l'immutabilité par le compilateur. Démontrer qu'un mauvais ordre d'étapes ET une mutation échouent à la compilation. Discuter la limite des compose variadiques.",
      pitfalls: ["Génériques de compose trop lâches : le type se perd au milieu du pipeline (retour implicite à any).", "Muter un readonly en croyant que c'est bloqué à l'exécution : c'est bloqué à la COMPILATION, c'est le but.", "Composer dans le mauvais ordre sans typage : le bug de branchement ne surgit qu'à l'appel.", "Imposer la FP partout par dogme là où la POO (état encapsulé) serait plus claire."],
      checks: ["compose et pipe typés fonctionnent sur une chaîne de 3 fonctions sans perte de type.", "memoriser typé fonctionne sur 2 types de fonctions différents.", "Le pipeline est 100 % pur et le compilateur interdit la mutation (readonly prouvé).", "Réordonner mal les étapes provoque une erreur de compilation (démontré)."],
      oral: "Résume l'idée forte : « le typage transforme mes conventions (pureté, immutabilité) en contrats que le compilateur vérifie ». Montre que compose typé refuse un mauvais branchement et que readonly refuse une mutation, tous deux à la compilation. Puis nuance : « FP pour les pipelines de données, POO pour l'état encapsulé — je choisis selon le problème ». Reconnaître spontanément la difficulté de typer les compose variadiques prouve une compréhension fine, pas juste un usage superficiel.",
    },
  },

  // ── Préparation du Projet 1 : TaskFlow — types et interface Store ──
  44: {
    lessons: ['architecture-basics.md', 'typescript-basics.md'],
    theory: `**Modèle mental : concevoir les TYPES avant d'écrire la moindre logique force à comprendre le DOMAINE — quelles entités, quels états possibles, quelles opérations. Et l'interface \`Store\` est ta première INVERSION DE DÉPENDANCE consciente : la logique métier dépend d'un CONTRAT (save/load/all), pas d'une implémentation (JSON, SQLite). Le jour où le stockage change, UN SEUL fichier change. Un modèle clair rend l'implémentation évidente ; un modèle flou la rend tortueuse.** Ce jour ne code pas TaskFlow : il en pose les fondations pour que le reste coule de source.\n\nPourquoi concevoir avant de coder. La tentation est de « commencer à coder » tout de suite. Mais sans modèle du domaine, on découvre les incohérences en cours de route, on refactore sans cesse, on empile les cas particuliers. Poser d'abord les types (Task, statuts, opérations) OBLIGE à répondre aux vraies questions — un statut peut-il être vide ? une tâche a-t-elle une priorité obligatoire ? — avant qu'elles ne deviennent des bugs. Le type est un plan qui révèle les décisions cachées.\n\nL'inversion de dépendance, depuis les fondations. Normalement, la logique métier « dépend » du stockage : elle appelle directement des fonctions de lecture/écriture de fichier. Problème : changer de stockage oblige à réécrire la logique. L'inversion consiste à faire dépendre la logique d'une ABSTRACTION — une interface \`Store { save(); load(); all() }\` — et à fournir une IMPLÉMENTATION concrète (JsonStore) séparément. La logique ne connaît QUE le contrat ; l'implémentation est branchée « par le dehors ». C'est le D de SOLID, et le germe de l'architecture hexagonale (ports & adapters) du mois 10, appliqué ici à petite échelle.\n\nLa différence contrat vs implémentation. Le CONTRAT (\`interface Store\`) décrit CE QU'ON PEUT FAIRE (sauver une tâche, tout charger) sans dire COMMENT. L'IMPLÉMENTATION (JsonStore, plus tard SqliteStore) dit COMMENT (fichier JSON, base SQL). Programmer contre le contrat rend le COMMENT interchangeable. C'est exactement l'interface \`Notifieur\` du jour 38, appliquée à la persistance : le polymorphisme au service du découplage.\n\nLe backlog en jalons DÉMONTRABLES. « Avancer sur le CLI » n'est pas un jalon : on ne peut pas le cocher. « add + list fonctionnent de bout en bout » en est un : il est vérifiable. Découper en jalons démontrables rend le progrès mesurable et la procrastination visible — chaque jalon est une preuve, pas une intention.\n\nLimites et compromis. Sur-concevoir les types (abstractions prématurées pour des besoins hypothétiques) est aussi néfaste que sous-concevoir (\`any\` partout) : le bon niveau est « assez précis pour interdire l'incohérence, assez souple pour évoluer ». L'inversion de dépendance a un coût (une interface de plus) qui ne se justifie que si l'implémentation peut réellement varier — ici oui (JSON → SQLite est explicitement prévu). Enfin, un modèle n'est jamais figé : il s'ajustera au contact du code, et c'est normal.\n\nLa règle du jour : **conçois les types AVANT la logique pour comprendre le domaine, et fais dépendre ta logique d'un CONTRAT (interface Store) pas d'une implémentation — c'est l'inversion de dépendance, qui rend le stockage interchangeable (un seul fichier change). Découpe en jalons démontrables, pas en activités floues.**`,
    guided: `**Énoncé** : concevoir l'interface \`Store\` et les types de TaskFlow de sorte que passer de JSON à SQLite ne change QU'UN fichier.\n\n**Exemple simple** : le test de l'inversion réussie. Si je demande « combien de fichiers changent pour migrer vers SQLite ? » et que la réponse est UN (la nouvelle implémentation), la conception est bonne. Si c'est « toute la logique », l'inversion a échoué.\n\n**Raisonnement pas-à-pas** : (1) je modélise l'entité \`Task\` et ses états (statut en union littérale) ; (2) je définis le CONTRAT \`Store\` (ce qu'on peut faire, pas comment) ; (3) la logique métier ne dépendra QUE de \`Store\` ; (4) l'implémentation concrète (JsonStore) sera fournie séparément — interchangeable.\n\n**Conception (types.ts)** :\n\`\`\`ts\ntype Statut = 'todo' | 'doing' | 'done';\ntype Priorite = 'basse' | 'moyenne' | 'haute';\n\ninterface Task {\n  readonly id: number;\n  titre: string;\n  statut: Statut;\n  priorite: Priorite;\n  creeLe: string;   // ISO\n}\n\n// Le CONTRAT : ce qu'on peut faire, sans dire COMMENT (fichier ? base ?)\ninterface Store {\n  all(): Task[];\n  save(taches: Task[]): void;\n  load(): Task[];\n}\n\n// La logique métier dépend de l'ABSTRACTION Store, jamais d'un fichier\nfunction ajouterTache(store: Store, titre: string, priorite: Priorite): Task {\n  const taches = store.load();\n  const id = taches.length ? Math.max(...taches.map(t => t.id)) + 1 : 1;  // max+1\n  const t: Task = { id, titre, statut: 'todo', priorite, creeLe: new Date().toISOString() };\n  store.save([...taches, t]);   // immuable : nouvelle liste\n  return t;\n}\n// Demain, SqliteStore implements Store -> ajouterTache N'EST PAS touchée.\n\`\`\`\nLa logique parle au contrat \`Store\` ; l'implémentation (JSON aujourd'hui, SQLite demain) est branchée par le dehors. Un seul fichier changera.`,
    quiz: [
      { q: "Pourquoi concevoir les types AVANT d'écrire la logique ?", a: "Parce que modéliser le domaine oblige à répondre aux vraies questions (un statut peut-il être vide ? la priorité est-elle obligatoire ?) avant qu'elles ne deviennent des bugs. Un modèle clair rend l'implémentation évidente." },
      { q: "Qu'est-ce que l'inversion de dépendance avec l'interface Store ?", a: "Faire dépendre la logique métier d'un CONTRAT abstrait (Store) plutôt que d'une implémentation concrète (fichier JSON). Le COMMENT devient interchangeable : changer de stockage ne touche pas la logique." },
      { q: "Combien de fichiers doivent changer pour migrer TaskFlow vers SQLite ?", a: "Un seul : la nouvelle implémentation SqliteStore qui respecte le contrat Store. Si la réponse est « toute la logique », l'inversion de dépendance a échoué." },
      { q: "Qu'est-ce qui distingue un jalon démontrable d'une activité floue ?", a: "Un jalon démontrable est vérifiable/cochable (« add + list fonctionnent de bout en bout ») ; une activité floue (« avancer sur le CLI ») ne se coche pas et masque la procrastination." },
    ],
    caseStudy: `L'inversion de dépendance est un pilier de l'architecture logicielle professionnelle : les applications testables et évolutives font dépendre leur cœur métier d'INTERFACES (dépôts, passerelles, services) plutôt que de détails d'infrastructure (base précise, API tierce, système de fichiers). Bénéfices concrets : on peut TESTER la logique avec une fausse implémentation (un Store en mémoire) sans base réelle, CHANGER de fournisseur (SQLite → PostgreSQL → cloud) sans réécrire le métier, et FAIRE ÉVOLUER l'infrastructure indépendamment. C'est le fondement de l'architecture hexagonale (ports & adapters) très demandée sur les postes back. Concevoir une interface Store pour TaskFlow, même petit, démontre en entretien que tu penses découplage et testabilité — et un backlog en jalons démontrables prouve que tu sais planifier un projet, pas seulement coder.`,
    interview: `**« Comment conçois-tu une application pour que changer de base de données ne casse pas la logique métier ? »** → J'applique l'inversion de dépendance : la logique métier ne dépend pas d'une implémentation de stockage, mais d'un CONTRAT abstrait — une interface, par exemple \`Store { load(); save(); all() }\`. La logique parle uniquement à ce contrat ; l'implémentation concrète (un JsonStore, plus tard un SqliteStore ou un PostgresStore) est fournie séparément et branchée « par le dehors ». Résultat : migrer de JSON à SQLite ne change QU'UN fichier — la nouvelle implémentation — sans toucher au métier. C'est le D de SOLID et le principe de l'architecture hexagonale (ports & adapters). Ça rend aussi le métier TESTABLE : je lui injecte un Store en mémoire pour les tests, sans base réelle. En amont, je conçois les TYPES avant la logique, parce que modéliser le domaine (entités, états en union littérale, opérations) fait émerger les décisions avant qu'elles ne deviennent des bugs. **Relance possible** : « quand cette abstraction n'en vaut-elle pas la peine ? » → quand l'implémentation ne variera jamais : ajouter une interface pour un stockage unique et définitif est de la sur-ingénierie. Ici, JSON → SQLite est explicitement prévu, donc elle se justifie.`,
    future: `- **Bankable** : penser en interfaces (inversion de dépendance, testabilité) est un pilier des postes back et de l'architecture hexagonale ; le démontrer sur un petit projet est un signal fort en entretien.\n- L'interface Store conçue ici est directement implémentée aux jours 45-47 (JsonStore) et rebranchée sur SQLite au jour 57 — la migration prouvera le découplage.\n- Concevoir les types avant le code et découper en jalons démontrables sont des réflexes de conception réutilisés à chaque projet du parcours.`,
    solution: {
      logic: "Poser les fondations sans coder la logique : modéliser Task et ses états (union littérale), définir le CONTRAT Store (all/load/save) qui masque la persistance, faire dépendre la future logique de ce contrat (inversion de dépendance), et découper le travail en jalons démontrables. Le critère de réussite : la migration vers SQLite ne changera qu'un fichier.",
      simple: "Écrire types.ts avec Task et une interface Store, plus un backlog. Suffisant pour démarrer.",
      improved: "Modéliser les statuts en union littérale (états illégaux irreprésentables), concevoir Store comme un vrai port (contrat minimal et suffisant), rédiger un backlog uniquement en jalons DÉMONTRABLES, et répondre explicitement par écrit « un seul fichier change pour migrer vers SQLite » en le justifiant. Préparer le README (sections vides) pour cadrer la finition.",
      pitfalls: ["Types trop rigides (impossibles à faire évoluer) ou trop lâches (any qui annule le bénéfice de la conception).", "Jalons non démontrables (« avancer sur le CLI ») qui masquent la procrastination.", "Faire dépendre la logique d'un fichier concret au lieu du contrat Store : l'inversion échoue, la migration touchera tout.", "Sur-concevoir des abstractions pour des besoins hypothétiques : l'interface ne se justifie que si l'implémentation varie réellement (ici oui)."],
      checks: ["types.ts complet : Task, statuts en union littérale, interface Store minimale et suffisante.", "Le backlog ne contient que des jalons démontrables.", "Réponse écrite : un seul fichier change pour migrer vers SQLite, avec justification.", "La logique esquissée dépend de Store (abstraction), jamais d'un accès fichier direct."],
      oral: "Explique la démarche : « je conçois les types d'abord pour comprendre le domaine, puis je fais dépendre la logique d'un contrat Store, pas d'un fichier ». Donne le test de réussite : « migrer vers SQLite = un fichier ». Relie à SOLID et à l'hexagonale pour situer le principe, et à la testabilité (Store en mémoire pour les tests). Mentionner que l'abstraction ne se justifie que parce que le stockage VA réellement changer montre que tu évites la sur-ingénierie — un signe de jugement.",
    },
  },

  // ── Projet 1 — TaskFlow : Store JSON et commandes de base ──
  45: {
    lessons: ['typescript-basics.md', 'clean-code.md'],
    theory: `**Modèle mental : un projet réel commence par le SQUELETTE QUI MARCHE, pas par la fonctionnalité parfaite. Un CLI qui répond « commande inconnue » et persiste une liste vide est un meilleur point de départ qu'une commande \`add\` brillante dans un programme qui ne se lance pas — parce que la boucle de feedback (coder → lancer → constater) doit rester sous 10 secondes pour que tu progresses vite. On construit un tube qui traverse tout, puis on l'épaissit.** Implémenter \`JsonStore\` derrière l'interface du jour 44, c'est donner corps au contrat sans jamais y coupler la logique.\n\nPourquoi le squelette d'abord. Un programme qui ne démarre pas ne donne aucun retour : on code à l'aveugle, les erreurs s'accumulent. Un squelette qui tourne (même minimal) établit la boucle de feedback : chaque ajout se teste immédiatement. C'est le principe du « walking skeleton » : faire traverser une fonctionnalité de bout en bout (CLI → logique → Store → fichier → retour) le plus tôt possible, puis enrichir. La vitesse de la boucle de feedback est le premier facteur de productivité.\n\nLa persistance robuste, depuis les fondations. \`JsonStore\` implémente \`Store\` (jour 44) et recycle le pattern de lecture/écriture du jour 12. Le point délicat est \`charger()\` : il doit DISTINGUER trois cas — fichier ABSENT (premier lancement : normal, on part d'une liste vide), JSON VALIDE (on parse), JSON CORROMPU (erreur : on s'ARRÊTE sans écraser, pour ne pas détruire des données). Confondre « absent » et « corrompu » mène soit à planter au premier lancement, soit à écraser silencieusement des données.\n\nLes identifiants uniques : max+1, jamais length+1. Générer un id avec \`length + 1\` crée des DOUBLONS après suppression : si on a les ids [1,2,3], qu'on supprime le 2 (reste [1,3], length=2), le prochain id serait 3 → collision avec l'existant. \`max(ids) + 1\` (ici 4) garantit l'unicité indépendamment des suppressions. C'est un bug classique et instructif : l'id doit dériver du MAXIMUM existant, pas du nombre d'éléments.\n\nLa différence parsing robuste vs naïf. Un \`JSON.parse\` non protégé plante sur un fichier corrompu et peut, mal géré, mener à écraser. Un parsing robuste enveloppe la lecture, distingue les cas, et échoue de façon SÛRE (on préfère s'arrêter avec un message clair plutôt que corrompre davantage). Tout ce qui entre (fichier, arguments) est hostile jusqu'à validation.\n\nLimites et compromis. Un stockage JSON complet réécrit tout le fichier à chaque sauvegarde : simple et parfait pour un CLI personnel, inadapté à de gros volumes ou à la concurrence (d'où SQLite au jour 57). Les commits atomiques par fonctionnalité racontent la construction — un historique propre est une vitrine pour le recruteur, au prix d'une discipline de commit.\n\nLa règle du jour : **fais marcher un squelette de bout en bout d'abord (boucle de feedback < 10 s), implémente JsonStore derrière le contrat Store, gère les trois cas de fichier (absent/valide/corrompu) et génère les ids en max+1 (jamais length+1). Commits atomiques pour raconter la construction.**`,
    guided: `**Énoncé** : implémenter \`JsonStore\` (chargement robuste, ids en max+1) et la commande \`add\`, en gérant les trois cas de fichier.\n\n**Exemple simple** : le bug d'id à éviter. Avec \`length+1\` : ids [1,2,3], on supprime le 2 → [1,3] (length 2) → prochain id = 3 → DOUBLON. Avec \`max+1\` : max([1,3]) + 1 = 4 → unique. L'id dérive du maximum, pas du compte.\n\n**Raisonnement pas-à-pas** : (1) \`charger()\` distingue fichier absent (→ []), valide (→ parse), corrompu (→ on s'arrête sans écraser) ; (2) \`sauver()\` crée le dossier si besoin puis écrit ; (3) \`add\` calcule l'id en max+1 et persiste une NOUVELLE liste (immuable).\n\n**Implémentation** :\n\`\`\`ts\nimport { readFileSync, writeFileSync, existsSync, mkdirSync } from 'node:fs';\nimport { dirname } from 'node:path';\n\nclass JsonStore implements Store {\n  constructor(private readonly chemin: string) {}\n\n  load(): Task[] {\n    if (!existsSync(this.chemin)) return [];          // ABSENT : premier lancement, normal\n    const brut = readFileSync(this.chemin, 'utf8');\n    try {\n      return JSON.parse(brut) as Task[];              // VALIDE\n    } catch {\n      throw new Error(\`Fichier corrompu : \${this.chemin} (arrêt pour ne pas écraser).\`); // CORROMPU\n    }\n  }\n  save(taches: Task[]): void {\n    mkdirSync(dirname(this.chemin), { recursive: true });   // crée data/ si absent\n    writeFileSync(this.chemin, JSON.stringify(taches, null, 2));\n  }\n  all(): Task[] { return this.load(); }\n}\n\nfunction add(store: Store, titre: string): Task {\n  const taches = store.load();\n  const id = taches.length ? Math.max(...taches.map(t => t.id)) + 1 : 1;   // max+1, PAS length+1\n  const t: Task = { id, titre, statut: 'todo', priorite: 'moyenne', creeLe: new Date().toISOString() };\n  store.save([...taches, t]);   // nouvelle liste, pas de mutation\n  return t;\n}\n\`\`\`\nLe fichier absent ne plante pas, le corrompu arrête proprement, et \`max+1\` évite les doublons — trois pièges désamorcés avant qu'ils ne deviennent des bugs de production.`,
    quiz: [
      { q: "Pourquoi commencer par un « squelette qui marche » plutôt qu'une fonctionnalité parfaite ?", a: "Pour établir la boucle de feedback (coder → lancer → constater) sous 10 secondes. Un programme qui tourne se teste à chaque ajout ; un programme qui ne démarre pas fait coder à l'aveugle." },
      { q: "Quels trois cas `charger()` doit-il distinguer, et pourquoi ?", a: "Fichier absent (premier lancement : liste vide, normal), JSON valide (on parse), JSON corrompu (on s'arrête SANS écraser). Confondre absent et corrompu fait planter au démarrage ou détruit des données." },
      { q: "Pourquoi générer les ids en max+1 et non length+1 ?", a: "length+1 crée des doublons après suppression : ids [1,2,3], on supprime 2 → [1,3] (length 2) → prochain id 3 = collision. max([1,3])+1 = 4 reste unique quelles que soient les suppressions." },
      { q: "Quelle limite a un JsonStore qui réécrit tout le fichier à chaque sauvegarde ?", a: "Simple et parfait pour un CLI personnel, mais inadapté aux gros volumes et à la concurrence (réécriture totale, pas de transactions) — d'où le passage à SQLite au jour 57." },
    ],
    caseStudy: `Les principes de ce jour se retrouvent dans tout logiciel qui persiste des données. Le « walking skeleton » (faire traverser une fonctionnalité de bout en bout tôt) est une pratique répandue pour dé-risquer un projet : on valide l'architecture complète avant d'investir dans les détails. La gestion robuste des cas de fichier (absent/valide/corrompu) est le b.a.-ba de la fiabilité : un service qui plante au premier démarrage (fichier de config absent) ou qui écrase des données sur une lecture corrompue est un incident classique. Le bug d'id length+1 illustre une catégorie d'erreurs (dériver un identifiant du COMPTE plutôt que de l'état) qu'on retrouve jusque dans des systèmes distribués. Et un historique de commits atomiques et clairs est ce qu'un recruteur inspecte pour juger ta rigueur : il raconte comment tu construis, pas seulement ce que tu livres.`,
    interview: `**« Comment gères-tu le chargement d'un fichier de données de façon robuste, et comment génères-tu des identifiants uniques ? »** → Pour le chargement, je distingue explicitement trois cas. Fichier ABSENT : c'est un premier lancement normal, je pars d'une liste vide — pas une erreur. JSON VALIDE : je parse. JSON CORROMPU : je m'ARRÊTE avec un message clair sans écraser le fichier, pour ne pas détruire davantage de données. Confondre « absent » et « corrompu » mène soit à planter au démarrage, soit à écraser silencieusement — deux incidents classiques. Pour les identifiants, j'utilise \`max(ids) + 1\`, jamais \`length + 1\` : avec length+1, supprimer un élément crée des doublons (ids [1,2,3], je supprime le 2, il reste [1,3] de longueur 2, le prochain id serait 3 → collision). L'id doit dériver du MAXIMUM existant, pas du nombre d'éléments. Plus généralement, je fais d'abord marcher un squelette de bout en bout pour avoir une boucle de feedback rapide, puis j'enrichis. **Relance possible** : « et si deux processus écrivent en même temps ? » → mon JsonStore réécrit tout le fichier, ce qui ne résiste pas à la concurrence ; il faudrait un verrou ou une vraie base transactionnelle (SQLite), ce que je mets en place plus tard dans le projet.`,
    future: `- **Bankable** : gérer proprement les cas de fichier (absent/valide/corrompu) et générer des ids sans collision sont des marqueurs de fiabilité qu'un recruteur repère vite dans du code.\n- Le JsonStore implémente le contrat Store du jour 44 ; il sera remplacé par SQLite au jour 57, prouvant concrètement le découplage.\n- Le réflexe « squelette qui marche d'abord, boucle de feedback rapide » et les commits atomiques structurent tous les projets du parcours et rassurent en entretien.`,
    solution: {
      logic: "Donner corps au contrat Store sans y coupler la logique : JsonStore implements Store, chargement qui distingue absent/valide/corrompu, sauvegarde qui crée le dossier, et commandes add/list. Générer les ids en max+1 pour éviter les doublons. Faire marcher un squelette de bout en bout d'abord, puis enrichir, avec des commits atomiques.",
      simple: "JsonStore avec load/save et les commandes add/list qui fonctionnent. Le cœur du projet tourne.",
      improved: "Rendre le chargement ROBUSTE (trois cas explicitement gérés, arrêt sûr sur corruption sans écraser), créer le dossier data/ au besoin, générer les ids en max+1, persister de façon immuable (nouvelle liste), et livrer un historique de commits atomiques par fonctionnalité. Boucle de feedback sous 10 s validée.",
      pitfalls: ["Générer les ids en length+1 : doublons après suppression.", "Oublier le mkdir du dossier data (écriture qui échoue au premier lancement).", "Confondre fichier absent et corrompu : planter au démarrage ou écraser des données valides.", "Muter la liste en place au lieu de persister une nouvelle liste (effets de bord difficiles à tester)."],
      checks: ["add et list fonctionnent de bout en bout avec persistance réelle.", "Les trois cas de fichier (absent, valide, corrompu) sont gérés et testés un par un.", "Les ids restent uniques après une suppression (max+1 prouvé sur un scénario).", "L'historique contient au moins 2 commits atomiques aux messages clairs."],
      oral: "Mets en avant la robustesse : « charger() distingue absent (normal), valide, et corrompu (je m'arrête sans écraser) ». Explique le bug d'id length+1 avec le scénario de suppression et pourquoi max+1 le résout. Souligne le « squelette qui marche d'abord » pour la boucle de feedback. Reconnaître la limite du JsonStore face à la concurrence (et annoncer SQLite) prouve que tu vois les compromis, pas seulement le code qui marche aujourd'hui.",
    },
  },

  // ── Projet 1 — TaskFlow : CRUD complet et filtres ──
  46: {
    lessons: ['clean-code.md'],
    theory: `**Modèle mental : le CRUD d'un vrai outil gère l'ÉTAT en style IMMUABLE — \`done\`/\`edit\` ne MODIFIENT pas une tâche, ils retournent une COPIE modifiée que le Store persiste (ton pattern \`mettreAJourParId\` du jour 26). Bénéfice concret : chaque opération devient une fonction PURE, testable sans fichiers ni montage. Et les filtres combinables (\`--done --priority haute\`) suivent le pattern « filtre conditionnel » — chaque critère ABSENT laisse tout passer — avec un piège \`falsy\` à désamorcer.** Compléter le CRUD, c'est appliquer immutabilité, filtrage et validation aux frontières sur un cas réel.\n\nPourquoi l'immutabilité pour le CRUD. Muter une tâche en place (\`tache.statut = 'done'\`) crée des effets de bord : si plusieurs parties du code partagent la référence, elles changent toutes (le bug de référence partagée du jour 41). En retournant une COPIE modifiée (\`{ ...tache, statut: 'done' }\`), l'original reste intact, l'opération est une fonction pure (mêmes entrées → même sortie), et on peut la tester sans fichiers. \`mettreAJourParId\` (jour 26) applique une transformation à la tâche ciblée et renvoie une nouvelle liste : c'est le cœur réutilisable de done/edit/rm.\n\nLes filtres combinables, depuis les fondations. Chaque filtre est un prédicat ; les combiner, c'est composer des \`filter\` (jour 23). Le principe clé : un critère NON FOURNI ne doit rien filtrer (laisser tout passer). D'où le piège \`falsy\` : tester \`if (priorite)\` échoue si une priorité légitime est une valeur falsy (chaîne vide, 0) — on confond « pas de filtre » et « filtre sur une valeur falsy ». Il faut tester la PRÉSENCE (\`priorite !== undefined\`), pas la véracité (le piège du jour 5).\n\nLa validation aux frontières. Tout argument CLI est HOSTILE jusqu'à preuve du contraire : un id non numérique, une priorité inconnue, une commande mal formée doivent produire un MESSAGE CLAIR et un \`exit 1\`, jamais une stack trace brute (qui fait peur et n'aide pas l'utilisateur). La frontière (l'entrée utilisateur) est l'endroit où l'on valide ; l'intérieur du programme peut alors faire confiance aux données.\n\nLa différence mise à jour immuable vs mutation. La MUTATION change l'objet existant : rapide mais source d'effets de bord et de bugs de partage. L'IMMUABILITÉ crée un nouvel objet : un léger coût mémoire contre la prévisibilité et la testabilité. Pour un CRUD d'outil, l'immutabilité est le bon choix — les volumes sont petits, la sûreté prime.\n\nLimites et compromis. L'immutabilité par copie a un coût sur de très gros volumes (rare ici). Multiplier les filtres peut compliquer la lisibilité — les composer proprement (une liste de prédicats appliqués en chaîne) garde le code clair. Et la validation exhaustive de chaque entrée est un travail ingrat mais décisif pour l'expérience : un outil qui plante avec une stack trace sur une faute de frappe paraît non fini.\n\nLa règle du jour : **gère le CRUD en immuable (retourne des copies modifiées, réutilise mettreAJourParId), compose les filtres en laissant passer les critères absents (attention au piège falsy : teste la présence, pas la véracité), et valide tout argument aux frontières avec un message clair plutôt qu'une stack trace.**`,
    guided: `**Énoncé** : implémenter \`done\` (marquer terminé) en IMMUABLE et un filtrage combinable, en évitant le piège falsy.\n\n**Exemple simple** : le piège falsy sur un filtre. Si je filtre \`if (opts.priorite) garder(...)\`, une priorité légitime \`''\` (ou un filtre « toutes ») serait ignorée. Je dois tester \`opts.priorite !== undefined\` : la PRÉSENCE du critère, pas sa véracité.\n\n**Raisonnement pas-à-pas** : (1) \`done\` renvoie une NOUVELLE liste où seule la tâche ciblée est copiée-modifiée (mettreAJourParId) ; (2) le filtrage compose des prédicats ; (3) un critère absent (undefined) laisse tout passer ; (4) chaque argument invalide produit un message clair, pas une stack trace.\n\n**Implémentation** :\n\`\`\`ts\n// mise à jour IMMUABLE ciblée (pattern du jour 26)\nfunction mettreAJourParId(taches: Task[], id: number, maj: (t: Task) => Task): Task[] {\n  return taches.map(t => (t.id === id ? maj(t) : t));   // copie ciblée, original intact\n}\nfunction done(taches: Task[], id: number): Task[] {\n  return mettreAJourParId(taches, id, t => ({ ...t, statut: 'done' }));  // COPIE modifiée\n}\n\n// filtres COMBINABLES : un critère absent (undefined) laisse tout passer\ninterface Filtres { statut?: Statut; priorite?: Priorite; }\nfunction filtrer(taches: Task[], f: Filtres): Task[] {\n  return taches.filter(t =>\n    (f.statut === undefined || t.statut === f.statut) &&          // présence, pas véracité\n    (f.priorite === undefined || t.priorite === f.priorite));\n}\n\n// validation à la frontière : message clair, jamais de stack trace\nfunction parseId(arg: string): number {\n  const id = Number(arg);\n  if (!Number.isInteger(id) || id <= 0) {\n    console.error(\`Id invalide : \"\${arg}\" (attendu : entier positif).\`);\n    process.exit(1);\n  }\n  return id;\n}\n\`\`\`\nL'original n'est jamais muté, les filtres se combinent sans le piège falsy, et une entrée invalide échoue proprement — un CRUD prévisible et testable.`,
    quiz: [
      { q: "Pourquoi mettre à jour une tâche en IMMUABLE plutôt qu'en la mutant ?", a: "La mutation crée des effets de bord (bug de référence partagée). Retourner une copie modifiée laisse l'original intact, rend l'opération pure et testable sans fichiers." },
      { q: "Qu'est-ce que le piège falsy dans un filtre combinable ?", a: "Tester `if (critere)` ignore une valeur légitime mais falsy (chaîne vide, 0). Il faut tester la PRÉSENCE (`critere !== undefined`), pas la véracité, pour distinguer « pas de filtre » de « filtre sur une valeur falsy »." },
      { q: "Comment un critère de filtre absent doit-il se comporter ?", a: "Il doit laisser TOUT passer (ne rien filtrer) : `f.statut === undefined || t.statut === f.statut`. Seuls les critères fournis restreignent le résultat." },
      { q: "Pourquoi valider les arguments à la frontière avec un message clair ?", a: "Tout argument CLI est hostile jusqu'à validation. Un message clair + exit 1 aide l'utilisateur ; une stack trace brute fait peur, n'aide pas et donne un aspect non fini." },
    ],
    caseStudy: `L'immutabilité et la validation aux frontières sont des piliers du logiciel fiable en entreprise. La mise à jour immuable de l'état est LE modèle de la gestion d'état moderne côté front (Redux, React) : ne jamais muter, toujours retourner un nouvel état, ce qui rend les changements traçables et le débogage possible (time-travel). Côté back, valider chaque entrée à la frontière (API, CLI, message de file) est la première ligne de défense : une entrée non validée est la source n°1 des bugs et des failles (injection, crash). Le piège falsy est une source récurrente de bugs subtils dans le code réel (une quantité 0, une chaîne vide traitées comme « absentes »). Un CRUD propre — immuable, filtres combinables sans piège, validation claire — est exactement ce qu'un recruteur veut voir dans un projet portfolio : il démontre que tu écris du code prévisible et défensif, pas seulement fonctionnel.`,
    interview: `**« Pourquoi mettre à jour l'état de façon immuable, et quel piège guette les filtres conditionnels ? »** → Muter une tâche en place (\`tache.statut = 'done'\`) crée des effets de bord : si une référence est partagée, plusieurs endroits changent ensemble — le bug de référence partagée. En retournant une COPIE modifiée (\`{ ...tache, statut: 'done' }\`), l'original reste intact, l'opération devient une fonction pure, donc testable sans fichiers ni montage. C'est le modèle de la gestion d'état moderne (Redux, React) : jamais muter, toujours un nouvel état, ce qui rend les changements traçables. Pour les filtres combinables, le piège est le \`falsy\` : si je teste \`if (critere)\`, une valeur légitime mais falsy (chaîne vide, 0) est ignorée à tort ; je dois tester la PRÉSENCE du critère (\`critere !== undefined\`), pas sa véracité, pour distinguer « pas de filtre » de « filtre sur une valeur falsy ». Un critère absent doit laisser tout passer. **Relance possible** : « et la robustesse face aux entrées utilisateur ? » → je valide tout argument à la frontière (un id non numérique, une priorité inconnue) avec un message clair et exit 1, jamais une stack trace brute : la frontière valide, l'intérieur fait confiance.`,
    future: `- **Bankable** : la mise à jour immuable de l'état est le modèle de la gestion d'état front (Redux/React) et un signal de code prévisible ; la validation aux frontières est la première défense contre bugs et failles.\n- Le pattern mettreAJourParId et les filtres composables se réinvestissent dans toute manipulation de collections (API, données).\n- Le piège falsy (tester la présence, pas la véracité) est une source récurrente de bugs subtils qu'un développeur mûr désamorce partout.`,
    solution: {
      logic: "Compléter le CRUD en style immuable : done/rm/edit retournent des copies modifiées via mettreAJourParId (jour 26), l'original n'est jamais muté. Composer les filtres (filter du jour 23) en laissant passer les critères absents, avec le test de PRÉSENCE (undefined) pour éviter le piège falsy (jour 5). Valider chaque argument à la frontière avec un message clair et exit 1.",
      simple: "done/rm/edit et un filtrage de base fonctionnent. Le CRUD est complet.",
      improved: "Rendre TOUTES les mises à jour immuables (prouver que l'original n'est jamais muté), composer proprement les filtres combinables en testant la présence des critères (pas leur véracité), et valider chaque entrée à la frontière (id, priorité) avec un message clair plutôt qu'une stack trace. Réutiliser explicitement les patterns du mois 1 (mettreAJourParId, filter, comparateurs).",
      pitfalls: ["Muter les tâches en place au lieu de retourner des copies : effets de bord et bugs de référence partagée.", "Piège falsy : filtres qui ignorent une valeur légitime falsy (chaîne vide, priorité 0) faute de tester la présence.", "Filtres qui ne se combinent pas correctement (un critère absent qui filtre au lieu de tout laisser passer).", "Laisser une entrée invalide produire une stack trace brute au lieu d'un message clair et d'un exit 1."],
      checks: ["done/rm/edit fonctionnent et les mises à jour sont immuables (l'original n'est jamais muté, vérifiable).", "Les filtres se combinent correctement, testés avec deux filtres simultanés.", "Le piège falsy est désamorcé (test de présence undefined, pas de véracité).", "Tout id invalide ou inconnu produit un message clair, jamais une stack trace."],
      oral: "Explique le choix de l'immutabilité par le bug qu'il évite : « je retourne une copie modifiée, l'original reste intact, l'opération est pure et testable — c'est le modèle Redux ». Puis pointe le piège falsy : « je teste la présence du critère, pas sa véracité, sinon une chaîne vide serait confondue avec pas de filtre ». Insister sur la validation aux frontières (message clair, pas de stack trace) montre que tu penses à l'expérience utilisateur et à la robustesse, pas seulement au chemin heureux.",
    },
  },

  // ── Projet 1 — TaskFlow : stats, tests, README, ADR ──
  47: {
    lessons: ['testing-foundations.md', 'readme-documentation.md', 'architecture-basics.md'],
    theory: `**Modèle mental : la finition est ce qui sépare « ça marche sur ma machine » de « c'est FINI ». Un projet livré, ce sont des stats utiles, des cas d'erreur verrouillés UN PAR UN, des tests sur la logique pure, un README qui rend le projet utilisable par un inconnu en 5 minutes, et un ADR qui PROUVE que tu as arbitré consciemment. Le code qui marche est la moitié du travail ; l'autre moitié — celle qui rend le projet crédible et ré-employable — est la documentation, les tests et les décisions tracées.** Cette ligne droite est précisément ce qu'un recruteur inspecte.\n\nPourquoi la finition compte autant que le code. Un projet dont la logique fonctionne mais sans README est INVISIBLE : personne ne sait l'installer, l'utiliser, ni ce qu'il démontre. Un projet sans tests est FRAGILE : chaque modification risque une régression silencieuse. Un projet sans décisions tracées paraît suivre des recettes sans comprendre. La finition transforme un tas de code en un ARTEFACT professionnel — c'est ce qui fait la différence entre un exercice et une pièce de portfolio.\n\nLes tests sur la logique pure, depuis les fondations. Grâce à l'immutabilité (jour 46), les opérations (add, done, filtrer, stats) sont des fonctions pures : mêmes entrées → mêmes sorties, sans fichiers. On peut donc les tester en isolation, rapidement, sans montage. Un test vérifie un comportement précis (« done marque la bonne tâche sans toucher les autres ») ET les cas limites (id inexistant, liste vide). Verrouiller les cas d'erreur UN PAR UN — écrire un test par cas — garantit qu'ils sont tous couverts, pas juste supposés.\n\nLe README qui rend utilisable en 5 minutes. Il doit contenir : à quoi sert le projet, comment l'INSTALLER (commandes exactes), comment l'UTILISER (exemples concrets), l'ARCHITECTURE (le contrat Store, l'inversion de dépendance), et « ce que ça prouve » / « ce que j'ai appris » (3-5 puces honnêtes). Le test : un inconnu clone le dépôt et, en 5 minutes, installe, lance et comprend. Un README bâclé rend le meilleur code invisible.\n\nL'ADR (Architecture Decision Record). Un document d'UNE page qui capture : le CONTEXTE, les OPTIONS considérées, la DÉCISION prise, ses CONSÉQUENCES, et ce qui te ferait CHANGER d'avis (révision). Pour TaskFlow : « JSON vs SQLite pour le stockage ». L'ADR PROUVE que tu arbitres consciemment — c'est une pièce maîtresse en entretien, parce qu'elle montre ton RAISONNEMENT, pas seulement ton résultat. La stats (\`reduce\` par statut/priorité, jour 24) complète l'outil.\n\nLimites et compromis. Tout tester est irréaliste et contre-productif : on teste la LOGIQUE (pure, à valeur) plutôt que le câblage trivial. Un README trop long dilue l'essentiel ; trop court, il n'aide pas — viser l'utilisabilité en 5 minutes. Un ADR ne se justifie que pour les décisions STRUCTURANTES (pas pour chaque choix mineur). La finition prend du temps qu'on est tenté de zapper — c'est justement ce qui distingue les projets finis des projets abandonnés à 90 %.\n\nLa règle du jour : **finis vraiment — stats utiles, tests sur la logique pure, cas d'erreur verrouillés un par un, README qui rend le projet utilisable et compréhensible par un inconnu en 5 minutes, et un ADR qui trace ta décision structurante. La finition est la moitié du travail, et celle que le recruteur regarde.**`,
    guided: `**Énoncé** : écrire un test qui verrouille le comportement de \`done\` (bonne tâche modifiée, autres intactes, id inexistant sans effet) et esquisser l'ADR JSON vs SQLite.\n\n**Exemple simple** : ce qu'un bon test capture. \`done([t1, t2], t1.id)\` doit renvoyer une liste où \`t1\` est \`done\` ET \`t2\` est INCHANGÉE ET l'original n'est pas muté. Trois assertions, un comportement verrouillé.\n\n**Raisonnement pas-à-pas** : (1) je teste la logique pure (pas les fichiers) grâce à l'immutabilité ; (2) une assertion par comportement, plus les cas limites (id inexistant, liste vide) un par un ; (3) l'ADR trace le POURQUOI de la décision de stockage.\n\n**Implémentation (tests avec node:test)** :\n\`\`\`ts\nimport { test } from 'node:test';\nimport assert from 'node:assert/strict';\n\ntest('done marque la bonne tâche sans toucher les autres ni muter l\\'original', () => {\n  const t1: Task = { id: 1, titre: 'A', statut: 'todo', priorite: 'moyenne', creeLe: '' };\n  const t2: Task = { id: 2, titre: 'B', statut: 'todo', priorite: 'haute', creeLe: '' };\n  const avant = [t1, t2];\n  const apres = done(avant, 1);\n  assert.equal(apres.find(t => t.id === 1)!.statut, 'done');   // bonne tâche\n  assert.equal(apres.find(t => t.id === 2)!.statut, 'todo');   // autre intacte\n  assert.equal(t1.statut, 'todo');                             // original NON muté\n});\n\ntest('done sur un id inexistant ne change rien', () => {\n  const taches: Task[] = [{ id: 1, titre: 'A', statut: 'todo', priorite: 'basse', creeLe: '' }];\n  assert.deepEqual(done(taches, 999), taches);   // cas limite verrouillé\n});\n\`\`\`\n\n**L'ADR n°1 (squelette)** :\n\`\`\`md\n# ADR 1 — Stockage : JSON vs SQLite\n## Contexte : TaskFlow doit persister des tâches, CLI mono-utilisateur.\n## Options : (a) fichier JSON — simple, lisible, zéro dépendance ;\n##           (b) SQLite — requêtes, transactions, monte en charge.\n## Décision : JSON pour la v1 (simplicité, boucle de feedback rapide).\n## Conséquences : réécriture totale du fichier à chaque save ; pas de concurrence.\n## Révision : passer à SQLite si volume/concurrence/requêtes le justifient (prévu jour 57).\n\`\`\`\nLe test verrouille un comportement précis et ses cas limites ; l'ADR prouve que le choix de stockage est RAISONNÉ, pas subi.`,
    quiz: [
      { q: "Pourquoi la finition (README, tests, ADR) compte-t-elle autant que le code qui marche ?", a: "Sans README, le projet est invisible (personne ne sait l'utiliser) ; sans tests, il est fragile (régressions) ; sans décisions tracées, il paraît suivre des recettes. La finition transforme du code en artefact professionnel." },
      { q: "Que doit contenir un README pour rendre un projet utilisable en 5 minutes ?", a: "À quoi il sert, comment l'installer (commandes exactes), comment l'utiliser (exemples), l'architecture (contrat Store, inversion de dépendance), et « ce que ça prouve / ce que j'ai appris »." },
      { q: "Qu'est-ce qu'un ADR et pourquoi vaut-il de l'or en entretien ?", a: "Un Architecture Decision Record : contexte, options, décision, conséquences, révision. Il prouve que tu arbitres CONSCIEMMENT et montre ton raisonnement, pas seulement ton résultat." },
      { q: "Pourquoi peut-on tester facilement la logique de TaskFlow ?", a: "Parce que l'immutabilité (jour 46) rend les opérations pures (mêmes entrées → mêmes sorties, sans fichiers) : on les teste en isolation, rapidement, sans montage." },
    ],
    caseStudy: `La finition distingue le développeur professionnel de l'amateur, et c'est mesurable en entreprise. Les tests sur la logique métier attrapent les régressions avant les utilisateurs — indispensable dès qu'une équipe modifie un code commun. Un README de qualité réduit le temps d'onboarding et rend un service maintenable par d'autres que son auteur. Les ADR sont une pratique répandue dans les équipes matures : ils tracent POURQUOI une décision a été prise, évitant que six mois plus tard personne ne se souvienne du contexte (et défasse un bon choix par ignorance). Pour un portfolio, ces éléments sont décisifs : un recruteur qui clone ton projet juge d'abord le README (peut-il l'utiliser ?), puis l'historique de commits et les tests (est-ce rigoureux ?), puis les ADR (raisonne-t-il ?). Un projet « fini » à ce niveau vaut plus que trois projets abandonnés à 90 % — c'est la preuve concrète que tu livres.`,
    interview: `**« Qu'est-ce qui fait qu'un projet est vraiment "fini" au-delà du code qui marche ? »** → La moitié du travail est le code ; l'autre moitié est ce qui rend le projet crédible et ré-employable. D'abord des TESTS sur la logique pure — grâce à l'immutabilité, mes opérations sont des fonctions pures, donc testables sans fichiers ; je verrouille les cas d'erreur UN PAR UN (id inexistant, liste vide), pas seulement le chemin heureux. Ensuite un README qui rend le projet utilisable par un inconnu en 5 minutes : à quoi ça sert, installation, usage, architecture, et « ce que j'ai appris ». Sans README, le meilleur code est invisible. Enfin un ADR — un document d'une page qui trace une décision structurante (ici JSON vs SQLite pour le stockage) avec contexte, options, décision, conséquences et révision : il prouve que j'arbitre consciemment, ce qui vaut de l'or en entretien parce qu'il montre mon raisonnement. Je ne teste pas TOUT (le câblage trivial n'a pas de valeur), je teste la logique ; je ne documente pas chaque choix mineur, je trace les décisions structurantes. **Relance possible** : « pourquoi un ADR plutôt qu'un commentaire dans le code ? » → parce qu'une décision d'architecture dépasse un fichier : l'ADR est un document dédié, retrouvable, qui explique le POURQUOI à toute l'équipe, présente et future.`,
    future: `- **Bankable** : un projet réellement fini (tests, README utilisable en 5 min, ADR) vaut plus que plusieurs projets abandonnés ; c'est ce qu'un recruteur inspecte en premier dans un portfolio.\n- Les tests sur la logique pure préparent la culture de test de tout le parcours ; l'ADR est la première pièce de ton dossier d'entretien (tu sais arbitrer).\n- Le README et la documentation d'architecture (contrat Store) sont réutilisés à chaque projet et démontrent que tu penses maintenabilité, pas seulement fonctionnalité.`,
    solution: {
      logic: "Finir vraiment TaskFlow : commande stats en reduce (jour 24), tests sur la logique pure (rendue testable par l'immutabilité) verrouillant les cas d'erreur un par un, README qui rend le projet utilisable et compréhensible par un inconnu en 5 minutes, et ADR n°1 (JSON vs SQLite) au format complet. Ne pas tout tester (le câblage trivial) mais la logique ; ne pas documenter chaque choix mais les décisions structurantes.",
      simple: "Ajouter stats, quelques tests et un README de base. Le projet est présentable.",
      improved: "Tester la logique pure en verrouillant CHAQUE cas d'erreur (id inexistant, liste vide) un par un, rédiger un README qui passe le test « inconnu opérationnel en 5 minutes » (install/usage/archi/appris), et écrire l'ADR n°1 au format complet (contexte/options/décision/conséquences/révision). Stats en reduce par statut et priorité.",
      pitfalls: ["README bâclé : le projet devient invisible et inutilisable par un tiers, quel que soit le code.", "Cas d'erreur non testés un par un : on suppose la couverture au lieu de la garantir.", "Vouloir tout tester (câblage trivial) au lieu de cibler la logique à valeur.", "ADR absent ou réduit à une phrase : la décision de stockage paraît subie, pas raisonnée."],
      checks: ["Toutes les commandes de la spec passent, stats incluse (reduce par statut/priorité).", "Les tests couvrent la logique et verrouillent les cas d'erreur un par un.", "Un inconnu peut installer, utiliser et comprendre l'architecture en 5 minutes via le README.", "L'ADR n°1 est écrit au format complet (contexte/options/décision/conséquences/révision)."],
      oral: "Insiste sur l'idée que « fini » dépasse « ça marche » : tests sur la logique pure (rendue testable par l'immutabilité), README utilisable en 5 minutes, ADR qui trace le raisonnement. Explique pourquoi l'ADR vaut de l'or (il montre comment tu arbitres, pas juste le résultat) et pourquoi on cible la logique plutôt que tout tester. Montrer que tu vois la finition comme la moitié du travail — celle que le recruteur regarde — te distingue de ceux qui livrent du code brut.",
    },
  },

  // ── Consolidation mois 2 : katas structures + entretien blanc algo ──
  48: {
    lessons: ['algorithmic-thinking.md', 'interview-preparation.md'],
    theory: `**Modèle mental : le RAPPEL ACTIF sous chronomètre est le seul diagnostic fiable de ce qui est ANCRÉ (disponible sans aide) versus ce qui a seulement été « vu ». Relire ses notes crée une illusion de maîtrise ; se forcer à REPRODUIRE de mémoire, sous contrainte de temps, révèle la vérité. Et VERBALISER en codant n'est pas un supplément : en entretien technique, le recruteur évalue ta DÉMARCHE (reformuler, exemples, plan, code, vérification) plus que ta solution finale. Le silence est l'erreur n°1.** Ce jour n'apprend rien de neuf : il MESURE ce qui est solide et transforme la révision en programme ciblé.\n\nPourquoi le rappel actif surpasse la relecture. La mémoire se renforce par la RÉCUPÉRATION, pas par la ré-exposition. Relire un kata résolu donne un sentiment de familiarité trompeur ; le refaire de mémoire, sans filet, force le cerveau à reconstruire le chemin — et c'est cette reconstruction qui ancre. Le chronomètre ajoute la pression de l'entretien réel et empêche de « tricher » en réfléchissant indéfiniment. Les katas RATÉS ne sont pas un échec : ce sont exactement ton programme de révision de la semaine, identifié objectivement.\n\nLe format entretien, depuis les fondations. Un entretien technique n'est pas un examen où l'on rend une copie : c'est une conversation où l'on PENSE À VOIX HAUTE. La démarche attendue : (1) REFORMULER le problème (montrer qu'on a compris) ; (2) donner des EXEMPLES et cas limites ; (3) esquisser un PLAN avant de coder ; (4) CODER en expliquant ; (5) VÉRIFIER sur un exemple. Le recruteur cherche à voir comment tu RAISONNES et réagis aux indices, pas seulement si tu trouves — un candidat qui code en silence la bonne solution marque moins qu'un candidat qui verbalise une démarche claire, même imparfaite.\n\nLa différence « savoir » vs « savoir sous pression ». Résoudre un kata tranquillement chez soi et le résoudre en 15 minutes, sous le regard, en expliquant, sont deux compétences distinctes. La seconde ne s'acquiert QUE par l'entraînement en conditions réelles : chronomètre, pas de documentation, verbalisation enregistrée. S'enregistrer et se réécouter avec une grille est le moyen le plus efficace de corriger le silence, les hésitations et les sauts de raisonnement.\n\nLes katas du jour et ce qu'ils ancrent. Parenthèses (pile), BFS (file), twoSum (hash map), inorder BST (récursion + propriété triée), permutations (backtracking), LRU (Map + ordre) : ils balaient toutes les structures de la semaine 5. Les refaire sous chrono mesure la consolidation réelle depuis le jour 20 — l'écart de temps est ta métrique de progrès.\n\nLimites et compromis. Le rappel actif est INCONFORTABLE (on préfère la relecture rassurante) : c'est justement pourquoi il marche. Le diagnostic exige une HONNÊTETÉ totale — s'aider « juste pour vérifier » pendant le chrono fausse la mesure. Et un entretien blanc solo (enregistré) ne remplace pas un vrai interlocuteur qui pose des indices, mais c'est la meilleure approximation disponible en autonomie.\n\nLa règle du jour : **diagnostique par rappel actif sous chrono, sans aucune aide, avec une honnêteté totale — les katas ratés deviennent ton programme de révision. Entraîne-toi à VERBALISER en codant (enregistre-toi, réécoute avec une grille) : en entretien, la démarche exprimée compte plus que la solution, et le silence est l'erreur n°1.**`,
    guided: `**Énoncé** : mener un kata en format entretien (twoSum), en VERBALISANT chaque étape de la démarche, puis s'auto-évaluer.\n\n**Exemple simple** : la démarche à voix haute sur twoSum. « Je reformule : trouver deux indices dont les valeurs somment à la cible. Exemple : [2,7,11], cible 9 → [0,1]. Cas limites : aucune paire, doublons. Plan : une passe + Map du complément, O(n). Je code… je vérifie sur l'exemple. » — c'est CETTE narration que le recruteur note.\n\n**Raisonnement pas-à-pas (la grille d'entretien)** : (1) reformuler ; (2) exemples + cas limites ; (3) approche naïve puis optimisée annoncées ; (4) coder en expliquant ; (5) vérifier ; (6) discuter la complexité. Je m'enregistre, puis je me réécoute et je coche chaque étape.\n\n**Protocole d'auto-évaluation** :\n\`\`\`text\nKATA : twoSum (chrono : ____ min, cible < 10 min)\n[ ] J'ai REFORMULÉ le problème avant de coder\n[ ] J'ai donné un EXEMPLE et cité les cas limites (aucune paire, doublons)\n[ ] J'ai annoncé l'approche naïve O(n²) PUIS l'optimisée O(n)\n[ ] J'ai VERBALISÉ pendant que je codais (pas de silence > 15 s)\n[ ] J'ai VÉRIFIÉ ma solution sur l'exemple\n[ ] J'ai énoncé la complexité (temps ET mémoire) et le compromis\n\nDIAGNOSTIC : réussi sans aide ? / temps vs jour 20 ? / à revoir : ____\n\`\`\`\nLe même protocole s'applique aux 6 katas (parenthèses, BFS, twoSum, inorder, permutations, LRU). Ceux échoués ou silencieux forment ton plan de révision — précis, pas vague.`,
    quiz: [
      { q: "Pourquoi le rappel actif sous chrono diagnostique-t-il mieux que la relecture ?", a: "La mémoire se renforce par la RÉCUPÉRATION, pas la ré-exposition. Relire donne une illusion de maîtrise ; reproduire de mémoire sous contrainte révèle ce qui est réellement ancré." },
      { q: "Qu'évalue un recruteur en entretien technique, au-delà de la solution ?", a: "La DÉMARCHE : reformuler, donner des exemples/cas limites, planifier, coder en expliquant, vérifier. Il regarde comment tu raisonnes et réagis aux indices, pas seulement si tu trouves." },
      { q: "Pourquoi le silence est-il l'erreur n°1 en entretien technique ?", a: "Parce que le recruteur ne peut pas évaluer un raisonnement qu'il n'entend pas : coder en silence la bonne solution marque moins que verbaliser une démarche claire, même imparfaite." },
      { q: "À quoi servent les katas RATÉS du diagnostic ?", a: "Ils identifient objectivement ton programme de révision de la semaine : ce qui n'est pas ancré, précisément, plutôt qu'une impression vague de ce qu'il faudrait revoir." },
    ],
    caseStudy: `L'entretien technique est un filtre incontournable pour décrocher un poste de développeur, et la capacité à VERBALISER sa démarche y est aussi décisive que la compétence algorithmique. Des candidats techniquement solides échouent parce qu'ils codent en silence, ne reformulent pas, ne vérifient pas — le recruteur ne peut alors pas juger leur raisonnement ni les aider avec des indices. À l'inverse, un candidat qui pense à voix haute, structure sa démarche et discute les compromis rassure, même s'il ne trouve pas la solution optimale du premier coup. Cette compétence — penser à voix haute sous pression — se travaille exactement comme ici : rappel actif chronométré, enregistrement, auto-évaluation avec grille. C'est aussi utile en poste : expliquer son raisonnement en pair programming, en revue de code ou en réunion technique repose sur les mêmes réflexes de verbalisation claire.`,
    interview: `**« Comment te prépares-tu aux entretiens techniques, et pourquoi la manière de résoudre compte-t-elle autant que la solution ? »** → Je m'entraîne par rappel actif sous chronomètre : je refais des katas de mémoire, sans aide, parce que relire mes notes ne fait que créer une illusion de maîtrise — seul reproduire sous contrainte révèle ce qui est vraiment ancré. Les katas que je rate deviennent mon programme de révision, identifié objectivement. Et je m'entraîne surtout à VERBALISER en codant, parce qu'en entretien le recruteur évalue ma DÉMARCHE autant que ma solution : je reformule le problème, je donne un exemple et les cas limites, j'annonce l'approche naïve puis l'optimisée, je code en expliquant, je vérifie, je discute la complexité. Le silence est l'erreur n°1 : coder juste sans parler marque moins que verbaliser une démarche claire, même imparfaite — le recruteur cherche à voir comment je raisonne et réagis à ses indices. Je m'enregistre et me réécoute avec une grille pour corriger les silences et les sauts de raisonnement. **Relance possible** : « et si tu bloques en entretien ? » → je verbalise le blocage (« je pense à telle piste, mais elle coince ici »), je reviens à un exemple concret, et je propose une solution naïve quitte à l'optimiser ensuite : montrer une démarche vaut mieux que se figer en silence.`,
    future: `- **Bankable** : l'entretien technique est le filtre d'embauche n°1 ; savoir verbaliser sa démarche sous pression est aussi décisif que la compétence algorithmique et se travaille délibérément.\n- Le rappel actif chronométré est la méthode d'apprentissage la plus efficace, réutilisable pour consolider n'importe quel sujet du parcours.\n- Penser à voix haute clairement sert aussi en poste : pair programming, revue de code, réunions techniques reposent sur les mêmes réflexes.`,
    solution: {
      logic: "Se DIAGNOSTIQUER honnêtement par rappel actif chronométré, sans aide : 6 katas couvrant les structures de la semaine, temps notés et comparés au jour 20, puis un problème résolu à voix haute et enregistré, auto-évalué avec une grille d'entretien. Les katas ratés ou silencieux deviennent le plan de révision. La compétence travaillée n'est pas seulement algorithmique : c'est la VERBALISATION de la démarche.",
      simple: "Refaire les 6 katas et un exercice à voix haute. On révise.",
      improved: "Tenir des conditions d'entretien STRICTES (chrono, zéro documentation, honnêteté totale), s'ENREGISTRER pendant l'exercice à voix haute et se réécouter avec une grille (reformuler/exemples/plan/coder en parlant/vérifier/complexité), comparer les temps au jour 20 pour mesurer le progrès, et écrire un plan de rattrapage précis pour les katas ratés.",
      pitfalls: ["Relire ses fichiers « pour vérifier » pendant le diagnostic : cela fausse la mesure de ce qui est ancré.", "Coder en silence à l'entretien blanc : on perd des points sur la démarche, l'erreur n°1.", "Se contenter d'un sentiment vague de maîtrise (illusion de la relecture) au lieu du rappel actif inconfortable.", "Ne pas noter ni comparer les temps : on perd la métrique objective de progrès."],
      checks: ["Les 6 katas sont tentés sans aide, temps notés, avec honnêteté totale.", "L'exercice à voix haute est enregistré et réécouté avec la grille d'entretien.", "Les temps sont comparés à ceux du jour 20 (progrès mesuré).", "Un plan de rattrapage précis est écrit pour les katas ratés ou résolus en silence."],
      oral: "Explique ta méthode d'entraînement : « rappel actif chronométré sans aide, parce que relire ne fait qu'illusion ; les katas ratés deviennent mon programme de révision ». Insiste sur la verbalisation : « en entretien, la démarche exprimée compte plus que la solution ; le silence est l'erreur n°1 ». Décrire ta grille (reformuler → exemples → plan → coder en parlant → vérifier) et le fait que tu t'enregistres montre une préparation méthodique qui rassure autant qu'une bonne solution.",
    },
  },

  // ── Revue semaine 7 : FP typée, Projet 1 TaskFlow, consolidation ──
  49: {
    reviewSynthese: `La semaine 7 a produit ta première pièce de portfolio et consolidé le mois 2. La FP typée (jour 43) : transformer pureté et immutabilité en contrats vérifiés par le compilateur (compose/pipe génériques, readonly). Le Projet 1 TaskFlow, de la conception à la finition : concevoir les types et l'interface Store d'abord — inversion de dépendance, un seul fichier change pour migrer (jour 44) ; JsonStore robuste (absent/valide/corrompu) et ids en max+1 (jour 45) ; CRUD immuable et filtres combinables sans piège falsy (jour 46) ; finition — stats, tests sur la logique pure, README utilisable en 5 minutes, ADR qui trace la décision (jour 47). Puis la consolidation par rappel actif chronométré et entretien blanc verbalisé (jour 48). Le fil rouge : **on ne code plus seulement des fonctions — on conçoit une petite architecture découplée (dépendre d'un contrat, pas d'une implémentation), on la finit vraiment (tests, README, ADR), et on sait la défendre à l'oral.** TaskFlow est la preuve concrète que tu livres, et l'inversion de dépendance posée ici sera validée au jour 57 (migration SQLite). Tu entres dans le mois 3 (HTTP, API, SQL) avec un vrai projet derrière toi.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Typer compose/pipe et exploiter readonly (conventions devenues contrats) | |\n| Concevoir une interface (Store) et expliquer l'inversion de dépendance | |\n| Gérer une persistance robuste (absent/valide/corrompu) et des ids en max+1 | |\n| Mettre à jour l'état en immuable et composer des filtres sans piège falsy | |\n| Finir un projet : tests sur la logique, README utilisable en 5 min, ADR complet | |\n| Se diagnostiquer par rappel actif et verbaliser une démarche d'entretien | |\n\n**Total /18** : ≥ 15 = TaskFlow est une vraie pièce de portfolio, cap sur le mois 3 · 11-14 = consolider les items ≤ 1 · < 11 = finir/reprendre TaskFlow avant HTTP, car c'est ta première preuve d'employabilité.`,
    remediation: `- **compose/readonly mal typés** : rejoue le jour 43 — chaque étape reçoit le type de la précédente ; readonly interdit la mutation à la compilation.\n- **Inversion de dépendance floue** : jour 44 — la logique dépend du contrat Store, pas d'un fichier ; teste « un seul fichier change pour migrer ».\n- **Persistance fragile / ids en doublon** : jour 45 — distingue absent/valide/corrompu, génère les ids en max+1 (jamais length+1).\n- **Mutation d'état / piège falsy** : jour 46 — retourne des copies modifiées (mettreAJourParId), teste la présence des filtres, pas leur véracité.\n- **Projet non fini** : jour 47 — tests sur la logique, README « inconnu opérationnel en 5 min », ADR au format complet.\n- **Diagnostic biaisé / silence en entretien** : jour 48 — rappel actif sans aide, verbalise la démarche, enregistre-toi.`,
    interview: `1. **« Qu'apporte le typage à la programmation fonctionnelle ? »** Attendu : conventions (pureté, immutabilité) transformées en contrats vérifiés ; compose typé garantit l'emboîtement, readonly interdit la mutation à la compilation.\n2. **« Comment isoler la logique métier du stockage ? »** Attendu : inversion de dépendance, dépendre d'une interface Store, migration = un fichier, testabilité par implémentation en mémoire.\n3. **« Comment charger un fichier de données robustement et générer des ids uniques ? »** Attendu : distinguer absent/valide/corrompu (arrêt sans écraser), ids en max+1 pas length+1.\n4. **« Pourquoi mettre à jour l'état en immuable ? »** Attendu : éviter les effets de bord/références partagées, opérations pures testables, modèle de la gestion d'état front.\n5. **« Qu'est-ce qui rend un projet fini, et comment te prépares-tu aux entretiens ? »** Attendu : tests sur la logique, README utilisable en 5 min, ADR qui trace la décision ; rappel actif chronométré, verbalisation de la démarche.`,
  },

  // ────────────────────────────────────────────────────────────────────────
  // MOIS 3 · Semaine 8 (jours 50-55) : HTTP, REST, Express, validation, SQL
  // ────────────────────────────────────────────────────────────────────────

  // ── HTTP en profondeur : le protocole du web ──
  50: {
    lessons: ['http-rest-json.md'],
    theory: `**Modèle mental : HTTP est un protocole texte REQUÊTE/RÉPONSE SANS ÉTAT. « Sans état » signifie que chaque requête est AUTONOME et contient TOUT ce qu'il faut pour être traitée (méthode, URL, headers, corps) — le serveur répond puis OUBLIE tout. Les méthodes (GET/POST/PUT/PATCH/DELETE) portent une SÉMANTIQUE, et les statuts (2xx/3xx/4xx/5xx) forment un LANGAGE : choisir le bon statut et la bonne méthode EST de la conception d'API, pas un détail.** Comprendre HTTP en profondeur, c'est parler la langue de tout le web — chaque API, chaque page, chaque appel réseau.\n\nPourquoi HTTP est sans état, et ce que ça implique. Rendre chaque requête autonome permet de faire PASSER À L'ÉCHELLE : n'importe quel serveur peut traiter n'importe quelle requête, sans mémoire partagée, donc on peut en ajouter à volonté (répartition de charge). Le prix : l'état (qui suis-je, mon panier) doit être TRANSPORTÉ à chaque requête — via un token d'authentification, un cookie, un identifiant. C'est pourquoi l'authentification se rejoue à chaque appel : le serveur ne « se souvient » de rien.\n\nLes méthodes et leur sémantique, depuis les fondations. GET LIT (jamais d'effet de bord — un GET ne doit RIEN modifier). POST CRÉE (ou déclenche une action). PUT REMPLACE une ressource entière. PATCH MODIFIE partiellement. DELETE SUPPRIME. Cette sémantique est un CONTRAT : un client, un cache, un proxy s'y fient. Un GET qui modifierait des données casserait les caches et les attentes de tout l'écosystème.\n\nL'idempotence, un concept clé. Une méthode est IDEMPOTENTE si la rejouer produit le même effet que l'appeler une fois. GET, PUT, DELETE le sont (relire, re-remplacer, re-supprimer ne change rien de plus) ; POST ne l'est PAS (deux POST créent deux ressources). C'est CRUCIAL pour les RETRIES réseau : si une réponse se perd, on peut rejouer sans risque une requête idempotente, pas un POST (d'où les mécanismes d'idempotency-key pour les paiements).\n\nLes statuts comme langage, et les distinctions à ne pas rater. 2xx SUCCÈS (200 ok, 201 créé, 204 sans contenu). 3xx REDIRECTION. 4xx erreur du CLIENT (400 requête invalide, 401 NON AUTHENTIFIÉ — « je ne sais pas qui tu es », 403 INTERDIT — « je sais qui tu es mais tu n'as pas le droit », 404 absent, 409 conflit). 5xx erreur du SERVEUR. La confusion classique 401 vs 403 trahit une compréhension superficielle ; renvoyer 200 pour tout (avec un « error » dans le corps) prive les clients et les outils du langage des statuts.\n\nLe cycle complet, pour situer. Taper une URL déclenche : DNS (nom → IP) → TCP (connexion) → TLS (chiffrement en HTTPS) → HTTP (requête/réponse). Savoir dessiner cette chaîne montre qu'on comprend ce qui se passe « sous » l'API.\n\nLimites et compromis. Le sans-état simplifie la montée en charge mais reporte la gestion de l'état sur le client/token. Choisir le bon statut demande de la rigueur (la tentation du 200-pour-tout est réelle). HTTP/1.1 vs HTTP/2 (multiplexage) sont des raffinements de performance à connaître de nom. Et un statut mal choisi n'empêche pas le fonctionnement — il dégrade l'interopérabilité, silencieusement.\n\nLa règle du jour : **HTTP est sans état (chaque requête autonome porte tout, le serveur oublie), les méthodes portent une sémantique (GET lit sans effet de bord, POST crée…), l'idempotence (GET/PUT/DELETE) sécurise les retries, et les statuts sont un langage (distingue 401 « non authentifié » de 403 « interdit »). Choisir méthode et statut EST de la conception d'API.**`,
    guided: `**Énoncé** : explorer une API avec \`curl\` — un GET, un POST JSON, et l'observation des statuts — pour voir HTTP « en vrai ».\n\n**Exemple simple** : la différence de statuts qui parle. Un GET sur une ressource existante → 200. Un GET sur une ressource absente → 404. Un POST qui crée → 201. Une requête sans jeton → 401 ; avec jeton mais sans droit → 403. Le statut RÉSUME le résultat sans lire le corps.\n\n**Raisonnement pas-à-pas** : (1) un GET lit (aucun effet de bord) et renvoie 200 + un corps ; (2) un POST envoie un corps JSON (header \`Content-Type\`) et renvoie 201 + la ressource créée ; (3) j'observe le statut et les headers avec \`-i\` ; (4) je distingue 401 (non authentifié) de 403 (interdit).\n\n**Exploration curl** :\n\`\`\`bash\n# GET : lire (idempotent, sans effet de bord). -i affiche statut + headers\ncurl -i https://api.exemple.dev/taches/1\n# -> HTTP/1.1 200 OK ... { \"id\": 1, \"titre\": \"...\" }\n\n# GET absent -> 404 (le statut dit \"pas trouvé\", pas besoin de lire le corps)\ncurl -i https://api.exemple.dev/taches/9999      # -> 404 Not Found\n\n# POST : créer. Content-Type + corps JSON. Non idempotent (2 POST = 2 créations)\ncurl -i -X POST https://api.exemple.dev/taches \\\n     -H \"Content-Type: application/json\" \\\n     -d '{ \"titre\": \"Apprendre HTTP\" }'\n# -> HTTP/1.1 201 Created ... { \"id\": 42, \"titre\": \"Apprendre HTTP\" }\n\n# Sans jeton -> 401 (non authentifié) ; avec jeton sans droit -> 403 (interdit)\ncurl -i https://api.exemple.dev/admin              # -> 401 Unauthorized\ncurl -i -H \"Authorization: Bearer TOKEN_USER\" https://api.exemple.dev/admin  # -> 403 Forbidden\n\`\`\`\nChaque statut est une information exploitable par le client ; répondre 200 partout la détruirait. Rejouer le GET est sûr (idempotent) ; rejouer le POST créerait un doublon.`,
    quiz: [
      { q: "Que signifie « HTTP est sans état » et quelle en est la conséquence ?", a: "Chaque requête est autonome et contient tout ; le serveur répond puis oublie. Conséquence : l'état (identité, panier) doit être transporté à chaque requête (token/cookie), et l'auth se rejoue à chaque appel — mais on peut ajouter des serveurs à volonté." },
      { q: "Quelle est la différence entre 401 et 403 ?", a: "401 = non AUTHENTIFIÉ (« je ne sais pas qui tu es »). 403 = INTERDIT (« je sais qui tu es, mais tu n'as pas le droit »). Les confondre trahit une compréhension superficielle de HTTP." },
      { q: "Qu'est-ce que l'idempotence et pourquoi est-ce crucial pour les retries ?", a: "Une méthode idempotente donne le même effet qu'on l'appelle une ou plusieurs fois (GET/PUT/DELETE). On peut donc rejouer sans risque après une réponse perdue — contrairement à POST (deux POST = deux créations)." },
      { q: "Pourquoi un GET ne doit-il jamais modifier de données ?", a: "Parce que la sémantique de GET (lire, sans effet de bord) est un contrat sur lequel se fient clients, caches et proxies. Un GET qui modifie casserait les caches et les attentes de tout l'écosystème." },
    ],
    caseStudy: `HTTP est le protocole de TOUT le web : chaque appel d'API, chaque page chargée, chaque intégration entre services passe par lui. Maîtriser sa sémantique est un prérequis absolu pour le développement back et l'intégration. Concrètement : le caractère sans état fonde l'architecture des services scalables (on ajoute des serveurs derrière un répartiteur de charge parce qu'aucun ne détient d'état) ; l'idempotence est vitale pour la fiabilité (les systèmes de paiement utilisent des idempotency-keys pour qu'un retry après une réponse perdue ne débite pas deux fois) ; le choix des statuts détermine si une API est correctement consommable par d'autres équipes et outils (monitoring, caches, clients générés). La confusion 401/403 ou le « 200 pour tout » sont des red flags en revue de code et en entretien back. Savoir dessiner le cycle DNS→TCP→TLS→HTTP montre qu'on comprend l'infrastructure sous l'API, pas seulement l'API.`,
    interview: `**« Explique ce que "HTTP est sans état" implique, et la différence entre 401 et 403. »** → Sans état signifie que chaque requête est AUTONOME : elle contient tout ce qu'il faut (méthode, URL, headers, corps), le serveur la traite puis OUBLIE tout. La conséquence pratique est double : d'un côté ça permet de passer à l'échelle — n'importe quel serveur peut traiter n'importe quelle requête, donc on en ajoute derrière un répartiteur de charge ; de l'autre, l'état (mon identité, mon panier) doit être TRANSPORTÉ à chaque requête, via un token ou un cookie, et l'authentification se rejoue à chaque appel. Pour 401 vs 403 : 401 veut dire « non AUTHENTIFIÉ » — le serveur ne sait pas qui tu es (jeton absent ou invalide) ; 403 veut dire « INTERDIT » — il sait qui tu es, mais tu n'as pas le droit d'accéder à cette ressource. Plus largement, les statuts sont un langage (2xx succès, 4xx erreur client, 5xx erreur serveur) et choisir le bon EST de la conception d'API — répondre 200 pour tout prive les clients et les outils de cette information. **Relance possible** : « pourquoi l'idempotence compte-t-elle ? » → parce qu'une méthode idempotente (GET/PUT/DELETE) peut être rejouée sans risque après une réponse réseau perdue, contrairement à POST qui créerait un doublon — d'où les idempotency-keys pour les paiements.`,
    future: `- **Bankable** : maîtriser la sémantique HTTP (sans-état, méthodes, idempotence, statuts) est un prérequis des postes back ; la confusion 401/403 ou le « 200 pour tout » sont des red flags en entretien.\n- Ce socle conditionne tout le mois 3 : REST design (jour 51), Express (jours 52-53), validation et erreurs centralisées (jour 54).\n- Le sans-état fonde l'authentification par token (mois ultérieurs) et l'architecture des services scalables ; l'idempotence fonde la fiabilité des retries et des paiements.`,
    solution: {
      logic: "Comprendre HTTP comme un protocole sans état requête/réponse où méthodes et statuts portent une SÉMANTIQUE. Explorer concrètement avec curl (GET, POST JSON, headers, redirection, statuts), documenter 8 statuts avec leur cas d'usage précis, et distinguer les concepts clés : sans-état, idempotence, 401 vs 403. Rejouer dans Postman une collection organisée et versionnée. La preuve : savoir choisir méthode et statut, et dessiner le cycle complet.",
      simple: "Faire des GET/POST avec curl et noter les statuts observés. On voit HTTP fonctionner.",
      improved: "Explorer méthodiquement (GET, POST JSON avec Content-Type, headers via -i, redirection), documenter 8 statuts avec un cas d'usage PRÉCIS chacun (dont 401 vs 403, 201, 204, 409), expliquer le sans-état et l'idempotence, organiser une collection Postman versionnée dans Git, et savoir dessiner DNS→TCP→TLS→HTTP.",
      pitfalls: ["Répondre 200 pour tout : les statuts sont de l'information exploitable par les clients et les outils.", "Confondre 401 (non authentifié) et 403 (interdit) : distinction sémantique essentielle.", "Croire qu'un GET peut modifier des données : il doit être sans effet de bord (contrat des caches/proxies).", "Ignorer l'idempotence : rejouer un POST après une réponse perdue crée un doublon."],
      checks: ["Exploration curl complète (GET, POST JSON, headers, redirection) réalisée sans notes.", "8 statuts documentés avec leur cas d'usage précis, dont la distinction 401/403.", "Le caractère sans-état et l'idempotence sont expliqués avec un exemple.", "La collection Postman est organisée et versionnée dans Git."],
      oral: "Structure autour de « sans état » : chaque requête autonome, le serveur oublie, d'où la scalabilité et le transport de l'état par token. Enchaîne sur les statuts comme langage et martèle la distinction 401 (non authentifié) / 403 (interdit). Mentionner l'idempotence pour les retries et savoir dessiner DNS→TCP→TLS→HTTP montre que tu comprends l'infrastructure sous l'API — un niveau au-dessus de « je sais appeler une API ».",
    },
  },
};
