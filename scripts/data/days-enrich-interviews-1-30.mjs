// Chantier B — Questions d'entretien SPÉCIFIQUES pour les jours 1-30.
// Remplace les 3 questions génériques par compétence (git commit / valeur-vs-référence /
// complexité) réutilisées sur 25 jours, par une question distincte, ancrée sur le contenu
// EXACT de chaque journée et sur le niveau atteint à ce moment du cursus.
// Ce fichier ne fournit QUE le champ `interview` ; il est fusionné en DERNIER dans
// generate-curriculum.mjs et n'écrase donc aucun autre champ (théorie/exemple guidé/exercice/
// correction restent inchangés). Jours NON traités : 30 (déjà distinct), 7/14/21/28 (revues).
// Aucune question n'utilise un concept enseigné après la journée concernée.
// Format : question-mise en situation · ce que ça évalue · réponse attendue · niveaux
//   débutant/correct/excellent · relance · à éviter · formulation orale.

export const ENRICH_INTERVIEWS_1_30 = {
  // ══════════════════════════════════════════════════════════════════════════
  // SOUS-BATCH B1 — jours 1 à 10 (affectés : 1,2,3,4,5,6,8,9,10)
  // ══════════════════════════════════════════════════════════════════════════

  // ── Jour 1 — Installation de l'environnement et navigation au terminal ──
  1: {
    interview: `**Mise en situation : « On te donne l'accès à une machine sans interface graphique. Tu arrives dans un projet inconnu. Comment retrouves-tu OÙ tu es, ce que le dossier contient, et comment y descendre — uniquement au terminal ? »**\n\n*Ce que la question évalue :* le modèle mental du système de fichiers comme une arborescence, et l'autonomie au terminal (pas la mémorisation d'une liste de commandes).\n\n**Réponse attendue :** \`pwd\` pour savoir où je suis (le chemin courant), \`ls\` (ou \`ls -la\`) pour voir le contenu, y compris les fichiers cachés, \`cd <dossier>\` pour descendre et \`cd ..\` pour remonter. J'insiste sur la distinction chemin ABSOLU (depuis la racine \`/\`) vs RELATIF (depuis où je suis).\n\n**Débutant / correct / excellent :** débutant = cite \`ls\`/\`cd\` sans \`pwd\` ni la notion d'arborescence ; correct = les trois + relatif/absolu ; excellent = ajoute \`ls -la\` pour les fichiers cachés, \`cd -\` pour revenir au précédent, et explique pourquoi le terminal est plus rapide et scriptable que l'explorateur.\n\n**Relance possible :** « et si tu ne sais plus dans quel dossier tu es après plusieurs \`cd\` ? » → \`pwd\` donne toujours la réponse absolue.\n\n**À éviter :** répondre « je regarderais dans l'explorateur de fichiers » (la question exclut le graphique) ; confondre \`cd ..\` (remonter) et \`cd ~\` (aller au home).\n\n**Formulation orale naturelle :** « D'abord \`pwd\` pour me situer, puis \`ls -la\` pour voir ce qu'il y a, y compris les fichiers cachés, et je navigue avec \`cd\` — en gardant en tête que le système de fichiers est un arbre. »`,
  },

  // ── Jour 2 — Terminal avancé (flux, droits) et premier script Node ──
  2: {
    interview: `**Mise en situation : « Tu veux enregistrer la liste des fichiers d'un dossier dans un fichier \`inventaire.txt\`, sans ouvrir d'éditeur. Puis tu veux AJOUTER une seconde liste à la suite. Comment fais-tu, et quelle est la différence entre les deux opérations ? »**\n\n*Ce que la question évalue :* la compréhension des flux (redirection, pipe) — pas juste la syntaxe, mais quand chacun s'applique.\n\n**Réponse attendue :** \`ls > inventaire.txt\` redirige la sortie vers le fichier en l'ÉCRASANT ; \`ls >> inventaire.txt\` AJOUTE à la fin sans écraser. Un \`|\` (pipe) est différent : il branche la sortie d'une commande sur l'ENTRÉE d'une autre (ex. \`ls | grep .js\` pour ne garder que les fichiers .js).\n\n**Débutant / correct / excellent :** débutant = connaît \`>\` mais pas \`>>\` ni la différence écrase/ajoute ; correct = distingue \`>\` et \`>>\` ; excellent = ajoute le pipe \`|\` et un exemple (\`ls | grep\`), et sait qu'un script Node peut lire des arguments avec \`process.argv\`.\n\n**Relance possible :** « et si tu voulais compter combien de fichiers \`.js\` il y a ? » → \`ls | grep .js | wc -l\` (chaîne de pipes).\n\n**À éviter :** confondre \`>\` (écrase, danger de perte) et \`>>\` (ajoute) — une erreur qui détruit un fichier ; croire qu'un pipe et une redirection font la même chose.\n\n**Formulation orale naturelle :** « \`>\` écrit dans un fichier en l'écrasant, \`>>\` ajoute à la suite, et le pipe \`|\` enchaîne des commandes en branchant la sortie de l'une sur l'entrée de l'autre. »`,
  },

  // ── Jour 3 — Git : working directory / staging / commits ──
  3: {
    interview: `**Mise en situation : « Tu as modifié trois fichiers, mais un seul est vraiment prêt. Comment fais-tu un commit qui ne contient QUE ce fichier-là, et pourquoi Git t'oblige-t-il à passer par une étape intermédiaire avant de committer ? »**\n\n*Ce que la question évalue :* le modèle mental des trois zones de Git (working directory → staging → historique), et l'intérêt du staging — pas la récitation de \`git commit\`.\n\n**Réponse attendue :** je fais \`git add <le-fichier-prêt>\` pour ne mettre QUE lui dans la zone de STAGING, puis \`git commit -m "message"\` : le commit ne fige que ce qui est stagé, pas tout le working directory. Cette étape existe justement pour COMPOSER des commits cohérents (une intention par commit) au lieu de tout enregistrer en vrac.\n\n**Débutant / correct / excellent :** débutant = \`git add .\` puis commit (met tout, rate la question) ; correct = \`git add <fichier>\` ciblé + explique le staging ; excellent = ajoute que \`git status\` montre les trois zones, et qu'un bon message de commit raconte le POURQUOI, pas le QUOI.\n\n**Relance possible :** « et si tu t'aperçois que tu as stagé un fichier par erreur ? » → \`git restore --staged <fichier>\` le retire du staging sans perdre tes modifications.\n\n**À éviter :** \`git add .\` systématique (commits fourre-tout) ; messages vagues (\`fix\`, \`wip\`) ; croire qu'un commit enregistre le working directory entier plutôt que le contenu stagé.\n\n**Formulation orale naturelle :** « Je stage seulement le fichier prêt avec \`git add\`, puis je commit — le staging me permet de composer un commit propre qui raconte une seule chose. »`,
  },

  // ── Jour 4 — Variables, types, opérateurs, coercition ──
  4: {
    interview: `**Mise en situation : « Sans exécuter le code, dis-moi ce qu'affichent \`'5' + 3\` et \`'5' - 3\` en JavaScript, et surtout POURQUOI ils diffèrent. »**\n\n*Ce que la question évalue :* la compréhension des types et de la coercition implicite — la capacité à PRÉDIRE le comportement, pas à réciter la liste des types.\n\n**Réponse attendue :** \`'5' + 3\` donne \`'53'\` (une chaîne) : avec \`+\`, si un opérande est une chaîne, JavaScript CONVERTIT l'autre en chaîne et concatène. \`'5' - 3\` donne \`2\` (un nombre) : l'opérateur \`-\` n'a de sens qu'entre nombres, donc JavaScript convertit \`'5'\` en nombre. C'est la coercition implicite.\n\n**Débutant / correct / excellent :** débutant = devine les résultats sans expliquer ; correct = donne les deux résultats et nomme la coercition ; excellent = généralise (le \`+\` est surchargé concat/addition, les autres opérateurs forcent le numérique) et conclut « d'où l'intérêt de \`===\` et de convertir explicitement avec \`Number()\` ».\n\n**Relance possible :** « et \`'5' == 5\` ? » → \`true\` avec \`==\` (coercition), mais \`false\` avec \`===\` (compare type ET valeur) — je préfère \`===\` par défaut.\n\n**À éviter :** répondre au hasard sans la règle sous-jacente ; ignorer que le \`+\` se comporte différemment des autres opérateurs.\n\n**Formulation orale naturelle :** « \`'5' + 3\` fait \`'53'\` parce que le \`+\` concatène dès qu'il y a une chaîne ; \`'5' - 3\` fait \`2\` parce que le \`-\` force la conversion en nombre — c'est la coercition, et c'est pour ça que j'utilise \`===\`. »`,
  },

  // ── Jour 5 — Conditions, early return, opérateurs logiques, piège falsy ──
  5: {
    interview: `**Mise en situation : « On te donne une fonction avec trois \`if\` imbriqués en pyramide qui valident une commande. Comment l'aplatis-tu pour la rendre lisible, et quel piège guette une condition comme \`if (quantite)\` ? »**\n\n*Ce que la question évalue :* la structuration d'une logique de décision (early return / clauses de garde) et la connaissance du piège des valeurs *falsy* — un raisonnement, pas une définition.\n\n**Réponse attendue :** j'inverse les conditions en CLAUSES DE GARDE avec *early return* : je traite d'abord les cas invalides (\`if (!x) return ...\`) pour sortir tôt, ce qui aplatit la pyramide et rend le chemin nominal lisible en fin de fonction. Le piège de \`if (quantite)\` : \`0\` est *falsy*, donc une quantité de 0 (légitime) serait traitée comme « absente » — il faut tester la PRÉSENCE (\`quantite !== undefined\`), pas la véracité.\n\n**Débutant / correct / excellent :** débutant = garde la pyramide, ne voit pas le piège falsy ; correct = applique l'early return ET cite le piège \`0\`/\`''\` ; excellent = liste les valeurs falsy (\`0, '', null, undefined, NaN, false\`) et distingue « absent » de « falsy légitime ».\n\n**Relance possible :** « et si tu dois vérifier que deux conditions sont vraies en même temps ? » → l'opérateur \`&&\`, en gardant chaque condition simple et nommée.\n\n**À éviter :** empiler les \`else if\` sans early return ; confondre « valeur absente » et « valeur falsy » (le bug classique sur \`0\` et la chaîne vide).\n\n**Formulation orale naturelle :** « Je remonte les cas d'échec en clauses de garde avec early return pour aplatir la pyramide, et je me méfie de \`if (quantite)\` parce que \`0\` est falsy — je teste la présence, pas la véracité. »`,
  },

  // ── Jour 6 — Boucles (for, while, for...of), compteurs, accumulateurs ──
  6: {
    interview: `**Mise en situation : « Tu dois additionner tous les prix d'un panier. Tu peux écrire une boucle \`for\` classique avec un index, ou un \`for...of\`. Lequel choisis-tu ici, et quelle erreur de boucle guette le débutant ? »**\n\n*Ce que la question évalue :* le choix de la bonne boucle selon le besoin et la conscience de l'erreur *off-by-one* — un arbitrage, pas la syntaxe.\n\n**Réponse attendue :** ici je n'ai pas besoin de l'index, juste des valeurs, donc \`for...of\` est plus lisible (\`for (const prix of panier) total += prix\`). Le \`for\` classique avec index est utile quand j'ai besoin de la POSITION ou d'un pas particulier. L'erreur classique est l'*off-by-one* : \`i <= panier.length\` déborde d'un cran (le dernier index est \`length - 1\`), il faut \`i < panier.length\`.\n\n**Débutant / correct / excellent :** débutant = écrit un \`for\` avec index sans justifier ni voir l'off-by-one ; correct = choisit \`for...of\` pour l'itération de valeurs et cite l'off-by-one ; excellent = explique le pattern accumulateur (initialiser \`total = 0\` AVANT la boucle) et quand l'index est réellement nécessaire.\n\n**Relance possible :** « et si tu devais t'arrêter dès qu'un prix dépasse 100 ? » → une boucle avec \`break\`, ou un \`for\` classique — le \`for...of\` reste possible avec \`break\`.\n\n**À éviter :** utiliser un index dont on n'a pas besoin (moins lisible) ; l'off-by-one \`<=\` ; oublier d'initialiser l'accumulateur avant la boucle.\n\n**Formulation orale naturelle :** « Comme je ne me sers que des valeurs, je prends un \`for...of\` — plus lisible ; je réserve le \`for\` indexé aux cas où j'ai besoin de la position, et je fais attention à la borne pour éviter l'off-by-one. »`,
  },

  // ── Jour 8 — Tableaux : mutation vs copie ──
  8: {
    interview: `**Mise en situation : « Tu passes un tableau à une fonction qui le trie. L'appelant est surpris : SON tableau d'origine est trié lui aussi, alors qu'il ne le voulait pas. Explique ce qui s'est passé et corrige-le. »**\n\n*Ce que la question évalue :* la compréhension mutation vs copie sur les tableaux et le diagnostic d'un effet de bord — une situation concrète, pas la liste des méthodes.\n\n**Réponse attendue :** un tableau est passé par RÉFÉRENCE : la fonction et l'appelant pointent le MÊME tableau. \`sort()\` trie EN PLACE (il mute), donc l'original est modifié. Correction : travailler sur une COPIE — \`[...tableau].sort()\` (ou \`tableau.slice().sort()\`) — pour ne pas toucher l'original.\n\n**Débutant / correct / excellent :** débutant = ne comprend pas pourquoi l'original change ; correct = identifie la référence partagée + \`sort\` qui mute, et copie avec le spread ; excellent = distingue les méthodes qui MUTENT (\`sort\`, \`push\`, \`splice\`, \`reverse\`) de celles qui renvoient une nouvelle valeur (\`map\`, \`filter\`, \`slice\`, \`concat\`).\n\n**Relance possible :** « comment vérifierais-tu que ta correction marche ? » → comparer le tableau d'origine avant/après l'appel : il doit être inchangé.\n\n**À éviter :** croire que passer un tableau le copie ; ignorer que \`sort()\` mute en place ; « corriger » en réassignant sans copier.\n\n**Formulation orale naturelle :** « Le tableau est passé par référence et \`sort\` trie en place, donc l'original est muté ; je corrige en triant une copie faite avec le spread \`[...tableau]\`. »`,
  },

  // ── Jour 9 — Fonctions : responsabilité unique, portée, shadowing ──
  9: {
    interview: `**Mise en situation : « Une fonction fait 40 lignes et enchaîne trois choses : valider une entrée, calculer un total, et afficher un message. Comment la restructures-tu, et qu'est-ce que le *shadowing* de variable pourrait casser au passage ? »**\n\n*Ce que la question évalue :* le découpage en fonctions à responsabilité unique et la compréhension de la portée / du shadowing — une décision de conception, pas une définition.\n\n**Réponse attendue :** je la découpe en trois fonctions nommées d'après ce qu'elles font (\`valider\`, \`calculerTotal\`, \`formaterMessage\`), chacune avec une entrée et un \`return\` clairs ; la fonction d'origine ne fait plus qu'orchestrer. Le shadowing : si une variable locale porte le même nom qu'une variable externe, elle la MASQUE dans sa portée — on peut croire modifier l'externe alors qu'on manipule la locale, source de bugs silencieux.\n\n**Débutant / correct / excellent :** débutant = laisse une grosse fonction ou découpe sans return cohérent ; correct = trois fonctions à responsabilité unique + explique le shadowing ; excellent = insiste sur les valeurs de retour explicites (une fonction sans \`return\` renvoie \`undefined\`) et sur la testabilité isolée de chaque petite fonction.\n\n**Relance possible :** « pourquoi préférer trois petites fonctions plutôt qu'une grosse ? » → chacune est lisible, testable isolément, réutilisable, et nommée d'après son intention.\n\n**À éviter :** découper au hasard sans responsabilité claire ; oublier un \`return\` (renvoie \`undefined\`) ; réutiliser un nom de variable externe sans conscience du masquage.\n\n**Formulation orale naturelle :** « Je sépare en trois fonctions à responsabilité unique avec des retours clairs, et je fais attention au shadowing : une variable locale de même nom masque l'externe, ce qui crée des bugs discrets. »`,
  },

  // ── Jour 10 — Objets : modélisation, imbrication, destructuring ──
  10: {
    interview: `**Mise en situation : « Tu modélises un utilisateur qui a une adresse (rue, ville, code postal). Tu veux extraire la ville proprement. Comment structures-tu l'objet et récupères-tu la ville, et que se passe-t-il si l'adresse est absente ? »**\n\n*Ce que la question évalue :* la modélisation d'une entité par un objet imbriqué et l'usage du destructuring, avec la gestion du cas « propriété absente » — un raisonnement concret.\n\n**Réponse attendue :** je modélise \`utilisateur = { nom, adresse: { rue, ville, codePostal } }\` — l'adresse est un objet imbriqué, pas des champs à plat. Pour extraire la ville : \`const { ville } = utilisateur.adresse\`, ou en imbriqué \`const { adresse: { ville } } = utilisateur\`. Si \`adresse\` est absente (\`undefined\`), accéder à \`.ville\` lève une erreur — je me protège avec l'accès optionnel \`utilisateur.adresse?.ville\` ou une valeur par défaut.\n\n**Débutant / correct / excellent :** débutant = met tout à plat (\`adresseVille\`) ou accède sans se soucier de l'absence ; correct = objet imbriqué + destructuring + conscience du cas absent ; excellent = utilise l'accès optionnel \`?.\` et/ou une valeur par défaut au destructuring, et explique pourquoi l'imbrication reflète la structure réelle.\n\n**Relance possible :** « et si tu voulais une valeur par défaut quand la ville manque ? » → \`const { ville = 'inconnue' } = utilisateur.adresse ?? {}\`.\n\n**À éviter :** aplatir une structure naturellement imbriquée ; accéder à \`adresse.ville\` sans vérifier que \`adresse\` existe (erreur « Cannot read property of undefined »).\n\n**Formulation orale naturelle :** « L'adresse est un objet imbriqué dans l'utilisateur ; j'extrais la ville par destructuring, et je me protège du cas où l'adresse est absente avec l'accès optionnel \`?.\` ou une valeur par défaut. »`,
  },

  // ══════════════════════════════════════════════════════════════════════════
  // SOUS-BATCH B2 — jours 11 à 20 (affectés : 11,12,13,15,16,17,18,19,20)
  // ══════════════════════════════════════════════════════════════════════════

  // ── Jour 11 — Tableaux d'objets : chercher, filtrer, trier, agréger ──
  11: {
    interview: `**Mise en situation : « Tu as une liste de commandes, chacune un objet \`{ client, montant }\`. On te demande le total dépensé PAR client. Comment t'y prends-tu, et quel piège guette si tu veux ensuite trier les clients par total décroissant ? »**\n\n*Ce que la question évalue :* la manipulation d'un tableau d'objets (parcourir, regrouper, trier) — le format de données le plus courant — sans se tromper sur le tri.\n\n**Réponse attendue :** je parcours la liste (une boucle) en accumulant dans un objet \`totaux\` indexé par client (\`totaux[c.client] = (totaux[c.client] ?? 0) + c.montant\`) ; puis je transforme en liste \`[{client, total}]\` que je trie. Le piège du tri : \`sort()\` par défaut compare en CHAÎNES (« 100 » < « 20 ») ; pour trier par nombre il faut un comparateur \`(a, b) => b.total - a.total\`.\n\n**Débutant / correct / excellent :** débutant = boucle sans regroupement propre, ou tri sans comparateur ; correct = regroupement par objet-accumulateur + tri avec comparateur numérique ; excellent = mentionne que \`sort\` mute (donc trier une copie) et que la casse/valeurs manquantes doivent être gérées.\n\n**Relance possible :** « et pour ne garder que les clients ayant dépensé plus de 500 ? » → un \`filter\` sur la liste agrégée (\`filter(x => x.total > 500)\`).\n\n**À éviter :** trier des nombres sans comparateur (tri lexicographique faux) ; muter la liste d'origine ; oublier d'initialiser le total à 0.\n\n**Formulation orale naturelle :** « Je regroupe par client dans un objet-accumulateur en parcourant la liste, puis je trie avec un comparateur numérique \`b.total - a.total\` — sans oublier que \`sort\` compare en chaînes par défaut. »`,
  },

  // ── Jour 12 — Lecture/écriture de fichiers, JSON, try/catch ──
  12: {
    interview: `**Mise en situation : « Ton programme charge des données depuis un fichier JSON au démarrage. Deux situations peuvent survenir : le fichier n'existe pas encore, ou il existe mais son contenu est un JSON invalide. Traites-tu ces deux cas de la même façon ? Explique. »**\n\n*Ce que la question évalue :* la robustesse de la lecture de fichiers et la distinction entre un cas NORMAL et une vraie ERREUR — un diagnostic, pas la syntaxe de \`fs\`.\n\n**Réponse attendue :** non, ce sont deux cas différents. Fichier ABSENT = premier lancement, c'est NORMAL : je retourne une valeur vide par défaut (\`[]\`) et je continue. Fichier CORROMPU (JSON invalide) = une vraie anomalie : le \`JSON.parse\` lève une exception que j'attrape avec \`try/catch\`, et je m'arrête proprement avec un message clair PLUTÔT que d'écraser le fichier — pour ne pas détruire des données potentiellement récupérables.\n\n**Débutant / correct / excellent :** débutant = traite tout pareil, ou laisse le programme planter ; correct = distingue absent (→ vide) et corrompu (→ try/catch + message) ; excellent = insiste sur « ne jamais écraser un fichier corrompu automatiquement » et vérifie l'existence avant de lire (ou attrape l'erreur spécifique).\n\n**Relance possible :** « pourquoi ne pas simplement réécrire un fichier vide quand le JSON est invalide ? » → parce qu'on détruirait des données peut-être réparables à la main ; mieux vaut alerter.\n\n**À éviter :** confondre « fichier absent » (normal) et « fichier corrompu » (erreur) ; laisser \`JSON.parse\` planter sans \`try/catch\` ; écraser silencieusement un fichier illisible.\n\n**Formulation orale naturelle :** « Un fichier absent, c'est un premier lancement normal, je pars d'une liste vide ; un JSON corrompu, c'est une erreur que j'attrape avec try/catch et je m'arrête sans écraser, pour ne pas perdre de données. »`,
  },

  // ── Jour 13 — Mini-projet annuaire : assembler + valider l'entrée ──
  13: {
    interview: `**Mise en situation : « Dans ton annuaire, la commande \`ajouter\` doit refuser un email invalide (sans @) et un email en double. À quel MOMENT valides-tu l'entrée par rapport à l'écriture dans le fichier, et pourquoi cet ordre compte-t-il ? »**\n\n*Ce que la question évalue :* la capacité à assembler un petit programme complet en plaçant la validation AVANT l'effet de bord (l'écriture) — un réflexe de conception, sur un projet réel.\n\n**Réponse attendue :** je valide D'ABORD (email contient un @ et un point, et n'existe pas déjà dans la liste), et je n'écris dans le fichier QU'ENSUITE, seulement si tout est bon. L'ordre compte : écrire puis valider laisserait des données invalides sur le disque ; valider avant garantit que le fichier ne contient jamais d'entrée incorrecte. En cas d'échec, je renvoie un message clair et je n'écris rien.\n\n**Débutant / correct / excellent :** débutant = écrit puis vérifie (ou ne vérifie pas les doublons) ; correct = valide avant d'écrire, gère email invalide ET doublon ; excellent = sépare la validation (une fonction) de l'écriture, et renvoie un message d'erreur utile plutôt qu'un plantage.\n\n**Relance possible :** « comment détectes-tu un doublon d'email efficacement ? » → chercher dans la liste existante (insensible à la casse) avant d'ajouter.\n\n**À éviter :** écrire dans le fichier puis valider (données invalides persistées) ; oublier le cas du doublon ; laisser le programme planter sur une entrée invalide au lieu d'un message clair.\n\n**Formulation orale naturelle :** « Je valide l'email et l'absence de doublon AVANT d'écrire ; comme ça le fichier ne contient jamais de données invalides, et en cas d'échec je renvoie un message clair sans rien enregistrer. »`,
  },

  // ── Jour 15 — Big O : classer et prédire ──
  15: {
    interview: `**Mise en situation : « Un traitement met 1 seconde pour 1 000 éléments. Combien de temps prendra-t-il, approximativement, pour 2 000 éléments s'il est en O(n) ? Et s'il est en O(n²) ? Comment reconnais-tu lequel en LISANT le code ? »**\n\n*Ce que la question évalue :* la compréhension concrète de Big O comme prédiction de croissance, pas comme étiquette apprise par cœur.\n\n**Réponse attendue :** en O(n), doubler les données double le temps → ~2 secondes. En O(n²), doubler les données QUADRUPLE le temps → ~4 secondes. Pour le reconnaître : une seule boucle sur les données = O(n) ; deux boucles IMBRIQUÉES sur les mêmes données = O(n²) ; diviser le problème par deux à chaque étape (recherche binaire) = O(log n).\n\n**Débutant / correct / excellent :** débutant = confond ou récite les classes sans prédire ; correct = 2 s / 4 s + lien boucle simple / double boucle ; excellent = ajoute O(log n) et O(1), et précise que Big O décrit la CROISSANCE asymptotique, pas le temps absolu (les constantes sont ignorées).\n\n**Relance possible :** « et si ton code fait une boucle PUIS une autre boucle séparée (pas imbriquée) ? » → O(n) + O(n) = O(n) : on garde le terme dominant.\n\n**À éviter :** croire que O(n²) double (au lieu de quadrupler) ; confondre boucles imbriquées et boucles séquentielles ; juger la performance sans regarder la croissance.\n\n**Formulation orale naturelle :** « En O(n) ça double, donc ~2 secondes ; en O(n²) ça quadruple, donc ~4 secondes — et je repère l'un ou l'autre selon qu'il y a une seule boucle ou deux boucles imbriquées. »`,
  },

  // ── Jour 16 — Recherche binaire : prérequis trié, bornes ──
  16: {
    interview: `**Mise en situation : « Tu appliques une recherche binaire sur un tableau qui n'est PAS trié. Que se passe-t-il ? Et quelle erreur dans la gestion des bornes fait boucler l'algorithme à l'infini ? »**\n\n*Ce que la question évalue :* la compréhension du prérequis « tableau trié » et la maîtrise des bornes de la dichotomie — le point qui distingue une recherche binaire correcte d'une buggée.\n\n**Réponse attendue :** sur un tableau non trié, la recherche binaire donne un résultat FAUX : elle élimine une moitié en supposant l'ordre, donc elle peut rater la cible alors qu'elle est présente. C'est pourquoi le TRI est un prérequis. Pour les bornes : à chaque tour on compare au milieu et on ajuste \`debut\` ou \`fin\` ; l'erreur classique est de ne pas RÉDUIRE l'intervalle (oublier \`milieu + 1\` / \`milieu - 1\`), ce qui laisse l'intervalle inchangé et boucle à l'infini.\n\n**Débutant / correct / excellent :** débutant = ne voit pas le prérequis trié ni le risque d'infini ; correct = explique le prérequis + l'erreur de borne ; excellent = donne la complexité O(log n), la condition d'arrêt \`debut <= fin\`, et note que le coût du tri (O(n log n)) doit être amorti par plusieurs recherches pour valoir le coup.\n\n**Relance possible :** « comment renvoies-tu 'non trouvé' proprement ? » → quand \`debut > fin\`, on sort et on renvoie \`-1\`.\n\n**À éviter :** appliquer la recherche binaire sur un tableau non trié ; oublier \`milieu ± 1\` (boucle infinie) ; mauvaise condition d'arrêt.\n\n**Formulation orale naturelle :** « Sur un tableau non trié le résultat est faux, car la recherche binaire suppose l'ordre pour éliminer une moitié ; et si j'oublie de faire \`milieu + 1\` ou \`milieu - 1\`, l'intervalle ne se réduit pas et ça boucle à l'infini. »`,
  },

  // ── Jour 17 — Tris simples O(n²) vs tri natif ──
  17: {
    interview: `**Mise en situation : « Un collègue écrit un tri à bulles pour trier un million de lignes. Tu proposes d'utiliser \`sort()\` natif à la place. Qui a raison, pourquoi, et existe-t-il un cas où un tri simple comme l'insertion se justifie quand même ? »**\n\n*Ce que la question évalue :* le raisonnement par compromis sur les tris — coût O(n²) vs O(n log n) — et la nuance (pas de dogme).\n\n**Réponse attendue :** sur un million de lignes, le tri à bulles est O(n²) → ~10¹² opérations, impraticable ; \`sort()\` natif est en O(n log n), incomparablement plus rapide : mon collègue a tort à cette échelle. MAIS le tri par insertion (O(n²)) se justifie sur de TRÈS PETITS tableaux ou sur des données presque déjà triées, où il est simple et efficace en pratique (c'est d'ailleurs pourquoi certains tris hybrides l'utilisent sur les petits sous-tableaux).\n\n**Débutant / correct / excellent :** débutant = « le tri natif est toujours mieux » sans chiffres ni nuance ; correct = O(n²) vs O(n log n) + tranche pour le natif à grande échelle ; excellent = donne le cas où l'insertion gagne (petits/quasi-triés) et évoque que le natif est un tri hybride optimisé.\n\n**Relance possible :** « pourquoi ne pas TOUJOURS réécrire son propre tri ? » → le natif est testé, optimisé et lisible ; on ne réécrit un tri que pour apprendre ou pour un besoin très spécifique.\n\n**À éviter :** répondre par dogme (« natif toujours » / « maison toujours ») ; ignorer l'ordre de grandeur O(n²) sur un million ; oublier le cas légitime des petits tableaux.\n\n**Formulation orale naturelle :** « À un million de lignes, le tri à bulles en O(n²) est impraticable et le natif en O(n log n) gagne largement ; mais sur un tout petit tableau ou des données quasi triées, un tri par insertion reste simple et efficace. »`,
  },

  // ── Jour 18 — Git branches, merges, conflits ──
  18: {
    interview: `**Mise en situation : « Tu dois développer DEUX fonctionnalités en parallèle sans les mélanger, et pouvoir livrer l'une même si l'autre n'est pas finie. Comment organises-tu ton travail avec Git, et que fais-tu concrètement face à un conflit de merge ? »**\n\n*Ce que la question évalue :* le workflow branche-par-fonctionnalité et la gestion sereine d'un conflit — pas la définition de « branche ».\n\n**Réponse attendue :** je crée une branche DÉDIÉE par fonctionnalité (\`git switch -c feature/x\`) à partir de \`main\` : les deux évoluent isolément, et je peux fusionner la fonctionnalité terminée dans \`main\` sans attendre l'autre. Face à un conflit de merge : Git marque les zones en conflit (\`<<<<<<< / ======= / >>>>>>>\`) ; j'ouvre le fichier, je choisis/combine les deux versions à la main, je supprime les marqueurs, puis \`git add\` le fichier résolu et je termine le merge. Un conflit n'est pas une erreur, c'est une décision à prendre.\n\n**Débutant / correct / excellent :** débutant = tout sur \`main\`, panique devant un conflit ; correct = une branche par fonctionnalité + résout un conflit méthodiquement ; excellent = ajoute que les branches courtes limitent les conflits, et qu'on ne réécrit pas l'historique déjà partagé.\n\n**Relance possible :** « pourquoi des branches courtes plutôt qu'une longue branche de plusieurs semaines ? » → plus une branche diverge longtemps, plus les conflits sont gros et risqués ; on fusionne souvent.\n\n**À éviter :** tout développer sur \`main\` ; avoir peur des conflits au point de les éviter ; supprimer un travail en « résolvant » un conflit sans le lire.\n\n**Formulation orale naturelle :** « Une branche par fonctionnalité depuis \`main\`, je fusionne celle qui est prête ; et un conflit, je l'ouvre, je choisis quoi garder entre les marqueurs, je nettoie, \`git add\`, et je termine le merge — c'est une décision, pas une catastrophe. »`,
  },

  // ── Jour 19 — Méthode de résolution de problèmes ──
  19: {
    interview: `**Mise en situation : « On te donne un problème que tu ne sais PAS résoudre du premier coup. Décris-moi ta démarche AVANT d'écrire la moindre ligne de code. »**\n\n*Ce que la question évalue :* l'existence d'une MÉTHODE structurée (pas l'improvisation) — exactement ce qu'un recruteur observe en entretien technique.\n\n**Réponse attendue :** je suis une méthode : (1) COMPRENDRE l'énoncé en le reformulant avec mes mots ; (2) construire des EXEMPLES concrets, y compris des cas limites ; (3) DÉCOMPOSER en sous-problèmes ; (4) écrire un PSEUDO-CODE ou un plan ; (5) CODER une étape à la fois ; (6) VÉRIFIER sur mes exemples. Je ne me jette pas sur le clavier : la moitié du travail est de comprendre avant de coder.\n\n**Débutant / correct / excellent :** débutant = « je commence à coder et je vois » ; correct = cite les grandes étapes (comprendre → exemples → plan → coder → vérifier) ; excellent = insiste sur les cas limites dès les exemples et sur la vérification, et VERBALISE (le recruteur évalue la démarche).\n\n**Relance possible :** « et si, à mi-chemin, ton plan ne marche pas ? » → je reviens aux exemples : souvent un cas limite oublié révèle où le plan casse.\n\n**À éviter :** coder sans comprendre ni exemples ; rester silencieux (le recruteur ne peut pas suivre le raisonnement) ; sauter la vérification.\n\n**Formulation orale naturelle :** « Je reformule le problème, je me fais des exemples avec des cas limites, je décompose et je pose un plan, je code une étape à la fois, puis je vérifie sur mes exemples — et je verbalise tout au long. »`,
  },

  // ── Jour 20 — Katas chronométrés : consolidation sous pression ──
  20: {
    interview: `**Mise en situation : « En exercice chronométré, tu bloques à mi-parcours sur un kata. Comment réagis-tu pour ne pas paniquer et surtout ne pas perdre les points de DÉMARCHE, même si tu ne finis pas ? »**\n\n*Ce que la question évalue :* le comportement sous pression et la conscience que la démarche compte autant que la solution — la compétence réelle d'un entretien technique chronométré.\n\n**Réponse attendue :** je reviens à la MÉTHODE (jour 19) : je reformule où j'en suis, je reteste mon exemple pour localiser où ça casse, et je verbalise mon blocage (« je pensais faire X, mais ça coince sur Y »). Je propose une solution NAÏVE qui marche quitte à l'optimiser après, plutôt que de rester bloqué sur l'optimale. Le rappel actif chronométré m'a entraîné à ça : mieux vaut une démarche claire et incomplète qu'un silence.\n\n**Débutant / correct / excellent :** débutant = se fige, code au hasard, ou abandonne ; correct = revient à la méthode, verbalise, propose une solution naïve ; excellent = découpe le problème pour livrer une partie qui marche, et explique ce qu'il ferait avec plus de temps.\n\n**Relance possible :** « pourquoi une solution naïve plutôt que rien ? » → parce qu'elle prouve que tu comprends le problème et donne une base à optimiser ; le recruteur note la progression, pas seulement le résultat final.\n\n**À éviter :** coder frénétiquement sans plan ; rester silencieux ; s'acharner sur la solution optimale au point de ne rien rendre.\n\n**Formulation orale naturelle :** « Je reviens à ma méthode : je reformule où j'en suis, je verbalise le blocage, et je propose une version naïve qui marche quitte à l'améliorer — une démarche claire et incomplète vaut mieux qu'un silence. »`,
  },

  // ══════════════════════════════════════════════════════════════════════════
  // SOUS-BATCH B3 — jours 21 à 30 (affectés : 22,23,24,25,26,27,29)
  // ══════════════════════════════════════════════════════════════════════════

  // ── Jour 22 — Fonctions comme valeurs, callbacks, ordre supérieur ──
  22: {
    interview: `**Mise en situation : « Tu remarques que tu as écrit trois fois presque le même code pour trier une liste — une fois par prix, une fois par nom, une fois par date — seul le critère de comparaison change. Comment supprimes-tu cette répétition ? »**\n\n*Ce que la question évalue :* la compréhension qu'une fonction est une VALEUR qu'on peut passer en argument (fonction d'ordre supérieur) pour injecter le comportement qui varie — un réflexe de factorisation, pas une définition.\n\n**Réponse attendue :** j'extrais ce qui VARIE — le critère de comparaison — et je le passe en ARGUMENT sous forme de fonction (un callback). J'écris une seule fonction \`trierPar(liste, comparateur)\`, et j'appelle \`trierPar(articles, (a,b) => a.prix - b.prix)\`, etc. La fonction devient une donnée qu'on injecte : c'est une fonction d'ordre supérieur (elle prend une fonction en paramètre).\n\n**Débutant / correct / excellent :** débutant = copie-colle avec une variable de plus, ou ne voit pas comment paramétrer le comportement ; correct = passe le comparateur en argument (HOF) ; excellent = généralise (c'est le même mécanisme que le callback de \`sort\`) et explique que « le comportement devient une valeur » ouvre la voie à \`map\`/\`filter\`.\n\n**Relance possible :** « peux-tu me donner un autre exemple de fonction qui prend une fonction ? » → \`sort\` avec son comparateur, ou une future \`map\`/\`filter\`.\n\n**À éviter :** dupliquer le code avec un simple paramètre booléen (\`trierParPrix\` / \`trierParNom\`) au lieu d'injecter le comportement ; ne pas voir qu'une fonction peut être passée comme argument.\n\n**Formulation orale naturelle :** « J'extrais le critère qui varie et je le passe en argument : une seule fonction \`trierPar(liste, comparateur)\` — la fonction de comparaison devient une valeur que j'injecte, c'est une fonction d'ordre supérieur. »`,
  },

  // ── Jour 23 — map et filter, et quand garder une boucle ──
  23: {
    interview: `**Mise en situation : « Voici une boucle \`for\` qui parcourt des articles, garde ceux dont le prix est positif, et construit une nouvelle liste avec ces prix doublés. Réécris-la avec \`map\`/\`filter\`. Puis donne-moi un cas où tu GARDERAIS plutôt une boucle. »**\n\n*Ce que la question évalue :* la pensée en TRANSFORMATIONS (map) et SÉLECTIONS (filter), ET le discernement de ne pas les utiliser à tout prix.\n\n**Réponse attendue :** \`articles.filter(a => a.prix > 0).map(a => a.prix * 2)\` — \`filter\` sélectionne, \`map\` transforme, on chaîne dans cet ordre. Je GARDE une boucle quand : je dois m'ARRÊTER en cours (\`break\`), gérer plusieurs accumulateurs en un seul passage, ou quand un \`map\`/\`filter\` imbriqué deviendrait moins lisible qu'une boucle claire.\n\n**Débutant / correct / excellent :** débutant = réécrit mais met \`map\` avant \`filter\` (transforme des éléments qu'il jettera), ou ne voit aucun cas pour la boucle ; correct = filter puis map, chaîné, + un cas légitime de boucle ; excellent = note que \`map\`/\`filter\` renvoient de NOUVELLES listes (pas de mutation) et que l'ordre filter→map évite de transformer inutilement.\n\n**Relance possible :** « pourquoi filter AVANT map ici ? » → pour ne pas doubler des prix qu'on va de toute façon éliminer (moins de travail, plus clair).\n\n**À éviter :** utiliser \`map\` pour filtrer (en renvoyant \`undefined\`) ; croire que map/filter mutent la liste ; les employer partout même quand une boucle est plus lisible.\n\n**Formulation orale naturelle :** « \`filter(prix > 0)\` puis \`map(prix * 2)\` — sélectionner d'abord, transformer ensuite ; et je garde une boucle quand je dois m'arrêter en cours ou gérer plusieurs accumulateurs. »`,
  },

  // ── Jour 24 — reduce : accumulation ──
  24: {
    interview: `**Mise en situation : « Tu veux compter le nombre de ventes PAR catégorie à partir d'une liste de ventes. Comment fais-tu avec \`reduce\`, qu'est-ce que l'accumulateur ici, et quelle erreur d'initialisation casse tout ? »**\n\n*Ce que la question évalue :* la maîtrise de \`reduce\` comme outil d'agrégation universel et la compréhension profonde de l'accumulateur — pas la récitation de la signature.\n\n**Réponse attendue :** \`ventes.reduce((acc, v) => { acc[v.categorie] = (acc[v.categorie] ?? 0) + 1; return acc; }, {})\`. L'accumulateur \`acc\` est ici un OBJET qui se construit au fil du parcours (catégorie → compte). L'erreur classique : oublier la VALEUR INITIALE (\`{}\`) — sans elle, \`reduce\` prend le premier élément comme accumulateur, et le type est faux ; autre erreur : oublier de RETOURNER \`acc\` à chaque tour (il devient \`undefined\`).\n\n**Débutant / correct / excellent :** débutant = tente une boucle sans voir reduce, ou oublie la valeur initiale / le return ; correct = reduce correct avec \`{}\` initial et return ; excellent = explique que reduce généralise somme/min/max/comptage/regroupement, et sait quand une boucle serait plus lisible qu'un reduce complexe.\n\n**Relance possible :** « et pour faire la SOMME des montants plutôt qu'un comptage ? » → même structure, accumulateur numérique initialisé à \`0\` : \`reduce((s, v) => s + v.montant, 0)\`.\n\n**À éviter :** oublier la valeur initiale de \`reduce\` ; oublier de retourner l'accumulateur ; utiliser reduce là où un \`map\`/\`filter\` serait plus clair.\n\n**Formulation orale naturelle :** « Un \`reduce\` avec un objet accumulateur initialisé à \`{}\` : à chaque vente j'incrémente le compteur de sa catégorie et je retourne l'accumulateur — les deux pièges sont d'oublier la valeur initiale ou le return. »`,
  },

  // ── Jour 25 — Récursion : pile d'appels, cas de base ──
  25: {
    interview: `**Mise en situation : « Explique-moi ce qui se passe dans la MÉMOIRE quand une fonction récursive s'exécute. Que se passe-t-il si tu oublies le cas de base, et à quel moment préfères-tu une version itérative ? »**\n\n*Ce que la question évalue :* le modèle mental de la pile d'appels et le jugement récursif vs itératif — une compréhension du mécanisme, pas une définition scolaire.\n\n**Réponse attendue :** chaque appel récursif EMPILE un nouveau contexte sur la pile d'appels (ses variables locales), et on ne dépile qu'au retour. Sans CAS DE BASE, la fonction s'appelle indéfiniment : la pile grossit jusqu'au débordement (« Maximum call stack size exceeded »). Je préfère l'ITÉRATIF quand la profondeur peut être grande (risque de débordement) ou quand la boucle est plus simple ; la récursion brille quand le problème se DÉCOMPOSE naturellement (arbres, structures imbriquées).\n\n**Débutant / correct / excellent :** débutant = décrit la récursion sans la pile ni le cas de base ; correct = pile d'appels + cas de base + débordement ; excellent = donne le compromis (profondeur/lisibilité), sait convertir récursif ↔ itératif, et cite un cas où la récursion est clairement plus lisible.\n\n**Relance possible :** « peux-tu transformer une somme récursive en version itérative ? » → oui, avec une boucle et un accumulateur — même résultat, sans pile qui grossit.\n\n**À éviter :** oublier le cas de base (débordement) ; ignorer que chaque appel consomme de la pile ; utiliser la récursion là où une simple boucle suffit.\n\n**Formulation orale naturelle :** « Chaque appel empile un contexte sur la pile d'appels ; sans cas de base ça déborde. Je prends l'itératif quand la profondeur est grande ou la boucle plus simple, et la récursion quand le problème se décompose naturellement. »`,
  },

  // ── Jour 26 — Fonctions pures et immutabilité ──
  26: {
    interview: `**Mise en situation : « Voici une fonction \`ajouterTache(liste, texte)\` qui fait \`liste.push({texte, date: Date.now()})\` puis renvoie \`liste\`. Pourquoi est-elle DIFFICILE à tester, et comment la rends-tu pure ? »**\n\n*Ce que la question évalue :* la distinction fonction pure / effet de bord et la pratique de l'immutabilité — sur un cas concret de code impur à diagnostiquer.\n\n**Réponse attendue :** deux problèmes. (1) Elle MUTE son argument (\`push\` modifie la \`liste\` de l'appelant) : effet de bord, comportement dépendant de l'extérieur. (2) Elle lit \`Date.now()\` : sa sortie n'est pas déterministe, donc un test ne peut pas prédire le résultat. Pour la rendre pure : retourner une NOUVELLE liste sans muster l'entrée (\`return [...liste, { texte, date }]\`), et INJECTER la date en paramètre (\`ajouterTache(liste, texte, date)\`) au lieu de la lire dans la fonction. Ainsi mêmes entrées → même sortie, sans effet de bord.\n\n**Débutant / correct / excellent :** débutant = ne voit ni la mutation ni le non-déterminisme ; correct = identifie les deux et corrige (spread + date injectée) ; excellent = généralise (« isoler les effets aux bords, garder un cœur pur ») et explique pourquoi une fonction pure est triviale à tester.\n\n**Relance possible :** « pourquoi une fonction pure est-elle plus facile à tester ? » → mêmes entrées → même sortie, sans montage ni horloge : on compare simplement la sortie à l'attendu.\n\n**À éviter :** muter l'argument avec \`push\` ; lire l'horloge/le hasard au cœur de la logique ; croire que renvoyer la liste suffit alors qu'on l'a mutée.\n\n**Formulation orale naturelle :** « Elle mute son argument avec \`push\` et lit \`Date.now()\`, donc elle est imprévisible : je renvoie une nouvelle liste avec le spread et j'injecte la date en paramètre — mêmes entrées, même sortie, testable. »`,
  },

  // ── Jour 27 — stats.js : pipeline fonctionnel complet ──
  27: {
    interview: `**Mise en situation : « Présente-moi l'architecture de ton analyseur de ventes. Pourquoi enchaîner \`map\` → \`filter\` → \`reduce\` plutôt qu'une seule grosse boucle qui fait tout, et comment GARANTIS-tu qu'aucune donnée d'entrée n'est modifiée au passage ? »**\n\n*Ce que la question évalue :* la capacité à défendre un pipeline fonctionnel (lisibilité, composition, immutabilité) — la synthèse des acquis fonctionnels du mois.\n\n**Réponse attendue :** j'enchaîne des étapes NOMMÉES et à responsabilité unique — filtrer les ventes valides, transformer, agréger — ce qui rend chaque étape lisible et testable isolément, là où une grosse boucle mélange tout et est difficile à modifier. Je garantis l'immutabilité en n'utilisant que des opérations qui renvoient de NOUVELLES valeurs (\`map\`/\`filter\`/spread), jamais \`push\`/\`sort\` sur l'entrée : la liste d'origine reste intacte, ce que je peux VÉRIFIER en la comparant avant/après.\n\n**Débutant / correct / excellent :** débutant = une grosse boucle ou un pipeline qui mute l'entrée ; correct = pipeline d'étapes pures + immutabilité vérifiable ; excellent = sépare le cœur pur (calculs) de la coquille impure (lecture fichier/affichage) et explique que le pipeline se teste étape par étape.\n\n**Relance possible :** « comment testerais-tu ce pipeline ? » → chaque étape est une fonction pure : je lui donne une entrée connue et je compare la sortie à l'attendu, sans fichiers.\n\n**À éviter :** une boucle monolithique qui fait tout ; muter la liste d'entrée (\`sort\`/\`push\`) ; mélanger lecture de fichier et calcul dans la même fonction.\n\n**Formulation orale naturelle :** « J'enchaîne des étapes pures et nommées — filtrer, transformer, agréger — plus lisibles et testables qu'une grosse boucle ; et je n'utilise que des opérations qui renvoient de nouvelles valeurs, donc la liste d'entrée reste intacte, ce que je vérifie. »`,
  },

  // ── Jour 29 — Récursion niveau 2 : structures imbriquées ──
  29: {
    interview: `**Mise en situation : « On te donne un objet JSON de profondeur INCONNUE (des objets et des tableaux imbriqués les uns dans les autres) et tu dois compter tous les nombres qu'il contient, à tous les niveaux. Pourquoi la récursion est-elle l'outil naturel ici, et quel est ton cas de base ? »**\n\n*Ce que la question évalue :* l'application de la récursion à des structures imbriquées réelles (le pont algo ↔ données) — sur un problème concret, pas une définition.\n\n**Réponse attendue :** la structure est récursive (un objet peut contenir des objets/tableaux, à profondeur inconnue), donc un algorithme récursif épouse sa forme : pour chaque valeur, si c'est un nombre je le compte, si c'est un objet ou un tableau je RAPPELLE la fonction sur ses éléments. Le CAS DE BASE, c'est la valeur SCALAIRE (nombre, chaîne, booléen) : on ne descend plus, on décide sur place. La récursion s'arrête naturellement quand il n'y a plus de niveau à explorer.\n\n**Débutant / correct / excellent :** débutant = tente des boucles imbriquées fixes (marche seulement pour une profondeur connue) ; correct = récursion qui distingue scalaire (cas de base) et conteneur (appel récursif) ; excellent = gère à la fois objets ET tableaux, évite les pièges (\`typeof null === 'object'\`), et note qu'une profondeur énorme pourrait déborder la pile.\n\n**Relance possible :** « pourquoi des boucles imbriquées ne suffisent-elles pas ? » → parce que la profondeur est INCONNUE : on ne peut pas écrire à l'avance le bon nombre de boucles ; la récursion s'adapte à n'importe quelle profondeur.\n\n**À éviter :** supposer une profondeur fixe ; oublier de traiter tableaux ET objets ; le piège \`typeof null === 'object'\` ; ne pas définir clairement le cas de base scalaire.\n\n**Formulation orale naturelle :** « La donnée est imbriquée à profondeur inconnue, donc la récursion épouse sa forme : je descends sur chaque objet ou tableau, et mon cas de base est la valeur scalaire où je compte sur place — des boucles fixes ne marcheraient que pour une profondeur connue. »`,
  },
};
