// Enrichissement Batch 5D — jours 91-120 (React : composants/hooks/effets/
// formulaires/routing/état ; ingénierie logicielle front : tests/clean code/
// hooks/erreurs ; Projet 3 BiblioApp ; entrée Python idiomatique). Fusionné
// dans generate-curriculum.mjs.
// Jours de travail : theory, guided, caseStudy, interview, future,
//   solution {logic, pitfalls, checks, oral}, lessons (override).
//   (guided intègre un exemple simple + un mini-quiz ; solution.logic donne
//    une solution simple PUIS une solution améliorée.)
// Jours de revue (91, 98, 105, 112, 119) : reviewSynthese, reviewGrid,
//   remediation, interview.

export const ENRICH_91_120 = {
  // ── Revue semaine 13 : Projet 2 (LivreAPI + Postman + revue mensuelle 3) ──
  91: {
    reviewSynthese: `La semaine 13 a clôturé le mois 3 avec le Projet 2 (LivreAPI) : une API REST complète avec ses routes CRUD, sa validation, ses codes HTTP corrects, sa collection Postman documentée et une revue mensuelle. Le fil rouge du mois : une API n'est pas « du code qui répond », c'est un CONTRAT — des routes prévisibles, des statuts qui disent la vérité (200/201/400/404/500), des erreurs structurées, et une documentation exécutable (Postman) qui prouve qu'elle marche. Tu entres maintenant dans le mois 4 (React) avec un back-end réel à consommer : ton front des semaines suivantes se branchera sur CETTE API. La bascule mentale à réussir : tu passes de « je produis des réponses JSON » à « je consomme un contrat et j'en affiche les trois états (chargement, erreur, données) ».`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Concevoir des routes REST cohérentes (noms de ressources, verbes HTTP corrects) | |\n| Renvoyer le bon code de statut selon le cas (200/201/400/404/500) | |\n| Valider les entrées et renvoyer une erreur structurée (pas un plantage) | |\n| Documenter l'API dans une collection Postman rejouable | |\n| Expliquer pourquoi une API est un CONTRAT (stabilité, prévisibilité) | |\n| Décrire les 3 états qu'un client devra gérer face à cette API | |\n\n**Total /18** : ≥ 15 = solide, attaque React sereinement · 11-14 = revoir les items ≤ 1 · < 11 = rejouer LivreAPI avant le front, car tu vas la consommer.`,
    remediation: `- **Routes incohérentes ou verbes mal choisis** (GET qui modifie, POST pour lire) : reprends la conception REST — une ressource au pluriel, le verbe HTTP porte l'action. Une API dont on ne devine pas les routes n'est pas consommable.\n- **Codes de statut approximatifs** (tout en 200, même les erreurs) : le statut est la PREMIÈRE information que ton front React lira pour choisir entre les 3 états. Réaligne 201 (créé), 400 (entrée invalide), 404 (absent), 500 (bug serveur).\n- **Pas de validation** : une entrée non validée = un plantage 500 au lieu d'un 400 explicite. Ajoute la validation en entrée de chaque route qui reçoit un corps.\n- **Collection Postman absente ou non rejouable** : sans elle, tu ne peux pas PROUVER que l'API marche ni t'y brancher méthodiquement. Reconstruis-la, une requête par route, avec un exemple de corps et la réponse attendue.`,
    interview: `1. **« Qu'est-ce qui fait qu'une API REST est bien conçue ? »** Attendu : des ressources nommées clairement, les bons verbes HTTP, des codes de statut qui disent la vérité, des erreurs structurées, une documentation qui permet de la consommer sans lire le code.\n2. **« Pourquoi un code de statut HTTP compte-t-il autant pour le client ? »** Attendu : c'est le premier signal que le client lit pour décider quoi faire (afficher, réessayer, montrer une erreur) ; tout renvoyer en 200 force le client à deviner.\n3. **« Quels sont les trois états qu'un front doit gérer face à une API ? »** Attendu : chargement, erreur, données — un fetch a TOUJOURS ces trois états, et on ne peut jamais afficher « données » sans données.\n4. **« Que prouve une collection Postman ? »** Attendu : que l'API marche vraiment, route par route ; c'est une documentation exécutable, pas une promesse.`,
  },

  // ── React : composants, props, JSX ──
  92: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : un composant React est une FONCTION PURE qui prend des props et renvoie du JSX — l'UI est le RÉSULTAT d'un calcul, pas une suite d'ordres donnés au DOM.** C'est le renversement le plus important à intégrer. En JavaScript vanilla (ou jQuery), tu écrivais des instructions IMPÉRATIVES : « crée un élément, ajoute-le, change son texte, retire cette classe ». En React, tu écris une fonction DÉCLARATIVE : « pour ces props, voici à quoi l'UI doit ressembler » — et React se charge de faire correspondre le DOM à ta description. Tu ne touches plus le DOM à la main.\n\nLes trois notions du jour :\n1. **Le composant** : une fonction qui renvoie du JSX. Son nom commence par une MAJUSCULE (React distingue \`<Carte/>\`, ton composant, de \`<div/>\`, une balise HTML). Elle doit être PURE : mêmes props → même rendu, sans effet de bord.\n2. **Les props** : les ARGUMENTS du composant, en lecture seule. Un composant ne modifie JAMAIS ses props — il les reçoit et les affiche. C'est le flux de données unidirectionnel : les données descendent du parent vers l'enfant.\n3. **Le JSX** : une syntaxe qui ressemble à du HTML mais EST du JavaScript. \`className\` (pas \`class\`), \`{expression}\` pour injecter une valeur, un seul élément racine (ou un Fragment \`<>...</>\`). Le JSX est compilé en appels de fonction — ce n'est pas une chaîne de caractères.\n\nLa règle qui gouverne tout le mois : **penser en composants, c'est décomposer une interface en petites fonctions réutilisables et typées, chacune responsable d'un morceau**. Une bonne décomposition rend l'UI lisible (chaque composant tient dans la tête), réutilisable (la même Carte pour tous les produits) et testable. C'est exactement la modularité du clean code (mois 2) appliquée à l'écran.`,
    guided: `**Énoncé** : décomposer une carte de livre en composants typés, à partir de props.\n\n**Exemple simple** : un composant minimal — \`function Badge({ texte }: { texte: string }) { return <span className=\"badge\">{texte}</span>; }\`. Une fonction, une prop typée, du JSX. Tout le reste n'est que composition de briques comme celle-ci.\n\n**Raisonnement pas-à-pas** : repérer les blocs répétés d'une maquette → en faire des composants → typer leurs props → les composer dans un parent.\n\n**Solution** :\n\`\`\`tsx\ntype Livre = { id: number; titre: string; auteur: string; dispo: boolean };\n\nfunction Badge({ dispo }: { dispo: boolean }) {\n  return <span className={dispo ? 'ok' : 'ko'}>{dispo ? 'Dispo' : 'Emprunté'}</span>;\n}\n\nfunction CarteLivre({ livre }: { livre: Livre }) {   // props en lecture seule\n  return (\n    <article className=\"carte\">\n      <h3>{livre.titre}</h3>\n      <p>{livre.auteur}</p>\n      <Badge dispo={livre.dispo} />   {/* composition : un composant dans un autre */}\n    </article>\n  );\n}\n\nfunction Liste({ livres }: { livres: Livre[] }) {\n  return <div>{livres.map((l) => <CarteLivre key={l.id} livre={l} />)}</div>;\n}\n\`\`\`\n\n**Mini-quiz** (réponds sans regarder) : (a) Pourquoi le nom d'un composant commence-t-il par une majuscule ? (b) Un composant peut-il modifier ses props ? (c) Pourquoi \`className\` et pas \`class\` ?\n→ (a) pour que React le distingue d'une balise HTML native ; (b) non, les props sont en lecture seule (flux unidirectionnel) ; (c) \`class\` est un mot réservé de JavaScript, le JSX étant du JS.\n\n**Variante** : ajoute une prop \`onEmprunter\` (une fonction) passée du parent à la Carte, et un bouton qui l'appelle — tu prépares la remontée d'événements du jour 100.`,
    caseStudy: `Toute interface d'entreprise moderne (tableau de bord, e-commerce, back-office) est un arbre de composants React ou équivalent. Les équipes qui décomposent proprement — une Carte réutilisée partout, des props typées — livrent vite et cassent rarement ; celles qui empilent du JSX monolithique de 400 lignes passent leur temps à débugger des régressions. Savoir découper une maquette en composants réutilisables est la compétence front n°1 attendue d'un développeur, et le typage des props (TypeScript) est ce qui distingue un front professionnel d'un prototype fragile.`,
    interview: `**« Qu'est-ce qu'un composant React, et pourquoi dit-on qu'il doit être 'pur' ? »** → Un composant est une fonction qui reçoit des props et renvoie du JSX décrivant l'UI. « Pur » signifie : mêmes props → même rendu, sans effet de bord ni mutation de ses props. Cette pureté est ce qui permet à React de re-rendre de façon fiable et prévisible — il peut appeler la fonction quand il veut sans surprise. Le corollaire : les données descendent du parent vers l'enfant (flux unidirectionnel), et un composant ne modifie jamais ce qu'il reçoit.`,
    future: `- **Bankable** : penser en composants typés et réutilisables est la base de tout développement front — la compétence évaluée dès le premier test technique d'un poste full-stack.\n- Cette décomposition structure BiblioApp (Projet 3, jours 113-118) et l'interface de DocSense (mois 11) — tout ce que tu affiches sera un arbre de composants.\n- Le réflexe déclaratif (« je décris l'UI, je ne la manipule pas ») se transfère à toute UI moderne et à la façon dont tu raisonneras sur les interfaces d'agents IA (mois 10).`,
    solution: {
      logic: "Solution simple : une fonction par bloc visuel, des props typées, composées dans un parent. Solution améliorée : isoler les vrais composants RÉUTILISABLES (Badge servira ailleurs), typer précisément les props (pas de any), garder chaque composant pur (aucune mutation, aucun effet de bord), et préparer la communication vers le haut via des props-fonctions (onEmprunter). La preuve de bonne décomposition : la même Carte s'utilise dans plusieurs contextes sans modification.",
      pitfalls: ["Un composant géant qui fait tout : illisible et non réutilisable — découpe en briques nommées d'après leur rôle.", "Muter les props (les props sont en lecture seule) : casse le flux unidirectionnel et provoque des bugs invisibles.", "Oublier la majuscule sur le nom du composant : React le prend pour une balise HTML et ne le rend pas.", "Typer les props avec any : on perd toute la sécurité de TypeScript, qui est justement l'intérêt de typer un front."],
      checks: ["Chaque composant est une fonction pure qui renvoie du JSX.", "Les props sont typées précisément (aucun any).", "Aucun composant ne modifie ses props.", "Au moins un composant est réellement réutilisé dans plusieurs contextes.", "La liste utilise une key stable (id), pas l'index."],
      oral: "Explique le renversement déclaratif : « en vanilla je donnais des ordres au DOM ; en React j'écris une fonction qui décrit l'UI pour des props données, et React fait correspondre le DOM ». Puis la règle : composants purs, props en lecture seule, données qui descendent. Savoir décomposer une maquette au tableau en 5 composants nommés est un signal fort en entretien front.",
    },
  },

  // ── State et interactivité (useState) ──
  93: {
    lessons: ['react-hooks-effects.md'],
    theory: `**Modèle mental : le state n'est pas une variable que tu MODIFIES, c'est une valeur que tu REMPLACES — et c'est ce remplacement qui déclenche un nouveau rendu.** \`useState\` te donne deux choses : la valeur actuelle et une fonction pour la remplacer. Tu n'écris jamais \`compteur = compteur + 1\` ; tu appelles \`setCompteur(compteur + 1)\`. La différence est fondamentale : React ne « surveille » pas tes variables — il re-rend uniquement quand tu l'INFORMES via un setter qu'une nouvelle valeur existe.\n\nLes trois lois du state :\n1. **Immutabilité** : pour un objet ou un tableau, tu ne modifies pas l'ancien, tu en crées un NOUVEAU. \`liste.push(x)\` ne re-rend PAS (même référence) ; \`setListe([...liste, x])\` re-rend (nouvelle référence). C'est le piège valeur/référence de JS (mois 1) qui devient une règle React : React compare les références pour décider s'il re-rend.\n2. **Le state est une capture** : à chaque rendu, la valeur du state est FIGÉE pour ce rendu. D'où le setter fonctionnel \`setCompteur(c => c + 1)\` quand la nouvelle valeur dépend de l'ancienne — indispensable si tu fais plusieurs mises à jour d'affilée.\n3. **Le bon niveau de granularité** : plusieurs \`useState\` pour des valeurs indépendantes, un seul state-objet quand les valeurs bougent ensemble (comme la machine à 3 états loading/error/data du jour 95).\n\nLa règle qui gouverne l'interactivité React : **un événement met à jour le state (immuablement), et le nouveau state produit un nouveau rendu**. L'UI est toujours une fonction du state courant — tu ne manipules jamais l'affichage directement, tu changes le state et laisses React recalculer l'écran. C'est la boucle événement → setState → rendu, le cœur de toute app interactive.`,
    guided: `**Énoncé** : un compteur borné et une liste de courses, avec un state 100 % immuable.\n\n**Exemple simple** : la mise à jour immuable la plus courante — ajouter à une liste : \`setItems([...items, nouvel])\`. Jamais \`items.push(nouvel)\`. Retiens ce spread : 90 % des bugs de state viennent d'une mutation oubliée.\n\n**Raisonnement pas-à-pas** : identifier ce qui change (le state) → le mettre à jour via le setter avec une NOUVELLE référence → laisser React re-rendre.\n\n**Solution** :\n\`\`\`tsx\nfunction Compteur({ min = 0, max = 10 }: { min?: number; max?: number }) {\n  const [n, setN] = useState(min);\n  return (\n    <div>\n      <button onClick={() => setN((c) => Math.max(min, c - 1))}>-</button>\n      <span>{n}</span>\n      <button onClick={() => setN((c) => Math.min(max, c + 1))}>+</button>\n    </div>\n  );\n}\n\nfunction Courses() {\n  const [items, setItems] = useState<{ id: number; nom: string; pris: boolean }[]>([]);\n  const ajouter = (nom: string) =>\n    setItems((xs) => [...xs, { id: Date.now(), nom, pris: false }]);   // NOUVEAU tableau\n  const cocher = (id: number) =>\n    setItems((xs) => xs.map((x) => x.id === id ? { ...x, pris: !x.pris } : x)); // NOUVEAUX objets\n  return /* ... rendu de la liste ... */;\n}\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi \`items.push(x)\` ne déclenche-t-il pas de rendu ? (b) Quand utiliser \`setN(c => c + 1)\` plutôt que \`setN(n + 1)\` ? (c) Que compare React pour décider de re-rendre ?\n→ (a) parce que c'est la MÊME référence de tableau — React ne voit pas de changement ; (b) quand la nouvelle valeur dépend de l'ancienne, surtout en cas de mises à jour multiples ; (c) la référence (identité) de la valeur, pas son contenu.\n\n**Variante** : ajoute un bouton « reset » et un compteur d'articles cochés DÉRIVÉ du state (\`items.filter(x => x.pris).length\`) — n'en fais PAS un state séparé : ce qui se calcule ne se stocke pas.`,
    caseStudy: `Le bug le plus fréquent des juniors React en production : « j'ai modifié le tableau mais l'écran ne bouge pas ». La cause est toujours la même — une mutation (\`push\`, \`sort\`, \`obj.champ = x\`) au lieu d'un remplacement immuable. Les équipes imposent l'immutabilité (lint, revues) parce qu'elle rend le state prévisible et débuggable. Maîtriser l'immutabilité n'est pas de la coquetterie : c'est ce qui fait qu'une UI interactive se comporte de façon fiable, et c'est directement transférable à Redux, à la gestion d'état de toute app, et au raisonnement sur les données immuables en général.`,
    interview: `**« Pourquoi ne faut-il jamais muter le state directement en React ? »** → Parce que React décide de re-rendre en comparant les RÉFÉRENCES, pas le contenu. Si je fais \`liste.push(x)\`, la référence du tableau ne change pas : React ne voit rien, l'UI ne se met pas à jour. Je dois créer une NOUVELLE référence (\`[...liste, x]\` ou \`.map(...)\`), passée au setter. C'est le comportement valeur/référence de JavaScript qui devient une règle : l'immutabilité rend le state prévisible et permet à React d'optimiser les rendus. Corollaire : ce qui se calcule à partir du state (un total, un filtre) ne se stocke pas en state, on le dérive.`,
    future: `- **Bankable** : l'immutabilité du state est LE réflexe qui sépare un front qui marche d'un front qui a des bugs fantômes — évalué en test technique et en revue de code dès le premier jour d'un poste front.\n- Cette discipline se réutilise dans BiblioApp (CRUD immuable, jour 114), dans toute gestion d'état (Context jour 101, Redux) et dans le raisonnement sur les données côté data/IA.\n- Le principe « ce qui se calcule ne se stocke pas » (state dérivé) évite une classe entière de bugs de désynchronisation, dans le front comme dans les pipelines de données.`,
    solution: {
      logic: "Solution simple : useState pour chaque valeur qui change, setter appelé sur chaque événement. Solution améliorée : garantir l'immutabilité partout (spread pour ajouter, map avec spread d'objet pour modifier, filter pour retirer), utiliser le setter FONCTIONNEL quand la valeur dépend de l'ancienne, DÉRIVER les valeurs calculables (total, nombre coché) au lieu de les stocker, et borner le state (min/max) dans le setter. La preuve : aucune mutation, l'UI reste toujours synchrone avec le state.",
      pitfalls: ["Muter au lieu de remplacer (push/sort/obj.x = v) : pas de re-rendu, bug classique — spread systématique.", "Stocker en state une valeur dérivable (le total des articles) : double source de vérité, désynchronisation garantie — dérive-la à la volée.", "Utiliser setN(n+1) dans plusieurs mises à jour successives : elles se basent sur la même valeur figée — utilise setN(c => c+1).", "Un seul gros state-objet pour des valeurs indépendantes (ou l'inverse) : granularité mal choisie, mises à jour plus fragiles."],
      checks: ["Chaque mise à jour crée une nouvelle référence (aucune mutation).", "Les valeurs calculables sont dérivées, pas stockées en state.", "Le setter fonctionnel est utilisé quand la valeur dépend de l'ancienne.", "L'UI reflète toujours le state courant (jamais de manipulation directe du DOM).", "Le compteur respecte ses bornes min/max."],
      oral: "Montre la boucle au tableau : événement → setState (immuable) → nouveau rendu. Puis la phrase clé : « je ne modifie pas le state, je le remplace ; React re-rend parce que la référence change ». Illustre avec le bug du push qui ne re-rend pas — c'est l'exemple qui prouve que tu as compris valeur/référence appliqué à React.",
    },
  },

  // ── Rendu conditionnel et listes ──
  94: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : afficher une liste, c'est PROJETER des données en JSX (\`map\`), et la \`key\` est l'IDENTITÉ stable que React utilise pour savoir quel élément est lequel entre deux rendus — ce n'est pas une décoration.** Deux mécanismes s'assemblent aujourd'hui : le rendu conditionnel (montrer un morceau d'UI selon une condition) et le rendu de listes (transformer un tableau de données en tableau d'éléments). Tous deux découlent du même principe : le JSX est du JavaScript, donc tu utilises les outils JS (\`&&\`, ternaire, \`.map\`, \`.filter\`) pour construire l'UI.\n\nLes points à maîtriser :\n1. **Le rendu conditionnel** : \`{condition && <X/>}\` (afficher si vrai), \`{cond ? <A/> : <B/>}\` (l'un ou l'autre). Piège classique : \`{liste.length && <Liste/>}\` affiche \`0\` quand la liste est vide (car \`0\` est falsy mais rendu) — écris \`{liste.length > 0 && ...}\`.\n2. **Le rendu de listes** : \`{items.map((it) => <Ligne key={it.id} {...it} />)}\`. La \`key\` DOIT être un identifiant STABLE et unique (l'id métier), jamais l'index du tableau si la liste peut être réordonnée, filtrée ou modifiée. Une key = index provoque des bugs subtils : React réutilise le mauvais composant, un champ de formulaire garde la valeur du voisin supprimé.\n3. **Les états edge** : une liste a toujours au moins trois cas à rendre — vide (« aucun résultat »), en chargement (squelette/spinner), et rempli. Une UI professionnelle traite le cas vide EXPLICITEMENT, pas comme un oubli.\n\nLa règle qui gouverne le jour : **une liste rendue est une fonction pure de ses données, et son identité passe par des keys stables**. Pourquoi la key compte tant : React « réconcilie » l'ancien et le nouveau rendu en appariant les éléments par leur key. Une key stable = React sait qu'un élément a bougé/changé/disparu et met à jour le minimum. Une mauvaise key = React se trompe d'élément, avec des bugs d'affichage et d'état invisibles à la lecture.`,
    guided: `**Énoncé** : une liste filtrable avec ses états vide et chargement, et des keys correctes.\n\n**Exemple simple** : le cas vide, souvent oublié — \`{resultats.length === 0 && <p>Aucun résultat</p>}\`. Toujours prévoir ce que l'écran montre quand il n'y a RIEN à montrer.\n\n**Raisonnement pas-à-pas** : gérer d'abord chargement, puis vide, puis la liste ; mapper avec une key stable ; dériver la liste filtrée du state (jour 93).\n\n**Solution** :\n\`\`\`tsx\nfunction Livres({ livres, chargement }: { livres: Livre[]; chargement: boolean }) {\n  const [q, setQ] = useState('');\n  const filtres = livres.filter((l) => l.titre.toLowerCase().includes(q.toLowerCase()));\n\n  if (chargement) return <Spinner />;                       // état 1 : chargement\n  return (\n    <>\n      <input value={q} onChange={(e) => setQ(e.target.value)} placeholder=\"Rechercher\" />\n      {filtres.length === 0                                   // état 2 : vide (explicite)\n        ? <p>Aucun livre ne correspond.</p>\n        : <ul>{filtres.map((l) => <li key={l.id}>{l.titre}</li>)}</ul>  // état 3 : key stable\n      }\n    </>\n  );\n}\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi ne pas utiliser l'index du tableau comme key sur une liste filtrable ? (b) Que rend \`{liste.length && <X/>}\` quand la liste est vide ? (c) Où vient la liste filtrée — state ou dérivée ?\n→ (a) l'index change quand on filtre/réordonne, React apparie les mauvais éléments (bugs d'état) ; (b) \`0\` s'affiche à l'écran (0 est falsy mais rendu) — utilise \`length > 0\` ; (c) dérivée du state \`q\`, jamais stockée (jour 93).\n\n**Variante** : sur chaque ligne, ajoute un bouton supprimer ; vérifie qu'avec des keys = id, supprimer le 2e élément ne « décale » aucune valeur — puis teste avec key = index pour VOIR le bug.`,
    caseStudy: `Le bug « key=index » est un classique des revues de code : une liste avec des champs éditables où supprimer une ligne fait apparaître la valeur d'une autre. La cause est toujours la même — React apparie les éléments par leur key, et un index n'est pas une identité stable. Les équipes front l'attrapent en revue parce que c'est une source de bugs coûteux et difficiles à reproduire. Savoir choisir une key stable et traiter le cas vide explicitement est un signe de maturité front : ça montre que tu penses aux données réelles (vides, filtrées, réordonnées), pas seulement au cas idéal.`,
    interview: `**« À quoi sert la prop \`key\` dans une liste React, et pourquoi éviter l'index ? »** → La \`key\` donne à chaque élément une IDENTITÉ stable que React utilise pour réconcilier l'ancien et le nouveau rendu : il sait ainsi quel élément a bougé, changé ou disparu, et met à jour le minimum. Si j'utilise l'index du tableau, l'identité change dès que la liste est filtrée ou réordonnée — React apparie les mauvais éléments, ce qui provoque des bugs d'état invisibles (un champ qui garde la valeur du voisin supprimé). Il faut une key issue des données (un id métier unique et stable).`,
    future: `- **Bankable** : les keys stables et le traitement des états vide/chargement sont des marqueurs immédiats de sérieux front — repérés en revue de code et en test technique.\n- Ce réflexe structure toutes les listes de BiblioApp (recherche/filtres, jour 115) et toute UI affichant des données (résultats RAG, historiques d'agents).\n- Penser systématiquement aux états edge (vide, chargement, erreur) est une discipline qui se transfère à toute interface data — un tableau de résultats vide doit dire pourquoi, pas rester blanc.`,
    solution: {
      logic: "Solution simple : map avec key + un ternaire pour le cas vide. Solution améliorée : gérer les trois états (chargement, vide, rempli) explicitement et dans le bon ordre, utiliser une key issue des données (id métier stable), dériver la liste filtrée du state de recherche (pas de doublon de state), et éviter le piège \`length &&\` qui affiche 0. La preuve de maîtrise : supprimer/réordonner un élément ne décale aucune donnée voisine.",
      pitfalls: ["key = index sur une liste filtrable/réordonnable : React apparie les mauvais éléments, bugs d'état difficiles à reproduire.", "\`{liste.length && <X/>}\` : affiche \`0\` quand vide — toujours \`length > 0 &&\`.", "Oublier le cas vide : l'écran reste blanc sans expliquer pourquoi — un état vide explicite est attendu.", "Stocker la liste filtrée en state au lieu de la dériver : désynchronisation avec la recherche (jour 93)."],
      checks: ["Chaque élément de liste a une key stable issue des données (pas l'index).", "Les états chargement, vide et rempli sont tous rendus explicitement.", "La liste filtrée est dérivée du state de recherche.", "Le cas vide affiche un message clair.", "Supprimer/réordonner ne décale aucune valeur voisine (test de la variante)."],
      oral: "Explique la réconciliation par key : « React apparie les éléments par leur key pour mettre à jour le minimum ; un index n'est pas une identité stable, donc il se trompe d'élément quand la liste bouge ». Montre le bug du champ qui garde la valeur du voisin supprimé — c'est la démonstration concrète qui prouve que tu as compris à quoi sert vraiment la key.",
    },
  },

  // ── Effets et fetch (useEffect) ──
  95: {
    lessons: ['react-hooks-effects.md'],
    theory: `**Modèle mental : un effet (\`useEffect\`) SYNCHRONISE ton composant avec un système EXTÉRIEUR à React (une API, un timer, un abonnement) — ce n'est pas « du code qui tourne après le rendu », c'est un pont vers le monde hors React, avec un nettoyage obligatoire.** Le rendu d'un composant doit rester PUR (jour 92) : il calcule du JSX, sans effet de bord. Mais une vraie app doit parler au monde extérieur — chercher des données, poser un timer. \`useEffect\` est l'endroit prévu pour ces effets de bord, exécutés APRÈS le rendu, sans polluer la pureté.\n\nLes trois pièces d'un effet :\n1. **La fonction d'effet** : ce qui parle à l'extérieur (le fetch, l'abonnement).\n2. **Le tableau de dépendances** : quand l'effet doit-il se ré-exécuter ? \`[]\` = une seule fois au montage ; \`[id]\` = à chaque changement d'\`id\`. Une dépendance oubliée = un effet qui utilise une valeur périmée (bug de « stale closure »).\n3. **Le nettoyage** (la fonction retournée) : annuler ce que l'effet a lancé (abonnement, timer, requête). Sans lui, un composant démonté qui reçoit une réponse tardive provoque un warning « setState sur composant démonté » et des fuites.\n\nLe fetch impose sa discipline : **un appel réseau a TOUJOURS trois états** — chargement, erreur, données — et on ne peut jamais afficher « données » sans données. On modélise ces trois états dans un seul state (une machine à états, jour 93) et on en rend exactement un à la fois. C'est la même rigueur que côté API (mois 3, les 3 réponses possibles) vue depuis le client.\n\nLa règle du jour : **le rendu décrit l'UI pour un state donné ; l'effet va chercher les données qui remplissent ce state, et nettoie derrière lui**. La bascule mentale par rapport à JS vanilla : tu ne fais plus « au clic, fetch puis manipule le DOM » ; tu fais « l'effet remplit le state, le state produit l'UI ». Le nombre de \`useEffect\` que tu écris doit rester petit : beaucoup de choses qu'on croit être des effets sont en fait des valeurs dérivées (jour 93).`,
    guided: `**Énoncé** : charger une liste depuis une API avec ses 3 états, une dépendance correcte et un nettoyage.\n\n**Exemple simple** : la machine à 3 états en un seul state — \`useState<{ s: 'load' | 'err' | 'ok'; data?: Livre[] }>({ s: 'load' })\`. On ne peut JAMAIS être en 'ok' sans \`data\` : le typage l'interdit presque.\n\n**Raisonnement pas-à-pas** : un state qui encode les 3 états → un effet qui fetch et le met à jour → un nettoyage qui ignore une réponse tardive → un rendu qui affiche l'état courant.\n\n**Solution** :\n\`\`\`tsx\nfunction Livres({ url }: { url: string }) {\n  const [etat, setEtat] = useState<{ s: 'load' | 'err' | 'ok'; data?: Livre[] }>({ s: 'load' });\n\n  useEffect(() => {\n    let annule = false;                                  // garde-fou de nettoyage\n    setEtat({ s: 'load' });\n    fetch(url)\n      .then((r) => { if (!r.ok) throw new Error(String(r.status)); return r.json(); })\n      .then((data) => { if (!annule) setEtat({ s: 'ok', data }); })\n      .catch(() => { if (!annule) setEtat({ s: 'err' }); });\n    return () => { annule = true; };                     // NETTOYAGE : ignore la réponse tardive\n  }, [url]);                                             // DÉPENDANCE : re-fetch si l'url change\n\n  if (etat.s === 'load') return <Spinner />;\n  if (etat.s === 'err') return <p>Erreur de chargement <button>réessayer</button></p>;\n  return <ul>{etat.data!.map((l) => <li key={l.id}>{l.titre}</li>)}</ul>;\n}\n\`\`\`\n\n**Mini-quiz** : (a) Que se passe-t-il si on oublie \`url\` dans les dépendances ? (b) À quoi sert le drapeau \`annule\` ? (c) Peut-on afficher l'état 'ok' sans données ?\n→ (a) l'effet garde la première url (stale closure), ne re-fetch pas quand elle change ; (b) à ignorer une réponse arrivant après le démontage/changement, évitant un setState sur composant démonté ; (c) non, le state encode data avec 'ok' — la machine à états l'empêche.\n\n**Variante** : rends le bouton « réessayer » fonctionnel en ajoutant un state \`essai\` incrémenté au clic et mis dans les dépendances — tu déclenches un re-fetch propre sans dupliquer la logique.`,
    caseStudy: `Les fuites et warnings « setState sur composant démonté » sont un problème réel : un utilisateur navigue vite entre deux pages, la première reçoit sa réponse API après avoir disparu, et l'app plante ou fuit. Le nettoyage d'effet règle exactement ce cas. Et la discipline des 3 états est ce qui distingue une app qui « marche sur ma machine » (réseau rapide, jamais d'erreur) d'une app robuste en conditions réelles (réseau lent, API en panne). Les recruteurs testent précisément cela : « montre-moi comment tu charges des données » — la bonne réponse inclut chargement, erreur ET nettoyage, pas juste le cas heureux.`,
    interview: `**« Explique \`useEffect\` : à quoi sert-il et quels sont ses pièges ? »** → \`useEffect\` sert à SYNCHRONISER le composant avec un système extérieur (API, timer, abonnement) — les effets de bord qui n'ont pas leur place dans le rendu, qui doit rester pur. Trois pièges : (1) le tableau de dépendances — en oublier une donne une « stale closure » qui utilise une valeur périmée ; (2) l'absence de nettoyage — un composant démonté qui reçoit une réponse tardive provoque un warning et des fuites, d'où la fonction de nettoyage retournée ; (3) l'over-use — beaucoup de « données qu'on va chercher » sont en fait des valeurs dérivées qui n'ont pas besoin d'effet. Et tout fetch a trois états à rendre : chargement, erreur, données.`,
    future: `- **Bankable** : savoir charger des données proprement (3 états + dépendances justes + nettoyage) est LA compétence testée en entretien front — « montre-moi comment tu fetches » revient systématiquement.\n- Ce patron alimente chaque écran de BiblioApp qui consomme ton API (jours 113-118) et toute UI branchée sur un back-end (dont DocSense).\n- La rigueur « chargement / erreur / données, toujours les trois » se transfère à toute interface asynchrone, y compris les UI d'agents IA qui attendent une réponse LLM.`,
    solution: {
      logic: "Solution simple : un useEffect qui fetch et remplit un state, avec loading/error/data. Solution améliorée : encoder les 3 états dans UNE machine à états (impossible d'être 'ok' sans data), déclarer les BONNES dépendances (re-fetch quand l'url change), ajouter un nettoyage (drapeau annule) qui ignore les réponses tardives, gérer le statut HTTP (r.ok) et proposer un re-essai propre via une dépendance dédiée. La preuve : naviguer vite entre deux ressources ne produit aucun warning ni affichage incohérent.",
      pitfalls: ["Dépendances incomplètes : stale closure, l'effet utilise une valeur périmée — inclure tout ce que l'effet lit.", "Pas de nettoyage : setState sur composant démonté, warning et fuite quand une réponse arrive tard.", "Ne gérer que le cas heureux : pas d'état d'erreur ni de chargement — l'app casse sur réseau lent ou API en panne.", "Transformer en effet ce qui est une valeur dérivée : un useEffect inutile qui recopie une donnée calculable (jour 93)."],
      checks: ["Les 3 états (chargement, erreur, données) sont rendus, un seul à la fois.", "Le tableau de dépendances contient tout ce que l'effet lit.", "Un nettoyage ignore les réponses tardives (pas de setState après démontage).", "Le statut HTTP est vérifié (r.ok) avant de parser.", "Le re-essai relance un fetch propre sans dupliquer la logique."],
      oral: "Définis l'effet comme une synchronisation avec l'extérieur, pas comme « du code après le rendu ». Puis déroule les 3 pièges (dépendances, nettoyage, over-use) et la règle des 3 états. Montre le drapeau \`annule\` et explique le bug qu'il évite (réponse tardive sur composant démonté) — ce détail prouve que tu as géré des cas réels, pas juste le tutoriel.",
    },
  },

  // ── Formulaires contrôlés et validation ──
  96: {
    lessons: ['react-hooks-effects.md'],
    theory: `**Modèle mental : dans un formulaire contrôlé, le state React est la SOURCE DE VÉRITÉ et le champ HTML n'en est que le reflet — la valeur affichée EST le state, pas l'inverse.** Un \`<input>\` non contrôlé garde sa propre valeur dans le DOM ; tu dois aller la lire. Un input CONTRÔLÉ tire sa valeur du state (\`value={x}\`) et renvoie chaque frappe au state (\`onChange\`). Le state pilote l'affichage ; le DOM ne fait qu'obéir. C'est le prolongement direct de « l'UI est une fonction du state » (jour 93) appliqué à la saisie.\n\nLe cycle d'un champ contrôlé :\n1. \`value={valeur}\` : le champ AFFICHE le state.\n2. \`onChange={e => setValeur(e.target.value)}\` : chaque frappe MET À JOUR le state.\n3. Le nouveau state re-rend le champ avec la nouvelle valeur. La boucle est fermée : à aucun moment le DOM n'est « en avance » sur le state.\n\nPourquoi cette contrainte est un avantage : parce que le state contient TOUJOURS la valeur actuelle, tu peux valider en direct, désactiver le bouton d'envoi tant que c'est invalide, formater à la volée, et pré-remplir un formulaire d'édition (jour 114) en initialisant le state. La validation devient une fonction PURE du state : \`erreurs = valider(form)\`, recalculée à chaque frappe — pas un contrôle éparpillé.\n\nLes deux règles qui font un formulaire professionnel :\n- **La validation affiche des erreurs UTILES au bon moment** : pas « champ invalide » mais « l'email doit contenir @ » ; idéalement après que l'utilisateur a quitté le champ (blur), pas à la première lettre tapée.\n- **La soumission empêche l'envoi invalide** : \`e.preventDefault()\` (sinon la page recharge), on valide, et on ne poste que si tout est bon ; pendant l'envoi, on désactive le bouton (état « en cours ») pour éviter le double-clic.\n\nLa bascule par rapport à un formulaire HTML classique : tu ne « lis pas le DOM à la soumission », tu as déjà tout dans le state à chaque instant.`,
    guided: `**Énoncé** : un formulaire d'ajout de livre contrôlé, validé en direct, qui poste vers l'API.\n\n**Exemple simple** : un seul champ contrôlé — \`<input value={titre} onChange={e => setTitre(e.target.value)} />\`. La valeur vient du state, chaque frappe y retourne. Tout formulaire n'est que la répétition de ce cycle par champ.\n\n**Raisonnement pas-à-pas** : un state par champ (ou un state-objet) → une validation dérivée pure → un bouton désactivé si invalide → une soumission qui preventDefault, revalide et poste.\n\n**Solution** :\n\`\`\`tsx\nfunction AjoutLivre({ onCreer }: { onCreer: (l: NouveauLivre) => Promise<void> }) {\n  const [form, setForm] = useState({ titre: '', auteur: '' });\n  const [envoi, setEnvoi] = useState(false);\n  const maj = (champ: string) => (e: React.ChangeEvent<HTMLInputElement>) =>\n    setForm((f) => ({ ...f, [champ]: e.target.value }));           // maj immuable\n\n  const erreurs = {                                                 // validation DÉRIVÉE (pure)\n    titre: form.titre.trim() ? '' : 'Le titre est requis',\n    auteur: form.auteur.trim() ? '' : \"L'auteur est requis\",\n  };\n  const valide = !erreurs.titre && !erreurs.auteur;\n\n  const soumettre = async (e: React.FormEvent) => {\n    e.preventDefault();                                             // pas de rechargement\n    if (!valide) return;\n    setEnvoi(true);\n    try { await onCreer(form); setForm({ titre: '', auteur: '' }); }\n    finally { setEnvoi(false); }                                    // bouton réactivé quoi qu'il arrive\n  };\n\n  return (\n    <form onSubmit={soumettre}>\n      <input value={form.titre} onChange={maj('titre')} />\n      {erreurs.titre && <span className=\"err\">{erreurs.titre}</span>}\n      <button disabled={!valide || envoi}>{envoi ? 'Envoi...' : 'Ajouter'}</button>\n    </form>\n  );\n}\n\`\`\`\n\n**Mini-quiz** : (a) Où vit la valeur d'un champ contrôlé ? (b) Pourquoi \`e.preventDefault()\` à la soumission ? (c) Pourquoi la validation est-elle dérivée et non stockée en state ?\n→ (a) dans le state React (le champ n'en est que le reflet) ; (b) sinon le navigateur recharge la page et perd le state ; (c) elle se recalcule à chaque frappe à partir du form — la stocker créerait une source de vérité redondante à resynchroniser (jour 93).\n\n**Variante** : n'affiche l'erreur d'un champ qu'après un \`onBlur\` (champ quitté) — l'utilisateur n'est pas harcelé dès la première lettre, une UX bien plus soignée.`,
    caseStudy: `Les formulaires sont partout (inscription, paiement, recherche, back-office) et sont la première source de frustration utilisateur quand ils sont mal faits : validation qui hurle dès la première lettre, bouton d'envoi cliquable deux fois qui crée un doublon, page qui recharge et perd la saisie. Un formulaire contrôlé bien construit — validation dérivée, bouton désactivé pendant l'envoi, erreurs utiles au bon moment — règle tout cela. C'est un livrable que les recruteurs demandent explicitement (« code-moi un formulaire d'inscription validé ») parce qu'il condense state, validation, async et UX.`,
    interview: `**« Quelle est la différence entre un champ contrôlé et non contrôlé, et lequel préfères-tu ? »** → Un champ CONTRÔLÉ tire sa valeur du state React (\`value={x}\`) et la met à jour à chaque frappe (\`onChange\`) : le state est la source de vérité, le DOM n'est qu'un reflet. Un champ NON contrôlé garde sa valeur dans le DOM et je dois aller la lire. Je préfère contrôlé par défaut : parce que le state contient toujours la valeur, je peux valider en direct, désactiver l'envoi tant que c'est invalide, formater à la volée et pré-remplir un formulaire d'édition. La validation devient une fonction pure du state, recalculée à chaque frappe.`,
    future: `- **Bankable** : le formulaire contrôlé validé est l'exercice technique front le plus demandé — il prouve d'un coup ta maîtrise du state, de la validation, de l'async et de l'UX.\n- Il alimente directement le CRUD de BiblioApp (ajout/édition, jour 114) et tout écran de saisie de tes futurs projets.\n- La discipline « state = source de vérité, validation dérivée, envoi protégé » se transfère à toute interface de saisie, y compris les prompts et configurations d'apps IA.`,
    solution: {
      logic: "Solution simple : un state par champ, value + onChange, un handler de soumission. Solution améliorée : maj immuable d'un state-objet, validation DÉRIVÉE pure (recalculée à chaque frappe), bouton désactivé si invalide ou pendant l'envoi, preventDefault à la soumission, gestion de l'état « en cours » avec réactivation garantie (finally), et affichage des erreurs au bon moment (onBlur). La preuve : impossible d'envoyer un formulaire invalide ou de double-cliquer.",
      pitfalls: ["Champ non contrôlé qu'on lit à la soumission : on perd la validation en direct et le pré-remplissage — contrôlé par défaut.", "Stocker les erreurs en state au lieu de les dériver : resynchronisation à faire à chaque frappe, source de bugs.", "Oublier e.preventDefault() : la page recharge et perd toute la saisie.", "Bouton d'envoi non désactivé pendant l'async : double-clic = doublon créé côté API."],
      checks: ["Chaque champ est contrôlé (value depuis le state, onChange vers le state).", "La validation est dérivée du state et recalculée à chaque frappe.", "Le bouton d'envoi est désactivé si invalide et pendant l'envoi.", "La soumission fait preventDefault et ne poste que si valide.", "Les erreurs s'affichent à un moment pertinent (onBlur), pas dès la première lettre."],
      oral: "Formule le principe : « le state est la source de vérité, le champ n'est que son reflet ». Déroule le cycle value → onChange → re-rendu, puis les protections de soumission (preventDefault, bouton désactivé, finally). Mentionne l'UX du onBlur — savoir QUAND montrer une erreur, pas seulement comment la calculer, distingue un front pro d'un front qui coche la case.",
    },
  },

  // ── Module api.ts et gestion d'erreur centralisée ──
  97: {
    lessons: ['error-handling.md', 'api-design-basics.md'],
    theory: `**Modèle mental : centraliser les appels réseau dans un seul module \`api.ts\`, c'est créer une FRONTIÈRE unique entre ton application et le réseau — un seul endroit qui connaît l'URL de base, les en-têtes, le parsing JSON et la traduction des erreurs HTTP en erreurs applicatives.** Sans cette frontière, chaque composant fait son \`fetch\` à sa façon : URL en dur répétée, statut HTTP parfois vérifié parfois non, erreurs gérées différemment partout. Le jour où l'API change (nouvelle URL de base, en-tête d'auth), tu modifies dix composants. Avec un module api, tu modifies UN fichier.\n\nCe que la centralisation apporte concrètement :\n1. **Une seule configuration** : l'URL de base, les en-têtes communs (Content-Type, plus tard le token d'auth) définis une fois.\n2. **Un parsing et une vérification uniformes** : chaque appel vérifie \`response.ok\`, parse le JSON, et transforme un statut d'erreur en une erreur TYPÉE que le front sait afficher — au lieu de laisser chaque composant redécouvrir que \`fetch\` ne rejette PAS sur un 404 (piège classique : \`fetch\` ne lève une erreur que sur panne réseau, pas sur un statut 4xx/5xx).\n3. **Des fonctions métier lisibles** : \`api.getLivres()\`, \`api.creerLivre(x)\` — les composants expriment l'intention, pas la mécanique HTTP. C'est le clean code (mois 2) appliqué au réseau : une couche qui NOMME les opérations.\n\nLa gestion d'erreur centralisée suit la même logique que côté serveur (mois 3) : **un seul endroit décide comment une erreur réseau devient un message pour l'utilisateur**. On distingue les familles — erreur réseau (pas de connexion), erreur client (4xx : mauvaise requête, non trouvé), erreur serveur (5xx : bug back-end) — et on les traduit en messages ou comportements adaptés (réessayer, corriger la saisie, signaler un bug). Le composant, lui, reçoit soit des données, soit une erreur claire ; il n'a plus à connaître les codes HTTP.\n\nLa règle du jour : **le réseau est un détail d'infrastructure qui vit dans une seule couche ; le reste de l'app parle à cette couche, pas au réseau**. C'est la version front du principe de séparation des responsabilités.`,
    guided: `**Énoncé** : un module \`api.ts\` typé et centralisé qui branche ton front sur LivreAPI.\n\n**Exemple simple** : le cœur du module est une fonction \`request\` qui vérifie \`res.ok\` et parse — \`if (!res.ok) throw new ApiError(res.status)\`. Tout le module se construit autour de ce garde-fou, car \`fetch\` ne rejette PAS sur un 404.\n\n**Raisonnement pas-à-pas** : une fonction \`request\` privée qui gère URL de base + en-têtes + \`res.ok\` + erreur typée → des fonctions métier typées qui l'utilisent → les composants n'appellent que ces fonctions métier.\n\n**Solution** :\n\`\`\`tsx\nconst BASE = import.meta.env.VITE_API_URL ?? 'http://localhost:3000';\n\nexport class ApiError extends Error {\n  constructor(public status: number, message: string) { super(message); }\n}\n\nasync function request<T>(chemin: string, options?: RequestInit): Promise<T> {\n  let res: Response;\n  try { res = await fetch(BASE + chemin, {\n    headers: { 'Content-Type': 'application/json' }, ...options });\n  } catch { throw new ApiError(0, 'Réseau indisponible'); }        // panne réseau\n  if (!res.ok) throw new ApiError(res.status,                       // fetch NE rejette PAS sur 4xx/5xx\n    res.status === 404 ? 'Ressource introuvable' : 'Erreur serveur');\n  return res.status === 204 ? (undefined as T) : res.json();\n}\n\nexport const api = {                                               // fonctions métier NOMMÉES\n  getLivres: () => request<Livre[]>('/livres'),\n  getLivre: (id: number) => request<Livre>(\`/livres/\${id}\`),\n  creerLivre: (l: NouveauLivre) =>\n    request<Livre>('/livres', { method: 'POST', body: JSON.stringify(l) }),\n};\n\`\`\`\n\n**Mini-quiz** : (a) \`fetch\` lève-t-il une erreur sur un statut 404 ? (b) Pourquoi centraliser l'URL de base ? (c) Que gagne un composant à appeler \`api.getLivres()\` plutôt qu'un \`fetch\` direct ?\n→ (a) non — il ne rejette que sur panne réseau ; il faut vérifier \`res.ok\` soi-même ; (b) un seul point à changer si l'API bouge, pas dix composants ; (c) il exprime l'intention, ignore la mécanique HTTP, et bénéficie du parsing/erreur uniformes.\n\n**Variante** : ajoute une fonction \`majLivre\` (PUT) et \`supprimerLivre\` (DELETE) — tu obtiens le client complet dont BiblioApp aura besoin (jour 114), déjà typé et robuste.`,
    caseStudy: `Le module api centralisé est un standard de toute base de code front sérieuse : sans lui, une équipe se retrouve avec des \`fetch\` dispersés, des URL en dur, et le fameux bug « le 404 n'est pas géré » (parce que \`fetch\` ne rejette pas dessus). Le jour où l'API passe en HTTPS, change de domaine ou ajoute une authentification, la couche api absorbe le changement en un seul fichier. Les revues de code front rejettent systématiquement un \`fetch\` en dur dans un composant : c'est le signe d'un couplage au réseau qu'on sait coûteux. Savoir construire cette couche prouve que tu penses architecture, pas juste « faire marcher l'appel ».`,
    interview: `**« Comment organises-tu les appels réseau dans une app front ? »** → Je centralise tout dans une couche \`api.ts\` : une frontière unique entre l'app et le réseau. Elle connaît l'URL de base et les en-têtes, vérifie \`response.ok\` (car \`fetch\` ne rejette PAS sur un 404 — seulement sur panne réseau), parse le JSON, et traduit les statuts d'erreur en erreurs typées que le front sait afficher. Les composants n'appellent que des fonctions métier nommées (\`api.getLivres()\`), sans connaître les codes HTTP. Bénéfice : si l'API change (URL, auth), je modifie un seul fichier ; et la gestion d'erreur est uniforme au lieu d'être réinventée dans chaque composant.`,
    future: `- **Bankable** : une couche api propre est un marqueur d'architecture front — les revues de code rejettent un \`fetch\` en dur, et savoir centraliser prouve que tu penses au-delà du « ça marche ».\n- Ce module devient le client réseau de BiblioApp (CRUD complet, jours 113-118) et le patron de toute app branchée sur un back-end, dont DocSense.\n- La séparation « le réseau vit dans une seule couche » se transfère à tout système : c'est le même principe que la frontière de confiance côté agents IA (une seule porte vers l'extérieur).`,
    solution: {
      logic: "Solution simple : un objet api avec une fonction par endpoint qui fait fetch + json. Solution améliorée : une fonction request privée qui centralise URL de base, en-têtes, vérification de res.ok (fetch ne rejette pas sur 4xx/5xx), gestion de la panne réseau, du 204 sans corps, et une erreur TYPÉE (ApiError avec status) ; par-dessus, des fonctions métier nommées et typées. La preuve : changer l'URL de base ou ajouter un en-tête d'auth ne touche qu'un seul fichier.",
      pitfalls: ["Croire que fetch rejette sur un 404 : il ne rejette que sur panne réseau — vérifier res.ok est obligatoire.", "URL de base en dur répétée dans chaque composant : un changement d'API impose de modifier dix fichiers.", "Erreurs non typées / gérées différemment partout : le front ne peut pas réagir de façon cohérente (réessayer vs corriger la saisie).", "Laisser des fetch directs dans les composants : couplage au réseau que les revues de code rejettent."],
      checks: ["Tous les appels passent par la couche api (aucun fetch dans les composants).", "res.ok est vérifié et un 4xx/5xx devient une erreur typée.", "L'URL de base et les en-têtes sont définis une seule fois.", "La panne réseau et le 204 (sans corps) sont gérés.", "Les fonctions métier sont nommées et typées (api.getLivres(), api.creerLivre(...))."],
      oral: "Explique la frontière : « le réseau est un détail qui vit dans une seule couche ; le reste de l'app parle à cette couche ». Insiste sur le piège du fetch qui ne rejette pas sur 404 — c'est LE détail qui montre que tu as géré de vrais appels. Conclus sur le bénéfice architecture : un changement d'API = un seul fichier modifié.",
    },
  },

  // ── Revue semaine 14 : React fondamentaux ──
  98: {
    reviewSynthese: `La semaine 14 a posé les fondations de React : penser en composants purs et props typées (jour 92), gérer l'état local sans le muter (93), afficher conditionnellement et mapper des listes avec des keys stables (94), consommer une API avec ses 3 états et un nettoyage d'effet (95), maîtriser les formulaires contrôlés validés (96), et centraliser le réseau dans une couche api robuste (97). Le fil rouge : **l'UI est une fonction pure du state ; on ne manipule jamais le DOM, on change le state et React recalcule l'écran**. Les deux disciplines qui reviennent partout : l'IMMUTABILITÉ (remplacer, pas muter) et la MACHINE À ÉTATS (chargement/erreur/données, toujours les trois). Tu as maintenant de quoi construire une vraie app front branchée sur ton API — ce que fera BiblioApp (Projet 3).`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Décomposer une maquette en composants purs à props typées | |\n| Mettre à jour le state immuablement (spread, map, filter) et dériver le calculable | |\n| Rendre des listes avec des keys stables et traiter les états vide/chargement | |\n| Charger des données avec les 3 états, les bonnes dépendances et un nettoyage | |\n| Construire un formulaire contrôlé validé qui protège la soumission | |\n| Centraliser les appels réseau dans une couche api typée (res.ok, erreurs) | |\n\n**Total /18** : ≥ 15 = solide, passe au routing/état partagé · 11-14 = revoir les items ≤ 1 · < 11 = rejouer les jours faibles avant d'empiler routing et Context.`,
    remediation: `- **State muté** (l'UI ne bouge pas après un changement) : rejoue le jour 93 — React compare les RÉFÉRENCES ; spread pour ajouter, map+spread pour modifier, filter pour retirer. C'est la cause n°1 des bugs front des débutants.\n- **Effet mal maîtrisé** (dépendances oubliées, pas de nettoyage) : reprends le jour 95 — dépendances = tout ce que l'effet lit ; nettoyage = ignorer les réponses tardives. Un fetch a toujours 3 états.\n- **key = index** : reviens au jour 94 — une key doit être une identité stable issue des données, sinon React apparie les mauvais éléments.\n- **fetch en dur dans les composants** : centralise (jour 97) — vérifie res.ok (fetch ne rejette pas sur 404), types d'erreur, URL de base unique. Sans cette couche, BiblioApp deviendra vite ingérable.\n- **Formulaire non contrôlé ou envoi non protégé** : rejoue le jour 96 — value depuis le state, validation dérivée, bouton désactivé pendant l'envoi.`,
    interview: `1. **« Pourquoi ne mute-t-on jamais le state en React ? »** Attendu : React re-rend en comparant les références ; muter ne change pas la référence donc ne re-rend pas ; l'immutabilité rend le state prévisible.\n2. **« À quoi sert le tableau de dépendances de useEffect ? »** Attendu : dire quand l'effet se ré-exécute ; une dépendance oubliée = stale closure (valeur périmée) ; sans nettoyage, setState sur composant démonté.\n3. **« Contrôlé vs non contrôlé : quelle différence ? »** Attendu : contrôlé = valeur dans le state (source de vérité), permet validation directe et pré-remplissage ; non contrôlé = valeur dans le DOM.\n4. **« Pourquoi une couche api centralisée ? »** Attendu : une frontière unique (URL de base, en-têtes, res.ok, erreurs typées) ; fetch ne rejette pas sur 4xx/5xx ; un changement d'API ne touche qu'un fichier.\n5. **« Que sont les 3 états d'un chargement de données ? »** Attendu : chargement, erreur, données — toujours les trois, un seul rendu à la fois, jamais 'données' sans données.`,
  },

  // ── Routing et navigation ──
  99: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : l'URL est un ÉTAT — partageable, bookmarkable, et piloté par le bouton retour du navigateur. Le routing mappe cet état-URL à l'arbre de composants affiché.** Sans routing, ton app est un seul écran dont l'état vit uniquement en mémoire : impossible de partager un lien vers « la fiche du livre 42 », le bouton retour quitte le site, et rafraîchir remet tout à zéro. Le routing règle cela en faisant de l'URL la source de vérité de « quelle vue est affichée ».\n\nLes notions clés d'un routeur (React Router) :\n1. **Les routes** : une correspondance entre un motif d'URL et un composant. \`/livres\` → \`<Liste/>\`, \`/livres/:id\` → \`<Detail/>\`, \`*\` → \`<PageIntrouvable/>\`. Le routeur regarde l'URL et rend le composant correspondant.\n2. **Les paramètres d'URL** : \`:id\` est une variable lue par le composant (\`useParams\`). L'URL \`/livres/42\` transporte l'identifiant : la fiche sait quel livre charger à partir de l'URL, pas d'un state interne. L'URL EST le paramètre.\n3. **La navigation** : un \`<Link to=\"/livres/42\">\` (pas un \`<a href>\` qui rechargerait la page et perdrait le state) ; une navigation programmatique (\`useNavigate\`) après une action (« créer » → aller vers la fiche créée).\n4. **Le 404** : une route attrape-tout (\`*\`) pour les URL inconnues — une app pro ne montre jamais un écran blanc sur une mauvaise URL.\n\nLa règle du jour : **une vue navigable a une URL, et cette URL suffit à la reconstruire**. Le test : si je copie l'URL d'une fiche et l'ouvre dans un onglet neuf, je dois retomber sur la même fiche. Si oui, ton état de navigation est bien dans l'URL ; sinon, il est piégé en mémoire et l'app n'est pas vraiment navigable. C'est le même principe que côté API (mois 3) : une ressource a une adresse stable.`,
    guided: `**Énoncé** : ajouter le routing (liste / détail / édition + 404) à ton app, avec des URL propres.\n\n**Exemple simple** : une route paramétrée et sa lecture — \`<Route path=\"/livres/:id\" element={<Detail/>} />\`, et dans Detail : \`const { id } = useParams()\`. L'URL transporte l'identifiant ; le composant le lit. C'est tout le principe.\n\n**Raisonnement pas-à-pas** : déclarer les routes (dont le 404) → naviguer avec Link → lire les paramètres avec useParams → charger la ressource à partir de l'URL.\n\n**Solution** :\n\`\`\`tsx\nfunction App() {\n  return (\n    <BrowserRouter>\n      <nav><Link to=\"/livres\">Livres</Link></nav>\n      <Routes>\n        <Route path=\"/livres\" element={<Liste />} />\n        <Route path=\"/livres/:id\" element={<Detail />} />\n        <Route path=\"/livres/:id/edit\" element={<Edition />} />\n        <Route path=\"*\" element={<PageIntrouvable />} />   {/* 404 : jamais d'écran blanc */}\n      </Routes>\n    </BrowserRouter>\n  );\n}\n\nfunction Detail() {\n  const { id } = useParams();                             // l'URL transporte l'id\n  const [livre, setLivre] = useState<Livre | null>(null);\n  useEffect(() => { api.getLivre(Number(id)).then(setLivre); }, [id]); // recharge si l'id change\n  if (!livre) return <Spinner />;\n  return <><h1>{livre.titre}</h1><Link to={\`/livres/\${id}/edit\`}>Éditer</Link></>;\n}\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi \`<Link>\` et pas \`<a href>\` ? (b) Où le composant Detail lit-il l'id du livre ? (c) Que se passe-t-il sur une URL inconnue sans route \`*\` ?\n→ (a) \`<a>\` recharge la page et perd tout le state ; \`<Link>\` change l'URL côté client sans rechargement ; (b) dans l'URL, via \`useParams\` — pas dans un state interne ; (c) écran blanc — d'où la route attrape-tout qui affiche un 404 propre.\n\n**Variante** : ajoute \`useNavigate\` pour rediriger vers la fiche après une création (\`navigate('/livres/' + nouvel.id)\`) — la navigation programmatique après une action, indispensable au CRUD (jour 114).`,
    caseStudy: `Le routing est ce qui transforme une « page unique » en application : des URL partageables (un collègue vous envoie le lien exact d'une fiche), un bouton retour qui marche, un rafraîchissement qui retombe au bon endroit. Les apps qui gardent l'état de navigation en mémoire au lieu de l'URL frustrent les utilisateurs (impossible de partager un lien, retour qui quitte le site) et posent des problèmes de référencement. Savoir mettre l'état navigable dans l'URL — et gérer le 404 — est un attendu de base de toute SPA professionnelle, et le principe « une ressource, une URL stable » fait écho au design d'API que tu connais déjà.`,
    interview: `**« Pourquoi mettre l'état de navigation dans l'URL plutôt qu'en mémoire ? »** → Parce que l'URL est un état PARTAGEABLE et bookmarkable, piloté par le bouton retour du navigateur. Si l'état de navigation vit seulement en mémoire, on ne peut pas partager un lien vers une vue précise, le bouton retour quitte le site, et rafraîchir remet tout à zéro. Avec le routing, l'URL est la source de vérité de la vue affichée : \`/livres/42\` suffit à reconstruire la fiche du livre 42 (lue via les paramètres d'URL). Le test : copier l'URL dans un onglet neuf doit retomber sur la même vue. J'utilise \`<Link>\` (pas \`<a>\` qui recharge) et une route attrape-tout pour un 404 propre.`,
    future: `- **Bankable** : mettre l'état navigable dans l'URL (liens partageables, retour, 404) est un attendu de toute SPA — un manque immédiatement visible en test technique.\n- Le routing structure la navigation de BiblioApp (liste / détail / édition, jours 113-118) et de toute app multi-vues, dont l'interface de DocSense.\n- Le principe « une vue = une URL reconstructible » prolonge le design d'API REST (une ressource, une adresse stable) — une cohérence que les recruteurs apprécient d'entendre articulée.`,
    solution: {
      logic: "Solution simple : déclarer les routes et naviguer avec Link. Solution améliorée : mettre l'état navigable dans l'URL (paramètres lus via useParams, ressource chargée à partir de l'URL avec l'id en dépendance d'effet), gérer le 404 par une route attrape-tout, utiliser Link (pas <a>) et la navigation programmatique (useNavigate) après une action. La preuve : copier l'URL d'une fiche et l'ouvrir dans un onglet neuf reconstruit exactement la même vue.",
      pitfalls: ["<a href> au lieu de <Link> : recharge la page et perd tout le state de l'app.", "Garder l'id de la ressource dans un state interne au lieu de l'URL : la vue n'est ni partageable ni rechargeable.", "Oublier la route attrape-tout : une URL inconnue affiche un écran blanc au lieu d'un 404 propre.", "Ne pas mettre le paramètre d'URL dans les dépendances de l'effet : changer d'id ne recharge pas la bonne ressource."],
      checks: ["Chaque vue navigable a sa propre URL.", "Les paramètres d'URL sont lus via useParams et pilotent le chargement.", "La navigation utilise <Link> / useNavigate, pas <a href>.", "Une route attrape-tout affiche un 404 propre.", "Ouvrir l'URL d'une fiche dans un onglet neuf reconstruit la même vue."],
      oral: "Pose le principe : « l'URL est un état partageable ; le routing la mappe à la vue ». Donne le test décisif — copier l'URL dans un onglet neuf doit retomber sur la même fiche. Mentionne le piège du <a> qui recharge et la route 404 attrape-tout. Relie au design d'API (une ressource, une adresse) pour montrer une cohérence d'architecture.",
    },
  },

  // ── Lever l'état et le partager ──
  100: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : l'état vit au PLUS PETIT ANCÊTRE COMMUN des composants qui en ont besoin — « lever l'état » (lifting state up) n'est pas une contrainte arbitraire, c'est la conséquence directe du flux de données unidirectionnel.** En React, les données descendent (props) et les événements remontent (callbacks). Si deux composants frères doivent partager une donnée (un filtre qui affecte une liste, un panier affiché dans deux endroits), cette donnée ne peut pas vivre dans l'un des deux — elle doit vivre dans leur PARENT commun, qui la passe aux deux en props et reçoit leurs mises à jour via des callbacks.\n\nLe raisonnement pour placer un état :\n1. **Qui LIT cette donnée ?** Liste les composants qui l'affichent ou en dépendent.\n2. **Qui la MODIFIE ?** Liste les composants qui la changent.\n3. **L'état va au plus petit ancêtre commun de tous ceux-là** — assez haut pour que tout le monde y accède, pas plus haut (sinon on remonte inutilement et on complique).\n\nLe motif « données vers le bas, événements vers le haut » : le parent détient l'état et une fonction pour le modifier ; il passe la valeur en prop à l'enfant qui l'affiche, et la fonction en prop à l'enfant qui la déclenche. L'enfant ne « possède » pas la donnée ; il la reçoit et signale un événement. C'est ce qui rend le flux prévisible : une seule source de vérité par donnée.\n\nLe piège opposé — le **props drilling** : passer une prop à travers cinq niveaux de composants qui ne l'utilisent pas, juste pour la faire descendre. C'est le SIGNAL qu'un état est mal placé (trop haut, ou qu'il faudrait Context, jour 101). La règle d'équilibre : **l'état au plus bas possible qui couvre tous ses lecteurs** — ni trop local (impossible à partager), ni trop global (props drilling, re-rendus inutiles).\n\nLa bascule mentale : tu ne cherches pas « où c'est pratique de mettre ce state », tu DÉDUIS sa place de qui le lit et le modifie. Bien placer l'état est ce qui garde une app compréhensible quand elle grossit — exactement ce que teste le cadrage de BiblioApp (jour 104).`,
    guided: `**Énoncé** : refactorer une app pour remonter l'état au bon niveau et éliminer le props drilling excessif.\n\n**Exemple simple** : deux frères partagent un filtre → il monte dans le parent. \`function Parent(){ const [q,setQ]=useState(''); return <><Barre q={q} onChange={setQ}/><Liste q={q}/></>; }\`. La donnée partagée vit chez l'ancêtre commun ; les deux enfants la reçoivent.\n\n**Raisonnement pas-à-pas** : repérer une donnée partagée coincée dans un enfant → identifier ses lecteurs et modificateurs → la remonter à leur ancêtre commun → passer valeur (bas) et callback (haut).\n\n**Solution** :\n\`\`\`tsx\n// AVANT : le filtre vit dans <Barre>, mais <Liste> en a besoin → impossible à partager.\n// APRÈS : l'état monte dans le parent (leur plus petit ancêtre commun).\nfunction Catalogue({ livres }: { livres: Livre[] }) {\n  const [q, setQ] = useState('');                         // source de vérité UNIQUE\n  const filtres = livres.filter((l) => l.titre.includes(q)); // dérivé (jour 93)\n  return (\n    <>\n      <BarreRecherche q={q} onChange={setQ} />             {/* callback : événement vers le HAUT */}\n      <Liste livres={filtres} />                           {/* prop : donnée vers le BAS */}\n      <Compteur total={filtres.length} />                  {/* un autre lecteur du même état */}\n    </>\n  );\n}\n\`\`\`\n\n**Mini-quiz** : (a) Où placer un état lu par deux composants frères ? (b) Que signifie du props drilling sur cinq niveaux ? (c) Quel est le sens du flux « données / événements » ?\n→ (a) dans leur plus petit ancêtre commun ; (b) que l'état est mal placé ou qu'il faut Context (jour 101) ; (c) les données descendent en props, les événements remontent en callbacks.\n\n**Variante** : documente l'arbre de composants avec, à côté de chaque nœud, l'état qu'il détient — ce schéma est exactement le livrable de cadrage de BiblioApp (jour 104).`,
    caseStudy: `Le mauvais placement de l'état est la cause profonde de la plupart des refactors front douloureux : un state trop local qu'il faut soudain partager (réécriture), ou un state trop global qui provoque du props drilling et des re-rendus en cascade. Les développeurs expérimentés placent l'état par déduction (qui le lit, qui le modifie) dès la conception, ce qui garde l'app extensible. En entretien, « où placerais-tu cet état ? » est une question d'architecture front courante : la bonne réponse (plus petit ancêtre commun, ni plus ni moins) montre que tu raisonnes sur le flux de données, pas au hasard.`,
    interview: `**« Comment décides-tu où placer un état en React ? »** → Je le déduis de deux questions : qui LIT cette donnée, et qui la MODIFIE. L'état va au plus petit ancêtre commun de tous ces composants — assez haut pour que tout le monde y accède, pas plus haut. Le flux est « données vers le bas (props), événements vers le haut (callbacks) » : le parent détient la source de vérité unique et la partage. Si je me retrouve à faire du props drilling sur plusieurs niveaux, c'est le signal que l'état est mal placé ou qu'il faut Context. L'objectif : une seule source de vérité par donnée, placée au plus bas qui couvre tous ses lecteurs.`,
    future: `- **Bankable** : placer l'état par déduction (plus petit ancêtre commun) est une compétence d'architecture front directement évaluée — « où mettrais-tu cet état ? » est une question d'entretien fréquente.\n- Ce raisonnement structure le plan d'état de BiblioApp (cadrage jour 104, CRUD jours 113-118) et prépare la décision Context vs props (jour 101).\n- « Une seule source de vérité, placée au bon niveau » est un principe qui dépasse React : il gouverne toute gestion d'état, du front aux pipelines de données.`,
    solution: {
      logic: "Solution simple : remonter au parent un état partagé par deux enfants, passé en props. Solution améliorée : déduire la place de l'état de ses lecteurs ET modificateurs (plus petit ancêtre commun), appliquer strictement « données vers le bas, événements vers le haut », dériver ce qui se calcule (la liste filtrée), et éviter à la fois le state trop local (impossible à partager) et trop global (props drilling). La preuve : chaque donnée a une source de vérité unique et l'arbre de composants est documentable.",
      pitfalls: ["État trop local coincé dans un enfant alors qu'un frère en a besoin : partage impossible sans refactor.", "État remonté trop haut : props drilling à travers des composants qui ne l'utilisent pas, re-rendus inutiles.", "Dupliquer la donnée dans plusieurs composants : plusieurs sources de vérité à resynchroniser.", "Confondre 'lever l'état' et 'tout mettre global' : l'objectif est le plus BAS possible qui couvre les lecteurs, pas le plus haut."],
      checks: ["Chaque donnée partagée vit au plus petit ancêtre commun de ses lecteurs.", "Le flux respecte 'données vers le bas (props), événements vers le haut (callbacks)'.", "Aucune donnée n'a plusieurs sources de vérité.", "Il n'y a pas de props drilling excessif (signal d'un mauvais placement).", "L'arbre de composants et l'état détenu par chaque nœud sont documentés."],
      oral: "Explique la déduction : « je regarde qui lit et qui modifie, l'état va à leur plus petit ancêtre commun ». Donne le flux (données bas, événements haut) et le signal d'alerte (props drilling = état mal placé ou besoin de Context). Montre un avant/après où remonter un filtre débloque le partage — c'est la démonstration concrète du raisonnement d'architecture front.",
    },
  },

  // ── Context pour l'état global léger ──
  101: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : Context est un CANAL DE DIFFUSION pour un état GLOBAL et STABLE (le thème, l'utilisateur connecté, la langue) — pas un remplacement des props, et surtout pas un endroit pour un état qui change souvent.** Tu sais lever l'état (jour 100) ; mais quand une donnée est lue par BEAUCOUP de composants à des profondeurs variées (le thème de toute l'app, l'utilisateur connecté partout), la faire descendre en props traverse dix niveaux qui ne l'utilisent pas : le props drilling. Context résout précisément ce cas — il rend une valeur disponible à tout un sous-arbre SANS la passer manuellement de parent en enfant.\n\nLes trois pièces de Context :\n1. **Le \`createContext\`** : déclare le canal et sa valeur par défaut.\n2. **Le \`Provider\`** : enveloppe un sous-arbre et y injecte la valeur courante (souvent un state + son setter).\n3. **Le \`useContext\`** : n'importe quel composant du sous-arbre lit la valeur, quelle que soit sa profondeur, sans props intermédiaires.\n\nMais Context a un COÛT qu'il faut connaître : **quand la valeur du Provider change, TOUS les composants qui la consomment se re-rendent**. C'est pourquoi Context convient à un état qui change RAREMENT (thème, utilisateur) et devient un piège pour un état qui change souvent (la valeur d'un champ, la position de la souris) — il déclencherait des re-rendus massifs (jour 102). Context n'est pas non plus un gestionnaire d'état : c'est un mécanisme de TRANSPORT.\n\nLa règle qui évite la sur-ingénierie : **Context quand une donnée est vraiment globale et stable ET que le props drilling devient douloureux — sinon, des props suffisent**. Le réflexe débutant « tout mettre en Context pour éviter les props » est une erreur : les props sont explicites et locales (faciles à suivre), Context est implicite et global (plus difficile à tracer). On paie Context en lisibilité et en re-rendus ; on ne l'utilise que quand le bénéfice (éliminer un vrai props drilling) dépasse ce coût.`,
    guided: `**Énoncé** : un thème clair/sombre partagé via Context, avec la justification du choix.\n\n**Exemple simple** : le hook de consommation typé — \`const { theme, toggle } = useContext(ThemeContext)\`. N'importe quel composant du sous-arbre y accède sans qu'aucun parent n'ait passé de prop. C'est exactement ce que Context fait gagner.\n\n**Raisonnement pas-à-pas** : créer le contexte → un Provider qui détient le state → des consommateurs qui lisent, quelle que soit leur profondeur → vérifier que la donnée est bien globale ET stable.\n\n**Solution** :\n\`\`\`tsx\ntype ThemeCtx = { theme: 'clair' | 'sombre'; toggle: () => void };\nconst ThemeContext = createContext<ThemeCtx | null>(null);\n\nfunction ThemeProvider({ children }: { children: React.ReactNode }) {\n  const [theme, setTheme] = useState<'clair' | 'sombre'>('clair');\n  const toggle = () => setTheme((t) => (t === 'clair' ? 'sombre' : 'clair'));\n  return <ThemeContext.Provider value={{ theme, toggle }}>{children}</ThemeContext.Provider>;\n}\n\nfunction useTheme() {                                    // hook custom : accès sûr\n  const ctx = useContext(ThemeContext);\n  if (!ctx) throw new Error('useTheme doit être utilisé dans un ThemeProvider');\n  return ctx;\n}\n// N'importe où DANS le Provider, à n'importe quelle profondeur :\nfunction BoutonTheme() { const { theme, toggle } = useTheme(); return <button onClick={toggle}>{theme}</button>; }\n\`\`\`\n\n**Mini-quiz** : (a) Que se passe-t-il pour les consommateurs quand la valeur du Provider change ? (b) Context convient-il à la valeur d'un champ de saisie ? (c) Context remplace-t-il les props ?\n→ (a) ils se re-rendent tous — d'où Context pour un état STABLE ; (b) non, ça change trop souvent, re-rendus massifs — garde-le local ; (c) non, c'est un transport pour un état global ; les props restent le défaut pour le local.\n\n**Variante** : ajoute un garde-fou (le hook \`useTheme\` qui lève une erreur hors Provider) — il transforme une erreur silencieuse (valeur par défaut inattendue) en message clair, un réflexe de robustesse.`,
    caseStudy: `Context est utilisé dans presque toutes les apps React pour un petit nombre de valeurs vraiment globales : thème, utilisateur authentifié, langue, notifications. Le problème récurrent en entreprise est l'ABUS de Context — des équipes y mettent des états qui changent souvent, provoquant des re-rendus en cascade et des bugs de performance difficiles à diagnostiquer (jour 102). Les revues de code front vérifient qu'un Context est justifié (donnée globale + stable) et non un raccourci pour éviter de réfléchir au placement de l'état. Savoir QUAND ne PAS utiliser Context est aussi valorisé que savoir l'utiliser — c'est le signe d'un jugement d'architecture mûr.`,
    interview: `**« Quand utilises-tu Context, et quels sont ses pièges ? »** → J'utilise Context pour un état vraiment GLOBAL et STABLE (thème, utilisateur connecté, langue) quand le passer en props provoquerait du props drilling sur plusieurs niveaux. Context est un mécanisme de TRANSPORT, pas un gestionnaire d'état. Son piège principal : quand la valeur du Provider change, TOUS ses consommateurs se re-rendent — donc il est inadapté à un état qui change souvent (valeur de champ, position souris), qui déclencherait des re-rendus massifs. Le réflexe « tout mettre en Context » est une erreur : les props sont explicites et locales, Context est implicite et global. Je ne paie ce coût que quand il élimine un vrai props drilling.`,
    future: `- **Bankable** : savoir justifier Context (donnée globale + stable) ET savoir quand l'éviter est un signal de jugement d'architecture front — les revues de code sanctionnent l'abus de Context.\n- Context porte le thème et l'utilisateur de BiblioApp et de tes futures apps ; le choix « Context vs local » est l'ADR n°3 de BiblioApp (jour 118).\n- Comprendre le coût de re-rendu de Context prépare directement l'optimisation des performances (jour 102) et le raisonnement sur la propagation d'état dans toute app.`,
    solution: {
      logic: "Solution simple : createContext + Provider + useContext pour partager le thème. Solution améliorée : encapsuler la consommation dans un hook custom (useTheme) qui lève une erreur hors Provider, garder dans le Context UNIQUEMENT un état global et stable (pas un état changeant), et documenter la justification (le props drilling évité vs le coût de re-rendu). La preuve de jugement : une note explicite « pourquoi Context ici et pas des props ».",
      pitfalls: ["Mettre en Context un état qui change souvent : tous les consommateurs se re-rendent à chaque changement — re-rendus massifs (jour 102).", "Utiliser Context pour éviter de réfléchir au placement de l'état : les props explicites et locales sont souvent plus lisibles.", "Oublier le garde-fou hors Provider : useContext renvoie la valeur par défaut silencieusement, bug difficile à traquer.", "Confondre Context et gestion d'état : Context transporte, il ne gère pas la logique — celle-ci reste dans un state/reducer."],
      checks: ["Le Context ne contient qu'un état global ET stable (thème, utilisateur).", "La consommation passe par un hook custom qui échoue proprement hors Provider.", "Aucun état à changement fréquent n'est mis en Context.", "Une note justifie le choix Context vs props.", "Les composants profonds accèdent à la valeur sans props intermédiaires."],
      oral: "Pose la règle : « Context = transport d'un état global et stable ; son coût = re-rendu de tous les consommateurs quand la valeur change ». Insiste sur le jugement : « je sais aussi quand NE PAS l'utiliser — un état changeant reste local ». Cite le garde-fou du hook custom. Savoir refuser Context là où des props suffisent impressionne plus que de l'utiliser partout.",
    },
  },

  // ── Performance React : re-renders ──
  102: {
    lessons: ['react-hooks-effects.md', 'caching-performance.md'],
    theory: `**Modèle mental : React re-rend un composant quand son state ou ses props changent, ET re-rend ses enfants par défaut — la performance, ce n'est pas empêcher TOUS les re-rendus, c'est éliminer les re-rendus INUTILES, et seulement après les avoir MESURÉS.** Un re-rendu n'est pas mauvais en soi : recalculer du JSX est rapide. Le problème survient quand un composant coûteux (grande liste, calcul lourd) se re-rend alors que rien de ce qui l'affecte n'a changé. L'optimisation prématurée (mémoïser partout « au cas où ») ajoute de la complexité pour un gain nul, voire négatif.\n\nLa démarche correcte, dans l'ordre :\n1. **MESURER d'abord** : le React DevTools Profiler montre quels composants se re-rendent et combien de temps ils prennent. Sans mesure, tu optimises à l'aveugle. « Mesurer avant d'optimiser » est la règle d'or, identique côté back-end.\n2. **Comprendre la cause** : un re-rendu inutile vient souvent d'une nouvelle RÉFÉRENCE créée à chaque rendu du parent — un objet, un tableau ou une fonction recréés en ligne (\`onClick={() => ...}\`, \`style={{...}}\`) qui changent l'identité des props de l'enfant à chaque fois.\n3. **Appliquer le bon outil** : \`React.memo\` (ne re-rend l'enfant que si ses props changent réellement), \`useMemo\` (mémoïse une VALEUR calculée coûteuse), \`useCallback\` (mémoïse une FONCTION pour garder son identité stable entre rendus). Ces trois outils reposent sur la stabilité des références — la même notion que l'immutabilité (jour 93).\n\nLa règle qui évite le sur-usage : **on mémoïse un composant/valeur/fonction quand un profil PROUVE un re-rendu coûteux inutile — pas par principe**. Mémoïser a un coût (mémoire, comparaison des props, code plus complexe) qui ne se justifie que face à un vrai problème mesuré. Un \`useMemo\` sur un calcul trivial est du bruit qui dégrade la lisibilité sans rien gagner. La compétence n'est pas « connaître memo/useMemo/useCallback », c'est savoir QUAND ils valent leur coût — ce qui suppose d'avoir mesuré.\n\n---\n\n**Au-delà des re-rendus : le POIDS livré au navigateur.** Les re-rendus sont le coût à l'EXÉCUTION ; l'autre moitié de la performance front est le coût au CHARGEMENT — la taille du JavaScript envoyé. Le *bundle size* (poids du paquet JS) est décisif : chaque kilo-octet doit être téléchargé, parsé puis exécuté avant que la page soit interactive, surtout sur mobile et réseau lent. On le réduit par le *code splitting* (découper le bundle en morceaux chargés à la demande) et le *lazy loading* (ne charger un composant ou une route que lorsqu'il devient nécessaire — \`React.lazy\` + \`Suspense\` pour une vue rarement visitée). Contre-exemple fréquent : tout importer en haut de l'app « pour simplifier », comme une grosse librairie de graphes chargée sur la page d'accueil qui ne l'affiche jamais. Comme côté back-end, on MESURE (un *bundle analyzer* montre ce qui pèse ; Lighthouse mesure le temps d'interactivité) et on se fixe un BUDGET DE PERFORMANCE (« bundle initial < 200 Ko ») dont tout dépassement est une RÉGRESSION à traiter — vérifiée en CI, pas découverte par l'utilisateur. Le fil rouge de la performance, front comme back : établir une baseline, mesurer, corriger la vraie cause, protéger le gain par un budget.`,
    guided: `**Énoncé** : identifier un re-rendu coûteux au Profiler et le corriger avec le bon outil.\n\n**Exemple simple** : la cause n°1 d'un re-rendu inutile — une fonction recréée en ligne — \`<Enfant onClic={() => faire(id)} />\`. À chaque rendu du parent, \`onClic\` a une nouvelle identité, donc \`React.memo(Enfant)\` ne sert à rien tant qu'on ne stabilise pas la fonction avec \`useCallback\`.\n\n**Raisonnement pas-à-pas** : profiler → repérer le composant coûteux qui se re-rend sans raison → identifier la référence instable → stabiliser (memo + useCallback/useMemo) → RE-profiler pour prouver le gain.\n\n**Solution** :\n\`\`\`tsx\nconst LigneLourde = React.memo(function LigneLourde(\n  { livre, onSelect }: { livre: Livre; onSelect: (id: number) => void }) {\n  return <li onClick={() => onSelect(livre.id)}>{livre.titre}</li>;\n}); // ne re-rend QUE si livre ou onSelect changent d'identité\n\nfunction Liste({ livres }: { livres: Livre[] }) {\n  const [sel, setSel] = useState<number | null>(null);\n  const onSelect = useCallback((id: number) => setSel(id), []); // identité STABLE\n  const tri = useMemo(() => [...livres].sort((a, b) =>              // calcul coûteux mémoïsé\n    a.titre.localeCompare(b.titre)), [livres]);\n  return <ul>{tri.map((l) => <LigneLourde key={l.id} livre={l} onSelect={onSelect} />)}</ul>;\n}\n\`\`\`\n\n**Mini-quiz** : (a) Que faut-il faire AVANT de mémoïser quoi que ce soit ? (b) Pourquoi \`React.memo\` seul ne suffit-il pas si on passe \`onClick={() => ...}\` ? (c) Un \`useMemo\` sur \`a + b\` est-il utile ?\n→ (a) mesurer avec le Profiler pour prouver un re-rendu coûteux inutile ; (b) la fonction en ligne a une nouvelle identité à chaque rendu, donc les props changent — il faut useCallback ; (c) non, calcul trivial : du bruit qui dégrade la lisibilité sans gain.\n\n**Variante** : retire le \`useCallback\` et re-profile — observe que \`React.memo\` cesse d'agir (l'enfant re-rend à nouveau). Tu PROUVES la dépendance entre stabilité de référence et mémoïsation.`,
    caseStudy: `Les problèmes de performance React en production sont presque toujours des re-rendus inutiles d'une grande liste ou d'un composant lourd, déclenchés par des références instables (fonctions/objets recréés). Les équipes matures traitent cela par la MESURE (Profiler) puis une mémoïsation ciblée — jamais par une mémoïsation systématique qui alourdit le code sans preuve. Le contre-exemple fréquent : un junior sème des \`useMemo\`/\`useCallback\` partout « pour la performance », rendant le code illisible sans gain mesurable. Savoir mesurer, diagnostiquer la référence instable, et n'optimiser que le prouvé est exactement le jugement recherché — et « mesurer avant d'optimiser » se transfère à toute optimisation, front comme back.`,
    interview: `**« Comment optimises-tu les performances d'un composant React ? »** → D'abord je MESURE avec le React DevTools Profiler : je ne devine pas, je repère quels composants se re-rendent inutilement et combien ça coûte. La cause est souvent une RÉFÉRENCE instable — un objet, un tableau ou une fonction recréés en ligne à chaque rendu du parent, qui changent l'identité des props de l'enfant. Je corrige avec le bon outil : \`React.memo\` pour ne re-rendre un enfant que si ses props changent vraiment, \`useCallback\` pour stabiliser une fonction, \`useMemo\` pour un calcul coûteux. Puis je RE-profile pour prouver le gain. Je ne mémoïse jamais par principe : ça a un coût qui ne se justifie que face à un problème mesuré.`,
    future: `- **Bankable** : « mesurer avant d'optimiser » et savoir diagnostiquer une référence instable est un réflexe d'ingénieur — évalué en entretien front senior et applicable à toute optimisation.\n- Ce jugement sert à garder BiblioApp fluide (listes, recherche, jours 115-117) sans sur-ingénierie, et éclaire le coût de re-rendu de Context (jour 101).\n- La discipline « profiler → diagnostiquer → corriger le prouvé → re-mesurer » est universelle : elle vaut pour les requêtes SQL, les pipelines data et l'inférence LLM (coûts, mois 9).`,
    solution: {
      logic: "Solution simple : envelopper le composant lourd dans React.memo. Solution améliorée : MESURER d'abord au Profiler, diagnostiquer la référence instable (fonction/objet en ligne), stabiliser avec useCallback/useMemo pour que memo agisse réellement, puis RE-profiler pour prouver le gain. Le jugement clé : ne mémoïser que ce qu'un profil désigne comme coûteux et inutile — pas par principe. La preuve : un avant/après chiffré au Profiler.",
      pitfalls: ["Mémoïser partout « au cas où » : coût en complexité et en mémoire sans gain, code illisible.", "React.memo sans stabiliser les props-fonctions (useCallback) : l'enfant re-rend quand même car l'identité change à chaque rendu.", "Optimiser sans mesurer : on complexifie à l'aveugle, souvent le mauvais composant.", "useMemo sur un calcul trivial : du bruit qui dégrade la lisibilité pour un gain nul."],
      checks: ["Un profil (Profiler) identifie le re-rendu coûteux AVANT toute optimisation.", "La cause (référence instable) est diagnostiquée, pas devinée.", "memo est accompagné de useCallback/useMemo pour que les props restent stables.", "Un re-profilage prouve le gain après optimisation.", "Aucune mémoïsation n'est posée sans justification mesurée."],
      oral: "Formule la règle d'or : « mesurer avant d'optimiser ». Explique la chaîne référence instable → props changent → memo inefficace, et pourquoi useCallback débloque memo. Insiste sur le jugement anti-sur-ingénierie : « je ne mémoïse que le re-rendu coûteux prouvé au Profiler ». Montrer un avant/après chiffré est bien plus convaincant que réciter les trois hooks.",
    },
  },

  // ── Accessibilité et UX de base ──
  103: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : l'accessibilité (a11y) n'est pas une couche cosmétique ajoutée à la fin — c'est du HTML SÉMANTIQUE correct, une navigation au CLAVIER qui marche, et un FOCUS géré ; et ça améliore l'expérience de TOUS les utilisateurs, pas seulement de ceux en situation de handicap.** Un bouton qui est un vrai \`<button>\` (et non une \`<div onClick>\`) est automatiquement focusable, activable au clavier, et annoncé par les lecteurs d'écran — gratuitement. La majorité de l'accessibilité consiste à UTILISER le bon élément HTML plutôt qu'à rajouter des rustines ARIA par-dessus des \`<div>\`.\n\nLes piliers de l'accessibilité de base :\n1. **Le HTML sémantique** : \`<button>\`, \`<nav>\`, \`<main>\`, \`<label>\` liés à leurs champs, des titres hiérarchisés (\`<h1>\` → \`<h2>\`). Le sens porté par la balise est lu par les technologies d'assistance ET structure la page.\n2. **Le clavier** : tout ce qui est cliquable doit être atteignable et activable au clavier (Tab, Entrée, Espace). Un menu qui ne marche qu'à la souris exclut une partie des utilisateurs — teste ton app SANS souris.\n3. **Le focus visible et géré** : on doit toujours VOIR où est le focus (ne jamais supprimer l'\`outline\` sans le remplacer), et après une action (ouvrir une modale), déplacer le focus au bon endroit.\n4. **Les textes et contrastes** : chaque image a un \`alt\` pertinent, chaque champ un \`<label>\`, et le contraste texte/fond est suffisant pour être lisible.\n\nLa règle du jour : **l'accessibilité se construit avec le bon HTML dès le départ, pas en rustines à la fin — et un audit clavier + lecteur d'écran révèle l'essentiel des problèmes**. Pourquoi ça compte au-delà de l'éthique et du légal (obligations d'accessibilité) : une app accessible est plus utilisable pour tous (clavier plus rapide, structure claire), mieux référencée (le sémantique aide le SEO), et plus robuste. C'est aussi un marqueur de qualité que les recruteurs remarquent : un candidat qui pense clavier et \`<label>\` montre un souci du détail professionnel.`,
    guided: `**Énoncé** : auditer l'accessibilité de ton app et corriger cinq problèmes concrets.\n\n**Exemple simple** : la correction la plus fréquente — transformer une fausse-interactivité en vraie — \`<div onClick={...}>\` devient \`<button onClick={...}>\`. Le \`<button>\` est focusable, activable au clavier et annoncé, sans une ligne d'ARIA.\n\n**Raisonnement pas-à-pas** : parcourir l'app AU CLAVIER (sans souris) → repérer ce qui n'est pas atteignable ou pas annoncé → remplacer par du HTML sémantique → vérifier focus visible, labels, contraste, alt.\n\n**Corrections types** :\n\`\`\`tsx\n// 1. Faux bouton → vrai bouton (focusable + clavier + annoncé, gratuitement)\n// AVANT: <div className=\"btn\" onClick={supprimer}>×</div>\n<button aria-label=\"Supprimer le livre\" onClick={supprimer}>×</button>\n\n// 2. Champ sans label → label lié (le lecteur d'écran annonce le champ)\n<label htmlFor=\"q\">Rechercher</label>\n<input id=\"q\" value={q} onChange={...} />\n\n// 3. Image sans alternative → alt pertinent (ou alt=\"\" si décorative)\n<img src={couverture} alt={\`Couverture de \${livre.titre}\`} />\n\n// 4. Focus supprimé → jamais ; garder un focus VISIBLE (outline personnalisé, pas none)\n// 5. Structure : un seul <h1>, titres hiérarchisés, <nav>/<main> sémantiques\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi \`<button>\` vaut-il mieux qu'une \`<div onClick>\` ? (b) Comment tester rapidement l'accessibilité clavier ? (c) Que met-on dans l'\`alt\` d'une image décorative ?\n→ (a) il est focusable, activable au clavier et annoncé par les lecteurs d'écran sans effort ; (b) parcourir toute l'app au clavier (Tab/Entrée) sans toucher la souris ; (c) \`alt=\"\"\` (vide) pour que le lecteur d'écran l'ignore.\n\n**Variante** : lance un audit automatique (l'onglet Lighthouse/axe des DevTools) et compare ses alertes à ce que TON parcours clavier a trouvé — l'outil attrape le mesurable (contraste, alt manquant), toi le vécu (ordre de focus illogique).`,
    caseStudy: `L'accessibilité est une obligation légale dans de nombreux contextes (secteur public, grandes entreprises) et un vrai enjeu d'inclusion : un site inaccessible exclut des utilisateurs et expose l'entreprise. Mais au-delà du légal, les équipes constatent que le HTML sémantique et le souci du clavier rendent l'app plus maintenable et mieux référencée. En entretien front, montrer qu'on pense \`<label>\`, clavier et focus distingue immédiatement un candidat soigneux d'un candidat qui « fait juste marcher le clic ». C'est un différenciateur peu coûteux à acquérir et très visible.`,
    interview: `**« Comment rends-tu une interface accessible ? »** → L'essentiel de l'accessibilité, c'est du HTML SÉMANTIQUE : un vrai \`<button>\` (pas une \`<div onClick>\`) est focusable, activable au clavier et annoncé par les lecteurs d'écran gratuitement ; des \`<label>\` liés aux champs, des titres hiérarchisés, \`<nav>\`/\`<main>\`. Ensuite je vérifie que tout est atteignable AU CLAVIER (je parcours l'app sans souris), que le focus reste VISIBLE et bien géré, et que les images ont un \`alt\` pertinent avec un contraste suffisant. Je construis l'accessibilité dès le départ avec le bon élément, pas en rustines ARIA à la fin. Bénéfice au-delà de l'inclusion et du légal : meilleure utilisabilité pour tous et meilleur référencement.`,
    future: `- **Bankable** : le souci de l'accessibilité (sémantique, clavier, focus, labels) est un différenciateur front peu coûteux et très visible en entretien et en revue de code.\n- Ces réflexes s'appliquent à chaque écran de BiblioApp (formulaires labellisés, listes navigables au clavier, jours 113-118) et à l'interface de DocSense.\n- Penser « le bon HTML dès le départ » plutôt que « des rustines à la fin » est une posture de qualité transférable à tout travail d'interface, y compris les UI d'outils IA.`,
    solution: {
      logic: "Solution simple : ajouter des alt et des labels manquants. Solution améliorée : construire sur du HTML sémantique (vrais boutons, labels liés, titres hiérarchisés, nav/main), garantir la navigation clavier complète (audit sans souris), un focus visible et géré, des contrastes suffisants, et croiser un audit automatique (Lighthouse/axe) avec le parcours clavier vécu. La preuve : l'app est entièrement utilisable au clavier et cinq problèmes réels sont corrigés, pas seulement signalés.",
      pitfalls: ["<div onClick> au lieu de <button> : non focusable, inutilisable au clavier, muet pour les lecteurs d'écran.", "Supprimer l'outline de focus sans le remplacer : l'utilisateur clavier ne voit plus où il est.", "Champs sans <label> lié : le lecteur d'écran n'annonce pas à quoi sert le champ.", "Se fier uniquement à l'audit automatique : il rate l'ordre de focus illogique et l'expérience clavier réelle."],
      checks: ["Toute interactivité utilise le HTML sémantique adéquat (button, label, nav).", "L'app entière est navigable et activable au clavier (testé sans souris).", "Le focus est toujours visible et géré après les actions.", "Images (alt) et contrastes sont conformes.", "Cinq problèmes concrets sont réellement corrigés, audit automatique + clavier croisés."],
      oral: "Résume : « l'accessibilité, c'est surtout le bon HTML dès le départ — un vrai bouton est accessible gratuitement ». Décris ton audit clavier (parcourir sans souris) comme le test le plus révélateur. Mentionne le double bénéfice (inclusion + SEO + utilisabilité pour tous). Montrer qu'on pense clavier et label signale un souci du détail que les recruteurs front remarquent.",
    },
  },

  // ── Consolidation front + préparation Projet 3 ──
  104: {
    lessons: ['react-fundamentals.md', 'architecture-basics.md'],
    theory: `**Modèle mental : cadrer un projet AVANT de coder, c'est transformer une idée floue en trois artefacts décidés sur papier — un arbre de composants, un plan d'état, un backlog — et l'architecture décidée sur papier coûte dix fois moins cher que celle découverte au milieu du code.** Aujourd'hui n'est pas un nouveau cours React : c'est le jour où tu SOUDES les acquis de la semaine (composants, state, listes, effets, formulaires, api, routing, lever l'état, Context, performance, a11y) en les APPLIQUANT à la conception de BiblioApp (Projet 3). Coder sans cadrage, c'est improviser une maison sans plan ; tu le paieras en refactors.\n\nLes trois artefacts de cadrage :\n1. **L'arbre de composants** : la décomposition de chaque écran en composants (jour 92), avec les composants réutilisables identifiés (une CarteLivre servira dans la liste ET la recherche). C'est la structure statique de l'app.\n2. **Le plan d'état** : pour chaque donnée, OÙ elle vit (jour 100 : plus petit ancêtre commun) et COMMENT elle circule (props vers le bas, callbacks vers le haut ; Context pour le global stable, jour 101). C'est le système nerveux de l'app — le décider maintenant évite le props drilling et les états mal placés.\n3. **Le backlog** : la liste ORDONNÉE des tâches, découpées en incréments livrables (socle navigable → CRUD → recherche → tests → polish → doc, jours 113-118). Chaque incrément produit quelque chose de démontrable — le découpage vertical qui évite l'effet tunnel.\n\nLa règle du jour : **un bon cadrage rend le codage ENNUYEUX — chaque décision structurante est déjà prise, il ne reste qu'à exécuter**. C'est le signe d'un cadrage réussi : pendant le développement (jours 113-118), tu ne te demandes pas « où mettre cet état ? » ou « comment découper cet écran ? », car tu l'as décidé ici. Ce travail de conception amont — arbre, état, backlog — est exactement ce qu'un développeur fait avant tout projet sérieux, et savoir le produire distingue quelqu'un qui « code des features » de quelqu'un qui « conçoit une application ».`,
    guided: `**Énoncé** : produire le cadrage complet de BiblioApp — arbre de composants, plan d'état, backlog.\n\n**Exemple simple** : une ligne de plan d'état bien faite — « liste des livres : vit dans \`<Catalogue>\` (chargée via api au montage) ; passée en props à \`<Liste>\` et \`<Recherche>\` ». Une donnée, un lieu, ses lecteurs. Répète pour chaque donnée.\n\n**Raisonnement pas-à-pas** : lister les écrans → décomposer chacun en composants (réutilisables repérés) → pour chaque donnée, décider où elle vit et comment elle circule → ordonner les tâches en incréments livrables.\n\n**Squelette de cadrage** :\n\`\`\`\nARBRE DE COMPOSANTS (par route)\n  /livres        → <Catalogue> [BarreRecherche, Filtres, Liste[CarteLivre], Compteur]\n  /livres/:id    → <Detail> [InfosLivre, BoutonsActions]\n  /livres/:id/edit → <Edition> [FormulaireLivre (réutilisé par l'ajout)]\n  *              → <PageIntrouvable>\nPLAN D'ÉTAT\n  livres[]        : <Catalogue> (api au montage) → props vers Liste/Recherche\n  recherche q     : <Catalogue> → dérive la liste filtrée (pas de state en double)\n  thème           : Context (global + stable)\n  form d'édition  : local à <FormulaireLivre> (contrôlé, jour 96)\nBACKLOG (incréments livrables, jours 113-118)\n  1. Socle navigable (routing + liste + détail + api)\n  2. CRUD complet (ajout/édition/suppression immuables)\n  3. Recherche + filtres · 4. Tests · 5. Polish/états edge · 6. README+ADR+démo\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi décider le plan d'état AVANT de coder ? (b) Que doit produire chaque incrément du backlog ? (c) À quoi reconnaît-on un bon cadrage ?\n→ (a) pour éviter les états mal placés et le props drilling découverts en cours de route (refactors coûteux) ; (b) quelque chose de démontrable (découpage vertical, pas d'effet tunnel) ; (c) le codage devient « ennuyeux » — les décisions structurantes sont déjà prises.\n\n**Variante** : pour chaque composant réutilisable de ton arbre, note où il est réutilisé — si aucun ne l'est, ta décomposition est probablement trop plate ; si tout l'est, tu as bien pensé en briques.`,
    caseStudy: `Le cadrage amont (arbre, état, backlog) est ce qui sépare un projet qui avance d'un projet qui s'enlise. Les équipes qui sautent cette étape « pour coder plus vite » découvrent à mi-parcours qu'un état est au mauvais endroit et réécrivent des pans entiers ; celles qui cadrent exécutent sereinement. En entretien, « comment démarrerais-tu ce projet ? » teste exactement cette capacité : la bonne réponse parle de décomposition, de placement de l'état et de découpage en incréments livrables — pas « je commence à coder l'écran principal ». Savoir produire ces artefacts est un signal de séniorité, même à niveau junior.`,
    interview: `**« Comment cadres-tu une application front avant de la coder ? »** → Je produis trois artefacts. D'abord l'arbre de composants : je décompose chaque écran en composants et j'identifie les réutilisables. Ensuite le plan d'état : pour chaque donnée, je décide OÙ elle vit (le plus petit ancêtre commun de ses lecteurs) et COMMENT elle circule (props vers le bas, callbacks vers le haut, Context pour le global stable). Enfin le backlog : des incréments livrables et ordonnés (socle → CRUD → recherche → tests → polish → doc), chacun produisant quelque chose de démontrable. Un bon cadrage rend le codage « ennuyeux » : les décisions structurantes sont déjà prises, il ne reste qu'à exécuter — ce qui évite les refactors coûteux découverts en cours de route.`,
    future: `- **Bankable** : savoir cadrer (arbre + état + backlog) avant de coder est un signal de séniorité — « comment démarrerais-tu ce projet ? » est une question d'entretien où cette réponse fait la différence.\n- Ce cadrage EST le plan d'exécution de BiblioApp (jours 113-118) : un bon cadrage aujourd'hui rend ces jours fluides.\n- La discipline « concevoir sur papier avant d'implémenter » se réapplique à tout projet — API, pipeline data, application IA (DocSense, mois 11).`,
    solution: {
      logic: "Solution simple : une liste d'écrans et de composants. Solution améliorée : trois artefacts articulés — un arbre de composants avec les réutilisables identifiés, un plan d'état qui place chaque donnée (plus petit ancêtre commun, props/Context justifiés, état dérivé repéré) et décrit sa circulation, et un backlog d'incréments livrables ordonnés. Le critère de réussite : le codage des jours 113-118 devient de la simple exécution, sans décision structurante restante.",
      pitfalls: ["Sauter le cadrage pour « coder plus vite » : les décisions d'état mal placées se paient en refactors à mi-parcours.", "Un plan d'état flou (« on verra où mettre ça ») : props drilling et doublons de state garantis.", "Un backlog par couches horizontales (tout le CSS, puis toute la logique) : effet tunnel, rien de démontrable avant la fin — découpe en incréments verticaux.", "Une décomposition trop plate sans composants réutilisables : on n'a pas vraiment pensé en briques."],
      checks: ["L'arbre de composants couvre chaque route et identifie les réutilisables.", "Le plan d'état place chaque donnée (lieu + circulation + Context justifié).", "L'état dérivé est distingué de l'état stocké (pas de doublon).", "Le backlog est fait d'incréments livrables et ordonnés.", "Chaque incrément produit quelque chose de démontrable (découpage vertical)."],
      oral: "Explique le principe : « je décide l'architecture sur papier — arbre, état, backlog — parce que c'est dix fois moins cher que de la découvrir dans le code ». Donne le critère de réussite : « un bon cadrage rend le codage ennuyeux ». Décrire ces trois artefacts en réponse à « comment démarres-tu ? » signale que tu conçois une application, pas que tu empiles des features.",
    },
  },

  // ── Revue semaine 15 : effets, routing, état partagé, perf, a11y ──
  105: {
    reviewSynthese: `La semaine 15 a fait passer React du composant isolé à l'APPLICATION structurée : routing avec l'URL comme état partageable et 404 propre (jour 99), placement de l'état par déduction — lever l'état au plus petit ancêtre commun (100), Context pour l'état global et stable sans props drilling (101), performance par la mesure et l'élimination des re-rendus inutiles (102), accessibilité par le HTML sémantique et le clavier (103), et cadrage complet de BiblioApp — arbre, plan d'état, backlog (104). Le fil rouge : **une application front, ce n'est pas des composants empilés, c'est un système où l'état est bien placé, l'URL navigable, les performances mesurées et l'interface accessible — et tout cela se DÉCIDE avant de coder**. Tu entres dans la semaine 16 (tests) avec une app cadrée à construire proprement.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Mettre l'état navigable dans l'URL (routing, paramètres, 404) | |\n| Placer l'état au plus petit ancêtre commun (lever l'état, flux uni) | |\n| Utiliser Context à bon escient (global + stable) ET savoir l'éviter | |\n| Diagnostiquer et corriger un re-rendu inutile APRÈS l'avoir mesuré | |\n| Rendre une UI accessible (sémantique, clavier, focus, labels) | |\n| Cadrer un projet : arbre de composants + plan d'état + backlog | |\n\n**Total /18** : ≥ 15 = solide, attaque les tests · 11-14 = revoir les items ≤ 1 · < 11 = rejouer avant BiblioApp, car ces décisions structurent tout le projet.`,
    remediation: `- **État de navigation en mémoire** (pas partageable, retour cassé) : rejoue le jour 99 — l'URL est un état ; \`<Link>\` pas \`<a>\` ; paramètres via useParams ; route 404 attrape-tout.\n- **État mal placé / props drilling** : reprends le jour 100 — déduis la place de l'état de ses lecteurs et modificateurs (plus petit ancêtre commun) ; données vers le bas, événements vers le haut.\n- **Abus de Context** (re-rendus massifs) : jour 101 — Context uniquement pour un état global ET stable ; un état changeant reste local.\n- **Optimisation à l'aveugle** : jour 102 — mesure au Profiler AVANT ; la cause est souvent une référence instable ; ne mémoïse que le prouvé.\n- **Interface inaccessible** : jour 103 — HTML sémantique dès le départ, audit clavier sans souris, focus visible, labels.\n- **Pas de cadrage** : jour 104 — sans arbre + plan d'état + backlog, BiblioApp partira dans le mur ; produis-les avant de coder.`,
    interview: `1. **« Pourquoi mettre l'état de navigation dans l'URL ? »** Attendu : URL = état partageable/bookmarkable, bouton retour, rafraîchissement ; \`<Link>\` pas \`<a>\` ; 404 attrape-tout.\n2. **« Où places-tu un état partagé par deux composants ? »** Attendu : au plus petit ancêtre commun de ses lecteurs et modificateurs ; données vers le bas, événements vers le haut ; props drilling = signal d'un mauvais placement.\n3. **« Quand Context, quand des props ? »** Attendu : Context pour du global + stable (thème, utilisateur) car tous les consommateurs re-rendent quand la valeur change ; props pour le local ; savoir refuser Context.\n4. **« Comment optimises-tu un re-rendu coûteux ? »** Attendu : mesurer au Profiler d'abord, diagnostiquer la référence instable, memo + useCallback/useMemo ciblés, re-profiler ; jamais par principe.\n5. **« Comment démarres-tu une app front ? »** Attendu : arbre de composants, plan d'état (placement + circulation), backlog d'incréments livrables — cadrer avant de coder.`,
  },

  // ── Tests unitaires (Vitest) ──
  106: {
    lessons: ['testing-foundations.md'],
    theory: `**Modèle mental : un test unitaire FIGE un comportement attendu, et sa valeur se PROUVE en le faisant rougir — un test qui ne peut jamais échouer ne teste rien.** Écrire un test, ce n'est pas « vérifier que ça marche une fois » : c'est décrire, en code exécutable, ce que ta fonction DOIT faire, de sorte que toute régression future casse le test au lieu de casser la production silencieusement. Le test est un filet de sécurité ET une documentation vivante du comportement attendu.\n\nL'anatomie d'un bon test unitaire (schéma AAA) :\n1. **Arrange** : préparer les données d'entrée et le contexte.\n2. **Act** : appeler la fonction testée UNE fois.\n3. **Assert** : vérifier que la sortie est celle attendue — une assertion claire, focalisée sur UN comportement.\n\nUn test unitaire vise une UNITÉ isolée (une fonction de validation, un calcul, un filtre) — pas toute l'app. Il doit être : RAPIDE (des millisecondes, on en lance des centaines), DÉTERMINISTE (même entrée → même résultat, sans dépendre du réseau ou de l'horloge — d'où les mocks, jour 108), et LISIBLE (son nom décrit le comportement : « rejette un email sans @ »).\n\nLe geste qui distingue un vrai test d'un test décoratif : **le faire ROUGIR volontairement**. Écris le test, sabote le code (change un \`>\` en \`>=\`, retourne \`null\`) et VÉRIFIE que le test échoue. Un test qui reste vert alors que tu as cassé le code ne protège de rien — c'est un faux sentiment de sécurité, pire que pas de test. On teste les cas NORMAUX, les cas LIMITES (vide, zéro, négatif, très grand) et les cas d'ERREUR (entrée invalide) — c'est aux limites que les bugs se cachent.\n\nLa règle du jour : **un test prouve sa valeur par sa capacité à échouer quand le code est faux**. Couvrir la logique métier (validation, calculs, filtres) par des tests qui rougissent quand on les sabote transforme « je crois que ça marche » en « je peux prouver que ça marche, et je serai alerté si ça casse ».`,
    guided: `**Énoncé** : écrire 10+ tests sur une logique métier et prouver qu'ils rougissent.\n\n**Exemple simple** : un test AAA minimal — \`test('rejette un email sans @', () => { expect(valider('bob')).toBe(false); })\`. Un comportement nommé, un appel, une assertion. Dix tests, c'est dix fois cette structure sur des cas différents.\n\n**Raisonnement pas-à-pas** : lister les comportements (normaux + limites + erreurs) → un test AAA par comportement → lancer (vert) → SABOTER le code → vérifier que ça rougit → réparer.\n\n**Solution** :\n\`\`\`ts\nimport { describe, test, expect } from 'vitest';\nimport { validerLivre, filtrerParTitre } from './metier';\n\ndescribe('validerLivre', () => {\n  test('accepte un livre complet', () => expect(validerLivre({ titre: 'A', auteur: 'B' }).ok).toBe(true));\n  test('rejette un titre vide', () => expect(validerLivre({ titre: '', auteur: 'B' }).ok).toBe(false));\n  test('rejette des espaces seuls (cas limite)', () => expect(validerLivre({ titre: '   ', auteur: 'B' }).ok).toBe(false));\n});\ndescribe('filtrerParTitre', () => {\n  const livres = [{ titre: 'React' }, { titre: 'Rust' }];\n  test('filtre insensible à la casse', () => expect(filtrerParTitre(livres, 'r').length).toBe(2));\n  test('liste vide si aucun match', () => expect(filtrerParTitre(livres, 'zzz')).toEqual([]));\n});\n// PREUVE : change 'titre.trim()' en 'titre' dans validerLivre → le test des espaces DOIT rougir.\n\`\`\`\n\n**Mini-quiz** : (a) Que prouve le fait de saboter le code volontairement ? (b) Pourquoi un test doit-il être déterministe ? (c) Où se cachent le plus souvent les bugs ?\n→ (a) que le test peut vraiment échouer, donc qu'il protège d'une régression ; (b) sinon il rougit/verdit au hasard (réseau, horloge), on ne peut pas s'y fier ; (c) aux cas limites (vide, zéro, espaces, très grand) et d'erreur.\n\n**Variante** : pour chaque fonction, ajoute un test de cas limite auquel tu n'avais pas pensé (chaîne très longue, caractères spéciaux) — c'est souvent là qu'un vrai bug apparaît.`,
    caseStudy: `Les tests unitaires sont le socle de la confiance en entreprise : ils permettent de refactorer et de livrer sans peur, car une régression casse un test avant d'atteindre la production. Le contre-exemple classique est le test « décoratif » qui ne peut pas échouer (il teste que \`2+2===4\` sans toucher au code métier, ou il n'a pas d'assertion réelle) — il gonfle la couverture sans rien protéger. Les équipes sérieuses vérifient qu'un test ROUGIT quand on casse le code (parfois via le « mutation testing »). Savoir écrire des tests qui échouent utilement, en couvrant les cas limites, est une compétence directement évaluée et un prérequis pour travailler sur une base de code partagée.`,
    interview: `**« Qu'est-ce qui fait un bon test unitaire ? »** → Il fige UN comportement attendu (schéma Arrange-Act-Assert), il est rapide, déterministe (même entrée → même résultat, sans dépendre du réseau ou de l'horloge) et lisible (son nom décrit le comportement). Surtout, sa valeur se prouve par sa capacité à ROUGIR : j'écris le test, je sabote volontairement le code, et je vérifie qu'il échoue — un test qui reste vert quand le code est cassé ne protège de rien, c'est pire que pas de test. Je couvre les cas normaux, limites (vide, zéro, négatif) et d'erreur, parce que c'est aux limites que les bugs se cachent. Un test unitaire vise une unité isolée ; ses dépendances externes sont mockées pour rester déterministe.`,
    future: `- **Bankable** : savoir écrire des tests qui rougissent utilement et couvrent les cas limites est un prérequis pour toute base de code partagée — évalué dès l'entretien technique.\n- Cette suite protège la logique de BiblioApp (validation, filtres, jours 113-116) et te permet de refactorer sans peur.\n- La discipline « un test prouve sa valeur en échouant » se transfère au test de composants (jour 107), aux pipelines data, et à l'évaluation des systèmes IA (un golden set est un test qui doit pouvoir échouer).`,
    solution: {
      logic: "Solution simple : quelques tests qui appellent la fonction et vérifient la sortie. Solution améliorée : structurer chaque test en Arrange-Act-Assert avec un nom qui décrit le comportement, couvrir explicitement les cas normaux, limites et d'erreur, garantir le déterminisme (aucune dépendance réseau/horloge), et surtout PROUVER chaque test en sabotant le code pour vérifier qu'il rougit. La preuve de valeur : une régression volontaire casse le bon test.",
      pitfalls: ["Test « décoratif » sans vraie assertion ou qui ne touche pas le code métier : gonfle la couverture, ne protège rien.", "Ne pas vérifier que le test peut rougir : un test toujours vert donne un faux sentiment de sécurité.", "Ne couvrir que le cas heureux : les bugs se cachent aux cas limites (vide, zéro, espaces, très grand).", "Test non déterministe (dépend du réseau ou de la date) : rougit/verdit au hasard, on ne peut pas s'y fier."],
      checks: ["Chaque test suit Arrange-Act-Assert et porte un nom décrivant le comportement.", "Les cas normaux, limites et d'erreur sont couverts.", "Chaque test a été prouvé en sabotant le code (il rougit).", "Les tests sont rapides et déterministes.", "Au moins 10 tests couvrent la logique métier réelle."],
      oral: "Insiste sur le geste clé : « je sabote le code pour prouver que le test peut échouer — sinon il ne protège rien ». Explique AAA, le déterminisme et la couverture des cas limites (« c'est là que les bugs vivent »). Le test décoratif qui ne peut pas rougir est le contre-exemple parfait à citer — il montre que tu comprends À QUOI sert vraiment un test.",
    },
  },

  // ── Tester des composants React ──
  107: {
    lessons: ['testing-foundations.md'],
    theory: `**Modèle mental : on teste ce que l'UTILISATEUR voit et fait (un texte à l'écran, un clic sur un bouton, un rôle accessible) — PAS l'implémentation interne (le nom du state, la structure des composants). Un test couplé à l'implémentation casse à chaque refactor sans détecter aucun vrai bug.** C'est le principe fondateur de Testing Library : « plus tes tests ressemblent à la façon dont l'utilisateur se sert de ton app, plus ils te donnent confiance ». Tu ne vérifies pas « le state \`ouvert\` vaut \`true\` » ; tu vérifies « le menu est visible à l'écran ».\n\nLes trois gestes du test de composant :\n1. **Rendre** le composant dans un DOM virtuel (\`render(<X/>)\`).\n2. **Trouver** les éléments comme un utilisateur : par leur rôle, leur label, leur texte (\`getByRole('button', { name: 'Ajouter' })\`, \`getByText\`) — PAS par une classe CSS ou un id technique. Chercher par rôle pousse en prime vers un HTML accessible (jour 103).\n3. **Interagir et vérifier** : simuler un clic ou une saisie (\`userEvent.click\`), puis affirmer sur ce qui a CHANGÉ à l'écran (\`expect(screen.getByText('Ajouté')).toBeVisible()\`).\n\nPourquoi ce principe est décisif : un test couplé à l'implémentation (« le composant \`<Modal>\` a un state \`isOpen\`») se casse dès que tu renommes le state ou restructures les composants — alors même que l'app fonctionne parfaitement pour l'utilisateur. Il génère du bruit et décourage le refactor. À l'inverse, un test orienté COMPORTEMENT (« après le clic, la modale apparaît ») survit aux refactors internes et n'échoue que si le comportement RÉEL casse — ce qui est exactement ce qu'on veut d'un test.\n\nLa règle du jour : **teste le comportement observable, jamais les détails d'implémentation ; cherche les éléments comme le fait un utilisateur (rôle, label, texte)**. Ce qui teste le comportement résiste au refactor et attrape les vrais bugs ; ce qui teste l'implémentation casse pour rien. C'est la même philosophie que le test d'API par son contrat (mois 3) : on teste l'interface publique, pas la plomberie interne.`,
    guided: `**Énoncé** : tester trois composants clés par leur comportement, pas leur implémentation.\n\n**Exemple simple** : un test orienté utilisateur — \`render(<Compteur/>); await userEvent.click(screen.getByRole('button', { name: '+' })); expect(screen.getByText('1')).toBeVisible();\`. On clique comme un humain, on vérifie ce qu'il verrait.\n\n**Raisonnement pas-à-pas** : rendre → trouver par rôle/label/texte → interagir → affirmer sur le DOM visible, jamais sur le state interne.\n\n**Solution** :\n\`\`\`tsx\nimport { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\n\ntest('le formulaire affiche une erreur si le titre est vide', async () => {\n  render(<AjoutLivre onCreer={async () => {}} />);\n  await userEvent.click(screen.getByRole('button', { name: /ajouter/i }));\n  expect(screen.getByText(/titre est requis/i)).toBeVisible();   // comportement visible\n});\n\ntest('appelle onCreer avec les valeurs saisies', async () => {\n  const onCreer = vi.fn();\n  render(<AjoutLivre onCreer={onCreer} />);\n  await userEvent.type(screen.getByLabelText(/titre/i), 'React');  // trouvé par LABEL (a11y)\n  await userEvent.type(screen.getByLabelText(/auteur/i), 'Dan');\n  await userEvent.click(screen.getByRole('button', { name: /ajouter/i }));\n  expect(onCreer).toHaveBeenCalledWith({ titre: 'React', auteur: 'Dan' });\n});\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi chercher par rôle/label plutôt que par classe CSS ? (b) Que teste-t-on : le state interne ou le DOM visible ? (c) Pourquoi un test couplé à l'implémentation est-il un problème ?\n→ (a) c'est ce que voit l'utilisateur, et ça résiste au refactor tout en poussant vers un HTML accessible ; (b) le DOM visible / le comportement observable ; (c) il casse à chaque refactor interne sans détecter de vrai bug — du bruit qui décourage le refactor.\n\n**Variante** : après avoir écrit ces tests, RENOMME un state interne ou restructure un sous-composant sans changer le comportement — tes tests doivent rester VERTS. S'ils cassent, c'est qu'ils testaient l'implémentation.`,
    caseStudy: `Le passage d'Enzyme (qui testait l'implémentation : instances, state) à Testing Library (qui teste le comportement) dans l'écosystème React illustre exactement ce principe à l'échelle de l'industrie : les équipes ont adopté « teste comme l'utilisateur » parce que les tests couplés à l'implémentation coûtaient plus qu'ils ne rapportaient — ils cassaient à chaque refactor sans attraper de bugs. Aujourd'hui, tester par rôle/label/texte est le standard, et c'est ce qu'un employeur attend. Bonus notable : comme on cherche les éléments par leur rôle accessible, écrire de bons tests POUSSE naturellement vers une UI accessible — les deux compétences se renforcent.`,
    interview: `**« Comment testes-tu un composant React, et que veut dire 'tester le comportement pas l'implémentation' ? »** → Je rends le composant, je trouve les éléments comme un UTILISATEUR le ferait (par rôle, label ou texte — pas par classe CSS ni id technique), j'interagis (clic, saisie) et j'affirme sur ce qui change À L'ÉCRAN. « Tester le comportement » signifie vérifier ce que l'utilisateur voit et fait (« après le clic, la modale apparaît »), pas les détails internes (« le state \`isOpen\` vaut true »). Un test couplé à l'implémentation casse à chaque refactor sans détecter de vrai bug ; un test orienté comportement survit aux refactors et n'échoue que si le comportement réel casse. Effet de bord vertueux : chercher par rôle pousse vers un HTML accessible.`,
    future: `- **Bankable** : tester par rôle/label/texte (comportement, pas implémentation) est le standard React attendu par les employeurs — et ça renforce l'accessibilité.\n- Ces tests couvrent les parcours critiques de BiblioApp (formulaires, listes, jours 116) et te permettent de refactorer sereinement.\n- Le principe « teste l'interface publique, pas la plomberie » est universel : il vaut pour les composants, les API (mois 3) et tout module dont le contrat compte plus que l'implémentation.`,
    solution: {
      logic: "Solution simple : render + un clic + une assertion. Solution améliorée : trouver les éléments par rôle/label/texte (jamais par classe/id technique), simuler de vraies interactions avec userEvent, affirmer uniquement sur le comportement observable (DOM visible, callbacks appelés), et PROUVER l'indépendance à l'implémentation en refactorant l'interne sans casser les tests. La preuve : renommer un state ou restructurer les sous-composants laisse les tests verts.",
      pitfalls: ["Chercher par classe CSS ou id technique : le test casse dès qu'on change le style ou la structure, sans vrai bug.", "Affirmer sur le state interne au lieu du DOM visible : couplage à l'implémentation, tests fragiles.", "Tester des détails de rendu sans valeur (nombre de divs) plutôt que le comportement utilisateur.", "Oublier d'attendre les interactions asynchrones (userEvent/await) : assertions sur un DOM pas encore à jour."],
      checks: ["Les éléments sont trouvés par rôle, label ou texte (comme un utilisateur).", "Les assertions portent sur le comportement observable, pas le state interne.", "Les vraies interactions sont simulées (userEvent).", "Un refactor interne sans changement de comportement laisse les tests verts.", "Les trois composants clés couvrent rendu, interaction et états."],
      oral: "Cite le principe de Testing Library : « plus le test ressemble à l'usage réel, plus il donne confiance ». Oppose test d'implémentation (casse au refactor, n'attrape rien) et test de comportement (résiste, attrape les vrais bugs). Mentionne le bonus a11y (chercher par rôle). Proposer de refactorer l'interne sans casser les tests est la démonstration qui prouve que tu as compris.",
    },
  },

  // ── Mocks et tests d'intégration ──
  108: {
    lessons: ['testing-foundations.md'],
    theory: `**Modèle mental : un MOCK remplace une dépendance que tu ne contrôles pas (l'API, l'horloge, le hasard) pour rendre ton test ISOLÉ et DÉTERMINISTE ; un test d'INTÉGRATION vérifie que plusieurs pièces marchent ENSEMBLE. La règle : mock ce que tu ne possèdes pas, teste en vrai ce que tu possèdes.** Un test unitaire doit être déterministe (jour 106) — or un vrai appel réseau ne l'est pas (lent, faillible, dépend d'un serveur). Le mock résout cela : tu remplaces \`fetch\` par une fonction qui renvoie une réponse contrôlée, et ton test vérifie comment TON code réagit à cette réponse — sans jamais toucher le réseau.\n\nCe qu'un mock permet de tester proprement :\n1. **Le cas de succès** : l'API renvoie des données → l'UI les affiche.\n2. **Le cas d'erreur** : l'API renvoie un 500 → l'UI montre l'erreur. Ce cas est presque IMPOSSIBLE à tester avec une vraie API (comment forcer un 500 à la demande ?) mais TRIVIAL avec un mock. C'est un des grands intérêts du mock : provoquer les cas rares.\n3. **Le cas limite** : réponse vide, réseau lent, timeout — tous simulables.\n\nLe test d'INTÉGRATION monte d'un cran : au lieu de tester une unité isolée, il vérifie un FLUX complet — par exemple « l'utilisateur remplit le formulaire → l'appel API (mocké) part avec les bonnes données → l'UI affiche la confirmation ». Il teste la COLLABORATION entre le formulaire, la couche api et l'affichage. On mocke toujours la frontière externe (le réseau), mais on laisse collaborer TOUT ton code interne — c'est là que se cachent les bugs d'assemblage qu'un test unitaire ne voit pas.\n\nLe piège du mock : **trop mocker rend le test inutile**. Si tu mockes ton propre code (la fonction que tu veux justement tester), tu ne testes plus rien — tu vérifies que ton mock renvoie ce que tu lui as dit de renvoyer. La ligne est nette : mocke la FRONTIÈRE EXTERNE (réseau, temps, aléatoire), jamais la logique interne que le test est censé couvrir.\n\nLa règle du jour : **mocke la frontière, teste l'intégration de ton code ; les cas d'erreur rares deviennent testables, et les bugs d'assemblage remontent**.`,
    guided: `**Énoncé** : mocker les appels API et écrire un test d'intégration d'un flux complet.\n\n**Exemple simple** : mocker la couche api pour un cas d'erreur impossible à provoquer autrement — \`vi.mock('./api'); vi.mocked(api.getLivres).mockRejectedValue(new ApiError(500, 'boom'));\`. Ton test vérifie alors que l'UI affiche l'erreur.\n\n**Raisonnement pas-à-pas** : mocker la frontière réseau (api) → tester le succès, l'erreur et le vide → puis un test d'intégration du flux formulaire → api mockée → UI.\n\n**Solution** :\n\`\`\`tsx\nimport { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport { api } from './api';\nvi.mock('./api');                                          // on mocke la FRONTIÈRE externe\n\ntest('affiche l\\'erreur quand l\\'API échoue', async () => {\n  vi.mocked(api.getLivres).mockRejectedValue(new Error('500')); // cas rare, trivial à simuler\n  render(<Catalogue />);\n  expect(await screen.findByText(/erreur/i)).toBeVisible();\n});\n\ntest('intégration : créer un livre affiche la confirmation', async () => {\n  vi.mocked(api.creerLivre).mockResolvedValue({ id: 1, titre: 'React', auteur: 'Dan' });\n  render(<AjoutLivre />);\n  await userEvent.type(screen.getByLabelText(/titre/i), 'React');\n  await userEvent.type(screen.getByLabelText(/auteur/i), 'Dan');\n  await userEvent.click(screen.getByRole('button', { name: /ajouter/i }));\n  expect(api.creerLivre).toHaveBeenCalledWith({ titre: 'React', auteur: 'Dan' }); // bonnes données\n  expect(await screen.findByText(/ajouté/i)).toBeVisible();                        // UI mise à jour\n});\n\`\`\`\n\n**Mini-quiz** : (a) Que faut-il mocker, et que faut-il laisser réel ? (b) Pourquoi un mock rend-il testable un cas d'erreur 500 ? (c) Que se passe-t-il si on mocke le code qu'on veut tester ?\n→ (a) mocker la frontière externe (réseau, temps) ; laisser réel ton code interne ; (b) on force la réponse voulue sans dépendre d'un vrai serveur ; (c) on ne teste plus rien — juste que le mock renvoie ce qu'on lui a dit.\n\n**Variante** : ajoute un test du cas « réponse vide » (\`mockResolvedValue([])\`) et vérifie l'état vide de l'UI — un flux souvent oublié qui casse en production.`,
    caseStudy: `Les mocks et tests d'intégration sont ce qui rend une suite de tests à la fois rapide, fiable et utile en entreprise : mocker le réseau permet de tester les cas d'erreur (500, timeout, réponse vide) qu'on ne peut pas provoquer avec un vrai serveur, et les tests d'intégration attrapent les bugs d'ASSEMBLAGE (le formulaire envoie le mauvais format à l'api) que les tests unitaires ratent. L'anti-pattern coûteux, courant chez les débutants, est le sur-mock : mocker tellement de choses que le test ne vérifie plus que la configuration des mocks. Les équipes matures mockent uniquement la frontière externe. Savoir où placer cette frontière est un jugement de test directement valorisé.`,
    interview: `**« Quand et quoi mockes-tu dans tes tests ? »** → Je mocke ce que je ne CONTRÔLE pas et qui rend le test non déterministe : le réseau (l'API), l'horloge, l'aléatoire. Ça me permet de tester proprement les cas de succès, mais surtout les cas d'ERREUR rares (un 500, un timeout, une réponse vide) que je ne peux pas provoquer avec un vrai serveur. En revanche, je ne mocke JAMAIS le code que le test est censé couvrir — sinon je ne teste plus rien. Un test d'INTÉGRATION monte d'un cran : il mocke la frontière réseau mais laisse collaborer tout mon code interne (formulaire → api → UI), ce qui attrape les bugs d'assemblage qu'un test unitaire ne voit pas. La règle : mock la frontière externe, teste en vrai ce que tu possèdes.`,
    future: `- **Bankable** : savoir mocker la bonne frontière (ni trop, ni trop peu) et écrire des tests d'intégration est un jugement de test valorisé — il rend une suite rapide, fiable et réellement protectrice.\n- Ces techniques testent les flux critiques de BiblioApp, y compris les cas d'erreur API (jour 116), impossibles à couvrir autrement.\n- La distinction « mocke ce que tu ne possèdes pas, teste en vrai ce que tu possèdes » se transfère à tout test de système intégré, dont les pipelines data et les appels LLM (qu'on mocke pour tester le code autour).`,
    solution: {
      logic: "Solution simple : mocker fetch/api et vérifier l'affichage. Solution améliorée : mocker uniquement la frontière externe (réseau, temps), couvrir succès + erreur + vide grâce au mock (cas impossibles à provoquer en vrai), écrire un test d'intégration qui laisse collaborer tout le code interne (formulaire → api → UI) et vérifie AUSSI les bonnes données envoyées, sans jamais mocker la logique testée. La preuve : un cas d'erreur 500 et un bug d'assemblage sont couverts.",
      pitfalls: ["Sur-mocker jusqu'à mocker le code testé : le test ne vérifie plus que la config des mocks.", "Faire de vrais appels réseau dans les tests : lents, non déterministes, cas d'erreur intestables.", "Ne tester que le succès : les cas 500/timeout/vide, pourtant faciles à mocker, cassent en production.", "Oublier de vérifier les DONNÉES envoyées à l'api (toHaveBeenCalledWith) : on rate les bugs de format d'assemblage."],
      checks: ["Seule la frontière externe (réseau, temps) est mockée, pas la logique testée.", "Les cas succès, erreur et vide sont couverts via le mock.", "Un test d'intégration vérifie un flux complet (formulaire → api → UI).", "Les données envoyées à l'api sont vérifiées (bon format).", "Les tests restent rapides et déterministes."],
      oral: "Formule la règle : « mock ce que tu ne possèdes pas (réseau, temps), teste en vrai ce que tu possèdes ». Explique le grand intérêt du mock : rendre testables les cas d'erreur rares (500, timeout) impossibles à provoquer autrement. Oppose test unitaire (une unité) et intégration (l'assemblage). Citer l'anti-pattern du sur-mock montre que tu sais où placer la frontière.",
    },
  },

  // ── Clean code appliqué au front ──
  109: {
    lessons: ['clean-code.md'],
    theory: `**Modèle mental : un composant se refactore comme une fonction — quand il fait trop de choses, on EXTRAIT (sous-composants, hooks custom, fonctions nommées) ; la lisibilité se mesure au temps qu'il faut pour comprendre ce composant.** Le clean code du mois 2 (noms clairs, fonctions courtes, une responsabilité) s'applique directement au front. Un composant de 300 lignes qui gère l'état, le fetch, la validation, le rendu et le formatage est le pendant React de la fonction fourre-tout : illisible, non testable, source de bugs. Le refactor front, c'est le découper en pièces qui font chacune UNE chose.\n\nLes signaux qu'un composant a besoin de refactor (les « code smells » front) :\n1. **Il est trop long** : on doit scroller pour le comprendre. Extrait des sous-composants (une section = un composant nommé).\n2. **Il mélange logique et affichage** : du fetch, des calculs et du JSX entremêlés. Extrait la logique à état dans un hook custom (jour 110) ; le composant ne garde que le rendu.\n3. **Il a des noms vagues** : \`data\`, \`handleClick\`, \`temp\`. Renomme d'après l'intention (\`livres\`, \`supprimerLivre\`). Un bon nom est la première documentation.\n4. **Il répète du JSX** : trois cartes copiées-collées → un composant \`Carte\` mappé. La duplication est le signal le plus fiable qu'une abstraction manque.\n5. **Il a des props en cascade ou des conditions imbriquées** : simplifie, extrait, aplatis.\n\nLa règle qui guide chaque extraction : **une pièce (composant, hook, fonction) doit avoir UNE responsabilité qu'on peut nommer d'une phrase**. Si tu ne peux pas nommer ce que fait un composant sans « et » (« il affiche la liste ET gère le formulaire ET fetch »), il en fait trop. Le refactor n'ajoute AUCUNE fonctionnalité — il réorganise pour la lisibilité et la testabilité, et se fait sous la protection des tests (jours 106-108) qui garantissent qu'on n'a rien cassé.\n\nLa bascule : le code est lu bien plus souvent qu'écrit. Un composant clair fait gagner du temps à chaque personne qui le relira — toi dans un mois, un collègue, un recruteur qui lit ton repo. Refactorer pour la lisibilité n'est pas cosmétique : c'est un investissement mesurable.`,
    guided: `**Énoncé** : refactorer un composant trop gros — avant/après — par extraction.\n\n**Exemple simple** : la première extraction évidente — une section répétée devient un composant nommé. \`{livres.map(l => <CarteLivre key={l.id} livre={l} />)}\` remplace trois blocs de JSX copiés. Moins de lignes, un nom, réutilisable.\n\n**Raisonnement pas-à-pas** : repérer les smells (long, mélange logique/rendu, noms vagues, duplication) → extraire une pièce à la fois → renommer d'après l'intention → vérifier que les tests restent verts.\n\n**Avant / après** :\n\`\`\`tsx\n// AVANT : <Catalogue> fait TOUT (fetch, filtre, rendu, formatage) — 120 lignes, intestable.\n// APRÈS : responsabilités séparées, chaque pièce nommée et testable.\nfunction Catalogue() {\n  const { livres, erreur, chargement } = useLivres();        // logique à état → hook custom (jour 110)\n  const [q, setQ] = useState('');\n  const filtres = livres.filter((l) => matchTitre(l, q));     // fonction pure nommée\n  if (chargement) return <Spinner />;\n  if (erreur) return <MessageErreur erreur={erreur} />;       // sous-composant nommé\n  return (\n    <>\n      <BarreRecherche q={q} onChange={setQ} />                {/* une section = un composant */}\n      <ListeLivres livres={filtres} />\n    </>\n  );\n}\n\`\`\`\n\n**Mini-quiz** : (a) Quel est le signal le plus fiable qu'une abstraction manque ? (b) Où va la logique à état quand on refactore un gros composant ? (c) Un refactor doit-il ajouter des fonctionnalités ?\n→ (a) la duplication (JSX copié-collé) ; (b) dans un hook custom, pour que le composant ne garde que le rendu ; (c) non — il réorganise pour la lisibilité/testabilité sans changer le comportement, sous protection des tests.\n\n**Variante** : après refactor, chronométre le temps qu'un camarade (ou toi, à froid demain) met à comprendre le composant — la lisibilité se MESURE en temps de compréhension, pas en impression.`,
    caseStudy: `Le refactor pour la lisibilité est un travail quotidien en entreprise, car le code est lu bien plus qu'il n'est écrit : un composant clair fait gagner du temps à chaque développeur qui le touche, un composant fourre-tout en fait perdre à tout le monde et concentre les bugs. Les revues de code front rejettent régulièrement les composants de plusieurs centaines de lignes qui mélangent tout, précisément parce qu'ils sont impossibles à tester et à faire évoluer sans risque. Savoir découper un gros composant en pièces à responsabilité unique — et le faire sous la protection des tests — est une compétence d'ingénierie directement observable dans un repo, et donc évaluée par les recruteurs.`,
    interview: `**« Comment refactores-tu un composant devenu trop gros ? »** → Je le traite comme une fonction fourre-tout : je repère les signaux (trop long, mélange logique/rendu, noms vagues, JSX dupliqué) et j'EXTRAIS une pièce à la fois. La logique à état part dans un hook custom pour que le composant ne garde que le rendu ; les sections répétées ou distinctes deviennent des sous-composants nommés ; je renomme d'après l'intention. La règle : chaque pièce doit avoir UNE responsabilité nommable d'une phrase sans « et ». Le refactor n'ajoute aucune fonctionnalité — il réorganise pour la lisibilité et la testabilité, sous la protection des tests qui garantissent que le comportement n'a pas changé. Je le fais parce que le code est lu bien plus souvent qu'écrit.`,
    future: `- **Bankable** : découper un gros composant en pièces à responsabilité unique, sous protection des tests, est une compétence d'ingénierie directement lisible dans ton repo — donc évaluée par les recruteurs.\n- Ce réflexe garde BiblioApp maintenable pendant sa construction et son polish (jours 113-118) et prépare l'extraction de hooks (jour 110).\n- « Une pièce, une responsabilité nommable » et « le code est lu plus qu'écrit » sont des principes universels, du front au back en passant par les pipelines data.`,
    solution: {
      logic: "Solution simple : couper un gros composant en plus petits. Solution améliorée : diagnostiquer les code smells (longueur, mélange logique/rendu, noms vagues, duplication), extraire la logique à état dans un hook custom et les sections en sous-composants nommés, renommer d'après l'intention, et garantir par les tests que le comportement est inchangé (refactor = zéro fonctionnalité ajoutée). Le critère : chaque pièce a une responsabilité nommable d'une phrase sans « et ».",
      pitfalls: ["Extraire sans nommer d'après l'intention : des sous-composants \`Bloc1\`/\`Bloc2\` n'améliorent pas la lisibilité.", "Refactorer sans tests : on ne peut pas prouver que le comportement est resté identique.", "Laisser la logique à état dans le composant de rendu : il reste intestable et mélangé — extraire un hook.", "Ajouter des fonctionnalités pendant le refactor : on ne sait plus si une régression vient du refactor ou de la feature."],
      checks: ["Chaque composant/hook/fonction extrait a une responsabilité nommable d'une phrase.", "La logique à état est dans des hooks, le rendu dans les composants.", "Les noms expriment l'intention (pas data/temp/handleClick).", "Le JSX dupliqué est factorisé en composants mappés.", "Les tests restent verts : le comportement est inchangé."],
      oral: "Traite le composant comme une fonction : « trop long, mélange logique/rendu, noms vagues, duplication → j'extrais, une responsabilité par pièce ». Insiste sur « le refactor ne change rien au comportement, il se fait sous protection des tests » et « le code est lu plus qu'écrit ». Proposer de mesurer la lisibilité en temps de compréhension montre une vision d'ingénieur, pas d'esthète.",
    },
  },

  // ── Hooks personnalisés ──
  110: {
    lessons: ['react-hooks-effects.md', 'clean-code.md'],
    theory: `**Modèle mental : un hook personnalisé extrait une LOGIQUE À ÉTAT réutilisable (pas du JSX) — c'est la fonction réutilisable du clean code appliquée à la logique stateful de React.** Quand plusieurs composants répètent la même mécanique — charger des données avec les 3 états, lire/écrire dans le localStorage, gérer un formulaire — cette logique n'a pas sa place dupliquée dans chacun. Un hook custom (une fonction dont le nom commence par \`use\`) l'encapsule une fois, et chaque composant l'APPELLE. Le composant se recentre sur le rendu ; la logique vit dans le hook, testable et partagée.\n\nCe qu'un hook custom est et n'est pas :\n1. **C'est une fonction qui appelle d'autres hooks** (\`useState\`, \`useEffect\`) et renvoie ce dont le composant a besoin (des valeurs, des fonctions). \`useFetch(url)\` renvoie \`{ data, erreur, chargement }\` ; \`useLocalStorage(cle, defaut)\` renvoie \`[valeur, setValeur]\`.\n2. **Ce n'est PAS du partage de state** : deux composants qui appellent \`useFetch\` ont chacun leur PROPRE état — le hook partage la LOGIQUE, pas les données. (Pour partager des données, c'est Context, jour 101.)\n3. **Il obéit aux règles des hooks** : appelé au niveau racine du composant/hook (jamais dans une condition ou une boucle), et son nom commence par \`use\` pour que React et l'outillage le reconnaissent.\n\nLe bénéfice en cascade : un hook custom bien fait rend les composants MAIGRES (jour 109 : logique hors du rendu), TESTABLES (on teste le hook isolément, la logique n'est plus enfouie dans le JSX), et COHÉRENTS (tous les fetchs de l'app passent par le même \`useFetch\`, donc gèrent les 3 états de la même façon). C'est la même valeur qu'une bonne fonction utilitaire : écrite une fois, correcte partout.\n\nLa règle du jour : **extrais en hook custom une logique à état DÈS qu'elle se répète ou qu'elle alourdit un composant — nomme-le \`useX\`, fais-lui renvoyer une interface claire, et teste-le isolément**. Attention au sur-abstraction inverse : n'extrais pas un hook pour une logique utilisée à un seul endroit et triviale ; comme toute abstraction, un hook se justifie par la réutilisation ou la clarté gagnée, pas par principe.`,
    guided: `**Énoncé** : créer deux hooks custom typés et testés — \`useFetch\` et \`useLocalStorage\`.\n\n**Exemple simple** : l'interface d'un hook AVANT son corps — \`function useFetch<T>(url: string): { data?: T; erreur?: string; chargement: boolean }\`. Décider ce que le hook RENVOIE (son contrat) avant de l'implémenter guide tout le reste.\n\n**Raisonnement pas-à-pas** : identifier la logique répétée → l'extraire dans une fonction \`useX\` qui appelle les hooks de base → renvoyer une interface claire → tester le hook isolément.\n\n**Solution** :\n\`\`\`tsx\nfunction useFetch<T>(url: string) {\n  const [etat, setEtat] = useState<{ s: 'load' | 'err' | 'ok'; data?: T }>({ s: 'load' });\n  useEffect(() => {\n    let annule = false;\n    setEtat({ s: 'load' });\n    api.get<T>(url).then((d) => !annule && setEtat({ s: 'ok', data: d }))\n                   .catch(() => !annule && setEtat({ s: 'err' }));\n    return () => { annule = true; };                        // logique du jour 95, écrite UNE fois\n  }, [url]);\n  return etat;                                              // chaque appelant a SON propre état\n}\n\nfunction useLocalStorage<T>(cle: string, defaut: T) {\n  const [val, setVal] = useState<T>(() => {\n    const brut = localStorage.getItem(cle);\n    return brut ? JSON.parse(brut) : defaut;                // initialisation paresseuse\n  });\n  useEffect(() => { localStorage.setItem(cle, JSON.stringify(val)); }, [cle, val]);\n  return [val, setVal] as const;\n}\n\`\`\`\n\n**Mini-quiz** : (a) Deux composants qui appellent \`useFetch\` partagent-ils leur data ? (b) Pourquoi le nom doit-il commencer par \`use\` ? (c) Quand extraire un hook custom ?\n→ (a) non — chacun a son propre état ; le hook partage la LOGIQUE, pas les données (ça, c'est Context) ; (b) pour que React et le linter appliquent les règles des hooks et le reconnaissent ; (c) dès qu'une logique à état se répète ou alourdit un composant — pas pour du trivial à usage unique.\n\n**Variante** : écris un test du hook \`useLocalStorage\` (avec un localStorage mocké, jour 108) qui vérifie qu'il relit la valeur au montage — tu testes la LOGIQUE sans aucun composant autour, ce qui prouve qu'elle est bien isolée.`,
    caseStudy: `Les hooks personnalisés sont la façon standard de partager de la logique en React moderne, et toute base de code React d'entreprise en contient : \`useFetch\`, \`useAuth\`, \`useForm\`, \`useDebounce\`. Leur intérêt est double : ils suppriment la duplication (la logique de fetch écrite une fois, correcte partout) et rendent les composants maigres et testables. Les bibliothèques les plus populaires de l'écosystème (React Query, etc.) sont essentiellement des hooks custom sophistiqués. Savoir extraire un hook propre, typé et testé — et savoir quand NE pas sur-abstraire — est une compétence React attendue au-delà du niveau débutant, immédiatement visible dans la qualité d'un repo.`,
    interview: `**« Qu'est-ce qu'un hook personnalisé et quand en crées-tu un ? »** → C'est une fonction dont le nom commence par \`use\`, qui appelle d'autres hooks et encapsule une LOGIQUE À ÉTAT réutilisable — \`useFetch\`, \`useLocalStorage\`. J'en crée un dès qu'une logique stateful se répète entre composants ou alourdit un composant : je l'extrais, elle est écrite une fois et testable isolément, et le composant se recentre sur le rendu. Point clé : un hook partage la LOGIQUE, pas les données — deux composants qui appellent \`useFetch\` ont chacun leur propre état (pour partager des données, c'est Context). Il obéit aux règles des hooks (appelé au niveau racine, pas dans une condition). Comme toute abstraction, il se justifie par la réutilisation ou la clarté gagnée, pas par principe.`,
    future: `- **Bankable** : extraire un hook custom propre, typé et testé — et savoir quand ne pas sur-abstraire — est une compétence React attendue au-delà du débutant, visible dans la qualité du repo.\n- Ces hooks (useFetch, useLocalStorage) sont réutilisés partout dans BiblioApp (jours 113-118), garantissant une gestion cohérente des 3 états.\n- Le principe « extraire la logique réutilisable dans une abstraction nommée et testable » est le clean code (jour 109) appliqué au stateful — transférable à tout langage et paradigme.`,
    solution: {
      logic: "Solution simple : une fonction useX qui appelle useState/useEffect et renvoie ce qu'il faut. Solution améliorée : définir d'abord le contrat (ce que le hook renvoie), encapsuler la logique complète (les 3 états et le nettoyage pour useFetch, l'initialisation paresseuse et la persistance pour useLocalStorage), typer génériquement (<T>), respecter les règles des hooks, et tester le hook ISOLÉMENT. La preuve : deux composants réutilisent le hook avec chacun leur propre état, et la logique est testée sans composant autour.",
      pitfalls: ["Croire qu'un hook partage les DONNÉES : il partage la logique ; chaque appelant a son propre état (partage de données = Context).", "Extraire un hook pour une logique triviale à usage unique : sur-abstraction sans bénéfice.", "Violer les règles des hooks (appel conditionnel, hors du niveau racine) : comportement cassé et linter en alerte.", "Un hook qui renvoie une interface floue ou instable : les composants appelants deviennent compliqués — soigner le contrat de retour."],
      checks: ["Le nom commence par use et le hook respecte les règles des hooks.", "La logique à état répétée est encapsulée une fois (fetch/localStorage complets).", "Le hook est typé génériquement et renvoie une interface claire.", "Le hook est testé isolément (sans composant autour).", "Chaque composant appelant a son propre état (logique partagée, pas données)."],
      oral: "Définis le hook comme « la fonction réutilisable du clean code appliquée au stateful ». Le point qui prouve la compréhension : « il partage la LOGIQUE, pas les données — deux useFetch = deux états ; pour partager des données, c'est Context ». Mentionne les règles des hooks et le test isolé. Ajoute le garde-fou anti-abstraction : « je n'extrais que ce qui se répète ou clarifie ».",
    },
  },

  // ── Gestion d'erreur front robuste ──
  111: {
    lessons: ['error-handling.md'],
    theory: `**Modèle mental : une UI robuste ne se contente pas d'afficher les données — elle prévoit CHAQUE façon dont ça peut rater et dégrade PROPREMENT au lieu de montrer un écran blanc. Et il existe deux familles d'erreurs distinctes qui demandent deux parades différentes.** Depuis le jour 95 tu gères l'erreur d'un fetch (état 'error'). Aujourd'hui tu généralises : une application entière ne doit JAMAIS casser sans rien dire. Un écran blanc est le pire échec UX — l'utilisateur ne sait ni ce qui s'est passé ni quoi faire.\n\nLes deux familles d'erreurs et leurs parades :\n1. **Les erreurs ATTENDUES (asynchrones)** : un appel réseau échoue (500, timeout, hors ligne). Elles se gèrent avec l'état 'error' (jour 95) et un message + un bouton « réessayer ». Elles sont prévisibles : tu SAIS que le réseau peut tomber, donc tu codes le chemin d'erreur explicitement. Un \`try/catch\` autour de l'appel suffit.\n2. **Les erreurs INATTENDUES (exceptions de rendu)** : un composant lève une exception pendant son rendu (une propriété \`undefined\` lue, un bug). Un \`try/catch\` ne peut PAS les attraper — elles surviennent DANS le rendu de React, hors de ton flux d'exécution. C'est le rôle de l'**error boundary** : un composant spécial qui ENVELOPPE une partie de l'arbre, attrape toute exception de rendu de ses enfants, et affiche une UI de repli au lieu de laisser toute l'app planter en écran blanc.\n\nLes principes d'une app résiliente :\n- **Chaque zone risquée est enveloppée** d'une error boundary, avec une granularité réfléchie : une boundary par section importante (pas une seule pour toute l'app, sinon une erreur dans un widget tue tout l'écran).\n- **Chaque état non-heureux est traité** : loading, error, empty — partout, pas seulement sur l'écran principal.\n- **L'erreur offre une SORTIE** : réessayer, revenir, contacter — jamais un cul-de-sac.\n- **On PROVOQUE les erreurs pour tester la résilience** : casser volontairement (couper le réseau, forcer une exception) et vérifier que l'app dégrade proprement — la même démarche que le red teaming ou le sabotage des tests (jour 106).\n\nLa règle du jour : **une UI professionnelle ne peut pas montrer d'écran blanc ; toute erreur, attendue ou non, mène à un état lisible avec une issue**.`,
    guided: `**Énoncé** : rendre l'app résiliente — error boundary + états d'erreur soignés + retry, prouvé en cassant l'app.\n\n**Exemple simple** : l'error boundary attrape ce qu'un try/catch ne peut pas — une exception PENDANT le rendu. \`<ErrorBoundary fallback={<Repli/>}><SectionRisquée/></ErrorBoundary>\` : si \`SectionRisquée\` plante, l'utilisateur voit \`Repli\`, pas un écran blanc.\n\n**Raisonnement pas-à-pas** : distinguer erreur async (try/catch + état error) et exception de rendu (error boundary) → envelopper chaque section → offrir une sortie (réessayer) → PROVOQUER les deux pour vérifier.\n\n**Solution** :\n\`\`\`tsx\nclass ErrorBoundary extends React.Component<{ fallback: React.ReactNode; children: React.ReactNode }, { crash: boolean }> {\n  state = { crash: false };\n  static getDerivedStateFromError() { return { crash: true }; }   // attrape l'exception de rendu\n  componentDidCatch(err: Error) { console.error('Crash UI :', err); } // journalise (jour 111 → prod)\n  render() { return this.state.crash ? this.props.fallback : this.props.children; }\n}\n\n// Erreur ATTENDUE (async) : état 'error' + retry (jour 95)\nfunction Section() {\n  const etat = useFetch<Livre[]>('/livres');                       // hook du jour 110\n  if (etat.s === 'err') return <MessageErreur onRetry={/* relance */ () => {}} />;\n  /* ... */\n}\n// Erreur INATTENDUE (rendu) : enveloppée par la boundary\n// <ErrorBoundary fallback={<Repli/>}><Section/></ErrorBoundary>\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi un try/catch ne suffit-il pas pour toutes les erreurs React ? (b) Quelle granularité pour les error boundaries ? (c) Comment prouver que l'app est résiliente ?\n→ (a) il n'attrape pas les exceptions levées PENDANT le rendu de React — c'est le rôle de l'error boundary ; (b) une par section importante, pas une seule pour toute l'app (sinon un widget cassé tue tout l'écran) ; (c) en cassant volontairement (couper le réseau, forcer une exception) et en vérifiant la dégradation propre.\n\n**Variante** : ajoute un composant qui lève une exception au clic (\`throw new Error()\`) dans une section, et vérifie que SEULE cette section montre le repli, le reste de l'app restant utilisable — la preuve d'une bonne granularité.`,
    caseStudy: `L'écran blanc — l'app qui plante entièrement à cause d'une exception dans un composant — est un incident de production classique et coûteux : une seule propriété \`undefined\` non prévue peut rendre toute l'interface inutilisable. Les error boundaries, placées par section, contiennent le dégât : un widget cassé affiche un repli local, le reste de l'app continue de fonctionner. Les équipes sérieuses journalisent aussi ces crashs (componentDidCatch → outil de suivi) pour les corriger. Combiné au traitement systématique des états loading/error/empty et à une sortie sur chaque erreur, cela distingue une app « qui marche en démo » d'une app « qui tient en production ». Cette robustesse est directement observable et valorisée.`,
    interview: `**« Comment rends-tu une application React robuste aux erreurs ? »** → Je distingue deux familles. Les erreurs ATTENDUES (async : réseau qui échoue) se gèrent avec un état 'error' explicite, un message et un bouton réessayer — je code le chemin d'erreur car je SAIS que le réseau peut tomber. Les erreurs INATTENDUES (exceptions levées pendant le rendu) ne peuvent PAS être attrapées par un try/catch ; j'utilise des error boundaries — des composants qui enveloppent une section, attrapent l'exception de ses enfants et affichent un repli au lieu d'un écran blanc. Je place une boundary par section importante (granularité) pour qu'un widget cassé ne tue pas tout l'écran, je journalise les crashs, et je PROUVE la résilience en cassant volontairement l'app. Règle : jamais d'écran blanc, toute erreur mène à un état lisible avec une issue.`,
    future: `- **Bankable** : une app sans écran blanc (error boundaries par section + états d'erreur soignés + sorties) est un marqueur de robustesse directement observable — la différence entre « marche en démo » et « tient en production ».\n- Cette résilience protège chaque écran de BiblioApp (polish, jour 117) et de tes futures apps.\n- La distinction « erreur attendue vs inattendue » et la posture « provoquer pour prouver la résilience » se transfèrent à tout système : back-end, pipelines, agents IA (dégradation propre plutôt que crash).`,
    solution: {
      logic: "Solution simple : un try/catch autour des appels et un état d'erreur. Solution améliorée : distinguer les deux familles — erreurs async (état 'error' + retry) et exceptions de rendu (error boundaries) qu'un try/catch ne peut pas attraper ; placer une boundary par section (granularité) pour contenir les crashs, journaliser via componentDidCatch, offrir une sortie sur chaque erreur, et PROUVER la résilience en cassant volontairement l'app. La preuve : une section qui plante affiche un repli local, le reste reste utilisable.",
      pitfalls: ["Croire qu'un try/catch attrape les exceptions de rendu : il ne le fait pas — c'est le rôle des error boundaries.", "Une seule error boundary pour toute l'app : un widget cassé fait tomber tout l'écran — une par section.", "Laisser des culs-de-sac (erreur sans bouton réessayer/retour) : l'utilisateur est bloqué.", "Ne pas provoquer les erreurs : sans les tester volontairement, on ne sait pas si la dégradation est réellement propre."],
      checks: ["Les erreurs async ont un état 'error' avec message et retry.", "Les exceptions de rendu sont attrapées par des error boundaries.", "La granularité des boundaries contient les crashs (une par section).", "Chaque état d'erreur offre une sortie (réessayer/retour).", "La résilience est prouvée en cassant volontairement l'app (réseau coupé, exception forcée)."],
      oral: "Structure ta réponse en deux familles : « attendues (async → état error + retry) et inattendues (exceptions de rendu → error boundary, car try/catch ne les attrape pas) ». Insiste sur la granularité (une boundary par section) et la preuve par sabotage (couper le réseau, forcer une exception). « Jamais d'écran blanc » est la formule qui résume une pensée de robustesse mûre.",
    },
  },

  // ── Revue semaine 16 : tests et ingénierie front ──
  112: {
    reviewSynthese: `La semaine 16 a transformé « du code qui marche » en « du code de qualité, prouvé et maintenable » : tests unitaires Vitest qui rougissent quand on sabote le code (jour 106), tests de composants orientés comportement et non implémentation (107), mocks de la frontière réseau et tests d'intégration des flux (108), refactor de composants trop gros par extraction à responsabilité unique (109), hooks personnalisés pour partager la logique à état (110), et gestion d'erreur robuste sans écran blanc — error boundaries et états dégradés (111). Le fil rouge : **un front professionnel se PROUVE (des tests qui peuvent échouer), se LIT (des pièces à responsabilité unique) et ne CASSE jamais silencieusement (toute erreur mène à un état lisible)**. Tu abordes la semaine 17 (Projet 3, BiblioApp) avec de quoi construire une app testée, propre et résiliente — pas seulement fonctionnelle.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Écrire des tests unitaires qui rougissent quand on sabote le code | |\n| Tester un composant par son comportement (rôle/label/texte), pas son implémentation | |\n| Mocker la bonne frontière et écrire un test d'intégration d'un flux | |\n| Refactorer un gros composant en pièces à responsabilité unique | |\n| Extraire une logique à état dans un hook personnalisé testé | |\n| Rendre une app résiliente (error boundaries, états d'erreur, sorties) | |\n\n**Total /18** : ≥ 15 = solide, attaque BiblioApp · 11-14 = revoir les items ≤ 1 · < 11 = rejouer avant le projet, car tu construiras BiblioApp AVEC ces pratiques.`,
    remediation: `- **Tests décoratifs** (qui ne peuvent pas rougir) : rejoue le jour 106 — sabote le code pour prouver que chaque test échoue ; couvre les cas limites, pas seulement le cas heureux.\n- **Tests couplés à l'implémentation** : jour 107 — cherche par rôle/label/texte, affirme sur le DOM visible ; un refactor interne ne doit pas casser tes tests.\n- **Sur-mock ou vrais appels réseau** : jour 108 — mocke uniquement la frontière externe ; teste en vrai ton code interne ; un test d'intégration attrape les bugs d'assemblage.\n- **Composants fourre-tout** : jour 109 — extrais logique (hooks) et sections (sous-composants), une responsabilité nommable par pièce, sous protection des tests.\n- **Logique dupliquée** : jour 110 — extrais un hook custom (partage la logique, pas les données), testé isolément.\n- **Écran blanc possible** : jour 111 — error boundaries par section (try/catch n'attrape pas les exceptions de rendu), états d'erreur avec sortie ; prouve en cassant l'app.`,
    interview: `1. **« Qu'est-ce qui prouve la valeur d'un test unitaire ? »** Attendu : sa capacité à ROUGIR quand on sabote le code ; AAA, déterminisme, couverture des cas limites ; un test toujours vert ne protège rien.\n2. **« Tester le comportement vs l'implémentation ? »** Attendu : chercher par rôle/label/texte, affirmer sur le DOM visible ; un test d'implémentation casse au refactor sans attraper de bug.\n3. **« Que mockes-tu, que laisses-tu réel ? »** Attendu : mocker la frontière externe (réseau, temps) pour le déterminisme et les cas d'erreur rares ; ne jamais mocker le code testé ; l'intégration attrape les bugs d'assemblage.\n4. **« Quand extraire un hook personnalisé ? »** Attendu : dès qu'une logique à état se répète ou alourdit un composant ; il partage la logique pas les données ; testé isolément.\n5. **« Comment évites-tu l'écran blanc ? »** Attendu : error boundaries par section pour les exceptions de rendu (try/catch ne les attrape pas), états d'erreur avec sortie, résilience prouvée en cassant l'app.`,
  },

  // ── Projet 3 — BiblioApp : socle ──
  113: {
    lessons: ['react-fundamentals.md', 'architecture-basics.md'],
    theory: `**Modèle mental : le socle d'un projet, c'est le SQUELETTE MARCHANT — la plus petite version navigable de bout en bout qui PROUVE que l'architecture tient, avant d'ajouter la moindre fonctionnalité.** Tu ne commences pas BiblioApp par le formulaire le plus complexe ; tu commences par le chemin le plus fin qui traverse toutes les couches : afficher la liste des livres (depuis ton API via la couche api), cliquer sur un livre, voir son détail, revenir. Si ce squelette marche, tu as validé que le routing (jour 99), la couche api (jour 97) et l'architecture d'état (jour 104) fonctionnent ENSEMBLE — le reste n'est que du remplissage de ce cadre éprouvé.\n\nPourquoi commencer par le squelette marchant (« walking skeleton ») :\n1. **Il valide l'assemblage tôt** : les bugs les plus coûteux sont ceux d'intégration entre couches (le front n'arrive pas à parler à l'API, le routing casse le chargement). Les découvrir sur un squelette de 50 lignes est trivial ; les découvrir après avoir codé dix écrans est un cauchemar.\n2. **Il donne un fil de bout en bout** : chaque fonctionnalité suivante (CRUD, recherche) s'accroche à un squelette qui marche déjà, au lieu de flotter dans le vide.\n3. **Il applique ton cadrage** (jour 104) : le socle est le premier incrément du backlog, celui qui met en place l'arbre de composants et le plan d'état décidés.\n\nLa discipline des COMMITS ATOMIQUES accompagne le socle : chaque commit est un pas COHÉRENT et réversible (« ajoute le routing », « branche la liste sur l'api », « ajoute la vue détail »), avec un message qui dit le POURQUOI. Un historique de commits atomiques raconte la construction du projet, permet de revenir en arrière proprement, et est LU par les recruteurs qui regardent ton GitHub. À l'inverse, un commit géant « BiblioApp » de 2000 lignes est impossible à relire et signale une absence de méthode.\n\nLa règle du jour : **construis d'abord le chemin le plus fin qui traverse toute l'architecture, commit par commit atomique — le socle marchant est la fondation sur laquelle tout le reste s'accroche**.`,
    guided: `**Énoncé** : le socle navigable de BiblioApp — liste, détail, navigation, couche api — en commits atomiques.\n\n**Exemple simple** : le squelette le plus fin tient en une phrase de code — la liste chargée depuis l'api et rendue. \`const etat = useFetch<Livre[]>('/livres')\` puis rendre les 3 états. Si ça marche, la couche api + le hook + le rendu sont validés ensemble.\n\n**Raisonnement pas-à-pas** : mettre en place le routing → brancher la liste sur l'api (3 états) → ajouter la vue détail par id d'URL → vérifier la navigation aller-retour, un commit atomique par étape.\n\n**Structure du socle** :\n\`\`\`tsx\n// Commit 1 : \"socle: routing liste/détail/404\"\n<Routes>\n  <Route path=\"/livres\" element={<Liste />} />\n  <Route path=\"/livres/:id\" element={<Detail />} />\n  <Route path=\"*\" element={<PageIntrouvable />} />\n</Routes>\n// Commit 2 : \"socle: liste branchée sur l'API (3 états)\"\nfunction Liste() {\n  const etat = useFetch<Livre[]>('/livres');                 // couche api (jour 97) + hook (jour 110)\n  if (etat.s !== 'ok') return <EtatChargementOuErreur etat={etat} />;\n  return <ul>{etat.data.map((l) => <li key={l.id}><Link to={\`/livres/\${l.id}\`}>{l.titre}</Link></li>)}</ul>;\n}\n// Commit 3 : \"socle: vue détail par id d'URL\"\nfunction Detail() { const { id } = useParams(); const etat = useFetch<Livre>(\`/livres/\${id}\`); /* ... */ }\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi commencer par un squelette marchant plutôt que par la feature la plus riche ? (b) Qu'est-ce qu'un commit atomique ? (c) Que valide le socle en traversant toutes les couches ?\n→ (a) pour découvrir tôt les bugs d'intégration entre couches, quand ils sont triviaux à corriger ; (b) un pas cohérent et réversible, avec un message qui dit le pourquoi ; (c) que routing + couche api + architecture d'état marchent ENSEMBLE.\n\n**Variante** : après le socle, vérifie qu'ouvrir directement l'URL \`/livres/3\` dans un onglet neuf charge la fiche (l'id vient de l'URL, jour 99) — c'est le test qui prouve que ton architecture de navigation est saine.`,
    caseStudy: `Le « walking skeleton » est une pratique reconnue de démarrage de projet : construire d'abord le chemin le plus fin de bout en bout pour valider l'architecture avant d'investir dans les fonctionnalités. Les équipes qui le font découvrent les problèmes d'intégration (front↔API, configuration, déploiement) alors qu'ils sont faciles à régler ; celles qui construisent feature par feature sans squelette découvrent au pire moment que les couches ne s'assemblent pas. Couplé aux commits atomiques — lus par les recruteurs sur GitHub comme un récit de la construction —, cette approche signale une méthode de développement professionnelle, bien au-delà du « ça finit par marcher ».`,
    interview: `**« Comment démarres-tu la construction d'une application ? »** → Par un squelette marchant : la plus petite version navigable de bout en bout qui traverse toutes les couches — pour BiblioApp, afficher la liste depuis l'API, aller au détail, revenir. Ça valide TÔT que le routing, la couche api et l'architecture d'état fonctionnent ensemble, quand les bugs d'intégration sont triviaux à corriger. Ensuite chaque fonctionnalité s'accroche à ce squelette qui marche déjà. Je travaille en commits ATOMIQUES : chaque commit est un pas cohérent et réversible, avec un message qui dit le pourquoi — un historique lisible que je peux dérouler et qu'un recruteur peut lire. Je préfère ça au commit géant impossible à relire et à réverter.`,
    future: `- **Bankable** : démarrer par un squelette marchant et travailler en commits atomiques signale une méthode de développement professionnelle — visible dans ton historique GitHub que les recruteurs lisent.\n- Ce socle est la fondation sur laquelle s'accrochent le CRUD (114), la recherche (115), les tests (116) et le polish (117) de BiblioApp.\n- « Valider l'architecture de bout en bout avant d'investir dans les features » est une pratique transférable à tout projet — API, pipeline data, application IA.`,
    solution: {
      logic: "Solution simple : coder la liste et le détail. Solution améliorée : construire le chemin le plus FIN de bout en bout (routing + liste depuis l'api avec 3 états + détail par id d'URL) qui valide l'assemblage des couches AVANT toute feature, en commits atomiques dont chaque message dit le pourquoi, en réutilisant la couche api (97), le hook useFetch (110) et l'architecture d'état cadrée (104). La preuve : ouvrir directement l'URL d'une fiche la charge (id depuis l'URL).",
      pitfalls: ["Commencer par la fonctionnalité la plus riche : les bugs d'intégration entre couches se découvrent tard et coûtent cher.", "Un commit géant « BiblioApp » : illisible, irréversible, signale une absence de méthode aux recruteurs.", "Refaire des fetchs à la main dans chaque composant au lieu de réutiliser la couche api et useFetch : incohérence et duplication.", "Négliger le 404 et les 3 états dès le socle : l'architecture de navigation n'est pas réellement validée."],
      checks: ["Le socle est navigable de bout en bout (liste → détail → retour).", "La liste et le détail passent par la couche api et gèrent les 3 états.", "Chaque étape est un commit atomique avec un message expliquant le pourquoi.", "Ouvrir l'URL d'une fiche dans un onglet neuf la charge (id depuis l'URL).", "Le socle applique l'arbre de composants et le plan d'état du cadrage (jour 104)."],
      oral: "Explique le walking skeleton : « je construis le chemin le plus fin qui traverse toutes les couches pour valider l'architecture tôt, quand les bugs d'intégration sont triviaux ». Décris les commits atomiques comme un récit lisible de la construction. Le test « ouvrir /livres/3 dans un onglet neuf » prouve concrètement que ton architecture de navigation est saine.",
    },
  },

  // ── Projet 3 — CRUD complet ──
  114: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : le CRUD, c'est le cycle de vie complet d'une ressource (Créer, Lire, Modifier, Supprimer) — et chaque ÉCRITURE combine trois réflexes déjà acquis : formulaire contrôlé validé (jour 96), mise à jour d'état IMMUABLE (jour 93), appel via la couche api (jour 97). Tu n'apprends rien de neuf ; tu ASSEMBLES.** Sur le socle marchant (jour 113, le « R » de lire), tu ajoutes maintenant les trois opérations d'écriture. Chacune suit le même patron : l'utilisateur agit (formulaire ou bouton) → validation → appel api → mise à jour immuable de l'état local → l'UI reflète le changement.\n\nCe que chaque écriture exige de spécifique :\n1. **Créer (C)** : un formulaire contrôlé validé (jour 96) qui POST vers l'api ; à la réussite, ajouter le nouvel élément à l'état (\`[...livres, nouveau]\`) et rediriger ou confirmer.\n2. **Modifier (U)** : le MÊME formulaire, pré-rempli avec les valeurs existantes (initialiser le state du formulaire depuis la ressource) ; un PUT/PATCH ; à la réussite, remplacer l'élément dans l'état (\`livres.map(l => l.id === id ? modifié : l)\`). Réutiliser le formulaire pour créer ET modifier est un signe de bonne conception.\n3. **Supprimer (D)** : une action DESTRUCTRICE, donc à CONFIRMER (« Confirmer la suppression ? ») — jamais un clic direct qui détruit sans filet ; un DELETE ; à la réussite, retirer l'élément (\`livres.filter(l => l.id !== id)\`).\n\nLe point de vigilance transversal : **garder l'état local COHÉRENT avec le serveur après chaque écriture, immuablement**. Deux stratégies — soit re-fetcher la liste après l'écriture (simple, sûr, un aller-retour de plus), soit mettre à jour l'état local en miroir de ce que le serveur a fait (plus réactif, mais il faut gérer l'échec : si l'api rejette, ne pas laisser l'UI mentir). Le débutant oublie souvent ce cas : l'UI affiche « supprimé » alors que le serveur a refusé.\n\nLa règle du jour : **chaque opération d'écriture valide, appelle l'api, met à jour l'état immuablement, et gère son échec ; la suppression se confirme toujours**. Le CRUD complet est le cœur fonctionnel de BiblioApp — et le patron de la quasi-totalité des applications de gestion.`,
    guided: `**Énoncé** : le CRUD complet de BiblioApp — ajout/édition validés, suppression confirmée, état immuable.\n\n**Exemple simple** : les trois écritures immuables tiennent en trois expressions — ajouter \`[...livres, n]\`, modifier \`livres.map(l => l.id===id?m:l)\`, supprimer \`livres.filter(l => l.id!==id)\`. Le CRUD d'état, c'est ces trois lignes appliquées après chaque appel api réussi.\n\n**Raisonnement pas-à-pas** : réutiliser le formulaire contrôlé pour créer ET modifier → appeler l'api → mettre à jour l'état immuablement à la réussite → confirmer avant de supprimer → gérer l'échec.\n\n**Solution** :\n\`\`\`tsx\nasync function creer(l: NouveauLivre) {\n  const cree = await api.creerLivre(l);                     // POST (jour 97)\n  setLivres((xs) => [...xs, cree]);                         // ajout IMMUABLE\n}\nasync function modifier(id: number, patch: Partial<Livre>) {\n  const maj = await api.majLivre(id, patch);                // PUT\n  setLivres((xs) => xs.map((l) => (l.id === id ? maj : l))); // remplacement IMMUABLE\n}\nasync function supprimer(id: number) {\n  if (!confirm('Confirmer la suppression ?')) return;        // action destructrice → CONFIRMÉE\n  try {\n    await api.supprimerLivre(id);                            // DELETE\n    setLivres((xs) => xs.filter((l) => l.id !== id));        // retrait IMMUABLE\n  } catch { alert('Suppression refusée par le serveur'); }  // l'UI ne ment pas si l'api rejette\n}\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi réutiliser le même formulaire pour créer et modifier ? (b) Pourquoi confirmer une suppression ? (c) Que se passe-t-il si on met à jour l'UI sans gérer l'échec de l'api ?\n→ (a) c'est le même formulaire de saisie validé, seule l'initialisation et le verbe HTTP changent — moins de code, cohérence ; (b) c'est destructeur et irréversible côté utilisateur — jamais un clic direct sans filet ; (c) l'UI « ment » : elle affiche un changement que le serveur a refusé.\n\n**Variante** : pré-remplis le formulaire d'édition en initialisant son state depuis la ressource chargée (\`useState(livre)\`) — le même composant sert alors à l'ajout (state vide) et à l'édition (state pré-rempli).`,
    caseStudy: `Le CRUD est le cœur de la quasi-totalité des applications de gestion en entreprise (clients, commandes, stocks, utilisateurs) : maîtriser ce patron — formulaire réutilisé, écriture immuable, suppression confirmée, échec géré — c'est savoir construire la brique la plus répandue du métier. Les bugs récurrents que les revues attrapent : l'UI qui « ment » (affiche un changement que le serveur a rejeté), la suppression sans confirmation qui détruit des données par mégarde, et l'état muté qui désynchronise l'affichage. Un CRUD propre, cohérent avec le serveur et sûr pour les actions destructrices, est un livrable que tout recruteur sait évaluer — et qui prouve que tu sais assembler des acquis en une fonctionnalité complète.`,
    interview: `**« Comment implémentes-tu un CRUD complet côté front ? »** → Chaque opération assemble trois réflexes : formulaire contrôlé validé, mise à jour d'état IMMUABLE, appel via la couche api. Créer : POST puis ajout immuable (\`[...xs, cree]\`). Modifier : le MÊME formulaire pré-rempli, PUT, puis remplacement (\`map\`). Supprimer : une CONFIRMATION obligatoire (action destructrice), DELETE, puis retrait (\`filter\`). Le point de vigilance : garder l'état local cohérent avec le serveur — soit re-fetcher, soit mettre à jour en miroir en gérant l'ÉCHEC (si l'api rejette, ne pas laisser l'UI afficher un changement qui n'a pas eu lieu). Réutiliser le formulaire pour créer et modifier est un signe de bonne conception.`,
    future: `- **Bankable** : un CRUD propre (formulaire réutilisé, écriture immuable, suppression confirmée, échec géré) est la brique la plus répandue des applications de gestion — un livrable que tout recruteur sait évaluer.\n- Il complète le socle de BiblioApp (113) et sert de base à la recherche/filtres (115) et aux tests (116).\n- Le patron « valider → appeler l'api → mettre à jour immuablement → gérer l'échec » se réapplique à toute écriture de données, dans le front comme dans les back-ends et pipelines.`,
    solution: {
      logic: "Solution simple : des fonctions creer/modifier/supprimer qui appellent l'api. Solution améliorée : réutiliser le formulaire contrôlé validé pour créer ET modifier (initialisation différente, verbe HTTP différent), mettre à jour l'état IMMUABLEMENT à chaque réussite (spread/map/filter), CONFIRMER toute suppression, et gérer l'ÉCHEC de l'api pour que l'UI ne mente jamais (rollback ou message). La preuve : une écriture rejetée par le serveur ne modifie pas l'affichage.",
      pitfalls: ["UI qui « ment » : mettre à jour l'état sans gérer l'échec de l'api affiche un changement que le serveur a refusé.", "Suppression sans confirmation : destruction de données par un clic accidentel.", "Muter l'état après écriture (push/splice) au lieu de spread/map/filter : désynchronisation de l'affichage.", "Dupliquer deux formulaires quasi identiques pour créer et modifier au lieu d'en réutiliser un seul : plus de code, incohérences."],
      checks: ["Les quatre opérations (créer, lire, modifier, supprimer) fonctionnent.", "Le formulaire contrôlé validé est réutilisé pour créer et modifier.", "Chaque écriture met à jour l'état immuablement (spread/map/filter).", "La suppression est confirmée avant exécution.", "L'échec d'un appel api est géré : l'UI ne reflète jamais un changement refusé."],
      oral: "Présente le CRUD comme un ASSEMBLAGE : « formulaire validé + écriture immuable + couche api, décliné en C/U/D ». Insiste sur les deux points de maturité : la suppression confirmée (action destructrice) et l'UI qui ne ment pas (gérer l'échec de l'api). Mentionner le formulaire réutilisé pour créer et modifier montre que tu penses conception, pas copier-coller.",
    },
  },

  // ── Projet 3 — Recherche et filtres ──
  115: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : la recherche et les filtres sont de l'état DÉRIVÉ — on ne stocke JAMAIS la liste filtrée, on la CALCULE à partir des données brutes + des critères courants ; et des filtres combinables ne sont qu'une COMPOSITION de prédicats.** C'est l'application directe de « ce qui se calcule ne se stocke pas » (jour 93) à un cas riche. Le state contient les CRITÈRES (le texte recherché, les filtres actifs) ; la liste affichée se dérive à chaque rendu en appliquant ces critères aux données. Stocker la liste filtrée en state serait une seconde source de vérité à resynchroniser sans cesse — la garantie de bugs.\n\nLa structure d'une recherche/filtres propre :\n1. **Le state = les critères, pas les résultats** : \`q\` (texte), \`filtres\` (genre, disponibilité...). Ce sont les seules choses qui « changent » ; le reste se déduit.\n2. **Les résultats = une dérivation** : \`livres.filter(l => matchTexte(l, q) && matchFiltres(l, filtres))\`. Des filtres COMBINABLES = un ET de prédicats indépendants ; ajouter un filtre = ajouter un prédicat, sans toucher aux autres. Cette composition rend le système extensible.\n3. **Les états edge de la recherche** : le cas VIDE (« aucun résultat pour 'xyz' ») est différent du cas « aucune donnée » — une recherche qui ne trouve rien doit le dire clairement, avec le terme cherché, et idéalement offrir de réinitialiser les filtres.\n\nLa performance, avec mesure (jour 102) : sur de grandes listes, filtrer à chaque frappe peut ramer. La parade mesurée est le DEBOUNCE (attendre que l'utilisateur arrête de taper avant de filtrer) — mais on ne l'ajoute que si le Profiler montre un vrai problème ; sur une petite liste, filtrer en direct est parfait et le debounce serait de la complexité inutile.\n\nLa règle du jour : **stocke les critères, dérive les résultats ; compose les filtres comme des prédicats indépendants ; traite le cas 'aucun résultat' explicitement**. Cette approche garde la recherche prévisible (une seule source de vérité), extensible (un filtre de plus = un prédicat de plus) et lisible.`,
    guided: `**Énoncé** : barre de recherche + filtres combinables + états vides soignés, en état dérivé.\n\n**Exemple simple** : la dérivation est le cœur — \`const resultats = livres.filter(l => l.titre.toLowerCase().includes(q.toLowerCase()))\`. La liste affichée n'existe pas en state : elle se recalcule à chaque frappe. Ajouter un filtre, c'est ajouter une condition à ce \`filter\`.\n\n**Raisonnement pas-à-pas** : stocker les critères en state → dériver les résultats en composant des prédicats → gérer le cas « aucun résultat » distinct du cas « aucune donnée » → mesurer avant d'optimiser.\n\n**Solution** :\n\`\`\`tsx\nfunction Catalogue({ livres }: { livres: Livre[] }) {\n  const [q, setQ] = useState('');\n  const [dispoSeule, setDispoSeule] = useState(false);       // critères en STATE\n\n  const matchTexte = (l: Livre) => l.titre.toLowerCase().includes(q.toLowerCase());\n  const matchDispo = (l: Livre) => (dispoSeule ? l.dispo : true);\n  const resultats = livres.filter((l) => matchTexte(l) && matchDispo(l)); // DÉRIVÉ : prédicats composés\n\n  return (\n    <>\n      <input value={q} onChange={(e) => setQ(e.target.value)} placeholder=\"Rechercher un titre\" />\n      <label><input type=\"checkbox\" checked={dispoSeule} onChange={(e) => setDispoSeule(e.target.checked)} /> Disponibles seulement</label>\n      {resultats.length === 0\n        ? <p>Aucun livre ne correspond à « {q || 'ces filtres'} ». <button onClick={() => { setQ(''); setDispoSeule(false); }}>Réinitialiser</button></p>\n        : <ListeLivres livres={resultats} />}\n    </>\n  );\n}\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi ne pas stocker la liste filtrée en state ? (b) Comment rend-on des filtres combinables ? (c) Quand ajouter un debounce ?\n→ (a) ce serait une seconde source de vérité à resynchroniser — on la dérive des critères ; (b) en composant des prédicats indépendants par un ET (\`match1 && match2\`) ; (c) seulement si le Profiler montre un vrai ralentissement sur une grande liste — sinon c'est de la complexité inutile.\n\n**Variante** : ajoute un troisième filtre (par genre) — tu dois pouvoir le faire en ajoutant UN prédicat au \`filter\`, sans toucher aux autres. Si ça t'oblige à réécrire la logique, c'est que tes filtres ne sont pas bien composés.`,
    caseStudy: `La recherche et le filtrage sont attendus dans presque toute application affichant des données (catalogues, back-offices, tableaux de bord), et la façon de les implémenter distingue immédiatement le niveau : stocker la liste filtrée en state (et la resynchroniser à la main) est le bug de conception classique du débutant, source de désynchronisations ; dériver les résultats des critères est l'approche propre et extensible. La composition en prédicats indépendants permet d'ajouter des filtres sans casser l'existant — exactement ce qu'exige une fonctionnalité qui grossit avec le temps. Savoir dériver plutôt que stocker, et composer des filtres, est un marqueur de conception d'état sain que les revues valorisent.`,
    interview: `**« Comment implémentes-tu une recherche avec filtres combinables ? »** → Je stocke les CRITÈRES en state (le texte, les filtres actifs), jamais la liste filtrée : celle-ci est de l'état DÉRIVÉ, recalculé à chaque rendu en appliquant les critères aux données. Stocker les résultats créerait une seconde source de vérité à resynchroniser — un nid à bugs. Les filtres combinables sont une COMPOSITION de prédicats indépendants reliés par un ET : ajouter un filtre = ajouter un prédicat, sans toucher aux autres, donc le système reste extensible. Je traite le cas « aucun résultat » explicitement (avec le terme cherché et un moyen de réinitialiser), distinct du cas « aucune donnée ». Le debounce, je ne l'ajoute que si le Profiler prouve un ralentissement — pas par principe.`,
    future: `- **Bankable** : dériver les résultats des critères (au lieu de stocker la liste filtrée) et composer des filtres en prédicats est un marqueur de conception d'état sain — valorisé en revue et en test technique.\n- Cette recherche complète BiblioApp (113-114) et sera testée au jour 116 ; le patron sert à toute UI de catalogue ou de tableau de bord.\n- « Stocker les critères, dériver les résultats » et « composer des prédicats indépendants » se transfèrent au filtrage de données côté back-end et data (requêtes composables).`,
    solution: {
      logic: "Solution simple : filtrer la liste selon le texte recherché. Solution améliorée : stocker uniquement les CRITÈRES en state et DÉRIVER les résultats à chaque rendu, composer les filtres comme des prédicats indépendants reliés par un ET (extensible sans toucher à l'existant), traiter le cas « aucun résultat » distinctement du cas « aucune donnée » (avec le terme et une réinitialisation), et n'ajouter un debounce que si le Profiler prouve un ralentissement. La preuve : ajouter un filtre ne demande qu'un prédicat de plus.",
      pitfalls: ["Stocker la liste filtrée en state : seconde source de vérité à resynchroniser, désynchronisations garanties.", "Filtres entremêlés impossibles à étendre : ajouter un critère oblige à réécrire la logique — composer des prédicats indépendants.", "Confondre « aucun résultat de recherche » et « aucune donnée » : messages différents, l'un doit montrer le terme cherché et offrir de réinitialiser.", "Ajouter un debounce sans mesurer : complexité inutile sur une petite liste où le filtrage direct est instantané."],
      checks: ["Le state ne contient que les critères, pas les résultats.", "Les résultats sont dérivés des données + critères à chaque rendu.", "Les filtres sont composés en prédicats indépendants (extensibles).", "Le cas « aucun résultat » est traité explicitement avec réinitialisation.", "Le debounce n'est présent que si une mesure le justifie."],
      oral: "Formule le principe : « je stocke les critères, je dérive les résultats — jamais l'inverse ». Explique la composition de prédicats indépendants (\`match1 && match2\`) qui rend les filtres extensibles. Cite le piège du débutant (stocker la liste filtrée) et la règle du debounce (seulement si mesuré). Ajouter un filtre en une ligne est la démonstration que ta conception est saine.",
    },
  },

  // ── Projet 3 — Tests ──
  116: {
    lessons: ['testing-foundations.md'],
    theory: `**Modèle mental : tester un projet réel, ce n'est PAS tout couvrir — c'est protéger les PARCOURS CRITIQUES (ceux dont la casse coûte le plus) avec le bon type de test pour chaque chose. On ne vise pas 100 % de couverture, on vise la confiance là où elle compte.** BiblioApp a maintenant une logique (validation, recherche/filtres) et des composants (formulaires, listes, CRUD). Les tester tous à l'identique serait du gaspillage ; les tester INTELLIGEMMENT, c'est appliquer à chaque chose le test adapté, sur les parcours qui comptent vraiment.\n\nLa stratégie de test d'un projet (la « pyramide » appliquée) :\n1. **Logique métier → tests unitaires** (jour 106) : la validation d'un livre, la composition des filtres (jour 115), les calculs. Rapides, nombreux, ils couvrent les cas limites. C'est la base de la pyramide.\n2. **Composants → tests de comportement** (jour 107) : le formulaire affiche ses erreurs, la liste rend les résultats, la suppression demande confirmation. Testés par rôle/label/texte, pas par implémentation.\n3. **Flux critiques → tests d'intégration** (jour 108) : « créer un livre → il apparaît dans la liste », « supprimer → il disparaît », avec l'api MOCKÉE. Ces tests attrapent les bugs d'ASSEMBLAGE entre formulaire, api et affichage — les plus coûteux.\n\nComment choisir QUOI tester en priorité : les parcours dont l'échec fait le plus mal. Pour BiblioApp : créer un livre (cœur métier), supprimer (destructeur — un bug ici perd des données), rechercher (le plus utilisé). Un bug sur ces parcours ruine l'app ; un bug sur un détail cosmétique, non. Tester d'abord le critique donne le maximum de confiance pour l'effort.\n\nLes tests protègent aussi le POLISH à venir (jour 117) et tout refactor futur : une suite verte te dit que tes changements n'ont rien cassé. C'est ce qui rend un projet MODIFIABLE sans peur — la vraie valeur d'une suite de tests.\n\nLa règle du jour : **couvre les parcours critiques avec le type de test adapté (unitaire pour la logique, comportement pour les composants, intégration pour les flux) ; vise la confiance, pas le pourcentage**. Une suite qui rougit quand un parcours critique casse vaut plus que 100 % de couverture de code trivial.`,
    guided: `**Énoncé** : une suite de tests verte couvrant les parcours critiques de BiblioApp.\n\n**Exemple simple** : le test d'intégration le plus rentable — le flux de création. \`remplir le formulaire → cliquer Ajouter → le livre apparaît dans la liste\`. Un seul test qui traverse formulaire + api mockée + affichage, et attrape les bugs d'assemblage.\n\n**Raisonnement pas-à-pas** : lister les parcours critiques (créer, supprimer, rechercher) → tester la logique en unitaire, les composants en comportement, les flux en intégration → prouver que chaque test rougit sur une régression.\n\n**Solution** :\n\`\`\`tsx\n// 1. UNITAIRE — logique de filtres (jour 115), cas limites\ntest('combine texte ET disponibilité', () => {\n  expect(filtrer(livres, { q: 'react', dispoSeule: true })).toHaveLength(1);\n});\n// 2. COMPORTEMENT — le composant confirme avant de supprimer (jour 107)\ntest('demande confirmation avant suppression', async () => {\n  const onSuppr = vi.fn(); vi.spyOn(window, 'confirm').mockReturnValue(false);\n  render(<CarteLivre livre={livre} onSupprimer={onSuppr} />);\n  await userEvent.click(screen.getByRole('button', { name: /supprimer/i }));\n  expect(onSuppr).not.toHaveBeenCalled();                    // annulé → aucune suppression\n});\n// 3. INTÉGRATION — flux critique créer (jour 108), api mockée\ntest('créer un livre l\\'ajoute à la liste', async () => {\n  vi.mocked(api.creerLivre).mockResolvedValue({ id: 9, titre: 'Vue', auteur: 'Evan' });\n  render(<BiblioApp />);\n  await userEvent.type(screen.getByLabelText(/titre/i), 'Vue');\n  await userEvent.click(screen.getByRole('button', { name: /ajouter/i }));\n  expect(await screen.findByText('Vue')).toBeVisible();      // assemblage form → api → liste\n});\n\`\`\`\n\n**Mini-quiz** : (a) Vise-t-on 100 % de couverture ? (b) Quel type de test pour un flux « créer → apparaît dans la liste » ? (c) Comment choisir quoi tester en priorité ?\n→ (a) non — on vise la CONFIANCE sur les parcours critiques, pas le pourcentage ; (b) un test d'intégration (api mockée) qui traverse formulaire → api → affichage ; (c) les parcours dont l'échec coûte le plus (créer, supprimer, rechercher).\n\n**Variante** : sabote la logique de filtres (inverse un \`&&\` en \`||\`) et vérifie que le test unitaire correspondant rougit — la preuve que ta suite protège vraiment le parcours de recherche.`,
    caseStudy: `Une suite de tests bien ciblée est ce qui rend un projet MODIFIABLE en confiance : les équipes livrent et refactorent sans peur parce qu'une régression sur un parcours critique casse un test avant la production. Le contre-exemple coûteux est la course au « 100 % de couverture » qui teste massivement du code trivial (getters, rendu statique) tout en laissant les vrais flux (créer, supprimer) non couverts — beaucoup d'effort, peu de protection. Les équipes matures raisonnent en parcours critiques et en pyramide (beaucoup d'unitaires rapides, quelques intégrations ciblées). Pour un portfolio, une suite verte qui couvre les flux critiques de BiblioApp est un signal fort : elle prouve que tu sais rendre un projet fiable, pas seulement fonctionnel.`,
    interview: `**« Comment décides-tu quoi tester dans un projet ? »** → Je ne vise pas 100 % de couverture, je vise la CONFIANCE sur les PARCOURS CRITIQUES — ceux dont l'échec coûte le plus (pour BiblioApp : créer, supprimer, rechercher). J'applique à chaque chose le bon type de test : unitaire pour la logique métier (validation, filtres, cas limites), comportement pour les composants (par rôle/label, pas l'implémentation), intégration pour les flux (créer → apparaît dans la liste, api mockée) car ils attrapent les bugs d'assemblage, les plus coûteux. C'est la pyramide : beaucoup d'unitaires rapides, quelques intégrations ciblées. Une suite qui rougit quand un parcours critique casse vaut bien plus que 100 % de couverture de code trivial — et c'est ce qui rend le projet modifiable sans peur.`,
    future: `- **Bankable** : savoir cibler les parcours critiques avec le bon type de test (au lieu de courir après la couverture) est un jugement de test recherché — et une suite verte sur BiblioApp est un signal de portfolio fort.\n- Ces tests protègent le polish (117) et tout refactor de BiblioApp, rendant le projet modifiable en confiance.\n- La stratégie « parcours critiques + pyramide » se transfère à tout projet testé, du front au back aux pipelines data et aux évaluations IA.`,
    solution: {
      logic: "Solution simple : ajouter des tests un peu partout. Solution améliorée : identifier les PARCOURS CRITIQUES (créer, supprimer, rechercher) et leur appliquer le bon type de test — unitaire pour la logique (filtres, validation, cas limites), comportement pour les composants (rôle/label), intégration pour les flux (api mockée) qui attrapent les bugs d'assemblage — en visant la confiance, pas le pourcentage, et en prouvant que chaque test rougit sur une régression. La preuve : saboter un parcours critique casse un test.",
      pitfalls: ["Courir après 100 % de couverture : beaucoup d'effort sur du code trivial, peu de protection sur les vrais flux.", "Ne faire que des tests unitaires : on rate les bugs d'ASSEMBLAGE entre formulaire, api et affichage (rôle de l'intégration).", "Tester les composants par leur implémentation : la suite casse au refactor sans attraper de vrais bugs.", "Ne pas vérifier que les tests rougissent : une suite verte qui ne peut pas échouer sur un parcours critique est un faux filet."],
      checks: ["Les parcours critiques (créer, supprimer, rechercher) sont couverts.", "Chaque chose a le bon type de test (unitaire/comportement/intégration).", "Les flux critiques ont un test d'intégration avec api mockée.", "La suite est verte et chaque test a été prouvé en sabotant le code.", "La stratégie vise la confiance sur le critique, pas un pourcentage de couverture."],
      oral: "Explique la stratégie : « je vise la confiance sur les parcours critiques, pas 100 % de couverture ». Décris la pyramide (unitaires nombreux, intégrations ciblées) et le rôle unique des tests d'intégration (bugs d'assemblage). Cite le contre-exemple de la course à la couverture. Montrer qu'une régression sur « créer » casse un test prouve que ta suite protège ce qui compte.",
    },
  },

  // ── Projet 3 — Polish et états edge ──
  117: {
    lessons: ['react-fundamentals.md'],
    theory: `**Modèle mental : le polish, c'est traiter tous les états NON-heureux (loading, error, empty) et les cas limites PARTOUT — et c'est précisément ce qui distingue une démo d'un produit. Une large part de la perception de qualité vient de ces états rarement montrés mais toujours remarqués quand ils manquent.** BiblioApp « marche » (socle, CRUD, recherche, tests). Mais « marcher sur le chemin heureux » et « être soigné » sont deux choses différentes. Le polish, c'est la passe où tu parcours CHAQUE écran en te demandant : que voit l'utilisateur quand ça charge ? quand ça échoue ? quand c'est vide ? quand il fait quelque chose d'inattendu ?\n\nLes dimensions du polish :\n1. **Les trois états, partout** : loading (un squelette ou spinner, pas un écran figé), error (un message avec sortie, jour 111), empty (« aucun livre — ajoutez-en un », pas une page blanche). Tu les connais depuis le jour 94 ; le polish, c'est vérifier qu'AUCUN écran ne les oublie.\n2. **Les cas limites** : un titre très long qui casse la mise en page, une liste de 1000 éléments, un champ avec des caractères spéciaux, un double-clic sur « ajouter ». Les cas que le chemin heureux ne rencontre jamais mais que la réalité produit.\n3. **Les transitions et le feedback** : une action a-t-elle un retour visible (un bouton qui passe en « envoi... », une confirmation après ajout) ? L'utilisateur ne doit jamais se demander « est-ce que ça a marché ? ».\n4. **La cohérence** : mêmes styles, mêmes libellés, mêmes comportements d'un écran à l'autre. L'incohérence donne une impression d'amateurisme même si tout fonctionne.\n\nLa méthode du polish : parcourir l'app en ADVERSAIRE de soi-même — chercher activement ce qui casse, ce qui est laid, ce qui laisse l'utilisateur dans le doute. C'est la même posture que provoquer les erreurs (jour 111) ou saboter les tests (jour 106), appliquée à l'UX. Un tableau « écran × état » (chaque écran, ses états loading/error/empty/edge) rend la passe systématique plutôt qu'au hasard.\n\nLa règle du jour : **le polish n'ajoute pas de fonctionnalité, il rend soigné ce qui existe — en traitant tous les états non-heureux et les cas limites, partout, avec cohérence**. C'est le travail le moins visible et le plus déterminant pour la perception de qualité.`,
    guided: `**Énoncé** : une passe de polish systématique sur BiblioApp — tous les états, partout.\n\n**Exemple simple** : le tableau écran × état, l'outil du polish — pour chaque écran, une case loading / error / empty / edge. \`Liste : [spinner] [erreur+retry] [\"aucun livre\"] [titre très long tronqué]\`. Remplir ce tableau transforme le polish en checklist au lieu d'un « au feeling ».\n\n**Raisonnement pas-à-pas** : dresser le tableau écran × état → parcourir chaque case en adversaire → corriger les états manquants, les cas limites, les feedbacks absents → vérifier la cohérence.\n\n**Grille de polish** :\n\`\`\`\nÉCRAN × ÉTAT (cocher = traité)\n              loading    error         empty              edge\nListe         squelette  msg + retry   \"aucun livre +CTA\"  titre long tronqué\nDétail        spinner    404 propre    —                  auteur manquant → \"—\"\nFormulaire    —          erreurs champ —                  double-clic bloqué (jour 96)\nRecherche     —          —             \"aucun résultat\"    100 items → fluide (jour 102)\nFEEDBACK : ajout → confirmation · suppression → confirmation · envoi → bouton \"envoi...\"\nCOHÉRENCE : mêmes libellés, mêmes styles de boutons, même format de date partout\n\`\`\`\n\n**Mini-quiz** : (a) Le polish ajoute-t-il des fonctionnalités ? (b) Pourquoi parcourir l'app « en adversaire » ? (c) D'où vient une grande part de la perception de qualité ?\n→ (a) non — il rend soigné ce qui existe déjà ; (b) pour chercher activement ce qui casse/est laid/laisse dans le doute, au lieu d'attendre que l'utilisateur le trouve ; (c) des états non-heureux (loading/error/empty) et cas limites, rarement montrés mais toujours remarqués quand ils manquent.\n\n**Variante** : donne ton app à quelqu'un en lui demandant de « la casser » — les cas limites qu'il trouve en 5 minutes sont ceux que tu as normalisés à force de tester le chemin heureux. C'est ton audit d'états edge grandeur nature.`,
    caseStudy: `Le polish est ce qui sépare, à fonctionnalités égales, un projet qui inspire confiance d'un projet qui semble bâclé — et les recruteurs le perçoivent en quelques clics : un écran de chargement soigné, un état vide qui guide, une erreur avec une sortie, un titre long qui ne casse pas la mise en page. En entreprise, les états non-heureux mal gérés sont une source majeure de tickets support et de frustration (« l'app a planté » = en réalité un état d'erreur non géré). Le tableau écran × état est une pratique concrète pour rendre cette passe systématique. Un portfolio où les cas limites sont manifestement soignés signale un souci du détail et une maturité que le seul « chemin heureux » ne montre jamais.`,
    interview: `**« Qu'est-ce qui distingue une démo d'un produit fini selon toi ? »** → Le traitement des états NON-heureux et des cas limites, partout. Une démo marche sur le chemin heureux ; un produit gère le loading (squelette, pas écran figé), l'error (message avec sortie), l'empty (un état vide qui guide, pas une page blanche), et les cas limites (titre très long, liste énorme, double-clic, caractères spéciaux). J'aborde le polish en ADVERSAIRE de moi-même : je parcours chaque écran en cherchant ce qui casse ou laisse l'utilisateur dans le doute, souvent avec un tableau écran × état pour être systématique. Le polish n'ajoute pas de fonctionnalité, il rend soigné ce qui existe — et une grande part de la perception de qualité vient précisément de ces états rarement montrés mais toujours remarqués quand ils manquent.`,
    future: `- **Bankable** : un projet manifestement soigné (états non-heureux et cas limites traités partout) inspire confiance en quelques clics — un différenciateur de portfolio que le seul chemin heureux ne donne jamais.\n- Cette passe finalise BiblioApp avant sa mise en vitrine (README/démo, jour 118).\n- La posture « parcourir en adversaire, tableau écran × état » se réapplique à toute interface — et la rigueur sur les états non-heureux vaut pour les API, les pipelines et les apps IA (une réponse LLM vide/erronée doit dégrader proprement).`,
    solution: {
      logic: "Solution simple : ajouter quelques spinners et messages d'erreur. Solution améliorée : dresser un tableau écran × état (loading/error/empty/edge) pour rendre la passe SYSTÉMATIQUE, parcourir chaque case en adversaire de soi-même, corriger les états non-heureux manquants, les cas limites (titre long, grande liste, double-clic, caractères spéciaux), les feedbacks d'action absents, et assurer la cohérence (libellés, styles). La preuve : aucun écran n'a d'état non-heureux oublié, et un testeur externe peine à « casser » l'app.",
      pitfalls: ["Ne soigner que le chemin heureux : les états loading/error/empty oubliés donnent une impression de bâclé.", "Polir « au feeling » sans méthode : on oublie des écrans — le tableau écran × état rend la passe systématique.", "Ignorer les cas limites (titre long, grande liste, double-clic) : ils cassent en conditions réelles.", "Négliger le feedback d'action : l'utilisateur ne sait pas si son ajout/suppression a marché."],
      checks: ["Chaque écran gère loading, error et empty explicitement.", "Les cas limites (titre long, grande liste, double-clic, caractères spéciaux) sont traités.", "Chaque action donne un feedback visible (envoi..., confirmation).", "La cohérence (libellés, styles, formats) est assurée d'un écran à l'autre.", "Le polish a été mené systématiquement (tableau écran × état), pas au hasard."],
      oral: "Définis le polish : « traiter tous les états non-heureux et cas limites, partout — ce qui sépare une démo d'un produit ». Décris ta méthode (tableau écran × état, parcours en adversaire). Souligne que « le polish n'ajoute pas de feature, il rend soigné ce qui existe » et que la perception de qualité vient de ces états rarement montrés. Faire tester l'app par un tiers est l'audit qui le prouve.",
    },
  },

  // ── Projet 3 — README, schéma, ADR, démo ──
  118: {
    lessons: ['readme-documentation.md', 'architecture-basics.md'],
    theory: `**Modèle mental : un projet non documenté et non démontrable n'existe pas pour un recruteur — le README, le schéma, l'ADR et la démo sont l'INTERFACE entre ton travail et le marché. BiblioApp est fini ; aujourd'hui tu le rends LISIBLE en trois minutes par quelqu'un qui ne l'a jamais vu.** Tu as déjà rendu un projet présentable pour LivreAPI (mois 3) ; ici tu le fais pour une app front full-stack, avec des livrables adaptés à ce type de projet. C'est la dernière ligne droite qui transforme du code en PIÈCE DE PORTFOLIO.\n\nLes quatre livrables et leur rôle DISTINCT pour BiblioApp :\n1. **Le README** raconte l'histoire : le problème (gérer une bibliothèque de livres), ce que fait l'app (avec un GIF de démo qui vaut mille mots), comment la lancer en une commande, et les limites. Il commence par le POURQUOI, pas par « BiblioApp utilise React et Vite ». Un recruteur le lit en 90 secondes et décide s'il creuse.\n2. **Le schéma d'architecture 3-tiers** : le trait distinctif d'un projet full-stack — montrer visuellement les trois couches (le front React ↔ l'API REST ↔ la base de données) et comment une requête les traverse. Ce schéma prouve que tu comprends l'ARCHITECTURE, pas seulement le code d'un écran.\n3. **L'ADR n°3 : « état global (Context) vs état local »** : une décision d'architecture RÉELLE de BiblioApp, figée avec son pourquoi — pourquoi le thème est en Context mais l'état d'un formulaire reste local (jours 100-101), les options écartées, les conséquences. Une ADR montre que tu as ARBITRÉ, le signal recherché au-dessus du niveau junior.\n4. **La démo de 2 minutes** montre l'app VIVANTE : lister, créer, rechercher, supprimer — un parcours fluide qui prouve que « ça marche » là où le README l'affirme.\n\nLa règle qui gouverne les quatre : ils s'adressent à un lecteur EXTÉRIEUR pressé, pas à toi. Le test du regard neuf : quelqu'un qui n'a jamais vu BiblioApp comprend-il, via le README et la démo, ce que fait l'app, comment elle est construite (le schéma), pourquoi tu as fait tes choix (l'ADR), et comment la lancer — SANS te parler ? Si oui, ton projet est présentable et vendable. Sinon, c'est un projet que seul toi comprends, donc invisible sur le marché.`,
    guided: `**Énoncé** : rendre BiblioApp présentable — README + schéma 3-tiers + ADR n°3 + démo 2 min.\n\n**Exemple simple** : la première ligne du README n'est pas « BiblioApp est une app React/Vite » mais « BiblioApp permet de gérer une bibliothèque : lister, ajouter, rechercher et supprimer des livres, avec une interface soignée et testée ». Le POURQUOI et la valeur avant la stack.\n\n**Raisonnement pas-à-pas** : écrire pour un lecteur extérieur pressé — histoire (README + GIF), architecture (schéma 3-tiers), décision arbitrée (ADR), preuve vivante (démo) — puis TEST du regard neuf.\n\n**Structure** :\n\`\`\`\nREADME.md\n  1. Le problème + ce que fait BiblioApp (2 phrases) + GIF de démo\n  2. Fonctionnalités : liste, CRUD, recherche/filtres, états soignés, tests\n  3. Lancer en 1 commande (npm install && npm run dev)\n  4. Architecture → lien vers le schéma 3-tiers\n  5. Décisions → lien vers les ADR · 6. Limites assumées\nschéma 3-tiers (image) :\n  [Front React] --HTTP/JSON--> [API REST] --SQL--> [Base de données]\n  (une requête \"créer un livre\" traverse les trois : formulaire → POST → INSERT)\nADR-003 : état global (Context) vs local\n  contexte → options (tout en Context / tout local / mixte) → décision (mixte :\n  thème en Context, formulaires en local) → conséquences → révision si\ndémo 2 min : lister → créer un livre → le rechercher → le supprimer (parcours fluide)\n\`\`\`\n\n**Mini-quiz** : (a) Par quoi commence un bon README ? (b) Que prouve un schéma 3-tiers qu'un écran de code ne prouve pas ? (c) Que montre l'ADR « Context vs local » ?\n→ (a) le problème et la valeur, pas la stack ; (b) que tu comprends l'ARCHITECTURE (comment front, API et base collaborent), pas juste un composant ; (c) que tu as ARBITRÉ une vraie décision (thème global, formulaire local) avec son pourquoi et ses options écartées.\n\n**Variante** : fais lire ton README à quelqu'un qui ne connaît pas BiblioApp et chronomètre — comprend-il en 90 secondes ce que fait l'app, comment elle est construite et comment la lancer ? Ses questions sont tes trous à combler.`,
    caseStudy: `Les recruteurs techniques passent 30 à 90 secondes sur un repo GitHub avant de décider s'ils creusent : un README qui commence par le problème et montre un GIF de démo capte cette fenêtre ; un README « TODO » ou noyé dans la config la gâche. Pour un projet full-stack, le schéma 3-tiers est décisif — il prouve en un coup d'œil que le candidat comprend comment le front, l'API et la base collaborent, ce que beaucoup de juniors ne savent pas expliquer. Et l'ADR distingue celui qui a « fait des choix » de celui qui « a suivi un tuto ». À compétence égale, le candidat dont le projet se comprend en deux minutes obtient l'entretien que l'autre n'obtient pas : le README, le schéma et la démo ne sont pas de la cosmétique, ils sont l'interface entre ton travail et le marché.`,
    interview: `**« Présente-moi un projet full-stack dont tu es fier. »** → Je déroule BiblioApp en 2-3 minutes : le problème (gérer une bibliothèque — lister, créer, rechercher, supprimer), la démo vivante (le parcours CRUD complet), puis l'architecture via un schéma 3-tiers (front React ↔ API REST ↔ base de données, et comment une création traverse les trois couches). J'explique une décision d'architecture arbitrée — mon ADR « Context vs état local » : le thème est global donc en Context, l'état d'un formulaire reste local, avec les options que j'ai écartées. Et je mentionne que l'app est testée (parcours critiques) et soignée (états loading/error/empty partout). Un README qui commence par le pourquoi, un schéma qui montre que je comprends l'architecture, une démo qui prouve que ça marche — pour que la valeur soit comprise sans que j'aie tout à expliquer.`,
    future: `- **Bankable** : un projet full-stack présentable en 2 minutes (README + GIF + schéma 3-tiers + ADR + démo) passe le premier filtre des recruteurs — l'interface entre ton travail et le marché.\n- Le schéma 3-tiers et l'ADR démontrent une compréhension d'ARCHITECTURE (front/API/base, décisions arbitrées) — le signal recherché au-dessus du niveau junior.\n- Cette discipline « rendre lisible de l'extérieur » se réapplique à chaque projet de portfolio (dont DocSense, mois 11) et à toute présentation de projet en entretien.`,
    solution: {
      logic: "Solution simple : un README qui décrit l'app et comment la lancer. Solution améliorée : un README qui commence par le problème et la valeur avec un GIF de démo, un schéma d'architecture 3-tiers (front ↔ API ↔ base, avec le parcours d'une requête), une ADR qui fige une VRAIE décision arbitrée de BiblioApp (Context vs local) avec options écartées et conséquences, et une démo de 2 min du parcours CRUD — le tout écrit pour un lecteur extérieur pressé et validé par le test du regard neuf.",
      pitfalls: ["README qui commence par la stack (« React + Vite ») au lieu du problème et de la valeur : le lecteur ne sait pas pourquoi le projet existe.", "Pas de schéma d'architecture : sur un projet full-stack, on ne prouve pas qu'on comprend comment les couches collaborent.", "ADR qui ne liste que la décision sans les options écartées : sans l'arbitrage, elle ne prouve pas la pensée d'architecte.", "Démo hésitante ou absente : « ça marche sur ma machine » ne convainc pas — un parcours CRUD fluide de 2 min, oui."],
      checks: ["Le README commence par le problème/la valeur et inclut un GIF de démo.", "Un schéma 3-tiers montre front ↔ API ↔ base et le parcours d'une requête.", "L'ADR fige une vraie décision (Context vs local) avec options écartées et conséquences.", "La démo de 2 min déroule le parcours CRUD de façon fluide.", "Le test du regard neuf est passé : un inconnu comprend en 90 secondes."],
      oral: "Entraîne ta démo de 2 minutes jusqu'à la fluidité : problème → parcours CRUD vivant → schéma 3-tiers → décision arbitrée (ADR Context vs local). Insiste sur ce que le full-stack ajoute : le schéma prouve que tu comprends comment front, API et base collaborent. C'est LA présentation de projet que tu réutiliseras en entretien — répète-la chronomètre en main.",
    },
  },

  // ── Revue semaine 17 : Projet 3 BiblioApp + revue mensuelle 4 ──
  119: {
    reviewSynthese: `La semaine 17 a produit BiblioApp, ta troisième pièce de portfolio et ta première application front full-stack complète : socle marchant validant l'architecture de bout en bout (jour 113), CRUD complet en écritures immuables et suppressions confirmées (114), recherche et filtres en état dérivé et prédicats composés (115), suite de tests ciblée sur les parcours critiques (116), polish systématique de tous les états non-heureux (117), et mise en vitrine — README, schéma 3-tiers, ADR, démo (118). C'est aussi la revue mensuelle 4, qui clôt le mois React/front : tu es passé du composant isolé (mois 4, début) à une application testée, résiliente, soignée et présentable. Le fil rouge du mois : **une application, ce n'est pas des écrans qui marchent — c'est une architecture d'état pensée, une qualité prouvée par les tests, une robustesse sans écran blanc, et une présentation qui la rend lisible sur le marché**. BiblioApp est la preuve que tu sais MENER un projet front, pas seulement coder des features.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Démarrer par un socle marchant qui valide l'architecture, en commits atomiques | |\n| Implémenter un CRUD complet (écritures immuables, suppression confirmée, échec géré) | |\n| Recherche/filtres en état dérivé et prédicats composables | |\n| Tester les parcours critiques avec le bon type de test (unitaire/comportement/intégration) | |\n| Polir tous les états non-heureux et cas limites, systématiquement | |\n| Rendre le projet présentable : README + schéma 3-tiers + ADR + démo | |\n\n**Total /18 (revue mensuelle 4)** : ≥ 15 = BiblioApp est une vraie pièce de portfolio · 11-14 = renforcer les items ≤ 1 · < 11 = reprendre les incréments faibles avant d'entrer dans Python (mois 4→5).`,
    remediation: `- **Pas de socle marchant / commits géants** : rejoue le jour 113 — construis le chemin le plus fin de bout en bout d'abord, en commits atomiques lisibles (les recruteurs lisent ton historique).\n- **CRUD fragile** (UI qui ment, suppression sans confirmation, état muté) : reprends le jour 114 — valider → api → mise à jour immuable → gérer l'échec ; confirmer toute suppression.\n- **Liste filtrée stockée en state** : jour 115 — stocke les critères, dérive les résultats ; compose des prédicats indépendants.\n- **Tests absents ou mal ciblés** : jour 116 — couvre les parcours critiques (créer/supprimer/rechercher) avec le bon type de test ; vise la confiance, pas 100 %.\n- **États non-heureux oubliés** : jour 117 — tableau écran × état, parcours en adversaire, loading/error/empty partout.\n- **Projet illisible de l'extérieur** : jour 118 — README (pourquoi + valeur + lancement), schéma 3-tiers, ADR arbitrée, démo ; passe le test du regard neuf.`,
    interview: `1. **« Comment démarres-tu un projet full-stack ? »** Attendu : socle marchant qui valide l'architecture de bout en bout, commits atomiques lisibles, features accrochées ensuite.\n2. **« Comment implémentes-tu un CRUD sûr ? »** Attendu : formulaire réutilisé, écritures immuables, suppression confirmée, cohérence avec le serveur (gérer l'échec, l'UI ne ment pas).\n3. **« Recherche et filtres : où vit l'état ? »** Attendu : les critères en state, les résultats dérivés ; filtres combinables = prédicats indépendants composés.\n4. **« Comment décides-tu quoi tester ? »** Attendu : parcours critiques, bon type de test par chose (unitaire/comportement/intégration), confiance plutôt que couverture.\n5. **« Qu'est-ce qui distingue une démo d'un produit ? »** Attendu : les états non-heureux et cas limites traités partout, systématiquement.\n6. **« Présente ce projet en 2 minutes. »** Attendu : problème → démo CRUD → schéma 3-tiers → une décision arbitrée (ADR) — lisible sans explication.`,
  },

  // ── Python : syntaxe et structures ──
  120: {
    lessons: ['python-foundations.md'],
    theory: `**Modèle mental : passer de JavaScript à Python, ce n'est pas apprendre une nouvelle syntaxe pour faire les mêmes gestes — c'est adopter un STYLE IDIOMATIQUE. Le même algorithme s'écrit différemment parce que Python a d'autres idiomes naturels, et écrire du Python « avec un accent JavaScript » fonctionne mais trahit un débutant.** Tu sais déjà programmer (4 mois de JS/TS) ; tu n'apprends pas à penser, tu apprends à t'exprimer dans une nouvelle langue. Python sera ta langue pour la data et l'IA (mois 5+) — d'où l'importance de l'écrire idiomatiquement dès le départ.\n\nLes différences JS → Python qui comptent le plus :\n1. **Les blocs : indentation, pas accolades.** En Python, l'indentation EST la syntaxe — pas de \`{}\`, pas de \`;\`. Un bloc mal indenté est une erreur, pas un détail de style. C'est déroutant au début, discipline ensuite.\n2. **Les boucles-transformations : comprehensions, pas \`for...push\`.** Là où tu écrivais \`const out = []; for (const x of xs) out.push(f(x))\`, Python écrit \`out = [f(x) for x in xs]\` — une COMPREHENSION. Pour filtrer : \`[x for x in xs if cond]\`. C'est l'idiome le plus caractéristique : un JS-teur écrit des boucles là où un pythoniste écrit des comprehensions.\n3. **Les structures : \`dict\` et \`list\`, pas objets et tableaux.** L'objet JS \`{cle: valeur}\` devient le \`dict\` Python \`{'cle': valeur}\` (accès \`d['cle']\` ou \`d.get('cle')\`). Le tableau devient la \`list\`. S'ajoutent le \`tuple\` (séquence immuable, \`(a, b)\`) et le \`set\` (ensemble unique) — des structures que JS n'a pas nativement au même niveau.\n4. **L'itération : \`enumerate\` et \`zip\`, pas les index.** Pour l'indice ET la valeur : \`for i, x in enumerate(xs)\` (pas \`for (let i...)\`). Pour parcourir deux listes ensemble : \`zip(a, b)\`. Itérer par index à la JS est un anti-idiome en Python.\n5. **Le nommage et le style (PEP 8)** : \`snake_case\` pour les variables/fonctions (pas \`camelCase\`), et une préférence pour le code explicite et lisible.\n\nEt l'outillage indispensable : le **venv** (environnement virtuel). Contrairement à npm qui isole par projet via \`node_modules\`, Python installe globalement par défaut — le venv crée un environnement isolé par projet (\`python -m venv .venv\`) pour que les dépendances d'un projet ne polluent pas les autres. Travailler sans venv est la première mauvaise habitude à éviter.\n\nLa règle du jour : **écris du Python idiomatique, pas du JavaScript traduit — comprehensions au lieu de boucles-push, dict/tuple/set, enumerate/zip, snake_case, et un venv par projet**. Le même algorithme, exprimé dans les idiomes de la langue.`,
    guided: `**Énoncé** : réécrire 5 exercices JS du mois 1 en Python IDIOMATIQUE, dans un venv.\n\n**Exemple simple** : la traduction la plus révélatrice — une boucle-push JS devient une comprehension. JS : \`const carres = []; for (const n of nums) carres.push(n*n);\` → Python : \`carres = [n*n for n in nums]\`. Si tu écris la version avec \`for ... .append()\`, tu écris du JS en Python.\n\n**Raisonnement pas-à-pas** : créer le venv → pour chaque exercice, repérer l'idiome JS → le remplacer par l'idiome Python (comprehension, dict, enumerate) → vérifier que c'est lisible et pythonique.\n\n**Solution** :\n\`\`\`python\n# venv d'abord : python -m venv .venv && source .venv/bin/activate\n\n# 1. Transformer (JS: map/for-push) → comprehension\ncarres = [n * n for n in nombres]\n\n# 2. Filtrer (JS: filter/for-if-push) → comprehension conditionnelle\npairs = [n for n in nombres if n % 2 == 0]\n\n# 3. Compter les occurrences (JS: objet + for) → dict\ncompte = {}\nfor mot in mots:\n    compte[mot] = compte.get(mot, 0) + 1        # .get(cle, defaut) : pas de KeyError\n\n# 4. Indice + valeur (JS: for(let i...)) → enumerate\nfor i, mot in enumerate(mots):\n    print(f\"{i}: {mot}\")                         # f-string, snake_case\n\n# 5. Deux listes en parallèle (JS: for + index) → zip, et dédoublonner → set\nfor titre, auteur in zip(titres, auteurs):\n    print(f\"{titre} — {auteur}\")\nauteurs_uniques = set(auteurs)                   # ensemble unique\n\`\`\`\n\n**Mini-quiz** : (a) Comment écrit-on \`xs.map(f)\` idiomatiquement en Python ? (b) Pourquoi \`compte.get(mot, 0)\` plutôt que \`compte[mot]\` ? (c) À quoi sert le venv, et en quoi diffère-t-il de npm ?\n→ (a) une comprehension : \`[f(x) for x in xs]\` ; (b) \`compte[mot]\` lève une KeyError si la clé n'existe pas ; \`.get(mot, 0)\` renvoie un défaut ; (c) il isole les dépendances par projet, là où Python installe globalement par défaut (npm isole déjà via node_modules).\n\n**Variante** : prends ta version « JS traduit » (avec des boucles-push) et ta version idiomatique côte à côte — la différence de lisibilité EST la leçon. Fais relire par quelqu'un : la version pythonique se lit d'un coup d'œil.`,
    caseStudy: `Python est la langue dominante de la data science et de l'IA : pandas, NumPy, scikit-learn, PyTorch — tout ton mois 5 et au-delà s'écrira en Python. Un code écrit idiomatiquement (comprehensions, dict, enumerate) est non seulement plus court mais aussi plus lisible et plus performant que sa traduction littérale depuis JavaScript, et c'est ce que les relecteurs et recruteurs attendent. À l'inverse, du « JavaScript déguisé en Python » (boucles-push partout, camelCase, index manuels) signale immédiatement quelqu'un qui n'a pas intégré la langue. Comme beaucoup de profils IA arrivent d'autres langages, savoir écrire du Python VRAIMENT pythonique dès le départ est un différenciateur concret — et le venv est le premier réflexe d'hygiène que toute équipe Python exige.`,
    interview: `**« Tu viens de JavaScript : quelles différences idiomatiques retiens-tu en passant à Python ? »** → Ce n'est pas une nouvelle syntaxe pour les mêmes gestes, c'est un STYLE. Les plus marquantes : les blocs par INDENTATION (pas d'accolades) ; les COMPREHENSIONS au lieu des boucles-push (\`[f(x) for x in xs]\` remplace \`for...push\`) — l'idiome le plus caractéristique ; les structures \`dict\`/\`list\`/\`tuple\`/\`set\` (avec \`.get(cle, defaut)\` pour éviter les KeyError) ; l'itération par \`enumerate\` (indice + valeur) et \`zip\` (deux listes en parallèle) plutôt que par index ; et le \`snake_case\` (PEP 8). Côté outillage, le \`venv\` isole les dépendances par projet, car Python installe globalement par défaut. Écrire du « JavaScript traduit » fonctionne mais trahit un débutant ; je vise du Python idiomatique dès le départ.`,
    future: `- **Bankable** : écrire du Python VRAIMENT idiomatique (comprehensions, dict, enumerate, venv) dès le départ est un différenciateur — beaucoup de profils IA arrivent d'autres langages et écrivent du « JS traduit ».\n- Python est la langue de toute la suite du parcours : data (mois 5, pandas/NumPy), ML (mois 6-7), LLM/RAG/agents (mois 8-10) — chaque ligne s'appuiera sur ces idiomes.\n- Le réflexe « exprimer un algorithme dans les idiomes de la langue » (pas le traduire littéralement) est transférable à tout nouveau langage que tu apprendras.`,
    solution: {
      logic: "Solution simple : traduire les exercices JS en Python ligne à ligne. Solution améliorée : les réécrire IDIOMATIQUEMENT — comprehensions au lieu des boucles-push (transformer et filtrer), dict avec .get(cle, defaut) pour le comptage (pas de KeyError), enumerate pour indice+valeur, zip pour deux listes, set pour dédoublonner, snake_case et f-strings — le tout dans un venv activé. La preuve : mise côte à côte, la version pythonique est nettement plus courte et lisible que la traduction littérale.",
      pitfalls: ["Écrire des boucles \`for ... .append()\` là où une comprehension s'impose : du JavaScript traduit, pas du Python.", "Accéder à un dict par \`d[cle]\` sans gérer la clé absente : KeyError — utiliser \`.get(cle, defaut)\`.", "Itérer par index (\`range(len(xs))\`) au lieu d'\`enumerate\`/\`zip\` : anti-idiome peu lisible.", "Travailler sans venv : les dépendances polluent l'installation globale (Python installe globalement par défaut, contrairement à npm)."],
      checks: ["Les transformations et filtres utilisent des comprehensions, pas des boucles-push.", "Le comptage utilise un dict avec .get(cle, defaut).", "L'itération indice+valeur utilise enumerate, deux listes utilisent zip.", "Le nommage est en snake_case et les chaînes utilisent des f-strings.", "Les scripts tournent dans un venv activé, isolé du système."],
      oral: "Formule la bascule : « je ne traduis pas mon JS, j'adopte les idiomes de Python ». Cite les plus marquants : comprehensions (vs for-push), dict avec .get, enumerate/zip (vs index), indentation, snake_case, venv. Montrer les deux versions côte à côte (JS traduit vs pythonique) et pointer la différence de lisibilité prouve que tu as intégré la LANGUE, pas juste sa syntaxe.",
    },
  },
};
