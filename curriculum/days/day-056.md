# Jour 56 — Revue hebdomadaire — semaine 8

> **Mois 2** · **Semaine 8** · Compétence : **HTTP / API** · Difficulté : Facile/5 · Durée : 4.5 h

[← Dashboard](../../) · [Semaine 8](../week-08.md) · [Mois 2](../month-02.md)

## 🎯 Objectif du jour
Faire le bilan de la semaine 8 (HTTP et REST, Node puis Express, validation et erreurs ; premiers SELECT + revue mensuelle 2), valider les acquis et décider du passage à la semaine suivante.

## 🔁 Revue hebdomadaire

**Thème de la semaine :** HTTP et REST, Node puis Express, validation et erreurs ; premiers SELECT + revue mensuelle 2

### Bilan
Tu passes de consommateur d'API à producteur, et tu ne sautes pas l'étape du dessous : d'abord ce qui circule vraiment sur le fil, ensuite le module http natif, ensuite seulement Express. La semaine ferme le mois 2 : revue mensuelle 2 en fin de semaine.

### Test pratique
90 min, en trois temps. (1) Avec curl uniquement : GET une API publique, POST du JSON, affiche les headers, suis une redirection, explique chaque statut reçu. (2) Un serveur Express : GET /livres, GET /livres/:id, POST /livres avec validation du titre, DELETE /livres/:id — données en mémoire, statuts corrects. (3) Ajoute-lui un middleware d'erreurs centralisé et des 400 détaillées, puis CASSE-le exprès (JSON invalide, id inexistant, champ manquant) et vérifie qu'aucune erreur ne fuit en 500.

### Test théorique
Le trajet complet d'une requête (DNS → TCP → requête → réponse) ; huit statuts avec leur cas d'usage (200/201/204/301/400/401/404/500) ; pourquoi GET ne doit pas modifier l'état ; ce qu'est un middleware et dans quel ordre ils s'exécutent ; où mettre la validation et pourquoi jamais dans la route ; erreur opérationnelle vs bug, et pourquoi les deux ne se traitent pas pareil ; et pour le SQL de vendredi : que fait un JOIN, dessine-le.

### Mini-projet / livrable
API 'citations' : CRUD complet en mémoire, route GET /citations/aleatoire, middleware de log des requêtes, gestion d'erreurs centralisée et validation manuelle des entrées. C'est le squelette réutilisable qui servira de base au projet 2.

### Checklist
- [ ] J'ai utilisé curl AVANT Postman, et je sais lire une réponse brute
- [ ] Le module http natif écrit à la main une fois, avant Express
- [ ] Aucune validation dans le corps d'une route
- [ ] Une entrée invalide ne produit jamais un 500
- [ ] Revue mensuelle 2 faite

### Critères de passage à la semaine suivante
- [ ] Les quatre routes répondent avec les bons statuts
- [ ] Middleware d'erreurs démontré sur trois entrées cassées
- [ ] Cinq requêtes SQL de base écrites sans aide
- [ ] Revue mensuelle 2 complétée

### Exercice de réflexion architecturale
Ton API citations est appelée par un front que tu n'écris pas. Liste six choses que ce front a le droit d'attendre de toi (statuts stables, forme d'erreur constante, pagination, CORS, versionnage, contrat documenté) et écris pour chacune ce que tu casserais si tu la changeais sans prévenir.

### Rappel actif, puis relecture ciblée

**Étape 1 — rappel actif (20 min), leçons FERMÉES.** Pour chacune des 7 leçons ci-dessous, écris de mémoire, en deux lignes : ce qu'elle enseigne, et **la condition dans laquelle sa règle cesse de valoir**. Ce que tu n'arrives pas à écrire est exactement ce que tu n'as pas retenu.

**Étape 2 — relecture ciblée, et elle seule.** Rouvre uniquement les leçons que tu n'as pas su restituer à l'étape 1. En général une ou deux, pas la liste entière : relire ce qu'on sait déjà donne un sentiment de maîtrise sans rien ajouter à la mémoire.

- [API de production : idempotence, pagination, limites et versions](/doc/lessons/api-production-contracts)
- [HTTP, REST et JSON](/doc/lessons/http-rest-json)
- [Concevoir une API](/doc/lessons/api-design-basics)
- [Backend Express : structure et robustesse](/doc/lessons/express-backend)
- [Clean code](/doc/lessons/clean-code)
- [JavaScript asynchrone](/doc/lessons/async-javascript) — *vue il y a 29 jours, et sur une autre compétence — l'employer ici, c'est l'employer hors de son contexte*
- [JavaScript : les bases solides](/doc/lessons/javascript-basics) — *vue il y a 29 jours, et sur une autre compétence — l'employer ici, c'est l'employer hors de son contexte*

> **Ce que cette étape coûte, écrit noir sur blanc.** Le rappel actif : 20 min. La relecture ciblée : environ 25 min par leçon rouverte, et tu ne devrais pas en rouvrir plus de deux. **Relire les 7 intégralement n'est pas ce qui est demandé** — ce serait plus long, et moins efficace.

### Synthèse de la semaine
La semaine 8 a ouvert le mois 3 et posé le socle du développement backend. HTTP en profondeur (jour 50) : un protocole sans état où chaque requête est autonome, les méthodes portent une sémantique, l'idempotence sécurise les retries, et les statuts sont un langage (401 « non authentifié » vs 403 « interdit »). REST design (jour 51) : des ressources (noms pluriels), les verbes HTTP pour les actions, une API PRÉVISIBLE qui se devine sans doc. Node natif puis Express (jour 52) : éprouver la friction du bas niveau (req/res, chunks, res.end) pour démystifier ce que le framework automatise. Express en couches (jour 53) : middlewares en chaîne (l'ordre = l'exécution) et séparation routes/services/data qui rend la logique testable sans serveur. Validation et erreurs centralisées (jour 54) : toute entrée est hostile jusqu'à validation, l'opérationnel (400/404) se distingue du bug (500 générique sans fuite), on prouve la robustesse en attaquant l'API. SQL (jour 55) : le déclaratif, JOIN, GROUP BY, la distinction WHERE/HAVING. Le fil rouge : **construire une API, c'est comprendre le protocole (HTTP), concevoir un contrat cohérent (REST), structurer proprement (couches, middlewares), se défendre (validation, erreurs sans fuite) et savoir interroger ses données (SQL).** Tu entres dans la suite du mois 3 (SQLite branché, modélisation, Projet 2 LivreAPI) avec les fondations d'un vrai backend.

### Grille de notation
Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :

| Compétence | /3 |
|---|---|
| Expliquer HTTP sans état, l'idempotence et les statuts (401 vs 403) | |
| Concevoir une API REST prévisible (ressources, verbes, statuts, cohérence) | |
| Comprendre ce qu'Express automatise (req/res natif, chunks, res.end) | |
| Structurer une API en middlewares et 3 couches (logique hors des routes) | |
| Valider les entrées et centraliser les erreurs sans fuiter les détails | |
| Écrire du SQL (JOIN, GROUP BY, WHERE vs HAVING) et paramétrer les valeurs | |

**Total /18** : ≥ 15 = socle backend solide, cap sur SQLite et le Projet 2 · 11-14 = revoir les items ≤ 1 · < 11 = rejouer avant de brancher une vraie base, car ces fondations conditionnent LivreAPI.

### Plan de remédiation (si un critère échoue)
- **HTTP flou (statuts, idempotence)** : rejoue le jour 50 — sans-état, méthodes sémantiques, distinction 401/403, idempotence pour les retries.
- **API non prévisible / verbes dans l'URL** : jour 51 — ressources au pluriel, verbes HTTP pour les actions, statuts par cas, cohérence.
- **Express en boîte noire** : jour 52 — réécris un serveur natif (req/res, chunks, res.end) pour voir ce que le framework automatise.
- **Logique dans les routes** : jour 53 — sépare routes/services/data, teste la logique des services sans serveur ; maîtrise la chaîne de middlewares (ordre, next).
- **API fragile / fuite d'erreurs** : jour 54 — valide toute entrée (400 + liste), centralise les erreurs (opérationnel vs bug), n'expose jamais la stack ; attaque ta propre API.
- **SQL confus (WHERE/HAVING, JOIN)** : jour 55 — WHERE filtre les lignes avant, HAVING les groupes après ; INNER vs LEFT ; paramètre les valeurs.

### Questions d'entretien de la semaine
1. **« HTTP est sans état : qu'est-ce que ça implique, et 401 vs 403 ? »** Attendu : requête autonome, état transporté par token, scalabilité ; 401 non authentifié, 403 interdit ; idempotence pour les retries.
2. **« Conçois une API REST : quelles règles ? »** Attendu : ressources au pluriel, verbes HTTP pour les actions (jamais dans l'URL), statuts par cas, query string pour raffiner, prévisibilité.
3. **« Qu'est-ce qu'Express fait pour toi par rapport au module http ? »** Attendu : routing, parsing du corps (chunks), réponse (res.end) ; le framework automatise ce qu'on fait à la main.
4. **« Comment structures-tu une API et gères-tu les middlewares ? »** Attendu : 3 couches (routes/services/data), logique testable hors des routes ; chaîne de middlewares, ordre = exécution, next obligatoire.
5. **« Comment rends-tu une API robuste, et différence WHERE/HAVING en SQL ? »** Attendu : entrée hostile jusqu'à validation, erreurs centralisées sans fuite ; WHERE filtre les lignes avant, HAVING les groupes après.

### Auto-évaluation
- Note honnête de la semaine (0-5) : ____
- Mets à jour tes scores de compétences dans l'application.
- Ce que je dois revoir : ____
