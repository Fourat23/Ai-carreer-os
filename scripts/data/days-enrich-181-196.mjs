// Enrichissement Batch 4A — semaines 26-28 (jours 181, 183-188, 190-195).
// Fusionné via days-enrich-181-210.mjs. Champs : theory, guided, caseStudy,
// interview, future (« bankable »), solution {logic, pitfalls, checks, oral}.

export const ENRICH_W26_28 = {
  181: {
    theory: `**Modèle mental : un projet ML non reproductible n'existe pas.** Si un tiers ne peut pas relancer ton pipeline et obtenir les MÊMES chiffres, tes résultats sont des anecdotes. La reproductibilité tient à quatre verrous : les données figées (ou leur téléchargement scripté), les seeds fixées partout, l'environnement déclaré (requirements.txt), et UNE commande d'entrée (\`python run.py\`). Le README raconte ensuite l'histoire : question métier → méthode → chiffres → limites. C'est la dernière ligne droite qui transforme ChurnScope de « notebook qui a marché une fois » en pièce de portfolio.`,
    guided: `**Énoncé** : verrouiller la reproductibilité de ChurnScope.\n\n**Raisonnement** : chaque source de hasard ou d'implicite doit être fixée ou déclarée.\n\n**Checklist exécutable** :\n\`\`\`\n1. random_state=42 partout (split, modèles, CV) → 2 runs = mêmes scores ?\n2. pip freeze > requirements.txt ; venv documenté.\n3. run.py enchaîne : données → features → entraînement → éval → rapport.\n4. Clone frais dans /tmp : suivre le README à la lettre. Chaque écart = bug de doc.\n\`\`\`\n\n**Explication** : le test du clone frais est le seul juge honnête. **Variante** : demande à quelqu'un d'autre de le faire (ou refais-le demain, de mémoire).`,
    caseStudy: `En entreprise, un modèle part en revue avant mise en production : si le reviewer ne peut pas reproduire tes métriques, la revue s'arrête là. La reproductibilité n'est pas du confort, c'est la condition d'entrée.`,
    interview: `**« Je clone ton projet ML : comment je vérifie tes chiffres ? »** → Une commande (\`python run.py\`), seeds fixées, données scriptées, mêmes scores affichés que dans le README — et si un écart apparaît, c'est un bug que je veux connaître.`,
    future: `- **Bankable** : 90 % des portfolios ML sont des notebooks non rejouables — le tien se relance en une commande, chiffres identiques.\n- L'évaluation RAG (mois 9) exigera la même discipline : scores versionnés et reproductibles.\n- En entretien, « teste-le toi-même » est l'argument qui clôt le scepticisme.`,
    solution: {
      logic: "Reproductibilité = fixer le hasard (seeds), figer l'environnement (requirements), scripter les données, unifier l'entrée (run.py). Le README raconte : problème → méthode → chiffres → limites, dans cet ordre.",
      pitfalls: ["Seed fixée au split mais pas au modèle (RandomForest a son propre hasard).", "Données « déjà là » sur TA machine mais non téléchargeables par le lecteur.", "README écrit de mémoire sans re-suivre les étapes sur un clone frais."],
      checks: ["Deux exécutions consécutives → scores identiques au centième.", "Clone frais + README suivi à la lettre → tout passe.", "La démo de 2 min est enregistrée et fluide."],
      oral: "Sache raconter ChurnScope en STAR : la question métier, la baseline battue (chiffres), UNE décision de feature engineering justifiée, ce que le modèle NE sait pas faire. Deux minutes, zéro jargon creux.",
    },
  },
  183: {
    theory: `**Modèle mental : un neurone est une machine à peser.** Il reçoit des entrées, les multiplie par des POIDS (l'importance apprise de chaque entrée), ajoute un BIAIS (un seuil de déclenchement), et passe le tout dans une ACTIVATION non linéaire : \`sortie = σ(w·x + b)\`. Apprendre = régler ces poids.\n\nPourquoi le coder en NumPy pur, sans framework ? Parce que c'est la seule façon de VOIR qu'il n'y a pas de magie : une multiplication, une addition, une fonction. La sigmoïde écrase le résultat entre 0 et 1 (une « probabilité »). L'erreur (loss) mesure l'écart entre la sortie et la cible. Et l'apprentissage (demain) consistera à tourner les poids dans le sens qui réduit cette erreur. Le ET logique est le terrain d'entraînement parfait : 4 exemples, 2 poids, 1 biais — tout se calcule à la main et se vérifie.`,
    guided: `**Énoncé** : un neurone qui apprend le ET logique.\n\n**Raisonnement** : sortie attendue 1 seulement si x1 ET x2 valent 1 ; le neurone doit apprendre des poids qui rendent w·x + b fortement positif uniquement dans ce cas.\n\n**Solution (squelette NumPy)** :\n\`\`\`python\nimport numpy as np\nX = np.array([[0,0],[0,1],[1,0],[1,1]]); y = np.array([0,0,0,1])\nw, b = np.random.randn(2), 0.0\nsigmoide = lambda z: 1/(1+np.exp(-z))\nfor epoch in range(1000):\n    pred = sigmoide(X @ w + b)          # 4 prédictions d'un coup\n    erreur = pred - y                    # l'écart\n    w -= 0.5 * (X.T @ erreur) / 4        # gradient (admis aujourd'hui)\n    b -= 0.5 * erreur.mean()\nprint(np.round(pred, 2))                 # → [0. 0.05 0.05 0.93]\n\`\`\`\n\n**Explication** : \`X @ w\` pèse les entrées ; l'erreur guide la correction des poids (le POURQUOI de cette formule = demain). **Variante** : essaie le OU logique (change y) — puis le XOR : constate l'échec (un neurone seul est une frontière DROITE ; le XOR exigera une couche cachée, jour 186).`,
    caseStudy: `Chaque neurone d'un LLM à des milliards de paramètres fait EXACTEMENT ce calcul : peser, sommer, activer. Comprendre l'unité de base te permet de parler des grands modèles sans réciter — tu sais ce qu'il y a dedans.`,
    interview: `**« Explique-moi ce qu'est un poids dans un réseau de neurones. »** → L'importance apprise d'une entrée : un multiplicateur réglé pendant l'entraînement pour réduire l'erreur. Le biais est le seuil de déclenchement ; l'activation apporte la non-linéarité.`,
    future: `- **Bankable** : « j'ai codé un neurone et sa descente de gradient en NumPy pur » — la phrase qui prouve que tu comprends le DL au lieu de l'importer.\n- Toute la suite (MLP, transformers, LLM) est cet atome, répété et organisé.\n- Les questions d'entretien DL junior commencent presque toutes ici.`,
    solution: {
      logic: "Le neurone converge sur le ET parce que le problème est linéairement séparable : une droite peut isoler (1,1) des trois autres points. La loss décroissante est ta preuve d'apprentissage ; les prédictions finales proches de [0,0,0,1] ta preuve de convergence.",
      pitfalls: ["Learning rate trop grand (0.5 → 50) : les prédictions oscillent, la loss diverge — observe-le exprès.", "Oublier la non-linéarité (retirer la sigmoïde) : le neurone devient une régression linéaire.", "Conclure du XOR que « le neurone est cassé » : c'est la LIMITE structurelle d'un neurone seul, la raison d'être des couches."],
      checks: ["Prédictions finales : [~0, ~0.05, ~0.05, ~0.9].", "La courbe de loss décroît de façon monotone (trace-la).", "Le XOR échoue quel que soit le nombre d'epochs — et tu sais dire pourquoi."],
      oral: "Dessine un neurone (entrées → poids → somme+biais → activation → sortie) en 30 secondes et raconte le calcul sur UN exemple chiffré. C'est le schéma le plus rentable de tout le mois.",
    },
  },
  184: {
    theory: `**Modèle mental : une vallée dans le brouillard.** La loss dessine un paysage vallonné dont chaque point est un réglage possible des poids ; l'altitude est l'erreur. On ne voit pas la carte (des millions de dimensions), mais on sent la PENTE sous ses pieds : c'est le **gradient** — la direction de la plus forte montée. Descendre = faire un petit pas dans la direction opposée, et recommencer.\n\nLe **learning rate** est la taille du pas, et ses deux pathologies se VOIENT sur la courbe de loss : trop grand, on saute par-dessus la vallée (loss qui oscille ou explose) ; trop petit, on avance à l'infini (loss qui stagne haut). La **backpropagation** n'est que la machinerie qui calcule cette pente efficacement pour TOUS les poids à la fois (dérivation en chaîne, automatisée). Aujourd'hui tu EXPÉRIMENTES : mêmes données, même modèle, seuls les learning rates changent — et les courbes racontent trois histoires différentes.`,
    guided: `**Énoncé** : visualiser l'effet du learning rate sur ton neurone d'hier.\n\n**Raisonnement** : une seule variable change (le LR), tout le reste est fixé (seed comprise) ; les courbes de loss se comparent alors honnêtement.\n\n**Protocole** :\n\`\`\`python\nfor lr in [0.01, 0.5, 5.0]:\n    np.random.seed(42)              # même point de départ !\n    w, b = np.random.randn(2), 0.0\n    pertes = []\n    for epoch in range(300):\n        pred = sigmoide(X @ w + b)\n        pertes.append(((pred - y)**2).mean())\n        w -= lr * (X.T @ (pred - y)) / 4\n        b -= lr * (pred - y).mean()\n    plt.plot(pertes, label=f\"lr={lr}\")\n\`\`\`\n\n**Explication** : lr=0.01 descend lentement (stagne haut à 300 epochs), lr=0.5 converge proprement, lr=5.0 oscille/diverge. **Variante** : trouve le LR au-delà duquel ça diverge systématiquement — tu viens de faire ta première recherche d'hyperparamètre.`,
    caseStudy: `Le fine-tuning d'un modèle en entreprise, c'est ce réglage à grande échelle : un LR mal choisi brûle des heures de GPU (donc des centaines d'euros) pour une loss qui diverge. Savoir LIRE une courbe d'entraînement est une compétence facturable.`,
    interview: `**« Explique la descente de gradient à quelqu'un de non technique. »** → Tu es dans une vallée en plein brouillard : tu sens la pente sous tes pieds et tu fais des petits pas vers le bas. Le gradient est la pente, le learning rate la taille du pas — trop grand tu dépasses le fond, trop petit tu n'arrives jamais.`,
    future: `- **Bankable** : lire une courbe de loss (converge/oscille/stagne) est le geste de diagnostic n°1 de tout entraînement — ML, DL, fine-tuning.\n- La métaphore de la vallée est LA réponse attendue en entretien DL junior.\n- Les hyperparamètres des LLM (mois 8+) se raisonnent avec exactement cette grammaire.`,
    solution: {
      logic: "Une expérience propre : UNE variable (le LR), même seed, mêmes données, mêmes epochs — les courbes deviennent comparables. Les trois régimes (lent / bon / divergent) doivent être visibles sur ton graphe.",
      pitfalls: ["Oublier de re-seeder à chaque LR : les points de départ diffèrent, la comparaison est faussée.", "Conclure sur 20 epochs (le lent semble « cassé » alors qu'il est juste lent).", "Comparer des loss de formules différentes entre runs."],
      checks: ["Le graphe superpose les 3 courbes avec légende.", "Tu sais pointer : lequel divergerait encore plus avec plus d'epochs ? Lequel finirait par converger ?", "Ton LR « limite de divergence » est noté."],
      oral: "Raconte les deux pathologies du learning rate en montrant (ou dessinant) les courbes : osciller au-dessus de la vallée vs ramper sans progresser. Une image, deux phrases, question maîtrisée.",
    },
  },
  185: {
    theory: `**Modèle mental : PyTorch = NumPy + mémoire de calcul.** Un tenseur PyTorch est un tableau NumPy qui SE SOUVIENT des opérations qu'on lui applique (le graphe de calcul). Grâce à cette mémoire, \`loss.backward()\` remonte le fil et calcule automatiquement le gradient de CHAQUE poids : c'est l'**autograd** — la backpropagation d'hier, industrialisée.\n\nLes gestes de base : créer des tenseurs (\`torch.tensor\`, shapes et dtypes comme NumPy), marquer ce qui s'apprend (\`requires_grad=True\`), calculer, appeler \`backward()\`, lire \`.grad\`. Ta mission du jour est un exercice de TRADUCTION : le neurone NumPy d'hier, ligne à ligne en PyTorch — mêmes données, mêmes résultats. La différence : tu n'écris plus la formule du gradient à la main, autograd la dérive pour toi. Comprendre ce que le framework AUTOMATISE (parce que tu l'as fait à la main hier) est exactement ce qui sépare « utiliser PyTorch » de « subir PyTorch ».`,
    guided: `**Énoncé** : le neurone ET logique, traduit en PyTorch avec autograd.\n\n**Raisonnement** : remplacer le gradient manuel par backward() ; tout le reste est identique.\n\n**Solution** :\n\`\`\`python\nimport torch\nX = torch.tensor([[0.,0.],[0.,1.],[1.,0.],[1.,1.]]); y = torch.tensor([0.,0.,0.,1.])\nw = torch.randn(2, requires_grad=True); b = torch.zeros(1, requires_grad=True)\nfor epoch in range(1000):\n    pred = torch.sigmoid(X @ w + b)\n    loss = ((pred - y)**2).mean()\n    loss.backward()                  # autograd calcule w.grad et b.grad\n    with torch.no_grad():            # mise à jour SANS enregistrer dans le graphe\n        w -= 0.5 * w.grad; b -= 0.5 * b.grad\n    w.grad.zero_(); b.grad.zero_()   # remettre à zéro (sinon ACCUMULATION)\n\`\`\`\n\n**Explication** : backward() fait ce que ta formule \`X.T @ erreur\` faisait hier — vérifie-le en comparant w.grad au calcul manuel sur la première itération ! **Variante** : remplace la boucle manuelle par \`torch.optim.SGD([w, b], lr=0.5)\` (optimizer.step() / zero_grad()) — la forme canonique.`,
    caseStudy: `Tout l'écosystème DL (fine-tuning, embeddings, transformers) est bâti sur autograd : les équipes définissent le MODÈLE et la LOSS, le framework dérive. Savoir ce qu'il automatise te permet de débugger quand il proteste (shapes, gradients NaN, no_grad oublié).`,
    interview: `**« Que fait loss.backward() dans PyTorch ? »** → Il remonte le graphe de calcul enregistré et calcule le gradient de la loss par rapport à chaque tenseur marqué requires_grad — la backpropagation automatisée. D'où les deux rituels : no_grad() pour la mise à jour, zero_grad() entre les itérations.`,
    future: `- **Bankable** : « je peux comparer le gradient autograd à mon calcul manuel » — la profondeur que 95 % des utilisateurs de PyTorch n'ont pas.\n- Les 5 gestes (zero_grad → forward → loss → backward → step) sont la boucle de TOUT entraînement, MLP comme fine-tuning LLM.\n- Les bugs PyTorch classiques (accumulation de gradients, shapes) deviennent lisibles.`,
    solution: {
      logic: "La traduction réussie prouve l'équivalence : mêmes données + même seed → mêmes prédictions finales que la version NumPy. w.grad à l'itération 1 doit égaler ton calcul manuel (à la précision près) — c'est LA vérification qui ancre.",
      pitfalls: ["Oublier zero_grad() : les gradients s'ACCUMULENT silencieusement, l'entraînement devient chaotique — provoque-le une fois pour le reconnaître.", "Mettre à jour les poids hors de no_grad() : PyTorch enregistre la mise à jour dans le graphe → erreur ou fuite mémoire.", "Comparer NumPy et PyTorch avec des seeds différentes et s'étonner de l'écart."],
      checks: ["w.grad (itération 1) ≈ ton gradient manuel d'hier.", "Prédictions finales ≈ [0, .05, .05, .9].", "La variante avec optim.SGD donne le même résultat que la boucle manuelle."],
      oral: "Explique la boucle canonique en 5 gestes et POURQUOI zero_grad existe (l'accumulation par défaut sert aux gros batchs fractionnés — mais piège les débutants).",
    },
  },
  186: {
    theory: `**Modèle mental : empiler pour courber.** Un neurone seul trace une frontière DROITE (le XOR l'a prouvé au jour 183). Un **MLP** (perceptron multicouche) empile des couches : chaque couche re-décrit les données dans un nouvel espace, et une frontière courbe dans l'espace d'origine devient... une droite dans le dernier espace. Les couches cachées sont des CHANGEMENTS DE REPRÉSENTATION successifs — c'est l'idée la plus profonde du deep learning : apprendre les bonnes représentations, pas seulement la décision finale.\n\nEn PyTorch, le MLP s'écrit en composition : \`nn.Sequential(nn.Linear(2, 8), nn.ReLU(), nn.Linear(8, 1))\`. \`Linear\` = tes poids+biais en matrice ; \`ReLU\` = l'activation moderne (max(0, x) — plus simple et plus stable que la sigmoïde en interne). La loss adaptée à la classification binaire est \`BCEWithLogitsLoss\` (elle intègre la sigmoïde, numériquement stable). Ta mission : vaincre le XOR — le problème impossible d'avant-hier tombe avec UNE couche cachée, et tu VERRAS pourquoi.`,
    guided: `**Énoncé** : un MLP qui résout le XOR.\n\n**Raisonnement** : 2 entrées → couche cachée (4-8 neurones, ReLU) → 1 sortie. La couche cachée transforme le problème non séparable en problème séparable.\n\n**Solution** :\n\`\`\`python\nmodel = nn.Sequential(nn.Linear(2, 8), nn.ReLU(), nn.Linear(8, 1))\ncritere = nn.BCEWithLogitsLoss()\nopt = torch.optim.Adam(model.parameters(), lr=0.05)\nX = torch.tensor([[0.,0.],[0.,1.],[1.,0.],[1.,1.]]); y = torch.tensor([[0.],[1.],[1.],[0.]])\nfor epoch in range(500):\n    opt.zero_grad()\n    loss = critere(model(X), y)\n    loss.backward(); opt.step()\nprint(torch.sigmoid(model(X)).round())   # → [[0],[1],[1],[0]] : XOR vaincu\n\`\`\`\n\n**Explication** : les 8 neurones cachés apprennent des « détecteurs » intermédiaires (x1 sans x2, x2 sans x1...) que la sortie combine. **Variante** : réduis la couche cachée à 1 neurone → l'échec revient (pas assez de « place » pour re-représenter). À 2 ? Parfois oui, selon la seed — instructif sur le rôle de l'initialisation.`,
    caseStudy: `« Apprendre des représentations » est LE principe des embeddings (jour 191) : les couches intermédiaires d'un réseau entraîné sur du texte produisent des vecteurs où le sens est géométrie. Le MLP jouet d'aujourd'hui et les embeddings de ton futur RAG sont le même phénomène à deux échelles.`,
    interview: `**« Pourquoi un réseau multicouche peut-il apprendre ce qu'un neurone seul ne peut pas ? »** → Chaque couche re-représente les données ; une frontière courbe dans l'espace d'origine devient linéairement séparable dans l'espace appris. Sans non-linéarité entre les couches, l'empilement resterait une seule transformation linéaire.`,
    future: `- **Bankable** : raconter « j'ai vu le XOR échouer sur un neurone puis tomber avec une couche cachée » est une narration d'entretien parfaite — concrète, chiffrée, comprise.\n- Le concept « couches = représentations » explique embeddings, transfer learning et fine-tuning.\n- nn.Sequential/Linear/ReLU/optimizer : la grammaire PyTorch de tout ce qui suit.`,
    solution: {
      logic: "Le XOR tombe parce que la couche cachée offre un espace intermédiaire où recombiner les entrées. La preuve de compréhension n'est pas « ça marche » mais l'ABLATION : 1 neurone caché échoue, 8 réussissent, et tu sais expliquer l'écart.",
      pitfalls: ["Oublier la ReLU entre les Linear : deux transformations linéaires composées = UNE linéaire → le XOR échoue encore.", "BCEWithLogitsLoss attend les logits BRUTS (pas de sigmoide avant la loss).", "y en shape [4] au lieu de [[4,1]] : broadcast silencieux et loss fantaisiste."],
      checks: ["Prédictions arrondies exactement [0,1,1,0].", "L'ablation (1 vs 8 neurones cachés) est faite et documentée.", "Retirer la ReLU fait échouer — vérifié."],
      oral: "Le récit XOR en 60 secondes : un neurone = frontière droite → échec ; une couche cachée = re-représentation → victoire ; et la phrase clé « le deep learning apprend des représentations, pas seulement des décisions ».",
    },
  },
  187: {
    theory: `**Modèle mental : l'entraînement réel est une usine, pas une expérience de labo.** Sur MNIST (60 000 images), plus question de tout charger d'un coup : le **DataLoader** débite des **batchs** (paquets de 64) mélangés à chaque **epoch** (un passage complet sur les données). La boucle canonique tourne par batch, et l'évaluation se fait sur un jeu de VALIDATION jamais vu à l'entraînement — le protocole d'honnêteté du mois 6, appliqué au DL.\n\nLes deux courbes (loss train / loss validation) sont ton tableau de bord : elles baissent ensemble = ça apprend ; le train continue de baisser pendant que la validation remonte = **overfitting**, le réseau mémorise. S'ajoutent les gestes d'usine : \`model.train()\` / \`model.eval()\` (certains composants se comportent différemment en test), \`torch.no_grad()\` pour évaluer sans payer le graphe, et la sauvegarde du meilleur modèle. Objectif du jour : > 95 % sur le test MNIST avec un MLP simple — et surtout des courbes que tu sais LIRE.`,
    guided: `**Énoncé** : la boucle d'entraînement complète sur MNIST.\n\n**Raisonnement** : imbriquer la boucle canonique dans les epochs, mesurer sur validation à chaque epoch.\n\n**Squelette** :\n\`\`\`python\ntrain_loader = DataLoader(train_ds, batch_size=64, shuffle=True)\nmodel = nn.Sequential(nn.Flatten(), nn.Linear(784, 128), nn.ReLU(), nn.Linear(128, 10))\nopt = torch.optim.Adam(model.parameters(), lr=1e-3)\nfor epoch in range(10):\n    model.train()\n    for X, y in train_loader:\n        opt.zero_grad()\n        loss = F.cross_entropy(model(X), y)\n        loss.backward(); opt.step()\n    model.eval()\n    with torch.no_grad():\n        val_acc = accuracy(model, val_loader)   # à écrire : argmax == label\n    print(epoch, val_acc)\n\`\`\`\n\n**Explication** : cross_entropy = la loss multi-classes (10 chiffres) ; Flatten aplatit l'image 28×28 en 784. **Variante** : logge AUSSI la loss train par epoch et trace les deux courbes — c'est elles qu'on lira demain.`,
    caseStudy: `Dans une équipe ML, on ne « lance » pas un entraînement : on le SURVEILLE (courbes en direct, checkpoints, arrêt si divergence). Un entraînement de production coûte des heures de GPU — les réflexes d'aujourd'hui (courbes, éval par epoch, sauvegarde du meilleur) sont les gestes du métier.`,
    interview: `**« Batch, epoch, itération : définis et relie. »** → Une itération = une mise à jour sur UN batch ; une epoch = toutes les itérations couvrant le dataset une fois ; le batch aménage mémoire et stabilité du gradient. 60 000 images / 64 par batch ≈ 938 itérations par epoch.`,
    future: `- **Bankable** : « > 95 % sur MNIST avec ma boucle écrite main, courbes à l'appui » — l'exercice canonique que les recruteurs DL reconnaissent immédiatement.\n- La discipline train/eval/no_grad se transpose au fine-tuning et à l'éval de LLM.\n- Lire des courbes en direct = la compétence qui évite de brûler du GPU pour rien.`,
    solution: {
      logic: "L'architecture minimale (784→128→10) suffit largement pour 95 %+ : le POINT du jour est la boucle propre et l'évaluation honnête, pas la performance. La précision se calcule sur des données jamais vues, en mode eval, sans gradient.",
      pitfalls: ["Évaluer en mode train (ou sans no_grad) : chiffres faussés et mémoire gaspillée.", "shuffle=False sur le train : les batchs ordonnés biaisent l'apprentissage.", "Mesurer l'accuracy sur le train et s'émerveiller (mémorisation, pas généralisation)."],
      checks: ["val_acc > 0.95 avant 10 epochs.", "Les deux courbes (train/val) sont tracées et cohérentes.", "model.eval() + no_grad() encadrent bien toute évaluation."],
      oral: "Déroule la boucle d'entraînement de mémoire (les 5 gestes × batchs × epochs) et explique pourquoi l'évaluation exige un jeu à part — en une minute, tableau blanc.",
    },
  },
  188: {
    theory: `**Modèle mental : l'overfitting est une mémorisation, la régularisation une gêne salutaire.** Quand le réseau a trop de capacité pour trop peu de données, il apprend les exemples PAR CŒUR (bruit compris) au lieu d'apprendre le motif. Symptôme sur les courbes : loss train qui plonge, loss validation qui remonte — le grand écart.\n\nLes trois remèdes du jour : le **dropout** (éteindre aléatoirement des neurones à l'entraînement — chaque neurone doit devenir utile SANS compter sur ses voisins, ce qui interdit les mémorisations fragiles) ; l'**early stopping** (s'arrêter quand la validation cesse de s'améliorer — pourquoi continuer à mémoriser ?) ; et la **réduction de capacité** (moins de neurones = moins de place pour le par-cœur). La méthode expérimentale du mois 6 s'applique intégralement : UNE variable à la fois, seed fixée, tableau de résultats. Aujourd'hui tu PROVOQUES un overfitting (petit sous-ensemble de MNIST, gros réseau), puis tu le soignes, mesures à l'appui.`,
    guided: `**Énoncé** : provoquer puis corriger un overfitting.\n\n**Raisonnement** : réduire les données amplifie la mémorisation ; on soigne ensuite par dropout et early stopping, en comparant les courbes.\n\n**Protocole** :\n\`\`\`python\npetit_train = Subset(train_ds, range(1000))       # 1000 images seulement\ngros_modele = nn.Sequential(nn.Flatten(), nn.Linear(784, 512), nn.ReLU(),\n                            nn.Linear(512, 512), nn.ReLU(), nn.Linear(512, 10))\n# Run A : tel quel, 40 epochs → courbes train/val divergent (overfit VISIBLE)\n# Run B : + nn.Dropout(0.5) après chaque ReLU → l'écart se réduit\n# Run C : + early stopping (garder le modèle à la meilleure val_acc)\n\`\`\`\n\n**Explication** : A est le témoin ; B et C changent UNE chose chacun. Le dropout ne s'active qu'en mode train (model.eval() le coupe — d'où l'importance du rituel d'hier). **Variante** : run D avec le modèle RÉDUIT (784→64→10) sans dropout — souvent aussi efficace : la capacité est un levier, pas un détail.`,
    caseStudy: `Le fine-tuning d'un modèle sur un petit corpus d'entreprise (500 documents) est un terrain d'overfitting idéal : sans ces réflexes, le modèle récite le corpus au lieu de généraliser. Les remèdes d'aujourd'hui sont exactement ceux qu'on applique alors.`,
    interview: `**« Comment détectes-tu et corriges-tu l'overfitting en deep learning ? »** → Détection : l'écart croissant entre loss train et validation. Remèdes, dans l'ordre de coût : plus de données, moins de capacité, dropout, early stopping — en changeant une variable à la fois et en comparant les courbes.`,
    future: `- **Bankable** : montrer un tableau de 4 runs (témoin, dropout, early stopping, capacité réduite) avec courbes est une démonstration de MÉTHODE que peu de juniors ont.\n- Le sur-ajustement réapparaîtra en éval RAG (sur-adapter au golden set) : même vigilance.\n- « Une variable à la fois, seed fixée » : la discipline transversale de toute ta carrière IA.`,
    solution: {
      logic: "La preuve de maîtrise est le TABLEAU comparatif : 4 runs, une variable d'écart chacun, val_acc finale + forme des courbes. L'overfitting doit être VISIBLE dans le témoin (sinon rien n'est démontré) avant d'être corrigé.",
      pitfalls: ["Dropout actif à l'évaluation (model.eval() oublié) : les scores de val sous-estimés.", "Early stopping sur la loss TRAIN (contresens complet).", "Changer dropout ET capacité en même temps : effet indémêlable."],
      checks: ["Run A : écart train/val flagrant sur les courbes.", "Runs B/C/D : val_acc ≥ A avec écart réduit.", "Le tableau final tient sur un écran et se raconte en 90 secondes."],
      oral: "Dessine les courbes de l'overfitting (train qui plonge, val qui remonte, le point d'arrêt idéal) et récite les remèdes par ordre de coût. Question posée dans quasi tous les entretiens ML/DL.",
    },
  },
  190: {
    theory: `**Modèle mental : le tokenizer est le hachoir d'entrée des LLM.** Un modèle ne lit ni lettres ni mots : il lit des **tokens**, des morceaux de mots issus d'un vocabulaire appris (~50-100k entrées) sur des statistiques de fréquence (BPE). « chat » = 1 token ; « anticonstitutionnellement » = 6-8 morceaux ; et le français, moins bien couvert que l'anglais, se découpe plus fin (donc coûte plus cher à longueur égale !).\n\nPourquoi les sous-mots ? Les mots entiers rendraient le vocabulaire infini (tout mot nouveau = inconnu) ; les caractères seuls rendraient les séquences interminables. Le sous-mot est le compromis : tout texte est encodable, les mots fréquents restent compacts. Conséquences d'ingénieur immédiates : le COÛT se compte en tokens (entrée et sortie), la FENÊTRE de contexte aussi, et certains comportements étranges des LLM (compter les lettres d'un mot, arithmétique sur grands nombres) s'expliquent par la tokenisation. Aujourd'hui : tu manipules un vrai tokenizer (tiktoken) et tu mesures tes propres textes — fini les estimations au doigt mouillé.`,
    guided: `**Énoncé** : autopsie de la tokenisation sur tes textes.\n\n**Raisonnement** : encoder, décoder token par token, compter — sur du français, de l'anglais, du code.\n\n**Protocole** :\n\`\`\`python\nimport tiktoken\nenc = tiktoken.get_encoding(\"cl100k_base\")\npour chaque texte in [phrase_fr, phrase_en_equivalente, extrait_code, \"anticonstitutionnellement\"]:\n    ids = enc.encode(texte)\n    morceaux = [enc.decode([i]) for i in ids]\n    print(len(ids), morceaux)\n\`\`\`\n\n**Observations attendues** : l'espace COLLE au mot suivant (\" chat\" ≠ \"chat\" : deux tokens différents !) ; le français ~20-30 % plus dense en tokens que l'anglais ; le code se découpe étonnamment bien (l'indentation a ses tokens). **Variante** : calcule le ratio tokens/mots de 3 de tes prompts réels — ton facteur de conversion personnel pour estimer les coûts.`,
    caseStudy: `Une entreprise traduit son assistant en français et voit sa facture LLM grimper de 25 % à trafic égal : la tokenisation moins favorable du français en est la cause directe. Savoir l'expliquer (et le chiffrer) évite des surprises budgétaires.`,
    interview: `**« Qu'est-ce qu'un token et pourquoi pas simplement des mots ? »** → Un morceau de mot d'un vocabulaire appris par fréquence (BPE). Les mots entiers = vocabulaire infini et inconnus partout ; les caractères = séquences trop longues. Le sous-mot encode tout, compactement — et c'est l'unité de facturation et de contexte.`,
    future: `- **Bankable** : chiffrer un coût LLM à partir de tokens RÉELS (pas d'estimations vagues) est un réflexe d'ingénieur rare chez les juniors.\n- Le chunking RAG (mois 8) se calibre en tokens ; la fenêtre de contexte se budgète en tokens.\n- Les « bizarreries » des LLM (lettres, chiffres) s'expliquent en entretien par la tokenisation.`,
    solution: {
      logic: "La manipulation directe (encoder/décoder morceau par morceau) construit l'intuition qu'aucune lecture ne donne : les espaces collés, la granularité variable, l'écart français/anglais. Ton ratio tokens/mots personnel est le livrable durable.",
      pitfalls: ["Croire 1 token = 1 mot (faux : ~0.75 mot en anglais, moins en français).", "Oublier que la SORTIE se paie aussi en tokens.", "Comparer des tokenizers différents comme s'ils étaient interchangeables (chaque famille de modèles a le sien)."],
      checks: ["Tableau : 4 textes × (nb tokens, morceaux surprenants).", "Ratio tokens/mots calculé sur TES prompts (~1.3-2 en français).", "\" chat\" et \"chat\" donnent des ids différents — vérifié et compris."],
      oral: "Montre UN exemple de découpage surprenant et déroule ses trois conséquences : coût, fenêtre, comportements étranges. Concret, mémorable, trois phrases.",
    },
  },
  191: {
    theory: `**Modèle mental : un embedding place le sens sur une carte.** Un modèle d'embedding transforme un texte en un point dans un espace à ~1000 dimensions, positionné pour que **sens proche = points proches** : « congés payés » et « droit aux vacances » sont voisins SANS partager un seul mot. C'est la matérialisation de l'idée du jour 186 (les couches apprennent des représentations) — ici, la représentation EST le produit.\n\nLa **similarité cosinus** mesure l'angle entre deux vecteurs (1 = même direction/sens, ~0 = sans rapport) : on compare des DIRECTIONS, pas des longueurs, ce qui la rend robuste à la taille des textes. Sur des vecteurs normalisés, elle se réduit à un produit scalaire. La recherche sémantique en découle : embedder la question, embedder les documents (UNE fois, à l'avance), trier par cosinus. Deux limites à toucher du doigt dès aujourd'hui : « similaire » n'est pas « pertinent » (deux textes proches ne se répondent pas forcément), et les termes EXACTS (références, codes) échappent parfois au sens — deux limites que ton futur RAG comblera (reranking, hybride).`,
    guided: `**Énoncé** : vérifier que les embeddings capturent le sens — et leurs limites.\n\n**Raisonnement** : construire des paires dont TU connais la relation, mesurer, confronter à l'intuition.\n\n**Protocole** :\n\`\`\`\nPaires à tester (10) :\n- synonymes sans mots communs (« augmenter le salaire » / « revaloriser la paie »)\n- même domaine, sens différent (« congé maladie » / « congés payés »)\n- mots communs, sens éloigné (« avocat du barreau » / « avocat en salade »)\n- sans rapport (« embedding » / « recette de couscous »)\ncosinus(a, b) = dot(va, vb) / (norm(va) * norm(vb))   # implémenté TOI-même\n\`\`\`\n\n**Attendu** : synonymes ~0.7-0.9 ; domaine proche ~0.5-0.7 ; homonymes plus bas que les synonymes (le contexte a désambiguïsé !) ; sans rapport < 0.3. **Variante** : trie 10 phrases par proximité à une requête et compare TON classement intuitif au classement cosinus — les désaccords sont les cas les plus instructifs.`,
    caseStudy: `Le support client cherche « je n'arrive pas à me connecter » dans une base de fiches : la recherche par mots-clés rate la fiche « problèmes d'authentification » ; la recherche par embeddings la trouve. C'est le cœur de la recherche sémantique d'entreprise — et de ton projet 6.`,
    interview: `**« Qu'est-ce qu'un embedding, géométriquement, et comment compare-t-on deux textes ? »** → Un point dans un espace où la proximité encode le sens ; on compare par similarité cosinus (l'angle entre les directions), robuste à la longueur. Et similaire ≠ pertinent — d'où le reranking en aval.`,
    future: `- **Bankable** : avoir codé la similarité cosinus À LA MAIN et testé ses limites sur des paires construites te distingue des utilisateurs de boîtes noires.\n- C'est la brique n°1 du retrieval RAG (mois 8-9) : chunking, vector DB, hybride s'appuient dessus.\n- Les questions d'entretien RAG commencent presque toutes par « explique les embeddings ».`,
    solution: {
      logic: "Le protocole vaut par ses paires CONSTRUITES : chaque catégorie teste une propriété (sens sans mots, mots sans sens, désambiguïsation). Ta fonction cosinus doit passer cosinus(v, v)=1 et la symétrie avant toute interprétation.",
      pitfalls: ["Comparer des embeddings de modèles différents (espaces incompatibles).", "Interpréter les valeurs absolues dans l'absolu (0.6 « bon » ? ça dépend du modèle) — ce sont les ÉCARTS entre catégories qui parlent.", "Ré-embedder les mêmes textes à chaque comparaison (coût inutile — cache-les)."],
      checks: ["cosinus(v, v) = 1.0 ; cosinus(a, b) = cosinus(b, a).", "Le classement par catégories est respecté (synonymes > domaine > homonymes > sans rapport).", "Au moins UN désaccord intuition/cosinus analysé par écrit."],
      oral: "L'exemple « avocat/avocat » en 30 secondes : mêmes mots, sens différents, cosinus plus bas que deux synonymes sans mots communs — la preuve que c'est le SENS qui est mesuré.",
    },
  },
  192: {
    theory: `**Modèle mental : l'attention est une réunion où chaque mot interroge les autres.** Pour préciser son propre sens, chaque token pose une question (**requête** : « qui peut m'éclairer ? »), les autres présentent leur offre (**clé** : « voilà ce que je suis ») et leur contenu (**valeur** : « voilà ce que j'apporte »). L'affinité requête×clé donne des POIDS d'attention ; le token repart avec un MÉLANGE pondéré des valeurs des autres. Dans « la souris ne répond plus », « souris » prête attention à « répond » et bascule vers son sens informatique.\n\nPourquoi trois rôles distincts (Q, K, V) ? Parce que ce qu'un mot CHERCHE, ce qu'il AFFICHE pour être trouvé, et ce qu'il TRANSMET une fois choisi sont trois choses différentes — trois projections apprises du même vecteur. Et plusieurs **têtes** d'attention mènent des réunions parallèles (l'une suit la syntaxe, l'autre les coréférences...). Ce mécanisme a détrôné les RNN pour deux raisons : tout se calcule EN PARALLÈLE (adieu la lecture mot à mot), et les mots éloignés se parlent DIRECTEMENT. Son prix : chaque token regarde chaque autre — coût quadratique, d'où la fenêtre de contexte bornée... et le RAG.`,
    guided: `**Énoncé** : calculer une attention miniature à la main (3 tokens, dimensions 2).\n\n**Raisonnement** : dérouler mécaniquement scores → softmax → mélange, pour sentir chaque étape.\n\n**Déroulé** :\n\`\`\`\nq2 = [1, 0]          # la requête du token 2\nk1 = [1, 0], k2 = [0, 1], k3 = [0.9, 0.1]\nscores  = [q2·k1, q2·k2, q2·k3] = [1.0, 0.0, 0.9]\npoids   = softmax(scores)       ≈ [0.42, 0.15, 0.43]\nsortie2 = 0.42*v1 + 0.15*v2 + 0.43*v3   # le nouveau vecteur du token 2\n\`\`\`\n\n**Explication** : le token 2 « écoute » surtout les tokens 1 et 3 (clés alignées avec sa requête) ; sa nouvelle représentation est un mélange à 85 % d'eux. Le softmax transforme des scores bruts en poids qui somment à 1. **Variante** : change q2 en [0, 1] et recalcule — l'écoute bascule vers le token 2 : l'attention est DYNAMIQUE, dépendante du contenu.`,
    caseStudy: `Quand tu injectes 5 chunks dans un prompt RAG, c'est l'attention qui « relie » la question aux passages pertinents à l'intérieur du modèle. Comprendre qu'elle traite tout le contexte en interactions par paires explique aussi pourquoi trop de chunks NOIENT le signal.`,
    interview: `**« Explique l'attention avec une analogie, puis précise Q, K, V. »** → Une réunion où chaque mot interroge les autres pour préciser son sens ; requête = ce que je cherche, clé = ce que j'affiche, valeur = ce que je transmets ; les affinités Q·K deviennent des poids (softmax) qui mélangent les V.`,
    future: `- **Bankable** : dérouler un calcul d'attention CHIFFRÉ à la main (même minuscule) est un différenciateur massif — presque aucun candidat junior ne sait le faire.\n- Le coût quadratique de l'attention JUSTIFIE le RAG : tu tiens la connexion conceptuelle entre les mois 7 et 8-9.\n- Ton analogie personnelle, éprouvée, est un atout d'entretien réutilisable à vie.`,
    solution: {
      logic: "Le mini-calcul vaut mille lectures : scores par produits scalaires, softmax pour normaliser, mélange des valeurs. La compréhension se teste par la PRÉDICTION : avant de recalculer avec une autre requête, devine vers qui bascule l'écoute.",
      pitfalls: ["Confondre les rôles : la VALEUR est ce qui est transmis, pas ce qui décide (c'est Q·K qui décide).", "Oublier le softmax et mélanger avec des scores bruts (poids non normalisés).", "Réciter « queries keys values » sans savoir refaire le calcul 3×2."],
      checks: ["Ton calcul manuel : les poids somment à 1.", "Ta prédiction avant le recalcul (variante) était correcte.", "Tu sais dire pourquoi 3 projections distinctes plutôt qu'une seule."],
      oral: "L'analogie de la réunion + le mini-exemple chiffré + la phrase « coût quadratique → fenêtre bornée → RAG » : trois éléments, une réponse d'entretien complète en 90 secondes.",
    },
  },
  193: {
    theory: `**Modèle mental : le transformer est une chaîne de raffinage du sens.** Le trajet complet d'une phrase : (1) **tokenisation** (jour 190) ; (2) **embeddings + position** — chaque token devient un vecteur, et comme l'attention traite tout en parallèle, il faut lui dire QUI EST OÙ (sans position, « chien mord homme » = « homme mord chien ») ; (3) **des dizaines de blocs** identiques : attention (les tokens se contextualisent mutuellement — jour 192) puis petit réseau feed-forward (chaque token digère ce qu'il a absorbé), avec des connexions résiduelles qui stabilisent le tout ; (4) **la sortie** : pour prédire la suite, le vecteur du dernier token est projeté sur TOUT le vocabulaire → un score par token possible → softmax → une distribution de probabilités ; (5) **l'échantillonnage** (température) choisit, on ajoute le token, on recommence.\n\nCouche après couche, les représentations montent en abstraction : surface grammaticale d'abord, relations sémantiques ensuite. Rien dans cette chaîne ne « sait » ce qui est vrai — elle géométrise le plausible. Tu tiens ici l'explication MÉCANIQUE des hallucinations, deux semaines avant de les provoquer (jour 201).`,
    guided: `**Énoncé** : dessiner (à la main) le trajet de « Le chat dort » jusqu'au token suivant.\n\n**Raisonnement** : un schéma par étape, flèches et légendes — c'est TON support d'entretien et le brouillon de ta note du jour 195.\n\n**Trace attendue** :\n\`\`\`\n[Le][chat][dort]                       ← tokens (3)\n   ↓ embeddings + positions 1,2,3      ← 3 vecteurs « qui et où »\n   ↓ ×N blocs [attention → feed-forward]\n     (« dort » a absorbé « chat » : sommeil-de-chat)\n   ↓ dernier vecteur → projection vocabulaire\n   → { « paisiblement »: 21%, « profondément »: 14%, « . »: 11%, ... }\n   → température 0 : « paisiblement »\n\`\`\`\n\n**Explication** : chaque flèche de ton schéma doit répondre à « qu'est-ce qui entre, qu'est-ce qui sort ? ». **Variante** : ajoute sur le schéma OÙ interviennent la température (à l'échantillonnage) et la fenêtre de contexte (la longueur maximale de la ligne du haut).`,
    caseStudy: `Quand un fournisseur annonce « fenêtre de 200k tokens », tu sais désormais lire l'info : plus de contexte traitable d'un coup, mais un coût d'attention qui grimpe — et toujours AUCUNE mémoire au-delà. Les décisions d'architecture RAG (combien injecter, comment découper) découlent de cette lecture.`,
    interview: `**« Explique-moi un transformer en 3 minutes. »** → Dérouler les 5 étapes avec le schéma : tokens → vecteurs+positions → blocs attention/feed-forward qui raffinent le sens → distribution sur le vocabulaire → échantillonnage. Conclure : il prédit le PLAUSIBLE, pas le vrai.`,
    future: `- **Bankable** : « explique un transformer » est LA question différenciante des entretiens LLM — ton schéma fait main est la meilleure réponse possible.\n- Chaque limite d'ingénierie (fenêtre, coût, hallucination, non-déterminisme) se déduit du mécanisme : tu raisonnes, tu ne récites pas.\n- Ce schéma devient ta note publiée (jour 195) : un livrable portfolio.`,
    solution: {
      logic: "Le schéma est le livrable ET le test : si une étape ne se dessine pas, elle n'est pas comprise. L'ordre est intangible (tokens → embeddings+position → blocs → projection → échantillonnage) et chaque flèche a une entrée/sortie nommable.",
      pitfalls: ["Oublier la POSITION (l'erreur n°1) : sans elle, l'attention ne voit qu'un sac de mots.", "Dessiner « une couche » au lieu de N blocs identiques empilés.", "Placer la température au mauvais endroit (elle agit à l'échantillonnage, pas dans l'attention)."],
      checks: ["Ton schéma se redessine de MÉMOIRE en 2 minutes.", "Chaque flèche répond à « entrée → sortie ».", "Tu sais y pointer : fenêtre, température, source des hallucinations."],
      oral: "Chronomètre-toi : le trajet complet en 3 minutes, schéma à l'appui, en finissant par « il géométrise le plausible, pas le vrai » — la phrase qui ouvre la discussion suivante (RAG, grounding).",
    },
  },
  194: {
    theory: `**Modèle mental : les embeddings sont des features gratuites.** Pour classer du texte (spam/pas spam, urgent/normal), inutile d'entraîner un transformer : un modèle d'embedding déjà entraîné transforme chaque texte en vecteur riche de sens, et un classifieur LÉGER (régression logistique du mois 6 !) apprend la frontière dans cet espace. C'est le **transfer learning** dans sa forme la plus simple : un gros modèle a appris les représentations, ton petit modèle apprend la décision.\n\nLe pipeline complet : textes → embeddings (calculés UNE fois, mis en cache) → X de shape (n, dimensions) → train/test split → régression logistique → métriques honnêtes (matrice de confusion, F1 — tout le mois 6 s'applique tel quel). Ce pattern « embeddings + tête légère » résout une énorme part des besoins réels de classification de texte : rapide, peu de données nécessaires (quelques centaines d'exemples suffisent souvent), interprétable, et sans GPU. Le réflexe à ancrer : AVANT de penser fine-tuning ou LLM géant, tester embeddings + classifieur simple — c'est ta baseline, et elle est souvent imbattable en rapport qualité/coût.`,
    guided: `**Énoncé** : classifieur de tickets support (3 catégories) sur embeddings.\n\n**Raisonnement** : embedder les textes, puis appliquer TOUT le workflow ML du mois 6 — rien de nouveau, tout de réutilisé.\n\n**Pipeline** :\n\`\`\`python\n# 1. Embeddings (une fois, cachés sur disque)\nX = np.array([embed(t) for t in textes])       # (n, 1024)\ny = np.array(categories)\n# 2. Le workflow du mois 6, à l'identique\nX_tr, X_te, y_tr, y_te = train_test_split(X, y, random_state=42, stratify=y)\nclf = LogisticRegression(max_iter=1000).fit(X_tr, y_tr)\nprint(classification_report(y_te, clf.predict(X_te)))\n\`\`\`\n\n**Explication** : stratify préserve l'équilibre des classes dans le split ; le rapport donne précision/rappel PAR catégorie. **Variante** : compare à une baseline TF-IDF + régression logistique — sur des textes courts et nuancés, les embeddings gagnent nettement ; sur du vocabulaire très spécifique, l'écart se resserre (préfiguration du débat lexical/sémantique du RAG hybride).`,
    caseStudy: `Router automatiquement les tickets support (facturation / technique / commercial) avec embeddings + régression logistique : 300 exemples annotés, une après-midi de travail, zéro GPU — et un gain de tri immédiat. C'est le projet IA « premier euro » le plus fréquent en PME.`,
    interview: `**« On te demande un classifieur de texte avec 400 exemples annotés : ton approche ? »** → Embeddings pré-entraînés + classifieur léger (régression logistique), workflow ML honnête (split stratifié, F1 par classe, matrice de confusion), baseline TF-IDF pour comparer. Fine-tuning seulement si ça ne suffit pas.`,
    future: `- **Bankable** : savoir dire « pas besoin de fine-tuning ici » avec une alternative chiffrée est un signal de maturité que les équipes adorent.\n- Le routage par classifieur léger resservira dans tes workflows LLM (mois 10) : router AVANT d'appeler le gros modèle.\n- Ton mois 6 (métriques, splits) vient de se brancher sur le monde LLM : la continuité est ton argument de reconversion.`,
    solution: {
      logic: "La réussite du jour n'est pas le score brut mais la MÉTHODE recyclée : split stratifié, baseline comparée, métriques par classe, erreurs regardées une à une. Les embeddings ne dispensent d'aucune honnêteté du mois 6.",
      pitfalls: ["Ré-embedder à chaque exécution (lent et coûteux) : cache-les dès le premier calcul.", "Accuracy globale sur classes déséquilibrées (le F1 par classe dit la vérité).", "Oublier la baseline TF-IDF : sans comparaison, impossible de justifier les embeddings."],
      checks: ["F1 par classe ≥ baseline TF-IDF (ou l'écart expliqué).", "5 erreurs de classification lues et commentées.", "Les embeddings sont chargés depuis le cache au 2e run (instantané)."],
      oral: "Le pattern « embeddings + tête légère » en 60 secondes : pourquoi ça marche (représentations pré-apprises), quand ça suffit (peu de données, catégories claires), quand passer au cran supérieur.",
    },
  },
  195: {
    theory: `**Modèle mental : expliquer, c'est le test final de la compréhension.** La note « un LLM expliqué à un dev backend » n'est pas un exercice d'écriture — c'est ton EXAMEN du mois 7 : si une étape du trajet (tokens → embeddings+position → attention → blocs → distribution → échantillonnage) résiste à l'écriture, elle n'était pas comprise. La technique de Feynman appliquée : expliquer simplement force à combler les trous.\n\nLes règles d'une bonne note technique : un LECTEUR précis (un dev backend : il connaît les APIs, les caches, les coûts — parle sa langue) ; TES schémas (jour 193), pas des images empruntées ; chaque terme technique défini À SA PREMIÈRE apparition ; les analogies éprouvées (ta réunion de l'attention) ; et l'honnêteté sur les limites (plausible ≠ vrai, fenêtre bornée, coût par token). Publiée sur GitHub (ou LinkedIn), cette note devient un LIVRABLE de portfolio : elle prouve mieux qu'aucun CV que tu comprends — et la communication technique écrite est une compétence évaluée en soi.`,
    guided: `**Énoncé** : structurer la note avant de la rédiger.\n\n**Raisonnement** : le plan suit les questions naturelles d'un dev backend, pas l'ordre académique.\n\n**Plan éprouvé** :\n\`\`\`\n1. « C'est quoi, au fond ? » — un prédicteur de token suivant, à grande échelle.\n2. Le trajet d'une phrase — TES schémas du jour 193, étape par étape.\n3. « Pourquoi ça hallucine ? » — le plausible géométrisé, pas la vérité.\n4. Ce que ça implique pour ton code — non-déterminisme, latence, coût/token,\n   fenêtre bornée : le LLM comme dépendance externe faillible.\n5. Le vocabulaire pour suivre une conversation — 10 termes définis en une ligne.\n\`\`\`\n\n**Explication** : la section 4 est celle qui parle au lecteur backend — c'est elle qui distingue ta note d'un résumé de cours. **Variante** : fais-la relire (ou lis-la à voix haute) et traque chaque phrase qui te fait hésiter : c'est un trou de compréhension, pas un problème de style.`,
    caseStudy: `Les notes techniques internes (« comment fonctionne notre pipeline RAG ») sont un artefact quotidien des équipes IA : celui qui sait expliquer devient référent. Publier la tienne démontre cette compétence AVANT l'embauche.`,
    interview: `**« Vulgarise les LLM pour un collègue backend sceptique. »** → Exactement ta note : un prédicteur de plausible, le trajet en 5 étapes, et surtout les implications d'ingénierie (faillible, coûteux, borné) — le registre qui gagne un backend.`,
    future: `- **Bankable** : un article publié avec tes schémas est un actif de visibilité permanent (recruteurs, LinkedIn, entretiens : « j'ai écrit là-dessus, le lien est sur mon profil »).\n- La compétence « expliquer par écrit » est évaluée dans TOUS les rôles seniors : commence l'historique maintenant.\n- Cette note est la base de tes réponses d'entretien du mois 7 — rédigée une fois, resservie partout.`,
    solution: {
      logic: "Le juge de la note : un dev backend qui ne connaît RIEN aux LLM la lit en 10 minutes et peut ensuite expliquer à quelqu'un d'autre pourquoi un LLM hallucine et pourquoi le contexte est borné. Chaque section sert ce but ; tout le reste est coupé.",
      pitfalls: ["Écrire pour impressionner (jargon) au lieu d'expliquer (le lecteur backend décroche).", "Schémas empruntés : ils ne t'apprennent rien et se voient.", "Éviter les limites (hallucinations, coûts) : c'est la section qui crédibilise."],
      checks: ["Les 5 sections du plan sont là ; chaque terme technique est défini à sa 1re apparition.", "Tes schémas du jour 193 sont intégrés (refaits proprement).", "Publiée (GitHub/LinkedIn) avec le lien noté dans l'app."],
      oral: "Ta note EST ton oral : entraîne-toi à la dérouler en 5 minutes sans la lire. C'est la simulation d'entretien du mois 7 (voir month-07.md), déjà à moitié préparée.",
    },
  },
};
