// Enrichissement Batch 5E — jours 121-150 (Python cœur : fonctions/modules,
// exceptions, POO, pytest, outils ; pandas : charger/nettoyer/filtrer/grouper/
// merge/qualité ; SQL avancé : normalisation/index/transactions/analytique ;
// ETL ; Projet 4 DataPulse ; entrée statistiques). Fusionné dans
// generate-curriculum.mjs.
// Jours de travail : theory, guided, caseStudy, interview, future,
//   solution {logic, pitfalls, checks, oral}, lessons (override).
//   (guided intègre un exemple simple + un mini-quiz ; solution.logic donne
//    une solution simple PUIS une solution améliorée.)
// Jours de revue (126, 133, 140, 147) : reviewSynthese, reviewGrid,
//   remediation, interview.

export const ENRICH_121_150 = {
  // ── Python : fonctions, modules, fichiers ──
  121: {
    lessons: ['python-foundations.md'],
    theory: `**Modèle mental : un module Python est un NAMESPACE dans un fichier — organiser un projet, ce n'est pas empiler des fonctions dans un seul script, c'est découper le code en modules à responsabilité unique qu'on IMPORTE.** Tu sais écrire du Python idiomatique (jour 120) ; aujourd'hui tu structures un vrai projet. La brique de base est la fonction ; l'unité d'organisation est le module (un fichier \`.py\`) ; et un dossier de modules devient un package.\n\nCe qu'il faut maîtriser :\n1. **La fonction bien conçue** : \`def nom(params) -> type:\`, un \`return\` explicite (une fonction Python sans return renvoie \`None\`), des arguments par défaut (\`def f(x, sep=\", \")\`), et les arguments nommés (\`f(x, sep=\"; \")\`) qui rendent l'appel lisible. Une fonction fait UNE chose, nommée d'après ce qu'elle fait.\n2. **Le module et l'import** : chaque fichier \`.py\` est un module dont le nom est le nom de fichier. \`from notes.stockage import charger\` importe une fonction précise. Contrairement au \`<script>\` global du navigateur, rien n'est global entre fichiers : on importe explicitement ce dont on a besoin — c'est le cousin des modules ESM de JS, mais l'unité EST le fichier.\n3. **Le garde \`if __name__ == \"__main__\":\`** : il sépare « ce qui s'exécute quand je lance le fichier » de « ce qui est disponible quand je l'importe ». Sans lui, importer un module exécuterait tout son code — un piège classique. C'est ce qui fait qu'un module peut être À LA FOIS une bibliothèque et un point d'entrée.\n4. **Les fichiers** : \`with open(chemin) as f:\` (le \`with\` ferme le fichier automatiquement, même en cas d'erreur — l'équivalent du \`try/finally\`), et \`pathlib.Path\` pour manipuler les chemins proprement (\`Path(\"data\") / \"notes.json\"\`). Pour la persistance structurée : le module \`json\` (\`json.load\`/\`json.dump\`).\n\nLa règle du jour : **découpe en modules à responsabilité unique, importe explicitement, protège le point d'entrée avec \`__main__\`, et gère les fichiers avec \`with\`**. Un projet bien organisé se lit module par module ; un script de 500 lignes où tout se touche est le contraire de ce qu'on attend d'un développeur Python. C'est le clean code (mois 2) appliqué à la structure d'un projet Python.`,
    guided: `**Énoncé** : une CLI \`notes\` (ajouter/lister/chercher, persistée en JSON) découpée en modules propres avec \`argparse\`.\n\n**Exemple simple** : le garde de point d'entrée, à connaître par cœur — \`if __name__ == \"__main__\": main()\`. Il fait que lancer \`python cli.py\` exécute \`main()\`, mais \`import cli\` ne l'exécute PAS. C'est la frontière entre bibliothèque et programme.\n\n**Raisonnement pas-à-pas** : séparer le stockage (I/O JSON), la logique métier (ajouter/chercher) et l'interface (argparse) en modules distincts → chacun testable isolément → un point d'entrée qui les assemble.\n\n**Implémentation** :\n\`\`\`python\n# notes/stockage.py — responsabilité : lire/écrire le fichier JSON\nimport json\nfrom pathlib import Path\nFICHIER = Path(\"notes.json\")\ndef charger() -> list[dict]:\n    if not FICHIER.exists():\n        return []\n    with FICHIER.open(encoding=\"utf-8\") as f:   # with : fermeture garantie\n        return json.load(f)\ndef sauver(notes: list[dict]) -> None:\n    with FICHIER.open(\"w\", encoding=\"utf-8\") as f:\n        json.dump(notes, f, ensure_ascii=False, indent=2)\n\n# notes/metier.py — responsabilité : la logique, sans I/O ni CLI\ndef ajouter(notes: list[dict], texte: str) -> list[dict]:\n    return [*notes, {\"id\": len(notes) + 1, \"texte\": texte}]   # immuable\ndef chercher(notes: list[dict], mot: str) -> list[dict]:\n    return [n for n in notes if mot.lower() in n[\"texte\"].lower()]\n\n# cli.py — responsabilité : l'interface\nimport argparse\nfrom notes import stockage, metier\ndef main() -> None:\n    p = argparse.ArgumentParser()\n    p.add_argument(\"action\", choices=[\"ajouter\", \"lister\", \"chercher\"])\n    p.add_argument(\"valeur\", nargs=\"?\", default=\"\")\n    args = p.parse_args()\n    notes = stockage.charger()\n    if args.action == \"ajouter\":\n        stockage.sauver(metier.ajouter(notes, args.valeur))\n    elif args.action == \"chercher\":\n        for n in metier.chercher(notes, args.valeur): print(n[\"texte\"])\n    else:\n        for n in notes: print(n[\"texte\"])\nif __name__ == \"__main__\":   # point d'entrée protégé\n    main()\n\`\`\`\n\n**Mini-quiz** (réponds sans regarder) : (a) Que renvoie une fonction Python sans \`return\` ? (b) À quoi sert \`if __name__ == \"__main__\"\` ? (c) Pourquoi ouvrir un fichier avec \`with\` ?\n→ (a) \`None\` ; (b) séparer le code exécuté au lancement du code disponible à l'import ; (c) le fichier se ferme automatiquement même en cas d'erreur (comme un try/finally).\n\n**Variante** : ajoute une commande \`supprimer <id>\` — tu dois pouvoir le faire en ajoutant UNE fonction dans \`metier.py\` et un \`choices\` dans la CLI, sans toucher au stockage. Si ça t'oblige à tout modifier, tes modules sont mal découpés.`,
    caseStudy: `Tout outil ou service Python d'entreprise est organisé en modules à responsabilité unique : la couche d'accès aux données séparée de la logique métier, elle-même séparée de l'interface (CLI, API). Cette séparation permet de tester la logique sans fichier ni réseau, de remplacer le stockage (JSON → base de données) sans toucher au métier, et de faire évoluer chaque partie indépendamment. Le contre-exemple coûteux, courant chez les débutants venus de scripts, est le fichier unique de plusieurs centaines de lignes où l'I/O, la logique et l'affichage sont entremêlés : impossible à tester, à réutiliser ou à faire évoluer. Savoir structurer un projet Python en modules est un prérequis pour toute base de code partagée — et directement visible dans un repo.`,
    interview: `**« Comment organises-tu un projet Python, et à quoi sert \`if __name__ == \"__main__\"\` ? »** → Je découpe en modules à responsabilité unique — typiquement la couche d'accès aux données (I/O), la logique métier pure (sans I/O), et l'interface (CLI/API) — que j'importe explicitement. Rien n'est partagé implicitement entre fichiers : chaque module est un namespace, on importe ce dont on a besoin. Le garde \`if __name__ == \"__main__\":\` sépare le code exécuté quand on LANCE le fichier de celui disponible quand on l'IMPORTE — sans lui, importer un module exécuterait tout son code. Ça permet à un module d'être à la fois une bibliothèque et un point d'entrée. Pour les fichiers, j'utilise \`with open(...)\` qui garantit la fermeture, et \`pathlib\` pour les chemins.`,
    future: `- **Bankable** : structurer un projet Python en modules à responsabilité unique est un prérequis pour toute base de code partagée — visible dans ton repo et évalué dès l'entretien technique.\n- Cette organisation est la base de tout ton code Python à venir : pipelines ETL (jours 138-144), projet DataPulse, et code data/ML des mois suivants.\n- Le réflexe « logique métier pure séparée de l'I/O » rend le code testable et se transfère à tout langage — c'est le fondement des architectures propres que tu verras plus tard.`,
    solution: {
      logic: "Solution simple : une CLI qui lit un JSON, agit, réécrit. Solution améliorée : découper en modules à responsabilité unique (stockage I/O, métier pur sans I/O, interface argparse), protéger le point d'entrée par \`if __name__ == \"__main__\"\`, ouvrir les fichiers avec \`with\` et manipuler les chemins avec \`pathlib\`, garder la logique métier immuable et testable sans fichier. La preuve de bon découpage : ajouter une commande ne touche qu'un module.",
      pitfalls: ["Tout mettre dans un seul fichier (I/O + métier + CLI) : intestable, non réutilisable — découper en modules.", "Oublier \`if __name__ == \"__main__\"\` : le code s'exécute à l'import, effet de bord surprenant.", "Ouvrir un fichier sans \`with\` (open/close manuel) : le fichier reste ouvert en cas d'erreur — fuite de ressource.", "Mélanger I/O et logique dans les mêmes fonctions : impossible de tester le métier sans toucher au disque."],
      checks: ["Le projet est découpé en modules à responsabilité unique (stockage, métier, interface).", "La logique métier est pure (sans I/O) et testable isolément.", "Le point d'entrée est protégé par \`if __name__ == \"__main__\"\`.", "Les fichiers sont ouverts avec \`with\` et les chemins gérés par \`pathlib\`.", "Ajouter une commande ne modifie qu'un module (bon découpage)."],
      oral: "Explique la structure : « stockage (I/O) séparé du métier (pur) séparé de l'interface — j'importe explicitement, rien n'est global ». Insiste sur le rôle de \`__main__\` (bibliothèque vs point d'entrée) et de \`with\` (fermeture garantie). Montrer qu'ajouter une commande ne touche qu'un module prouve que ton découpage est sain, pas cosmétique.",
    },
  },

  // ── Python : exceptions et robustesse ──
  122: {
    lessons: ['error-handling.md', 'python-foundations.md'],
    theory: `**Modèle mental : en Python, on demande PARDON plutôt que la permission (EAFP : « Easier to Ask Forgiveness than Permission ») — on TENTE l'opération et on rattrape l'exception SPÉCIFIQUE, au lieu de tout vérifier avant (LBYL, « Look Before You Leap »). Une exception non rattrapée n'est pas un bug, c'est un signal ; le vrai danger, c'est de rattraper trop large et de masquer les vrais bugs.** Rendre un programme robuste, ce n'est pas empêcher toute erreur, c'est prévoir COMMENT il réagit quand une opération prévisible échoue (fichier absent, JSON corrompu, entrée invalide).\n\nLes mécanismes des exceptions Python :\n1. **\`try / except SpécifiqueError\`** : on rattrape le TYPE précis d'erreur attendu (\`FileNotFoundError\`, \`json.JSONDecodeError\`, \`ValueError\`), jamais un \`except:\` nu qui avale TOUT — y compris les bugs de programmation et l'interruption clavier. Rattraper spécifiquement, c'est traiter le cas qu'on a prévu et laisser remonter ce qu'on n'a pas prévu.\n2. **\`else\` et \`finally\`** : \`else\` s'exécute si AUCUNE exception (le chemin heureux, séparé du try), \`finally\` s'exécute TOUJOURS (nettoyage). C'est plus expressif que le \`try/catch\` de JS, qui n'a pas de \`else\`.\n3. **\`raise\`** : lever une exception soi-même quand une précondition est violée (\`raise ValueError(\"le montant doit être positif\")\`). On échoue TÔT et FORT plutôt que de propager une donnée invalide.\n4. **Les exceptions personnalisées** : \`class NoteInvalideError(Exception)\` pour les erreurs MÉTIER, distinctes des erreurs techniques. L'appelant peut alors rattraper précisément « erreur métier » sans confondre avec un bug.\n\nLa hiérarchie compte : toutes les exceptions héritent de \`Exception\`. Rattraper \`Exception\` est parfois légitime (à une frontière, pour journaliser et re-lever), mais rattraper le type le plus SPÉCIFIQUE possible est la règle : ça documente ce qu'on attend et laisse les surprises remonter.\n\nLa règle du jour : **tente, rattrape le type SPÉCIFIQUE, échoue tôt avec \`raise\` sur une précondition violée, et ne masque jamais un bug avec un \`except\` trop large**. Un programme robuste transforme chaque échec prévisible en message clair pour l'utilisateur, et laisse les vrais bugs remonter pour être corrigés — jamais l'inverse.`,
    guided: `**Énoncé** : ajouter une gestion d'exceptions complète à la CLI \`notes\` (fichier absent, JSON corrompu, entrées invalides).\n\n**Exemple simple** : le contraste EAFP vs LBYL — au lieu de \`if FICHIER.exists(): ... else: ...\` (LBYL, sujet aux courses), on écrit \`try: with open(...): ... except FileNotFoundError: return []\` (EAFP). On tente, on rattrape le cas précis.\n\n**Raisonnement pas-à-pas** : identifier chaque opération faillible (lire, parser, valider) → rattraper le type SPÉCIFIQUE de chacune → traduire en message clair → lever soi-même sur une précondition métier violée.\n\n**Implémentation** :\n\`\`\`python\nimport json\nfrom pathlib import Path\n\nclass NoteInvalideError(Exception):   # erreur MÉTIER, distincte des erreurs techniques\n    pass\n\ndef charger(fichier: Path) -> list[dict]:\n    try:\n        with fichier.open(encoding=\"utf-8\") as f:\n            return json.load(f)\n    except FileNotFoundError:                 # cas prévu 1 : première utilisation\n        return []\n    except json.JSONDecodeError as e:         # cas prévu 2 : fichier corrompu\n        raise NoteInvalideError(f\"Fichier de notes corrompu : {e}\") from e\n\ndef ajouter(notes: list[dict], texte: str) -> list[dict]:\n    if not texte.strip():                     # précondition métier\n        raise NoteInvalideError(\"Une note ne peut pas être vide\")  # échoue TÔT\n    return [*notes, {\"id\": len(notes) + 1, \"texte\": texte}]\n\ndef main() -> None:\n    try:\n        notes = charger(Path(\"notes.json\"))\n        # ... action ...\n    except NoteInvalideError as e:            # frontière : message clair à l'utilisateur\n        print(f\"Erreur : {e}\")\n        raise SystemExit(1)                   # code de sortie non nul\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi éviter un \`except:\` nu ? (b) Que fait le bloc \`else\` d'un try ? (c) Quand lever une exception soi-même avec \`raise\` ?\n→ (a) il avale TOUT, y compris les bugs et l'interruption clavier, masquant les vrais problèmes ; (b) il s'exécute si aucune exception n'a été levée (le chemin heureux, séparé du try) ; (c) quand une précondition est violée, pour échouer tôt plutôt que propager une donnée invalide.\n\n**Variante** : ajoute le \`raise ... from e\` (chaînage d'exceptions) partout où tu retransformes une erreur — la trace conserve alors la cause d'origine, ce qui rend le débogage bien plus facile.`,
    caseStudy: `La gestion d'exceptions distingue un script fragile (qui plante avec une trace incompréhensible au premier fichier manquant) d'un outil robuste (qui dit « fichier introuvable, création d'un nouveau »). En entreprise, le bug le plus dangereux est le \`except\` trop large qui avale silencieusement les erreurs : un pipeline qui « marche » mais produit des données fausses parce qu'il a rattrapé et ignoré une exception qu'il n'aurait pas dû. Les revues de code Python traquent les \`except:\` nus précisément pour cette raison. Savoir rattraper le type spécifique, échouer tôt avec \`raise\` sur une précondition, et distinguer erreurs métier et techniques est une compétence de fiabilité directement valorisée — surtout pour des pipelines de données où une erreur masquée corrompt tout en aval.`,
    interview: `**« Comment gères-tu les erreurs en Python, et pourquoi éviter un \`except\` trop large ? »** → Je suis le style EAFP : je TENTE l'opération et je rattrape le type SPÉCIFIQUE d'exception attendu (\`FileNotFoundError\`, \`json.JSONDecodeError\`, \`ValueError\`), plutôt que de tout vérifier avant. Un \`except:\` nu ou un \`except Exception\` trop large est dangereux : il avale TOUT, y compris les bugs de programmation et l'interruption clavier, ce qui masque les vrais problèmes — le pire cas étant un pipeline qui « marche » mais produit des données fausses. Je rattrape le plus spécifique possible, je laisse remonter ce que je n'ai pas prévu, j'échoue tôt avec \`raise\` sur une précondition violée, et je distingue les erreurs métier (exceptions personnalisées) des erreurs techniques. J'utilise aussi \`raise ... from e\` pour conserver la cause d'origine.`,
    future: `- **Bankable** : rattraper spécifiquement, échouer tôt et ne jamais masquer un bug est une compétence de fiabilité recherchée — critique pour les pipelines data où une erreur avalée corrompt tout en aval.\n- Cette rigueur rend robustes tes pipelines ETL (jours 138-144) et le projet DataPulse, où la gestion d'échec partiel est centrale.\n- La distinction erreur attendue (rattrapée) vs inattendue (laissée remonter) est universelle — c'est le pendant Python des error boundaries du front (jour 111) et de la gestion d'erreur des API.`,
    solution: {
      logic: "Solution simple : un try/except autour des opérations risquées. Solution améliorée : rattraper le type SPÉCIFIQUE de chaque opération faillible (FileNotFoundError, JSONDecodeError, ValueError), lever soi-même (\`raise\`) sur les préconditions métier violées, distinguer erreurs métier (exception personnalisée) et techniques, chaîner avec \`from e\` pour conserver la cause, et traduire en message clair + code de sortie non nul à la frontière. La preuve : chaque échec prévisible donne un message utile, et un vrai bug remonte au lieu d'être avalé.",
      pitfalls: ["\`except:\` nu ou \`except Exception\` trop large : avale bugs et interruptions, masque les vrais problèmes — rattraper le type spécifique.", "Rattraper puis ignorer silencieusement (\`except: pass\`) : le pire — un pipeline qui « marche » en produisant des données fausses.", "Vérifier tout avant (LBYL) au lieu de tenter (EAFP) : verbeux et sujet aux courses — préférer try/except.", "Ne pas chaîner (\`raise ... from e\`) : la trace perd la cause d'origine, débogage plus difficile."],
      checks: ["Chaque opération faillible rattrape son type d'exception SPÉCIFIQUE.", "Aucun \`except:\` nu ni rattrapage silencieux.", "Les préconditions métier lèvent une exception (\`raise\`) — échec tôt.", "Les erreurs métier (exception personnalisée) sont distinctes des techniques.", "Les erreurs sont chaînées (\`from e\`) et traduites en message clair à la frontière."],
      oral: "Oppose EAFP (tenter + rattraper spécifique) et LBYL (tout vérifier avant), et explique pourquoi le \`except\` large est dangereux (avale les bugs, données fausses silencieuses). Cite l'échec tôt (\`raise\` sur précondition) et la distinction métier/technique. Le pipeline qui « marche » en produisant des données fausses est l'exemple qui prouve que tu comprends l'enjeu réel.",
    },
  },

  // ── Python : POO et style pythonique ──
  123: {
    lessons: ['python-foundations.md', 'clean-code.md'],
    theory: `**Modèle mental : en Python, la POO est OPTIONNELLE et LÉGÈRE — une classe regroupe des données ET le comportement qui les manipule, mais le style pythonique préfère les \`dataclass\` pour les données, la COMPOSITION à l'héritage, et le duck typing aux hiérarchies rigides. La question n'est pas « comment faire une classe » mais « ai-je vraiment besoin d'une classe ici ? ».** Beaucoup de code Python n'a pas besoin de POO : une fonction sur un \`dict\` suffit souvent. On introduit une classe quand des données et des comportements vont ENSEMBLE et bénéficient d'être encapsulés.\n\nCe qu'il faut maîtriser :\n1. **La classe et \`self\`** : \`class Compte:\`, \`def __init__(self, solde)\` (le constructeur), \`self\` est la référence explicite à l'instance (là où JS a un \`this\` implicite et piégeux). Chaque méthode reçoit \`self\` en premier paramètre.\n2. **La \`dataclass\`** : pour une classe qui porte surtout des DONNÉES, \`@dataclass\` génère automatiquement \`__init__\`, \`__repr__\` (affichage lisible) et l'égalité. \`@dataclass\\nclass Livre:\\n    titre: str\\n    auteur: str\` remplace 15 lignes de code répétitif. C'est l'idiome pythonique pour les structures de données.\n3. **\`__repr__\` et les méthodes spéciales** : \`__repr__\` donne une représentation lisible pour le débogage ; les « dunder methods » (\`__eq__\`, \`__len__\`...) intègrent tes objets aux idiomes du langage.\n4. **Composition > héritage** : plutôt qu'une hiérarchie profonde (\`ClientPremium(Client)\`), on COMPOSE (un \`Client\` a une \`Adresse\`, une \`Facturation\`). L'héritage crée un couplage rigide ; la composition reste souple. On n'hérite que pour une vraie relation « est-un ».\n5. **Duck typing** : « si ça marche comme un canard et cancane comme un canard, c'est un canard ». Python ne vérifie pas le TYPE mais le COMPORTEMENT : n'importe quel objet ayant une méthode \`.lire()\` peut être utilisé là où on attend un « lecteur », sans hiérarchie commune.\n\nQuand NE PAS utiliser de classe : si tu as une classe avec un seul \`__init__\` et une seule méthode, c'est probablement une FONCTION déguisée. Si tu n'as que des données sans comportement, une \`dataclass\` (ou un simple \`dict\`) suffit.\n\nLa règle du jour : **une classe pour regrouper données + comportement quand ça a du sens ; une \`dataclass\` pour les données ; la composition par défaut, l'héritage seulement pour un vrai « est-un » ; et pas de classe quand une fonction suffit**. Le style pythonique privilégie la simplicité — la POO est un outil, pas une obligation.`,
    guided: `**Énoncé** : modéliser un système simple en classes Python idiomatiques (\`dataclass\`, style pythonique).\n\n**Exemple simple** : la \`dataclass\` qui remplace le code répétitif — \`@dataclass\\nclass Livre:\\n    titre: str\\n    auteur: str\\n    dispo: bool = True\`. Trois lignes te donnent constructeur, affichage lisible et égalité. C'est l'idiome à réflexe pour toute structure de données.\n\n**Raisonnement pas-à-pas** : distinguer ce qui est DONNÉE (dataclass) de ce qui est COMPORTEMENT (méthodes) → composer plutôt qu'hériter → n'ajouter une classe que si données et comportement vont ensemble.\n\n**Implémentation** :\n\`\`\`python\nfrom dataclasses import dataclass, field\n\n@dataclass\nclass Livre:                      # DONNÉES : dataclass (init/repr/eq générés)\n    titre: str\n    auteur: str\n    dispo: bool = True\n\n@dataclass\nclass Bibliotheque:               # DONNÉES + COMPORTEMENT : classe justifiée\n    livres: list[Livre] = field(default_factory=list)   # (pas [] mutable partagé !)\n    def ajouter(self, livre: Livre) -> None:\n        self.livres.append(livre)\n    def disponibles(self) -> list[Livre]:\n        return [l for l in self.livres if l.dispo]        # comprehension\n    def __len__(self) -> int:      # dunder : len(biblio) fonctionne\n        return len(self.livres)\n\nbiblio = Bibliotheque()\nbiblio.ajouter(Livre(\"React\", \"Dan\"))\nprint(len(biblio), biblio.disponibles())   # repr lisible gratuit\n\`\`\`\n\n**Mini-quiz** : (a) Quand préférer une \`dataclass\` à une classe classique ? (b) Pourquoi \`field(default_factory=list)\` et pas \`= []\` ? (c) Composition ou héritage par défaut ?\n→ (a) quand la classe porte surtout des données : \`@dataclass\` génère init/repr/eq ; (b) une valeur par défaut mutable (\`[]\`) est PARTAGÉE entre toutes les instances — piège classique ; \`default_factory\` crée une liste neuve à chaque fois ; (c) composition par défaut ; l'héritage seulement pour un vrai « est-un ».\n\n**Variante** : prends une de tes classes et demande-toi si elle a plus d'un \`__init__\` et une seule méthode — si oui, réécris-la en fonction. Savoir SUPPRIMER une classe inutile est aussi pythonique que savoir en écrire une.`,
    caseStudy: `Le style POO pythonique — dataclasses pour les données, composition, pas de classe superflue — produit du code plus court et plus lisible que la POO « à la Java » (hiérarchies profondes, getters/setters partout) que les développeurs venus d'autres langages ont tendance à écrire. Dans les bases de code Python data/ML, les dataclasses structurent les configurations, les enregistrements et les résultats d'analyse ; l'over-engineering POO (une classe pour ce qui devrait être une fonction) est un anti-pattern que les revues signalent. Le piège de la valeur par défaut mutable (\`= []\`) est un bug réel et fréquent qui partage un état entre instances. Savoir quand une classe est justifiée — et quand une fonction ou une dataclass suffit — est un marqueur de maturité Python valorisé.`,
    interview: `**« Quand utilises-tu une classe en Python, et qu'est-ce que le style pythonique ? »** → Une classe quand des DONNÉES et le COMPORTEMENT qui les manipule vont ensemble et bénéficient d'être encapsulés — pas par défaut. Pour des données surtout, j'utilise une \`@dataclass\` qui génère \`__init__\`, \`__repr__\` et l'égalité automatiquement. Je préfère la COMPOSITION à l'héritage (l'héritage crée un couplage rigide, réservé aux vraies relations « est-un ») et je m'appuie sur le duck typing : Python vérifie le comportement, pas le type, donc pas besoin de hiérarchie commune. Le style pythonique privilégie la simplicité : si une classe n'a qu'un \`__init__\` et une méthode, c'est une fonction déguisée. Attention au piège de la valeur par défaut mutable (\`= []\`), partagée entre instances — j'utilise \`field(default_factory=list)\`.`,
    future: `- **Bankable** : savoir quand une classe est justifiée (et quand une dataclass ou une fonction suffit) évite l'over-engineering et signale une maturité Python — recherchée au-delà du débutant.\n- Les dataclasses structureront tes configurations, enregistrements et résultats d'analyse dans les pipelines et le code ML des mois suivants.\n- Le réflexe « composition > héritage, pas de classe superflue » est un principe de conception transférable, et le piège de la valeur par défaut mutable est un bug que tu sauras désormais éviter partout.`,
    solution: {
      logic: "Solution simple : des classes avec __init__ et des méthodes. Solution améliorée : utiliser des \`@dataclass\` pour les structures de données (init/repr/eq générés), n'ajouter une classe que si données et comportement vont ensemble, composer plutôt qu'hériter, exposer les idiomes via les dunder methods (\`__len__\`), éviter la valeur par défaut mutable (\`field(default_factory=list)\`), et supprimer les classes qui ne sont que des fonctions déguisées. La preuve : le code est plus court et lisible qu'une POO « à la Java ».",
      pitfalls: ["Valeur par défaut mutable (\`livres: list = []\`) : partagée entre TOUTES les instances — utiliser \`field(default_factory=list)\`.", "Une classe pour ce qui devrait être une fonction (un seul __init__ + une méthode) : over-engineering.", "Hiérarchie d'héritage profonde là où la composition suffit : couplage rigide et fragile.", "Réécrire à la main \`__init__\`/\`__repr__\` que \`@dataclass\` génère : code répétitif et sujet aux erreurs."],
      checks: ["Les structures de données utilisent \`@dataclass\`.", "Une classe n'existe que si données et comportement vont ensemble.", "La composition est préférée à l'héritage (héritage réservé au « est-un »).", "Aucune valeur par défaut mutable (\`field(default_factory=...)\` utilisé).", "Aucune classe n'est une fonction déguisée."],
      oral: "Pose la question d'abord : « ai-je vraiment besoin d'une classe ? ». Explique dataclass (données), composition > héritage, duck typing (comportement pas type), et le piège de la valeur par défaut mutable. Savoir dire « ici une fonction suffit » est aussi pythonique que savoir écrire une classe — c'est ce qui distingue du code Python idiomatique d'une POO plaquée d'un autre langage.",
    },
  },

  // ── Python : tests (pytest) ──
  124: {
    lessons: ['testing-foundations.md'],
    theory: `**Modèle mental : pytest fait du test une simple FONCTION qui commence par \`test_\` et contient un \`assert\` — pas de framework verbeux ni de classes obligatoires ; sa valeur se prouve, comme tout test, en le faisant ROUGIR, et les FIXTURES fournissent un contexte réutilisable sans dupliquer le setup.** Tu connais les principes du test (jour 106, en JS) : figer un comportement, couvrir les cas limites, prouver qu'un test peut échouer. pytest est l'outil qui rend cela minimal et agréable en Python — au point que l'absence de cérémonie est sa force.\n\nCe qui rend pytest distinctif :\n1. **La simplicité radicale** : une fonction \`def test_ajoute_une_note():\` avec un \`assert resultat == attendu\`. pytest DÉCOUVRE automatiquement les fichiers \`test_*.py\` et les fonctions \`test_*\`. Pas de \`expect().toBe()\` — juste \`assert\`, et pytest affiche intelligemment pourquoi l'assertion a échoué (les valeurs comparées).\n2. **Les fixtures** : une fonction décorée \`@pytest.fixture\` qui prépare un contexte (des données, un fichier temporaire, une base en mémoire) et le fournit aux tests qui la demandent en PARAMÈTRE. C'est l'injection de dépendances du test : le setup est écrit UNE fois et réutilisé, sans le dupliquer dans chaque test. \`tmp_path\` (fixture intégrée) donne un dossier temporaire propre par test.\n3. **La paramétrisation** : \`@pytest.mark.parametrize\` exécute le MÊME test sur plusieurs jeux d'entrées/sorties — une table de cas plutôt que dix fonctions copiées. Idéal pour couvrir les cas limites d'une fonction de validation.\n4. **Le rougissement, toujours** : le principe du jour 106 reste vrai — un test qui ne peut pas échouer ne teste rien. On sabote le code pour vérifier que le test rougit.\n\nLa règle du jour : **écris des tests \`test_*\` minimalistes avec \`assert\`, factorise le setup en fixtures, couvre les cas limites par paramétrisation, et prouve chaque test en le faisant rougir**. pytest récompense le test parce qu'il est sans friction — et une suite verte sur ta CLI et tes fonctions est ce qui te permet de refactorer ton Python en confiance, exactement comme au front.`,
    guided: `**Énoncé** : installer pytest, écrire 10+ tests sur la CLI \`notes\` et ses fonctions, prouver le rougissement.\n\n**Exemple simple** : le test pytest minimal — \`def test_ajoute_incremente_id():\\n    assert ajouter([], \"a\")[0][\"id\"] == 1\`. Une fonction \`test_\`, un \`assert\`. pytest la découvre et l'exécute sans configuration.\n\n**Raisonnement pas-à-pas** : tester la logique métier pure (facile, sans I/O) → utiliser une fixture \`tmp_path\` pour tester le stockage sans polluer le disque → paramétrer les cas limites → saboter pour prouver le rougissement.\n\n**Implémentation** :\n\`\`\`python\nimport json, pytest\nfrom notes.metier import ajouter, chercher\nfrom notes.stockage import charger, sauver\n\n# 1. Logique pure — cas normaux et limites\ndef test_ajoute_incremente_id():\n    assert ajouter([], \"a\")[0][\"id\"] == 1\n\n@pytest.mark.parametrize(\"mot, attendu\", [   # une table de cas, pas 3 fonctions\n    (\"react\", 1), (\"REACT\", 1), (\"zzz\", 0),\n])\ndef test_cherche_insensible_casse(mot, attendu):\n    notes = [{\"id\": 1, \"texte\": \"React\"}]\n    assert len(chercher(notes, mot)) == attendu\n\n# 2. Stockage — fixture tmp_path : dossier temporaire propre par test\ndef test_sauver_puis_charger(tmp_path):\n    fichier = tmp_path / \"notes.json\"\n    sauver(fichier, [{\"id\": 1, \"texte\": \"a\"}])\n    assert charger(fichier) == [{\"id\": 1, \"texte\": \"a\"}]   # aller-retour\n\ndef test_charger_fichier_absent(tmp_path):\n    assert charger(tmp_path / \"absent.json\") == []          # cas prévu (jour 122)\n# PREUVE : change \`len(notes) + 1\` en \`len(notes)\` dans ajouter → test_ajoute rougit.\n\`\`\`\n\n**Mini-quiz** : (a) Comment pytest sait-il quelles fonctions sont des tests ? (b) À quoi sert la fixture \`tmp_path\` ? (c) Que fait \`@pytest.mark.parametrize\` ?\n→ (a) il découvre les fichiers \`test_*.py\` et les fonctions \`test_*\` automatiquement ; (b) fournir un dossier temporaire propre par test, pour tester l'I/O sans polluer le disque ; (c) exécuter le même test sur plusieurs jeux d'entrées/sorties (table de cas).\n\n**Variante** : écris une fixture personnalisée \`@pytest.fixture\` qui renvoie un jeu de notes de départ, et réutilise-la dans plusieurs tests — tu factorises le setup au lieu de le recopier dans chaque fonction.`,
    caseStudy: `pytest est le standard de fait des tests Python en entreprise, précisément parce que sa simplicité (fonction + \`assert\`) supprime la friction qui décourage d'écrire des tests. Les fixtures permettent de tester du code qui touche des fichiers, des bases ou des API sans dupliquer le setup ni polluer l'environnement — \`tmp_path\` pour un fichier jetable, une fixture de base en mémoire pour un test d'intégration. Dans les projets data, une suite pytest verte est ce qui permet de refactorer un pipeline sans craindre de casser une transformation silencieusement. Une suite de tests bien faite est directement lisible dans un repo et signale un développeur qui livre du code fiable — un critère que les recruteurs et les équipes vérifient systématiquement.`,
    interview: `**« Comment testes-tu en Python avec pytest ? »** → Un test est une fonction \`test_*\` avec un simple \`assert\` ; pytest découvre automatiquement les fichiers et fonctions de test, et affiche intelligemment pourquoi une assertion échoue. Pour le code qui touche l'I/O, j'utilise des FIXTURES — des fonctions \`@pytest.fixture\` qui préparent un contexte réutilisable (par exemple \`tmp_path\` pour un fichier temporaire propre par test), demandées en paramètre : le setup est écrit une fois, pas dupliqué. Pour couvrir les cas limites, je paramètre avec \`@pytest.mark.parametrize\` — une table de cas plutôt que dix fonctions copiées. Et comme tout test, la valeur se prouve en le faisant rougir : je sabote le code pour vérifier que le test échoue. Une suite verte me permet de refactorer en confiance.`,
    future: `- **Bankable** : une suite pytest verte sur ton code est un signal de fiabilité directement lisible dans un repo — un critère que recruteurs et équipes vérifient.\n- Ces tests protègent tes fonctions de nettoyage (jour 132), tes pipelines ETL (138-144) et le projet DataPulse contre les régressions silencieuses.\n- Les fixtures et la paramétrisation se réutilisent pour tester tout code Python data/ML — et le réflexe « prouver le rougissement » reste ta garantie qu'un test protège vraiment.`,
    solution: {
      logic: "Solution simple : des fonctions test_ avec assert sur la logique. Solution améliorée : tester la logique métier pure (sans I/O) ET le stockage via la fixture \`tmp_path\` (pas de pollution du disque), couvrir les cas limites par \`@pytest.mark.parametrize\`, factoriser le setup en fixtures personnalisées, et prouver chaque test en sabotant le code. La preuve de valeur : une régression volontaire (ex. l'incrément d'id) casse le bon test.",
      pitfalls: ["Tester en écrivant/lisant de vrais fichiers du projet : tests non déterministes et polluants — utiliser \`tmp_path\`.", "Dupliquer le même setup dans chaque test au lieu d'une fixture : maintenance lourde.", "Copier dix fonctions pour dix cas au lieu de \`parametrize\` : bruit et oublis.", "Ne pas vérifier le rougissement : une suite verte qui ne peut pas échouer donne un faux sentiment de sécurité."],
      checks: ["Les tests sont des fonctions \`test_*\` avec \`assert\`, découvertes automatiquement.", "L'I/O est testée via \`tmp_path\` (aucune pollution du disque).", "Les cas limites sont couverts par \`@pytest.mark.parametrize\`.", "Le setup partagé est factorisé en fixtures.", "Chaque test a été prouvé en sabotant le code (il rougit)."],
      oral: "Vante la simplicité de pytest (fonction + \`assert\`, découverte automatique) et explique les fixtures (setup réutilisable, \`tmp_path\` pour l'I/O jetable) et \`parametrize\` (table de cas). Rappelle le principe universel : « je sabote le code pour prouver que le test peut rougir ». Une suite verte qui permet de refactorer sans peur est l'argument qui montre la vraie valeur des tests.",
    },
  },

  // ── Python : environnements et outils ──
  125: {
    lessons: ['python-foundations.md'],
    theory: `**Modèle mental : un projet Python REPRODUCTIBLE tient sur trois piliers — un \`venv\` qui ISOLE les dépendances, un fichier qui les FIGE (requirements/pyproject), et un linter/formateur (ruff/black) qui rend le style NON négociable et AUTOMATIQUE. Sans ces trois, « ça marche sur ma machine » est la norme, pas l'exception.** Tu as croisé le venv au jour 120 ; aujourd'hui tu montes l'outillage complet d'un projet Python professionnel. Ces outils ne sont pas de la bureaucratie : ils garantissent qu'un collègue (ou toi dans six mois) peut installer et faire tourner ton projet à l'identique.\n\nLes trois piliers en détail :\n1. **L'isolation — \`venv\`** : Python installe les paquets GLOBALEMENT par défaut, donc deux projets qui veulent des versions différentes d'une bibliothèque entrent en conflit. \`python -m venv .venv\` crée un environnement isolé par projet ; \`source .venv/bin/activate\` l'active. Chaque projet a SES dépendances, sans polluer le système ni les autres projets.\n2. **Le figeage — \`requirements.txt\` / \`pyproject.toml\`** : lister les dépendances AVEC leurs versions (\`pandas==2.2.0\`) pour que l'installation soit reproductible. \`pip freeze > requirements.txt\` capture l'état exact ; \`pip install -r requirements.txt\` le restaure. \`pyproject.toml\` est le standard moderne qui regroupe dépendances, métadonnées et config des outils.\n3. **Le style automatique — linter + formateur** : \`black\` (ou \`ruff format\`) FORMATE le code de façon déterministe (fini les débats sur les espaces) ; \`ruff\` (linter) DÉTECTE les problèmes (imports inutilisés, variables non définies, anti-patterns) avant l'exécution. Le style devient une propriété de l'outil, pas une discipline manuelle — comme ESLint/Prettier au front.\n\nPourquoi c'est décisif : un projet sans isolation casse dès qu'une dépendance change ailleurs ; un projet sans figeage n'est pas installable à l'identique ; un projet sans linter/formateur accumule les incohérences de style et les bugs triviaux. Ces outils sont le premier réflexe d'hygiène que TOUTE équipe Python exige.\n\nLa règle du jour : **un projet Python = venv isolé + dépendances figées + linter/formateur configurés, le tout documenté**. C'est ce qui rend ton code installable, reproductible et propre — les conditions de base pour travailler à plusieurs ou montrer un projet.`,
    guided: `**Énoncé** : configurer un projet Python propre (venv, requirements, ruff/black, structure) et documenter 5 conventions.\n\n**Exemple simple** : la séquence d'isolation à connaître — \`python -m venv .venv && source .venv/bin/activate && pip install pandas\`. À partir de là, \`pandas\` n'existe que dans CE projet. \`deactivate\` en sort. C'est le point de départ de tout projet Python.\n\n**Raisonnement pas-à-pas** : créer et activer le venv → installer et figer les dépendances → configurer ruff/black → structurer le projet → documenter les conventions.\n\n**Mise en place** :\n\`\`\`bash\npython -m venv .venv && source .venv/bin/activate   # 1. isolation\npip install pandas pytest ruff                       # 2. dépendances du projet\npip freeze > requirements.txt                        # 3. figeage reproductible\nruff check . && ruff format .                         # 4. lint + format automatiques\n\`\`\`\n\`\`\`toml\n# pyproject.toml — configuration centralisée (standard moderne)\n[project]\nname = \"datapulse\"\ndependencies = [\"pandas>=2.2\", \"pytest>=8.0\"]\n[tool.ruff]\nline-length = 100\n\`\`\`\n\`\`\`\nStructure :  datapulse/  (code)   tests/  (tests)   pyproject.toml   README.md   .gitignore (dont .venv/)\nMes 5 conventions : snake_case · type hints partout · docstrings sur les fonctions publiques ·\n                    ruff format avant chaque commit · tests dans tests/ miroir du code\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi un venv par projet ? (b) Que capture \`pip freeze\` ? (c) Quelle différence entre \`black\`/\`ruff format\` et \`ruff check\` ?\n→ (a) Python installe globalement par défaut ; le venv isole les dépendances pour éviter les conflits de versions entre projets ; (b) l'état exact des dépendances installées avec leurs versions, pour une installation reproductible ; (c) le formateur (black/ruff format) réécrit le style de façon déterministe ; le linter (ruff check) détecte les problèmes (imports inutiles, bugs) sans réécrire.\n\n**Variante** : ajoute \`.venv/\` à ton \`.gitignore\` et vérifie que \`git status\` ne propose plus de committer des milliers de fichiers de dépendances — un venv ne se commite JAMAIS, seul le \`requirements.txt\`/\`pyproject.toml\` le fait.`,
    caseStudy: `L'outillage (venv + dépendances figées + linter/formateur) est le premier réflexe d'hygiène qu'exige toute équipe Python, et son absence est la cause n°1 du « ça marche sur ma machine » : un projet sans venv casse quand une dépendance change ailleurs, un projet sans requirements n'est pas installable à l'identique, un projet sans formateur accumule les incohérences et les bugs triviaux. En entreprise, la CI refuse un code non formaté ou qui échoue au linter, et un projet non reproductible est un risque. Pour un portfolio, un projet avec un venv propre, un \`requirements.txt\`/\`pyproject.toml\`, une config ruff et un \`.gitignore\` correct signale immédiatement un développeur qui connaît les standards — bien avant même de lire le code métier.`,
    interview: `**« Comment mets-tu en place un projet Python reproductible ? »** → Trois piliers. L'ISOLATION : un \`venv\` par projet (\`python -m venv .venv\`), parce que Python installe globalement par défaut et que deux projets peuvent vouloir des versions différentes d'une bibliothèque. Le FIGEAGE : \`requirements.txt\` (via \`pip freeze\`) ou \`pyproject.toml\` qui liste les dépendances avec leurs versions, pour une installation à l'identique. Le STYLE AUTOMATIQUE : un formateur (\`black\` ou \`ruff format\`) qui rend le style déterministe et un linter (\`ruff\`) qui détecte imports inutiles et bugs avant l'exécution — l'équivalent de Prettier/ESLint. J'ajoute un \`.gitignore\` (le venv ne se commite jamais, seul le fichier de dépendances). Sans ces trois, « ça marche sur ma machine » devient la norme.`,
    future: `- **Bankable** : un projet avec venv, dépendances figées, ruff/black et .gitignore correct signale un développeur qui connaît les standards — visible avant même le code métier, exigé par toute CI.\n- Cet outillage est le socle du projet DataPulse (jours 141-146) et de tout code Python que tu montreras ou partageras.\n- La reproductibilité (« un collègue installe et lance à l'identique ») est un principe universel — c'est le pendant Python de la conteneurisation et des lockfiles que tu verras plus tard.`,
    solution: {
      logic: "Solution simple : un venv et un pip install. Solution améliorée : les trois piliers — venv isolé, dépendances FIGÉES avec versions (requirements.txt/pyproject.toml), linter + formateur (ruff/black) configurés — plus une structure claire (code/tests séparés), un \`.gitignore\` qui exclut le venv, et 5 conventions documentées. La preuve de reproductibilité : un tiers clone, \`pip install -r requirements.txt\`, et le projet tourne à l'identique.",
      pitfalls: ["Installer globalement sans venv : conflits de versions entre projets, « ça marche sur ma machine ».", "Ne pas figer les versions (requirements sans numéros) : l'installation n'est pas reproductible.", "Committer le dossier \`.venv/\` : des milliers de fichiers inutiles dans le repo — l'exclure via \`.gitignore\`.", "Compter sur la discipline manuelle pour le style au lieu d'un formateur : incohérences et débats stériles."],
      checks: ["Un venv isole les dépendances du projet.", "Les dépendances sont figées avec versions (requirements.txt/pyproject.toml).", "Un linter et un formateur (ruff/black) sont configurés.", "Le \`.gitignore\` exclut le venv (jamais commité).", "5 conventions sont documentées et la structure sépare code et tests."],
      oral: "Structure ta réponse en trois piliers : isolation (venv), figeage (requirements/pyproject), style automatique (ruff/black). Explique le POURQUOI de chacun (Python installe globalement ; installation reproductible ; style non négociable). Mentionne que le venv ne se commite jamais. « Un collègue clone et lance à l'identique » est le critère qui prouve que tu vises la reproductibilité, pas juste « ça tourne ».",
    },
  },

  // ── Revue semaine 18 : Python cœur ──
  126: {
    reviewSynthese: `La semaine 18 a transformé « je connais la syntaxe Python » en « je sais construire un projet Python propre » : syntaxe et structures idiomatiques (jour 120), fonctions/modules/fichiers et organisation en responsabilités uniques (121), gestion d'exceptions robuste avec rattrapage spécifique et échec tôt (122), POO pythonique légère — dataclasses, composition, pas de classe superflue (123), tests pytest minimalistes avec fixtures et paramétrisation (124), et outillage reproductible — venv, dépendances figées, ruff/black (125). Le fil rouge : **du Python professionnel n'est pas du code qui marche une fois, c'est du code ORGANISÉ (modules), ROBUSTE (exceptions spécifiques), TESTÉ (pytest) et REPRODUCTIBLE (venv + outils)**. Tu entres dans la semaine 19 (pandas) avec les fondations pour écrire du code data propre, pas seulement des notebooks jetables.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Organiser un projet en modules à responsabilité unique (I/O, métier, interface) | |\n| Gérer les exceptions spécifiquement (EAFP), échouer tôt avec \`raise\` | |\n| Écrire du Python POO idiomatique (dataclass, composition, pas de classe superflue) | |\n| Tester avec pytest (assert, fixtures, parametrize) et prouver le rougissement | |\n| Configurer un projet reproductible (venv, dépendances figées, ruff/black) | |\n| Écrire du Python idiomatique (comprehensions, dict, enumerate/zip) | |\n\n**Total /18** : ≥ 15 = solide, attaque pandas · 11-14 = revoir les items ≤ 1 · < 11 = rejouer avant pandas, car tu vas écrire beaucoup de Python data.`,
    remediation: `- **Script monolithique** (tout dans un fichier) : rejoue le jour 121 — découpe en modules (I/O, métier pur, interface), protège le point d'entrée avec \`__main__\`, logique testable sans fichier.\n- **Exceptions mal gérées** (\`except:\` large, plantages) : jour 122 — rattrape le type spécifique, échoue tôt avec \`raise\`, ne masque jamais un bug ; distingue erreurs métier et techniques.\n- **POO plaquée d'un autre langage** (hiérarchies, getters/setters) : jour 123 — dataclass pour les données, composition, pas de classe quand une fonction suffit ; attention à la valeur par défaut mutable.\n- **Pas de tests ou tests fragiles** : jour 124 — pytest (assert, fixtures \`tmp_path\`, parametrize), prouve le rougissement.\n- **Projet non reproductible** : jour 125 — venv, dépendances figées, ruff/black, .gitignore ; un collègue doit pouvoir installer et lancer à l'identique.`,
    interview: `1. **« Comment organises-tu un projet Python ? »** Attendu : modules à responsabilité unique (I/O, métier pur, interface), import explicite, \`__main__\` pour séparer bibliothèque et point d'entrée.\n2. **« Pourquoi éviter un \`except\` trop large ? »** Attendu : il avale bugs et interruptions, masque les vrais problèmes (données fausses silencieuses) ; rattraper le type spécifique, échouer tôt avec \`raise\`.\n3. **« Quand une classe en Python, et le style pythonique ? »** Attendu : classe si données + comportement ensemble ; dataclass pour les données ; composition > héritage ; pas de classe pour une fonction ; piège de la valeur par défaut mutable.\n4. **« Comment testes-tu avec pytest ? »** Attendu : fonctions \`test_*\` + assert, fixtures (\`tmp_path\`) pour l'I/O, parametrize pour les cas limites, prouver le rougissement.\n5. **« Comment rends-tu un projet Python reproductible ? »** Attendu : venv isolé, dépendances figées (requirements/pyproject), linter/formateur, venv jamais commité.`,
  },

  // ── pandas : charger et inspecter ──
  127: {
    lessons: ['pandas-data-wrangling.md'],
    theory: `**Modèle mental : un DataFrame pandas est un TABLEAU 2D ÉTIQUETÉ — des lignes indexées et des colonnes NOMMÉES et TYPÉES — et pandas manipule des COLONNES entières d'un coup (vectorisation), pas des cellules une par une avec des boucles. Charger des données, c'est d'abord les INSPECTER avant d'y toucher.** Si tu penses « boucle sur chaque ligne » (le réflexe JS/Python de base), tu écris du pandas lent et non idiomatique. Le bon réflexe : « quelle opération sur la colonne entière ? ». Un DataFrame ressemble à une table SQL chargée en mémoire, avec la puissance d'expression de Python.\n\nLes notions du jour :\n1. **Series et DataFrame** : une \`Series\` est une colonne (un tableau 1D étiqueté avec un dtype) ; un \`DataFrame\` est un ensemble de Series partageant le même index. Chaque colonne a un TYPE (\`int64\`, \`float64\`, \`object\` pour le texte, \`datetime64\`, \`bool\`) — et le type conditionne ce qu'on peut faire.\n2. **Charger** : \`pd.read_csv(\"data.csv\")\`, \`pd.read_json(...)\` — pandas infère les types, mais l'inférence peut se tromper (un code postal lu comme un nombre, une date comme du texte). D'où l'inspection.\n3. **Inspecter AVANT d'agir** — le réflexe professionnel :\n   - \`df.shape\` : combien de lignes/colonnes (l'ordre de grandeur).\n   - \`df.info()\` : les colonnes, leurs TYPES, et le nombre de valeurs non-nulles (donc les manquants).\n   - \`df.describe()\` : les statistiques (min/max/moyenne/quartiles) des colonnes numériques — révèle les valeurs aberrantes.\n   - \`df.head()\` / \`df.sample(5)\` : à quoi ressemblent vraiment les données.\n   - \`df[\"col\"].value_counts()\` : la distribution d'une colonne catégorielle — révèle les catégories inattendues, les fautes de frappe, les déséquilibres.\n\nPourquoi inspecter d'abord : les données réelles sont TOUJOURS plus sales et surprenantes qu'annoncé. Sauter l'inspection, c'est construire une analyse sur des types faux, des manquants ignorés et des valeurs aberrantes invisibles — l'analyse sera fausse sans que rien ne plante. Inspecter, c'est se forger une compréhension AVANT de conclure.\n\nLa règle du jour : **charge, puis INSPECTE systématiquement (shape, info, describe, value_counts) et formule des observations, AVANT toute transformation ; pense colonnes vectorisées, pas boucles**. L'inspection n'est pas une formalité, c'est là que tu découvres ce que tes données sont réellement.`,
    guided: `**Énoncé** : charger un vrai dataset, l'inspecter (info/describe/value_counts) et formuler 5 observations.\n\n**Exemple simple** : la première commande après tout chargement — \`df.info()\`. Elle te dit en une ligne par colonne : le nom, le type, et combien de valeurs manquent. Avant \`info()\`, tu ne sais pas ce que tu as ; après, tu sais où regarder.\n\n**Raisonnement pas-à-pas** : charger → mesurer la taille (shape) → examiner types et manquants (info) → statistiques numériques (describe) → distributions catégorielles (value_counts) → écrire ce qu'on OBSERVE.\n\n**Implémentation** :\n\`\`\`python\nimport pandas as pd\ndf = pd.read_csv(\"ventes.csv\")\n\nprint(df.shape)                 # (10432, 8) — ordre de grandeur\ndf.info()                       # types + non-nuls : 'date' est object (→ à convertir !), 'prix' a 210 manquants\nprint(df.describe())            # prix : max=99999 (aberrant ?), min=-5 (impossible → donnée sale)\nprint(df[\"categorie\"].value_counts())   # 'Livre' 4000, 'livre' 12 (faute de casse !), 'Jeu' 3000\n\n# 5 observations (le vrai livrable de l'inspection) :\n# 1. 'date' est typée object, pas datetime → conversion nécessaire avant analyse temporelle.\n# 2. 'prix' a 210 valeurs manquantes (2%) et un min négatif (-5) → données à nettoyer (jour 128).\n# 3. 'categorie' a des doublons de casse ('Livre'/'livre') → normalisation nécessaire.\n# 4. describe montre un prix max de 99999 → valeur aberrante probable à investiguer.\n# 5. 10432 lignes, 8 colonnes → volume modeste, tient en mémoire sans souci.\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi inspecter AVANT de transformer ? (b) Que révèle \`value_counts()\` qu'un \`describe()\` ne montre pas ? (c) Pourquoi pandas est-il rapide sans boucles ?\n→ (a) les données réelles sont plus sales qu'annoncé ; sauter l'inspection produit une analyse fausse sans plantage ; (b) la distribution d'une colonne catégorielle — catégories inattendues, fautes de casse, déséquilibres (describe ne couvre que le numérique) ; (c) il vectorise : les opérations s'appliquent à la colonne entière en code optimisé, pas cellule par cellule.\n\n**Variante** : après l'inspection, écris \`df.isna().sum()\` pour compter les manquants par colonne — c'est la carte précise de ce que le jour 128 devra nettoyer.`,
    caseStudy: `L'inspection systématique avant toute analyse est ce qui sépare un analyste fiable d'un producteur de conclusions fausses : les données réelles arrivent avec des types mal inférés (dates en texte, codes en nombres), des manquants, des doublons de casse et des valeurs aberrantes — et une analyse construite dessus sans inspection est fausse SANS que rien ne plante, ce qui est le pire des cas. En entreprise, un rapport basé sur des données non inspectées peut orienter une décision coûteuse dans la mauvaise direction. Le réflexe « shape → info → describe → value_counts → observations » est le premier geste attendu de tout data analyst, et la capacité à formuler des observations pertinentes à partir de cette inspection est directement évaluée en test technique data.`,
    interview: `**« Quels sont tes premiers gestes quand tu reçois un nouveau dataset ? »** → J'INSPECTE avant de toucher quoi que ce soit, parce que les données réelles sont toujours plus sales qu'annoncé et qu'une analyse sur des données non inspectées est fausse sans planter. Ma séquence : \`df.shape\` pour l'ordre de grandeur, \`df.info()\` pour les types et les manquants (une date lue comme texte, une colonne à 2% de nuls), \`df.describe()\` pour les statistiques numériques et les valeurs aberrantes (un prix négatif ou à 99999), \`value_counts()\` sur les colonnes catégorielles pour repérer les catégories inattendues et les fautes de casse. J'en tire des observations écrites qui orientent le nettoyage. Et je pense en COLONNES vectorisées, pas en boucles — un DataFrame se manipule colonne entière par colonne entière.`,
    future: `- **Bankable** : le réflexe d'inspection systématique (shape/info/describe/value_counts → observations) est le premier geste attendu d'un data analyst — directement évalué en test technique data.\n- Cette inspection précède chaque nettoyage (jour 128), chaque analyse et le projet DataPulse (jours 141-146).\n- Penser « colonnes vectorisées, pas boucles » est le fondement de tout travail pandas/NumPy performant, jusqu'au feature engineering pour le ML (mois 6-7).`,
    solution: {
      logic: "Solution simple : charger le CSV et regarder les premières lignes. Solution améliorée : inspecter systématiquement (shape pour la taille, info pour types et manquants, describe pour les aberrants numériques, value_counts pour les distributions catégorielles et fautes de casse), puis FORMULER des observations écrites qui orientent le nettoyage — le tout en pensant colonnes vectorisées. La preuve : les 5 observations pointent des problèmes réels et actionnables (type à convertir, manquants, doublons de casse, aberrants).",
      pitfalls: ["Transformer/analyser sans inspecter : une analyse fausse sur des types mal inférés et des manquants, SANS plantage.", "Se fier à l'inférence de type de pandas : une date peut être lue comme texte, un code postal comme un nombre.", "Ignorer les colonnes catégorielles (pas de value_counts) : on rate les fautes de casse et catégories inattendues.", "Boucler ligne par ligne au lieu de vectoriser : lent et non idiomatique."],
      checks: ["shape, info, describe et value_counts ont été utilisés.", "Les types mal inférés (dates, codes) sont repérés.", "Les manquants et valeurs aberrantes sont identifiés.", "Au moins 5 observations écrites orientent le nettoyage à venir.", "L'analyse pense en colonnes vectorisées, pas en boucles."],
      oral: "Déroule la séquence d'inspection (shape → info → describe → value_counts) en expliquant ce que CHAQUE commande révèle. Insiste sur le POURQUOI : « une analyse sur des données non inspectées est fausse sans planter, le pire cas ». Donner un exemple concret d'observation (une date en texte, un prix négatif) prouve que tu sais lire des données réelles, pas juste appeler des méthodes.",
    },
  },

  // ── pandas : nettoyer (data quality) ──
  128: {
    lessons: ['data-cleaning-quality.md', 'pandas-data-wrangling.md'],
    theory: `**Modèle mental : nettoyer des données, ce n'est PAS « les rendre jolies », c'est prendre des DÉCISIONS JUSTIFIÉES sur les manquants, les doublons et les formats — et chaque décision (supprimer, imputer, corriger) a un COÛT et introduit un BIAIS qu'il faut assumer et DOCUMENTER.** Il n'existe pas de nettoyage « neutre » : remplacer un manquant par la moyenne modifie la distribution, supprimer les lignes incomplètes peut éliminer une sous-population, corriger un format peut masquer un problème de collecte. Le nettoyage professionnel ne cache pas ces choix, il les EXPLICITE dans un rapport avant/après.\n\nLes trois familles de problèmes et leurs arbitrages :\n1. **Les valeurs manquantes (NaN)** : trois options, chacune avec son biais.\n   - SUPPRIMER les lignes (\`dropna\`) : simple, mais perd de l'information et peut biaiser si les manquants ne sont pas aléatoires.\n   - IMPUTER (\`fillna\` avec moyenne/médiane/valeur) : garde les lignes, mais invente des données et réduit la variance.\n   - LAISSER et gérer en aval : parfois le manquant est une information (« non renseigné »).\n   Le choix DÉPEND du contexte et se justifie — il n'y a pas de règle universelle.\n2. **Les doublons** : \`df.duplicated()\` puis \`drop_duplicates()\` — mais il faut d'abord définir ce qu'est un doublon (lignes strictement identiques ? même clé métier ?). Supprimer sans définir la clé peut effacer des données légitimes.\n3. **Les formats et types** : convertir (\`pd.to_datetime\`, \`.astype(int)\`), normaliser le texte (\`.str.lower().str.strip()\` pour fusionner « Livre »/« livre »), corriger les valeurs impossibles (un prix négatif). Chaque correction doit être vérifiée.\n\nLe livrable qui prouve un nettoyage sérieux : un RAPPORT AVANT/APRÈS chiffré — combien de manquants avant/après, combien de doublons retirés, quelles conversions, avec la JUSTIFICATION de chaque décision. Sans ce rapport, personne (toi compris) ne peut savoir ce que le nettoyage a fait aux données, ni si l'analyse en aval est fiable. C'est la reproductibilité (mois précédents) appliquée aux données.\n\nLa règle du jour : **chaque décision de nettoyage est un arbitrage justifié et documenté (avant/après), pas un geste automatique ; on assume le biais qu'on introduit plutôt que de le cacher**. Un nettoyage sans rapport est un nettoyage suspect.`,
    guided: `**Énoncé** : nettoyer un CSV volontairement sale (manquants, doublons, formats) avec un rapport avant/après justifié.\n\n**Exemple simple** : la décision d'imputation, JUSTIFIÉE — \`df[\"prix\"].fillna(df[\"prix\"].median())\`. On choisit la MÉDIANE (pas la moyenne) parce qu'elle résiste aux valeurs aberrantes ; et on ÉCRIT pourquoi. La justification fait partie du nettoyage.\n\n**Raisonnement pas-à-pas** : mesurer l'état initial (le « avant ») → traiter chaque problème par une décision justifiée → mesurer l'état final (le « après ») → rédiger le rapport.\n\n**Implémentation** :\n\`\`\`python\nimport pandas as pd\ndf = pd.read_csv(\"ventes_sales.csv\")\navant = {\"lignes\": len(df), \"manquants_prix\": df[\"prix\"].isna().sum(),\n         \"doublons\": df.duplicated().sum()}\n\n# 1. Formats/types AVANT le reste (pour que les comparaisons marchent)\ndf[\"date\"] = pd.to_datetime(df[\"date\"], errors=\"coerce\")   # texte → datetime\ndf[\"categorie\"] = df[\"categorie\"].str.lower().str.strip()   # fusionne 'Livre'/'livre'\n\n# 2. Valeurs impossibles : décision = retirer (un prix négatif est une erreur de saisie)\ndf = df[df[\"prix\"] >= 0]\n\n# 3. Manquants : décision = imputer par la MÉDIANE (résiste aux aberrants) — justifié\ndf[\"prix\"] = df[\"prix\"].fillna(df[\"prix\"].median())\n\n# 4. Doublons : décision = même (date, produit, client) = doublon → garder le premier\ndf = df.drop_duplicates(subset=[\"date\", \"produit\", \"client\"], keep=\"first\")\n\napres = {\"lignes\": len(df), \"manquants_prix\": df[\"prix\"].isna().sum(), \"doublons\": 0}\n# RAPPORT : avant→après + JUSTIFICATION de chaque décision (le vrai livrable)\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi imputer par la médiane plutôt que la moyenne ici ? (b) Un nettoyage est-il « neutre » ? (c) Pourquoi un rapport avant/après ?\n→ (a) la médiane résiste aux valeurs aberrantes qui tireraient la moyenne ; (b) non — chaque décision (supprimer/imputer/corriger) introduit un biais qu'il faut assumer ; (c) sans lui, personne ne sait ce que le nettoyage a fait aux données ni si l'analyse en aval est fiable.\n\n**Variante** : pour les manquants, produis DEUX versions (dropna vs fillna médiane) et compare une statistique clé (la moyenne du prix) — tu VOIS l'impact de ta décision sur les données, ce qui rend le biais concret plutôt qu'abstrait.`,
    caseStudy: `Le nettoyage de données est là où la plupart du temps d'un data analyst se passe, et où les erreurs les plus coûteuses se produisent : un manquant imputé sans réfléchir qui fausse une moyenne, des doublons supprimés sans définir la clé qui effacent des ventes légitimes, une normalisation de casse oubliée qui compte « Livre » et « livre » comme deux catégories. En entreprise, une décision fondée sur des données mal nettoyées peut coûter cher, et l'absence de rapport rend l'erreur indétectable. Les équipes data sérieuses exigent un rapport de qualité (avant/après, décisions justifiées) précisément parce que le nettoyage n'est jamais neutre. Savoir arbitrer ET documenter chaque décision est le cœur du métier de data analyst — et un rapport de nettoyage clair est un livrable de portfolio convaincant.`,
    interview: `**« Comment nettoies-tu un dataset avec des valeurs manquantes ? »** → D'abord je comprends POURQUOI elles manquent et si elles sont aléatoires, car ça conditionne le choix. Trois options, chacune avec son biais : SUPPRIMER les lignes (\`dropna\`, simple mais perd de l'info et peut biaiser si les manquants ne sont pas aléatoires), IMPUTER (\`fillna\` médiane/moyenne, garde les lignes mais invente des données et réduit la variance — je préfère souvent la médiane qui résiste aux aberrants), ou LAISSER si le manquant est lui-même une information. Il n'y a pas de règle universelle : je choisis selon le contexte et je JUSTIFIE. Surtout, je produis un rapport AVANT/APRÈS chiffré, parce qu'un nettoyage n'est jamais neutre — chaque décision introduit un biais que j'assume plutôt que de le cacher. Sans rapport, personne ne peut juger si l'analyse en aval est fiable.`,
    future: `- **Bankable** : savoir arbitrer ET documenter chaque décision de nettoyage (rapport avant/après justifié) est le cœur du métier de data analyst — un livrable de portfolio convaincant.\n- Ce rapport de qualité est exactement le livrable attendu au jour 143 (transform de DataPulse) et dans tout pipeline data.\n- Le principe « chaque décision introduit un biais qu'on assume et documente » est fondamental pour la fiabilité des analyses et, plus tard, la qualité des données d'entraînement ML.`,
    solution: {
      logic: "Solution simple : dropna, drop_duplicates, quelques conversions. Solution améliorée : mesurer l'état AVANT, traiter les formats/types d'abord (dates, casse) puis les valeurs impossibles, choisir pour les manquants une stratégie JUSTIFIÉE selon le contexte (médiane si aberrants), définir la CLÉ d'un doublon avant de dédoublonner, mesurer l'état APRÈS, et produire un rapport avant/après avec la justification de chaque décision. La preuve : chaque choix est chiffré et argumenté, le biais introduit est assumé.",
      pitfalls: ["Imputer/supprimer sans justifier : chaque décision introduit un biais — l'assumer et le documenter, pas le cacher.", "Dédoublonner sans définir la clé métier : on efface des données légitimes.", "Oublier de normaliser la casse/les espaces : 'Livre' et 'livre' comptés comme deux catégories distinctes.", "Ne pas produire de rapport avant/après : le nettoyage devient une boîte noire, l'analyse en aval invérifiable."],
      checks: ["L'état avant (manquants, doublons, types) est mesuré.", "Chaque décision (supprimer/imputer/corriger) est justifiée selon le contexte.", "La clé d'un doublon est définie avant dédoublonnage.", "Les formats et la casse sont normalisés.", "Un rapport avant/après chiffré documente toutes les décisions."],
      oral: "Martèle que « le nettoyage n'est jamais neutre » : chaque décision (supprimer/imputer/corriger) a un biais. Déroule les trois options pour les manquants avec leurs coûts, la nécessité de définir la clé d'un doublon, et surtout le rapport avant/après. Comparer deux stratégies (dropna vs médiane) sur une statistique clé prouve que tu comprends l'impact réel de tes choix.",
    },
  },

  // ── pandas : filtrer, trier, sélectionner ──
  129: {
    lessons: ['pandas-data-wrangling.md'],
    theory: `**Modèle mental : sélectionner dans un DataFrame se fait par MASQUE BOOLÉEN (\`df[df.age > 30]\`), pas par boucle — un masque est une \`Series\` de True/False alignée sur l'index, et pandas garde les lignes où c'est True. Et il faut distinguer nettement \`.loc\` (accès par ÉTIQUETTE) de \`.iloc\` (accès par POSITION), jamais confondre les deux.** C'est le cœur de la manipulation pandas et l'équivalent vectorisé du \`WHERE\`/\`ORDER BY\`/\`SELECT\` de SQL (que tu connais depuis le mois 1) — sauf qu'ici tout se compose en Python.\n\nLes trois opérations et leurs idiomes :\n1. **Filtrer (le \`WHERE\`)** : \`df[df[\"prix\"] > 100]\` garde les lignes où la condition est vraie. Pour combiner : \`df[(df.prix > 100) & (df.dispo)]\` — les parenthèses sont OBLIGATOIRES et on utilise \`&\`/\`|\`/\`~\` (pas \`and\`/\`or\`/\`not\`, qui ne fonctionnent pas sur des Series). C'est la composition de prédicats, vectorisée.\n2. **Sélectionner (le \`SELECT\`)** : \`df[[\"titre\", \"prix\"]]\` choisit des colonnes ; \`df.loc[masque, [\"titre\", \"prix\"]]\` combine filtre de lignes ET choix de colonnes. \`.loc\` accède par étiquette (nom de colonne, valeur d'index) ; \`.iloc\` par position entière (\`df.iloc[0:5, 0:2]\`). Les confondre est une source de bugs.\n3. **Trier (le \`ORDER BY\`)** : \`df.sort_values(\"prix\", ascending=False)\`, ou sur plusieurs colonnes \`sort_values([\"categorie\", \"prix\"])\`.\n\nLe piège majeur — le \`SettingWithCopyWarning\` : \`df[df.prix > 100][\"remise\"] = 0.1\` (indexation chaînée) peut modifier une COPIE temporaire, pas le DataFrame original — la modification est perdue silencieusement. La règle : pour MODIFIER, on utilise \`.loc\` en une seule fois : \`df.loc[df.prix > 100, \"remise\"] = 0.1\`. C'est l'erreur pandas la plus fréquente.\n\nLa règle du jour : **filtre par masque booléen (avec \`&\`/\`|\` et parenthèses), sélectionne colonnes et lignes avec \`.loc\`/\`.iloc\` sans les confondre, trie avec \`sort_values\`, et modifie toujours via \`.loc\` en une fois pour éviter le piège de la copie**. Ces opérations composées remplacent toute boucle de filtrage — c'est le SQL du mois 1, exprimé en pandas vectorisé.`,
    guided: `**Énoncé** : reproduire 5 requêtes du jour 11 (SQL) en pandas — filtres, tri, sélection — et comparer avec JS.\n\n**Exemple simple** : le filtre à condition multiple, avec le piège des parenthèses — \`df[(df.prix > 100) & (df.dispo)]\`. Sans les parenthèses, Python se trompe de priorité et lève une erreur. Avec \`&\` (pas \`and\`), l'opération est vectorisée. Retiens ces deux détails : ils causent 80% des erreurs de filtrage.\n\n**Raisonnement pas-à-pas** : traduire chaque \`WHERE/ORDER BY/SELECT\` SQL en son idiome pandas → composer les conditions avec \`&\`/\`|\` et parenthèses → modifier via \`.loc\` en une fois.\n\n**Implémentation** :\n\`\`\`python\nimport pandas as pd\ndf = pd.read_csv(\"produits.csv\")\n\n# SQL: SELECT titre, prix FROM produits WHERE prix > 100 AND dispo ORDER BY prix DESC\nr = (df[(df[\"prix\"] > 100) & (df[\"dispo\"])]          # WHERE (parenthèses + &)\n       .sort_values(\"prix\", ascending=False)          # ORDER BY\n       [[\"titre\", \"prix\"]])                            # SELECT colonnes\n\n# Sélection par position vs étiquette (à ne pas confondre)\ndf.iloc[0:5]                # 5 PREMIÈRES lignes (position)\ndf.loc[df[\"prix\"] > 100]    # lignes par CONDITION (étiquette/masque)\n\n# MODIFIER : toujours .loc en une fois (évite SettingWithCopyWarning)\ndf.loc[df[\"prix\"] > 100, \"segment\"] = \"premium\"        # ✅\n# df[df[\"prix\"] > 100][\"segment\"] = \"premium\"          # ❌ modifie une copie, perdu\n\`\`\`\nComparaison : en JS, \`produits.filter(p => p.prix > 100 && p.dispo).sort(...).map(p => ...)\` — même logique, mais pandas est vectorisé (colonne entière) et gère des millions de lignes efficacement.\n\n**Mini-quiz** : (a) Pourquoi \`&\` et des parenthèses, pas \`and\` ? (b) Différence entre \`.loc\` et \`.iloc\` ? (c) Pourquoi \`df[cond][\"col\"] = x\` est-il dangereux ?\n→ (a) \`and\` ne fonctionne pas sur des Series (vectorisation), et sans parenthèses la priorité des opérateurs casse la condition ; (b) \`.loc\` accède par étiquette/masque, \`.iloc\` par position entière ; (c) indexation chaînée : modifie possiblement une copie temporaire, la modification est perdue silencieusement — utiliser \`.loc\` en une fois.\n\n**Variante** : chronomètre un filtre pandas vectorisé vs une boucle Python \`for\` équivalente sur le même DataFrame — l'écart de vitesse (souvent 10-100×) rend concret pourquoi on pense « colonnes », pas « boucles ».`,
    caseStudy: `Le filtrage, le tri et la sélection sont les opérations les plus fréquentes du travail data quotidien — extraire un sous-ensemble, classer, projeter des colonnes. Deux pièges y coûtent un temps considérable en pratique : l'usage de \`and\`/\`or\` au lieu de \`&\`/\`|\` (erreur immédiate mais déroutante pour un débutant), et surtout le \`SettingWithCopyWarning\` — une modification faite sur une copie chaînée qui semble marcher mais est perdue, produisant des résultats faux SANS erreur. Les data analysts qui maîtrisent \`.loc\`/\`.iloc\` et le masque booléen écrivent du code correct et rapide ; ceux qui bricolent des boucles ou ignorent l'avertissement produisent du code lent et parfois faux. Cette maîtrise est un attendu de base de tout poste data et se teste directement en entretien technique.`,
    interview: `**« Comment filtres-tu et modifies-tu un DataFrame, et quel est le piège du \`SettingWithCopyWarning\` ? »** → Je filtre par MASQUE BOOLÉEN : \`df[df.prix > 100]\` garde les lignes où la condition est vraie. Pour combiner des conditions, j'utilise \`&\`/\`|\`/\`~\` avec des parenthèses obligatoires (pas \`and\`/\`or\`, qui ne marchent pas sur des Series). Je sélectionne colonnes et lignes avec \`.loc\` (par étiquette/masque) ou \`.iloc\` (par position), sans les confondre. Le piège du \`SettingWithCopyWarning\` : une indexation chaînée comme \`df[cond][\"col\"] = x\` peut modifier une COPIE temporaire, donc la modification est perdue silencieusement et l'analyse devient fausse sans planter. La règle : pour modifier, toujours \`.loc\` en une seule opération — \`df.loc[cond, \"col\"] = x\`. Tout ça est vectorisé, donc rapide, contrairement à une boucle.`,
    future: `- **Bankable** : maîtriser masque booléen, \`.loc\`/\`.iloc\` et le piège de la copie chaînée est un attendu de base de tout poste data — testé en entretien technique.\n- Ces opérations sont le quotidien de toute analyse et du projet DataPulse (filtres, sélections, jours 141-146).\n- Le passage « SQL → pandas → (plus tard) NumPy » ancre le raisonnement vectorisé qui sert au feature engineering et au ML.`,
    solution: {
      logic: "Solution simple : filtrer avec un masque et trier. Solution améliorée : composer les conditions avec \`&\`/\`|\`/\`~\` et parenthèses (jamais \`and\`/\`or\`), distinguer \`.loc\` (étiquette/masque) de \`.iloc\` (position), enchaîner filtre → tri → sélection de colonnes lisiblement, et surtout MODIFIER via \`.loc\` en une seule opération pour éviter le \`SettingWithCopyWarning\`. La preuve : le code reproduit fidèlement les requêtes SQL, est vectorisé (rapide), et ne modifie jamais une copie par erreur.",
      pitfalls: ["\`and\`/\`or\` au lieu de \`&\`/\`|\` sur des Series : erreur de vectorisation ; oublier les parenthèses casse la priorité.", "Confondre \`.loc\` (étiquette) et \`.iloc\` (position) : sélection erronée silencieuse.", "Indexation chaînée pour modifier (\`df[cond][\"col\"] = x\`) : modifie une copie, changement perdu — utiliser \`.loc\` en une fois.", "Boucler ligne par ligne au lieu de filtrer par masque : lent (10-100×) et non idiomatique."],
      checks: ["Les filtres utilisent des masques booléens avec \`&\`/\`|\` et parenthèses.", "\`.loc\` et \`.iloc\` sont utilisés à bon escient (étiquette vs position).", "Les modifications passent par \`.loc\` en une seule opération.", "Le tri utilise \`sort_values\`.", "Le code est vectorisé (aucune boucle de filtrage)."],
      oral: "Explique le masque booléen (Series de True/False alignée), les opérateurs \`&\`/\`|\` avec parenthèses, et la distinction \`.loc\`/\`.iloc\`. Insiste sur le \`SettingWithCopyWarning\` : « modifier une copie chaînée donne des résultats faux sans planter — toujours \`.loc\` en une fois ». Comparer à \`filter().sort().map()\` en JS montre que tu vois la continuité, et l'écart de vitesse justifie la vectorisation.",
    },
  },

  // ── pandas : grouper et agréger ──
  130: {
    lessons: ['pandas-data-wrangling.md'],
    theory: `**Modèle mental : \`groupby\` applique le schéma SPLIT-APPLY-COMBINE — DÉCOUPER les données en groupes selon une clé, APPLIQUER une agrégation à chaque groupe, RECOMBINER les résultats en un tableau. C'est le \`GROUP BY\` de SQL, vectorisé ; il faut penser en GROUPES, pas en boucles imbriquées.** Le réflexe non idiomatique serait de boucler sur les catégories, puis sur les lignes de chaque catégorie, en accumulant des totaux — lent et verbeux. \`groupby\` exprime la même intention en une ligne déclarative et rapide.\n\nLe déroulé split-apply-combine :\n1. **Split** : \`df.groupby(\"categorie\")\` partitionne les lignes par valeur de la clé (une partition par catégorie). Rien n'est encore calculé — c'est un objet groupé « en attente ».\n2. **Apply** : \`.agg(...)\` (ou \`.sum()\`, \`.mean()\`, \`.count()\`) applique la fonction d'agrégation à chaque groupe. \`df.groupby(\"categorie\")[\"ca\"].sum()\` donne le CA par catégorie.\n3. **Combine** : pandas recombine automatiquement en une \`Series\`/\`DataFrame\` indexé par la clé de groupe.\n\nLes formes utiles :\n- Agrégations MULTIPLES en une passe : \`.agg({\"ca\": \"sum\", \"quantite\": \"mean\", \"client\": \"nunique\"})\` — plusieurs statistiques par groupe d'un coup.\n- Grouper par PLUSIEURS clés : \`df.groupby([\"region\", \"categorie\"])[\"ca\"].sum()\` — le croisement.\n- \`.reset_index()\` pour rettransformer le résultat groupé en DataFrame plat (souvent nécessaire pour la suite).\n\nQuand groupby vs autre chose : \`groupby\` pour RÉSUMER par catégorie (une ligne par groupe) ; un \`pivot_table\` pour croiser deux dimensions en tableau ; une simple agrégation globale (\`df[\"ca\"].sum()\`) quand il n'y a pas de groupe. Choisir la bonne forme évite des contorsions.\n\nLa règle du jour : **pense split-apply-combine — groupe par la clé, agrège (une ou plusieurs statistiques), recombine ; jamais de boucle imbriquée pour ce que \`groupby\` fait en une ligne vectorisée**. C'est le même GROUP BY qu'en SQL et une agrégation JS avec \`reduce\`, mais exprimé de façon déclarative et performante.`,
    guided: `**Énoncé** : reproduire les agrégations du jour 24 (SQL) en pandas et comparer les 3 syntaxes JS/SQL/pandas.\n\n**Exemple simple** : le split-apply-combine minimal — \`df.groupby(\"categorie\")[\"ca\"].sum()\`. Découpe par catégorie, somme le CA de chacune, recombine en une Series indexée par catégorie. Toute agrégation par groupe est une variation de cette ligne.\n\n**Raisonnement pas-à-pas** : identifier la clé de groupe → choisir l'agrégation (une ou plusieurs) → recombiner → aplatir avec reset_index si besoin.\n\n**Implémentation** :\n\`\`\`python\nimport pandas as pd\ndf = pd.read_csv(\"ventes.csv\")\n\n# CA et panier moyen par catégorie, + nombre de clients uniques — en UNE passe\nresume = (df.groupby(\"categorie\")\n            .agg(ca=(\"montant\", \"sum\"),\n                 panier_moyen=(\"montant\", \"mean\"),\n                 clients=(\"client\", \"nunique\"))\n            .reset_index()                     # résultat plat\n            .sort_values(\"ca\", ascending=False))\n\n# Croisement à deux clés : CA par région ET catégorie\nca_croise = df.groupby([\"region\", \"categorie\"])[\"montant\"].sum()\n\`\`\`\nComparaison des 3 syntaxes (même intention : CA par catégorie) :\n\`\`\`\nSQL    : SELECT categorie, SUM(montant) FROM ventes GROUP BY categorie;\npandas : df.groupby(\"categorie\")[\"montant\"].sum()\nJS     : Object.entries(ventes.reduce((acc,v)=>{acc[v.cat]=(acc[v.cat]||0)+v.montant;return acc},{}))\n\`\`\`\n→ SQL et pandas sont DÉCLARATIFS (on dit QUOI) ; le JS avec reduce est plus impératif (on dit COMMENT accumuler).\n\n**Mini-quiz** : (a) Quelles sont les 3 étapes de \`groupby\` ? (b) Comment faire plusieurs agrégations en une passe ? (c) Pourquoi \`reset_index()\` après un groupby ?\n→ (a) split (découper par clé), apply (agréger chaque groupe), combine (recombiner) ; (b) \`.agg({...})\` avec plusieurs colonnes/fonctions ; (c) pour retransformer le résultat indexé par la clé de groupe en DataFrame plat, plus pratique pour la suite.\n\n**Variante** : ajoute une agrégation personnalisée avec \`.agg(lambda x: x.max() - x.min())\` (l'étendue par groupe) — tu vois que « apply » accepte n'importe quelle fonction, pas seulement sum/mean.`,
    caseStudy: `L'agrégation par groupe est l'opération analytique la plus courante en entreprise : CA par région, panier moyen par segment, taux de conversion par canal — toute question business « par X » est un groupby. Les analysts qui pensent split-apply-combine produisent ces résumés en une ligne lisible et rapide ; ceux qui bouclent à la main écrivent du code long, lent et sujet aux erreurs d'accumulation. La capacité à traduire une question métier (« quel est le CA moyen par catégorie et par région ? ») en un groupby correct, avec les bonnes agrégations, est exactement ce que teste un entretien data — et la comparaison SQL/pandas montre que c'est le même raisonnement d'agrégation décliné selon l'outil.`,
    interview: `**« Explique \`groupby\` en pandas et le schéma split-apply-combine. »** → \`groupby\` résume des données par catégorie selon le schéma SPLIT-APPLY-COMBINE : SPLIT découpe les lignes en groupes selon une clé (\`df.groupby(\"categorie\")\`), APPLY applique une agrégation à chaque groupe (\`.sum()\`, \`.mean()\`, ou \`.agg({...})\` pour plusieurs statistiques en une passe), COMBINE recombine en un tableau indexé par la clé. Je peux grouper par plusieurs clés pour croiser des dimensions, et \`reset_index()\` réaplatit le résultat. C'est le \`GROUP BY\` de SQL, vectorisé : je pense en GROUPES, jamais en boucles imbriquées — pandas fait en une ligne rapide ce qu'une double boucle ferait lentement. Toute question métier « par X » se traduit en un groupby sur X.`,
    future: `- **Bankable** : traduire une question métier « par X » en un groupby correct est exactement ce que teste un entretien data — une compétence analytique quotidienne.\n- Les agrégations alimentent le dashboard de DataPulse (répondre aux 3 questions, jour 145) et toute analyse exploratoire.\n- Le schéma split-apply-combine est universel (SQL, pandas, Spark) et le raisonnement par groupe prépare le feature engineering agrégé pour le ML.`,
    solution: {
      logic: "Solution simple : \`df.groupby(cle)[col].sum()\`. Solution améliorée : penser split-apply-combine, faire plusieurs agrégations en une passe avec \`.agg({...})\` (somme, moyenne, nunique), grouper par plusieurs clés pour croiser des dimensions, réaplatir avec \`reset_index()\`, et savoir choisir groupby vs pivot_table vs agrégation globale. La preuve : les agrégations reproduisent le GROUP BY SQL en une ligne vectorisée, et le tableau comparatif montre le même raisonnement en JS/SQL/pandas.",
      pitfalls: ["Boucler sur les catégories puis les lignes au lieu de groupby : long, lent, sujet aux erreurs d'accumulation.", "Faire plusieurs passes pour plusieurs statistiques au lieu d'un seul \`.agg({...})\`.", "Oublier \`reset_index()\` et se retrouver bloqué avec un index de groupe pour la suite.", "Confondre groupby (résumé par groupe) et pivot_table (croisement en tableau) : contorsions inutiles."],
      checks: ["L'agrégation utilise groupby (split-apply-combine), pas de boucle.", "Les agrégations multiples sont faites en une passe avec \`.agg\`.", "Le groupement par plusieurs clés croise correctement les dimensions.", "\`reset_index()\` aplatit le résultat quand nécessaire.", "Le tableau comparatif JS/SQL/pandas est correct et éclairant."],
      oral: "Déroule split-apply-combine en nommant chaque étape, et donne la forme multi-agrégation (\`.agg({...})\`). Relie explicitement au \`GROUP BY\` SQL et au \`reduce\` JS pour montrer que c'est le même raisonnement d'agrégation. « Toute question métier par X est un groupby sur X » est la formule qui prouve que tu relies l'outil au besoin analytique.",
    },
  },

  // ── pandas : joindre (merge) ──
  131: {
    lessons: ['pandas-data-wrangling.md', 'database-modeling.md'],
    theory: `**Modèle mental : un \`merge\` JOINT deux tables sur une CLÉ commune, et le TYPE de jointure (inner/left/right/outer) décide QUOI FAIRE des lignes sans correspondance — le vrai risque n'est pas la syntaxe mais les lignes qu'on PERD ou qu'on DUPLIQUE silencieusement.** Joindre deux sources (des ventes + un référentiel produits, des utilisateurs + leurs commandes) est une opération de tous les jours. La difficulté n'est pas d'écrire \`pd.merge\` : c'est de comprendre ce qu'il advient des lignes qui n'ont PAS de correspondance, et de VÉRIFIER que le résultat a le nombre de lignes attendu.\n\nLes types de jointure — la décision centrale :\n1. **\`inner\` (défaut)** : ne garde que les lignes présentes des DEUX côtés. Les lignes sans correspondance sont SILENCIEUSEMENT perdues — un piège si tu ne l'attends pas (des ventes dont le produit n'est pas dans le référentiel disparaissent).\n2. **\`left\`** : garde TOUTES les lignes de gauche, complète avec la droite quand possible (NaN sinon). C'est le choix quand la table de gauche est la référence à préserver (toutes les ventes, enrichies du produit si connu).\n3. **\`outer\`** : garde toutes les lignes des deux côtés. Utile pour un audit de correspondance (qui n'a pas de match ?).\n4. **\`right\`** : symétrique de left, rarement nécessaire (on inverse les tables).\n\nLes deux pièges qui coûtent cher :\n- **La perte silencieuse** : un \`inner\` qui élimine des lignes attendues. TOUJOURS comparer \`len\` avant/après et se demander « ai-je perdu des lignes, et est-ce normal ? ».\n- **La duplication (one-to-many mal maîtrisé)** : si la clé n'est pas unique du côté droit, chaque ligne de gauche est DUPLIQUÉE pour chaque correspondance — le nombre de lignes EXPLOSE. Le paramètre \`validate=\"many_to_one\"\` fait ÉCHOUER le merge si la relation n'est pas celle attendue : un garde-fou qui attrape ce bug tôt.\n\nLa clé doit aussi être PROPRE : mêmes types des deux côtés (un id en \`int\` d'un côté, en \`str\` de l'autre ne matchera jamais), casse et espaces normalisés (jour 128). Une clé sale = zéro correspondance sans erreur.\n\nLa règle du jour : **choisis le type de jointure selon ce que tu veux faire des non-correspondances, VÉRIFIE le nombre de lignes avant/après, et protège la cardinalité avec \`validate\` ; une clé doit être propre et de même type des deux côtés**. Le merge est facile à écrire et facile à se tromper silencieusement — la vérification EST le travail.`,
    guided: `**Énoncé** : merger 2+ sources, gérer les clés manquantes, et VALIDER le résultat.\n\n**Exemple simple** : le garde-fou de cardinalité — \`pd.merge(ventes, produits, on=\"produit_id\", validate=\"many_to_one\")\`. Il fait échouer le merge si un \`produit_id\` apparaît plusieurs fois côté produits (ce qui dupliquerait les ventes). Un merge sans \`validate\` est un merge à surveiller.\n\n**Raisonnement pas-à-pas** : vérifier les clés (types, propreté) → choisir le type de jointure selon les non-correspondances voulues → merger avec \`validate\` → COMPARER les tailles avant/après.\n\n**Implémentation** :\n\`\`\`python\nimport pandas as pd\nventes = pd.read_csv(\"ventes.csv\")\nproduits = pd.read_csv(\"produits.csv\")\n\n# 1. Clés propres et de MÊME type des deux côtés (sinon zéro match silencieux)\nventes[\"produit_id\"] = ventes[\"produit_id\"].astype(str).str.strip()\nproduits[\"produit_id\"] = produits[\"produit_id\"].astype(str).str.strip()\n\n# 2. left : garder TOUTES les ventes, enrichir du produit si connu\nenrichi = pd.merge(ventes, produits, on=\"produit_id\", how=\"left\",\n                   validate=\"many_to_one\")   # garde-fou : 1 produit par vente max\n\n# 3. VALIDER : ai-je perdu ou dupliqué des lignes ?\nassert len(enrichi) == len(ventes), \"le merge a changé le nombre de ventes !\"\nsans_produit = enrichi[\"nom_produit\"].isna().sum()   # ventes sans produit référencé\nprint(f\"{sans_produit} ventes sans produit correspondant (à investiguer)\")\n\`\`\`\n\n**Mini-quiz** : (a) Que deviennent les lignes sans correspondance avec un \`inner\` ? (b) Comment un merge peut-il faire EXPLOSER le nombre de lignes ? (c) À quoi sert \`validate\` ?\n→ (a) elles sont silencieusement supprimées — piège si non attendu ; (b) si la clé n'est pas unique côté droit, chaque ligne de gauche est dupliquée par correspondance (one-to-many) ; (c) faire échouer le merge si la cardinalité réelle n'est pas celle attendue (garde-fou contre la duplication).\n\n**Variante** : fais volontairement un merge sur une clé de types différents (int vs str) et observe que le résultat est vide malgré des valeurs identiques à l'œil — la preuve qu'une clé doit être de même type des deux côtés.`,
    caseStudy: `Joindre des sources est au cœur de tout travail data (enrichir des transactions avec un référentiel, croiser des utilisateurs et leur activité), et les bugs de merge sont parmi les plus insidieux car ILS NE PLANTENT PAS : un \`inner\` qui perd 15% des lignes attendues, ou une clé dupliquée qui triple le chiffre d'affaires par duplication. Une analyse ou un rapport bâti sur un merge mal maîtrisé donne des chiffres FAUX qui paraissent plausibles. Les data engineers expérimentés vérifient systématiquement la cardinalité (\`validate\`) et le nombre de lignes avant/après, précisément parce que l'erreur est silencieuse. Cette rigueur — choisir le bon type de jointure et VALIDER le résultat — est un marqueur de sérieux data directement évalué, et centrale pour le projet DataPulse qui joindra des sources.`,
    interview: `**« Quels sont les pièges d'un merge en pandas, et comment les évites-tu ? »** → Deux pièges, tous deux SILENCIEUX. D'abord la perte de lignes : un \`inner\` (le défaut) ne garde que ce qui matche des deux côtés, donc élimine sans avertir les lignes sans correspondance — je choisis \`left\` quand je veux préserver la table de référence. Ensuite la DUPLICATION : si la clé n'est pas unique côté droit, chaque ligne de gauche est dupliquée par correspondance (one-to-many), et le nombre de lignes explose — un CA qui triple silencieusement. Je m'en protège avec \`validate=\"many_to_one\"\` qui fait échouer le merge si la cardinalité n'est pas celle attendue, et je COMPARE toujours \`len\` avant/après. Enfin, je m'assure que la clé est propre et de même type des deux côtés, sinon zéro correspondance sans erreur. La vérification EST le travail.`,
    future: `- **Bankable** : choisir le bon type de jointure et VALIDER la cardinalité (lignes perdues/dupliquées) est un marqueur de sérieux data — les bugs de merge sont silencieux et coûteux.\n- Le merge joint les sources de DataPulse (jours 141-144) et de toute analyse croisant plusieurs tables.\n- Le raisonnement sur les jointures et la cardinalité est identique en SQL (JOIN) et en pandas — une compétence transférable à tout travail sur données relationnelles.`,
    solution: {
      logic: "Solution simple : \`pd.merge(a, b, on=cle)\`. Solution améliorée : nettoyer et aligner les types de la clé des deux côtés, choisir le type de jointure selon le sort voulu des non-correspondances (left pour préserver la référence, inner/outer selon le besoin), protéger la cardinalité avec \`validate\`, et VALIDER le résultat en comparant le nombre de lignes avant/après et en comptant les non-correspondances. La preuve : le merge ne perd ni ne duplique de lignes par surprise, et les cas sans correspondance sont quantifiés.",
      pitfalls: ["\`inner\` par défaut qui perd silencieusement les lignes sans correspondance : choisir \`left\`/\`outer\` selon le besoin.", "Clé non unique côté droit sans \`validate\` : duplication one-to-many, nombre de lignes qui explose.", "Clé de types différents (int vs str) : zéro correspondance malgré des valeurs identiques à l'œil.", "Ne pas comparer \`len\` avant/après : les pertes et duplications passent inaperçues."],
      checks: ["La clé est propre et de même type des deux côtés.", "Le type de jointure est choisi selon le sort voulu des non-correspondances.", "\`validate\` protège la cardinalité attendue.", "Le nombre de lignes avant/après est comparé et cohérent.", "Les lignes sans correspondance sont quantifiées et investiguées."],
      oral: "Insiste sur le caractère SILENCIEUX des deux pièges : perte (inner) et duplication (one-to-many). Explique le choix left/inner/outer selon les non-correspondances, le garde-fou \`validate\`, et la vérification systématique du nombre de lignes. « La vérification est le travail » résume pourquoi un merge facile à écrire est facile à rater — et prouve ta rigueur data.",
    },
  },

  // ── Data quality en fonctions réutilisables ──
  132: {
    lessons: ['data-cleaning-quality.md', 'clean-code.md'],
    theory: `**Modèle mental : sortir du notebook, c'est transformer une analyse JETABLE en un PIPELINE de fonctions PURES et testables (load → validate → clean → report) — un notebook prouve une idée une fois, une fonction pure la rend RÉUTILISABLE, TESTABLE et REJOUABLE.** Un notebook est parfait pour EXPLORER (jours 127-131) : on essaie, on voit, on ajuste. Mais un nettoyage qui doit tourner chaque semaine sur de nouvelles données ne peut pas rester une suite de cellules exécutées dans le désordre, dépendant d'un état caché. On le PROMEUT en code : des fonctions à responsabilité unique (jour 121), pures (mêmes entrées → mêmes sorties, sans effet de bord caché), et testées (jour 124).\n\nPourquoi une fonction pure change tout :\n1. **Réutilisable** : \`nettoyer(df)\` s'applique à n'importe quel DataFrame de même forme, pas seulement à celui chargé dans la cellule 3.\n2. **Testable** : une fonction pure se teste avec pytest (jour 124) — on lui donne un petit DataFrame sale connu et on vérifie la sortie. Un notebook ne se teste pas.\n3. **Rejouable et déterministe** : relancer le pipeline sur les mêmes données donne le même résultat, sans dépendre de l'ordre d'exécution des cellules ni d'un état résiduel.\n4. **Composable** : \`report(clean(validate(load(chemin))))\` — chaque étape isolée, on peut en remplacer une sans casser les autres.\n\nLes étapes canoniques d'un pipeline de qualité :\n- **load** : lire la source (et rien d'autre).\n- **validate** : vérifier les invariants attendus (colonnes présentes, types, plages) et ÉCHOUER TÔT (jour 122) si les données ne sont pas celles attendues — un pipeline qui traite des données invalides produit des résultats invalides.\n- **clean** : appliquer les décisions de nettoyage (jour 128), chacune justifiée.\n- **report** : produire le rapport avant/après (jour 128) et les métriques de qualité.\n\nLe piège à éviter : une fonction \`clean\` qui MUTE son entrée ou lit un état global. Une fonction pure prend un DataFrame et en RENVOIE un nouveau (\`df.copy()\` au besoin), sans surprise pour l'appelant.\n\nLa règle du jour : **promeus ton nettoyage exploratoire en fonctions pures à responsabilité unique (load/validate/clean/report), testées et composables ; le notebook explore, le module PRODUIT**. C'est la transition de « j'ai fait marcher une analyse » à « j'ai un outil de qualité réutilisable » — exactement ce qui sépare un débutant d'un data engineer.`,
    guided: `**Énoncé** : transformer ton nettoyage (jour 128) en fonctions pures Python testables (load/validate/clean/report).\n\n**Exemple simple** : la signature d'une fonction pure de nettoyage — \`def clean(df: pd.DataFrame) -> pd.DataFrame:\`. Elle prend un DataFrame, en renvoie un NOUVEAU (jamais ne mute l'entrée), ne lit aucun état global. Cette pureté est ce qui la rend testable.\n\n**Raisonnement pas-à-pas** : découper le nettoyage en étapes à responsabilité unique → chacune pure (entrée → nouvelle sortie) → valider tôt → tester chaque fonction avec un petit cas connu.\n\n**Implémentation** :\n\`\`\`python\nimport pandas as pd\n\ndef load(chemin: str) -> pd.DataFrame:\n    return pd.read_csv(chemin)                       # responsabilité unique : lire\n\ndef validate(df: pd.DataFrame) -> pd.DataFrame:\n    attendues = {\"date\", \"produit\", \"prix\"}\n    manquantes = attendues - set(df.columns)\n    if manquantes:                                    # échoue TÔT (jour 122)\n        raise ValueError(f\"Colonnes manquantes : {manquantes}\")\n    return df\n\ndef clean(df: pd.DataFrame) -> pd.DataFrame:\n    df = df.copy()                                    # PUR : ne mute pas l'entrée\n    df[\"produit\"] = df[\"produit\"].str.lower().str.strip()\n    df = df[df[\"prix\"] >= 0]\n    df[\"prix\"] = df[\"prix\"].fillna(df[\"prix\"].median())\n    return df\n\ndef report(avant: pd.DataFrame, apres: pd.DataFrame) -> dict:\n    return {\"lignes_avant\": len(avant), \"lignes_apres\": len(apres),\n            \"lignes_retirees\": len(avant) - len(apres)}\n\n# Composition : chaque étape isolée, remplaçable, testable\nbrut = load(\"ventes.csv\"); valide = validate(brut); propre = clean(valide)\n\n# Test pytest (jour 124) — une fonction pure se teste avec un cas connu :\ndef test_clean_retire_prix_negatif():\n    entree = pd.DataFrame({\"produit\": [\"A\"], \"prix\": [-5], \"date\": [\"2024-01-01\"]})\n    assert len(clean(entree)) == 0\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi \`df.copy()\` dans \`clean\` ? (b) Que teste-t-on plus facilement dans une fonction que dans un notebook ? (c) Pourquoi \`validate\` échoue-t-il tôt ?\n→ (a) pour rester PUR : ne pas muter l'entrée, éviter les surprises pour l'appelant ; (b) le comportement — on donne un cas connu et on vérifie la sortie (un notebook ne se teste pas) ; (c) un pipeline qui traite des données invalides produit des résultats invalides — mieux vaut échouer clairement à l'entrée.\n\n**Variante** : écris un test qui donne à \`validate\` un DataFrame SANS la colonne \`prix\` et vérifie qu'il lève \`ValueError\` — tu prouves que ton garde-fou de validation fonctionne, pas seulement le chemin heureux.`,
    caseStudy: `La promotion d'un nettoyage exploratoire en fonctions pures testées est exactement la transition « notebook → production » que font les équipes data : un notebook est excellent pour explorer mais ingérable en production (état caché, ordre d'exécution, non testable, non rejouable). Les pipelines de données d'entreprise sont faits de fonctions pures composables, précisément pour être testées, rejouées et maintenues. Un data analyst qui reste au notebook plafonne ; celui qui sait transformer son analyse en module de qualité réutilisable accède aux rôles de data engineer. Cette compétence — code pur, testé, composable — est directement visible dans un repo et attendue dès qu'un traitement doit tourner plus d'une fois.`,
    interview: `**« Comment passes-tu d'un nettoyage en notebook à du code de production ? »** → Je PROMEUS le nettoyage en fonctions PURES à responsabilité unique : load (lire), validate (vérifier les invariants et échouer tôt si les données ne sont pas celles attendues), clean (appliquer les décisions justifiées), report (métriques avant/après). Pures signifie : mêmes entrées → mêmes sorties, sans effet de bord ni état global, et sans muter l'entrée (\`df.copy()\`). Ça change tout : c'est réutilisable (sur n'importe quel DataFrame de même forme), testable avec pytest (un petit cas sale connu → sortie vérifiée), rejouable (déterministe, indépendant de l'ordre des cellules) et composable (\`report(clean(validate(load(x))))\`). Le notebook explore, le module produit — c'est la transition de « une analyse qui a marché » à « un outil de qualité réutilisable ».`,
    future: `- **Bankable** : transformer une analyse en fonctions pures testées est la transition « analyst → data engineer » — directement visible dans un repo et attendue dès qu'un traitement tourne plus d'une fois.\n- Ces fonctions (load/validate/clean/report) sont exactement la couche transform de DataPulse (jour 143) et de tout pipeline ETL (jours 138-139).\n- Le principe « fonction pure, testable, composable » est universel — c'est le clean code (mois 2) et la testabilité (jour 124) appliqués à la donnée.`,
    solution: {
      logic: "Solution simple : mettre le code du notebook dans des fonctions. Solution améliorée : découper en fonctions PURES à responsabilité unique (load/validate/clean/report), chacune renvoyant une nouvelle valeur sans muter l'entrée ni lire d'état global, valider tôt (échec clair sur données invalides), et TESTER chaque fonction avec pytest sur des cas connus (y compris les cas d'échec). La preuve : le pipeline se compose, se rejoue de façon déterministe, et chaque étape est couverte par un test.",
      pitfalls: ["Fonction qui mute son entrée ou lit un état global : impure, source de surprises — utiliser \`df.copy()\` et ne dépendre que des paramètres.", "Tout garder dans le notebook : non testable, non rejouable, dépendant de l'ordre d'exécution des cellules.", "Sauter la validation : un pipeline qui traite des données invalides produit des résultats invalides sans le signaler.", "Une fonction fourre-tout qui load + clean + report : impossible à tester ou remplacer par morceau — une responsabilité par fonction."],
      checks: ["Le nettoyage est découpé en fonctions pures à responsabilité unique (load/validate/clean/report).", "Aucune fonction ne mute son entrée ni ne lit d'état global.", "\`validate\` échoue tôt sur des données non conformes.", "Chaque fonction est testée avec pytest (chemin heureux ET cas d'échec).", "Le pipeline se compose et se rejoue de façon déterministe."],
      oral: "Explique la transition « le notebook explore, le module produit » : fonctions pures (mêmes entrées → mêmes sorties, pas de mutation), à responsabilité unique, testables et composables. Insiste sur validate qui échoue tôt et sur la testabilité (un notebook ne se teste pas). Cette transition « analyst → data engineer » est ce qui montre que tu sais livrer un outil, pas juste une analyse.",
    },
  },

  // ── SQL avancé : normalisation ──
  134: {
    lessons: ['database-modeling.md'],
    theory: `**Modèle mental : normaliser un schéma, c'est éliminer la REDONDANCE en donnant à chaque fait UN SEUL endroit où vivre — et la redondance n'est pas qu'un gaspillage d'espace, c'est la SOURCE des anomalies de mise à jour (modifier une donnée à un endroit et l'oublier à un autre laisse la base incohérente).** Tu connais SQL depuis le mois 1 ; ici tu apprends à CONCEVOIR un schéma sain. La normalisation est une suite d'étapes (formes normales) qui mènent à un principe simple : chaque donnée non-clé dépend de LA clé, de TOUTE la clé, et de RIEN QUE la clé.\n\nLes trois anomalies que la redondance provoque (le POURQUOI) :\n- **Anomalie de mise à jour** : l'adresse d'un client répétée sur 100 commandes ; il déménage, on met à jour 99 lignes et on en oublie une → deux adresses contradictoires.\n- **Anomalie d'insertion** : impossible d'enregistrer un produit tant qu'il n'a pas été commandé (s'il n'existe que dans la table commandes).\n- **Anomalie de suppression** : supprimer la dernière commande d'un client efface aussi ses coordonnées.\n\nLes formes normales (le COMMENT), progressivement :\n1. **1NF** : chaque cellule contient une valeur ATOMIQUE (pas de liste « rouge, bleu » dans une colonne), chaque ligne est unique. On sort les valeurs répétées en lignes.\n2. **2NF** (sur 1NF) : chaque attribut non-clé dépend de TOUTE la clé primaire, pas d'une partie. Pertinent quand la clé est composite — un attribut qui ne dépend que d'une partie de la clé part dans sa propre table.\n3. **3NF** (sur 2NF) : aucun attribut non-clé ne dépend d'un AUTRE attribut non-clé (pas de dépendance transitive). Si \`ville\` dépend de \`code_postal\` qui dépend de la clé, \`ville\` doit sortir — le code postal détermine la ville indépendamment de la commande.\n\nLe résultat : des tables reliées par des CLÉS ÉTRANGÈRES, chaque fait stocké une seule fois. Les jointures (jour 131) recomposent l'information à la lecture.\n\nQuand DÉNORMALISER (l'arbitrage) : la normalisation optimise l'INTÉGRITÉ (écritures sûres) mais multiplie les jointures à la lecture. Pour des besoins de lecture massive (analytique, reporting), on dénormalise parfois délibérément — en ACCEPTANT la redondance et en gérant sa cohérence autrement. C'est un choix conscient, pas une négligence.\n\nLa règle du jour : **normalise jusqu'en 3NF pour éliminer la redondance et ses anomalies (chaque fait à un seul endroit, relié par clés étrangères), et dénormalise seulement délibérément quand la lecture l'exige**. Un schéma normalisé est un schéma où l'on ne peut pas se contredire soi-même.`,
    guided: `**Énoncé** : modéliser un schéma e-commerce en 3NF, en justifiant chaque relation.\n\n**Exemple simple** : la dépendance transitive à éliminer (3NF) — dans \`commandes(id, client_id, client_ville)\`, \`client_ville\` dépend de \`client_id\`, pas de la commande. On la SORT dans une table \`clients\` : \`commandes\` ne garde que \`client_id\` (clé étrangère). Un fait client vit chez le client.\n\n**Raisonnement pas-à-pas** : partir d'une table plate redondante → repérer les dépendances (qui détermine quoi) → sortir chaque groupe de faits dans sa table → relier par clés étrangères.\n\n**Schéma** :\n\`\`\`sql\n-- AVANT (table plate, redond­ante) : anomalies garanties\n-- ventes(id, client_nom, client_ville, produit_nom, produit_prix, quantite)\n--   → client_ville répété, produit_prix répété : incohérences à la 1re mise à jour\n\n-- APRÈS (3NF) : chaque fait à UN endroit, relié par clés étrangères\nCREATE TABLE clients (\n  id INTEGER PRIMARY KEY, nom TEXT NOT NULL, ville TEXT NOT NULL);\nCREATE TABLE produits (\n  id INTEGER PRIMARY KEY, nom TEXT NOT NULL, prix REAL NOT NULL);   -- prix : 1 seul endroit\nCREATE TABLE commandes (\n  id INTEGER PRIMARY KEY,\n  client_id INTEGER NOT NULL REFERENCES clients(id),               -- clé étrangère\n  produit_id INTEGER NOT NULL REFERENCES produits(id),\n  quantite INTEGER NOT NULL CHECK (quantite > 0));\n-- La ville d'un client, le prix d'un produit : stockés une fois, recomposés par JOIN.\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi la redondance est-elle dangereuse, au-delà de l'espace ? (b) Que dit la 3NF en une phrase ? (c) Quand dénormaliser volontairement ?\n→ (a) elle provoque des anomalies : mettre à jour un fait à un endroit et l'oublier ailleurs laisse la base incohérente ; (b) chaque attribut non-clé dépend de la clé, de toute la clé, et de rien que la clé (pas d'un autre attribut non-clé) ; (c) quand la lecture massive (analytique/reporting) l'exige, en acceptant et gérant la redondance délibérément.\n\n**Variante** : prends ton schéma et simule un déménagement de client — vérifie qu'il ne faut modifier QU'UNE ligne (dans \`clients\`). Si tu dois toucher plusieurs tables, il reste de la redondance à normaliser.`,
    caseStudy: `La conception de schéma est une décision structurante en entreprise : un schéma mal normalisé accumule les incohérences (deux adresses pour un même client, deux prix pour un même produit) qui corrompent les rapports et minent la confiance dans les données. Un schéma en 3NF garantit qu'un fait ne peut pas se contredire, car il n'existe qu'à un endroit. À l'inverse, les bases analytiques (entrepôts de données) sont souvent délibérément dénormalisées pour accélérer les lectures massives — un arbitrage conscient entre intégrité et performance de lecture. Savoir normaliser (et justifier quand dénormaliser) est une compétence de data engineering et de conception de bases directement évaluée, et le fondement de tout schéma sur lequel reposent des analyses fiables.`,
    interview: `**« Qu'est-ce que la normalisation, et pourquoi la 3NF ? »** → Normaliser, c'est éliminer la REDONDANCE en donnant à chaque fait un seul endroit où vivre. Ce n'est pas qu'une question d'espace : la redondance provoque des ANOMALIES — modifier une donnée à un endroit et l'oublier ailleurs laisse la base incohérente (deux adresses pour un client), ou impossible d'insérer un produit non encore commandé. Les formes normales y mènent progressivement : 1NF (valeurs atomiques), 2NF (dépendance de toute la clé composite), 3NF (pas de dépendance transitive entre attributs non-clés). La 3NF se résume : chaque attribut non-clé dépend de la clé, de toute la clé, et de rien que la clé. Le résultat : des tables reliées par clés étrangères, chaque fait stocké une fois, recomposé par jointure. Je dénormalise seulement délibérément, pour des lectures massives, en acceptant la redondance consciemment.`,
    future: `- **Bankable** : concevoir un schéma en 3NF (et justifier quand dénormaliser) est une compétence de conception de bases directement évaluée — le fondement de données fiables.\n- Un schéma sain est la cible de chargement de DataPulse (load, jour 144) et de tout pipeline qui écrit en base.\n- Le raisonnement « chaque fait à un seul endroit, arbitrage intégrité vs lecture » guide toute modélisation de données, des bases relationnelles aux entrepôts analytiques.`,
    solution: {
      logic: "Solution simple : séparer les données en quelques tables. Solution améliorée : atteindre la 3NF en repérant les dépendances (1NF valeurs atomiques, 2NF dépendance de toute la clé, 3NF pas de dépendance transitive), sortir chaque groupe de faits dans sa table reliée par clé étrangère (avec contraintes NOT NULL/CHECK/REFERENCES), justifier chaque relation, et décider consciemment de toute dénormalisation. La preuve : modifier un fait (déménagement client) ne touche qu'une seule ligne.",
      pitfalls: ["Laisser de la redondance (prix/adresse répétés) : anomalies de mise à jour, insertion, suppression garanties.", "Dépendance transitive non éliminée (ville dépend du code postal dépend de la clé) : viole la 3NF.", "Valeurs non atomiques (liste dans une cellule) : viole la 1NF, filtrage/jointure impossibles.", "Dénormaliser par négligence plutôt que par choix : redondance non gérée, incohérences silencieuses."],
      checks: ["Chaque cellule est atomique et chaque ligne unique (1NF).", "Chaque attribut non-clé dépend de toute la clé (2NF) et de rien que la clé (3NF).", "Les tables sont reliées par des clés étrangères (REFERENCES).", "Modifier un fait ne touche qu'une seule ligne.", "Toute dénormalisation éventuelle est un choix justifié, pas un oubli."],
      oral: "Explique que la redondance cause des ANOMALIES (pas juste du gaspillage) : mise à jour, insertion, suppression. Résume la 3NF (« la clé, toute la clé, rien que la clé ») et montre la recomposition par jointure. Le test du déménagement (une seule ligne à modifier) prouve concrètement que ton schéma est sain. Mentionne la dénormalisation délibérée pour montrer que tu connais l'arbitrage intégrité/lecture.",
    },
  },

  // ── SQL avancé : index ──
  135: {
    lessons: ['database-modeling.md', 'sql-foundations.md'],
    theory: `**Modèle mental : un index est une STRUCTURE DE RECHERCHE (un arbre trié, le plus souvent un B-tree) qui transforme un BALAYAGE COMPLET de la table (O(n), lire chaque ligne) en une recherche directe (O(log n)) — mais il a un COÛT (espace disque + ralentissement des écritures), donc on n'indexe QUE les colonnes vraiment interrogées, et on MESURE avant/après.** C'est l'application directe de la pensée algorithmique (complexité) et du réflexe « mesurer avant d'optimiser » (jour 102) au monde des bases. Sans index, chercher \`WHERE email = 'x'\` oblige la base à lire TOUTE la table ; avec un index sur \`email\`, elle va droit au but.\n\nComment ça marche et pourquoi c'est rapide :\n1. **Le B-tree** : l'index maintient les valeurs d'une colonne TRIÉES dans une structure d'arbre équilibré. Chercher une valeur = descendre l'arbre (quelques sauts) au lieu de balayer n lignes. C'est pourquoi un index accélère les recherches d'égalité, de plage (\`BETWEEN\`, \`>\`) et les \`ORDER BY\` sur la colonne indexée.\n2. **Où indexer** : les colonnes des \`WHERE\` fréquents, les colonnes de JOIN (clés étrangères), les colonnes de tri fréquent. Un index composite \`(a, b)\` sert les requêtes filtrant sur \`a\` puis \`b\` (l'ordre compte).\n3. **Le coût — pourquoi ne pas tout indexer** : chaque index occupe de l'espace et doit être MIS À JOUR à chaque INSERT/UPDATE/DELETE (l'arbre se rééquilibre). Trop d'index ralentit les écritures et gaspille de l'espace. Un index sur une colonne jamais interrogée est un pur coût.\n\nL'outil de décision — \`EXPLAIN\` (ou \`EXPLAIN QUERY PLAN\`) : il montre le PLAN d'exécution — la base fait-elle un « scan » complet (lent) ou un « index seek » (rapide) ? C'est ce qui permet de DIAGNOSTIQUER une requête lente et de VÉRIFIER qu'un index est réellement utilisé. On ne devine pas : on lit le plan.\n\nLa démarche professionnelle (identique au jour 102) : MESURER la requête lente (temps + plan), ajouter un index JUSTIFIÉ, REMESURER pour prouver le gain. Un index ajouté « au cas où » sans mesure est de l'optimisation à l'aveugle.\n\nLa règle du jour : **indexe les colonnes réellement interrogées (WHERE/JOIN/ORDER BY), diagnostique avec \`EXPLAIN\`, mesure avant/après, et n'indexe jamais par principe — chaque index accélère les lectures mais coûte en écritures et en espace**. L'index est un arbitrage lecture/écriture qu'on décide sur des mesures, pas des impressions.`,
    guided: `**Énoncé** : sur une base peuplée, mesurer une requête lente, ajouter un index justifié, remesurer.\n\n**Exemple simple** : le diagnostic avant tout — \`EXPLAIN QUERY PLAN SELECT * FROM commandes WHERE client_id = 42;\`. S'il affiche « SCAN commandes » (balayage complet), un index sur \`client_id\` le transformera en « SEARCH ... USING INDEX ». On LIT le plan, on ne devine pas.\n\n**Raisonnement pas-à-pas** : identifier la requête lente → lire son plan (\`EXPLAIN\`) → si scan complet sur une colonne filtrée, ajouter l'index → remesurer temps ET plan.\n\n**Implémentation** :\n\`\`\`sql\n-- 1. MESURER : plan AVANT (scan complet = lent sur grande table)\nEXPLAIN QUERY PLAN\nSELECT * FROM commandes WHERE client_id = 42;      -- \"SCAN commandes\"\n\n-- 2. INDEXER la colonne réellement filtrée (justifié : WHERE fréquent sur client_id)\nCREATE INDEX idx_commandes_client ON commandes(client_id);\n\n-- 3. REMESURER : plan APRÈS\nEXPLAIN QUERY PLAN\nSELECT * FROM commandes WHERE client_id = 42;      -- \"SEARCH ... USING INDEX idx_commandes_client\"\n\n-- Index composite pour un filtre à deux colonnes (l'ordre suit le WHERE) :\nCREATE INDEX idx_ventes_date_produit ON ventes(date, produit_id);\n-- Sert WHERE date = ? [AND produit_id = ?], mais PAS un filtre sur produit_id seul.\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi un index accélère-t-il la recherche ? (b) Pourquoi ne pas indexer toutes les colonnes ? (c) À quoi sert \`EXPLAIN\` ?\n→ (a) il maintient les valeurs triées dans un arbre : recherche en O(log n) au lieu d'un balayage O(n) ; (b) chaque index coûte de l'espace et ralentit les écritures (mise à jour de l'arbre à chaque INSERT/UPDATE) ; un index jamais interrogé est un pur coût ; (c) montrer le plan d'exécution — scan complet vs utilisation d'index — pour diagnostiquer et vérifier.\n\n**Variante** : sur la même table, ajoute un index sur une colonne JAMAIS filtrée et mesure le ralentissement d'un gros INSERT en lot — tu vois concrètement le coût en écriture d'un index inutile.`,
    caseStudy: `Les index sont le premier levier de performance des bases en production : une requête qui balaie des millions de lignes (secondes ou minutes) devient instantanée avec le bon index. Mais l'excès d'index est un problème réel et courant — chaque index ralentit les écritures et gonfle la base, si bien qu'une table sur-indexée devient lente à alimenter. Les DBA et data engineers diagnostiquent avec \`EXPLAIN\`, ajoutent des index ciblés sur des requêtes mesurées, et suppriment les index inutilisés. Cette discipline « mesurer, indexer le prouvé, remesurer » est identique au réflexe de performance front (jour 102) et directement évaluée : savoir lire un plan d'exécution et justifier un index distingue quelqu'un qui comprend les bases de quelqu'un qui « ajoute des index au hasard ».`,
    interview: `**« Comment optimises-tu une requête SQL lente ? »** → Je ne devine pas, je DIAGNOSTIQUE avec \`EXPLAIN\` (le plan d'exécution) : la base fait-elle un balayage complet de la table (\`SCAN\`, lent) ou une recherche par index (\`SEARCH ... USING INDEX\`, rapide) ? Un index est une structure triée (B-tree) qui transforme une recherche O(n) en O(log n) ; j'en ajoute un sur les colonnes réellement interrogées — celles des \`WHERE\`, \`JOIN\` et \`ORDER BY\` fréquents. Puis je REMESURE (temps + plan) pour prouver le gain. Je n'indexe jamais par principe : chaque index coûte de l'espace et ralentit les écritures (l'arbre se met à jour à chaque INSERT/UPDATE), donc un index sur une colonne jamais filtrée est un pur coût. C'est un arbitrage lecture/écriture décidé sur des mesures — exactement comme « mesurer avant d'optimiser » au front.`,
    future: `- **Bankable** : savoir lire un plan \`EXPLAIN\`, justifier un index et remesurer distingue quelqu'un qui comprend les bases — une compétence de performance directement évaluée.\n- L'indexation garde performantes les requêtes de DataPulse et de tout tableau de bord sur base peuplée.\n- Le réflexe « diagnostiquer, optimiser le mesuré, remesurer » est universel (front jour 102, SQL ici, plus tard l'inférence LLM) — la marque d'un ingénieur qui ne devine pas.`,
    solution: {
      logic: "Solution simple : ajouter un index sur la colonne filtrée. Solution améliorée : MESURER d'abord avec \`EXPLAIN\` (scan vs index), n'indexer que les colonnes réellement interrogées (WHERE/JOIN/ORDER BY), choisir l'ordre d'un index composite selon le filtre, REMESURER temps et plan pour prouver le gain, et assumer l'arbitrage lecture/écriture (ne pas sur-indexer). La preuve : le plan passe de SCAN à SEARCH USING INDEX et le temps chute, sans dégrader inutilement les écritures.",
      pitfalls: ["Ajouter des index au hasard sans \`EXPLAIN\` : optimisation à l'aveugle, souvent sur la mauvaise colonne.", "Tout indexer « par sécurité » : écritures ralenties et espace gaspillé — chaque index a un coût.", "Index composite dans le mauvais ordre : inutile pour le filtre réel (l'ordre des colonnes compte).", "Ne pas remesurer après ajout : on ne prouve pas le gain ni que l'index est réellement utilisé."],
      checks: ["La requête lente est diagnostiquée avec \`EXPLAIN\` avant toute action.", "L'index porte sur une colonne réellement interrogée (WHERE/JOIN/ORDER BY).", "Le plan après montre l'utilisation de l'index (SEARCH, pas SCAN).", "Le gain est mesuré (temps avant/après).", "Le nombre d'index reste justifié (arbitrage lecture/écriture assumé)."],
      oral: "Décris l'index comme un arbre trié (O(log n) vs O(n)) et déroule la démarche mesurée : \`EXPLAIN\` → index justifié → remesure. Insiste sur le COÛT (écritures + espace) qui interdit d'indexer par principe. Relier au « mesurer avant d'optimiser » du front (jour 102) montre que tu vois le principe général, pas une astuce SQL isolée.",
    },
  },

  // ── SQL avancé : transactions (ACID) ──
  136: {
    lessons: ['database-modeling.md'],
    theory: `**Modèle mental : une transaction rend un GROUPE d'opérations ATOMIQUE — soit TOUT réussit et est validé (\`COMMIT\`), soit une erreur annule TOUT (\`ROLLBACK\`) et la base revient à son état d'avant. ACID garantit qu'une panne au milieu ne laisse JAMAIS la base dans un état incohérent — comme un stock décrémenté sans commande créée.** Certaines opérations métier n'ont de sens qu'ENSEMBLE : débiter un compte ET créditer un autre, créer une commande ET décrémenter le stock. Si la première réussit et la seconde échoue (panne, erreur), un état partiel corrompt les données. La transaction interdit cet entre-deux.\n\nACID — les quatre garanties (le POURQUOI) :\n- **A — Atomicité** : tout ou rien. Les opérations d'une transaction forment un bloc indivisible ; un échec au milieu annule tout ce qui précède.\n- **C — Cohérence** : la transaction fait passer la base d'un état valide à un autre état valide (les contraintes — clés étrangères, CHECK — restent respectées).\n- **I — Isolation** : deux transactions concurrentes ne se marchent pas dessus ; chacune voit une base cohérente, comme si elles s'exécutaient l'une après l'autre.\n- **D — Durabilité** : une fois \`COMMIT\`, les données survivent même à une coupure de courant.\n\nLe mécanisme : \`BEGIN\` ouvre la transaction, on exécute les opérations, \`COMMIT\` valide TOUT d'un coup, \`ROLLBACK\` annule TOUT en cas d'erreur. En pratique, on englobe les opérations dans un \`try\` : succès → commit, exception → rollback. C'est l'atomicité du CRUD front (jour 114 « l'UI ne ment pas ») portée au niveau des données : la base ne reste jamais à moitié modifiée.\n\nQuand une transaction est NÉCESSAIRE : dès que PLUSIEURS écritures doivent réussir ou échouer ENSEMBLE pour que les données restent cohérentes (transfert, commande+stock, écritures liées d'un pipeline). Quand elle est superflue : une seule écriture indépendante est déjà atomique en elle-même.\n\nL'isolation, en bref : plusieurs niveaux existent (des lectures plus ou moins strictes) ; l'essentiel à retenir est que sans isolation, des transactions concurrentes peuvent lire des états intermédiaires et produire des incohérences (double dépense, survente). Le niveau se choisit selon le compromis cohérence/performance.\n\nLa règle du jour : **englobe dans une transaction toute suite d'écritures qui doivent réussir ou échouer ENSEMBLE ; commit sur succès, rollback sur erreur ; ACID garantit qu'aucune panne ne laisse la base à moitié modifiée**. Une transaction, c'est la promesse « tout ou rien » qui protège l'intégrité des données.`,
    guided: `**Énoncé** : écrire une transaction (commande + décrément de stock) et PROUVER le rollback en cas d'échec.\n\n**Exemple simple** : la structure tout-ou-rien — \`BEGIN; ...opérations...; COMMIT;\` et \`ROLLBACK\` si une opération échoue. Les deux écritures (commande, stock) vivent dans le même bloc : impossible d'en valider une sans l'autre.\n\n**Raisonnement pas-à-pas** : ouvrir la transaction → exécuter les écritures liées → si toutes réussissent, commit → si l'une échoue (stock insuffisant), rollback → vérifier que la base est intacte.\n\n**Implémentation** :\n\`\`\`python\nimport sqlite3\ncon = sqlite3.connect(\"boutique.db\")\ntry:\n    con.execute(\"BEGIN\")                                    # début de transaction\n    con.execute(\"INSERT INTO commandes(produit_id, qte) VALUES (?, ?)\", (42, 3))\n    cur = con.execute(\"UPDATE produits SET stock = stock - 3 \"\n                      \"WHERE id = 42 AND stock >= 3\")        # échoue si stock insuffisant\n    if cur.rowcount == 0:\n        raise ValueError(\"Stock insuffisant\")                # déclenche le rollback\n    con.commit()                                             # TOUT validé d'un coup\nexcept Exception as e:\n    con.rollback()                                           # TOUT annulé : base intacte\n    print(f\"Transaction annulée : {e}\")\n\`\`\`\nPreuve du rollback : après un échec de stock, ni la commande n'est créée, ni le stock n'est décrémenté — la base est exactement comme avant. Sans transaction, la commande aurait été insérée puis l'échec du stock aurait laissé un état incohérent (commande fantôme).\n\n**Mini-quiz** : (a) Que garantit l'atomicité ? (b) Pourquoi cette opération a-t-elle besoin d'une transaction ? (c) Que se passe-t-il sur \`ROLLBACK\` ?\n→ (a) tout ou rien : un échec au milieu annule tout ce qui précède ; (b) commande et décrément de stock doivent réussir ou échouer ENSEMBLE, sinon état incohérent (stock décrémenté sans commande, ou l'inverse) ; (c) toutes les opérations depuis \`BEGIN\` sont annulées, la base revient à son état d'avant.\n\n**Variante** : provoque volontairement une erreur APRÈS l'INSERT mais AVANT le COMMIT (ex. une contrainte violée) et vérifie qu'AUCUNE des deux écritures ne subsiste — c'est la démonstration concrète de l'atomicité.`,
    caseStudy: `Les transactions sont indispensables dès qu'une opération métier touche plusieurs écritures liées : transfert bancaire (débit + crédit), commande e-commerce (commande + stock + paiement), écritures groupées d'un pipeline. Sans transaction, une panne au mauvais moment laisse un état incohérent — de l'argent débité sans être crédité, un stock décrémenté sans vente — qui exige une réconciliation manuelle coûteuse et mine la confiance. ACID est précisément ce qui permet aux systèmes financiers et transactionnels d'être fiables malgré les pannes. Savoir identifier quand une transaction est nécessaire, l'implémenter avec commit/rollback, et prouver l'atomicité est une compétence de fiabilité de données essentielle — et directement pertinente pour le chargement transactionnel de DataPulse (jour 144).`,
    interview: `**« Qu'est-ce qu'une transaction et que garantit ACID ? »** → Une transaction rend un groupe d'opérations ATOMIQUE : soit tout réussit et est validé (\`COMMIT\`), soit une erreur annule tout (\`ROLLBACK\`) et la base revient à son état d'avant. C'est nécessaire dès que plusieurs écritures doivent réussir ou échouer ENSEMBLE — une commande et son décrément de stock, un débit et un crédit — sinon une panne au milieu laisse un état incohérent. ACID : Atomicité (tout ou rien), Cohérence (on passe d'un état valide à un autre, contraintes respectées), Isolation (les transactions concurrentes ne se corrompent pas), Durabilité (après commit, ça survit à une panne). En pratique, j'englobe les écritures dans un \`try\` : succès → commit, exception → rollback. La garantie clé : la base n'est jamais laissée à moitié modifiée.`,
    future: `- **Bankable** : savoir quand une transaction est nécessaire et prouver l'atomicité est une compétence de fiabilité de données essentielle — critique pour tout système transactionnel.\n- Les transactions garantissent le chargement cohérent de DataPulse (load transactionnel, jour 144) et la rejouabilité des pipelines (jour 139).\n- Le principe « tout ou rien » est le pendant données de l'atomicité du CRUD front (jour 114) — une cohérence de bout en bout que tu sauras raisonner à tous les niveaux.`,
    solution: {
      logic: "Solution simple : BEGIN, les écritures, COMMIT. Solution améliorée : englober les écritures LIÉES dans un try (commit sur succès, rollback sur toute exception), détecter les échecs métier (stock insuffisant via rowcount) pour déclencher le rollback, et PROUVER l'atomicité en provoquant une erreur au milieu et en vérifiant qu'aucune écriture ne subsiste. La preuve : après un échec, la base est exactement dans son état d'avant — ni commande fantôme, ni stock incohérent.",
      pitfalls: ["Faire les écritures liées hors transaction : une panne au milieu laisse un état incohérent (stock décrémenté sans commande).", "Oublier le rollback dans le \`except\` : la transaction reste ouverte ou l'état partiel persiste.", "Ne pas détecter les échecs métier (rowcount == 0) : le décrément « réussit » sur 0 ligne et l'incohérence passe.", "Mettre dans une transaction des opérations indépendantes qui n'ont pas à être atomiques : verrouillage inutile."],
      checks: ["Les écritures qui doivent réussir ensemble sont dans une seule transaction.", "Commit sur succès complet, rollback sur toute exception.", "Les échecs métier (stock insuffisant) déclenchent le rollback.", "Après un échec provoqué, aucune écriture ne subsiste (atomicité prouvée).", "Aucune opération indépendante n'est enfermée inutilement dans une transaction."],
      oral: "Définis la transaction comme « tout ou rien » et déroule ACID (atomicité en tête). Donne l'exemple commande+stock : sans transaction, un échec laisse un état incohérent. Montre la structure try → commit / except → rollback, et la PREUVE par erreur provoquée (aucune écriture ne subsiste). Relier à l'atomicité du CRUD front (jour 114) montre une cohérence de raisonnement à tous les niveaux.",
    },
  },

  // ── SQL avancé : requêtes analytiques ──
  137: {
    lessons: ['sql-foundations.md'],
    theory: `**Modèle mental : les sous-requêtes et surtout les fonctions FENÊTRE (window functions) répondent aux questions que \`GROUP BY\` ne peut PAS — une window function calcule sur un groupe SANS écraser les lignes (rang, cumul, moyenne glissante, comparaison à la moyenne du groupe), là où \`GROUP BY\` RÉDUIT à une seule ligne par groupe.** Tu sais agréger (\`GROUP BY\`, jour 130 côté pandas). Mais beaucoup de questions analytiques ont besoin de GARDER chaque ligne tout en la reliant à son groupe : « le rang de chaque vente dans son mois », « le cumul des ventes jour après jour », « l'écart de chaque salaire à la moyenne de son département ». C'est le rôle des window functions.\n\nLes outils analytiques :\n1. **Les sous-requêtes** : une requête dans une requête. Dans le \`WHERE\` (\`WHERE prix > (SELECT AVG(prix) FROM produits)\`), dans le \`FROM\` (une table dérivée), ou corrélée (qui référence la requête externe). Elles composent des questions en plusieurs étapes.\n2. **Les CTE (\`WITH ... AS\`)** : nomment une sous-requête pour rendre une requête complexe LISIBLE et réutilisable, de haut en bas. \`WITH ventes_mois AS (...) SELECT ... FROM ventes_mois\` — l'équivalent SQL d'une variable intermédiaire.\n3. **Les fonctions fenêtre** : \`fonction() OVER (PARTITION BY groupe ORDER BY colonne)\`. La \`PARTITION BY\` définit le groupe (comme un GROUP BY), mais le résultat est calculé POUR CHAQUE LIGNE :\n   - \`ROW_NUMBER()\` / \`RANK()\` : le rang dans le groupe (top N par catégorie).\n   - \`SUM(...) OVER (ORDER BY date)\` : le cumul (running total).\n   - \`AVG(...) OVER (PARTITION BY dept)\` : la moyenne du groupe, répétée sur chaque ligne pour comparaison.\n   - \`LAG\`/\`LEAD\` : la valeur de la ligne précédente/suivante (évolution d'un mois à l'autre).\n\nLa distinction décisive : \`GROUP BY\` répond « une valeur PAR groupe » (le CA par mois) ; une window function répond « pour chaque ligne, sa relation à son groupe » (le rang de cette vente dans son mois, sans perdre la vente). Confondre les deux mène à des contorsions (auto-jointures compliquées) là où une window function fait le travail en une ligne.\n\nLa règle du jour : **utilise les sous-requêtes/CTE pour composer des questions en étapes lisibles, et les fonctions fenêtre pour calculer sur un groupe SANS écraser les lignes (rang, cumul, comparaison) — là où \`GROUP BY\` réduirait**. Ces outils transforment SQL d'un langage d'extraction en un langage d'ANALYSE.`,
    guided: `**Énoncé** : écrire 5 requêtes analytiques (CA/mois, top clients, produits jamais vendus, cumuls).\n\n**Exemple simple** : la window function qui garde les lignes — \`ROW_NUMBER() OVER (PARTITION BY mois ORDER BY montant DESC)\`. Elle numérote les ventes DANS chaque mois par montant décroissant, sans réduire : filtrer sur \`= 1\` donne la meilleure vente de chaque mois, lignes intactes.\n\n**Raisonnement pas-à-pas** : distinguer « une valeur par groupe » (GROUP BY) de « chaque ligne reliée à son groupe » (window) → composer les étapes avec des CTE → appliquer la bonne window function.\n\n**Implémentation** :\n\`\`\`sql\n-- 1. CA par mois (GROUP BY : une ligne par mois)\nSELECT strftime('%Y-%m', date) AS mois, SUM(montant) AS ca\nFROM ventes GROUP BY mois;\n\n-- 2. Top 3 clients par CA (window : rang SANS perdre les lignes)\nWITH par_client AS (\n  SELECT client_id, SUM(montant) AS ca FROM ventes GROUP BY client_id)\nSELECT client_id, ca,\n       RANK() OVER (ORDER BY ca DESC) AS rang\nFROM par_client\nWHERE rang <= 3;   -- (via sous-requête si le SGBD n'accepte pas rang au WHERE)\n\n-- 3. Produits JAMAIS vendus (sous-requête NOT IN / LEFT JOIN ... IS NULL)\nSELECT nom FROM produits\nWHERE id NOT IN (SELECT DISTINCT produit_id FROM ventes);\n\n-- 4. Cumul des ventes jour après jour (running total)\nSELECT date, SUM(montant) OVER (ORDER BY date) AS cumul FROM ventes;\n\n-- 5. Écart de chaque vente à la moyenne de son mois (comparaison intra-groupe)\nSELECT date, montant,\n       montant - AVG(montant) OVER (PARTITION BY strftime('%Y-%m', date)) AS ecart_moyenne\nFROM ventes;\n\`\`\`\n\n**Mini-quiz** : (a) Quelle différence entre \`GROUP BY\` et une window function ? (b) À quoi sert une CTE ? (c) Comment calculer un cumul (running total) ?\n→ (a) GROUP BY réduit à une ligne par groupe ; une window function calcule sur le groupe SANS écraser les lignes (rang, cumul, comparaison) ; (b) nommer une sous-requête pour rendre une requête complexe lisible et composable en étapes ; (c) \`SUM(...) OVER (ORDER BY date)\` — une somme fenêtrée ordonnée.\n\n**Variante** : ajoute \`LAG(ca) OVER (ORDER BY mois)\` à ta requête de CA mensuel pour calculer la croissance d'un mois sur l'autre — tu obtiens une analyse d'évolution qu'un GROUP BY seul ne peut pas produire.`,
    caseStudy: `Les requêtes analytiques (window functions, CTE, sous-requêtes) sont le cœur du reporting et de l'analyse en entreprise : classements (top clients, produits), cumuls (CA cumulé, cohortes), comparaisons intra-groupe (performance vs moyenne d'équipe), évolutions (croissance mois sur mois). Les analysts qui maîtrisent les window functions répondent à ces questions en une requête lisible ; ceux qui ne les connaissent pas s'enlisent dans des auto-jointures compliquées et fragiles, ou exportent tout vers un tableur. La maîtrise des window functions est un différenciateur net en entretien data — c'est souvent LA question SQL avancée posée, précisément parce qu'elle sépare ceux qui « savent extraire » de ceux qui « savent analyser » directement en base.`,
    interview: `**« Quelle est la différence entre \`GROUP BY\` et une fonction fenêtre ? »** → \`GROUP BY\` RÉDUIT : une ligne par groupe (le CA par mois). Une fonction fenêtre (\`OVER (PARTITION BY ... ORDER BY ...)\`) calcule sur le groupe SANS écraser les lignes : chaque ligne est conservée et reliée à son groupe. C'est ce qui permet le rang (\`ROW_NUMBER\`/\`RANK\` — top N par catégorie), le cumul (\`SUM() OVER (ORDER BY date)\` — running total), la comparaison intra-groupe (\`AVG() OVER (PARTITION BY dept)\` — écart à la moyenne du département), ou l'évolution (\`LAG\`/\`LEAD\`). Confondre les deux mène à des auto-jointures compliquées là où une window function fait le travail en une ligne. J'utilise aussi les CTE (\`WITH\`) pour découper une requête complexe en étapes lisibles. Ces outils font de SQL un langage d'analyse, pas seulement d'extraction.`,
    future: `- **Bankable** : les window functions sont souvent LA question SQL avancée en entretien data — elles séparent ceux qui extraient de ceux qui analysent directement en base.\n- Ces requêtes alimentent les analyses de DataPulse (répondre aux questions métier, jour 145) et tout reporting.\n- Le raisonnement « réduire vs relier chaque ligne à son groupe » se retrouve dans pandas (transform vs aggregate) et dans toute analyse — une pensée analytique transférable.`,
    solution: {
      logic: "Solution simple : des SELECT avec GROUP BY et sous-requêtes. Solution améliorée : distinguer les questions « une valeur par groupe » (GROUP BY) des questions « chaque ligne reliée à son groupe » (window functions), utiliser \`ROW_NUMBER/RANK\` pour les classements, \`SUM() OVER (ORDER BY)\` pour les cumuls, \`AVG() OVER (PARTITION BY)\` pour les comparaisons intra-groupe, \`LAG/LEAD\` pour les évolutions, et des CTE pour rendre les requêtes complexes lisibles. La preuve : les 5 requêtes répondent en une passe lisible à des questions qu'un GROUP BY seul ne peut pas traiter.",
      pitfalls: ["Utiliser GROUP BY là où il faut une window function : on perd les lignes qu'on voulait garder (rang, cumul impossibles).", "Auto-jointures compliquées pour un classement ou un cumul que \`ROW_NUMBER\`/\`SUM() OVER\` font en une ligne.", "Requête monolithique illisible au lieu de CTE nommées composant les étapes.", "Oublier l'\`ORDER BY\` dans une window de cumul : le running total n'a pas de sens sans ordre."],
      checks: ["Les questions « par groupe » utilisent GROUP BY, les questions « par ligne reliée au groupe » des window functions.", "Les classements utilisent ROW_NUMBER/RANK, les cumuls SUM() OVER (ORDER BY).", "Les comparaisons intra-groupe utilisent AVG/... OVER (PARTITION BY).", "Les requêtes complexes sont découpées en CTE lisibles.", "Les 5 requêtes analytiques sont correctes et commentées."],
      oral: "Pose la distinction clé : « GROUP BY réduit à une ligne par groupe ; une window function garde chaque ligne en la reliant à son groupe ». Illustre avec rang, cumul, comparaison à la moyenne du groupe. Mentionne les CTE pour la lisibilité. Savoir répondre à « rang de chaque vente dans son mois » avec une window function (pas une auto-jointure) est ce qui prouve ton niveau SQL analytique.",
    },
  },

  // ── ETL : concevoir un pipeline ──
  138: {
    lessons: ['etl-pipelines.md'],
    theory: `**Modèle mental : un pipeline ETL sépare TROIS responsabilités — EXTRACT (lire les sources telles quelles), TRANSFORM (nettoyer/normaliser/joindre en mémoire), LOAD (écrire dans la cible) — chacune isolée pour être testée, remplacée et débuggée INDÉPENDAMMENT. Le E, le T et le L ne se mélangent jamais.** Amener des données brutes et éparses (des CSV, une API) jusqu'à une base propre et interrogeable est un besoin universel. La tentation du débutant est de tout faire dans une grosse fonction ; le pipeline professionnel sépare les étapes, car chacune a des raisons d'échouer et d'évoluer différentes.\n\nLes trois étapes et leur responsabilité stricte :\n1. **Extract** : récupérer les données de chaque source SANS les transformer — lire le CSV, appeler l'API, télécharger. On capture le brut, on gère les erreurs de SOURCE (fichier absent, API en panne), et on s'arrête là. Mélanger extraction et nettoyage empêche de rejouer une transformation sans re-télécharger.\n2. **Transform** : tout le travail EN MÉMOIRE — nettoyer (jour 128), valider (jour 132), normaliser, joindre les sources (jour 131), calculer les colonnes dérivées. C'est le cœur métier, fait de fonctions pures testables (jour 132). Aucune écriture en base ici.\n3. **Load** : écrire le résultat propre dans la cible (SQLite), dans un schéma pensé (jour 134), transactionnellement (jour 136). On sépare l'écriture car la cible peut changer (SQLite → Postgres) sans toucher au reste.\n\nPourquoi cette séparation est décisive : elle permet de TESTER chaque étape isolément (transform sans base ni réseau), de REMPLACER une source ou une cible sans réécrire le pipeline, et de DÉBUGGER en localisant l'étape fautive. C'est l'architecture propre (responsabilités isolées) appliquée aux données — le pendant du découpage stockage/métier/interface du jour 121.\n\nLa notion de STAGING : entre les étapes, on matérialise parfois les données intermédiaires (fichier ou table de staging) pour rejouer une étape sans refaire les précédentes, et pour inspecter ce qui se passe entre E, T et L.\n\nLa règle du jour : **conçois le pipeline en trois étapes strictement séparées (extract lit, transform traite en mémoire, load écrit), chacune testable et remplaçable ; le brut ne se mélange jamais au traitement ni à l'écriture**. Un ETL bien conçu se lit étape par étape et se débogue en localisant laquelle a échoué.`,
    guided: `**Énoncé** : concevoir (papier + code) un pipeline extract (CSV+API) → transform → load (SQLite).\n\n**Exemple simple** : la signature qui matérialise la séparation — \`extract() -> dict[str, DataFrame]\`, \`transform(sources) -> DataFrame\`, \`load(df) -> None\`. Trois fonctions, trois responsabilités ; \`load(transform(extract()))\` compose le tout. Chaque étape se teste seule.\n\n**Raisonnement pas-à-pas** : dessiner le flux (sources → étapes → cible) → une fonction par étape, sans chevauchement → transform en fonctions pures → load transactionnel dans un schéma pensé.\n\n**Conception + code** :\n\`\`\`\nFLUX :  ([CSV ventes] + [API taux])  →  EXTRACT  →  TRANSFORM (nettoie, joint, convertit)  →  LOAD  →  [SQLite]\n\`\`\`\n\`\`\`python\ndef extract() -> dict:                                  # E : lire le BRUT, gérer erreurs de source\n    ventes = pd.read_csv(\"ventes.csv\")\n    taux = requests.get(URL_TAUX, timeout=10).json()    # (try/except sur la source)\n    return {\"ventes\": ventes, \"taux\": taux}\n\ndef transform(sources: dict) -> pd.DataFrame:           # T : tout en MÉMOIRE, fonctions pures\n    df = clean(sources[\"ventes\"])                        # jour 132\n    df = valider(df)\n    df[\"montant_eur\"] = df[\"montant\"] * sources[\"taux\"][\"eur\"]\n    return df                                            # aucune écriture ici\n\ndef load(df: pd.DataFrame, con) -> None:                # L : écrire, transactionnel (jour 136)\n    with con:                                            # transaction implicite\n        df.to_sql(\"ventes\", con, if_exists=\"replace\", index=False)\n\nload(transform(extract()), sqlite3.connect(\"entrepot.db\"))   # composition\n\`\`\`\n\n**Mini-quiz** : (a) Pourquoi ne pas nettoyer pendant l'extraction ? (b) Que contient l'étape transform, jamais ? (c) Pourquoi séparer le load ?\n→ (a) pour pouvoir rejouer une transformation sans re-télécharger, et isoler les erreurs de source des erreurs de traitement ; (b) aucune écriture en base — c'est le rôle du load ; (c) pour changer de cible (SQLite → Postgres) sans toucher au reste, et tester le traitement sans base.\n\n**Variante** : matérialise le résultat de \`extract\` dans un fichier de staging (\`brut.parquet\`) — tu peux alors rejouer \`transform\` autant de fois que tu veux sans rappeler l'API, ce qui accélère énormément le développement.`,
    caseStudy: `L'ETL est l'ossature de toute infrastructure de données : amener des sources hétérogènes (fichiers, API, bases) vers un entrepôt propre est le travail quotidien des data engineers. La séparation extract/transform/load n'est pas académique : elle permet de rejouer une transformation sans re-télécharger (crucial quand une source est lente ou limitée en appels), de remplacer une source ou une cible sans tout réécrire, et de localiser une panne (est-ce l'extraction, le traitement ou l'écriture ?). Les pipelines qui mélangent tout dans une fonction fourre-tout deviennent impossibles à tester, à rejouer et à maintenir. Concevoir un ETL en étapes séparées est une compétence de data engineering fondamentale, directement mise en œuvre dans le projet DataPulse (jours 141-144) et valorisée sur le marché.`,
    interview: `**« Comment conçois-tu un pipeline ETL ? »** → En trois étapes strictement séparées. EXTRACT : lire les sources telles quelles (CSV, API), gérer les erreurs de SOURCE, sans rien transformer — pour pouvoir rejouer la suite sans re-télécharger. TRANSFORM : tout le traitement EN MÉMOIRE (nettoyer, valider, normaliser, joindre, dériver), en fonctions pures testables, sans aucune écriture en base. LOAD : écrire le résultat propre dans la cible, dans un schéma pensé et transactionnellement. Cette séparation permet de tester chaque étape isolément (transform sans base ni réseau), de remplacer une source ou une cible sans réécrire le pipeline, et de localiser une panne à l'étape fautive. C'est l'architecture propre appliquée aux données. J'ajoute souvent un staging entre les étapes pour rejouer une étape sans refaire les précédentes.`,
    future: `- **Bankable** : concevoir un ETL en étapes séparées, testables et remplaçables, est une compétence de data engineering fondamentale — mise en œuvre dans DataPulse et valorisée sur le marché.\n- Cette conception EST le squelette du projet DataPulse (extract/transform/load, jours 142-144).\n- La séparation des responsabilités (E/T/L isolés) est le principe d'architecture propre du jour 121 porté aux données — transférable à tout système de traitement.`,
    solution: {
      logic: "Solution simple : une fonction qui lit, traite et écrit. Solution améliorée : trois étapes strictement séparées — extract (lecture du brut + erreurs de source), transform (traitement en mémoire, fonctions pures testables, aucune écriture), load (écriture transactionnelle dans un schéma pensé) — composées proprement, avec un staging optionnel pour rejouer une étape sans refaire les précédentes. La preuve : chaque étape se teste isolément et une source/cible se remplace sans toucher au reste.",
      pitfalls: ["Mélanger extraction et transformation : impossible de rejouer le traitement sans re-télécharger.", "Écrire en base depuis l'étape transform : couplage à la cible, transform non testable sans base.", "Une fonction fourre-tout E+T+L : intestable, impossible à rejouer partiellement ou à faire évoluer.", "Ne pas gérer les erreurs de source dans extract : une API en panne casse tout le pipeline sans diagnostic clair."],
      checks: ["Extract, transform et load sont trois responsabilités strictement séparées.", "Transform ne fait aucune écriture (fonctions pures en mémoire).", "Load écrit dans un schéma pensé, transactionnellement.", "Chaque étape est testable isolément.", "Une source ou une cible peut être remplacée sans réécrire le reste."],
      oral: "Déroule E/T/L en insistant sur la responsabilité STRICTE de chacune (extract lit, transform traite en mémoire, load écrit) et sur les bénéfices : rejouabilité sans re-télécharger, remplaçabilité des sources/cibles, débogage par localisation. Relier à l'architecture propre du jour 121 (responsabilités isolées) montre que tu vois le principe général derrière l'ETL.",
    },
  },

  // ── ETL : robustesse et rejouabilité ──
  139: {
    lessons: ['etl-pipelines.md'],
    theory: `**Modèle mental : un pipeline ROBUSTE est REJOUABLE (idempotent : le relancer ne duplique ni ne corrompt rien) et SURVIT aux échecs PARTIELS (reprise possible, logs pour diagnostiquer, écriture transactionnelle) — car « ça a tourné une fois sur mon poste » n'est pas « ça tourne en production », où les sources tombent, les jobs s'interrompent et les données arrivent sales.** Un pipeline conçu (jour 138) doit maintenant être fiable dans le monde réel. Les deux propriétés qui le rendent déployable sont l'idempotence et la survie aux échecs.\n\nL'IDEMPOTENCE — pouvoir relancer sans dégât :\n- Le problème : un pipeline qui INSÈRE des lignes à chaque exécution DUPLIQUE les données si on le relance (après un échec, ou pour rattraper). Un pipeline idempotent produit le MÊME état final qu'on le lance une ou dix fois.\n- Les techniques : le \`UPSERT\` (insérer ou mettre à jour selon une clé, \`INSERT ... ON CONFLICT DO UPDATE\`) plutôt qu'un INSERT aveugle ; le remplacement complet d'une partition (\`replace\` d'une table/période) ; une clé naturelle qui identifie une ligne pour éviter les doublons. L'idempotence est ce qui rend un pipeline REJOUABLE en confiance.\n\nLa SURVIE aux échecs partiels :\n- **Les logs** : à chaque étape, journaliser ce qui se passe (combien de lignes extraites, transformées, chargées ; les erreurs). Sans logs, un pipeline qui échoue la nuit est un mystère au matin — les logs sont les traces (jour 274 côté agents) appliquées à l'ETL.\n- **Le chargement transactionnel** (jour 136) : le load réussit ENTIÈREMENT ou est annulé — jamais une base à moitié chargée.\n- **La reprise / le point de contrôle** : pouvoir relancer après un échec sans tout refaire (grâce au staging, jour 138, et à l'idempotence). Un pipeline qui doit tout recommencer depuis zéro à chaque échec est fragile.\n- **La gestion des erreurs de données** : décider quoi faire d'une ligne invalide — la rejeter dans un « bac à rejets » (dead-letter) avec sa raison, plutôt que planter tout le pipeline ou l'ignorer silencieusement.\n\nLa règle du jour : **rends le pipeline idempotent (relançable sans duplication) et résilient (logs à chaque étape, load transactionnel, reprise après échec, rejets tracés) ; conçois pour l'interruption, pas seulement pour le chemin heureux**. Un pipeline de production n'est pas un script qui a marché une fois — c'est un script qui survit à la nuit où la source tombe à mi-parcours.`,
    guided: `**Énoncé** : rendre le pipeline robuste — logs, échec partiel, idempotence — et prouver la survie à une interruption.\n\n**Exemple simple** : le cœur de l'idempotence — l'upsert. \`INSERT INTO ventes(...) VALUES(...) ON CONFLICT(id) DO UPDATE SET ...\`. Relancer le pipeline met à jour les lignes existantes au lieu de les dupliquer. C'est ce qui rend « relancer » sûr.\n\n**Raisonnement pas-à-pas** : rendre le load idempotent (upsert/replace par clé) → journaliser chaque étape → charger transactionnellement → tracer les rejets → prouver en interrompant.\n\n**Implémentation** :\n\`\`\`python\nimport logging\nlogging.basicConfig(level=logging.INFO)\n\ndef run() -> None:\n    sources = extract()\n    logging.info(\"Extract : %d lignes\", len(sources[\"ventes\"]))\n\n    df, rejets = transform_avec_rejets(sources)         # rejets tracés, pas ignorés\n    logging.info(\"Transform : %d valides, %d rejetées\", len(df), len(rejets))\n    if rejets: rejets.to_csv(\"rejets.csv\", index=False)  # dead-letter avec raisons\n\n    with sqlite3.connect(\"entrepot.db\") as con:          # LOAD transactionnel (jour 136)\n        for _, r in df.iterrows():\n            con.execute(\"\"\"INSERT INTO ventes(id, montant) VALUES(?, ?)\n                           ON CONFLICT(id) DO UPDATE SET montant=excluded.montant\"\"\",  # UPSERT idempotent\n                        (r[\"id\"], r[\"montant\"]))\n    logging.info(\"Load : %d lignes chargées (rejouable)\", len(df))\n\`\`\`\nPreuve d'idempotence : lancer \`run()\` deux fois de suite laisse la table dans le MÊME état (pas de doublons) grâce à l'upsert. Preuve de survie : interrompre pendant le load, relancer → la transaction annule le load partiel, et l'upsert évite les doublons au redémarrage.\n\n**Mini-quiz** : (a) Qu'est-ce qu'un pipeline idempotent ? (b) Pourquoi tracer les lignes rejetées plutôt que les ignorer ? (c) À quoi servent les logs par étape ?\n→ (a) le relancer produit le même état final (une ou dix fois) — pas de duplication ; (b) pour ne pas perdre silencieusement des données et pouvoir diagnostiquer/corriger la cause ; (c) diagnostiquer un échec après coup (combien extrait/transformé/chargé, où ça a cassé) — sans eux, un échec nocturne est un mystère.\n\n**Variante** : simule une coupure en levant une exception au milieu du load, puis relance — vérifie qu'il n'y a NI doublon (idempotence) NI chargement partiel (transaction). C'est la preuve concrète que ton pipeline survit à l'interruption.`,
    caseStudy: `La robustesse d'un pipeline est ce qui sépare un script de démo d'un pipeline de production : en réel, les sources tombent, les API expirent, les jobs sont tués, les données arrivent sales — et un pipeline non idempotent qui duplique à chaque relance ou plante sans logs devient un cauchemar opérationnel (données corrompues, débogage à l'aveugle, réconciliations manuelles). Les data engineers conçoivent POUR l'échec : idempotence pour rejouer sans risque, logs pour diagnostiquer, transactions pour ne jamais laisser un état partiel, dead-letter pour tracer les rejets. Cette maturité — « je conçois pour la nuit où la source tombe à mi-parcours » — est exactement ce qui est attendu d'un rôle data engineer, et directement démontrable dans le projet DataPulse (pipeline rejouable en une commande).`,
    interview: `**« Comment rends-tu un pipeline ETL robuste et rejouable ? »** → Deux propriétés. L'IDEMPOTENCE : le relancer produit le même état final, sans duplication — j'utilise des UPSERT (\`ON CONFLICT DO UPDATE\`) ou un remplacement de partition plutôt que des INSERT aveugles, avec une clé qui identifie chaque ligne. C'est ce qui rend « relancer après un échec » sûr. La SURVIE aux échecs partiels : des logs à chaque étape (combien extrait/transformé/chargé, les erreurs) pour diagnostiquer après coup ; un load TRANSACTIONNEL (tout ou rien, jamais une base à moitié chargée) ; la reprise après échec grâce au staging ; et un « bac à rejets » (dead-letter) où j'envoie les lignes invalides avec leur raison, au lieu de planter ou d'ignorer silencieusement. Je conçois pour l'interruption, pas seulement pour le chemin heureux — « ça a tourné une fois » n'est pas « ça tient en production ».`,
    future: `- **Bankable** : concevoir un pipeline idempotent et résilient (logs, transactions, reprise, dead-letter) est exactement ce qu'attend un rôle data engineer — la maturité « je conçois pour l'échec ».\n- Cette robustesse est le livrable du chargement de DataPulse (rejouable en une commande, jour 144) et sa preuve de survie à interruption.\n- Idempotence, journalisation et gestion d'échec partiel sont universelles — elles se réappliquent aux jobs, aux workflows et aux agents (garde-fous, jour 274).`,
    solution: {
      logic: "Solution simple : ajouter des try/except et des prints. Solution améliorée : rendre le load IDEMPOTENT (upsert par clé ou remplacement de partition, pas d'INSERT aveugle), journaliser chaque étape (lignes extraites/transformées/chargées + erreurs), charger transactionnellement (tout ou rien), tracer les lignes invalides dans un dead-letter avec leur raison, permettre la reprise après échec, et PROUVER en interrompant. La preuve : relancer après une coupure ne produit ni doublon ni chargement partiel.",
      pitfalls: ["INSERT aveugle non idempotent : relancer duplique les données — utiliser un upsert ou un remplacement par clé.", "Aucun log : un échec nocturne est indiagnosticable au matin.", "Load non transactionnel : une interruption laisse la base à moitié chargée.", "Ignorer ou planter sur une ligne invalide : perte silencieuse ou pipeline fragile — la router vers un dead-letter avec sa raison."],
      checks: ["Le load est idempotent (relancer ne duplique pas).", "Chaque étape est journalisée (compteurs + erreurs).", "Le chargement est transactionnel (pas d'état partiel).", "Les lignes invalides sont tracées dans un dead-letter avec leur raison.", "Une interruption suivie d'un relancement ne produit ni doublon ni chargement partiel (prouvé)."],
      oral: "Structure autour de deux propriétés : idempotence (upsert → relancer sans dupliquer) et survie aux échecs (logs, load transactionnel, reprise, dead-letter). Martèle « je conçois pour l'interruption, pas pour le chemin heureux ». La preuve par coupure simulée (ni doublon, ni chargement partiel) est la démonstration qui prouve la maturité data engineer.",
    },
  },

  // ── Revue semaine 20 : SQL avancé + ETL ──
  140: {
    reviewSynthese: `La semaine 20 a fait passer SQL du « je sais requêter » au « je sais concevoir et alimenter une base fiable », et posé l'ETL : normalisation en 3NF pour éliminer la redondance et ses anomalies (jour 134), index diagnostiqués à l'\`EXPLAIN\` et mesurés (135), transactions ACID garantissant le tout-ou-rien (136), requêtes analytiques — sous-requêtes, CTE, fonctions fenêtre — pour analyser en base sans écraser les lignes (137), conception d'un pipeline ETL en étapes séparées (138), et robustesse — idempotence, logs, résilience aux échecs partiels (139). Le fil rouge : **une donnée fiable repose sur un schéma sain (chaque fait à un endroit), des accès performants et mesurés (index), des écritures atomiques (transactions), et des pipelines rejouables conçus pour l'échec**. Tu entres dans la semaine 21 (Projet 4 DataPulse) avec tout pour construire un pipeline ETL complet et un tableau de bord.`,
    reviewGrid: `Note chaque item de 0 à 3 (0 = pas fait, 1 = avec aide, 2 = seul, 3 = seul + expliqué à l'oral sans notes) :\n\n| Compétence | /3 |\n|---|---|\n| Concevoir un schéma normalisé (3NF) et justifier chaque relation | |\n| Diagnostiquer une requête lente (\`EXPLAIN\`), poser un index justifié, remesurer | |\n| Écrire une transaction atomique et prouver le rollback | |\n| Répondre à des questions analytiques (sous-requêtes, CTE, fonctions fenêtre) | |\n| Concevoir un pipeline ETL en étapes séparées (extract/transform/load) | |\n| Rendre un pipeline idempotent et résilient (logs, transactions, reprise) | |\n\n**Total /18** : ≥ 15 = solide, attaque DataPulse · 11-14 = revoir les items ≤ 1 · < 11 = rejouer avant le projet, car tu vas tout mobiliser.`,
    remediation: `- **Schéma redondant** (anomalies de mise à jour) : rejoue le jour 134 — normalise en 3NF (« la clé, toute la clé, rien que la clé »), relie par clés étrangères ; teste avec un déménagement de client (une seule ligne à changer).\n- **Optimisation à l'aveugle** : jour 135 — diagnostique avec \`EXPLAIN\`, indexe la colonne réellement filtrée, remesure ; n'indexe jamais par principe (coût en écritures).\n- **Écritures non atomiques** : jour 136 — englobe les écritures liées dans une transaction (commit/rollback), prouve l'atomicité par une erreur provoquée.\n- **Requêtes analytiques laborieuses** : jour 137 — window functions pour rang/cumul/comparaison sans écraser les lignes ; CTE pour la lisibilité.\n- **ETL fourre-tout** : jour 138 — sépare extract/transform/load, transform en fonctions pures sans écriture.\n- **Pipeline fragile** : jour 139 — idempotence (upsert), logs, load transactionnel, dead-letter ; prouve la survie à une interruption.`,
    interview: `1. **« Pourquoi normaliser, et jusqu'où ? »** Attendu : éliminer la redondance et ses anomalies (mise à jour/insertion/suppression) ; 3NF = chaque attribut non-clé dépend de la clé, toute la clé, rien que la clé ; dénormaliser seulement délibérément pour la lecture.\n2. **« Comment optimises-tu une requête lente ? »** Attendu : \`EXPLAIN\` (scan vs index), index sur la colonne filtrée, remesure ; arbitrage lecture/écriture, ne pas sur-indexer.\n3. **« Qu'est-ce qu'une transaction et ACID ? »** Attendu : groupe d'opérations atomique (tout ou rien, commit/rollback) ; ACID ; nécessaire quand plusieurs écritures doivent réussir ensemble.\n4. **« GROUP BY vs fonction fenêtre ? »** Attendu : GROUP BY réduit à une ligne par groupe ; window function calcule sur le groupe sans écraser les lignes (rang, cumul, comparaison).\n5. **« Comment conçois-tu un ETL robuste ? »** Attendu : étapes séparées (extract/transform/load) ; idempotence (upsert), logs, load transactionnel, reprise, dead-letter ; conçu pour l'échec.`,
  },
};
