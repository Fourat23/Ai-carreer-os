// Supplément des jours 31-90 : théorie courte + critères de validation.
// Fusionné par le générateur avec days-31-90.mjs (les champs ici complètent, sans écraser).
// Pour enrichir un jour : édite son entrée puis `npm run generate`.

export const EXTRAS_31_90 = {
  31: {
    theory: `Le pattern « complément » : au lieu de comparer chaque paire (double boucle, O(n²)), on parcourt UNE fois en se demandant à chaque élément « ai-je déjà vu la valeur qui me manque ? ». Une Map mémorise les valeurs vues avec leur index : la question coûte O(1), le total O(n).\n\nCe schéma (mémoriser le vu, interroger le passé) résout toute une famille de problèmes : paires de somme/différence donnée, doublons proches, sous-tableaux de somme cible (via les sommes cumulées : si cumul[j] - cumul[i] = 0, le sous-tableau i..j somme à zéro).\n\nC'est LE réflexe attendu en entretien : dès que tu écris une double boucle de recherche, demande-toi si une Map ne supprime pas la boucle interne.`,
    criteria: ['twoSum en O(n) correct (y compris le cas "aucune paire")', 'Les 3 variantes fonctionnent et sont vérifiées sur des cas construits à la main', 'Benchmark : l\'écart O(n) vs O(n²) est mesuré et conforme sur 100k éléments'],
  },
  32: {
    theory: `Générer toutes les combinaisons revient à explorer un arbre de décisions : pour chaque élément, deux branches — le prendre ou non. 2 choix × n éléments = 2^n feuilles (les sous-ensembles). Pour les permutations : n choix, puis n-1, puis n-2… = n! feuilles.\n\nLe backtracking est la technique d'exploration : on descend dans une branche (on « essaie »), on note le résultat au fond, puis on REMONTE en défaisant le choix (on « backtrack ») pour essayer l'autre branche. Le squelette : choisir → explorer récursivement → défaire.\n\nIci l'exponentiel est inévitable : c'est la TAILLE DU RÉSULTAT qui l'impose, pas ta solution. Savoir distinguer « problème exponentiel » de « solution maladroite » (fib naïf) est une compétence d'analyse.`,
    criteria: ['sousEnsembles([1,2,3]) retourne exactement 8 résultats corrects', 'permutations et combinaisonsDeSomme vérifiées à la main sur de petits cas', 'L\'arbre de décision est dessiné et correspond à l\'exécution'],
  },
  33: {
    theory: `Une Stack (pile) est LIFO : dernier entré, premier sorti — comme une pile d'assiettes. Une Queue (file) est FIFO : premier entré, premier sorti — comme une file d'attente. Les deux offrent des opérations O(1) aux extrémités.\n\nCas d'usage réels : la Stack porte l'historique undo/redo, la call stack de la récursion, la vérification de parenthèses ; la Queue porte les files de traitement, le BFS, les files de messages (mois 10).\n\nEn TypeScript, la classe générique \`Stack<T>\` fonctionne pour tout type sans perdre le typage : c'est exactement le cas d'usage des génériques vus au jour 37.`,
    criteria: ['Stack et Queue génériques compilent en strict et passent des tests manuels', 'parenthesesValides gère (), [], {} imbriqués ET les cas d\'échec (fermante orpheline, ouvrante en trop)', 'Je sais citer un cas d\'usage réel de chaque structure'],
  },
  34: {
    theory: `Une liste chaînée est une suite de nœuds où chacun pointe vers le suivant. Insérer en tête = créer un nœud qui pointe vers l'ancienne tête : O(1), là où un tableau doit tout décaler (O(n)). Mais accéder au n-ième élément exige de suivre n pointeurs : O(n), là où le tableau est O(1).\n\nUn arbre binaire de recherche (BST) range chaque valeur : plus petites à gauche, plus grandes à droite. Chercher = éliminer une moitié à chaque étage (la recherche binaire du jour 16, en structure) : O(hauteur). Le parcours « in-order » (gauche, nœud, droite) ressort les valeurs TRIÉES — c'est la propriété qui rend les BST utiles.\n\nPiège célèbre : insérer des valeurs déjà triées dégénère le BST en liste chaînée (O(n)). Les arbres auto-équilibrés (AVL, rouge-noir — à connaître de nom) corrigent ça.`,
    criteria: ['LinkedList : les 4 opérations correctes, y compris sur liste vide', 'BST : inorder ressort les valeurs triées (prouvé sur 20 insertions aléatoires)', 'Le benchmark tête-de-liste vs unshift montre l\'écart attendu et je sais l\'expliquer'],
  },
  36: {
    theory: `TypeScript ajoute un vérificateur de types AU-DESSUS de JavaScript : le code s'exécute pareil, mais un compilateur (tsc) relit tout avant et signale les incohérences (appeler une fonction avec le mauvais type, accéder à une propriété qui n'existe pas, oublier un cas null).\n\nLe typage est une DOCUMENTATION VÉRIFIÉE : la signature \`function payer(montant: number): Recu\` dit le contrat, et le compilateur garantit qu'il est respecté partout, pour toujours. En mode strict, les null/undefined implicites deviennent des erreurs : la moitié des bugs du mois 1 auraient été attrapés à la compilation.\n\n\`any\` désactive la vérification (à bannir) ; \`unknown\` dit « je ne sais pas encore » et FORCE une validation avant usage — c'est la bonne réponse aux données externes.`,
    criteria: ['Les 3 fichiers convertis compilent en strict, zéro any', 'Au moins un bug réel révélé par le typage est documenté dans les notes', 'Je sais expliquer any vs unknown avec un exemple'],
  },
  37: {
    theory: `Les types avancés font du compilateur un garde du corps : une union littérale \`type Statut = 'panier' | 'payee' | 'expediee'\` transforme une simple string en contrat — la typo 'payé' devient une erreur de compilation, et un switch sur le statut peut être vérifié exhaustif.\n\n\`interface\` décrit la forme d'un objet (extensible), \`type\` crée des alias et des unions. Le narrowing : TypeScript AFFINE le type dans les branches (\`if (typeof x === 'string')\` → x est string dedans).\n\nLes génériques \`<T>\` écrivent UNE logique valable pour tous les types sans perdre le typage : \`premier<T>(arr: T[], p: (x: T) => boolean)\` fonctionne sur des nombres comme des Commandes, et le résultat est du bon type. C'est le mécanisme derrière Array, Map, Promise.`,
    criteria: ['Le modèle de commande compile et le statut est une union littérale (typo impossible)', 'La fonction générique premier<T> est utilisée sur 2 types différents avec inférence correcte', 'Aucun any ; les cas optionnels sont explicites (?)'],
  },
  38: {
    theory: `La POO regroupe données + comportements dans des objets. Les 4 piliers : encapsulation (cacher l'état interne — \`private\`), abstraction (exposer un contrat simple), héritage (une classe en étend une autre — à utiliser avec parcimonie), polymorphisme (traiter des objets différents via un contrat commun).\n\nLe polymorphisme par interface est le pilier le plus utile : \`alerter(notifieurs: Notifieur[])\` ne connaît que le contrat \`envoyer()\` — ajouter Slack demain = une classe de plus, ZÉRO modification d'alerter. C'est le principe open/closed, et le germe de l'inversion de dépendance (ton interface Store au jour 44).\n\nLa règle moderne : composition > héritage. Un héritage profond (A extends B extends C) est fragile ; assembler des comportements est plus souple. Et une fonction pure + closure fait souvent le même travail qu'une petite classe : compare honnêtement.`,
    criteria: ['Les deux versions (POO et fonctionnelle) fonctionnent et sont comparées par écrit', 'Ajouter un 4e notifieur ne modifie AUCUN code existant (prouvé)', 'Je sais dire quand une classe se justifie face à une fonction + closure'],
  },
  39: {
    theory: `Un design pattern est une solution NOMMÉE à un problème récurrent — un vocabulaire partagé entre ingénieurs. Strategy : injecter un comportement interchangeable (tes callbacks du jour 22 en sont). Factory : centraliser la création d'objets (créer le bon Notifieur selon une config). Adapter : uniformiser deux interfaces incompatibles (ton module api.ts). Observer : notifier des abonnés d'un changement (les événements du DOM). Singleton : une instance unique (la connexion DB — mais attention, c'est un état global déguisé).\n\nLe piège : appliquer un pattern sans avoir le problème correspondant. La sur-ingénierie EST un anti-pattern. Le bon usage : reconnaître le problème quand il arrive, et nommer la solution qu'on a souvent déjà réinventée.`,
    criteria: ['Les 5 patterns implémentés EN CONTEXTE de ton propre code (pas des exemples jouets copiés)', 'Pour chaque pattern : le problème qu\'il résout est écrit en une phrase', 'Au moins un pattern retrouvé dans ton code du mois 1 et nommé'],
  },
  40: {
    theory: `Le clean code repose sur un fait : le code est LU 10 fois plus qu'il n'est écrit. Optimiser la lecture est donc rentable. Les leviers principaux : des noms qui révèlent l'INTENTION (calculerRemiseFidelite > calcRem > x), des fonctions courtes à UNE responsabilité, un niveau d'abstraction cohérent par fonction (ne pas mélanger logique métier et détails techniques dans le même bloc).\n\nLes commentaires : ils doivent dire POURQUOI (la décision, la contrainte invisible), jamais QUOI (la ligne suivante se lit toute seule). Un commentaire qui paraphrase le code est du bruit qui se périme.\n\nDRY (Don't Repeat Yourself) avec discernement : factoriser deux duplications identiques oui, mais forcer une abstraction sur deux choses juste RESSEMBLANTES crée un couplage pire que la duplication.`,
    criteria: ['Ta checklist de 10 code smells est écrite AVANT consultation de ressources', 'Un fichier du mois 1 refactoré avec diff avant/après commenté', 'Chaque renommage/découpage du refactor est justifiable en une phrase'],
  },
  41: {
    theory: `Le debugging méthodique remplace la loterie par un processus : (1) REPRODUIRE de façon fiable — sans ça, impossible de valider un fix ; (2) LIRE le message d'erreur en entier — il donne souvent ligne et cause ; (3) ISOLER par bissection — couper le code en deux, vérifier au milieu, éliminer une moitié (la recherche binaire appliquée au debug) ; (4) formuler UNE hypothèse falsifiable ; (5) la PROUVER par une observation (log, breakpoint) avant de corriger ; (6) corriger la CAUSE, pas le symptôme, puis re-reproduire.\n\nLes familles de bugs à connaître : référence partagée (deux choses changent ensemble), off-by-one (bornes de boucle), type inattendu (NaN qui se propage), async (l'ordre d'exécution n'est pas celui du code), état périmé. Chaque famille a sa signature — les reconnaître accélère le diagnostic.`,
    criteria: ['Les 5 bugs trouvés avec, pour chacun : symptôme → hypothèse → preuve → correction documentés', 'Aucune correction "au hasard" (chaque fix découle d\'une hypothèse prouvée)', 'Ta méthode personnelle de debug est écrite et affichée près de ton écran'],
  },
  43: {
    theory: `Typer les fonctions d'ordre supérieur révèle leur logique profonde : \`compose<A, B, C>(f: (b: B) => C, g: (a: A) => B): (a: A) => C\` dit exactement la contrainte de composition — la sortie de g doit nourrir l'entrée de f. Si les types ne s'emboîtent pas, la composition n'a pas de sens, et le compilateur le dit AVANT l'exécution.\n\n\`readonly\` et \`as const\` font vérifier l'immutabilité (jour 26) par le compilateur : muter un \`readonly string[]\` est une erreur de compilation. La discipline devient un contrat outillé.\n\nC'est le pattern général de TypeScript : transformer tes conventions (pureté, immutabilité, contrats) en règles VÉRIFIÉES mécaniquement.`,
    criteria: ['compose et pipe typés génériquement, utilisables sur une chaîne de 3 fonctions sans perte de type', 'memoriser typé fonctionne sur 2 types de fonctions différents', 'Le pipeline de données est 100% pur et le compilateur interdit la mutation (readonly prouvé)'],
  },
  44: {
    theory: `Concevoir les types AVANT le code force à comprendre le domaine : quelles entités (Task), quels états possibles (union littérale de statuts), quelles opérations (le contrat Store). Un modèle clair rend l'implémentation évidente ; un modèle flou la rend tortueuse.\n\nL'interface \`Store\` est ta première INVERSION DE DÉPENDANCE consciente : la logique métier dépend d'un CONTRAT (save/load/all), pas d'une implémentation (JSON, SQLite). Le jour où le stockage change, un seul fichier change. C'est le principe qui structurera l'architecture hexagonale du mois 10 — à petite échelle dès aujourd'hui.\n\nLe backlog en jalons DÉMONTRABLES (« add + list fonctionnent ») plutôt qu'en activités floues (« avancer sur le CLI ») rend le progrès mesurable et la procrastination visible.`,
    criteria: ['types.ts complet : Task, statuts en union littérale, interface Store', 'Le backlog contient uniquement des jalons démontrables', 'Réponse écrite : combien de fichiers changent si on migre vers SQLite ? (attendu : un)'],
  },
  45: {
    theory: `Un projet réel commence par le squelette qui MARCHE : un CLI qui répond « commande inconnue » est un meilleur point de départ qu'une fonctionnalité parfaite dans un programme qui ne se lance pas — la boucle de feedback (coder → lancer → constater) doit rester sous 10 secondes.\n\nLa persistance robuste recycle le pattern du jour 12 : charger() qui distingue « fichier absent = premier lancement » (normal) de « JSON corrompu » (on s'arrête sans écraser). Les ids en max+1 (jamais length+1 : bug de doublon après suppression).\n\nLes commits atomiques par fonctionnalité racontent la construction — un recruteur qui lit ton historique doit voir une progression méthodique.`,
    criteria: ['add et list fonctionnent de bout en bout avec persistance', 'Les 3 cas de fichier gérés (absent, valide, corrompu) — testés un par un', 'Historique : au moins 2 commits atomiques avec messages clairs'],
  },
  46: {
    theory: `Le CRUD complet d'un vrai outil gère l'ÉTAT en style immuable : done/edit ne modifient pas la tâche, ils retournent une copie modifiée que le Store persiste (ton pattern mettreAJourParId du jour 26). Bénéfice concret : chaque opération est une fonction pure testable sans fichiers.\n\nLes filtres combinables (--done --priority high) reprennent le pattern « filtre conditionnel » du jour 23 : chaque critère absent laisse tout passer. Attention au piège falsy : un filtre « priorité 0 » ou une chaîne vide ne doit pas être confondu avec « pas de filtre ».\n\nLa validation aux frontières : tout argument CLI est hostile jusqu'à preuve du contraire (id non numérique, priorité inconnue → message clair, exit 1).`,
    criteria: ['done/rm/edit fonctionnent, mises à jour immuables (l\'original n\'est jamais muté)', 'Les filtres se combinent correctement (testé avec 2 filtres simultanés)', 'Tout id invalide ou inconnu → message clair, jamais de stack trace'],
  },
  47: {
    theory: `La dernière ligne droite d'un projet est ce qui sépare « ça marche » de « c'est fini » : les stats (un reduce par statut/priorité), le verrouillage des cas d'erreur UN PAR UN, le README qui rend le projet utilisable par un inconnu en 5 minutes, et l'ADR qui documente ta décision de stockage.\n\nUn ADR (Architecture Decision Record) capture : le contexte, les options considérées, la décision, ses conséquences, et ce qui te ferait changer d'avis. C'est un document d'une page qui vaut de l'or en entretien : il PROUVE que tu arbitres consciemment au lieu de suivre des recettes.\n\nLa section « Ce que j'ai appris » du README : 3-5 puces honnêtes. Les recruteurs la lisent — l'honnêteté et la lucidité y sont des signaux forts.`,
    criteria: ['TaskFlow complet : toutes les commandes de la spec passent', 'README : un inconnu peut installer, utiliser et comprendre l\'architecture en 5 min', 'ADR n°1 écrit au format complet (contexte/options/décision/conséquences/révision)'],
  },
  48: {
    theory: `Le rappel actif sous chrono est le meilleur diagnostic de ce qui est ANCRÉ (disponible sans aide) versus ce qui est seulement « vu ». Le format simule l'entretien : temps limité, pas de documentation, et — le plus dur — VERBALISER en codant.\n\nParler à voix haute en résolvant est une compétence à part entière : le recruteur évalue ta démarche (reformuler, exemples, plan, code, vérification) plus que ta solution. Le silence est l'erreur n°1 en entretien technique ; elle se corrige par l'entraînement enregistré.\n\nCompare tes temps à ceux du jour 20 : l'écart mesure ta consolidation réelle. Les katas ratés d'aujourd'hui sont ton programme de révision de la semaine.`,
    criteria: ['6 katas tentés sans aide, temps notés, honnêteté totale', 'L\'exercice à voix haute est enregistré et réécouté avec la grille d\'entretien', 'Plan de rattrapage écrit pour les katas ratés'],
  },
  50: {
    theory: `HTTP est un protocole texte requête/réponse SANS ÉTAT : chaque requête est autonome et contient tout (méthode, URL, headers, corps). Le serveur répond un statut + headers + corps, et oublie tout.\n\nLes méthodes portent une SÉMANTIQUE : GET lit (sans effet de bord — jamais de modification sur un GET), POST crée, PUT remplace, PATCH modifie partiellement, DELETE supprime. GET/PUT/DELETE sont IDEMPOTENTES : les rejouer ne change rien de plus — propriété cruciale pour les retries réseau.\n\nLes statuts sont un langage : 2xx succès (200 ok, 201 créé, 204 sans contenu), 3xx redirection, 4xx erreur du CLIENT (400 requête invalide, 401 non authentifié, 403 interdit, 404 absent, 409 conflit), 5xx erreur du SERVEUR. Choisir le bon statut EST de la conception d'API.`,
    criteria: ['Exploration curl complète (GET, POST JSON, headers, redirection) sans notes', '8 statuts documentés avec leur cas d\'usage précis', 'Collection Postman organisée et versionnée dans Git'],
  },
  51: {
    theory: `REST modélise l'API en RESSOURCES (des noms au pluriel : /livres, /livres/42) manipulées par les verbes HTTP. La règle d'or : l'URL dit QUOI, le verbe dit COMMENT — jamais de verbe dans l'URL (/getLivres est un anti-pattern).\n\nUne bonne API est PRÉVISIBLE : qui a vu GET /livres devine POST /livres (créer), DELETE /livres/42 (supprimer), GET /livres/42/emprunts (sous-ressource). La cohérence des statuts complète le contrat : 201 + l'objet créé sur un POST réussi, 204 sur un DELETE, 404 sur un id inconnu, 400 avec les détails sur une entrée invalide.\n\nPagination, filtres et recherche passent en query string (?page=2&genre=sf) : ils raffinent la ressource, ils ne la changent pas.`,
    criteria: ['Le contrat couvre toutes les ressources avec verbes et statuts corrects', 'Aucun verbe dans les URLs, pluriels cohérents', 'Les cas d\'erreur sont spécifiés (pas seulement les succès)'],
  },
  52: {
    theory: `Sous Express il y a le module http de Node : un serveur = une fonction qui reçoit (req, res) et ÉCRIT une réponse. Tout ce qu'Express automatise, tu peux le faire à la main : router (un switch sur method + url), parser le corps (accumuler les chunks du flux entrant), répondre (statut + headers + res.end).\n\nL'exercice du jour a un but précis : éprouver la FRICTION. Parser le JSON à la main, router à la main, gérer les erreurs à la main — pour que demain, chaque facilité d'Express (express.json(), app.get, les middlewares) soit comprise comme une automatisation de quelque chose que tu SAIS faire, pas comme de la magie.\n\nC'est un principe d'apprentissage général : une couche basse comprise une fois = toutes les abstractions au-dessus démystifiées.`,
    criteria: ['Le serveur natif répond correctement à GET /ping et POST /echo', 'Le corps du POST est parsé chunk par chunk (compris, pas copié)', 'La liste de frictions est écrite : chaque friction est associée à ce qu\'Express automatise'],
  },
  53: {
    theory: `Un middleware Express est une fonction (req, res, next) dans une CHAÎNE : chaque maillon traite (log, auth, parsing, validation) puis passe la main avec next(), ou court-circuite en répondant. Comprendre la chaîne = comprendre Express : l'ordre de déclaration EST l'ordre d'exécution.\n\nLa structure en 3 couches sépare les responsabilités : routes (traduire HTTP ↔ appels de fonctions), services (la logique métier, PURE autant que possible — testable sans serveur), data (la persistance). Chaque couche ne connaît que celle du dessous.\n\nBénéfice immédiat : la logique métier dans les services se teste comme n'importe quelle fonction (jour 62). La logique enfouie dans les routes est intestable sans lancer un serveur — c'est LE code smell des APIs de débutant.`,
    criteria: ['CRUD citations complet, testé via Postman', 'Structure en 3 couches réelle : aucune logique métier dans les routes', 'Middleware de log écrit maison (méthode, chemin, statut, durée)'],
  },
  54: {
    theory: `Une API robuste part d'un principe : TOUTE entrée est hostile jusqu'à validation. Chaque champ est vérifié (présence, type, bornes, format) à l'entrée, et le refus est précis : 400 avec la liste des problèmes (ton pattern collecteur du jour 5).\n\nLa gestion d'erreurs se CENTRALISE : un middleware d'erreurs final attrape tout, distingue l'erreur OPÉRATIONNELLE (attendue : ressource absente → 404, entrée invalide → 400, on informe le client) du BUG (inattendu : on log les détails en interne, on répond un 500 générique — les détails internes ne fuient JAMAIS au client, c'est une règle de sécurité).\n\nLe test d'une API robuste : l'attaquer soi-même. Dix requêtes malveillantes (types faux, champs manquants, ids absurdes, corps géant, JSON invalide) — chacune doit recevoir une réponse propre, jamais un crash ni une stack trace.`,
    criteria: ['Les 10 requêtes malveillantes reçoivent chacune une réponse correcte (statut + message utile)', 'Le middleware d\'erreurs central distingue 400/404 (informatif) de 500 (générique, loggé en interne)', 'Le process ne crashe sur AUCUNE entrée'],
  },
  55: {
    theory: `SQL est déclaratif : tu décris le résultat voulu, le moteur trouve comment. SELECT choisit les colonnes, WHERE filtre les lignes, ORDER BY trie, LIMIT découpe — c'est ton pipeline filter/sort/slice du mois 1, en langage de base de données.\n\nJOIN relie les tables par leurs clés : « chaque emprunt avec son livre et son membre » = deux JOIN sur les clés étrangères. INNER JOIN garde les correspondances, LEFT JOIN garde aussi les orphelins de gauche (les livres jamais empruntés).\n\nGROUP BY regroupe pour agréger (COUNT, SUM, AVG) — ton geste « regrouper » du jour 11. La nuance d'entretien : WHERE filtre AVANT le regroupement (les lignes), HAVING filtre APRÈS (les groupes). Et les requêtes PARAMÉTRÉES (?) sont non négociables : elles seront ta défense contre l'injection (jour 57).`,
    criteria: ['10/10 requêtes correctes, résultats vérifiés à la main sur la base de test', 'Au moins un JOIN à 3 tables et un GROUP BY avec HAVING fonctionnels', 'La correspondance SQL ↔ map/filter/reduce est écrite pour 2 requêtes'],
  },
  57: {
    theory: `L'injection SQL est la faille historique n°1 : si tu CONCATÈNES l'entrée utilisateur dans ta requête (\`"SELECT * WHERE nom = '" + input + "'"\`), un input malin (\`' OR 1=1 --\`) devient du code SQL exécuté. Vol de données, destruction de tables : tout devient possible.\n\nLa défense est simple et absolue : les requêtes PARAMÉTRÉES. \`SELECT * WHERE nom = ?\` + la valeur passée séparément — le moteur traite l'entrée comme une DONNÉE, jamais comme du code. Règle à vie : zéro concaténation de SQL, sans exception.\n\nLa couche data isolée (un module qui contient TOUT le SQL) prépare la suite : le jour où SQLite devient PostgreSQL, un seul fichier change. C'est ton interface Store du projet 1, appliquée à une vraie base.`,
    criteria: ['L\'API fonctionne sur SQLite avec schéma versionné (init.sql)', '100% des requêtes paramétrées (vérifié par relecture : zéro concaténation)', 'Une tentative d\'injection documentée qui ÉCHOUE (preuve à l\'appui)'],
  },
  58: {
    theory: `La normalisation élimine la redondance : chaque fait vit à UN endroit. 1NF : des valeurs atomiques (pas de listes dans une cellule). 2NF/3NF : chaque colonne dépend de la clé, toute la clé, rien que la clé. Une donnée dupliquée finit toujours désynchronisée — c'est la règle « une source de vérité » du jour 10, au niveau base.\n\nUn INDEX est une structure auxiliaire (un arbre) qui accélère les recherches sur une colonne : O(log n) au lieu du parcours complet. Son coût : de l'espace et un ralentissement des ÉCRITURES (l'index se met à jour). D'où la règle : indexer les colonnes filtrées/jointes souvent, pas tout.\n\nUne TRANSACTION rend un groupe d'opérations atomique : tout réussit ou tout est annulé (rollback). ACID : Atomicité, Cohérence, Isolation, Durabilité. Sans transaction, une panne entre « créer la commande » et « décrémenter le stock » laisse la base incohérente.`,
    criteria: ['Schéma 3NF avec relations justifiées (validé contre la solution)', 'Chaque index posé a une justification écrite (quelle requête il sert)', 'La transaction testée : une interruption simulée déclenche bien le rollback (preuve)'],
  },
  59: {
    theory: `Une collection Postman bien construite est trois choses à la fois : une DOCUMENTATION vivante (chaque requête montre l'usage réel), une SUITE DE TESTS (les assertions vérifient statut et forme de réponse à chaque exécution), et un OUTIL DE DÉMO (un recruteur importe, lance, tout passe).\n\nLes variables d'environnement ({{baseUrl}}, {{token}}) rendent la collection portable : même collection en local et en prod. Les tests s'écrivent en JS dans l'onglet Tests : vérifier le statut ne suffit pas — vérifie aussi la FORME du corps (les champs attendus existent, les types sont bons).\n\nLe scénario enchaîné (create → read → update → delete, en passant l'id créé aux requêtes suivantes via les variables) teste un FLUX réel, pas des endpoints isolés.`,
    criteria: ['Collection avec variables d\'environnement, exportée et versionnée dans Git', 'Chaque requête a des tests (statut ET forme du corps)', 'Le scénario CRUD enchaîné passe de bout en bout en un clic (Run collection)'],
  },
  60: {
    theory: `Le cadrage d'un projet backend commence par les DONNÉES : un mauvais schéma condamne l'API (requêtes tortueuses, incohérences, migrations douloureuses). Les emprunts sont une table de LIAISON : elle relie livre + membre et porte ses propres données (dates). C'est le pattern à reconnaître partout (inscriptions, participations, commandes).\n\nLe CONTRAT d'API s'écrit AVANT le code : endpoints, verbes, statuts, corps — c'est une promesse que Postman vérifiera. Coder d'abord et documenter ensuite produit des APIs incohérentes ; promettre d'abord force la cohérence.\n\nQuestion de robustesse du modèle à te poser systématiquement : « et si un livre a plusieurs auteurs ? » — si la réponse casse ton schéma, il faut une table de liaison de plus. Anticiper UNE évolution probable est du bon design ; en anticiper dix est de la sur-ingénierie.`,
    criteria: ['Schéma complet avec la table de liaison emprunts (clés étrangères, dates)', 'Contrat d\'API écrit avec statuts (dont le 409 du double emprunt)', 'Backlog de la semaine en jalons démontrables'],
  },
  61: {
    theory: `L'implémentation d'un socle suit un ordre précis : le schéma d'abord (init.sql exécutable — la base se reconstruit d'une commande), la couche data ensuite (toutes les requêtes paramétrées, isolées dans leur module), les routes enfin (traduction HTTP pure). Chaque étage se teste avant de monter le suivant.\n\nLes statuts se choisissent avec précision dès maintenant : 201 + l'objet créé (avec son id) sur un POST réussi, 200 + l'objet sur un GET, 404 net sur un id inconnu, 400 détaillé sur une entrée invalide. Les corriger après coup casse les clients — les choisir bien d'emblée est gratuit.\n\nLe réflexe pro : après chaque endpoint, LE tester immédiatement dans Postman (cas nominal + un cas d'erreur), pas « à la fin ».`,
    criteria: ['CRUD livres complet sur SQLite, chaque endpoint testé via Postman', 'Statuts corrects partout (201/200/204/400/404)', 'init.sql reconstruit la base de zéro en une commande'],
  },
  62: {
    theory: `La logique MÉTIER est ce qui distingue une vraie API d'un CRUD généré : « on ne peut emprunter qu'un livre disponible » est une RÈGLE, et les règles vivent dans la couche service (pas dans les routes, pas dans la base seule).\n\nL'emprunt est une opération à étapes multiples (vérifier la disponibilité → créer l'emprunt → marquer indisponible) : c'est exactement le cas d'usage des TRANSACTIONS du jour 58. Sans elle, une panne au milieu laisse un livre fantôme. Le conflit métier (déjà emprunté) mérite son statut précis : 409 Conflict — pas 400, pas 500.\n\nLes JOIN entrent dans l'API : GET /livres/42 ramène le livre AVEC son auteur (un JOIN), pas un id opaque que le client devrait résoudre par une seconde requête.`,
    criteria: ['Le double emprunt est refusé avec un 409 (testé)', 'L\'opération emprunt est transactionnelle (interruption simulée → rien n\'est à moitié fait)', 'La règle métier vit dans le service (pas dans la route) — vérifiable en lisant le code'],
  },
  64: {
    theory: `La recherche s'implémente avec LIKE (\`WHERE titre LIKE '%terme%'\`) — attention à la casse selon le moteur, et toujours en requête paramétrée (le % se concatène à la VALEUR, pas au SQL).\n\nLa pagination standard : LIMIT (taille de page) + OFFSET (décalage). Sa limite cachée : OFFSET 100000 force le moteur à LIRE et jeter 100 000 lignes — sur de gros volumes, on pagine par CURSEUR (WHERE id > dernierIdVu LIMIT 20). Connaître cette limite est une question d'entretien classique.\n\nLes filtres combinables reprennent ton pattern du jour 23 côté SQL : chaque filtre absent ne contraint pas la requête. Et les colonnes cherchées/filtrées fréquemment méritent leur index (jour 58) — mesure avant/après pour le prouver.`,
    criteria: ['Recherche par titre/auteur fonctionnelle et insensible à la casse (choix documenté)', 'Pagination testée (page 1, page du milieu, page au-delà de la fin)', 'Filtres combinables corrects + un index justifié par une mesure'],
  },
  65: {
    theory: `Un test d'INTÉGRATION vérifie le système assemblé : une vraie requête HTTP traverse routes → services → base, et on vérifie la réponse ET l'état de la base. Complémentaire du test unitaire (une fonction isolée) : l'unitaire localise précisément les bugs de logique, l'intégration attrape les bugs de CÂBLAGE (mauvaise route, mauvaise colonne, transaction oubliée).\n\nLa règle d'or : une base de TEST isolée, réinitialisée avant chaque suite (jamais la base de dev — les tests doivent être rejouables à l'infini et indépendants de l'ordre).\n\nQue tester en priorité : les RÈGLES MÉTIER (le double emprunt → 409), les cas d'erreur (id inconnu, entrée invalide), et un flux complet (créer → emprunter → rendre). Les chemins heureux triviaux (GET qui liste) rapportent moins par test.`,
    criteria: ['Suite d\'intégration verte sur base de test isolée et réinitialisée', 'Les règles d\'emprunt testées (dont le 409)', 'Les tests passent deux fois de suite sans nettoyage manuel (rejouables)'],
  },
  66: {
    theory: `La documentation d'une API a un critère unique : un inconnu peut-il la tester en 5 minutes ? Le README répond dans l'ordre aux questions du lecteur : c'est quoi → comment lancer → comment tester (importer la collection) → comment c'est construit (architecture) → quelles limites.\n\nLa démo enregistrée (2 min, écran + voix) est un multiplicateur : elle prouve que ça marche sans rien installer, et elle t'entraîne à PRÉSENTER — la compétence du mois 12, commencée maintenant. Règle : scénario écrit, une répétition, une prise.\n\nL'ADR n°2 (validation à la main vs librairie) capture un arbitrage réel : à ta taille, la validation manuelle est pédagogique et suffisante ; en équipe sur 30 endpoints, une librairie de schémas gagne. Savoir dire OÙ la bascule se produit, c'est exactement ce qu'on attend d'un ingénieur.`,
    criteria: ['README complet testé (suivre ses propres instructions sur un clone frais)', 'Collection Postman exportée + démo 2 min enregistrée', 'ADR n°2 écrit avec le point de bascule explicite'],
  },
  67: {
    theory: `L'OWASP Top 10 recense les failles web les plus exploitées. Les 4 qui te concernent immédiatement : l'INJECTION (SQL — ta défense : le paramétrage), l'AUTH CASSÉE (tokens absents/faibles/mal vérifiés), l'EXPOSITION DE DONNÉES (renvoyer trop : mots de passe hashés, emails d'autres utilisateurs, stack traces), et la MAUVAISE CONFIGURATION (secrets commités, CORS en *, messages d'erreur bavards).\n\nLa posture qui change tout : ATTAQUER son propre système. Tu ne sais pas si ton API est sûre tant que tu n'as pas essayé de la casser — c'est l'état d'esprit que tu appliqueras aux systèmes LLM au mois 9 (prompt injection).\n\nLe rate limiting (limiter les requêtes par client/minute) protège contre la force brute et l'abus : une version simple (compteur en mémoire par IP) suffit à comprendre le principe.`,
    criteria: ['Audit des 4 familles de failles fait sur TES deux APIs, avec preuves', '3 failles réelles trouvées et corrigées (avant/après documenté)', 'Rate limiting fonctionnel (testé en dépassant la limite)'],
  },
  68: {
    theory: `L'authentification par token : le client envoie un secret dans le header \`Authorization\` à chaque requête ; un middleware le vérifie AVANT les routes protégées. Pourquoi le header et pas l'URL ? Les URLs sont loggées partout (serveurs, proxies, historiques) — un secret dans l'URL est un secret publié.\n\nLes secrets vivent dans l'ENVIRONNEMENT (.env, jamais commité — vérifie ton .gitignore) : le code lit process.env.API_TOKEN. Le fichier .env.example (committé, sans les valeurs) documente les variables attendues.\n\nCette hygiène prépare le mois 8 : tes clés d'API LLM suivront exactement le même circuit. Un secret commité une fois est compromis pour toujours (l'historique Git n'oublie pas) — d'où l'audit systématique avant tout push public.`,
    criteria: ['Routes d\'écriture protégées : sans token → 401, mauvais token → 401, bon token → succès (les 3 testés)', 'Secrets en .env, .env dans .gitignore, .env.example committé', 'Audit de l\'historique Git : aucun secret jamais commité'],
  },
  69: {
    theory: `Refactorer = améliorer la STRUCTURE sans changer le COMPORTEMENT. La garantie que le comportement n'a pas changé, ce sont les tests : d'abord verrouiller par des tests ce qui marche, ensuite seulement restructurer, en relançant les tests à chaque étape. Refactorer sans filet, c'est casser en croyant améliorer.\n\nLa méthode : par PETITES étapes (extraire une fonction, déplacer un module, renommer) — jamais de « grande réécriture » qui te laisse trois jours avec un système cassé. Chaque étape laisse le système fonctionnel et committable.\n\nLa dette technique se documente : ce qui reste imparfait, pourquoi c'est accepté, et le signal qui obligera à payer la dette. Une dette CONSCIENTE est une décision de gestion ; une dette ignorée est une bombe.`,
    criteria: ['Le refactor est couvert par des tests écrits AVANT (et toujours verts après)', 'Historique : plusieurs petites étapes commitées, jamais un big-bang', 'La dette restante est documentée (quoi, pourquoi accepté, signal de paiement)'],
  },
  71: {
    theory: `Sous HTTP il y a une pile : DNS traduit le nom de domaine en adresse IP (annuaire distribué, avec caches à chaque étage) ; TCP établit une connexion fiable (le « handshake » en 3 temps, la retransmission des paquets perdus) ; TLS chiffre le canal (HTTPS = HTTP dans TLS) — il garantit la CONFIDENTIALITÉ (personne ne lit) et l'INTÉGRITÉ (personne ne modifie), via les certificats qui prouvent l'identité du serveur.\n\nLa distinction clé en performance : LATENCE (le temps d'un aller-retour — incompressible, dictée par la distance) vs BANDE PASSANTE (le débit). Chaque connexion coûte des allers-retours (DNS + TCP + TLS) avant le premier octet utile — d'où les connexions réutilisées et HTTP/2 qui multiplexe plusieurs requêtes sur une connexion.\n\n« Que se passe-t-il quand tu tapes une URL ? » est LA question d'entretien système : ta réponse doit dérouler cette pile, dans l'ordre, en 3-5 minutes.`,
    criteria: ['Le trajet complet tracé avec de vrais outils (dig, curl -v) et documenté', 'Schéma légendé DNS → TCP → TLS → HTTP correct', 'Réponse orale de 3-5 min à « que se passe-t-il quand on tape une URL » (enregistrée)'],
  },
  72: {
    theory: `Un script shell encode un savoir-faire répétable : la séquence de commandes que tu tapes à la main devient un outil fiable. Les fondamentaux : \`set -e\` (le script s'arrête à la première erreur au lieu de continuer aveuglément), les codes de sortie vérifiés, les messages d'étape clairs.\n\nLes PERMISSIONS Unix (lecture/écriture/exécution × propriétaire/groupe/autres) : \`chmod +x script.sh\` rend exécutable. Les VARIABLES D'ENVIRONNEMENT sont l'interface de configuration universelle : un script qui lit $DB_PATH tourne partout sans modification — c'est exactement le contrat que la CI et Docker (mois 11) attendent.\n\nLes PROCESSUS : ps liste, kill termine proprement (SIGTERM) ou brutalement (SIGKILL), & lance en arrière-plan. Comprendre « mon serveur est un processus qui écoute un port » démystifie la moitié des problèmes de déploiement.`,
    criteria: ['setup.sh lance un projet complet de zéro (install + init + démarrage) avec set -e', 'Le script lit sa config depuis l\'environnement (aucune valeur en dur)', 'Une erreur simulée (dépendance manquante) arrête le script avec un message clair'],
  },
  73: {
    theory: `Le rebase RÉÉCRIT l'historique : au lieu de fusionner deux lignes divergentes (merge, qui crée un losange), il REJOUE tes commits par-dessus la base à jour — historique linéaire, lisible. Le rebase interactif (squash, reword, drop) nettoie une branche de travail : cinq commits « wip » deviennent deux commits cohérents et bien nommés.\n\nLA règle de sécurité absolue : on ne rebase JAMAIS des commits déjà poussés/partagés — réécrire une histoire que d'autres ont déjà, c'est corrompre leurs dépôts. Rebase = avant de partager ; merge = après.\n\nUne pull request raconte le POURQUOI : contexte, ce qui change, comment vérifier. Même en solo, rédiger des descriptions de PR t'entraîne au standard des équipes — et ton futur reviewer te remerciera d'un historique propre.`,
    criteria: ['Les 5 commits brouillons nettoyés en 2 commits propres par rebase interactif', 'Un conflit de rebase provoqué et résolu sereinement', 'Description de PR type écrite (contexte / changements / vérification)'],
  },
  74: {
    theory: `On documente pour un LECTEUR précis, jamais « en général » : le recruteur (30 secondes, il veut quoi/démo/preuve), le collègue (comment lancer, comment contribuer), le futur toi (pourquoi cette décision). Chaque document a UN lecteur cible et répond à SES questions dans SON ordre.\n\nLa hiérarchie de la documentation : le README (la porte d'entrée), les docstrings/JSDoc (le contrat des fonctions publiques : paramètres, retour, erreurs possibles), les ADRs (le pourquoi des décisions), le diagramme (la vue d'ensemble qu'aucun texte ne remplace — mais UN diagramme simple et à jour vaut mieux que cinq diagrammes périmés).\n\nLe test de qualité : lire son propre document À VOIX HAUTE en suivant ses instructions à la lettre, sur un environnement propre. Chaque friction rencontrée est un bug de documentation.`,
    criteria: ['README refondu et TESTÉ (instructions suivies sur clone frais)', 'Docstrings sur les fonctions publiques clés (contrat complet : params, retour, erreurs)', 'Un diagramme d\'architecture simple et juste (Mermaid ou photo de papier)'],
  },
  75: {
    theory: `En poste, tu liras 5 à 10 fois plus de code que tu n'en écriras — et personne n'enseigne à lire. La stratégie : ne JAMAIS lire linéairement. (1) Trouver le point d'entrée (package.json → main/scripts, le fichier qui démarre tout). (2) Cartographier les grandes zones (les dossiers racontent l'architecture). (3) Choisir UN flux utilisateur et le suivre de bout en bout, en notant les fonctions traversées. (4) Formuler des hypothèses (« ce module doit faire X ») et les VÉRIFIER — lire avec une question, jamais en spectateur.\n\nLes outils du lecteur : la recherche globale (où cette fonction est-elle appelée ?), le go-to-definition, git log sur un fichier (son histoire raconte ses douleurs), les tests (la documentation d'usage la plus fiable).\n\nAccepter l'inconfort : on comprend un codebase à 30%, puis 50%, jamais 100% d'emblée. L'objectif est de savoir OÙ intervenir, pas de tout savoir.`,
    criteria: ['Point d\'entrée et architecture du repo inconnu identifiés et documentés', 'Un flux complet tracé (liste des fonctions traversées, dans l\'ordre)', 'Tes hypothèses écrites AVANT vérification, avec le verdict (justes/fausses)'],
  },
  76: {
    theory: `Deux mesures gouvernent la qualité d'un découpage en modules : le COUPLAGE (à quel point les modules dépendent les uns des autres — à minimiser) et la COHÉSION (à quel point le contenu d'un module va ensemble — à maximiser). Le symptôme du couplage fort : changer une chose oblige à toucher cinq fichiers.\n\nUn bon module CACHE son implémentation derrière une interface minimale : le reste du monde connaît le contrat (les fonctions exposées), jamais les détails (la structure interne, la lib utilisée). Ton interface Store (jour 44), ton module api.ts : même principe. Ce qui est caché peut changer librement ; ce qui est exposé est un engagement.\n\nLe graphe de dépendances rend le design VISIBLE : les flèches doivent aller dans un sens (vers le bas, vers le stable), jamais en cercle (A→B→A = les deux modules n'en sont qu'un, déguisé).`,
    criteria: ['Le graphe de dépendances avant/après est dessiné et le couplage a diminué (moins de flèches, pas de cycle)', 'Chaque module expose une interface minimale documentée', 'Le test : changer l\'implémentation d\'un module ne casse aucun autre (essayé sur un cas)'],
  },
  78: {
    theory: `Le 3-TIERS découpe toute application en trois couches : PRÉSENTATION (l'UI — afficher et saisir), LOGIQUE (les règles métier — décider), DONNÉES (persister). La règle : chaque couche ne parle qu'à sa voisine, et la logique ne sait ni COMMENT elle est affichée ni OÙ elle est stockée.\n\nMVC organise la présentation : le Modèle (les données et leur logique), la Vue (le rendu), le Contrôleur (reçoit les actions, orchestre). Tu l'as pratiqué sans le nommer : tes routes Express sont des contrôleurs, tes services le modèle.\n\nPourquoi ces découpages comptent : chaque couche évolue, se teste et se remplace INDÉPENDAMMENT. Changer l'UI ne touche pas les règles ; migrer la base ne touche pas l'UI. Toute architecture plus avancée (hexagonale, mois 10) est un raffinement de cette idée : isoler ce qui change pour des raisons différentes.`,
    criteria: ['Tes projets cartographiés : chaque fichier assigné à sa couche (et les violations repérées)', 'L\'architecture cible de LivreAPI dessinée avec les frontières justifiées', 'Réponse écrite : où va une nouvelle règle métier ? une nouvelle vue ? un changement de base ?'],
  },
  79: {
    theory: `L'observabilité répond à UNE question : « que s'est-il passé en production ? » — sans elle, tu es aveugle. Trois piliers : les LOGS (des événements horodatés — ce qui s'est passé), les MÉTRIQUES (des agrégats — combien, à quelle vitesse), les TRACES (le parcours d'UNE requête à travers le système).\n\nLes logs STRUCTURÉS (JSON : {timestamp, level, message, requestId, ...}) sont interrogeables mécaniquement, là où le texte libre exige des yeux humains. Les NIVEAUX (debug/info/warn/error) permettent de filtrer. Le CORRELATION ID — un identifiant unique généré à l'entrée de chaque requête et propagé partout — relie tous les logs d'une même requête : c'est lui qui permet de « rejouer » une session.\n\nCe qu'on ne logge JAMAIS : mots de passe, tokens, données personnelles sensibles. Un log est une base de données que tout le monde lit.`,
    criteria: ['Logs JSON structurés avec niveaux et correlation id sur chaque requête', 'Une session complète reconstituée à partir des seuls logs (preuve)', 'Relecture : aucun secret ni donnée sensible dans les logs'],
  },
  80: {
    theory: `Règle n°1 de la performance : MESURER avant d'optimiser. L'intuition se trompe presque toujours sur l'emplacement du goulot — profile d'abord (temps par endpoint, requêtes SQL exécutées), optimise ensuite, re-mesure pour prouver le gain.\n\nLe problème N+1 est le bug de performance le plus courant des APIs : afficher 50 emprunts en faisant 1 requête pour la liste + 50 requêtes pour les livres associés = 51 allers-retours. La correction : UN JOIN (ou une requête batch WHERE id IN (...)). Symptôme : le nombre de requêtes SQL croît avec la taille de la liste.\n\nLe CACHE échange de la fraîcheur contre de la vitesse : garder en mémoire un résultat coûteux et le resservir. Ses deux problèmes difficiles (célèbres) : l'INVALIDATION (quand la donnée sous-jacente change, le cache ment) et le nommage des clés. Ton LRU du jour 30 est exactement l'outil.`,
    criteria: ['Un vrai problème de perf identifié PAR MESURE (chiffres avant)', 'Le N+1 corrigé par JOIN/batch, gain mesuré (chiffres après)', 'Cache avec stratégie d\'invalidation écrite (pas juste "on cache")'],
  },
  81: {
    theory: `Le raisonnement par trade-offs est LA marque de l'ingénieur mûr : toute solution sacrifie quelque chose, et la question n'est jamais « quelle est la meilleure ? » mais « quelle est la mieux ADAPTÉE à ces contraintes ? ». Les axes récurrents : temps vs mémoire, simplicité vs flexibilité, vitesse de livraison vs robustesse, coût vs performance, cohérence vs disponibilité.\n\nLe format d'analyse : les options (2-3, réelles), les critères (issus du CONTEXTE : volume, équipe, budget, délai), le choix, et — le plus important — ce qu'on SACRIFIE et le signal qui ferait changer d'avis.\n\nLes anti-patterns sont des solutions séduisantes qui nuisent à terme : la sur-ingénierie (résoudre des problèmes qu'on n'a pas), le god object (une classe qui fait tout), la duplication forcée en abstraction (DRY appliqué à des ressemblances superficielles), l'optimisation prématurée. Les reconnaître CHEZ SOI est le niveau attendu.`,
    criteria: ['3 analyses de trade-off complètes sur TES décisions passées (options/critères/choix/sacrifice/signal de révision)', '3 anti-patterns identifiés dans ton propre code avec explication', 'Chaque analyse répond à « pourquoi X plutôt que Y » sans dogme'],
  },
  82: {
    theory: `Python sera ta langue du ML (mois 5-7) : quasi toute la stack data/ML (pandas, scikit-learn, PyTorch) est Python. Bonne nouvelle : tu sais déjà PROGRAMMER — c'est un changement de syntaxe, pas de paradigme.\n\nLes correspondances : l'objet JS → le dict, le tableau → la list, les backticks → les f-strings, map/filter → les COMPREHENSIONS (\`[x*2 for x in nombres if x > 0]\` — le style idiomatique), null → None. Les pièges qui mordent : l'indentation EST la syntaxe (pas d'accolades), \`==\` compare les valeurs et \`is\` l'identité, les arguments par défaut mutables sont un piège célèbre.\n\nLe VENV isole les dépendances de chaque projet (l'équivalent du node_modules local) : \`python -m venv .venv\` puis activation — systématique, sinon les projets se polluent mutuellement. requirements.txt = le package.json de Python.`,
    criteria: ['Les 3 exercices transposés en Python IDIOMATIQUE (comprehensions, dicts — pas du JS déguisé)', 'venv + requirements.txt en place et documentés', 'Ta liste de différences pièges JS→Python écrite (au moins 5)'],
  },
  83: {
    theory: `Un bilan trimestriel n'est pas une formalité : c'est le mécanisme qui transforme l'expérience en compétence CONSCIENTE. La méthode : relire ses notes anciennes (la distance révèle la progression invisible au quotidien), réévaluer chaque compétence avec le test dur (« puis-je le produire SEUL + l'expliquer ? »), nommer les 3 lacunes prioritaires et leur affecter un créneau concret.\n\nLe piège de l'auto-évaluation : la familiarité se déguise en maîtrise. Avoir VU la récursion 20 fois n'est pas savoir l'ÉCRIRE — seul le test de production compte. Sous-évaluer est aussi une erreur : compare-toi au toi du jour 1, pas à un senior.\n\nLe portfolio se vérifie MAINTENANT, pas au mois 12 : un recruteur peut tomber sur ton GitHub aujourd'hui. Chaque repo doit être propre en continu.`,
    criteria: ['Les 20 scores réévalués avec le test « produire seul + expliquer »', '3 lacunes nommées avec un créneau de rattrapage DATÉ', 'TaskFlow et LivreAPI vérifiés propres sur GitHub (README, description, topics)'],
  },
  85: {
    theory: `Le durcissement final applique tout le mois d'un coup — c'est un exercice de SYNTHÈSE : l'audit sécurité (jour 67), les index mesurés (jour 64), le cache raisonné (jour 80), les logs structurés (jour 79). La différence entre un projet de tutoriel et un projet crédible tient précisément là : le tutoriel s'arrête à « ça marche », le projet pro survit à l'inspection et à la charge.\n\nLe test de charge léger (quelques centaines de requêtes concurrentes avec un outil simple, ou même une boucle de curl) révèle ce que le clic manuel cache : les fuites de connexions, les latences qui explosent, les erreurs sous concurrence.\n\nL'état d'esprit : tu passes de « développeur qui fait marcher » à « ingénieur qui garantit ». C'est exactement le niveau attendu pour DocSense au mois 11.`,
    criteria: ['La checklist complète du mois appliquée (sécurité, index, cache, logs) avec preuves', 'Un test de charge léger exécuté et les résultats interprétés', 'LivreAPI prête à être montrée : tu la défendrais en entretien sans rougir'],
  },
  86: {
    theory: `Un projet non racontable n'existe pas pour un recruteur. Le format STAR structure le récit : SITUATION (le problème et pour qui), TÂCHE (ce qu'il fallait accomplir), ACTION (tes décisions — c'est là que les ADRs brillent : « j'ai choisi X plutôt que Y parce que... »), RÉSULTAT (chiffré et démontrable).\n\nL'erreur classique : raconter les FEATURES (« mon API fait du CRUD et de la recherche ») au lieu des DÉCISIONS (« j'ai dû arbitrer entre valider à la main ou avec une librairie — voici mon raisonnement »). Les features montrent ce que tu as tapé ; les décisions montrent comment tu PENSES. On recrute la pensée.\n\nLa difficulté surmontée est l'or du récit : un vrai obstacle (la transaction d'emprunt, le bug de référence) + ta démarche pour le résoudre = la preuve vivante de ta valeur. Prépare-la, répète-la à voix haute, chronomètre 5 minutes.`,
    criteria: ['La fiche STAR de LivreAPI écrite (avec 2 décisions et 1 difficulté réelle)', 'La démo orale de 5 min répétée et enregistrée au moins une fois', 'Réécoute critique faite avec la grille d\'entretien (note honnête)'],
  },
  87: {
    theory: `Le modèle mental de React tient en une équation : UI = f(state). Tu ne modifies JAMAIS l'interface directement (pas de « trouve cet élément et change son texte ») — tu décris à quoi l'UI DOIT ressembler pour chaque état possible, et React recalcule le rendu quand l'état change.\n\nLe state se modifie exclusivement via son setter (\`setCompteur(n + 1)\`), et de façon IMMUABLE : \`setListe([...liste, nouvelItem])\`, jamais \`liste.push(...)\`. Pourquoi ? React détecte les changements par comparaison de RÉFÉRENCES — une mutation garde la même référence, React ne voit rien, l'écran ne bouge pas. Ta discipline d'immutabilité du jour 26 n'était pas un dogme : c'était l'entraînement pour aujourd'hui.\n\nUn composant est une FONCTION qui reçoit des props et retourne du JSX : la composition de fonctions (jour 22), appliquée à l'interface.`,
    criteria: ['Compteur et liste fonctionnels, typés, zéro warning console', 'Aucune mutation de state (relecture systématique des setters : spread partout)', 'Tu sais expliquer pourquoi une mutation ne re-rend pas (comparaison de références)'],
  },
  88: {
    theory: `useEffect synchronise ton composant avec l'EXTÉRIEUR (un fetch, un abonnement) : il s'exécute APRÈS le rendu, et son tableau de dépendances dit quand le rejouer. Le contrat des données async ne change pas : chaque fetch a TROIS états (loading, error, data) et l'UI doit décrire les trois — l'oublier donne les pages blanches et les spinners éternels.\n\nEn StrictMode (dev), React monte chaque composant DEUX fois exprès : si ton effet casse au double appel, c'est qu'il lui manque un cleanup ou qu'il n'est pas idempotent — c'est une feature de détection, pas un bug.\n\nLe module api.ts unique (l'Adapter du jour 39) centralise les appels : types partagés avec le backend, gestion d'erreur commune, un seul endroit à modifier si l'API change. Un fetch dispersé dans chaque composant est le code smell frontend n°1.`,
    criteria: ['Liste des livres avec les 3 états async explicitement rendus (loading, error, data)', 'Tous les appels passent par api.ts typé (zéro fetch hors module)', 'Le formulaire d\'ajout fonctionne et l\'erreur réseau est AFFICHÉE à l\'utilisateur (testé serveur éteint)'],
  },
  89: {
    theory: `Le full-stack assemblé est le moment de vérité : peux-tu suivre UNE donnée sur tout le trajet ? Clic « ajouter » → événement React → validation → fetch POST → route Express → service (règle métier) → requête SQL paramétrée → réponse 201 → mise à jour immuable du state → re-render. Si un maillon reste flou, c'est LÀ que tu dois creuser — la magie est interdite.\n\nLes états EDGE font la différence : la liste vide (un message utile, pas une page blanche), l'erreur réseau (un message + possibilité de réessayer), le chargement (un indicateur). Un utilisateur passe 5% du temps dans ces états, mais c'est là que se joue la perception de qualité.\n\nCe mini full-stack est l'échauffement du projet 3 (BiblioApp, semaine 17) qui l'amènera au niveau portfolio : tests, GIF de démo, schéma d'architecture.`,
    criteria: ['CRUD complet fonctionnel de bout en bout en local', 'Les 3 états edge soignés sur chaque vue (vide, erreur, chargement) — testés en coupant le serveur', 'Le trajet complet d\'une donnée expliqué à voix haute sans hésitation (enregistre-toi)'],
  },
  90: {
    theory: `Quatre-vingt-dix jours : tu es passé de « quelques notions de JavaScript » à développeur full-stack junior — terminal, Git, algorithmique, structures de données, TypeScript, API REST sécurisée et testée, SQL, et un front React branché sur TON backend. C'est déjà un profil employable sur des rôles full-stack juniors.\n\nLa suite change de registre : Python et la data (mois 4-5), le ML honnête (mois 6), puis l'IA appliquée (mois 7-12) qui te différenciera. Tout ce qui vient s'appuie sur ces fondations — le RAG du mois 8 est « de l'ingestion de données + une API + de l'évaluation », trois choses que tu sais déjà faire à 70%.\n\nLe rituel du bilan : relire le jour 1 (mesurer le chemin), réévaluer sans complaisance, nommer les peurs restantes (elles désignent les priorités), et écrire à ton toi du jour 180.`,
    criteria: ['Bilan écrit avec relecture du jour 1 et lettre au toi du jour 180', 'Les 20 scores réévalués + 3 lacunes avec plan daté', 'Les 2 projets GitHub inspectés une dernière fois (un recruteur peut regarder aujourd\'hui)'],
  },
};
